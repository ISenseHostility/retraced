/**
 * @name Retraced
 * @author ISenseHostility
 * @description Local-only statistics about your own Discord activity. Everything stays on this device — no network calls, ever.
 * @version 1.0.0
 * @website https://retraced.cc
 */
"use strict";
var __RetracedExports = (() => {
  var __create = Object.create;
  var __defProp = Object.defineProperty;
  var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
  var __getOwnPropNames = Object.getOwnPropertyNames;
  var __getProtoOf = Object.getPrototypeOf;
  var __hasOwnProp = Object.prototype.hasOwnProperty;
  var __commonJS = (cb, mod) => function __require() {
    return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
  };
  var __export = (target, all) => {
    for (var name in all)
      __defProp(target, name, { get: all[name], enumerable: true });
  };
  var __copyProps = (to2, from2, except, desc) => {
    if (from2 && typeof from2 === "object" || typeof from2 === "function") {
      for (let key of __getOwnPropNames(from2))
        if (!__hasOwnProp.call(to2, key) && key !== except)
          __defProp(to2, key, { get: () => from2[key], enumerable: !(desc = __getOwnPropDesc(from2, key)) || desc.enumerable });
    }
    return to2;
  };
  var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
    // If the importer is in node compatibility mode or this is not an ESM
    // file that has been converted to a CommonJS file using a Babel-
    // compatible transform (i.e. "__esModule" has not been set), then set
    // "default" to the CommonJS "module.exports" for node compatibility.
    isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
    mod
  ));
  var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

  // src/shims/react.cjs
  var require_react = __commonJS({
    "src/shims/react.cjs"(exports, module) {
      "use strict";
      module.exports = globalThis.BdApi.React;
    }
  });

  // src/shims/jsx-runtime.cjs
  var require_jsx_runtime = __commonJS({
    "src/shims/jsx-runtime.cjs"(exports, module) {
      "use strict";
      function react() {
        return globalThis.BdApi.React;
      }
      function jsx37(type, props, key) {
        const R = react();
        if (key === void 0) return R.createElement(type, props);
        return R.createElement(type, { ...props ?? {}, key });
      }
      module.exports = {
        get Fragment() {
          return react().Fragment;
        },
        jsx: jsx37,
        jsxs: jsx37,
        // Dev runtime passes extra (isStaticChildren, source, self) args — safely ignored.
        jsxDEV: jsx37
      };
    }
  });

  // src/shims/react-dom.cjs
  var require_react_dom = __commonJS({
    "src/shims/react-dom.cjs"(exports, module) {
      "use strict";
      module.exports = globalThis.BdApi.ReactDOM;
    }
  });

  // node_modules/use-sync-external-store/cjs/use-sync-external-store-shim.development.js
  var require_use_sync_external_store_shim_development = __commonJS({
    "node_modules/use-sync-external-store/cjs/use-sync-external-store-shim.development.js"(exports) {
      "use strict";
      (function() {
        function is3(x5, y5) {
          return x5 === y5 && (0 !== x5 || 1 / x5 === 1 / y5) || x5 !== x5 && y5 !== y5;
        }
        function useSyncExternalStore$2(subscribe, getSnapshot) {
          didWarnOld18Alpha || void 0 === React52.startTransition || (didWarnOld18Alpha = true, console.error(
            "You are using an outdated, pre-release alpha of React 18 that does not support useSyncExternalStore. The use-sync-external-store shim will not work correctly. Upgrade to a newer pre-release."
          ));
          var value2 = getSnapshot();
          if (!didWarnUncachedGetSnapshot) {
            var cachedValue = getSnapshot();
            objectIs(value2, cachedValue) || (console.error(
              "The result of getSnapshot should be cached to avoid an infinite loop"
            ), didWarnUncachedGetSnapshot = true);
          }
          cachedValue = useState29({
            inst: { value: value2, getSnapshot }
          });
          var inst = cachedValue[0].inst, forceUpdate = cachedValue[1];
          useLayoutEffect9(
            function() {
              inst.value = value2;
              inst.getSnapshot = getSnapshot;
              checkIfSnapshotChanged(inst) && forceUpdate({ inst });
            },
            [subscribe, value2, getSnapshot]
          );
          useEffect28(
            function() {
              checkIfSnapshotChanged(inst) && forceUpdate({ inst });
              return subscribe(function() {
                checkIfSnapshotChanged(inst) && forceUpdate({ inst });
              });
            },
            [subscribe]
          );
          useDebugValue2(value2);
          return value2;
        }
        function checkIfSnapshotChanged(inst) {
          var latestGetSnapshot = inst.getSnapshot;
          inst = inst.value;
          try {
            var nextValue = latestGetSnapshot();
            return !objectIs(inst, nextValue);
          } catch (error) {
            return true;
          }
        }
        function useSyncExternalStore$1(subscribe, getSnapshot) {
          return getSnapshot();
        }
        "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(Error());
        var React52 = require_react(), objectIs = "function" === typeof Object.is ? Object.is : is3, useState29 = React52.useState, useEffect28 = React52.useEffect, useLayoutEffect9 = React52.useLayoutEffect, useDebugValue2 = React52.useDebugValue, didWarnOld18Alpha = false, didWarnUncachedGetSnapshot = false, shim = "undefined" === typeof window || "undefined" === typeof window.document || "undefined" === typeof window.document.createElement ? useSyncExternalStore$1 : useSyncExternalStore$2;
        exports.useSyncExternalStore = void 0 !== React52.useSyncExternalStore ? React52.useSyncExternalStore : shim;
        "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(Error());
      })();
    }
  });

  // node_modules/use-sync-external-store/shim/index.js
  var require_shim = __commonJS({
    "node_modules/use-sync-external-store/shim/index.js"(exports, module) {
      "use strict";
      if (false) {
        module.exports = null;
      } else {
        module.exports = require_use_sync_external_store_shim_development();
      }
    }
  });

  // node_modules/use-sync-external-store/cjs/use-sync-external-store-shim/with-selector.development.js
  var require_with_selector_development = __commonJS({
    "node_modules/use-sync-external-store/cjs/use-sync-external-store-shim/with-selector.development.js"(exports) {
      "use strict";
      (function() {
        function is3(x5, y5) {
          return x5 === y5 && (0 !== x5 || 1 / x5 === 1 / y5) || x5 !== x5 && y5 !== y5;
        }
        "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(Error());
        var React52 = require_react(), shim = require_shim(), objectIs = "function" === typeof Object.is ? Object.is : is3, useSyncExternalStore5 = shim.useSyncExternalStore, useRef25 = React52.useRef, useEffect28 = React52.useEffect, useMemo26 = React52.useMemo, useDebugValue2 = React52.useDebugValue;
        exports.useSyncExternalStoreWithSelector = function(subscribe, getSnapshot, getServerSnapshot, selector, isEqual) {
          var instRef = useRef25(null);
          if (null === instRef.current) {
            var inst = { hasValue: false, value: null };
            instRef.current = inst;
          } else inst = instRef.current;
          instRef = useMemo26(
            function() {
              function memoizedSelector(nextSnapshot) {
                if (!hasMemo) {
                  hasMemo = true;
                  memoizedSnapshot = nextSnapshot;
                  nextSnapshot = selector(nextSnapshot);
                  if (void 0 !== isEqual && inst.hasValue) {
                    var currentSelection = inst.value;
                    if (isEqual(currentSelection, nextSnapshot))
                      return memoizedSelection = currentSelection;
                  }
                  return memoizedSelection = nextSnapshot;
                }
                currentSelection = memoizedSelection;
                if (objectIs(memoizedSnapshot, nextSnapshot))
                  return currentSelection;
                var nextSelection = selector(nextSnapshot);
                if (void 0 !== isEqual && isEqual(currentSelection, nextSelection))
                  return memoizedSnapshot = nextSnapshot, currentSelection;
                memoizedSnapshot = nextSnapshot;
                return memoizedSelection = nextSelection;
              }
              var hasMemo = false, memoizedSnapshot, memoizedSelection, maybeGetServerSnapshot = void 0 === getServerSnapshot ? null : getServerSnapshot;
              return [
                function() {
                  return memoizedSelector(getSnapshot());
                },
                null === maybeGetServerSnapshot ? void 0 : function() {
                  return memoizedSelector(maybeGetServerSnapshot());
                }
              ];
            },
            [getSnapshot, getServerSnapshot, selector, isEqual]
          );
          var value2 = useSyncExternalStore5(subscribe, instRef[0], instRef[1]);
          useEffect28(
            function() {
              inst.hasValue = true;
              inst.value = value2;
            },
            [value2]
          );
          useDebugValue2(value2);
          return value2;
        };
        "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(Error());
      })();
    }
  });

  // node_modules/use-sync-external-store/shim/with-selector.js
  var require_with_selector = __commonJS({
    "node_modules/use-sync-external-store/shim/with-selector.js"(exports, module) {
      "use strict";
      if (false) {
        module.exports = null;
      } else {
        module.exports = require_with_selector_development();
      }
    }
  });

  // node_modules/use-sync-external-store/cjs/use-sync-external-store-with-selector.development.js
  var require_use_sync_external_store_with_selector_development = __commonJS({
    "node_modules/use-sync-external-store/cjs/use-sync-external-store-with-selector.development.js"(exports) {
      "use strict";
      (function() {
        function is3(x5, y5) {
          return x5 === y5 && (0 !== x5 || 1 / x5 === 1 / y5) || x5 !== x5 && y5 !== y5;
        }
        "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(Error());
        var React52 = require_react(), objectIs = "function" === typeof Object.is ? Object.is : is3, useSyncExternalStore5 = React52.useSyncExternalStore, useRef25 = React52.useRef, useEffect28 = React52.useEffect, useMemo26 = React52.useMemo, useDebugValue2 = React52.useDebugValue;
        exports.useSyncExternalStoreWithSelector = function(subscribe, getSnapshot, getServerSnapshot, selector, isEqual) {
          var instRef = useRef25(null);
          if (null === instRef.current) {
            var inst = { hasValue: false, value: null };
            instRef.current = inst;
          } else inst = instRef.current;
          instRef = useMemo26(
            function() {
              function memoizedSelector(nextSnapshot) {
                if (!hasMemo) {
                  hasMemo = true;
                  memoizedSnapshot = nextSnapshot;
                  nextSnapshot = selector(nextSnapshot);
                  if (void 0 !== isEqual && inst.hasValue) {
                    var currentSelection = inst.value;
                    if (isEqual(currentSelection, nextSnapshot))
                      return memoizedSelection = currentSelection;
                  }
                  return memoizedSelection = nextSnapshot;
                }
                currentSelection = memoizedSelection;
                if (objectIs(memoizedSnapshot, nextSnapshot))
                  return currentSelection;
                var nextSelection = selector(nextSnapshot);
                if (void 0 !== isEqual && isEqual(currentSelection, nextSelection))
                  return memoizedSnapshot = nextSnapshot, currentSelection;
                memoizedSnapshot = nextSnapshot;
                return memoizedSelection = nextSelection;
              }
              var hasMemo = false, memoizedSnapshot, memoizedSelection, maybeGetServerSnapshot = void 0 === getServerSnapshot ? null : getServerSnapshot;
              return [
                function() {
                  return memoizedSelector(getSnapshot());
                },
                null === maybeGetServerSnapshot ? void 0 : function() {
                  return memoizedSelector(maybeGetServerSnapshot());
                }
              ];
            },
            [getSnapshot, getServerSnapshot, selector, isEqual]
          );
          var value2 = useSyncExternalStore5(subscribe, instRef[0], instRef[1]);
          useEffect28(
            function() {
              inst.hasValue = true;
              inst.value = value2;
            },
            [value2]
          );
          useDebugValue2(value2);
          return value2;
        };
        "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(Error());
      })();
    }
  });

  // node_modules/use-sync-external-store/with-selector.js
  var require_with_selector2 = __commonJS({
    "node_modules/use-sync-external-store/with-selector.js"(exports, module) {
      "use strict";
      if (false) {
        module.exports = null;
      } else {
        module.exports = require_use_sync_external_store_with_selector_development();
      }
    }
  });

  // node_modules/decimal.js-light/decimal.js
  var require_decimal = __commonJS({
    "node_modules/decimal.js-light/decimal.js"(exports, module) {
      (function(globalScope) {
        "use strict";
        var MAX_DIGITS = 1e9, Decimal3 = {
          // These values must be integers within the stated ranges (inclusive).
          // Most of these values can be changed during run-time using `Decimal.config`.
          // The maximum number of significant digits of the result of a calculation or base conversion.
          // E.g. `Decimal.config({ precision: 20 });`
          precision: 20,
          // 1 to MAX_DIGITS
          // The rounding mode used by default by `toInteger`, `toDecimalPlaces`, `toExponential`,
          // `toFixed`, `toPrecision` and `toSignificantDigits`.
          //
          // ROUND_UP         0 Away from zero.
          // ROUND_DOWN       1 Towards zero.
          // ROUND_CEIL       2 Towards +Infinity.
          // ROUND_FLOOR      3 Towards -Infinity.
          // ROUND_HALF_UP    4 Towards nearest neighbour. If equidistant, up.
          // ROUND_HALF_DOWN  5 Towards nearest neighbour. If equidistant, down.
          // ROUND_HALF_EVEN  6 Towards nearest neighbour. If equidistant, towards even neighbour.
          // ROUND_HALF_CEIL  7 Towards nearest neighbour. If equidistant, towards +Infinity.
          // ROUND_HALF_FLOOR 8 Towards nearest neighbour. If equidistant, towards -Infinity.
          //
          // E.g.
          // `Decimal.rounding = 4;`
          // `Decimal.rounding = Decimal.ROUND_HALF_UP;`
          rounding: 4,
          // 0 to 8
          // The exponent value at and beneath which `toString` returns exponential notation.
          // JavaScript numbers: -7
          toExpNeg: -7,
          // 0 to -MAX_E
          // The exponent value at and above which `toString` returns exponential notation.
          // JavaScript numbers: 21
          toExpPos: 21,
          // 0 to MAX_E
          // The natural logarithm of 10.
          // 115 digits
          LN10: "2.302585092994045684017991454684364207601101488628772976033327900967572609677352480235997205089598298341967784042286"
        }, external = true, decimalError = "[DecimalError] ", invalidArgument = decimalError + "Invalid argument: ", exponentOutOfRange = decimalError + "Exponent out of range: ", mathfloor = Math.floor, mathpow = Math.pow, isDecimal = /^(\d+(\.\d*)?|\.\d+)(e[+-]?\d+)?$/i, ONE, BASE = 1e7, LOG_BASE = 7, MAX_SAFE_INTEGER = 9007199254740991, MAX_E = mathfloor(MAX_SAFE_INTEGER / LOG_BASE), P = {};
        P.absoluteValue = P.abs = function() {
          var x5 = new this.constructor(this);
          if (x5.s) x5.s = 1;
          return x5;
        };
        P.comparedTo = P.cmp = function(y5) {
          var i, j, xdL, ydL, x5 = this;
          y5 = new x5.constructor(y5);
          if (x5.s !== y5.s) return x5.s || -y5.s;
          if (x5.e !== y5.e) return x5.e > y5.e ^ x5.s < 0 ? 1 : -1;
          xdL = x5.d.length;
          ydL = y5.d.length;
          for (i = 0, j = xdL < ydL ? xdL : ydL; i < j; ++i) {
            if (x5.d[i] !== y5.d[i]) return x5.d[i] > y5.d[i] ^ x5.s < 0 ? 1 : -1;
          }
          return xdL === ydL ? 0 : xdL > ydL ^ x5.s < 0 ? 1 : -1;
        };
        P.decimalPlaces = P.dp = function() {
          var x5 = this, w = x5.d.length - 1, dp = (w - x5.e) * LOG_BASE;
          w = x5.d[w];
          if (w) for (; w % 10 == 0; w /= 10) dp--;
          return dp < 0 ? 0 : dp;
        };
        P.dividedBy = P.div = function(y5) {
          return divide(this, new this.constructor(y5));
        };
        P.dividedToIntegerBy = P.idiv = function(y5) {
          var x5 = this, Ctor = x5.constructor;
          return round2(divide(x5, new Ctor(y5), 0, 1), Ctor.precision);
        };
        P.equals = P.eq = function(y5) {
          return !this.cmp(y5);
        };
        P.exponent = function() {
          return getBase10Exponent(this);
        };
        P.greaterThan = P.gt = function(y5) {
          return this.cmp(y5) > 0;
        };
        P.greaterThanOrEqualTo = P.gte = function(y5) {
          return this.cmp(y5) >= 0;
        };
        P.isInteger = P.isint = function() {
          return this.e > this.d.length - 2;
        };
        P.isNegative = P.isneg = function() {
          return this.s < 0;
        };
        P.isPositive = P.ispos = function() {
          return this.s > 0;
        };
        P.isZero = function() {
          return this.s === 0;
        };
        P.lessThan = P.lt = function(y5) {
          return this.cmp(y5) < 0;
        };
        P.lessThanOrEqualTo = P.lte = function(y5) {
          return this.cmp(y5) < 1;
        };
        P.logarithm = P.log = function(base) {
          var r2, x5 = this, Ctor = x5.constructor, pr = Ctor.precision, wpr = pr + 5;
          if (base === void 0) {
            base = new Ctor(10);
          } else {
            base = new Ctor(base);
            if (base.s < 1 || base.eq(ONE)) throw Error(decimalError + "NaN");
          }
          if (x5.s < 1) throw Error(decimalError + (x5.s ? "NaN" : "-Infinity"));
          if (x5.eq(ONE)) return new Ctor(0);
          external = false;
          r2 = divide(ln(x5, wpr), ln(base, wpr), wpr);
          external = true;
          return round2(r2, pr);
        };
        P.minus = P.sub = function(y5) {
          var x5 = this;
          y5 = new x5.constructor(y5);
          return x5.s == y5.s ? subtract(x5, y5) : add2(x5, (y5.s = -y5.s, y5));
        };
        P.modulo = P.mod = function(y5) {
          var q, x5 = this, Ctor = x5.constructor, pr = Ctor.precision;
          y5 = new Ctor(y5);
          if (!y5.s) throw Error(decimalError + "NaN");
          if (!x5.s) return round2(new Ctor(x5), pr);
          external = false;
          q = divide(x5, y5, 0, 1).times(y5);
          external = true;
          return x5.minus(q);
        };
        P.naturalExponential = P.exp = function() {
          return exp(this);
        };
        P.naturalLogarithm = P.ln = function() {
          return ln(this);
        };
        P.negated = P.neg = function() {
          var x5 = new this.constructor(this);
          x5.s = -x5.s || 0;
          return x5;
        };
        P.plus = P.add = function(y5) {
          var x5 = this;
          y5 = new x5.constructor(y5);
          return x5.s == y5.s ? add2(x5, y5) : subtract(x5, (y5.s = -y5.s, y5));
        };
        P.precision = P.sd = function(z) {
          var e, sd, w, x5 = this;
          if (z !== void 0 && z !== !!z && z !== 1 && z !== 0) throw Error(invalidArgument + z);
          e = getBase10Exponent(x5) + 1;
          w = x5.d.length - 1;
          sd = w * LOG_BASE + 1;
          w = x5.d[w];
          if (w) {
            for (; w % 10 == 0; w /= 10) sd--;
            for (w = x5.d[0]; w >= 10; w /= 10) sd++;
          }
          return z && e > sd ? e : sd;
        };
        P.squareRoot = P.sqrt = function() {
          var e, n, pr, r2, s, t, wpr, x5 = this, Ctor = x5.constructor;
          if (x5.s < 1) {
            if (!x5.s) return new Ctor(0);
            throw Error(decimalError + "NaN");
          }
          e = getBase10Exponent(x5);
          external = false;
          s = Math.sqrt(+x5);
          if (s == 0 || s == 1 / 0) {
            n = digitsToString(x5.d);
            if ((n.length + e) % 2 == 0) n += "0";
            s = Math.sqrt(n);
            e = mathfloor((e + 1) / 2) - (e < 0 || e % 2);
            if (s == 1 / 0) {
              n = "5e" + e;
            } else {
              n = s.toExponential();
              n = n.slice(0, n.indexOf("e") + 1) + e;
            }
            r2 = new Ctor(n);
          } else {
            r2 = new Ctor(s.toString());
          }
          pr = Ctor.precision;
          s = wpr = pr + 3;
          for (; ; ) {
            t = r2;
            r2 = t.plus(divide(x5, t, wpr + 2)).times(0.5);
            if (digitsToString(t.d).slice(0, wpr) === (n = digitsToString(r2.d)).slice(0, wpr)) {
              n = n.slice(wpr - 3, wpr + 1);
              if (s == wpr && n == "4999") {
                round2(t, pr + 1, 0);
                if (t.times(t).eq(x5)) {
                  r2 = t;
                  break;
                }
              } else if (n != "9999") {
                break;
              }
              wpr += 4;
            }
          }
          external = true;
          return round2(r2, pr);
        };
        P.times = P.mul = function(y5) {
          var carry, e, i, k, r2, rL, t, xdL, ydL, x5 = this, Ctor = x5.constructor, xd = x5.d, yd = (y5 = new Ctor(y5)).d;
          if (!x5.s || !y5.s) return new Ctor(0);
          y5.s *= x5.s;
          e = x5.e + y5.e;
          xdL = xd.length;
          ydL = yd.length;
          if (xdL < ydL) {
            r2 = xd;
            xd = yd;
            yd = r2;
            rL = xdL;
            xdL = ydL;
            ydL = rL;
          }
          r2 = [];
          rL = xdL + ydL;
          for (i = rL; i--; ) r2.push(0);
          for (i = ydL; --i >= 0; ) {
            carry = 0;
            for (k = xdL + i; k > i; ) {
              t = r2[k] + yd[i] * xd[k - i - 1] + carry;
              r2[k--] = t % BASE | 0;
              carry = t / BASE | 0;
            }
            r2[k] = (r2[k] + carry) % BASE | 0;
          }
          for (; !r2[--rL]; ) r2.pop();
          if (carry) ++e;
          else r2.shift();
          y5.d = r2;
          y5.e = e;
          return external ? round2(y5, Ctor.precision) : y5;
        };
        P.toDecimalPlaces = P.todp = function(dp, rm) {
          var x5 = this, Ctor = x5.constructor;
          x5 = new Ctor(x5);
          if (dp === void 0) return x5;
          checkInt32(dp, 0, MAX_DIGITS);
          if (rm === void 0) rm = Ctor.rounding;
          else checkInt32(rm, 0, 8);
          return round2(x5, dp + getBase10Exponent(x5) + 1, rm);
        };
        P.toExponential = function(dp, rm) {
          var str, x5 = this, Ctor = x5.constructor;
          if (dp === void 0) {
            str = toString2(x5, true);
          } else {
            checkInt32(dp, 0, MAX_DIGITS);
            if (rm === void 0) rm = Ctor.rounding;
            else checkInt32(rm, 0, 8);
            x5 = round2(new Ctor(x5), dp + 1, rm);
            str = toString2(x5, true, dp + 1);
          }
          return str;
        };
        P.toFixed = function(dp, rm) {
          var str, y5, x5 = this, Ctor = x5.constructor;
          if (dp === void 0) return toString2(x5);
          checkInt32(dp, 0, MAX_DIGITS);
          if (rm === void 0) rm = Ctor.rounding;
          else checkInt32(rm, 0, 8);
          y5 = round2(new Ctor(x5), dp + getBase10Exponent(x5) + 1, rm);
          str = toString2(y5.abs(), false, dp + getBase10Exponent(y5) + 1);
          return x5.isneg() && !x5.isZero() ? "-" + str : str;
        };
        P.toInteger = P.toint = function() {
          var x5 = this, Ctor = x5.constructor;
          return round2(new Ctor(x5), getBase10Exponent(x5) + 1, Ctor.rounding);
        };
        P.toNumber = function() {
          return +this;
        };
        P.toPower = P.pow = function(y5) {
          var e, k, pr, r2, sign2, yIsInt, x5 = this, Ctor = x5.constructor, guard = 12, yn = +(y5 = new Ctor(y5));
          if (!y5.s) return new Ctor(ONE);
          x5 = new Ctor(x5);
          if (!x5.s) {
            if (y5.s < 1) throw Error(decimalError + "Infinity");
            return x5;
          }
          if (x5.eq(ONE)) return x5;
          pr = Ctor.precision;
          if (y5.eq(ONE)) return round2(x5, pr);
          e = y5.e;
          k = y5.d.length - 1;
          yIsInt = e >= k;
          sign2 = x5.s;
          if (!yIsInt) {
            if (sign2 < 0) throw Error(decimalError + "NaN");
          } else if ((k = yn < 0 ? -yn : yn) <= MAX_SAFE_INTEGER) {
            r2 = new Ctor(ONE);
            e = Math.ceil(pr / LOG_BASE + 4);
            external = false;
            for (; ; ) {
              if (k % 2) {
                r2 = r2.times(x5);
                truncate(r2.d, e);
              }
              k = mathfloor(k / 2);
              if (k === 0) break;
              x5 = x5.times(x5);
              truncate(x5.d, e);
            }
            external = true;
            return y5.s < 0 ? new Ctor(ONE).div(r2) : round2(r2, pr);
          }
          sign2 = sign2 < 0 && y5.d[Math.max(e, k)] & 1 ? -1 : 1;
          x5.s = 1;
          external = false;
          r2 = y5.times(ln(x5, pr + guard));
          external = true;
          r2 = exp(r2);
          r2.s = sign2;
          return r2;
        };
        P.toPrecision = function(sd, rm) {
          var e, str, x5 = this, Ctor = x5.constructor;
          if (sd === void 0) {
            e = getBase10Exponent(x5);
            str = toString2(x5, e <= Ctor.toExpNeg || e >= Ctor.toExpPos);
          } else {
            checkInt32(sd, 1, MAX_DIGITS);
            if (rm === void 0) rm = Ctor.rounding;
            else checkInt32(rm, 0, 8);
            x5 = round2(new Ctor(x5), sd, rm);
            e = getBase10Exponent(x5);
            str = toString2(x5, sd <= e || e <= Ctor.toExpNeg, sd);
          }
          return str;
        };
        P.toSignificantDigits = P.tosd = function(sd, rm) {
          var x5 = this, Ctor = x5.constructor;
          if (sd === void 0) {
            sd = Ctor.precision;
            rm = Ctor.rounding;
          } else {
            checkInt32(sd, 1, MAX_DIGITS);
            if (rm === void 0) rm = Ctor.rounding;
            else checkInt32(rm, 0, 8);
          }
          return round2(new Ctor(x5), sd, rm);
        };
        P.toString = P.valueOf = P.val = P.toJSON = function() {
          var x5 = this, e = getBase10Exponent(x5), Ctor = x5.constructor;
          return toString2(x5, e <= Ctor.toExpNeg || e >= Ctor.toExpPos);
        };
        function add2(x5, y5) {
          var carry, d, e, i, k, len, xd, yd, Ctor = x5.constructor, pr = Ctor.precision;
          if (!x5.s || !y5.s) {
            if (!y5.s) y5 = new Ctor(x5);
            return external ? round2(y5, pr) : y5;
          }
          xd = x5.d;
          yd = y5.d;
          k = x5.e;
          e = y5.e;
          xd = xd.slice();
          i = k - e;
          if (i) {
            if (i < 0) {
              d = xd;
              i = -i;
              len = yd.length;
            } else {
              d = yd;
              e = k;
              len = xd.length;
            }
            k = Math.ceil(pr / LOG_BASE);
            len = k > len ? k + 1 : len + 1;
            if (i > len) {
              i = len;
              d.length = 1;
            }
            d.reverse();
            for (; i--; ) d.push(0);
            d.reverse();
          }
          len = xd.length;
          i = yd.length;
          if (len - i < 0) {
            i = len;
            d = yd;
            yd = xd;
            xd = d;
          }
          for (carry = 0; i; ) {
            carry = (xd[--i] = xd[i] + yd[i] + carry) / BASE | 0;
            xd[i] %= BASE;
          }
          if (carry) {
            xd.unshift(carry);
            ++e;
          }
          for (len = xd.length; xd[--len] == 0; ) xd.pop();
          y5.d = xd;
          y5.e = e;
          return external ? round2(y5, pr) : y5;
        }
        function checkInt32(i, min3, max4) {
          if (i !== ~~i || i < min3 || i > max4) {
            throw Error(invalidArgument + i);
          }
        }
        function digitsToString(d) {
          var i, k, ws, indexOfLastWord = d.length - 1, str = "", w = d[0];
          if (indexOfLastWord > 0) {
            str += w;
            for (i = 1; i < indexOfLastWord; i++) {
              ws = d[i] + "";
              k = LOG_BASE - ws.length;
              if (k) str += getZeroString(k);
              str += ws;
            }
            w = d[i];
            ws = w + "";
            k = LOG_BASE - ws.length;
            if (k) str += getZeroString(k);
          } else if (w === 0) {
            return "0";
          }
          for (; w % 10 === 0; ) w /= 10;
          return str + w;
        }
        var divide = /* @__PURE__ */ (function() {
          function multiplyInteger(x5, k) {
            var temp, carry = 0, i = x5.length;
            for (x5 = x5.slice(); i--; ) {
              temp = x5[i] * k + carry;
              x5[i] = temp % BASE | 0;
              carry = temp / BASE | 0;
            }
            if (carry) x5.unshift(carry);
            return x5;
          }
          function compare(a2, b, aL, bL) {
            var i, r2;
            if (aL != bL) {
              r2 = aL > bL ? 1 : -1;
            } else {
              for (i = r2 = 0; i < aL; i++) {
                if (a2[i] != b[i]) {
                  r2 = a2[i] > b[i] ? 1 : -1;
                  break;
                }
              }
            }
            return r2;
          }
          function subtract2(a2, b, aL) {
            var i = 0;
            for (; aL--; ) {
              a2[aL] -= i;
              i = a2[aL] < b[aL] ? 1 : 0;
              a2[aL] = i * BASE + a2[aL] - b[aL];
            }
            for (; !a2[0] && a2.length > 1; ) a2.shift();
          }
          return function(x5, y5, pr, dp) {
            var cmp, e, i, k, prod, prodL, q, qd, rem, remL, rem0, sd, t, xi, xL, yd0, yL, yz, Ctor = x5.constructor, sign2 = x5.s == y5.s ? 1 : -1, xd = x5.d, yd = y5.d;
            if (!x5.s) return new Ctor(x5);
            if (!y5.s) throw Error(decimalError + "Division by zero");
            e = x5.e - y5.e;
            yL = yd.length;
            xL = xd.length;
            q = new Ctor(sign2);
            qd = q.d = [];
            for (i = 0; yd[i] == (xd[i] || 0); ) ++i;
            if (yd[i] > (xd[i] || 0)) --e;
            if (pr == null) {
              sd = pr = Ctor.precision;
            } else if (dp) {
              sd = pr + (getBase10Exponent(x5) - getBase10Exponent(y5)) + 1;
            } else {
              sd = pr;
            }
            if (sd < 0) return new Ctor(0);
            sd = sd / LOG_BASE + 2 | 0;
            i = 0;
            if (yL == 1) {
              k = 0;
              yd = yd[0];
              sd++;
              for (; (i < xL || k) && sd--; i++) {
                t = k * BASE + (xd[i] || 0);
                qd[i] = t / yd | 0;
                k = t % yd | 0;
              }
            } else {
              k = BASE / (yd[0] + 1) | 0;
              if (k > 1) {
                yd = multiplyInteger(yd, k);
                xd = multiplyInteger(xd, k);
                yL = yd.length;
                xL = xd.length;
              }
              xi = yL;
              rem = xd.slice(0, yL);
              remL = rem.length;
              for (; remL < yL; ) rem[remL++] = 0;
              yz = yd.slice();
              yz.unshift(0);
              yd0 = yd[0];
              if (yd[1] >= BASE / 2) ++yd0;
              do {
                k = 0;
                cmp = compare(yd, rem, yL, remL);
                if (cmp < 0) {
                  rem0 = rem[0];
                  if (yL != remL) rem0 = rem0 * BASE + (rem[1] || 0);
                  k = rem0 / yd0 | 0;
                  if (k > 1) {
                    if (k >= BASE) k = BASE - 1;
                    prod = multiplyInteger(yd, k);
                    prodL = prod.length;
                    remL = rem.length;
                    cmp = compare(prod, rem, prodL, remL);
                    if (cmp == 1) {
                      k--;
                      subtract2(prod, yL < prodL ? yz : yd, prodL);
                    }
                  } else {
                    if (k == 0) cmp = k = 1;
                    prod = yd.slice();
                  }
                  prodL = prod.length;
                  if (prodL < remL) prod.unshift(0);
                  subtract2(rem, prod, remL);
                  if (cmp == -1) {
                    remL = rem.length;
                    cmp = compare(yd, rem, yL, remL);
                    if (cmp < 1) {
                      k++;
                      subtract2(rem, yL < remL ? yz : yd, remL);
                    }
                  }
                  remL = rem.length;
                } else if (cmp === 0) {
                  k++;
                  rem = [0];
                }
                qd[i++] = k;
                if (cmp && rem[0]) {
                  rem[remL++] = xd[xi] || 0;
                } else {
                  rem = [xd[xi]];
                  remL = 1;
                }
              } while ((xi++ < xL || rem[0] !== void 0) && sd--);
            }
            if (!qd[0]) qd.shift();
            q.e = e;
            return round2(q, dp ? pr + getBase10Exponent(q) + 1 : pr);
          };
        })();
        function exp(x5, sd) {
          var denominator, guard, pow2, sum3, t, wpr, i = 0, k = 0, Ctor = x5.constructor, pr = Ctor.precision;
          if (getBase10Exponent(x5) > 16) throw Error(exponentOutOfRange + getBase10Exponent(x5));
          if (!x5.s) return new Ctor(ONE);
          if (sd == null) {
            external = false;
            wpr = pr;
          } else {
            wpr = sd;
          }
          t = new Ctor(0.03125);
          while (x5.abs().gte(0.1)) {
            x5 = x5.times(t);
            k += 5;
          }
          guard = Math.log(mathpow(2, k)) / Math.LN10 * 2 + 5 | 0;
          wpr += guard;
          denominator = pow2 = sum3 = new Ctor(ONE);
          Ctor.precision = wpr;
          for (; ; ) {
            pow2 = round2(pow2.times(x5), wpr);
            denominator = denominator.times(++i);
            t = sum3.plus(divide(pow2, denominator, wpr));
            if (digitsToString(t.d).slice(0, wpr) === digitsToString(sum3.d).slice(0, wpr)) {
              while (k--) sum3 = round2(sum3.times(sum3), wpr);
              Ctor.precision = pr;
              return sd == null ? (external = true, round2(sum3, pr)) : sum3;
            }
            sum3 = t;
          }
        }
        function getBase10Exponent(x5) {
          var e = x5.e * LOG_BASE, w = x5.d[0];
          for (; w >= 10; w /= 10) e++;
          return e;
        }
        function getLn10(Ctor, sd, pr) {
          if (sd > Ctor.LN10.sd()) {
            external = true;
            if (pr) Ctor.precision = pr;
            throw Error(decimalError + "LN10 precision limit exceeded");
          }
          return round2(new Ctor(Ctor.LN10), sd);
        }
        function getZeroString(k) {
          var zs = "";
          for (; k--; ) zs += "0";
          return zs;
        }
        function ln(y5, sd) {
          var c2, c0, denominator, e, numerator, sum3, t, wpr, x22, n = 1, guard = 10, x5 = y5, xd = x5.d, Ctor = x5.constructor, pr = Ctor.precision;
          if (x5.s < 1) throw Error(decimalError + (x5.s ? "NaN" : "-Infinity"));
          if (x5.eq(ONE)) return new Ctor(0);
          if (sd == null) {
            external = false;
            wpr = pr;
          } else {
            wpr = sd;
          }
          if (x5.eq(10)) {
            if (sd == null) external = true;
            return getLn10(Ctor, wpr);
          }
          wpr += guard;
          Ctor.precision = wpr;
          c2 = digitsToString(xd);
          c0 = c2.charAt(0);
          e = getBase10Exponent(x5);
          if (Math.abs(e) < 15e14) {
            while (c0 < 7 && c0 != 1 || c0 == 1 && c2.charAt(1) > 3) {
              x5 = x5.times(y5);
              c2 = digitsToString(x5.d);
              c0 = c2.charAt(0);
              n++;
            }
            e = getBase10Exponent(x5);
            if (c0 > 1) {
              x5 = new Ctor("0." + c2);
              e++;
            } else {
              x5 = new Ctor(c0 + "." + c2.slice(1));
            }
          } else {
            t = getLn10(Ctor, wpr + 2, pr).times(e + "");
            x5 = ln(new Ctor(c0 + "." + c2.slice(1)), wpr - guard).plus(t);
            Ctor.precision = pr;
            return sd == null ? (external = true, round2(x5, pr)) : x5;
          }
          sum3 = numerator = x5 = divide(x5.minus(ONE), x5.plus(ONE), wpr);
          x22 = round2(x5.times(x5), wpr);
          denominator = 3;
          for (; ; ) {
            numerator = round2(numerator.times(x22), wpr);
            t = sum3.plus(divide(numerator, new Ctor(denominator), wpr));
            if (digitsToString(t.d).slice(0, wpr) === digitsToString(sum3.d).slice(0, wpr)) {
              sum3 = sum3.times(2);
              if (e !== 0) sum3 = sum3.plus(getLn10(Ctor, wpr + 2, pr).times(e + ""));
              sum3 = divide(sum3, new Ctor(n), wpr);
              Ctor.precision = pr;
              return sd == null ? (external = true, round2(sum3, pr)) : sum3;
            }
            sum3 = t;
            denominator += 2;
          }
        }
        function parseDecimal(x5, str) {
          var e, i, len;
          if ((e = str.indexOf(".")) > -1) str = str.replace(".", "");
          if ((i = str.search(/e/i)) > 0) {
            if (e < 0) e = i;
            e += +str.slice(i + 1);
            str = str.substring(0, i);
          } else if (e < 0) {
            e = str.length;
          }
          for (i = 0; str.charCodeAt(i) === 48; ) ++i;
          for (len = str.length; str.charCodeAt(len - 1) === 48; ) --len;
          str = str.slice(i, len);
          if (str) {
            len -= i;
            e = e - i - 1;
            x5.e = mathfloor(e / LOG_BASE);
            x5.d = [];
            i = (e + 1) % LOG_BASE;
            if (e < 0) i += LOG_BASE;
            if (i < len) {
              if (i) x5.d.push(+str.slice(0, i));
              for (len -= LOG_BASE; i < len; ) x5.d.push(+str.slice(i, i += LOG_BASE));
              str = str.slice(i);
              i = LOG_BASE - str.length;
            } else {
              i -= len;
            }
            for (; i--; ) str += "0";
            x5.d.push(+str);
            if (external && (x5.e > MAX_E || x5.e < -MAX_E)) throw Error(exponentOutOfRange + e);
          } else {
            x5.s = 0;
            x5.e = 0;
            x5.d = [0];
          }
          return x5;
        }
        function round2(x5, sd, rm) {
          var i, j, k, n, rd, doRound, w, xdi, xd = x5.d;
          for (n = 1, k = xd[0]; k >= 10; k /= 10) n++;
          i = sd - n;
          if (i < 0) {
            i += LOG_BASE;
            j = sd;
            w = xd[xdi = 0];
          } else {
            xdi = Math.ceil((i + 1) / LOG_BASE);
            k = xd.length;
            if (xdi >= k) return x5;
            w = k = xd[xdi];
            for (n = 1; k >= 10; k /= 10) n++;
            i %= LOG_BASE;
            j = i - LOG_BASE + n;
          }
          if (rm !== void 0) {
            k = mathpow(10, n - j - 1);
            rd = w / k % 10 | 0;
            doRound = sd < 0 || xd[xdi + 1] !== void 0 || w % k;
            doRound = rm < 4 ? (rd || doRound) && (rm == 0 || rm == (x5.s < 0 ? 3 : 2)) : rd > 5 || rd == 5 && (rm == 4 || doRound || rm == 6 && // Check whether the digit to the left of the rounding digit is odd.
            (i > 0 ? j > 0 ? w / mathpow(10, n - j) : 0 : xd[xdi - 1]) % 10 & 1 || rm == (x5.s < 0 ? 8 : 7));
          }
          if (sd < 1 || !xd[0]) {
            if (doRound) {
              k = getBase10Exponent(x5);
              xd.length = 1;
              sd = sd - k - 1;
              xd[0] = mathpow(10, (LOG_BASE - sd % LOG_BASE) % LOG_BASE);
              x5.e = mathfloor(-sd / LOG_BASE) || 0;
            } else {
              xd.length = 1;
              xd[0] = x5.e = x5.s = 0;
            }
            return x5;
          }
          if (i == 0) {
            xd.length = xdi;
            k = 1;
            xdi--;
          } else {
            xd.length = xdi + 1;
            k = mathpow(10, LOG_BASE - i);
            xd[xdi] = j > 0 ? (w / mathpow(10, n - j) % mathpow(10, j) | 0) * k : 0;
          }
          if (doRound) {
            for (; ; ) {
              if (xdi == 0) {
                if ((xd[0] += k) == BASE) {
                  xd[0] = 1;
                  ++x5.e;
                }
                break;
              } else {
                xd[xdi] += k;
                if (xd[xdi] != BASE) break;
                xd[xdi--] = 0;
                k = 1;
              }
            }
          }
          for (i = xd.length; xd[--i] === 0; ) xd.pop();
          if (external && (x5.e > MAX_E || x5.e < -MAX_E)) {
            throw Error(exponentOutOfRange + getBase10Exponent(x5));
          }
          return x5;
        }
        function subtract(x5, y5) {
          var d, e, i, j, k, len, xd, xe, xLTy, yd, Ctor = x5.constructor, pr = Ctor.precision;
          if (!x5.s || !y5.s) {
            if (y5.s) y5.s = -y5.s;
            else y5 = new Ctor(x5);
            return external ? round2(y5, pr) : y5;
          }
          xd = x5.d;
          yd = y5.d;
          e = y5.e;
          xe = x5.e;
          xd = xd.slice();
          k = xe - e;
          if (k) {
            xLTy = k < 0;
            if (xLTy) {
              d = xd;
              k = -k;
              len = yd.length;
            } else {
              d = yd;
              e = xe;
              len = xd.length;
            }
            i = Math.max(Math.ceil(pr / LOG_BASE), len) + 2;
            if (k > i) {
              k = i;
              d.length = 1;
            }
            d.reverse();
            for (i = k; i--; ) d.push(0);
            d.reverse();
          } else {
            i = xd.length;
            len = yd.length;
            xLTy = i < len;
            if (xLTy) len = i;
            for (i = 0; i < len; i++) {
              if (xd[i] != yd[i]) {
                xLTy = xd[i] < yd[i];
                break;
              }
            }
            k = 0;
          }
          if (xLTy) {
            d = xd;
            xd = yd;
            yd = d;
            y5.s = -y5.s;
          }
          len = xd.length;
          for (i = yd.length - len; i > 0; --i) xd[len++] = 0;
          for (i = yd.length; i > k; ) {
            if (xd[--i] < yd[i]) {
              for (j = i; j && xd[--j] === 0; ) xd[j] = BASE - 1;
              --xd[j];
              xd[i] += BASE;
            }
            xd[i] -= yd[i];
          }
          for (; xd[--len] === 0; ) xd.pop();
          for (; xd[0] === 0; xd.shift()) --e;
          if (!xd[0]) return new Ctor(0);
          y5.d = xd;
          y5.e = e;
          return external ? round2(y5, pr) : y5;
        }
        function toString2(x5, isExp, sd) {
          var k, e = getBase10Exponent(x5), str = digitsToString(x5.d), len = str.length;
          if (isExp) {
            if (sd && (k = sd - len) > 0) {
              str = str.charAt(0) + "." + str.slice(1) + getZeroString(k);
            } else if (len > 1) {
              str = str.charAt(0) + "." + str.slice(1);
            }
            str = str + (e < 0 ? "e" : "e+") + e;
          } else if (e < 0) {
            str = "0." + getZeroString(-e - 1) + str;
            if (sd && (k = sd - len) > 0) str += getZeroString(k);
          } else if (e >= len) {
            str += getZeroString(e + 1 - len);
            if (sd && (k = sd - e - 1) > 0) str = str + "." + getZeroString(k);
          } else {
            if ((k = e + 1) < len) str = str.slice(0, k) + "." + str.slice(k);
            if (sd && (k = sd - len) > 0) {
              if (e + 1 === len) str += ".";
              str += getZeroString(k);
            }
          }
          return x5.s < 0 ? "-" + str : str;
        }
        function truncate(arr, len) {
          if (arr.length > len) {
            arr.length = len;
            return true;
          }
        }
        function clone(obj) {
          var i, p, ps;
          function Decimal4(value2) {
            var x5 = this;
            if (!(x5 instanceof Decimal4)) return new Decimal4(value2);
            x5.constructor = Decimal4;
            if (value2 instanceof Decimal4) {
              x5.s = value2.s;
              x5.e = value2.e;
              x5.d = (value2 = value2.d) ? value2.slice() : value2;
              return;
            }
            if (typeof value2 === "number") {
              if (value2 * 0 !== 0) {
                throw Error(invalidArgument + value2);
              }
              if (value2 > 0) {
                x5.s = 1;
              } else if (value2 < 0) {
                value2 = -value2;
                x5.s = -1;
              } else {
                x5.s = 0;
                x5.e = 0;
                x5.d = [0];
                return;
              }
              if (value2 === ~~value2 && value2 < 1e7) {
                x5.e = 0;
                x5.d = [value2];
                return;
              }
              return parseDecimal(x5, value2.toString());
            } else if (typeof value2 !== "string") {
              throw Error(invalidArgument + value2);
            }
            if (value2.charCodeAt(0) === 45) {
              value2 = value2.slice(1);
              x5.s = -1;
            } else {
              x5.s = 1;
            }
            if (isDecimal.test(value2)) parseDecimal(x5, value2);
            else throw Error(invalidArgument + value2);
          }
          Decimal4.prototype = P;
          Decimal4.ROUND_UP = 0;
          Decimal4.ROUND_DOWN = 1;
          Decimal4.ROUND_CEIL = 2;
          Decimal4.ROUND_FLOOR = 3;
          Decimal4.ROUND_HALF_UP = 4;
          Decimal4.ROUND_HALF_DOWN = 5;
          Decimal4.ROUND_HALF_EVEN = 6;
          Decimal4.ROUND_HALF_CEIL = 7;
          Decimal4.ROUND_HALF_FLOOR = 8;
          Decimal4.clone = clone;
          Decimal4.config = Decimal4.set = config;
          if (obj === void 0) obj = {};
          if (obj) {
            ps = ["precision", "rounding", "toExpNeg", "toExpPos", "LN10"];
            for (i = 0; i < ps.length; ) if (!obj.hasOwnProperty(p = ps[i++])) obj[p] = this[p];
          }
          Decimal4.config(obj);
          return Decimal4;
        }
        function config(obj) {
          if (!obj || typeof obj !== "object") {
            throw Error(decimalError + "Object expected");
          }
          var i, p, v, ps = [
            "precision",
            1,
            MAX_DIGITS,
            "rounding",
            0,
            8,
            "toExpNeg",
            -1 / 0,
            0,
            "toExpPos",
            0,
            1 / 0
          ];
          for (i = 0; i < ps.length; i += 3) {
            if ((v = obj[p = ps[i]]) !== void 0) {
              if (mathfloor(v) === v && v >= ps[i + 1] && v <= ps[i + 2]) this[p] = v;
              else throw Error(invalidArgument + p + ": " + v);
            }
          }
          if ((v = obj[p = "LN10"]) !== void 0) {
            if (v == Math.LN10) this[p] = new this(v);
            else throw Error(invalidArgument + p + ": " + v);
          }
          return this;
        }
        Decimal3 = clone(Decimal3);
        Decimal3["default"] = Decimal3.Decimal = Decimal3;
        ONE = new Decimal3(1);
        if (typeof define == "function" && define.amd) {
          define(function() {
            return Decimal3;
          });
        } else if (typeof module != "undefined" && module.exports) {
          module.exports = Decimal3;
        } else {
          if (!globalScope) {
            globalScope = typeof self != "undefined" && self && self.self == self ? self : Function("return this")();
          }
          globalScope.Decimal = Decimal3;
        }
      })(exports);
    }
  });

  // node_modules/eventemitter3/index.js
  var require_eventemitter3 = __commonJS({
    "node_modules/eventemitter3/index.js"(exports, module) {
      "use strict";
      var has3 = Object.prototype.hasOwnProperty;
      var prefix2 = "~";
      function Events() {
      }
      if (Object.create) {
        Events.prototype = /* @__PURE__ */ Object.create(null);
        if (!new Events().__proto__) prefix2 = false;
      }
      function EE(fn, context, once) {
        this.fn = fn;
        this.context = context;
        this.once = once || false;
      }
      function addListener2(emitter, event, fn, context, once) {
        if (typeof fn !== "function") {
          throw new TypeError("The listener must be a function");
        }
        var listener2 = new EE(fn, context || emitter, once), evt = prefix2 ? prefix2 + event : event;
        if (!emitter._events[evt]) emitter._events[evt] = listener2, emitter._eventsCount++;
        else if (!emitter._events[evt].fn) emitter._events[evt].push(listener2);
        else emitter._events[evt] = [emitter._events[evt], listener2];
        return emitter;
      }
      function clearEvent(emitter, evt) {
        if (--emitter._eventsCount === 0) emitter._events = new Events();
        else delete emitter._events[evt];
      }
      function EventEmitter2() {
        this._events = new Events();
        this._eventsCount = 0;
      }
      EventEmitter2.prototype.eventNames = function eventNames() {
        var names = [], events, name;
        if (this._eventsCount === 0) return names;
        for (name in events = this._events) {
          if (has3.call(events, name)) names.push(prefix2 ? name.slice(1) : name);
        }
        if (Object.getOwnPropertySymbols) {
          return names.concat(Object.getOwnPropertySymbols(events));
        }
        return names;
      };
      EventEmitter2.prototype.listeners = function listeners(event) {
        var evt = prefix2 ? prefix2 + event : event, handlers = this._events[evt];
        if (!handlers) return [];
        if (handlers.fn) return [handlers.fn];
        for (var i = 0, l = handlers.length, ee = new Array(l); i < l; i++) {
          ee[i] = handlers[i].fn;
        }
        return ee;
      };
      EventEmitter2.prototype.listenerCount = function listenerCount(event) {
        var evt = prefix2 ? prefix2 + event : event, listeners = this._events[evt];
        if (!listeners) return 0;
        if (listeners.fn) return 1;
        return listeners.length;
      };
      EventEmitter2.prototype.emit = function emit(event, a1, a2, a3, a4, a5) {
        var evt = prefix2 ? prefix2 + event : event;
        if (!this._events[evt]) return false;
        var listeners = this._events[evt], len = arguments.length, args, i;
        if (listeners.fn) {
          if (listeners.once) this.removeListener(event, listeners.fn, void 0, true);
          switch (len) {
            case 1:
              return listeners.fn.call(listeners.context), true;
            case 2:
              return listeners.fn.call(listeners.context, a1), true;
            case 3:
              return listeners.fn.call(listeners.context, a1, a2), true;
            case 4:
              return listeners.fn.call(listeners.context, a1, a2, a3), true;
            case 5:
              return listeners.fn.call(listeners.context, a1, a2, a3, a4), true;
            case 6:
              return listeners.fn.call(listeners.context, a1, a2, a3, a4, a5), true;
          }
          for (i = 1, args = new Array(len - 1); i < len; i++) {
            args[i - 1] = arguments[i];
          }
          listeners.fn.apply(listeners.context, args);
        } else {
          var length = listeners.length, j;
          for (i = 0; i < length; i++) {
            if (listeners[i].once) this.removeListener(event, listeners[i].fn, void 0, true);
            switch (len) {
              case 1:
                listeners[i].fn.call(listeners[i].context);
                break;
              case 2:
                listeners[i].fn.call(listeners[i].context, a1);
                break;
              case 3:
                listeners[i].fn.call(listeners[i].context, a1, a2);
                break;
              case 4:
                listeners[i].fn.call(listeners[i].context, a1, a2, a3);
                break;
              default:
                if (!args) for (j = 1, args = new Array(len - 1); j < len; j++) {
                  args[j - 1] = arguments[j];
                }
                listeners[i].fn.apply(listeners[i].context, args);
            }
          }
        }
        return true;
      };
      EventEmitter2.prototype.on = function on(event, fn, context) {
        return addListener2(this, event, fn, context, false);
      };
      EventEmitter2.prototype.once = function once(event, fn, context) {
        return addListener2(this, event, fn, context, true);
      };
      EventEmitter2.prototype.removeListener = function removeListener2(event, fn, context, once) {
        var evt = prefix2 ? prefix2 + event : event;
        if (!this._events[evt]) return this;
        if (!fn) {
          clearEvent(this, evt);
          return this;
        }
        var listeners = this._events[evt];
        if (listeners.fn) {
          if (listeners.fn === fn && (!once || listeners.once) && (!context || listeners.context === context)) {
            clearEvent(this, evt);
          }
        } else {
          for (var i = 0, events = [], length = listeners.length; i < length; i++) {
            if (listeners[i].fn !== fn || once && !listeners[i].once || context && listeners[i].context !== context) {
              events.push(listeners[i]);
            }
          }
          if (events.length) this._events[evt] = events.length === 1 ? events[0] : events;
          else clearEvent(this, evt);
        }
        return this;
      };
      EventEmitter2.prototype.removeAllListeners = function removeAllListeners(event) {
        var evt;
        if (event) {
          evt = prefix2 ? prefix2 + event : event;
          if (this._events[evt]) clearEvent(this, evt);
        } else {
          this._events = new Events();
          this._eventsCount = 0;
        }
        return this;
      };
      EventEmitter2.prototype.off = EventEmitter2.prototype.removeListener;
      EventEmitter2.prototype.addListener = EventEmitter2.prototype.on;
      EventEmitter2.prefixed = prefix2;
      EventEmitter2.EventEmitter = EventEmitter2;
      if ("undefined" !== typeof module) {
        module.exports = EventEmitter2;
      }
    }
  });

  // node_modules/react-is/cjs/react-is.development.js
  var require_react_is_development = __commonJS({
    "node_modules/react-is/cjs/react-is.development.js"(exports) {
      "use strict";
      (function() {
        function typeOf(object) {
          if ("object" === typeof object && null !== object) {
            var $$typeof = object.$$typeof;
            switch ($$typeof) {
              case REACT_ELEMENT_TYPE:
                switch (object = object.type, object) {
                  case REACT_FRAGMENT_TYPE:
                  case REACT_PROFILER_TYPE:
                  case REACT_STRICT_MODE_TYPE:
                  case REACT_SUSPENSE_TYPE:
                  case REACT_SUSPENSE_LIST_TYPE:
                  case REACT_VIEW_TRANSITION_TYPE:
                    return object;
                  default:
                    switch (object = object && object.$$typeof, object) {
                      case REACT_CONTEXT_TYPE:
                      case REACT_FORWARD_REF_TYPE2:
                      case REACT_LAZY_TYPE:
                      case REACT_MEMO_TYPE2:
                        return object;
                      case REACT_CONSUMER_TYPE:
                        return object;
                      default:
                        return $$typeof;
                    }
                }
              case REACT_PORTAL_TYPE:
                return $$typeof;
            }
          }
        }
        var REACT_ELEMENT_TYPE = Symbol.for("react.transitional.element"), REACT_PORTAL_TYPE = Symbol.for("react.portal"), REACT_FRAGMENT_TYPE = Symbol.for("react.fragment"), REACT_STRICT_MODE_TYPE = Symbol.for("react.strict_mode"), REACT_PROFILER_TYPE = Symbol.for("react.profiler"), REACT_CONSUMER_TYPE = Symbol.for("react.consumer"), REACT_CONTEXT_TYPE = Symbol.for("react.context"), REACT_FORWARD_REF_TYPE2 = Symbol.for("react.forward_ref"), REACT_SUSPENSE_TYPE = Symbol.for("react.suspense"), REACT_SUSPENSE_LIST_TYPE = Symbol.for("react.suspense_list"), REACT_MEMO_TYPE2 = Symbol.for("react.memo"), REACT_LAZY_TYPE = Symbol.for("react.lazy"), REACT_VIEW_TRANSITION_TYPE = Symbol.for("react.view_transition"), REACT_CLIENT_REFERENCE = Symbol.for("react.client.reference");
        exports.ContextConsumer = REACT_CONSUMER_TYPE;
        exports.ContextProvider = REACT_CONTEXT_TYPE;
        exports.Element = REACT_ELEMENT_TYPE;
        exports.ForwardRef = REACT_FORWARD_REF_TYPE2;
        exports.Fragment = REACT_FRAGMENT_TYPE;
        exports.Lazy = REACT_LAZY_TYPE;
        exports.Memo = REACT_MEMO_TYPE2;
        exports.Portal = REACT_PORTAL_TYPE;
        exports.Profiler = REACT_PROFILER_TYPE;
        exports.StrictMode = REACT_STRICT_MODE_TYPE;
        exports.Suspense = REACT_SUSPENSE_TYPE;
        exports.SuspenseList = REACT_SUSPENSE_LIST_TYPE;
        exports.isContextConsumer = function(object) {
          return typeOf(object) === REACT_CONSUMER_TYPE;
        };
        exports.isContextProvider = function(object) {
          return typeOf(object) === REACT_CONTEXT_TYPE;
        };
        exports.isElement = function(object) {
          return "object" === typeof object && null !== object && object.$$typeof === REACT_ELEMENT_TYPE;
        };
        exports.isForwardRef = function(object) {
          return typeOf(object) === REACT_FORWARD_REF_TYPE2;
        };
        exports.isFragment = function(object) {
          return typeOf(object) === REACT_FRAGMENT_TYPE;
        };
        exports.isLazy = function(object) {
          return typeOf(object) === REACT_LAZY_TYPE;
        };
        exports.isMemo = function(object) {
          return typeOf(object) === REACT_MEMO_TYPE2;
        };
        exports.isPortal = function(object) {
          return typeOf(object) === REACT_PORTAL_TYPE;
        };
        exports.isProfiler = function(object) {
          return typeOf(object) === REACT_PROFILER_TYPE;
        };
        exports.isStrictMode = function(object) {
          return typeOf(object) === REACT_STRICT_MODE_TYPE;
        };
        exports.isSuspense = function(object) {
          return typeOf(object) === REACT_SUSPENSE_TYPE;
        };
        exports.isSuspenseList = function(object) {
          return typeOf(object) === REACT_SUSPENSE_LIST_TYPE;
        };
        exports.isValidElementType = function(type) {
          return "string" === typeof type || "function" === typeof type || type === REACT_FRAGMENT_TYPE || type === REACT_PROFILER_TYPE || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || "object" === typeof type && null !== type && (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE2 || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_CONSUMER_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE2 || type.$$typeof === REACT_CLIENT_REFERENCE || void 0 !== type.getModuleId) ? true : false;
        };
        exports.typeOf = typeOf;
      })();
    }
  });

  // node_modules/react-is/index.js
  var require_react_is = __commonJS({
    "node_modules/react-is/index.js"(exports, module) {
      "use strict";
      if (false) {
        module.exports = null;
      } else {
        module.exports = require_react_is_development();
      }
    }
  });

  // src/index.ts
  var index_exports = {};
  __export(index_exports, {
    default: () => Retraced
  });
  var import_react85 = __toESM(require_react(), 1);

  // plugin.meta.json
  var plugin_meta_default = {
    name: "Retraced",
    author: "ISenseHostility",
    description: "Local-only statistics about your own Discord activity. Everything stays on this device — no network calls, ever.",
    version: "1.0.0",
    website: "https://retraced.cc"
  };

  // src/aggregate/pending.ts
  var pendingKey = (a2, b) => `${a2} ${b}`;
  var PendingBuffer = class {
    events = [];
    messageCreates = /* @__PURE__ */ new Map();
    messageEdits = [];
    messageDeletes = [];
    /** key: `${date} ${channelId}` */
    daily = /* @__PURE__ */ new Map();
    /** key: `${date} ${hour}` → own messages sent */
    hourly = /* @__PURE__ */ new Map();
    peers = /* @__PURE__ */ new Map();
    /** key: `${date} ${channelId}` */
    voice = /* @__PURE__ */ new Map();
    /** key: date the session started — resolved usage sessions, as a duration histogram */
    sessions = /* @__PURE__ */ new Map();
    /** key: `${scope} ${term}` (term itself may contain a space — bigram "phrases") */
    words = /* @__PURE__ */ new Map();
    /** inverted-index postings for content-storable messages in this batch */
    searchIndex = [];
    /** key: `${guildScope} ${emojiId}` */
    emoji = /* @__PURE__ */ new Map();
    domains = /* @__PURE__ */ new Map();
    get isEmpty() {
      return this.events.length === 0 && this.messageDeletes.length === 0 && this.messageEdits.length === 0 && // a rollup-only buffer (the rebuild path strips raw events) still counts as work
      this.daily.size === 0 && this.hourly.size === 0 && this.voice.size === 0 && this.sessions.size === 0;
    }
  };

  // src/util/time.ts
  var DISCORD_EPOCH_MS = 14200704e5;
  function dateKey(ts) {
    const d = new Date(ts);
    const m2 = String(d.getMonth() + 1).padStart(2, "0");
    const day = String(d.getDate()).padStart(2, "0");
    return `${d.getFullYear()}-${m2}-${day}`;
  }
  function hourOf(ts) {
    return new Date(ts).getHours();
  }
  function parseDateKey(key) {
    const [y5, m2, d] = key.split("-").map(Number);
    return new Date(y5 ?? 1970, (m2 ?? 1) - 1, d ?? 1);
  }
  function shiftDate(key, days) {
    const date2 = parseDateKey(key);
    date2.setDate(date2.getDate() + days);
    return dateKey(date2.getTime());
  }
  function mondayOf(key) {
    const offset = (parseDateKey(key).getDay() + 6) % 7;
    return offset === 0 ? key : shiftDate(key, -offset);
  }
  function snowflakeToTs(id) {
    if (!id || !/^\d{6,}$/.test(id)) return null;
    try {
      return Number(BigInt(id) >> 22n) + DISCORD_EPOCH_MS;
    } catch {
      return null;
    }
  }

  // src/util/tokenize.ts
  var CUSTOM_EMOJI_RE = /<a?:\w+:\d+>/g;
  var MENTION_RE = /<[@#][!&]?\d+>/g;
  var URL_RE = /https?:\/\/\S+/gi;
  var EDGE_RE = /^[^\p{L}\p{N}]+|[^\p{L}\p{N}]+$/gu;
  var MAX_TOKEN_LENGTH = 32;
  function tokenize(content) {
    const cleaned = content.replace(CUSTOM_EMOJI_RE, " ").replace(MENTION_RE, " ").replace(URL_RE, " ");
    const out = [];
    for (const raw of cleaned.toLowerCase().split(/\s+/)) {
      const word = raw.replace(EDGE_RE, "");
      if (word.length === 0 || word.length > MAX_TOKEN_LENGTH) continue;
      out.push(word);
    }
    return out;
  }
  function bigrams(tokens) {
    const out = [];
    for (let i = 0; i + 1 < tokens.length; i++) out.push(`${tokens[i]} ${tokens[i + 1]}`);
    return out;
  }

  // src/aggregate/buckets.ts
  var REPLY_BUCKET_BOUNDS_MS = [1e4, 3e4, 12e4, 6e5, 36e5, 216e5];
  var REPLY_BUCKET_COUNT = REPLY_BUCKET_BOUNDS_MS.length + 1;
  var DELETE_BUCKET_BOUNDS_MS = [1e4, 6e4, 6e5, 36e5, 216e5, 864e5];
  var DELETE_BUCKET_COUNT = DELETE_BUCKET_BOUNDS_MS.length + 1;
  var SESSION_BUCKET_BOUNDS_MS = [3e5, 9e5, 18e5, 36e5, 72e5, 144e5];
  var SESSION_BUCKET_COUNT = SESSION_BUCKET_BOUNDS_MS.length + 1;
  var LENGTH_BUCKET_BOUNDS_CHARS = [10, 50, 200, 1e3];
  var LENGTH_BUCKET_COUNT = LENGTH_BUCKET_BOUNDS_CHARS.length + 1;
  function bucketIndex(ms, bounds) {
    for (let i = 0; i < bounds.length; i++) {
      if (ms < bounds[i]) return i;
    }
    return bounds.length;
  }
  function emptyBuckets(count) {
    return new Array(count).fill(0);
  }
  function addToBuckets(buckets, count, ms, bounds) {
    const normalized = buckets.length === count ? buckets : emptyBuckets(count);
    normalized[bucketIndex(ms, bounds)] += 1;
    return normalized;
  }

  // src/aggregate/reducers.ts
  var REPLY_FLOW_CAP_MS = 6 * 36e5;
  var SESSION_GAP_MS = 30 * 6e4;
  function createReducerContext(opts) {
    return {
      ownUserId: opts.ownUserId,
      settings: opts.settings,
      resolvePeerLabel: opts.resolvePeerLabel,
      dmRecipients: opts.dmRecipients,
      heads: opts.heads ?? /* @__PURE__ */ new Map(),
      session: opts.session ?? null,
      dayWords: /* @__PURE__ */ new Map(),
      currentDay: null
    };
  }
  function getDaily(pending, date2, channelId, guildId) {
    const key = pendingKey(date2, channelId);
    let delta = pending.daily.get(key);
    if (!delta) {
      delta = {
        guildId,
        sent: 0,
        edited: 0,
        chars: 0,
        words: 0,
        uniqueWords: 0,
        typingCommitted: 0,
        typingAborted: 0,
        typingMs: 0,
        dwellMs: 0,
        reactionsGiven: 0,
        reactionsReceived: 0,
        contentTypes: { text: 0, image: 0, link: 0, gif: 0, sticker: 0, attachment: 0 },
        initiatedByMe: 0,
        initiatedByThem: 0,
        longestBurst: 0,
        theirSent: 0,
        theirChars: 0,
        theirWords: 0,
        theirUniqueWords: 0,
        lengthBuckets: emptyBuckets(LENGTH_BUCKET_COUNT),
        theirLengthBuckets: emptyBuckets(LENGTH_BUCKET_COUNT)
      };
      pending.daily.set(key, delta);
    }
    return delta;
  }
  function getPeer(pending, ctx, userId, ts) {
    let delta = pending.peers.get(userId);
    if (!delta) {
      const resolved = ctx.resolvePeerLabel(userId);
      delta = {
        label: resolved?.label ?? null,
        avatarHash: resolved?.avatarHash ?? null,
        msgToThem: 0,
        msgFromThem: 0,
        initiatedByMe: 0,
        initiatedByThem: 0,
        latencyMine: emptyBuckets(REPLY_BUCKET_COUNT),
        latencyTheirs: emptyBuckets(REPLY_BUCKET_COUNT),
        typingAbortedAtThem: 0,
        reactionsToThem: 0,
        reactionsFromThem: 0,
        firstTs: ts,
        lastTs: ts
      };
      pending.peers.set(userId, delta);
    }
    delta.firstTs = Math.min(delta.firstTs, ts);
    delta.lastTs = Math.max(delta.lastTs, ts);
    return delta;
  }
  var tokenEdges = /^[^\p{L}\p{N}]+|[^\p{L}\p{N}]+$/gu;
  function countNewWords(ctx, date2, channelId, content) {
    if (ctx.currentDay !== date2) {
      ctx.dayWords.clear();
      ctx.currentDay = date2;
    }
    const key = pendingKey(date2, channelId);
    let seen = ctx.dayWords.get(key);
    if (!seen) {
      seen = /* @__PURE__ */ new Set();
      ctx.dayWords.set(key, seen);
    }
    let added = 0;
    for (const raw of content.toLowerCase().split(/\s+/)) {
      const word = raw.replace(tokenEdges, "");
      if (word.length === 0 || seen.has(word)) continue;
      seen.add(word);
      added++;
    }
    return added;
  }
  function isDmLike(kind) {
    return kind === "dm" || kind === "group-dm";
  }
  function reduceMessageCreated(ev, pending, ctx) {
    const date2 = dateKey(ev.ts);
    const dmLike = isDmLike(ev.channel.kind);
    const prev = ctx.heads.get(ev.channel.channelId);
    const gapMs = prev ? ev.ts - prev.ts : Infinity;
    const isInitiation = gapMs > ctx.settings().conversationGapMinutes * 6e4;
    const burst = isInitiation ? 1 : (prev?.burst ?? 0) + 1;
    {
      const daily = getDaily(pending, date2, ev.channel.channelId, ev.channel.guildId);
      daily.longestBurst = Math.max(daily.longestBurst, burst);
    }
    if (isInitiation) {
      const daily = getDaily(pending, date2, ev.channel.channelId, ev.channel.guildId);
      if (ev.isOwn) daily.initiatedByMe++;
      else daily.initiatedByThem++;
      if (dmLike) {
        if (ev.isOwn) {
          const recipients = ctx.dmRecipients(ev.channel.channelId);
          if (recipients?.length === 1) getPeer(pending, ctx, recipients[0], ev.ts).initiatedByMe++;
        } else {
          getPeer(pending, ctx, ev.authorId, ev.ts).initiatedByThem++;
        }
      }
    }
    let latencyRecorded = false;
    if (ev.replyToId && ev.replyToTs !== null && ev.replyToAuthorId) {
      const latency = Math.max(0, ev.ts - ev.replyToTs);
      if (ev.isOwn && ev.replyToAuthorId !== ctx.ownUserId) {
        const peer = getPeer(pending, ctx, ev.replyToAuthorId, ev.ts);
        peer.latencyMine = addToBuckets(peer.latencyMine, REPLY_BUCKET_COUNT, latency, REPLY_BUCKET_BOUNDS_MS);
        latencyRecorded = true;
      } else if (!ev.isOwn && ev.replyToAuthorId === ctx.ownUserId) {
        const peer = getPeer(pending, ctx, ev.authorId, ev.ts);
        peer.latencyTheirs = addToBuckets(peer.latencyTheirs, REPLY_BUCKET_COUNT, latency, REPLY_BUCKET_BOUNDS_MS);
        latencyRecorded = true;
      }
    }
    if (!latencyRecorded && dmLike && prev && prev.authorId !== ev.authorId && gapMs <= REPLY_FLOW_CAP_MS) {
      if (ev.isOwn && prev.authorId !== ctx.ownUserId) {
        const peer = getPeer(pending, ctx, prev.authorId, ev.ts);
        peer.latencyMine = addToBuckets(peer.latencyMine, REPLY_BUCKET_COUNT, gapMs, REPLY_BUCKET_BOUNDS_MS);
      } else if (!ev.isOwn && prev.authorId === ctx.ownUserId) {
        const peer = getPeer(pending, ctx, ev.authorId, ev.ts);
        peer.latencyTheirs = addToBuckets(peer.latencyTheirs, REPLY_BUCKET_COUNT, gapMs, REPLY_BUCKET_BOUNDS_MS);
      }
    }
    if (dmLike) {
      if (ev.isOwn) {
        const recipients = ctx.dmRecipients(ev.channel.channelId);
        if (ev.channel.kind === "dm" && recipients?.length === 1) {
          getPeer(pending, ctx, recipients[0], ev.ts).msgToThem++;
        }
      } else {
        getPeer(pending, ctx, ev.authorId, ev.ts).msgFromThem++;
      }
    }
    if (!ev.isOwn && dmLike) {
      const daily = getDaily(pending, date2, ev.channel.channelId, ev.channel.guildId);
      daily.theirSent++;
      daily.theirChars += ev.chars;
      daily.theirWords += ev.words;
      daily.theirLengthBuckets = addToBuckets(daily.theirLengthBuckets, LENGTH_BUCKET_COUNT, ev.chars, LENGTH_BUCKET_BOUNDS_CHARS);
      if (ev.content !== null) {
        daily.theirUniqueWords += countNewWords(ctx, date2, `theirs:${ev.channel.channelId}`, ev.content);
        collectText(ev.content, "theirs", ev, pending);
      }
    }
    if (ev.isOwn) {
      trackSession(ev.ts, pending, ctx);
      const daily = getDaily(pending, date2, ev.channel.channelId, ev.channel.guildId);
      daily.sent++;
      daily.chars += ev.chars;
      daily.words += ev.words;
      daily.lengthBuckets = addToBuckets(daily.lengthBuckets, LENGTH_BUCKET_COUNT, ev.chars, LENGTH_BUCKET_BOUNDS_CHARS);
      if (ev.content !== null) {
        daily.uniqueWords += countNewWords(ctx, date2, ev.channel.channelId, ev.content);
        collectText(ev.content, "mine", ev, pending);
      }
      for (const flag of Object.keys(daily.contentTypes)) {
        if (ev.contentTypes[flag]) daily.contentTypes[flag]++;
      }
      const hourKey = pendingKey(date2, hourOf(ev.ts));
      pending.hourly.set(hourKey, (pending.hourly.get(hourKey) ?? 0) + 1);
      const scope = ev.channel.guildId ?? "dm";
      for (const emoji of ev.customEmoji) {
        const key = pendingKey(scope, emoji.id);
        const entry = pending.emoji.get(key) ?? { name: emoji.name, count: 0 };
        entry.count++;
        entry.name = emoji.name;
        pending.emoji.set(key, entry);
      }
    }
    for (const domain of ev.domains) {
      const entry = pending.domains.get(domain) ?? { count: 0, lastTs: ev.ts };
      entry.count++;
      entry.lastTs = Math.max(entry.lastTs, ev.ts);
      pending.domains.set(domain, entry);
    }
    if (!pending.messageCreates.has(ev.messageId)) {
      pending.messageCreates.set(ev.messageId, {
        messageId: ev.messageId,
        ts: ev.ts,
        channelId: ev.channel.channelId,
        guildId: ev.channel.guildId,
        authorId: ev.authorId,
        isOwn: ev.isOwn,
        content: ev.content,
        revisions: [],
        deletedAt: null,
        deleteAfterMs: null,
        editCount: 0,
        lastEditedTs: null,
        replyToId: ev.replyToId,
        contentTypes: ev.contentTypes,
        chars: ev.chars,
        words: ev.words
      });
    }
    ctx.heads.set(ev.channel.channelId, { ts: ev.ts, authorId: ev.authorId, burst });
  }
  function collectText(content, scope, ev, pending) {
    const tokens = tokenize(content);
    for (const token of tokens) {
      const key = `${scope} ${token}`;
      pending.words.set(key, (pending.words.get(key) ?? 0) + 1);
    }
    for (const phrase of bigrams(tokens)) {
      const key = `${scope} ${phrase}`;
      pending.words.set(key, (pending.words.get(key) ?? 0) + 1);
    }
    for (const term of new Set(tokens)) {
      pending.searchIndex.push({
        term,
        messageId: ev.messageId,
        ts: ev.ts,
        channelId: ev.channel.channelId,
        guildId: ev.channel.guildId,
        isOwn: ev.isOwn
      });
    }
  }
  function trackSession(ts, pending, ctx) {
    const open = ctx.session;
    if (open && ts - open.lastTs <= SESSION_GAP_MS) {
      open.lastTs = Math.max(open.lastTs, ts);
      return;
    }
    if (open) {
      const date2 = dateKey(open.startTs);
      const delta = pending.sessions.get(date2) ?? { buckets: emptyBuckets(SESSION_BUCKET_COUNT), totalMs: 0 };
      const durationMs = Math.max(0, open.lastTs - open.startTs);
      delta.buckets = addToBuckets(delta.buckets, SESSION_BUCKET_COUNT, durationMs, SESSION_BUCKET_BOUNDS_MS);
      delta.totalMs += durationMs;
      pending.sessions.set(date2, delta);
    }
    ctx.session = { startTs: ts, lastTs: ts };
  }
  function reduceTyping(ev, pending, ctx) {
    const daily = getDaily(pending, dateKey(ev.ts), ev.channel.channelId, ev.channel.guildId);
    if (ev.committed) daily.typingCommitted++;
    else daily.typingAborted++;
    daily.typingMs += ev.durationMs;
    if (!ev.committed && ev.peerId) getPeer(pending, ctx, ev.peerId, ev.ts).typingAbortedAtThem++;
  }
  function reduceReaction(ev, pending, ctx) {
    const date2 = dateKey(ev.ts);
    if (ev.actorIsOwn) {
      getDaily(pending, date2, ev.channel.channelId, ev.channel.guildId).reactionsGiven += ev.direction;
      if (ev.messageAuthorId && !ev.messageAuthorIsOwn) {
        getPeer(pending, ctx, ev.messageAuthorId, ev.ts).reactionsToThem += ev.direction;
      }
      if (ev.emoji.id) {
        const key = pendingKey(ev.channel.guildId ?? "dm", ev.emoji.id);
        const entry = pending.emoji.get(key) ?? { name: ev.emoji.name, count: 0 };
        entry.count += ev.direction;
        entry.name = ev.emoji.name;
        pending.emoji.set(key, entry);
      }
    } else if (ev.messageAuthorIsOwn) {
      getDaily(pending, date2, ev.channel.channelId, ev.channel.guildId).reactionsReceived += ev.direction;
      getPeer(pending, ctx, ev.actorId, ev.ts).reactionsFromThem += ev.direction;
    }
  }
  function reduceEvent(ev, pending, ctx) {
    pending.events.push(ev);
    switch (ev.kind) {
      case "message-created":
        reduceMessageCreated(ev, pending, ctx);
        break;
      case "message-edited":
        if (ev.isOwn) getDaily(pending, dateKey(ev.ts), ev.channel.channelId, ev.channel.guildId).edited++;
        pending.messageEdits.push(ev);
        break;
      case "message-deleted":
        pending.messageDeletes.push(ev);
        break;
      case "typing-resolved":
        reduceTyping(ev, pending, ctx);
        break;
      case "reaction":
        reduceReaction(ev, pending, ctx);
        break;
      case "dwell":
        getDaily(pending, dateKey(ev.ts), ev.channel.channelId, ev.channel.guildId).dwellMs += ev.durationMs;
        break;
      case "voice-segment": {
        const key = pendingKey(dateKey(ev.ts), ev.channel.channelId);
        const entry = pending.voice.get(key) ?? { guildId: ev.channel.guildId, seconds: 0, coPresent: {} };
        entry.seconds += ev.seconds;
        for (const [userId, seconds2] of Object.entries(ev.coPresent)) {
          entry.coPresent[userId] = (entry.coPresent[userId] ?? 0) + seconds2;
        }
        pending.voice.set(key, entry);
        break;
      }
    }
  }

  // src/db/flush.ts
  var MAX_REVISIONS = 10;
  var MAX_PERSISTED_HEADS = 500;
  var clamp0 = (n) => Math.max(0, n);
  function emptyDaily(date2, channelId, guildId) {
    return {
      date: date2,
      channelId,
      guildId,
      sent: 0,
      edited: 0,
      deleted: 0,
      chars: 0,
      words: 0,
      uniqueWords: 0,
      typingCommitted: 0,
      typingAborted: 0,
      typingMs: 0,
      dwellMs: 0,
      reactionsGiven: 0,
      reactionsReceived: 0,
      contentTypes: { text: 0, image: 0, link: 0, gif: 0, sticker: 0, attachment: 0 },
      initiatedByMe: 0,
      initiatedByThem: 0,
      deleteAfterBuckets: emptyBuckets(DELETE_BUCKET_COUNT),
      longestBurst: 0,
      theirSent: 0,
      theirChars: 0,
      theirWords: 0,
      theirUniqueWords: 0,
      lengthBuckets: emptyBuckets(LENGTH_BUCKET_COUNT),
      theirLengthBuckets: emptyBuckets(LENGTH_BUCKET_COUNT)
    };
  }
  async function flushPending(db, pending, opts) {
    if (pending.isEmpty) return { events: 0, stores: 0 };
    const tx = db.transaction(
      ["meta", "events", "messages", "daily", "hourly", "peers", "voice", "sessions", "emoji", "domains", "words", "searchIndex"],
      "readwrite"
    );
    let storeWrites = 0;
    const events = tx.objectStore("events");
    for (const event of pending.events) {
      void events.add({ ts: event.ts, event });
    }
    const messages = tx.objectStore("messages");
    for (const row of pending.messageCreates.values()) {
      const existing = await messages.get(row.messageId);
      if (!existing) {
        await messages.put(row);
        storeWrites++;
      }
    }
    const searchIndex = tx.objectStore("searchIndex");
    for (const edit of pending.messageEdits) {
      const row = await messages.get(edit.messageId);
      if (!row) continue;
      if (edit.content !== null && edit.content !== row.content) {
        const oldTerms = new Set(row.content === null ? [] : tokenize(row.content));
        const newTerms = new Set(tokenize(edit.content));
        for (const term of oldTerms) {
          if (!newTerms.has(term)) void searchIndex.delete([term, row.messageId]);
        }
        for (const term of newTerms) {
          if (!oldTerms.has(term)) {
            void searchIndex.put({ term, messageId: row.messageId, ts: row.ts, channelId: row.channelId, guildId: row.guildId, isOwn: row.isOwn });
          }
        }
      }
      if (edit.content !== null && row.content !== null && edit.content !== row.content) {
        row.revisions.push({ ts: row.lastEditedTs ?? row.ts, content: row.content });
        if (row.revisions.length > MAX_REVISIONS) row.revisions.splice(0, row.revisions.length - MAX_REVISIONS);
      }
      if (edit.content !== null) row.content = edit.content;
      row.editCount += 1;
      row.lastEditedTs = edit.ts;
      await messages.put(row);
      storeWrites++;
    }
    const daily = tx.objectStore("daily");
    const dailyCache = /* @__PURE__ */ new Map();
    const loadDaily = async (date2, channelId, guildId) => {
      const cacheKey = `${date2} ${channelId}`;
      let row = dailyCache.get(cacheKey);
      if (!row) {
        row = await daily.get([date2, channelId]) ?? emptyDaily(date2, channelId, guildId);
        if (row.guildId === null && guildId !== null) row.guildId = guildId;
        dailyCache.set(cacheKey, row);
      }
      return row;
    };
    for (const [key, delta] of pending.daily) {
      const sep = key.indexOf(" ");
      const row = await loadDaily(key.slice(0, sep), key.slice(sep + 1), delta.guildId);
      row.sent += delta.sent;
      row.edited += delta.edited;
      row.chars += delta.chars;
      row.words += delta.words;
      row.uniqueWords += delta.uniqueWords;
      row.typingCommitted += delta.typingCommitted;
      row.typingAborted += delta.typingAborted;
      row.typingMs += delta.typingMs;
      row.dwellMs += delta.dwellMs;
      row.reactionsGiven = clamp0(row.reactionsGiven + delta.reactionsGiven);
      row.reactionsReceived = clamp0(row.reactionsReceived + delta.reactionsReceived);
      for (const flag of Object.keys(row.contentTypes)) {
        row.contentTypes[flag] += delta.contentTypes[flag];
      }
      row.initiatedByMe += delta.initiatedByMe;
      row.initiatedByThem += delta.initiatedByThem;
      row.longestBurst = Math.max(row.longestBurst ?? 0, delta.longestBurst);
      row.theirSent = (row.theirSent ?? 0) + delta.theirSent;
      row.theirChars = (row.theirChars ?? 0) + delta.theirChars;
      row.theirWords = (row.theirWords ?? 0) + delta.theirWords;
      row.theirUniqueWords = (row.theirUniqueWords ?? 0) + delta.theirUniqueWords;
      row.lengthBuckets = delta.lengthBuckets.map((n, i) => n + (row.lengthBuckets?.[i] ?? 0));
      row.theirLengthBuckets = delta.theirLengthBuckets.map((n, i) => n + (row.theirLengthBuckets?.[i] ?? 0));
    }
    for (const del of pending.messageDeletes) {
      const row = await messages.get(del.messageId);
      if (!row) continue;
      if (row.deletedAt === null) {
        row.deletedAt = del.ts;
        row.deleteAfterMs = Math.max(0, del.ts - row.ts);
        await messages.put(row);
        storeWrites++;
        if (row.isOwn) {
          const dailyRow = await loadDaily(dateKey(del.ts), row.channelId, row.guildId);
          dailyRow.deleted += 1;
          dailyRow.deleteAfterBuckets = addToBuckets(
            dailyRow.deleteAfterBuckets,
            DELETE_BUCKET_COUNT,
            row.deleteAfterMs,
            DELETE_BUCKET_BOUNDS_MS
          );
        }
      }
    }
    for (const row of dailyCache.values()) {
      await daily.put(row);
      storeWrites++;
    }
    const hourly = tx.objectStore("hourly");
    for (const [key, sent] of pending.hourly) {
      const sep = key.indexOf(" ");
      const date2 = key.slice(0, sep);
      const hour = Number(key.slice(sep + 1));
      const row = await hourly.get([date2, hour]) ?? { date: date2, hour, sent: 0 };
      row.sent += sent;
      await hourly.put(row);
      storeWrites++;
    }
    const peers = tx.objectStore("peers");
    for (const [userId, delta] of pending.peers) {
      const row = await peers.get(userId) ?? {
        userId,
        label: null,
        avatarHash: null,
        msgToThem: 0,
        msgFromThem: 0,
        initiatedByMe: 0,
        initiatedByThem: 0,
        latencyBucketsMine: emptyBuckets(delta.latencyMine.length),
        latencyBucketsTheirs: emptyBuckets(delta.latencyTheirs.length),
        typingAbortedAtThem: 0,
        reactionsToThem: 0,
        reactionsFromThem: 0,
        firstSeenTs: delta.firstTs,
        lastSeenTs: delta.lastTs
      };
      if (delta.label !== null) row.label = delta.label;
      if (delta.avatarHash !== null) row.avatarHash = delta.avatarHash;
      row.msgToThem += delta.msgToThem;
      row.msgFromThem += delta.msgFromThem;
      row.initiatedByMe += delta.initiatedByMe;
      row.initiatedByThem += delta.initiatedByThem;
      row.latencyBucketsMine = delta.latencyMine.map((n, i) => n + (row.latencyBucketsMine[i] ?? 0));
      row.latencyBucketsTheirs = delta.latencyTheirs.map((n, i) => n + (row.latencyBucketsTheirs[i] ?? 0));
      row.typingAbortedAtThem += delta.typingAbortedAtThem;
      row.reactionsToThem = clamp0(row.reactionsToThem + delta.reactionsToThem);
      row.reactionsFromThem = clamp0(row.reactionsFromThem + delta.reactionsFromThem);
      row.firstSeenTs = Math.min(row.firstSeenTs, delta.firstTs);
      row.lastSeenTs = Math.max(row.lastSeenTs, delta.lastTs);
      await peers.put(row);
      storeWrites++;
    }
    const voice = tx.objectStore("voice");
    for (const [key, delta] of pending.voice) {
      const sep = key.indexOf(" ");
      const date2 = key.slice(0, sep);
      const channelId = key.slice(sep + 1);
      const row = await voice.get([date2, channelId]) ?? { date: date2, channelId, guildId: delta.guildId, seconds: 0, coPresent: {} };
      row.seconds += delta.seconds;
      for (const [userId, seconds2] of Object.entries(delta.coPresent)) {
        row.coPresent[userId] = (row.coPresent[userId] ?? 0) + seconds2;
      }
      await voice.put(row);
      storeWrites++;
    }
    const words = tx.objectStore("words");
    const wordEntries = [...pending.words.entries()].map(([key, count]) => {
      const sep = key.indexOf(" ");
      return { scope: key.slice(0, sep), term: key.slice(sep + 1), count };
    });
    const existingWords = await Promise.all(wordEntries.map((e) => words.get([e.scope, e.term])));
    wordEntries.forEach((e, i) => {
      const row = existingWords[i] ?? { scope: e.scope, term: e.term, count: 0 };
      row.count += e.count;
      void words.put(row);
      storeWrites++;
    });
    for (const posting of pending.searchIndex) {
      void searchIndex.put(posting);
      storeWrites++;
    }
    const sessions = tx.objectStore("sessions");
    for (const [date2, delta] of pending.sessions) {
      const row = await sessions.get(date2) ?? { date: date2, buckets: emptyBuckets(delta.buckets.length), totalMs: 0 };
      row.buckets = delta.buckets.map((n, i) => n + (row.buckets[i] ?? 0));
      row.totalMs += delta.totalMs;
      await sessions.put(row);
      storeWrites++;
    }
    const emoji = tx.objectStore("emoji");
    for (const [key, delta] of pending.emoji) {
      const sep = key.indexOf(" ");
      const guildScope = key.slice(0, sep);
      const emojiId = key.slice(sep + 1);
      const row = await emoji.get([guildScope, emojiId]) ?? { guildScope, emojiId, name: delta.name, count: 0 };
      row.count = clamp0(row.count + delta.count);
      row.name = delta.name;
      await emoji.put(row);
      storeWrites++;
    }
    const domains = tx.objectStore("domains");
    for (const [domain, delta] of pending.domains) {
      const row = await domains.get(domain) ?? { domain, count: 0, lastTs: delta.lastTs };
      row.count += delta.count;
      row.lastTs = Math.max(row.lastTs, delta.lastTs);
      await domains.put(row);
      storeWrites++;
    }
    const meta = tx.objectStore("meta");
    await meta.put(opts.now, "lastFlushTs");
    const headEntries = [...opts.heads.entries()].sort((a2, b) => b[1].ts - a2[1].ts).slice(0, MAX_PERSISTED_HEADS);
    await meta.put(headEntries, "channelHeads");
    if (opts.session !== void 0) await meta.put(opts.session, "sessionState");
    await tx.done;
    return { events: pending.events.length, stores: storeWrites };
  }

  // src/db/prune.ts
  async function pruneEvents(db, cutoffTs) {
    const tx = db.transaction("events", "readwrite");
    let removed = 0;
    let cursor = await tx.store.index("ts").openCursor(IDBKeyRange.upperBound(cutoffTs, true));
    while (cursor) {
      await cursor.delete();
      removed++;
      cursor = await cursor.continue();
    }
    await tx.done;
    return removed;
  }
  async function pruneMessages(db, cutoffTs) {
    const tx = db.transaction(["messages", "searchIndex"], "readwrite");
    const searchIndex = tx.objectStore("searchIndex");
    let removed = 0;
    let cursor = await tx.objectStore("messages").index("ts").openCursor(IDBKeyRange.upperBound(cutoffTs, true));
    while (cursor) {
      const row = cursor.value;
      if (row.content !== null) {
        for (const term of new Set(tokenize(row.content))) {
          void searchIndex.delete([term, row.messageId]);
        }
      }
      await cursor.delete();
      removed++;
      cursor = await cursor.continue();
    }
    await tx.done;
    return removed;
  }

  // node_modules/idb/build/index.js
  var instanceOfAny = (object, constructors) => constructors.some((c2) => object instanceof c2);
  var idbProxyableTypes;
  var cursorAdvanceMethods;
  function getIdbProxyableTypes() {
    return idbProxyableTypes || (idbProxyableTypes = [
      IDBDatabase,
      IDBObjectStore,
      IDBIndex,
      IDBCursor,
      IDBTransaction
    ]);
  }
  function getCursorAdvanceMethods() {
    return cursorAdvanceMethods || (cursorAdvanceMethods = [
      IDBCursor.prototype.advance,
      IDBCursor.prototype.continue,
      IDBCursor.prototype.continuePrimaryKey
    ]);
  }
  var transactionDoneMap = /* @__PURE__ */ new WeakMap();
  var transformCache = /* @__PURE__ */ new WeakMap();
  var reverseTransformCache = /* @__PURE__ */ new WeakMap();
  function promisifyRequest(request) {
    const promise = new Promise((resolve, reject) => {
      const unlisten = () => {
        request.removeEventListener("success", success);
        request.removeEventListener("error", error);
      };
      const success = () => {
        resolve(wrap(request.result));
        unlisten();
      };
      const error = () => {
        reject(request.error);
        unlisten();
      };
      request.addEventListener("success", success);
      request.addEventListener("error", error);
    });
    reverseTransformCache.set(promise, request);
    return promise;
  }
  function cacheDonePromiseForTransaction(tx) {
    if (transactionDoneMap.has(tx))
      return;
    const done = new Promise((resolve, reject) => {
      const unlisten = () => {
        tx.removeEventListener("complete", complete);
        tx.removeEventListener("error", error);
        tx.removeEventListener("abort", error);
      };
      const complete = () => {
        resolve();
        unlisten();
      };
      const error = () => {
        reject(tx.error || new DOMException("AbortError", "AbortError"));
        unlisten();
      };
      tx.addEventListener("complete", complete);
      tx.addEventListener("error", error);
      tx.addEventListener("abort", error);
    });
    transactionDoneMap.set(tx, done);
  }
  var idbProxyTraps = {
    get(target, prop, receiver) {
      if (target instanceof IDBTransaction) {
        if (prop === "done")
          return transactionDoneMap.get(target);
        if (prop === "store") {
          return receiver.objectStoreNames[1] ? void 0 : receiver.objectStore(receiver.objectStoreNames[0]);
        }
      }
      return wrap(target[prop]);
    },
    set(target, prop, value2) {
      target[prop] = value2;
      return true;
    },
    has(target, prop) {
      if (target instanceof IDBTransaction && (prop === "done" || prop === "store")) {
        return true;
      }
      return prop in target;
    }
  };
  function replaceTraps(callback) {
    idbProxyTraps = callback(idbProxyTraps);
  }
  function wrapFunction(func) {
    if (getCursorAdvanceMethods().includes(func)) {
      return function(...args) {
        func.apply(unwrap(this), args);
        return wrap(this.request);
      };
    }
    return function(...args) {
      return wrap(func.apply(unwrap(this), args));
    };
  }
  function transformCachableValue(value2) {
    if (typeof value2 === "function")
      return wrapFunction(value2);
    if (value2 instanceof IDBTransaction)
      cacheDonePromiseForTransaction(value2);
    if (instanceOfAny(value2, getIdbProxyableTypes()))
      return new Proxy(value2, idbProxyTraps);
    return value2;
  }
  function wrap(value2) {
    if (value2 instanceof IDBRequest)
      return promisifyRequest(value2);
    if (transformCache.has(value2))
      return transformCache.get(value2);
    const newValue = transformCachableValue(value2);
    if (newValue !== value2) {
      transformCache.set(value2, newValue);
      reverseTransformCache.set(newValue, value2);
    }
    return newValue;
  }
  var unwrap = (value2) => reverseTransformCache.get(value2);
  function openDB(name, version2, { blocked, upgrade, blocking, terminated } = {}) {
    const request = indexedDB.open(name, version2);
    const openPromise = wrap(request);
    if (upgrade) {
      request.addEventListener("upgradeneeded", (event) => {
        upgrade(wrap(request.result), event.oldVersion, event.newVersion, wrap(request.transaction), event);
      });
    }
    if (blocked) {
      request.addEventListener("blocked", (event) => blocked(
        // Casting due to https://github.com/microsoft/TypeScript-DOM-lib-generator/pull/1405
        event.oldVersion,
        event.newVersion,
        event
      ));
    }
    openPromise.then((db) => {
      if (terminated)
        db.addEventListener("close", () => terminated());
      if (blocking) {
        db.addEventListener("versionchange", (event) => blocking(event.oldVersion, event.newVersion, event));
      }
    }).catch(() => {
    });
    return openPromise;
  }
  var readMethods = ["get", "getKey", "getAll", "getAllKeys", "count"];
  var writeMethods = ["put", "add", "delete", "clear"];
  var cachedMethods = /* @__PURE__ */ new Map();
  function getMethod(target, prop) {
    if (!(target instanceof IDBDatabase && !(prop in target) && typeof prop === "string")) {
      return;
    }
    if (cachedMethods.get(prop))
      return cachedMethods.get(prop);
    const targetFuncName = prop.replace(/FromIndex$/, "");
    const useIndex = prop !== targetFuncName;
    const isWrite = writeMethods.includes(targetFuncName);
    if (
      // Bail if the target doesn't exist on the target. Eg, getAll isn't in Edge.
      !(targetFuncName in (useIndex ? IDBIndex : IDBObjectStore).prototype) || !(isWrite || readMethods.includes(targetFuncName))
    ) {
      return;
    }
    const method = async function(storeName, ...args) {
      const tx = this.transaction(storeName, isWrite ? "readwrite" : "readonly");
      let target2 = tx.store;
      if (useIndex)
        target2 = target2.index(args.shift());
      return (await Promise.all([
        target2[targetFuncName](...args),
        isWrite && tx.done
      ]))[0];
    };
    cachedMethods.set(prop, method);
    return method;
  }
  replaceTraps((oldTraps) => ({
    ...oldTraps,
    get: (target, prop, receiver) => getMethod(target, prop) || oldTraps.get(target, prop, receiver),
    has: (target, prop) => !!getMethod(target, prop) || oldTraps.has(target, prop)
  }));
  var advanceMethodProps = ["continue", "continuePrimaryKey", "advance"];
  var methodMap = {};
  var advanceResults = /* @__PURE__ */ new WeakMap();
  var ittrProxiedCursorToOriginalProxy = /* @__PURE__ */ new WeakMap();
  var cursorIteratorTraps = {
    get(target, prop) {
      if (!advanceMethodProps.includes(prop))
        return target[prop];
      let cachedFunc = methodMap[prop];
      if (!cachedFunc) {
        cachedFunc = methodMap[prop] = function(...args) {
          advanceResults.set(this, ittrProxiedCursorToOriginalProxy.get(this)[prop](...args));
        };
      }
      return cachedFunc;
    }
  };
  async function* iterate(...args) {
    let cursor = this;
    if (!(cursor instanceof IDBCursor)) {
      cursor = await cursor.openCursor(...args);
    }
    if (!cursor)
      return;
    cursor = cursor;
    const proxiedCursor = new Proxy(cursor, cursorIteratorTraps);
    ittrProxiedCursorToOriginalProxy.set(proxiedCursor, cursor);
    reverseTransformCache.set(proxiedCursor, unwrap(cursor));
    while (cursor) {
      yield proxiedCursor;
      cursor = await (advanceResults.get(proxiedCursor) || cursor.continue());
      advanceResults.delete(proxiedCursor);
    }
  }
  function isIteratorProp(target, prop) {
    return prop === Symbol.asyncIterator && instanceOfAny(target, [IDBIndex, IDBObjectStore, IDBCursor]) || prop === "iterate" && instanceOfAny(target, [IDBIndex, IDBObjectStore]);
  }
  replaceTraps((oldTraps) => ({
    ...oldTraps,
    get(target, prop, receiver) {
      if (isIteratorProp(target, prop))
        return iterate;
      return oldTraps.get(target, prop, receiver);
    },
    has(target, prop) {
      return isIteratorProp(target, prop) || oldTraps.has(target, prop);
    }
  }));

  // src/db/schema.ts
  var DB_NAME = "retraced";
  var DB_VERSION = 3;
  var ALL_STORES = [
    "meta",
    "events",
    "messages",
    "daily",
    "hourly",
    "peers",
    "voice",
    "sessions",
    "emoji",
    "domains",
    "words",
    "searchIndex"
  ];
  async function openRetracedDb() {
    const db = await openDB(DB_NAME, DB_VERSION, {
      upgrade(database, oldVersion) {
        if (oldVersion < 1) {
          database.createObjectStore("meta");
          const events = database.createObjectStore("events", { keyPath: "id", autoIncrement: true });
          events.createIndex("ts", "ts");
          const messages = database.createObjectStore("messages", { keyPath: "messageId" });
          messages.createIndex("ts", "ts");
          messages.createIndex("channelId", "channelId");
          messages.createIndex("authorId", "authorId");
          const daily = database.createObjectStore("daily", { keyPath: ["date", "channelId"] });
          daily.createIndex("date", "date");
          daily.createIndex("guildId", "guildId");
          database.createObjectStore("hourly", { keyPath: ["date", "hour"] });
          database.createObjectStore("peers", { keyPath: "userId" });
          database.createObjectStore("voice", { keyPath: ["date", "channelId"] });
          database.createObjectStore("emoji", { keyPath: ["guildScope", "emojiId"] });
          database.createObjectStore("domains", { keyPath: "domain" });
        }
        if (oldVersion < 2) {
          database.createObjectStore("sessions", { keyPath: "date" });
        }
        if (oldVersion < 3) {
          const words = database.createObjectStore("words", { keyPath: ["scope", "term"] });
          words.createIndex("byCount", ["scope", "count"]);
          database.createObjectStore("searchIndex", { keyPath: ["term", "messageId"] });
        }
      }
    });
    if (await db.get("meta", "installDate") === void 0) {
      await db.put("meta", Date.now(), "installDate");
    }
    return db;
  }

  // src/db/wipe.ts
  async function wipeAll(db) {
    const tx = db.transaction([...ALL_STORES], "readwrite");
    for (const store of ALL_STORES) {
      void tx.objectStore(store).clear();
    }
    await tx.done;
  }
  async function wipeContentOnly(db) {
    const tx = db.transaction(["messages", "searchIndex", "words", "events"], "readwrite");
    void tx.objectStore("messages").clear();
    void tx.objectStore("searchIndex").clear();
    void tx.objectStore("words").clear();
    let cursor = await tx.objectStore("events").openCursor();
    while (cursor) {
      const row = cursor.value;
      if (typeof row.event.content === "string") {
        const cloned = { ...row, event: { ...row.event } };
        cloned.event.content = null;
        void cursor.update(cloned);
      }
      cursor = await cursor.continue();
    }
    await tx.done;
  }
  async function wipePeer(db, userId) {
    const tx = db.transaction(["peers", "messages", "searchIndex", "events"], "readwrite");
    void tx.objectStore("peers").delete(userId);
    const searchIndex = tx.objectStore("searchIndex");
    let msgCursor = await tx.objectStore("messages").index("authorId").openCursor(IDBKeyRange.only(userId));
    while (msgCursor) {
      const row = msgCursor.value;
      if (row.content !== null) {
        for (const term of new Set(tokenize(row.content))) void searchIndex.delete([term, row.messageId]);
      }
      await msgCursor.delete();
      msgCursor = await msgCursor.continue();
    }
    let evCursor = await tx.objectStore("events").openCursor();
    while (evCursor) {
      const ev = evCursor.value.event;
      if (ev.kind === "message-created" && ev.authorId === userId) await evCursor.delete();
      evCursor = await evCursor.continue();
    }
    await tx.done;
  }
  async function wipeDateRange(db, fromTs, toTs) {
    const fromDate = dateKey(fromTs);
    const toDate = dateKey(toTs);
    const tx = db.transaction(["events", "messages", "searchIndex", "daily", "hourly", "voice", "sessions"], "readwrite");
    let evCursor = await tx.objectStore("events").index("ts").openCursor(IDBKeyRange.bound(fromTs, toTs));
    while (evCursor) {
      await evCursor.delete();
      evCursor = await evCursor.continue();
    }
    const searchIndex = tx.objectStore("searchIndex");
    let msgCursor = await tx.objectStore("messages").index("ts").openCursor(IDBKeyRange.bound(fromTs, toTs));
    while (msgCursor) {
      const row = msgCursor.value;
      if (row.content !== null) {
        for (const term of new Set(tokenize(row.content))) void searchIndex.delete([term, row.messageId]);
      }
      await msgCursor.delete();
      msgCursor = await msgCursor.continue();
    }
    let dailyCursor = await tx.objectStore("daily").index("date").openCursor(IDBKeyRange.bound(fromDate, toDate));
    while (dailyCursor) {
      await dailyCursor.delete();
      dailyCursor = await dailyCursor.continue();
    }
    void tx.objectStore("hourly").delete(IDBKeyRange.bound([fromDate, 0], [toDate, 23]));
    void tx.objectStore("voice").delete(IDBKeyRange.bound([fromDate, ""], [toDate, "￿"]));
    void tx.objectStore("sessions").delete(IDBKeyRange.bound(fromDate, toDate));
    await tx.done;
  }

  // src/env/bd.ts
  var PLUGIN_ID = plugin_meta_default.name;
  var PLUGIN_VERSION = plugin_meta_default.version;
  function bd() {
    return globalThis.BdApi;
  }
  function log(...args) {
    const logger = bd()?.Logger;
    if (logger?.info) logger.info(PLUGIN_ID, ...args);
    else console.log(`[${PLUGIN_ID}]`, ...args);
  }
  function warn(...args) {
    const logger = bd()?.Logger;
    if (logger?.warn) logger.warn(PLUGIN_ID, ...args);
    else console.warn(`[${PLUGIN_ID}]`, ...args);
  }

  // src/lifecycle/disposer.ts
  var Disposer = class {
    fns = [];
    add(fn) {
      this.fns.push(fn);
    }
    dispose() {
      const fns = this.fns;
      this.fns = [];
      for (let i = fns.length - 1; i >= 0; i--) {
        try {
          fns[i]();
        } catch (e) {
          warn("a cleanup step failed (continuing with the rest)", e);
        }
      }
    }
  };

  // src/capture/dwell.ts
  var MIN_DWELL_MS = 1e3;
  var DwellTracker = class {
    constructor(opts) {
      this.opts = opts;
    }
    current = null;
    channelSelected(channelId) {
      const now2 = this.opts.now();
      this.finalize(now2);
      if (channelId) {
        this.current = {
          channelId,
          startTs: now2,
          accumulatedMs: 0,
          focusedSince: this.opts.isFocused() ? now2 : null
        };
      }
    }
    focusChanged(focused) {
      const now2 = this.opts.now();
      const segment = this.current;
      if (!segment) return;
      if (focused && segment.focusedSince === null) {
        segment.focusedSince = now2;
      } else if (!focused && segment.focusedSince !== null) {
        segment.accumulatedMs += now2 - segment.focusedSince;
        segment.focusedSince = null;
      }
    }
    closeAll() {
      this.finalize(this.opts.now());
    }
    finalize(now2) {
      const segment = this.current;
      this.current = null;
      if (!segment) return;
      const total = segment.accumulatedMs + (segment.focusedSince !== null ? now2 - segment.focusedSince : 0);
      if (total < MIN_DWELL_MS) return;
      this.opts.emit({
        kind: "dwell",
        ts: segment.startTs,
        channel: this.opts.resolveChannel(segment.channelId),
        durationMs: total
      });
    }
  };

  // src/capture/hesitation.ts
  var DEFAULT_LAPSE_MS = 12e3;
  var DEFAULT_STOP_GRACE_MS = 5e3;
  var TypingTracker = class {
    constructor(opts) {
      this.opts = opts;
      this.lapseMs = opts.lapseMs ?? DEFAULT_LAPSE_MS;
      this.stopGraceMs = opts.stopGraceMs ?? DEFAULT_STOP_GRACE_MS;
    }
    sessions = /* @__PURE__ */ new Map();
    lapseMs;
    stopGraceMs;
    get openSessionCount() {
      return this.sessions.size;
    }
    ping(channelId) {
      const now2 = this.opts.now();
      const session = this.sessions.get(channelId);
      if (session) {
        session.lastSeen = now2;
        session.stoppedAt = null;
      } else {
        this.sessions.set(channelId, { startedAt: now2, lastSeen: now2, stoppedAt: null });
      }
    }
    stopped(channelId) {
      const session = this.sessions.get(channelId);
      if (session) session.stoppedAt = this.opts.now();
    }
    messageSent(channelId) {
      const session = this.sessions.get(channelId);
      if (!session) return;
      this.sessions.delete(channelId);
      const now2 = this.opts.now();
      this.emit(channelId, session, now2 - session.startedAt, true, now2);
    }
    /** Called on a coarse interval (~3s). Resolves lapsed and stop-expired sessions. */
    sweep() {
      const now2 = this.opts.now();
      for (const [channelId, session] of [...this.sessions]) {
        if (session.stoppedAt !== null && now2 - session.stoppedAt >= this.stopGraceMs) {
          this.sessions.delete(channelId);
          this.emit(channelId, session, Math.max(0, session.stoppedAt - session.startedAt), false, now2);
        } else if (now2 - session.lastSeen >= this.lapseMs) {
          this.sessions.delete(channelId);
          this.emit(channelId, session, Math.max(0, session.lastSeen - session.startedAt), false, now2);
        }
      }
    }
    dispose() {
      this.sessions.clear();
    }
    emit(channelId, session, durationMs, committed, resolvedAt) {
      const channel = this.opts.resolveChannel(channelId);
      this.opts.resolve({
        channel,
        startedAt: session.startedAt,
        durationMs,
        committed,
        peerId: channel.kind === "dm" ? this.opts.resolveDmPeer(channelId) : null,
        resolvedAt
      });
    }
  };

  // src/capture/content-rule.ts
  var DM_TYPE = 1;
  var GROUP_DM_TYPE = 3;
  var THREAD_TYPES = /* @__PURE__ */ new Set([10, 11, 12]);
  var GUILD_TYPES = /* @__PURE__ */ new Set([0, 2, 4, 5, 13, 14, 15, 16]);
  function classifyChannel(channel) {
    const type = channel?.type;
    if (typeof type !== "number") return "unknown";
    if (type === DM_TYPE) return "dm";
    if (type === GROUP_DM_TYPE) return "group-dm";
    if (THREAD_TYPES.has(type)) return "guild-thread";
    if (GUILD_TYPES.has(type)) return "guild";
    return "unknown";
  }
  function isContentStorable(input) {
    return input.isOwn || input.channelKind === "dm" || input.channelKind === "group-dm";
  }

  // src/capture/normalize.ts
  var CHAT_MESSAGE_TYPES = /* @__PURE__ */ new Set([0, 19]);
  var EPHEMERAL_FLAG = 64;
  var CUSTOM_EMOJI_RE2 = /<(a?):(\w+):(\d+)>/g;
  var URL_RE2 = /https?:\/\/[^\s<>"')\]]+/gi;
  var GIF_DOMAINS = /* @__PURE__ */ new Set(["tenor.com", "giphy.com", "gfycat.com"]);
  function analyzeContent(content) {
    const trimmed = content.trim();
    const words = trimmed.length === 0 ? 0 : trimmed.split(/\s+/).length;
    const domains = [];
    for (const match of content.matchAll(URL_RE2)) {
      try {
        let host = new URL(match[0]).hostname.toLowerCase();
        if (host.startsWith("www.")) host = host.slice(4);
        if (host && !domains.includes(host)) domains.push(host);
      } catch {
      }
    }
    const customEmoji = [];
    for (const match of content.matchAll(CUSTOM_EMOJI_RE2)) {
      customEmoji.push({ name: match[2], id: match[3] });
    }
    return { chars: content.length, words, domains, customEmoji };
  }
  function contentTypeFlags(msg, content, domains) {
    const attachments = Array.isArray(msg.attachments) ? msg.attachments : [];
    const embeds = Array.isArray(msg.embeds) ? msg.embeds : [];
    const stickers = Array.isArray(msg.sticker_items ?? msg.stickerItems) ? msg.sticker_items ?? msg.stickerItems : [];
    const attachmentTypes = attachments.map((a2) => String(a2?.content_type ?? a2?.contentType ?? ""));
    const gif = attachmentTypes.some((t) => t === "image/gif") || embeds.some((e) => e?.type === "gifv") || domains.some((d) => GIF_DOMAINS.has(d));
    const image = attachmentTypes.some((t) => t.startsWith("image/") && t !== "image/gif") || embeds.some((e) => e?.type === "image");
    return {
      text: content.trim().length > 0,
      link: domains.length > 0,
      image,
      gif,
      sticker: stickers.length > 0,
      attachment: attachments.length > 0
    };
  }
  function messageTs(msg, ctx) {
    const parsed = typeof msg.timestamp === "string" ? Date.parse(msg.timestamp) : NaN;
    if (Number.isFinite(parsed)) return parsed;
    return snowflakeToTs(msg.id) ?? ctx.now();
  }
  function normalizeMessageCreate(action, ctx) {
    if (action?.optimistic) return null;
    const msg = action?.message;
    if (!msg?.id || !msg?.author?.id) return null;
    if (msg.state === "SENDING") return null;
    const type = typeof msg.type === "number" ? msg.type : 0;
    if (!CHAT_MESSAGE_TYPES.has(type)) return null;
    if (typeof msg.flags === "number" && (msg.flags & EPHEMERAL_FLAG) !== 0) return null;
    const channelId = String(action.channelId ?? msg.channel_id ?? msg.channelId ?? "");
    if (!channelId) return null;
    const channel = ctx.resolveChannel(channelId);
    const authorId = String(msg.author.id);
    const isOwn = authorId === ctx.ownUserId;
    const rawContent = typeof msg.content === "string" ? msg.content : "";
    const analysis = analyzeContent(rawContent);
    const ref = msg.message_reference ?? msg.messageReference;
    const referenced = msg.referenced_message ?? msg.referencedMessage;
    const referencedTs = typeof referenced?.timestamp === "string" ? Date.parse(referenced.timestamp) : NaN;
    return {
      kind: "message-created",
      ts: messageTs(msg, ctx),
      channel,
      messageId: String(msg.id),
      authorId,
      isOwn,
      content: ctx.contentStorageEnabled() && isContentStorable({ isOwn, channelKind: channel.kind }) ? rawContent : null,
      chars: analysis.chars,
      words: analysis.words,
      contentTypes: contentTypeFlags(msg, rawContent, analysis.domains),
      replyToId: ref?.message_id ? String(ref.message_id) : ref?.messageId ? String(ref.messageId) : null,
      replyToAuthorId: referenced?.author?.id ? String(referenced.author.id) : null,
      replyToTs: Number.isFinite(referencedTs) ? referencedTs : null,
      customEmoji: analysis.customEmoji,
      domains: analysis.domains
    };
  }
  function normalizeMessageUpdate(action, ctx) {
    const msg = action?.message;
    if (!msg?.id) return null;
    const editedRaw = msg.edited_timestamp ?? msg.editedTimestamp;
    const editedTs = typeof editedRaw === "string" ? Date.parse(editedRaw) : NaN;
    if (!Number.isFinite(editedTs)) return null;
    const channelId = String(action.channelId ?? msg.channel_id ?? msg.channelId ?? "");
    if (!channelId) return null;
    const channel = ctx.resolveChannel(channelId);
    const authorId = msg.author?.id ? String(msg.author.id) : null;
    const isOwn = authorId === ctx.ownUserId;
    const rawContent = typeof msg.content === "string" ? msg.content : null;
    const originalTs = typeof msg.timestamp === "string" ? Date.parse(msg.timestamp) : snowflakeToTs(msg.id) ?? NaN;
    return {
      kind: "message-edited",
      ts: editedTs,
      channel,
      messageId: String(msg.id),
      authorId,
      isOwn,
      content: rawContent !== null && ctx.contentStorageEnabled() && isContentStorable({ isOwn, channelKind: channel.kind }) ? rawContent : null,
      editLatencyMs: Number.isFinite(originalTs) ? Math.max(0, editedTs - originalTs) : null
    };
  }
  function normalizeMessageDelete(action, ctx) {
    const messageId = action?.id ?? action?.messageId;
    const channelId = action?.channelId ?? action?.channel_id;
    if (!messageId || !channelId) return null;
    return {
      kind: "message-deleted",
      ts: ctx.now(),
      messageId: String(messageId),
      channelId: String(channelId),
      guildId: action?.guildId ?? action?.guild_id ?? null
    };
  }
  function normalizeReaction(action, direction, ctx) {
    const channelId = action?.channelId ?? action?.channel_id;
    const messageId = action?.messageId ?? action?.message_id;
    const actorId = action?.userId ?? action?.user_id;
    const emoji = action?.emoji;
    if (!channelId || !messageId || !actorId || typeof emoji?.name !== "string") return null;
    const messageAuthorId = action?.messageAuthorId ?? action?.message_author_id ?? null;
    return {
      kind: "reaction",
      ts: ctx.now(),
      channel: ctx.resolveChannel(String(channelId)),
      direction,
      actorId: String(actorId),
      actorIsOwn: String(actorId) === ctx.ownUserId,
      messageId: String(messageId),
      messageAuthorId: messageAuthorId ? String(messageAuthorId) : null,
      messageAuthorIsOwn: messageAuthorId ? String(messageAuthorId) === ctx.ownUserId : false,
      emoji: { id: emoji.id ? String(emoji.id) : null, name: emoji.name }
    };
  }

  // src/capture/voice.ts
  var MIN_SEGMENT_MS = 1e3;
  var VoiceTracker = class {
    constructor(opts) {
      this.opts = opts;
    }
    session = null;
    handleVoiceStates(states) {
      const now2 = this.opts.now();
      for (const state of states) {
        if (!state?.userId) continue;
        if (state.userId === this.opts.ownUserId) this.handleOwnState(state, now2);
        else this.handleOtherState(state, now2);
      }
    }
    closeAll() {
      this.closeSession(this.opts.now());
    }
    handleOwnState(state, now2) {
      const currentChannel = this.session?.channelId ?? null;
      if (state.channelId === currentChannel) return;
      this.closeSession(now2);
      if (!state.channelId) return;
      const present = /* @__PURE__ */ new Map();
      for (const member of this.opts.getChannelMembers(state.channelId) ?? []) {
        if (member !== this.opts.ownUserId) present.set(member, now2);
      }
      this.session = { channelId: state.channelId, startTs: now2, present, accumulatedMs: /* @__PURE__ */ new Map() };
    }
    handleOtherState(state, now2) {
      const session = this.session;
      if (!session) return;
      const inMyChannel = state.channelId === session.channelId;
      const tracked = session.present.has(state.userId);
      if (inMyChannel && !tracked) {
        session.present.set(state.userId, now2);
      } else if (!inMyChannel && tracked) {
        const since = session.present.get(state.userId);
        session.present.delete(state.userId);
        session.accumulatedMs.set(state.userId, (session.accumulatedMs.get(state.userId) ?? 0) + (now2 - since));
      }
    }
    closeSession(now2) {
      const session = this.session;
      this.session = null;
      if (!session) return;
      const totalMs = now2 - session.startTs;
      if (totalMs < MIN_SEGMENT_MS) return;
      const coPresent = {};
      for (const [userId, ms] of session.accumulatedMs) {
        coPresent[userId] = (coPresent[userId] ?? 0) + ms;
      }
      for (const [userId, since] of session.present) {
        coPresent[userId] = (coPresent[userId] ?? 0) + (now2 - since);
      }
      for (const userId of Object.keys(coPresent)) {
        coPresent[userId] = Math.round(coPresent[userId] / 1e3);
        if (coPresent[userId] === 0) delete coPresent[userId];
      }
      this.opts.emit({
        kind: "voice-segment",
        ts: session.startTs,
        channel: this.opts.resolveChannel(session.channelId),
        seconds: Math.round(totalMs / 1e3),
        coPresent
      });
    }
  };

  // src/capture/engine.ts
  var EDIT_LRU_MAX = 2048;
  var freshCounters = () => ({
    byKind: {},
    total: 0,
    buffered: 0,
    flushedEvents: 0,
    flushes: 0,
    dropped: 0,
    openTypingSessions: 0,
    lastFlushTs: null
  });
  var CaptureEngine = class {
    hooks;
    settings;
    now;
    flushDebounceMs;
    sweepIntervalMs;
    pruneIntervalMs;
    db = null;
    disposer = null;
    status = "idle";
    statusDetail = null;
    userId = null;
    reducerCtx = null;
    normCtx = null;
    typing = null;
    dwell = null;
    voice = null;
    hydratedHeads = /* @__PURE__ */ new Map();
    hydratedSession = null;
    pending = new PendingBuffer();
    counters = freshCounters();
    editLru = /* @__PURE__ */ new Map();
    flushTimer = null;
    /** flushes are serialized on this chain; flushNow() always awaits completion */
    flushChain = Promise.resolve();
    stopping = false;
    listeners = /* @__PURE__ */ new Set();
    snapshot;
    constructor(opts) {
      this.hooks = opts.hooks;
      this.settings = opts.settings;
      this.now = opts.now ?? (() => Date.now());
      this.flushDebounceMs = opts.flushDebounceMs ?? 5e3;
      this.sweepIntervalMs = opts.sweepIntervalMs ?? 3e3;
      this.pruneIntervalMs = opts.pruneIntervalMs ?? 6 * 36e5;
      this.snapshot = { status: this.status, statusDetail: null, session: this.counters };
    }
    async start() {
      if (this.disposer) return;
      const d = this.disposer = new Disposer();
      this.setStatus("starting", null);
      if (typeof indexedDB === "undefined") {
        this.setStatus("unavailable", "IndexedDB is not available — capture disabled");
        return;
      }
      try {
        this.db = await openRetracedDb();
      } catch (e) {
        warn("could not open the retraced database — capture disabled", e);
        this.setStatus("unavailable", "database unavailable — capture disabled");
        return;
      }
      d.add(() => {
        this.db?.close();
        this.db = null;
      });
      try {
        void navigator?.storage?.persist?.();
      } catch {
      }
      await this.prune();
      await this.hydrateHeads();
      this.userId = this.hooks.getOwnUserId();
      if (this.userId) this.beginCapture(this.userId);
      const missing = [];
      const sub = (type, handler) => {
        const unsubscribe = this.hooks.subscribeDispatch(type, handler);
        if (unsubscribe) d.add(unsubscribe);
        else missing.push(type);
      };
      sub("MESSAGE_CREATE", (a2) => this.onMessageCreate(a2));
      sub("MESSAGE_UPDATE", (a2) => this.onMessageUpdate(a2));
      sub("MESSAGE_DELETE", (a2) => this.onMessageDelete(a2));
      sub("MESSAGE_REACTION_ADD", (a2) => this.onReaction(a2, 1));
      sub("MESSAGE_REACTION_REMOVE", (a2) => this.onReaction(a2, -1));
      sub("CHANNEL_SELECT", (a2) => this.dwell?.channelSelected(a2?.channelId ?? a2?.channel_id ?? null));
      sub("TYPING_START", (a2) => {
        if (this.userId && String(a2?.userId ?? a2?.user_id ?? "") === this.userId) {
          this.typing?.ping(String(a2?.channelId ?? a2?.channel_id ?? ""));
        }
      });
      sub("VOICE_STATE_UPDATES", (a2) => this.onVoiceStates(a2?.voiceStates ?? []));
      sub("VOICE_STATE_UPDATE", (a2) => this.onVoiceStates([a2?.voiceState ?? a2]));
      sub("CONNECTION_OPEN", () => void this.ensureUser());
      const unpatch = this.hooks.patchTyping(
        (channelId) => this.typing?.ping(channelId),
        (channelId) => this.typing?.stopped(channelId)
      );
      if (unpatch) d.add(unpatch);
      else missing.push("typing patch");
      const sweep = setInterval(() => {
        this.typing?.sweep();
        this.bumpTypingGauge();
      }, this.sweepIntervalMs);
      d.add(() => clearInterval(sweep));
      const prune = setInterval(() => void this.prune(), this.pruneIntervalMs);
      d.add(() => clearInterval(prune));
      if (typeof window !== "undefined") {
        const onFocus = () => this.dwell?.focusChanged(true);
        const onBlur = () => this.dwell?.focusChanged(false);
        const onUnload = () => void this.flushNow();
        window.addEventListener("focus", onFocus);
        window.addEventListener("blur", onBlur);
        window.addEventListener("beforeunload", onUnload);
        d.add(() => {
          window.removeEventListener("focus", onFocus);
          window.removeEventListener("blur", onBlur);
          window.removeEventListener("beforeunload", onUnload);
        });
      }
      if (missing.length >= 9) {
        this.setStatus("degraded", "Discord dispatcher not found — capture disabled until a plugin update");
      } else if (!this.userId) {
        this.setStatus("degraded", "current user not resolved yet — capture paused");
      } else {
        this.setStatus("running", missing.length > 0 ? `unavailable hooks: ${missing.join(", ")}` : null);
      }
    }
    async stop() {
      if (!this.disposer) return;
      this.stopping = true;
      this.typing?.dispose();
      this.dwell?.closeAll();
      this.voice?.closeAll();
      if (this.flushTimer) {
        clearTimeout(this.flushTimer);
        this.flushTimer = null;
      }
      await this.flushNow();
      this.disposer.dispose();
      this.disposer = null;
      this.typing = null;
      this.dwell = null;
      this.voice = null;
      this.reducerCtx = null;
      this.normCtx = null;
      this.userId = null;
      this.counters = freshCounters();
      this.stopping = false;
      this.setStatus("stopped", null);
    }
    subscribe = (listener2) => {
      this.listeners.add(listener2);
      return () => {
        this.listeners.delete(listener2);
      };
    };
    getSnapshot = () => this.snapshot;
    async getRowCounts() {
      if (!this.db) return null;
      const counts = {};
      for (const store of ALL_STORES) counts[store] = await this.db.count(store);
      return counts;
    }
    async getStorageEstimate() {
      try {
        const estimate = await navigator?.storage?.estimate?.();
        return estimate ? { usage: estimate.usage ?? 0, quota: estimate.quota ?? 0 } : null;
      } catch {
        return null;
      }
    }
    /** exposed for the dev tools (synthetic data) and Data-tab work in later phases */
    getDb() {
      return this.db;
    }
    /** read-only settings view for the UI (e.g. the stopword filter) */
    getSettings() {
      return this.settings();
    }
    /**
     * The kill-switch contract (spec §1.2): stop capturing text AND purge what
     * is already stored, without touching the rollups. Flushes first so no
     * buffered content survives in the pending batch.
     */
    async purgeContent() {
      if (!this.db) return;
      await this.flushNow();
      await wipeContentOnly(this.db);
      this.notify();
    }
    flushNow() {
      const run = this.flushChain.then(() => this.doFlush());
      this.flushChain = run.catch(() => void 0);
      return run;
    }
    async doFlush() {
      if (!this.db || this.pending.isEmpty) return;
      const batch = this.pending;
      this.pending = new PendingBuffer();
      try {
        const result = await flushPending(this.db, batch, {
          now: this.now(),
          heads: this.reducerCtx?.heads ?? /* @__PURE__ */ new Map(),
          session: this.reducerCtx ? this.reducerCtx.session : void 0
        });
        this.counters.flushedEvents += result.events;
        this.counters.flushes += 1;
        this.counters.lastFlushTs = this.now();
        this.counters.buffered = this.pending.events.length;
      } catch (e) {
        warn("flush failed — this batch is lost (rollups remain consistent)", e);
      } finally {
        this.notify();
      }
    }
    beginCapture(userId) {
      if (this.reducerCtx) {
        this.reducerCtx.ownUserId = userId;
        return;
      }
      this.reducerCtx = createReducerContext({
        ownUserId: userId,
        settings: () => ({ conversationGapMinutes: this.settings().conversationGapMinutes }),
        resolvePeerLabel: (id) => this.hooks.resolveUser(id),
        dmRecipients: (id) => this.hooks.resolveDmRecipients(id),
        heads: this.hydratedHeads,
        session: this.hydratedSession
      });
      this.normCtx = {
        ownUserId: userId,
        resolveChannel: (id) => this.hooks.resolveChannel(id),
        now: this.now,
        contentStorageEnabled: () => this.settings().contentStorageEnabled
      };
      this.typing = new TypingTracker({
        now: this.now,
        resolve: (r2) => this.ingest({
          kind: "typing-resolved",
          ts: r2.resolvedAt,
          channel: r2.channel,
          startedAt: r2.startedAt,
          durationMs: r2.durationMs,
          committed: r2.committed,
          peerId: r2.peerId
        }),
        resolveChannel: (id) => this.hooks.resolveChannel(id),
        resolveDmPeer: (id) => {
          const recipients = this.hooks.resolveDmRecipients(id);
          return recipients?.length === 1 ? recipients[0] : null;
        }
      });
      this.dwell = new DwellTracker({
        now: this.now,
        emit: (e) => this.ingest(e),
        resolveChannel: (id) => this.hooks.resolveChannel(id),
        isFocused: () => typeof document !== "undefined" ? document.hasFocus() : true
      });
      this.voice = new VoiceTracker({
        now: this.now,
        emit: (e) => this.ingest(e),
        resolveChannel: (id) => this.hooks.resolveChannel(id),
        ownUserId: userId,
        getChannelMembers: (id) => this.hooks.getVoiceChannelMembers(id)
      });
    }
    ensureUser() {
      if (this.userId) return true;
      this.userId = this.hooks.getOwnUserId();
      if (this.userId) {
        this.beginCapture(this.userId);
        if (this.status === "degraded") this.setStatus("running", null);
        return true;
      }
      this.counters.dropped += 1;
      this.notify();
      return false;
    }
    ingest(ev) {
      if (!this.reducerCtx) return;
      reduceEvent(ev, this.pending, this.reducerCtx);
      this.counters.byKind[ev.kind] = (this.counters.byKind[ev.kind] ?? 0) + 1;
      this.counters.total += 1;
      this.counters.buffered = this.pending.events.length;
      this.scheduleFlush();
      this.notify();
    }
    scheduleFlush() {
      if (this.flushTimer || this.stopping || !this.db) return;
      this.flushTimer = setTimeout(() => {
        this.flushTimer = null;
        void this.flushNow();
      }, this.flushDebounceMs);
    }
    onMessageCreate(action) {
      if (!this.ensureUser() || !this.normCtx) return;
      const ev = normalizeMessageCreate(action, this.normCtx);
      if (!ev) return;
      if (ev.isOwn) this.typing?.messageSent(ev.channel.channelId);
      this.ingest(ev);
      this.bumpTypingGauge();
    }
    onMessageUpdate(action) {
      if (!this.ensureUser() || !this.normCtx) return;
      const ev = normalizeMessageUpdate(action, this.normCtx);
      if (!ev || this.seenEdit(ev.messageId, ev.ts)) return;
      this.ingest(ev);
    }
    onMessageDelete(action) {
      if (!this.ensureUser() || !this.normCtx) return;
      const ev = normalizeMessageDelete(action, this.normCtx);
      if (ev) this.ingest(ev);
    }
    onReaction(action, direction) {
      if (!this.ensureUser() || !this.normCtx) return;
      const ev = normalizeReaction(action, direction, this.normCtx);
      if (ev) this.ingest(ev);
    }
    onVoiceStates(states) {
      if (!this.ensureUser() || !this.voice) return;
      this.voice.handleVoiceStates(
        (Array.isArray(states) ? states : []).map((s) => ({
          userId: String(s?.userId ?? s?.user_id ?? ""),
          channelId: s?.channelId ?? s?.channel_id ?? null
        }))
      );
    }
    seenEdit(messageId, editedTs) {
      if (this.editLru.get(messageId) === editedTs) return true;
      this.editLru.delete(messageId);
      this.editLru.set(messageId, editedTs);
      if (this.editLru.size > EDIT_LRU_MAX) {
        this.editLru.delete(this.editLru.keys().next().value);
      }
      return false;
    }
    async prune() {
      if (!this.db) return;
      try {
        const settings = this.settings();
        await pruneEvents(this.db, this.now() - settings.eventRetentionDays * 864e5);
        if (settings.messagesRetentionDays > 0) {
          await pruneMessages(this.db, this.now() - settings.messagesRetentionDays * 864e5);
        }
      } catch (e) {
        warn("prune failed", e);
      }
    }
    async hydrateHeads() {
      try {
        const stored = await this.db?.get("meta", "channelHeads");
        if (Array.isArray(stored)) this.hydratedHeads = new Map(stored);
        const session = await this.db?.get("meta", "sessionState");
        if (session && typeof session.startTs === "number" && typeof session.lastTs === "number") {
          this.hydratedSession = session;
        }
      } catch (e) {
        warn("could not hydrate channel heads — initiation attribution starts fresh", e);
      }
    }
    bumpTypingGauge() {
      const open = this.typing?.openSessionCount ?? 0;
      if (open !== this.counters.openTypingSessions) {
        this.counters.openTypingSessions = open;
        this.notify();
      }
    }
    setStatus(status, detail) {
      this.status = status;
      this.statusDetail = detail;
      this.notify();
    }
    notify() {
      this.snapshot = {
        status: this.status,
        statusDetail: this.statusDetail,
        session: { ...this.counters, byKind: { ...this.counters.byKind } }
      };
      for (const listener2 of [...this.listeners]) listener2();
    }
  };

  // src/patcher/webpack.ts
  var warned = /* @__PURE__ */ new Set();
  function warnOnce(key, ...args) {
    if (warned.has(key)) return;
    warned.add(key);
    warn(...args);
  }
  function safeGetModule(filter, options, what) {
    try {
      const mod = bd()?.Webpack?.getModule?.(filter, options);
      if (!mod) {
        warnOnce(what, `could not locate ${what} — the dependent feature is disabled until a plugin update catches up with Discord`);
        return null;
      }
      return mod;
    } catch (e) {
      warnOnce(what, `lookup for ${what} threw — the dependent feature is disabled`, e);
      return null;
    }
  }

  // src/patcher/stores.ts
  var UNRESOLVED = Symbol("unresolved");
  function lazy(resolve) {
    let value2 = UNRESOLVED;
    return () => {
      if (value2 === UNRESOLVED) value2 = resolve();
      return value2;
    };
  }
  function findStore(name) {
    try {
      const viaApi = bd()?.Webpack?.getStore?.(name);
      if (viaApi) return viaApi;
    } catch {
    }
    return safeGetModule((m2) => typeof m2?.getName === "function" && m2.getName() === name, { searchExports: true }, `${name}`);
  }

  // src/patcher/discord-hooks.ts
  function createDiscordHooks() {
    const getDispatcher = lazy(
      () => safeGetModule(
        (m2) => typeof m2?.dispatch === "function" && typeof m2?.subscribe === "function" && typeof m2?.unsubscribe === "function",
        { searchExports: true },
        "Flux dispatcher"
      )
    );
    const getUserStore = lazy(() => findStore("UserStore"));
    const getChannelStore = lazy(() => findStore("ChannelStore"));
    const getVoiceStateStore = lazy(() => findStore("VoiceStateStore"));
    return {
      subscribeDispatch(type, handler) {
        const dispatcher = getDispatcher();
        if (!dispatcher) return null;
        const safeHandler = (action) => {
          try {
            handler(action);
          } catch (e) {
            warn(`capture handler for ${type} failed`, e);
          }
        };
        try {
          dispatcher.subscribe(type, safeHandler);
        } catch (e) {
          warn(`could not subscribe to ${type}`, e);
          return null;
        }
        return () => {
          try {
            dispatcher.unsubscribe(type, safeHandler);
          } catch {
          }
        };
      },
      patchTyping(onStart, onStop) {
        const typingModule = safeGetModule(
          (m2) => typeof m2?.startTyping === "function" && typeof m2?.stopTyping === "function",
          { searchExports: true },
          "typing module (startTyping/stopTyping)"
        );
        if (!typingModule) return null;
        const patcher = bd().Patcher;
        const unpatchStart = patcher.after(PLUGIN_ID, typingModule, "startTyping", (_this, args) => {
          const channelId = args?.[0];
          if (channelId) onStart(String(channelId));
        });
        const unpatchStop = patcher.after(PLUGIN_ID, typingModule, "stopTyping", (_this, args) => {
          const channelId = args?.[0];
          if (channelId) onStop(String(channelId));
        });
        return () => {
          try {
            unpatchStart();
            unpatchStop();
          } catch {
          }
        };
      },
      getOwnUserId() {
        try {
          const id = getUserStore()?.getCurrentUser?.()?.id;
          return id ? String(id) : null;
        } catch {
          return null;
        }
      },
      resolveChannel(channelId) {
        try {
          const channel = getChannelStore()?.getChannel?.(channelId);
          let guildId = channel?.guild_id ?? channel?.guildId;
          if (guildId === void 0 && typeof channel?.getGuildId === "function") guildId = channel.getGuildId();
          return {
            channelId,
            guildId: guildId ? String(guildId) : null,
            kind: classifyChannel(channel)
          };
        } catch {
          return { channelId, guildId: null, kind: "unknown" };
        }
      },
      resolveDmRecipients(channelId) {
        try {
          const channel = getChannelStore()?.getChannel?.(channelId);
          const recipients = channel?.recipients ?? channel?.recipientIds;
          if (!Array.isArray(recipients)) return null;
          const ids = recipients.map((r2) => typeof r2 === "string" ? r2 : r2?.id).filter(Boolean).map(String);
          return ids.length > 0 ? ids : null;
        } catch {
          return null;
        }
      },
      resolveUser(userId) {
        try {
          const user = getUserStore()?.getUser?.(userId);
          if (!user) return null;
          return {
            label: user.globalName ?? user.global_name ?? user.username ?? null,
            avatarHash: user.avatar ?? null
          };
        } catch {
          return null;
        }
      },
      getVoiceChannelMembers(channelId) {
        try {
          const states = getVoiceStateStore()?.getVoiceStatesForChannel?.(channelId);
          return states && typeof states === "object" ? Object.keys(states) : null;
        } catch {
          return null;
        }
      }
    };
  }

  // src/patcher/settings-menu.ts
  var SECTION_ID = "retraced";
  function spliceSections(sections, element) {
    if (!Array.isArray(sections)) return sections;
    if (sections.some((s) => s?.section === SECTION_ID)) return sections;
    const entry = [
      { section: "HEADER", label: "Retraced" },
      { section: SECTION_ID, label: "Statistics", element },
      { section: "DIVIDER" }
    ];
    const anchor = sections.findIndex((s) => typeof s?.section === "string" && s.section.toLowerCase() === "logout");
    if (anchor === -1) sections.push(...entry);
    else sections.splice(anchor, 0, ...entry);
    return sections;
  }
  function injectSettingsSection(element) {
    const settingsView = safeGetModule(
      (m2) => typeof m2?.prototype?.getPredicateSections === "function",
      { searchExports: true },
      "user settings sidebar (SettingsView)"
    );
    if (!settingsView) return false;
    bd().Patcher.after(
      PLUGIN_ID,
      settingsView.prototype,
      "getPredicateSections",
      (_this, _args, ret) => spliceSections(ret, element)
    );
    return true;
  }

  // src/settings/index.ts
  var DEFAULT_STOPWORDS = [
    "a",
    "an",
    "the",
    "and",
    "or",
    "but",
    "if",
    "so",
    "as",
    "of",
    "at",
    "by",
    "for",
    "with",
    "about",
    "into",
    "through",
    "to",
    "from",
    "up",
    "down",
    "in",
    "out",
    "on",
    "off",
    "over",
    "under",
    "again",
    "once",
    "here",
    "there",
    "all",
    "any",
    "both",
    "each",
    "few",
    "more",
    "most",
    "other",
    "some",
    "such",
    "no",
    "nor",
    "not",
    "only",
    "own",
    "same",
    "than",
    "too",
    "very",
    "can",
    "will",
    "just",
    "now",
    "i",
    "im",
    "i'm",
    "me",
    "my",
    "we",
    "our",
    "u",
    "you",
    "your",
    "he",
    "him",
    "his",
    "she",
    "her",
    "it",
    "its",
    "it's",
    "they",
    "them",
    "their",
    "what",
    "which",
    "who",
    "this",
    "that",
    "these",
    "those",
    "am",
    "is",
    "are",
    "was",
    "were",
    "be",
    "been",
    "being",
    "have",
    "has",
    "had",
    "do",
    "does",
    "did",
    "dont",
    "don't",
    "yeah",
    "yes",
    "ok",
    "okay",
    "lol",
    "like"
  ];
  var DEFAULT_SETTINGS = Object.freeze({
    schemaVersion: 1,
    contentStorageEnabled: true,
    conversationGapMinutes: 30,
    eventRetentionDays: 30,
    messagesRetentionDays: 0,
    stopWords: [...DEFAULT_STOPWORDS]
  });
  var DATA_KEY = "settings";
  var SettingsStore = class {
    value = { ...DEFAULT_SETTINGS };
    listeners = /* @__PURE__ */ new Set();
    load = () => {
      const raw = bd()?.Data?.load?.(PLUGIN_ID, DATA_KEY);
      this.value = { ...DEFAULT_SETTINGS, ...raw && typeof raw === "object" ? raw : {} };
      return this.value;
    };
    get = () => this.value;
    update = (patch) => {
      this.value = { ...this.value, ...patch };
      bd()?.Data?.save?.(PLUGIN_ID, DATA_KEY, this.value);
      for (const fn of [...this.listeners]) fn(this.value);
    };
    subscribe = (fn) => {
      this.listeners.add(fn);
      return () => {
        this.listeners.delete(fn);
      };
    };
  };

  // src/settings/panel.tsx
  var import_react = __toESM(require_react(), 1);
  var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
  function SettingsPanel({ store, onOpenStats }) {
    const s = (0, import_react.useSyncExternalStore)(store.subscribe, store.get);
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { className: "retraced-settings-panel", children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { className: "retraced-settings-open", children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { type: "button", className: "retraced-button", onClick: onOpenStats, children: "Open statistics page" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { className: "retraced-note", children: [
          "…or press ",
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("kbd", { children: "Ctrl" }),
          "+",
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("kbd", { children: "Shift" }),
          "+",
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("kbd", { children: "H" }),
          " anywhere in Discord."
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
        SwitchRow,
        {
          label: "Store message content",
          note: "Keep message text on this device — DMs from both sides, servers your own messages only. Turning this off also erases already-stored text (search included); counts and statistics survive.",
          checked: s.contentStorageEnabled,
          onChange: (v) => store.update({ contentStorageEnabled: v })
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
        NumberRow,
        {
          label: "Conversation gap (minutes)",
          note: "Silence longer than this starts a new conversation.",
          value: s.conversationGapMinutes,
          min: 5,
          max: 240,
          onChange: (v) => store.update({ conversationGapMinutes: v })
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
        NumberRow,
        {
          label: "Raw event retention (days)",
          note: "How long the rebuildable raw event log is kept. Computed statistics are kept forever.",
          value: s.eventRetentionDays,
          min: 7,
          max: 365,
          onChange: (v) => store.update({ eventRetentionDays: v })
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
        NumberRow,
        {
          label: "Message retention (days)",
          note: "How long stored message text is kept. 0 keeps it forever; statistics survive either way.",
          value: s.messagesRetentionDays,
          min: 0,
          max: 3650,
          onChange: (v) => store.update({ messagesRetentionDays: v })
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StopwordsRow, { value: s.stopWords, onChange: (v) => store.update({ stopWords: v }) })
    ] });
  }
  function StopwordsRow({ value: value2, onChange }) {
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("label", { className: "retraced-row retraced-row--stacked", children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { className: "retraced-row-text", children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { className: "retraced-row-label", children: "Stopwords" }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { className: "retraced-note", children: "Hidden from the most-used-words charts. Comma or space separated; applies immediately, nothing is re-counted." })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
        "textarea",
        {
          className: "retraced-textarea",
          rows: 3,
          defaultValue: value2.join(", "),
          onBlur: (e) => {
            const words = [...new Set(e.currentTarget.value.toLowerCase().split(/[\s,]+/).filter((w) => w.length > 0))];
            onChange(words);
          }
        }
      )
    ] });
  }
  function SwitchRow({ label, note, checked, onChange }) {
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("label", { className: "retraced-row", children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { className: "retraced-row-text", children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { className: "retraced-row-label", children: label }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { className: "retraced-note", children: note })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", { type: "checkbox", checked, onChange: (e) => onChange(e.currentTarget.checked) })
    ] });
  }
  function NumberRow({
    label,
    note,
    value: value2,
    min: min3,
    max: max4,
    onChange
  }) {
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("label", { className: "retraced-row", children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { className: "retraced-row-text", children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { className: "retraced-row-label", children: label }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { className: "retraced-note", children: note })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "number",
          min: min3,
          max: max4,
          value: value2,
          onChange: (e) => {
            const n = Math.round(Number(e.currentTarget.value));
            if (Number.isFinite(n)) onChange(Math.min(max4, Math.max(min3, n)));
          }
        }
      )
    ] });
  }

  // src/patcher/react.ts
  function createRootIn(container) {
    const reactDom = bd()?.ReactDOM;
    if (typeof reactDom?.createRoot === "function") {
      try {
        return reactDom.createRoot(container);
      } catch (e) {
        warn("BdApi.ReactDOM.createRoot threw — trying fallbacks", e);
      }
    }
    const mod = safeGetModule((m2) => typeof m2?.createRoot === "function", { searchExports: true }, "ReactDOM.createRoot");
    if (mod) {
      try {
        return mod.createRoot(container);
      } catch (e) {
        warn("webpack-located createRoot threw — trying legacy render", e);
      }
    }
    if (typeof reactDom?.render === "function") {
      return {
        render: (node) => reactDom.render(node, container),
        unmount: () => void reactDom.unmountComponentAtNode?.(container)
      };
    }
    warn("no usable React root factory — the stats page cannot render");
    return null;
  }

  // src/ui/overlay.tsx
  var OverlayController = class {
    constructor(renderPage) {
      this.renderPage = renderPage;
    }
    el = null;
    root = null;
    onKeyDown = (e) => {
      if (e.key === "Escape") {
        e.preventDefault();
        e.stopPropagation();
        this.close();
      }
    };
    get isOpen() {
      return this.el !== null;
    }
    open() {
      if (this.el) return;
      const el = document.createElement("div");
      el.className = "retraced-overlay";
      el.setAttribute("role", "dialog");
      el.setAttribute("aria-modal", "true");
      (document.getElementById("app-mount") ?? document.body).appendChild(el);
      const root = createRootIn(el);
      if (!root) {
        el.remove();
        return;
      }
      this.el = el;
      this.root = root;
      document.addEventListener("keydown", this.onKeyDown, true);
      root.render(this.renderPage(() => this.close()));
    }
    close() {
      if (!this.el) return;
      const el = this.el;
      const root = this.root;
      this.el = null;
      this.root = null;
      document.removeEventListener("keydown", this.onKeyDown, true);
      queueMicrotask(() => {
        root?.unmount();
        el.remove();
      });
    }
    toggle() {
      if (this.isOpen) this.close();
      else this.open();
    }
  };

  // src/ui/page/StatsPage.tsx
  var import_react84 = __toESM(require_react(), 1);

  // src/ui/page/ContentTab.tsx
  var import_react70 = __toESM(require_react(), 1);

  // src/db/queries.ts
  async function getDailyRange(db, from2, to2) {
    const index2 = db.transaction("daily").store.index("date");
    return from2 === null ? index2.getAll() : index2.getAll(IDBKeyRange.bound(from2, to2));
  }
  async function getHourlyRange(db, from2, to2) {
    const store = db.transaction("hourly").store;
    return from2 === null ? store.getAll() : store.getAll(IDBKeyRange.bound([from2, 0], [to2, 23]));
  }
  async function getAllPeers(db) {
    return db.getAll("peers");
  }
  async function getAllVoice(db) {
    return db.getAll("voice");
  }
  async function getSessionRange(db, from2, to2) {
    const store = db.transaction("sessions").store;
    return from2 === null ? store.getAll() : store.getAll(IDBKeyRange.bound(from2, to2));
  }
  var SIZE_SAMPLE = 20;
  async function getStoreStats(db) {
    const out = [];
    for (const name of db.objectStoreNames) {
      const tx = db.transaction(name);
      const rows = await tx.store.count();
      let sampled = 0;
      let sampleBytes = 0;
      let cursor = await tx.store.openCursor();
      while (cursor && sampled < SIZE_SAMPLE) {
        try {
          sampleBytes += JSON.stringify(cursor.value)?.length ?? 0;
          sampled++;
        } catch {
          sampled++;
        }
        cursor = await cursor.continue();
      }
      out.push({ name, rows, approxBytes: sampled > 0 ? Math.round(sampleBytes / sampled * rows) : 0 });
    }
    return out.sort((a2, b) => b.approxBytes - a2.approxBytes);
  }
  async function getAllEmoji(db) {
    return db.getAll("emoji");
  }
  async function getAllDomains(db) {
    return db.getAll("domains");
  }
  async function getTopTerms(db, scope, opts) {
    const index2 = db.transaction("words").store.index("byCount");
    const range3 = IDBKeyRange.bound([scope, 0], [scope, Number.MAX_SAFE_INTEGER]);
    const out = [];
    let cursor = await index2.openCursor(range3, "prev");
    while (cursor && out.length < opts.limit) {
      const row = cursor.value;
      const isPhrase = row.term.includes(" ");
      if (opts.kind === "phrase" === isPhrase && !(opts.skip?.(row.term) ?? false)) {
        out.push({ term: row.term, count: row.count });
      }
      cursor = await cursor.continue();
    }
    return out;
  }
  var PREFIX_SCAN_CAP = 2e4;
  async function searchMessages(db, opts) {
    const limit = opts.limit ?? 50;
    const trailingExact = /\s$/.test(opts.query);
    const tokens = tokenize(opts.query);
    if (tokens.length === 0) return [];
    const exact = trailingExact ? tokens : tokens.slice(0, -1);
    const prefix2 = trailingExact ? null : tokens[tokens.length - 1];
    const tx = db.transaction(["searchIndex", "messages"]);
    const index2 = tx.objectStore("searchIndex");
    const perTerm = [];
    for (const term of exact) {
      const rows = await index2.getAll(IDBKeyRange.bound([term], [term, "￿"]));
      perTerm.push(new Map(rows.map((r2) => [r2.messageId, r2])));
    }
    if (prefix2 !== null) {
      const rows = await index2.getAll(IDBKeyRange.bound([prefix2], [`${prefix2}￿`]), PREFIX_SCAN_CAP);
      perTerm.push(new Map(rows.map((r2) => [r2.messageId, r2])));
    }
    perTerm.sort((a2, b) => a2.size - b.size);
    const matches3 = [];
    for (const [messageId, posting] of perTerm[0]) {
      if (perTerm.every((m2) => m2.has(messageId))) matches3.push(posting);
    }
    const scoped = matches3.filter(
      (p) => (opts.fromTs === void 0 || p.ts >= opts.fromTs) && (opts.toTs === void 0 || p.ts <= opts.toTs) && (opts.channelId === void 0 || p.channelId === opts.channelId)
    );
    scoped.sort((a2, b) => b.ts - a2.ts);
    const out = [];
    const messages = tx.objectStore("messages");
    for (const posting of scoped.slice(0, limit)) {
      const row = await messages.get(posting.messageId);
      if (row) out.push(row);
    }
    await tx.done;
    return out;
  }

  // src/patcher/labels.ts
  function createLabelResolvers() {
    const getGuildStore = lazy(() => findStore("GuildStore"));
    const getChannelStore = lazy(() => findStore("ChannelStore"));
    const getUserStore = lazy(() => findStore("UserStore"));
    return {
      guildLabel(guildId) {
        try {
          return getGuildStore()?.getGuild?.(guildId)?.name ?? guildId;
        } catch {
          return guildId;
        }
      },
      channelLabel(channelId) {
        try {
          const channel = getChannelStore()?.getChannel?.(channelId);
          if (!channel) return channelId;
          if (channel.type === 1) {
            const recipientId = (channel.recipients ?? [])[0];
            const id = typeof recipientId === "string" ? recipientId : recipientId?.id;
            const user = id ? getUserStore()?.getUser?.(id) : null;
            const name = user?.globalName ?? user?.global_name ?? user?.username;
            return name ? `@${name}` : "a DM";
          }
          if (channel.type === 3) return channel.name || "group chat";
          return channel.name ? `#${channel.name}` : channelId;
        } catch {
          return channelId;
        }
      }
    };
  }

  // src/ui/theme.ts
  var DARK_SERIES = ["#3987e5", "#008300", "#d55181", "#c98500", "#199e70", "#d95926", "#9085e9", "#e66767"];
  var LIGHT_SERIES = ["#2a78d6", "#008300", "#e87ba4", "#eda100", "#1baf7a", "#eb6834", "#4a3aa7", "#e34948"];
  var DARK_CALENDAR = ["#14406e", "#1a5fae", "#2a78d6", "#6da7ec"];
  var LIGHT_CALENDAR = ["#b7d3f6", "#6da7ec", "#2a78d6", "#184f95"];
  var DARK_LENGTH_RAMP = ["#256abf", "#3987e5", "#6da7ec", "#9ec5f4", "#cde2fb"];
  var LIGHT_LENGTH_RAMP = ["#6da7ec", "#3987e5", "#256abf", "#184f95", "#0d366b"];
  function luminanceOf(color2) {
    let r2, g, b;
    const hex2 = color2.match(/^#([0-9a-f]{6})$/i);
    const rgb2 = color2.match(/^rgba?\(\s*(\d+)\s*,\s*(\d+)\s*,\s*(\d+)/i);
    if (hex2) {
      r2 = parseInt(hex2[1].slice(0, 2), 16);
      g = parseInt(hex2[1].slice(2, 4), 16);
      b = parseInt(hex2[1].slice(4, 6), 16);
    } else if (rgb2) {
      [r2, g, b] = [Number(rgb2[1]), Number(rgb2[2]), Number(rgb2[3])];
    } else {
      return null;
    }
    return (0.2126 * r2 + 0.7152 * g + 0.0722 * b) / 255;
  }
  function readChartTheme() {
    let styles = null;
    let mode = "dark";
    try {
      styles = getComputedStyle(document.body);
      const background = styles.getPropertyValue("--background-primary").trim();
      const luminance = background ? luminanceOf(background) : null;
      if (luminance !== null) mode = luminance > 0.5 ? "light" : "dark";
    } catch {
    }
    const cssVar = (name, fallback) => {
      const value2 = styles?.getPropertyValue(name).trim();
      return value2 || fallback;
    };
    const dark = mode === "dark";
    return {
      mode,
      series: dark ? DARK_SERIES : LIGHT_SERIES,
      otherColor: dark ? "#80848e" : "#6d6f78",
      calendarBins: dark ? DARK_CALENDAR : LIGHT_CALENDAR,
      calendarZero: dark ? "rgba(255,255,255,0.055)" : "rgba(6,6,7,0.06)",
      lengthRamp: dark ? DARK_LENGTH_RAMP : LIGHT_LENGTH_RAMP,
      ink: {
        primary: cssVar("--header-primary", dark ? "#f2f3f5" : "#060607"),
        muted: cssVar("--text-muted", dark ? "#949ba4" : "#5c5e66"),
        grid: dark ? "rgba(255,255,255,0.06)" : "rgba(6,6,7,0.08)",
        axis: dark ? "rgba(255,255,255,0.16)" : "rgba(6,6,7,0.16)",
        surface: cssVar("--background-primary", dark ? "#313338" : "#ffffff"),
        card: cssVar("--background-secondary", dark ? "#2b2d31" : "#f2f3f5")
      }
    };
  }
  function colorForSlot(theme, slot) {
    if (slot === -2) return theme.ink.primary;
    if (slot < 0) return theme.otherColor;
    return theme.series[slot % theme.series.length];
  }
  var formatCount = (n) => n.toLocaleString("en-US");
  var formatCompact = (n) => n >= 1e4 ? `${(n / 1e3).toFixed(n >= 1e5 ? 0 : 1)}k` : formatCount(n);
  var formatMinutes = (minutes) => minutes >= 60 ? `${Math.floor(minutes / 60)}h ${minutes % 60}m` : `${minutes}m`;

  // src/ui/charts/BarRowList.tsx
  var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
  function BarRowList({ rows, color: color2, theme }) {
    const max4 = Math.max(...rows.map((r2) => r2.value), 1);
    return /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("div", { className: "retraced-barlist", role: "list", children: rows.map((row) => /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { className: "retraced-barlist-row", role: "listitem", children: [
      /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("span", { className: "retraced-barlist-label", title: row.label, children: [
        row.icon,
        /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("span", { className: "retraced-barlist-text", children: row.label })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("span", { className: "retraced-barlist-track", children: /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("span", { className: "retraced-barlist-bar", style: { width: `${Math.max(2, 100 * row.value / max4)}%`, background: color2 } }) }),
      /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("span", { className: "retraced-barlist-value", style: { color: theme.ink.primary }, children: row.valueLabel ?? formatCount(row.value) })
    ] }, row.key)) });
  }

  // src/ui/charts/ChartCard.tsx
  var import_react3 = __toESM(require_react(), 1);
  var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
  function ChartCard({
    title,
    subtitle,
    legend,
    table,
    empty,
    actions,
    children
  }) {
    const [showTable, setShowTable] = (0, import_react3.useState)(false);
    return /* @__PURE__ */ (0, import_jsx_runtime3.jsxs)("section", { className: "retraced-chart-card", children: [
      /* @__PURE__ */ (0, import_jsx_runtime3.jsxs)("header", { className: "retraced-chart-head", children: [
        /* @__PURE__ */ (0, import_jsx_runtime3.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("h3", { className: "retraced-chart-title", children: title }),
          subtitle ? /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("p", { className: "retraced-note", children: subtitle }) : null
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime3.jsxs)("div", { className: "retraced-card-actions", children: [
          !empty ? actions : null,
          !empty && table ? /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("button", { type: "button", className: "retraced-ghost-button", onClick: () => setShowTable((v) => !v), children: showTable ? "Chart" : "Data" }) : null
        ] })
      ] }),
      !empty && legend && legend.length > 1 ? /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("div", { className: "retraced-legend", children: legend.map((item) => /* @__PURE__ */ (0, import_jsx_runtime3.jsxs)("span", { className: "retraced-legend-pill", children: [
        /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("span", { className: "retraced-legend-dot", style: { background: item.color } }),
        item.label
      ] }, item.label)) }) : null,
      empty ? /* @__PURE__ */ (0, import_jsx_runtime3.jsxs)("div", { className: "retraced-empty", children: [
        /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("span", { className: "retraced-empty-title", children: "Not enough data yet" }),
        /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("span", { className: "retraced-note", children: empty })
      ] }) : showTable && table ? /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("div", { className: "retraced-table-wrap", children: /* @__PURE__ */ (0, import_jsx_runtime3.jsxs)("table", { className: "retraced-table", children: [
        /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("thead", { children: /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("tr", { children: table.columns.map((column) => /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("th", { children: column }, column)) }) }),
        /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("tbody", { children: table.rows.map((row, i) => /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("tr", { children: row.map((cell, j) => /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("td", { children: cell }, j)) }, i)) })
      ] }) }) : children
    ] });
  }
  function useElementWidth(fallback = 760) {
    const [width, setWidth] = (0, import_react3.useState)(fallback);
    const observerRef = (0, import_react3.useRef)(null);
    const attach = (el) => {
      observerRef.current?.disconnect();
      observerRef.current = null;
      if (!el || typeof ResizeObserver === "undefined") return;
      const observer = new ResizeObserver((entries) => {
        const w = entries[0]?.contentRect.width;
        if (w && w > 80) setWidth(Math.floor(w));
      });
      observer.observe(el);
      observerRef.current = observer;
    };
    (0, import_react3.useEffect)(() => () => observerRef.current?.disconnect(), []);
    return [width, attach];
  }

  // node_modules/recharts/es6/container/Surface.js
  var React = __toESM(require_react());
  var import_react6 = __toESM(require_react());

  // node_modules/clsx/dist/clsx.mjs
  function r(e) {
    var t, f, n = "";
    if ("string" == typeof e || "number" == typeof e) n += e;
    else if ("object" == typeof e) if (Array.isArray(e)) {
      var o = e.length;
      for (t = 0; t < o; t++) e[t] && (f = r(e[t])) && (n && (n += " "), n += f);
    } else for (f in e) e[f] && (n && (n += " "), n += f);
    return n;
  }
  function clsx() {
    for (var e, t, f = 0, n = "", o = arguments.length; f < o; f++) (e = arguments[f]) && (t = r(e)) && (n && (n += " "), n += t);
    return n;
  }

  // node_modules/recharts/es6/util/svgPropertiesAndEvents.js
  var import_react5 = __toESM(require_react());

  // node_modules/recharts/es6/util/excludeEventProps.js
  var EventKeys = ["dangerouslySetInnerHTML", "onCopy", "onCopyCapture", "onCut", "onCutCapture", "onPaste", "onPasteCapture", "onCompositionEnd", "onCompositionEndCapture", "onCompositionStart", "onCompositionStartCapture", "onCompositionUpdate", "onCompositionUpdateCapture", "onFocus", "onFocusCapture", "onBlur", "onBlurCapture", "onChange", "onChangeCapture", "onBeforeInput", "onBeforeInputCapture", "onInput", "onInputCapture", "onReset", "onResetCapture", "onSubmit", "onSubmitCapture", "onInvalid", "onInvalidCapture", "onLoad", "onLoadCapture", "onError", "onErrorCapture", "onKeyDown", "onKeyDownCapture", "onKeyPress", "onKeyPressCapture", "onKeyUp", "onKeyUpCapture", "onAbort", "onAbortCapture", "onCanPlay", "onCanPlayCapture", "onCanPlayThrough", "onCanPlayThroughCapture", "onDurationChange", "onDurationChangeCapture", "onEmptied", "onEmptiedCapture", "onEncrypted", "onEncryptedCapture", "onEnded", "onEndedCapture", "onLoadedData", "onLoadedDataCapture", "onLoadedMetadata", "onLoadedMetadataCapture", "onLoadStart", "onLoadStartCapture", "onPause", "onPauseCapture", "onPlay", "onPlayCapture", "onPlaying", "onPlayingCapture", "onProgress", "onProgressCapture", "onRateChange", "onRateChangeCapture", "onSeeked", "onSeekedCapture", "onSeeking", "onSeekingCapture", "onStalled", "onStalledCapture", "onSuspend", "onSuspendCapture", "onTimeUpdate", "onTimeUpdateCapture", "onVolumeChange", "onVolumeChangeCapture", "onWaiting", "onWaitingCapture", "onAuxClick", "onAuxClickCapture", "onClick", "onClickCapture", "onContextMenu", "onContextMenuCapture", "onDoubleClick", "onDoubleClickCapture", "onDrag", "onDragCapture", "onDragEnd", "onDragEndCapture", "onDragEnter", "onDragEnterCapture", "onDragExit", "onDragExitCapture", "onDragLeave", "onDragLeaveCapture", "onDragOver", "onDragOverCapture", "onDragStart", "onDragStartCapture", "onDrop", "onDropCapture", "onMouseDown", "onMouseDownCapture", "onMouseEnter", "onMouseLeave", "onMouseMove", "onMouseMoveCapture", "onMouseOut", "onMouseOutCapture", "onMouseOver", "onMouseOverCapture", "onMouseUp", "onMouseUpCapture", "onSelect", "onSelectCapture", "onTouchCancel", "onTouchCancelCapture", "onTouchEnd", "onTouchEndCapture", "onTouchMove", "onTouchMoveCapture", "onTouchStart", "onTouchStartCapture", "onPointerDown", "onPointerDownCapture", "onPointerMove", "onPointerMoveCapture", "onPointerUp", "onPointerUpCapture", "onPointerCancel", "onPointerCancelCapture", "onPointerEnter", "onPointerEnterCapture", "onPointerLeave", "onPointerLeaveCapture", "onPointerOver", "onPointerOverCapture", "onPointerOut", "onPointerOutCapture", "onGotPointerCapture", "onGotPointerCaptureCapture", "onLostPointerCapture", "onLostPointerCaptureCapture", "onScroll", "onScrollCapture", "onWheel", "onWheelCapture", "onAnimationStart", "onAnimationStartCapture", "onAnimationEnd", "onAnimationEndCapture", "onAnimationIteration", "onAnimationIterationCapture", "onTransitionEnd", "onTransitionEndCapture"];
  function isEventKey(key) {
    if (typeof key !== "string") {
      return false;
    }
    var allowedEventKeys = EventKeys;
    return allowedEventKeys.includes(key);
  }

  // node_modules/recharts/es6/util/svgPropertiesNoEvents.js
  var import_react4 = __toESM(require_react());
  var SVGElementPropKeys = [
    "aria-activedescendant",
    "aria-atomic",
    "aria-autocomplete",
    "aria-busy",
    "aria-checked",
    "aria-colcount",
    "aria-colindex",
    "aria-colspan",
    "aria-controls",
    "aria-current",
    "aria-describedby",
    "aria-details",
    "aria-disabled",
    "aria-errormessage",
    "aria-expanded",
    "aria-flowto",
    "aria-haspopup",
    "aria-hidden",
    "aria-invalid",
    "aria-keyshortcuts",
    "aria-label",
    "aria-labelledby",
    "aria-level",
    "aria-live",
    "aria-modal",
    "aria-multiline",
    "aria-multiselectable",
    "aria-orientation",
    "aria-owns",
    "aria-placeholder",
    "aria-posinset",
    "aria-pressed",
    "aria-readonly",
    "aria-relevant",
    "aria-required",
    "aria-roledescription",
    "aria-rowcount",
    "aria-rowindex",
    "aria-rowspan",
    "aria-selected",
    "aria-setsize",
    "aria-sort",
    "aria-valuemax",
    "aria-valuemin",
    "aria-valuenow",
    "aria-valuetext",
    "className",
    "color",
    "height",
    "id",
    "lang",
    "max",
    "media",
    "method",
    "min",
    "name",
    "style",
    /*
     * removed 'type' SVGElementPropKey because we do not currently use any SVG elements
     * that can use it, and it conflicts with the recharts prop 'type'
     * https://github.com/recharts/recharts/pull/3327
     * https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/type
     */
    // 'type',
    "target",
    "width",
    "role",
    "tabIndex",
    "accentHeight",
    "accumulate",
    "additive",
    "alignmentBaseline",
    "allowReorder",
    "alphabetic",
    "amplitude",
    "arabicForm",
    "ascent",
    "attributeName",
    "attributeType",
    "autoReverse",
    "azimuth",
    "baseFrequency",
    "baselineShift",
    "baseProfile",
    "bbox",
    "begin",
    "bias",
    "by",
    "calcMode",
    "capHeight",
    "clip",
    "clipPath",
    "clipPathUnits",
    "clipRule",
    "colorInterpolation",
    "colorInterpolationFilters",
    "colorProfile",
    "colorRendering",
    "contentScriptType",
    "contentStyleType",
    "cursor",
    "cx",
    "cy",
    "d",
    "decelerate",
    "descent",
    "diffuseConstant",
    "direction",
    "display",
    "divisor",
    "dominantBaseline",
    "dur",
    "dx",
    "dy",
    "edgeMode",
    "elevation",
    "enableBackground",
    "end",
    "exponent",
    "externalResourcesRequired",
    "fill",
    "fillOpacity",
    "fillRule",
    "filter",
    "filterRes",
    "filterUnits",
    "floodColor",
    "floodOpacity",
    "focusable",
    "fontFamily",
    "fontSize",
    "fontSizeAdjust",
    "fontStretch",
    "fontStyle",
    "fontVariant",
    "fontWeight",
    "format",
    "from",
    "fx",
    "fy",
    "g1",
    "g2",
    "glyphName",
    "glyphOrientationHorizontal",
    "glyphOrientationVertical",
    "glyphRef",
    "gradientTransform",
    "gradientUnits",
    "hanging",
    "horizAdvX",
    "horizOriginX",
    "href",
    "ideographic",
    "imageRendering",
    "in2",
    "in",
    "intercept",
    "k1",
    "k2",
    "k3",
    "k4",
    "k",
    "kernelMatrix",
    "kernelUnitLength",
    "kerning",
    "keyPoints",
    "keySplines",
    "keyTimes",
    "lengthAdjust",
    "letterSpacing",
    "lightingColor",
    "limitingConeAngle",
    "local",
    "markerEnd",
    "markerHeight",
    "markerMid",
    "markerStart",
    "markerUnits",
    "markerWidth",
    "mask",
    "maskContentUnits",
    "maskUnits",
    "mathematical",
    "mode",
    "numOctaves",
    "offset",
    "opacity",
    "operator",
    "order",
    "orient",
    "orientation",
    "origin",
    "overflow",
    "overlinePosition",
    "overlineThickness",
    "paintOrder",
    "panose1",
    "pathLength",
    "patternContentUnits",
    "patternTransform",
    "patternUnits",
    "pointerEvents",
    "pointsAtX",
    "pointsAtY",
    "pointsAtZ",
    "preserveAlpha",
    "preserveAspectRatio",
    "primitiveUnits",
    "r",
    "radius",
    "refX",
    "refY",
    "renderingIntent",
    "repeatCount",
    "repeatDur",
    "requiredExtensions",
    "requiredFeatures",
    "restart",
    "result",
    "rotate",
    "rx",
    "ry",
    "seed",
    "shapeRendering",
    "slope",
    "spacing",
    "specularConstant",
    "specularExponent",
    "speed",
    "spreadMethod",
    "startOffset",
    "stdDeviation",
    "stemh",
    "stemv",
    "stitchTiles",
    "stopColor",
    "stopOpacity",
    "strikethroughPosition",
    "strikethroughThickness",
    "string",
    "stroke",
    "strokeDasharray",
    "strokeDashoffset",
    "strokeLinecap",
    "strokeLinejoin",
    "strokeMiterlimit",
    "strokeOpacity",
    "strokeWidth",
    "surfaceScale",
    "systemLanguage",
    "tableValues",
    "targetX",
    "targetY",
    "textAnchor",
    "textDecoration",
    "textLength",
    "textRendering",
    "to",
    "transform",
    "u1",
    "u2",
    "underlinePosition",
    "underlineThickness",
    "unicode",
    "unicodeBidi",
    "unicodeRange",
    "unitsPerEm",
    "vAlphabetic",
    "values",
    "vectorEffect",
    "version",
    "vertAdvY",
    "vertOriginX",
    "vertOriginY",
    "vHanging",
    "vIdeographic",
    "viewTarget",
    "visibility",
    "vMathematical",
    "widths",
    "wordSpacing",
    "writingMode",
    "x1",
    "x2",
    "x",
    "xChannelSelector",
    "xHeight",
    "xlinkActuate",
    "xlinkArcrole",
    "xlinkHref",
    "xlinkRole",
    "xlinkShow",
    "xlinkTitle",
    "xlinkType",
    "xmlBase",
    "xmlLang",
    "xmlns",
    "xmlnsXlink",
    "xmlSpace",
    "y1",
    "y2",
    "y",
    "yChannelSelector",
    "z",
    "zoomAndPan",
    "ref",
    "key",
    "angle"
  ];
  var SVGElementPropKeySet = new Set(SVGElementPropKeys);
  function isSvgElementPropKey(key) {
    if (typeof key !== "string") {
      return false;
    }
    return SVGElementPropKeySet.has(key);
  }
  function isDataAttribute(key) {
    return typeof key === "string" && key.startsWith("data-");
  }
  function svgPropertiesNoEvents(obj) {
    if (typeof obj !== "object" || obj === null) {
      return {};
    }
    var result = {};
    for (var key in obj) {
      if (Object.prototype.hasOwnProperty.call(obj, key)) {
        if (isSvgElementPropKey(key) || isDataAttribute(key)) {
          result[key] = obj[key];
        }
      }
    }
    return result;
  }
  function svgPropertiesNoEventsFromUnknown(input) {
    if (input == null) {
      return null;
    }
    if (/* @__PURE__ */ (0, import_react4.isValidElement)(input) && typeof input.props === "object" && input.props !== null) {
      var p = input.props;
      return svgPropertiesNoEvents(p);
    }
    if (typeof input === "object" && !Array.isArray(input)) {
      return svgPropertiesNoEvents(input);
    }
    return null;
  }

  // node_modules/recharts/es6/util/svgPropertiesAndEvents.js
  function svgPropertiesAndEvents(obj) {
    var result = {};
    for (var key in obj) {
      if (Object.prototype.hasOwnProperty.call(obj, key)) {
        if (isSvgElementPropKey(key) || isDataAttribute(key) || isEventKey(key)) {
          result[key] = obj[key];
        }
      }
    }
    return result;
  }
  function svgPropertiesAndEventsFromUnknown(input) {
    if (input == null) {
      return null;
    }
    if (/* @__PURE__ */ (0, import_react5.isValidElement)(input)) {
      return svgPropertiesAndEvents(input.props);
    }
    if (typeof input === "object" && !Array.isArray(input)) {
      return svgPropertiesAndEvents(input);
    }
    return null;
  }

  // node_modules/recharts/es6/container/Surface.js
  var _excluded = ["children", "width", "height", "viewBox", "className", "style", "title", "desc"];
  function _extends() {
    return _extends = Object.assign ? Object.assign.bind() : function(n) {
      for (var e = 1; e < arguments.length; e++) {
        var t = arguments[e];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends.apply(null, arguments);
  }
  function _objectWithoutProperties(e, t) {
    if (null == e) return {};
    var o, r2, i = _objectWithoutPropertiesLoose(e, t);
    if (Object.getOwnPropertySymbols) {
      var n = Object.getOwnPropertySymbols(e);
      for (r2 = 0; r2 < n.length; r2++) o = n[r2], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
    }
    return i;
  }
  function _objectWithoutPropertiesLoose(r2, e) {
    if (null == r2) return {};
    var t = {};
    for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
      if (-1 !== e.indexOf(n)) continue;
      t[n] = r2[n];
    }
    return t;
  }
  var Surface = /* @__PURE__ */ (0, import_react6.forwardRef)((props, ref) => {
    var children = props.children, width = props.width, height = props.height, viewBox = props.viewBox, className = props.className, style = props.style, title = props.title, desc = props.desc, others = _objectWithoutProperties(props, _excluded);
    var svgView = viewBox || {
      width,
      height,
      x: 0,
      y: 0
    };
    var layerClass = clsx("recharts-surface", className);
    return /* @__PURE__ */ React.createElement("svg", _extends({}, svgPropertiesAndEvents(others), {
      className: layerClass,
      width,
      height,
      style,
      viewBox: "".concat(svgView.x, " ").concat(svgView.y, " ").concat(svgView.width, " ").concat(svgView.height),
      ref
    }), /* @__PURE__ */ React.createElement("title", null, title), /* @__PURE__ */ React.createElement("desc", null, desc), children);
  });

  // node_modules/recharts/es6/container/Layer.js
  var React2 = __toESM(require_react());
  var _excluded2 = ["children", "className"];
  function _extends2() {
    return _extends2 = Object.assign ? Object.assign.bind() : function(n) {
      for (var e = 1; e < arguments.length; e++) {
        var t = arguments[e];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends2.apply(null, arguments);
  }
  function _objectWithoutProperties2(e, t) {
    if (null == e) return {};
    var o, r2, i = _objectWithoutPropertiesLoose2(e, t);
    if (Object.getOwnPropertySymbols) {
      var n = Object.getOwnPropertySymbols(e);
      for (r2 = 0; r2 < n.length; r2++) o = n[r2], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
    }
    return i;
  }
  function _objectWithoutPropertiesLoose2(r2, e) {
    if (null == r2) return {};
    var t = {};
    for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
      if (-1 !== e.indexOf(n)) continue;
      t[n] = r2[n];
    }
    return t;
  }
  var Layer = /* @__PURE__ */ React2.forwardRef((props, ref) => {
    var children = props.children, className = props.className, others = _objectWithoutProperties2(props, _excluded2);
    var layerClass = clsx("recharts-layer", className);
    return /* @__PURE__ */ React2.createElement("g", _extends2({
      className: layerClass
    }, svgPropertiesAndEvents(others), {
      ref
    }), children);
  });

  // node_modules/recharts/es6/context/legendPortalContext.js
  var import_react7 = __toESM(require_react());
  var LegendPortalContext = /* @__PURE__ */ (0, import_react7.createContext)(null);

  // node_modules/d3-shape/src/constant.js
  function constant_default(x5) {
    return function constant2() {
      return x5;
    };
  }

  // node_modules/d3-shape/src/math.js
  var abs = Math.abs;
  var atan2 = Math.atan2;
  var cos = Math.cos;
  var max = Math.max;
  var min = Math.min;
  var sin = Math.sin;
  var sqrt = Math.sqrt;
  var epsilon = 1e-12;
  var pi = Math.PI;
  var halfPi = pi / 2;
  var tau = 2 * pi;
  function acos(x5) {
    return x5 > 1 ? 0 : x5 < -1 ? pi : Math.acos(x5);
  }
  function asin(x5) {
    return x5 >= 1 ? halfPi : x5 <= -1 ? -halfPi : Math.asin(x5);
  }

  // node_modules/d3-path/src/path.js
  var pi2 = Math.PI;
  var tau2 = 2 * pi2;
  var epsilon2 = 1e-6;
  var tauEpsilon = tau2 - epsilon2;
  function append(strings) {
    this._ += strings[0];
    for (let i = 1, n = strings.length; i < n; ++i) {
      this._ += arguments[i] + strings[i];
    }
  }
  function appendRound(digits) {
    let d = Math.floor(digits);
    if (!(d >= 0)) throw new Error(`invalid digits: ${digits}`);
    if (d > 15) return append;
    const k = 10 ** d;
    return function(strings) {
      this._ += strings[0];
      for (let i = 1, n = strings.length; i < n; ++i) {
        this._ += Math.round(arguments[i] * k) / k + strings[i];
      }
    };
  }
  var Path = class {
    constructor(digits) {
      this._x0 = this._y0 = // start of current subpath
      this._x1 = this._y1 = null;
      this._ = "";
      this._append = digits == null ? append : appendRound(digits);
    }
    moveTo(x5, y5) {
      this._append`M${this._x0 = this._x1 = +x5},${this._y0 = this._y1 = +y5}`;
    }
    closePath() {
      if (this._x1 !== null) {
        this._x1 = this._x0, this._y1 = this._y0;
        this._append`Z`;
      }
    }
    lineTo(x5, y5) {
      this._append`L${this._x1 = +x5},${this._y1 = +y5}`;
    }
    quadraticCurveTo(x1, y1, x5, y5) {
      this._append`Q${+x1},${+y1},${this._x1 = +x5},${this._y1 = +y5}`;
    }
    bezierCurveTo(x1, y1, x22, y22, x5, y5) {
      this._append`C${+x1},${+y1},${+x22},${+y22},${this._x1 = +x5},${this._y1 = +y5}`;
    }
    arcTo(x1, y1, x22, y22, r2) {
      x1 = +x1, y1 = +y1, x22 = +x22, y22 = +y22, r2 = +r2;
      if (r2 < 0) throw new Error(`negative radius: ${r2}`);
      let x0 = this._x1, y0 = this._y1, x21 = x22 - x1, y21 = y22 - y1, x01 = x0 - x1, y01 = y0 - y1, l01_2 = x01 * x01 + y01 * y01;
      if (this._x1 === null) {
        this._append`M${this._x1 = x1},${this._y1 = y1}`;
      } else if (!(l01_2 > epsilon2)) ;
      else if (!(Math.abs(y01 * x21 - y21 * x01) > epsilon2) || !r2) {
        this._append`L${this._x1 = x1},${this._y1 = y1}`;
      } else {
        let x20 = x22 - x0, y20 = y22 - y0, l21_2 = x21 * x21 + y21 * y21, l20_2 = x20 * x20 + y20 * y20, l21 = Math.sqrt(l21_2), l01 = Math.sqrt(l01_2), l = r2 * Math.tan((pi2 - Math.acos((l21_2 + l01_2 - l20_2) / (2 * l21 * l01))) / 2), t01 = l / l01, t21 = l / l21;
        if (Math.abs(t01 - 1) > epsilon2) {
          this._append`L${x1 + t01 * x01},${y1 + t01 * y01}`;
        }
        this._append`A${r2},${r2},0,0,${+(y01 * x20 > x01 * y20)},${this._x1 = x1 + t21 * x21},${this._y1 = y1 + t21 * y21}`;
      }
    }
    arc(x5, y5, r2, a0, a1, ccw) {
      x5 = +x5, y5 = +y5, r2 = +r2, ccw = !!ccw;
      if (r2 < 0) throw new Error(`negative radius: ${r2}`);
      let dx = r2 * Math.cos(a0), dy = r2 * Math.sin(a0), x0 = x5 + dx, y0 = y5 + dy, cw = 1 ^ ccw, da = ccw ? a0 - a1 : a1 - a0;
      if (this._x1 === null) {
        this._append`M${x0},${y0}`;
      } else if (Math.abs(this._x1 - x0) > epsilon2 || Math.abs(this._y1 - y0) > epsilon2) {
        this._append`L${x0},${y0}`;
      }
      if (!r2) return;
      if (da < 0) da = da % tau2 + tau2;
      if (da > tauEpsilon) {
        this._append`A${r2},${r2},0,1,${cw},${x5 - dx},${y5 - dy}A${r2},${r2},0,1,${cw},${this._x1 = x0},${this._y1 = y0}`;
      } else if (da > epsilon2) {
        this._append`A${r2},${r2},0,${+(da >= pi2)},${cw},${this._x1 = x5 + r2 * Math.cos(a1)},${this._y1 = y5 + r2 * Math.sin(a1)}`;
      }
    }
    rect(x5, y5, w, h) {
      this._append`M${this._x0 = this._x1 = +x5},${this._y0 = this._y1 = +y5}h${w = +w}v${+h}h${-w}Z`;
    }
    toString() {
      return this._;
    }
  };
  function path() {
    return new Path();
  }
  path.prototype = Path.prototype;

  // node_modules/d3-shape/src/path.js
  function withPath(shape) {
    let digits = 3;
    shape.digits = function(_) {
      if (!arguments.length) return digits;
      if (_ == null) {
        digits = null;
      } else {
        const d = Math.floor(_);
        if (!(d >= 0)) throw new RangeError(`invalid digits: ${_}`);
        digits = d;
      }
      return shape;
    };
    return () => new Path(digits);
  }

  // node_modules/d3-shape/src/arc.js
  function arcInnerRadius(d) {
    return d.innerRadius;
  }
  function arcOuterRadius(d) {
    return d.outerRadius;
  }
  function arcStartAngle(d) {
    return d.startAngle;
  }
  function arcEndAngle(d) {
    return d.endAngle;
  }
  function arcPadAngle(d) {
    return d && d.padAngle;
  }
  function intersect(x0, y0, x1, y1, x22, y22, x32, y32) {
    var x10 = x1 - x0, y10 = y1 - y0, x322 = x32 - x22, y322 = y32 - y22, t = y322 * x10 - x322 * y10;
    if (t * t < epsilon) return;
    t = (x322 * (y0 - y22) - y322 * (x0 - x22)) / t;
    return [x0 + t * x10, y0 + t * y10];
  }
  function cornerTangents(x0, y0, x1, y1, r1, rc, cw) {
    var x01 = x0 - x1, y01 = y0 - y1, lo = (cw ? rc : -rc) / sqrt(x01 * x01 + y01 * y01), ox = lo * y01, oy = -lo * x01, x11 = x0 + ox, y11 = y0 + oy, x10 = x1 + ox, y10 = y1 + oy, x00 = (x11 + x10) / 2, y00 = (y11 + y10) / 2, dx = x10 - x11, dy = y10 - y11, d2 = dx * dx + dy * dy, r2 = r1 - rc, D = x11 * y10 - x10 * y11, d = (dy < 0 ? -1 : 1) * sqrt(max(0, r2 * r2 * d2 - D * D)), cx0 = (D * dy - dx * d) / d2, cy0 = (-D * dx - dy * d) / d2, cx1 = (D * dy + dx * d) / d2, cy1 = (-D * dx + dy * d) / d2, dx0 = cx0 - x00, dy0 = cy0 - y00, dx1 = cx1 - x00, dy1 = cy1 - y00;
    if (dx0 * dx0 + dy0 * dy0 > dx1 * dx1 + dy1 * dy1) cx0 = cx1, cy0 = cy1;
    return {
      cx: cx0,
      cy: cy0,
      x01: -ox,
      y01: -oy,
      x11: cx0 * (r1 / r2 - 1),
      y11: cy0 * (r1 / r2 - 1)
    };
  }
  function arc_default() {
    var innerRadius = arcInnerRadius, outerRadius = arcOuterRadius, cornerRadius = constant_default(0), padRadius = null, startAngle = arcStartAngle, endAngle = arcEndAngle, padAngle = arcPadAngle, context = null, path3 = withPath(arc);
    function arc() {
      var buffer, r2, r0 = +innerRadius.apply(this, arguments), r1 = +outerRadius.apply(this, arguments), a0 = startAngle.apply(this, arguments) - halfPi, a1 = endAngle.apply(this, arguments) - halfPi, da = abs(a1 - a0), cw = a1 > a0;
      if (!context) context = buffer = path3();
      if (r1 < r0) r2 = r1, r1 = r0, r0 = r2;
      if (!(r1 > epsilon)) context.moveTo(0, 0);
      else if (da > tau - epsilon) {
        context.moveTo(r1 * cos(a0), r1 * sin(a0));
        context.arc(0, 0, r1, a0, a1, !cw);
        if (r0 > epsilon) {
          context.moveTo(r0 * cos(a1), r0 * sin(a1));
          context.arc(0, 0, r0, a1, a0, cw);
        }
      } else {
        var a01 = a0, a11 = a1, a00 = a0, a10 = a1, da0 = da, da1 = da, ap = padAngle.apply(this, arguments) / 2, rp = ap > epsilon && (padRadius ? +padRadius.apply(this, arguments) : sqrt(r0 * r0 + r1 * r1)), rc = min(abs(r1 - r0) / 2, +cornerRadius.apply(this, arguments)), rc0 = rc, rc1 = rc, t02, t12;
        if (rp > epsilon) {
          var p0 = asin(rp / r0 * sin(ap)), p1 = asin(rp / r1 * sin(ap));
          if ((da0 -= p0 * 2) > epsilon) p0 *= cw ? 1 : -1, a00 += p0, a10 -= p0;
          else da0 = 0, a00 = a10 = (a0 + a1) / 2;
          if ((da1 -= p1 * 2) > epsilon) p1 *= cw ? 1 : -1, a01 += p1, a11 -= p1;
          else da1 = 0, a01 = a11 = (a0 + a1) / 2;
        }
        var x01 = r1 * cos(a01), y01 = r1 * sin(a01), x10 = r0 * cos(a10), y10 = r0 * sin(a10);
        if (rc > epsilon) {
          var x11 = r1 * cos(a11), y11 = r1 * sin(a11), x00 = r0 * cos(a00), y00 = r0 * sin(a00), oc;
          if (da < pi) {
            if (oc = intersect(x01, y01, x00, y00, x11, y11, x10, y10)) {
              var ax = x01 - oc[0], ay = y01 - oc[1], bx = x11 - oc[0], by = y11 - oc[1], kc = 1 / sin(acos((ax * bx + ay * by) / (sqrt(ax * ax + ay * ay) * sqrt(bx * bx + by * by))) / 2), lc = sqrt(oc[0] * oc[0] + oc[1] * oc[1]);
              rc0 = min(rc, (r0 - lc) / (kc - 1));
              rc1 = min(rc, (r1 - lc) / (kc + 1));
            } else {
              rc0 = rc1 = 0;
            }
          }
        }
        if (!(da1 > epsilon)) context.moveTo(x01, y01);
        else if (rc1 > epsilon) {
          t02 = cornerTangents(x00, y00, x01, y01, r1, rc1, cw);
          t12 = cornerTangents(x11, y11, x10, y10, r1, rc1, cw);
          context.moveTo(t02.cx + t02.x01, t02.cy + t02.y01);
          if (rc1 < rc) context.arc(t02.cx, t02.cy, rc1, atan2(t02.y01, t02.x01), atan2(t12.y01, t12.x01), !cw);
          else {
            context.arc(t02.cx, t02.cy, rc1, atan2(t02.y01, t02.x01), atan2(t02.y11, t02.x11), !cw);
            context.arc(0, 0, r1, atan2(t02.cy + t02.y11, t02.cx + t02.x11), atan2(t12.cy + t12.y11, t12.cx + t12.x11), !cw);
            context.arc(t12.cx, t12.cy, rc1, atan2(t12.y11, t12.x11), atan2(t12.y01, t12.x01), !cw);
          }
        } else context.moveTo(x01, y01), context.arc(0, 0, r1, a01, a11, !cw);
        if (!(r0 > epsilon) || !(da0 > epsilon)) context.lineTo(x10, y10);
        else if (rc0 > epsilon) {
          t02 = cornerTangents(x10, y10, x11, y11, r0, -rc0, cw);
          t12 = cornerTangents(x01, y01, x00, y00, r0, -rc0, cw);
          context.lineTo(t02.cx + t02.x01, t02.cy + t02.y01);
          if (rc0 < rc) context.arc(t02.cx, t02.cy, rc0, atan2(t02.y01, t02.x01), atan2(t12.y01, t12.x01), !cw);
          else {
            context.arc(t02.cx, t02.cy, rc0, atan2(t02.y01, t02.x01), atan2(t02.y11, t02.x11), !cw);
            context.arc(0, 0, r0, atan2(t02.cy + t02.y11, t02.cx + t02.x11), atan2(t12.cy + t12.y11, t12.cx + t12.x11), cw);
            context.arc(t12.cx, t12.cy, rc0, atan2(t12.y11, t12.x11), atan2(t12.y01, t12.x01), !cw);
          }
        } else context.arc(0, 0, r0, a10, a00, cw);
      }
      context.closePath();
      if (buffer) return context = null, buffer + "" || null;
    }
    arc.centroid = function() {
      var r2 = (+innerRadius.apply(this, arguments) + +outerRadius.apply(this, arguments)) / 2, a2 = (+startAngle.apply(this, arguments) + +endAngle.apply(this, arguments)) / 2 - pi / 2;
      return [cos(a2) * r2, sin(a2) * r2];
    };
    arc.innerRadius = function(_) {
      return arguments.length ? (innerRadius = typeof _ === "function" ? _ : constant_default(+_), arc) : innerRadius;
    };
    arc.outerRadius = function(_) {
      return arguments.length ? (outerRadius = typeof _ === "function" ? _ : constant_default(+_), arc) : outerRadius;
    };
    arc.cornerRadius = function(_) {
      return arguments.length ? (cornerRadius = typeof _ === "function" ? _ : constant_default(+_), arc) : cornerRadius;
    };
    arc.padRadius = function(_) {
      return arguments.length ? (padRadius = _ == null ? null : typeof _ === "function" ? _ : constant_default(+_), arc) : padRadius;
    };
    arc.startAngle = function(_) {
      return arguments.length ? (startAngle = typeof _ === "function" ? _ : constant_default(+_), arc) : startAngle;
    };
    arc.endAngle = function(_) {
      return arguments.length ? (endAngle = typeof _ === "function" ? _ : constant_default(+_), arc) : endAngle;
    };
    arc.padAngle = function(_) {
      return arguments.length ? (padAngle = typeof _ === "function" ? _ : constant_default(+_), arc) : padAngle;
    };
    arc.context = function(_) {
      return arguments.length ? (context = _ == null ? null : _, arc) : context;
    };
    return arc;
  }

  // node_modules/d3-shape/src/array.js
  var slice = Array.prototype.slice;
  function array_default(x5) {
    return typeof x5 === "object" && "length" in x5 ? x5 : Array.from(x5);
  }

  // node_modules/d3-shape/src/curve/linear.js
  function Linear(context) {
    this._context = context;
  }
  Linear.prototype = {
    areaStart: function() {
      this._line = 0;
    },
    areaEnd: function() {
      this._line = NaN;
    },
    lineStart: function() {
      this._point = 0;
    },
    lineEnd: function() {
      if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
      this._line = 1 - this._line;
    },
    point: function(x5, y5) {
      x5 = +x5, y5 = +y5;
      switch (this._point) {
        case 0:
          this._point = 1;
          this._line ? this._context.lineTo(x5, y5) : this._context.moveTo(x5, y5);
          break;
        case 1:
          this._point = 2;
        // falls through
        default:
          this._context.lineTo(x5, y5);
          break;
      }
    }
  };
  function linear_default(context) {
    return new Linear(context);
  }

  // node_modules/d3-shape/src/point.js
  function x(p) {
    return p[0];
  }
  function y(p) {
    return p[1];
  }

  // node_modules/d3-shape/src/line.js
  function line_default(x5, y5) {
    var defined2 = constant_default(true), context = null, curve = linear_default, output = null, path3 = withPath(line);
    x5 = typeof x5 === "function" ? x5 : x5 === void 0 ? x : constant_default(x5);
    y5 = typeof y5 === "function" ? y5 : y5 === void 0 ? y : constant_default(y5);
    function line(data) {
      var i, n = (data = array_default(data)).length, d, defined0 = false, buffer;
      if (context == null) output = curve(buffer = path3());
      for (i = 0; i <= n; ++i) {
        if (!(i < n && defined2(d = data[i], i, data)) === defined0) {
          if (defined0 = !defined0) output.lineStart();
          else output.lineEnd();
        }
        if (defined0) output.point(+x5(d, i, data), +y5(d, i, data));
      }
      if (buffer) return output = null, buffer + "" || null;
    }
    line.x = function(_) {
      return arguments.length ? (x5 = typeof _ === "function" ? _ : constant_default(+_), line) : x5;
    };
    line.y = function(_) {
      return arguments.length ? (y5 = typeof _ === "function" ? _ : constant_default(+_), line) : y5;
    };
    line.defined = function(_) {
      return arguments.length ? (defined2 = typeof _ === "function" ? _ : constant_default(!!_), line) : defined2;
    };
    line.curve = function(_) {
      return arguments.length ? (curve = _, context != null && (output = curve(context)), line) : curve;
    };
    line.context = function(_) {
      return arguments.length ? (_ == null ? context = output = null : output = curve(context = _), line) : context;
    };
    return line;
  }

  // node_modules/d3-shape/src/area.js
  function area_default(x0, y0, y1) {
    var x1 = null, defined2 = constant_default(true), context = null, curve = linear_default, output = null, path3 = withPath(area);
    x0 = typeof x0 === "function" ? x0 : x0 === void 0 ? x : constant_default(+x0);
    y0 = typeof y0 === "function" ? y0 : y0 === void 0 ? constant_default(0) : constant_default(+y0);
    y1 = typeof y1 === "function" ? y1 : y1 === void 0 ? y : constant_default(+y1);
    function area(data) {
      var i, j, k, n = (data = array_default(data)).length, d, defined0 = false, buffer, x0z = new Array(n), y0z = new Array(n);
      if (context == null) output = curve(buffer = path3());
      for (i = 0; i <= n; ++i) {
        if (!(i < n && defined2(d = data[i], i, data)) === defined0) {
          if (defined0 = !defined0) {
            j = i;
            output.areaStart();
            output.lineStart();
          } else {
            output.lineEnd();
            output.lineStart();
            for (k = i - 1; k >= j; --k) {
              output.point(x0z[k], y0z[k]);
            }
            output.lineEnd();
            output.areaEnd();
          }
        }
        if (defined0) {
          x0z[i] = +x0(d, i, data), y0z[i] = +y0(d, i, data);
          output.point(x1 ? +x1(d, i, data) : x0z[i], y1 ? +y1(d, i, data) : y0z[i]);
        }
      }
      if (buffer) return output = null, buffer + "" || null;
    }
    function arealine() {
      return line_default().defined(defined2).curve(curve).context(context);
    }
    area.x = function(_) {
      return arguments.length ? (x0 = typeof _ === "function" ? _ : constant_default(+_), x1 = null, area) : x0;
    };
    area.x0 = function(_) {
      return arguments.length ? (x0 = typeof _ === "function" ? _ : constant_default(+_), area) : x0;
    };
    area.x1 = function(_) {
      return arguments.length ? (x1 = _ == null ? null : typeof _ === "function" ? _ : constant_default(+_), area) : x1;
    };
    area.y = function(_) {
      return arguments.length ? (y0 = typeof _ === "function" ? _ : constant_default(+_), y1 = null, area) : y0;
    };
    area.y0 = function(_) {
      return arguments.length ? (y0 = typeof _ === "function" ? _ : constant_default(+_), area) : y0;
    };
    area.y1 = function(_) {
      return arguments.length ? (y1 = _ == null ? null : typeof _ === "function" ? _ : constant_default(+_), area) : y1;
    };
    area.lineX0 = area.lineY0 = function() {
      return arealine().x(x0).y(y0);
    };
    area.lineY1 = function() {
      return arealine().x(x0).y(y1);
    };
    area.lineX1 = function() {
      return arealine().x(x1).y(y0);
    };
    area.defined = function(_) {
      return arguments.length ? (defined2 = typeof _ === "function" ? _ : constant_default(!!_), area) : defined2;
    };
    area.curve = function(_) {
      return arguments.length ? (curve = _, context != null && (output = curve(context)), area) : curve;
    };
    area.context = function(_) {
      return arguments.length ? (_ == null ? context = output = null : output = curve(context = _), area) : context;
    };
    return area;
  }

  // node_modules/d3-shape/src/curve/radial.js
  var curveRadialLinear = curveRadial(linear_default);
  function Radial(curve) {
    this._curve = curve;
  }
  Radial.prototype = {
    areaStart: function() {
      this._curve.areaStart();
    },
    areaEnd: function() {
      this._curve.areaEnd();
    },
    lineStart: function() {
      this._curve.lineStart();
    },
    lineEnd: function() {
      this._curve.lineEnd();
    },
    point: function(a2, r2) {
      this._curve.point(r2 * Math.sin(a2), r2 * -Math.cos(a2));
    }
  };
  function curveRadial(curve) {
    function radial2(context) {
      return new Radial(curve(context));
    }
    radial2._curve = curve;
    return radial2;
  }

  // node_modules/d3-shape/src/lineRadial.js
  function lineRadial(l) {
    var c2 = l.curve;
    l.angle = l.x, delete l.x;
    l.radius = l.y, delete l.y;
    l.curve = function(_) {
      return arguments.length ? c2(curveRadial(_)) : c2()._curve;
    };
    return l;
  }
  function lineRadial_default() {
    return lineRadial(line_default().curve(curveRadialLinear));
  }

  // node_modules/d3-shape/src/curve/bump.js
  var Bump = class {
    constructor(context, x5) {
      this._context = context;
      this._x = x5;
    }
    areaStart() {
      this._line = 0;
    }
    areaEnd() {
      this._line = NaN;
    }
    lineStart() {
      this._point = 0;
    }
    lineEnd() {
      if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
      this._line = 1 - this._line;
    }
    point(x5, y5) {
      x5 = +x5, y5 = +y5;
      switch (this._point) {
        case 0: {
          this._point = 1;
          if (this._line) this._context.lineTo(x5, y5);
          else this._context.moveTo(x5, y5);
          break;
        }
        case 1:
          this._point = 2;
        // falls through
        default: {
          if (this._x) this._context.bezierCurveTo(this._x0 = (this._x0 + x5) / 2, this._y0, this._x0, y5, x5, y5);
          else this._context.bezierCurveTo(this._x0, this._y0 = (this._y0 + y5) / 2, x5, this._y0, x5, y5);
          break;
        }
      }
      this._x0 = x5, this._y0 = y5;
    }
  };
  function bumpX(context) {
    return new Bump(context, true);
  }
  function bumpY(context) {
    return new Bump(context, false);
  }

  // node_modules/d3-shape/src/noop.js
  function noop_default() {
  }

  // node_modules/d3-shape/src/curve/basis.js
  function point(that, x5, y5) {
    that._context.bezierCurveTo(
      (2 * that._x0 + that._x1) / 3,
      (2 * that._y0 + that._y1) / 3,
      (that._x0 + 2 * that._x1) / 3,
      (that._y0 + 2 * that._y1) / 3,
      (that._x0 + 4 * that._x1 + x5) / 6,
      (that._y0 + 4 * that._y1 + y5) / 6
    );
  }
  function Basis(context) {
    this._context = context;
  }
  Basis.prototype = {
    areaStart: function() {
      this._line = 0;
    },
    areaEnd: function() {
      this._line = NaN;
    },
    lineStart: function() {
      this._x0 = this._x1 = this._y0 = this._y1 = NaN;
      this._point = 0;
    },
    lineEnd: function() {
      switch (this._point) {
        case 3:
          point(this, this._x1, this._y1);
        // falls through
        case 2:
          this._context.lineTo(this._x1, this._y1);
          break;
      }
      if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
      this._line = 1 - this._line;
    },
    point: function(x5, y5) {
      x5 = +x5, y5 = +y5;
      switch (this._point) {
        case 0:
          this._point = 1;
          this._line ? this._context.lineTo(x5, y5) : this._context.moveTo(x5, y5);
          break;
        case 1:
          this._point = 2;
          break;
        case 2:
          this._point = 3;
          this._context.lineTo((5 * this._x0 + this._x1) / 6, (5 * this._y0 + this._y1) / 6);
        // falls through
        default:
          point(this, x5, y5);
          break;
      }
      this._x0 = this._x1, this._x1 = x5;
      this._y0 = this._y1, this._y1 = y5;
    }
  };
  function basis_default(context) {
    return new Basis(context);
  }

  // node_modules/d3-shape/src/curve/basisClosed.js
  function BasisClosed(context) {
    this._context = context;
  }
  BasisClosed.prototype = {
    areaStart: noop_default,
    areaEnd: noop_default,
    lineStart: function() {
      this._x0 = this._x1 = this._x2 = this._x3 = this._x4 = this._y0 = this._y1 = this._y2 = this._y3 = this._y4 = NaN;
      this._point = 0;
    },
    lineEnd: function() {
      switch (this._point) {
        case 1: {
          this._context.moveTo(this._x2, this._y2);
          this._context.closePath();
          break;
        }
        case 2: {
          this._context.moveTo((this._x2 + 2 * this._x3) / 3, (this._y2 + 2 * this._y3) / 3);
          this._context.lineTo((this._x3 + 2 * this._x2) / 3, (this._y3 + 2 * this._y2) / 3);
          this._context.closePath();
          break;
        }
        case 3: {
          this.point(this._x2, this._y2);
          this.point(this._x3, this._y3);
          this.point(this._x4, this._y4);
          break;
        }
      }
    },
    point: function(x5, y5) {
      x5 = +x5, y5 = +y5;
      switch (this._point) {
        case 0:
          this._point = 1;
          this._x2 = x5, this._y2 = y5;
          break;
        case 1:
          this._point = 2;
          this._x3 = x5, this._y3 = y5;
          break;
        case 2:
          this._point = 3;
          this._x4 = x5, this._y4 = y5;
          this._context.moveTo((this._x0 + 4 * this._x1 + x5) / 6, (this._y0 + 4 * this._y1 + y5) / 6);
          break;
        default:
          point(this, x5, y5);
          break;
      }
      this._x0 = this._x1, this._x1 = x5;
      this._y0 = this._y1, this._y1 = y5;
    }
  };
  function basisClosed_default(context) {
    return new BasisClosed(context);
  }

  // node_modules/d3-shape/src/curve/basisOpen.js
  function BasisOpen(context) {
    this._context = context;
  }
  BasisOpen.prototype = {
    areaStart: function() {
      this._line = 0;
    },
    areaEnd: function() {
      this._line = NaN;
    },
    lineStart: function() {
      this._x0 = this._x1 = this._y0 = this._y1 = NaN;
      this._point = 0;
    },
    lineEnd: function() {
      if (this._line || this._line !== 0 && this._point === 3) this._context.closePath();
      this._line = 1 - this._line;
    },
    point: function(x5, y5) {
      x5 = +x5, y5 = +y5;
      switch (this._point) {
        case 0:
          this._point = 1;
          break;
        case 1:
          this._point = 2;
          break;
        case 2:
          this._point = 3;
          var x0 = (this._x0 + 4 * this._x1 + x5) / 6, y0 = (this._y0 + 4 * this._y1 + y5) / 6;
          this._line ? this._context.lineTo(x0, y0) : this._context.moveTo(x0, y0);
          break;
        case 3:
          this._point = 4;
        // falls through
        default:
          point(this, x5, y5);
          break;
      }
      this._x0 = this._x1, this._x1 = x5;
      this._y0 = this._y1, this._y1 = y5;
    }
  };
  function basisOpen_default(context) {
    return new BasisOpen(context);
  }

  // node_modules/d3-shape/src/curve/cardinal.js
  function point2(that, x5, y5) {
    that._context.bezierCurveTo(
      that._x1 + that._k * (that._x2 - that._x0),
      that._y1 + that._k * (that._y2 - that._y0),
      that._x2 + that._k * (that._x1 - x5),
      that._y2 + that._k * (that._y1 - y5),
      that._x2,
      that._y2
    );
  }
  function Cardinal(context, tension) {
    this._context = context;
    this._k = (1 - tension) / 6;
  }
  Cardinal.prototype = {
    areaStart: function() {
      this._line = 0;
    },
    areaEnd: function() {
      this._line = NaN;
    },
    lineStart: function() {
      this._x0 = this._x1 = this._x2 = this._y0 = this._y1 = this._y2 = NaN;
      this._point = 0;
    },
    lineEnd: function() {
      switch (this._point) {
        case 2:
          this._context.lineTo(this._x2, this._y2);
          break;
        case 3:
          point2(this, this._x1, this._y1);
          break;
      }
      if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
      this._line = 1 - this._line;
    },
    point: function(x5, y5) {
      x5 = +x5, y5 = +y5;
      switch (this._point) {
        case 0:
          this._point = 1;
          this._line ? this._context.lineTo(x5, y5) : this._context.moveTo(x5, y5);
          break;
        case 1:
          this._point = 2;
          this._x1 = x5, this._y1 = y5;
          break;
        case 2:
          this._point = 3;
        // falls through
        default:
          point2(this, x5, y5);
          break;
      }
      this._x0 = this._x1, this._x1 = this._x2, this._x2 = x5;
      this._y0 = this._y1, this._y1 = this._y2, this._y2 = y5;
    }
  };
  var cardinal_default = (function custom(tension) {
    function cardinal(context) {
      return new Cardinal(context, tension);
    }
    cardinal.tension = function(tension2) {
      return custom(+tension2);
    };
    return cardinal;
  })(0);

  // node_modules/d3-shape/src/curve/cardinalClosed.js
  function CardinalClosed(context, tension) {
    this._context = context;
    this._k = (1 - tension) / 6;
  }
  CardinalClosed.prototype = {
    areaStart: noop_default,
    areaEnd: noop_default,
    lineStart: function() {
      this._x0 = this._x1 = this._x2 = this._x3 = this._x4 = this._x5 = this._y0 = this._y1 = this._y2 = this._y3 = this._y4 = this._y5 = NaN;
      this._point = 0;
    },
    lineEnd: function() {
      switch (this._point) {
        case 1: {
          this._context.moveTo(this._x3, this._y3);
          this._context.closePath();
          break;
        }
        case 2: {
          this._context.lineTo(this._x3, this._y3);
          this._context.closePath();
          break;
        }
        case 3: {
          this.point(this._x3, this._y3);
          this.point(this._x4, this._y4);
          this.point(this._x5, this._y5);
          break;
        }
      }
    },
    point: function(x5, y5) {
      x5 = +x5, y5 = +y5;
      switch (this._point) {
        case 0:
          this._point = 1;
          this._x3 = x5, this._y3 = y5;
          break;
        case 1:
          this._point = 2;
          this._context.moveTo(this._x4 = x5, this._y4 = y5);
          break;
        case 2:
          this._point = 3;
          this._x5 = x5, this._y5 = y5;
          break;
        default:
          point2(this, x5, y5);
          break;
      }
      this._x0 = this._x1, this._x1 = this._x2, this._x2 = x5;
      this._y0 = this._y1, this._y1 = this._y2, this._y2 = y5;
    }
  };
  var cardinalClosed_default = (function custom2(tension) {
    function cardinal(context) {
      return new CardinalClosed(context, tension);
    }
    cardinal.tension = function(tension2) {
      return custom2(+tension2);
    };
    return cardinal;
  })(0);

  // node_modules/d3-shape/src/curve/catmullRom.js
  function point3(that, x5, y5) {
    var x1 = that._x1, y1 = that._y1, x22 = that._x2, y22 = that._y2;
    if (that._l01_a > epsilon) {
      var a2 = 2 * that._l01_2a + 3 * that._l01_a * that._l12_a + that._l12_2a, n = 3 * that._l01_a * (that._l01_a + that._l12_a);
      x1 = (x1 * a2 - that._x0 * that._l12_2a + that._x2 * that._l01_2a) / n;
      y1 = (y1 * a2 - that._y0 * that._l12_2a + that._y2 * that._l01_2a) / n;
    }
    if (that._l23_a > epsilon) {
      var b = 2 * that._l23_2a + 3 * that._l23_a * that._l12_a + that._l12_2a, m2 = 3 * that._l23_a * (that._l23_a + that._l12_a);
      x22 = (x22 * b + that._x1 * that._l23_2a - x5 * that._l12_2a) / m2;
      y22 = (y22 * b + that._y1 * that._l23_2a - y5 * that._l12_2a) / m2;
    }
    that._context.bezierCurveTo(x1, y1, x22, y22, that._x2, that._y2);
  }
  function CatmullRom(context, alpha) {
    this._context = context;
    this._alpha = alpha;
  }
  CatmullRom.prototype = {
    areaStart: function() {
      this._line = 0;
    },
    areaEnd: function() {
      this._line = NaN;
    },
    lineStart: function() {
      this._x0 = this._x1 = this._x2 = this._y0 = this._y1 = this._y2 = NaN;
      this._l01_a = this._l12_a = this._l23_a = this._l01_2a = this._l12_2a = this._l23_2a = this._point = 0;
    },
    lineEnd: function() {
      switch (this._point) {
        case 2:
          this._context.lineTo(this._x2, this._y2);
          break;
        case 3:
          this.point(this._x2, this._y2);
          break;
      }
      if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
      this._line = 1 - this._line;
    },
    point: function(x5, y5) {
      x5 = +x5, y5 = +y5;
      if (this._point) {
        var x23 = this._x2 - x5, y23 = this._y2 - y5;
        this._l23_a = Math.sqrt(this._l23_2a = Math.pow(x23 * x23 + y23 * y23, this._alpha));
      }
      switch (this._point) {
        case 0:
          this._point = 1;
          this._line ? this._context.lineTo(x5, y5) : this._context.moveTo(x5, y5);
          break;
        case 1:
          this._point = 2;
          break;
        case 2:
          this._point = 3;
        // falls through
        default:
          point3(this, x5, y5);
          break;
      }
      this._l01_a = this._l12_a, this._l12_a = this._l23_a;
      this._l01_2a = this._l12_2a, this._l12_2a = this._l23_2a;
      this._x0 = this._x1, this._x1 = this._x2, this._x2 = x5;
      this._y0 = this._y1, this._y1 = this._y2, this._y2 = y5;
    }
  };
  var catmullRom_default = (function custom3(alpha) {
    function catmullRom(context) {
      return alpha ? new CatmullRom(context, alpha) : new Cardinal(context, 0);
    }
    catmullRom.alpha = function(alpha2) {
      return custom3(+alpha2);
    };
    return catmullRom;
  })(0.5);

  // node_modules/d3-shape/src/curve/catmullRomClosed.js
  function CatmullRomClosed(context, alpha) {
    this._context = context;
    this._alpha = alpha;
  }
  CatmullRomClosed.prototype = {
    areaStart: noop_default,
    areaEnd: noop_default,
    lineStart: function() {
      this._x0 = this._x1 = this._x2 = this._x3 = this._x4 = this._x5 = this._y0 = this._y1 = this._y2 = this._y3 = this._y4 = this._y5 = NaN;
      this._l01_a = this._l12_a = this._l23_a = this._l01_2a = this._l12_2a = this._l23_2a = this._point = 0;
    },
    lineEnd: function() {
      switch (this._point) {
        case 1: {
          this._context.moveTo(this._x3, this._y3);
          this._context.closePath();
          break;
        }
        case 2: {
          this._context.lineTo(this._x3, this._y3);
          this._context.closePath();
          break;
        }
        case 3: {
          this.point(this._x3, this._y3);
          this.point(this._x4, this._y4);
          this.point(this._x5, this._y5);
          break;
        }
      }
    },
    point: function(x5, y5) {
      x5 = +x5, y5 = +y5;
      if (this._point) {
        var x23 = this._x2 - x5, y23 = this._y2 - y5;
        this._l23_a = Math.sqrt(this._l23_2a = Math.pow(x23 * x23 + y23 * y23, this._alpha));
      }
      switch (this._point) {
        case 0:
          this._point = 1;
          this._x3 = x5, this._y3 = y5;
          break;
        case 1:
          this._point = 2;
          this._context.moveTo(this._x4 = x5, this._y4 = y5);
          break;
        case 2:
          this._point = 3;
          this._x5 = x5, this._y5 = y5;
          break;
        default:
          point3(this, x5, y5);
          break;
      }
      this._l01_a = this._l12_a, this._l12_a = this._l23_a;
      this._l01_2a = this._l12_2a, this._l12_2a = this._l23_2a;
      this._x0 = this._x1, this._x1 = this._x2, this._x2 = x5;
      this._y0 = this._y1, this._y1 = this._y2, this._y2 = y5;
    }
  };
  var catmullRomClosed_default = (function custom4(alpha) {
    function catmullRom(context) {
      return alpha ? new CatmullRomClosed(context, alpha) : new CardinalClosed(context, 0);
    }
    catmullRom.alpha = function(alpha2) {
      return custom4(+alpha2);
    };
    return catmullRom;
  })(0.5);

  // node_modules/d3-shape/src/curve/linearClosed.js
  function LinearClosed(context) {
    this._context = context;
  }
  LinearClosed.prototype = {
    areaStart: noop_default,
    areaEnd: noop_default,
    lineStart: function() {
      this._point = 0;
    },
    lineEnd: function() {
      if (this._point) this._context.closePath();
    },
    point: function(x5, y5) {
      x5 = +x5, y5 = +y5;
      if (this._point) this._context.lineTo(x5, y5);
      else this._point = 1, this._context.moveTo(x5, y5);
    }
  };
  function linearClosed_default(context) {
    return new LinearClosed(context);
  }

  // node_modules/d3-shape/src/curve/monotone.js
  function sign(x5) {
    return x5 < 0 ? -1 : 1;
  }
  function slope3(that, x22, y22) {
    var h0 = that._x1 - that._x0, h1 = x22 - that._x1, s0 = (that._y1 - that._y0) / (h0 || h1 < 0 && -0), s1 = (y22 - that._y1) / (h1 || h0 < 0 && -0), p = (s0 * h1 + s1 * h0) / (h0 + h1);
    return (sign(s0) + sign(s1)) * Math.min(Math.abs(s0), Math.abs(s1), 0.5 * Math.abs(p)) || 0;
  }
  function slope2(that, t) {
    var h = that._x1 - that._x0;
    return h ? (3 * (that._y1 - that._y0) / h - t) / 2 : t;
  }
  function point4(that, t02, t12) {
    var x0 = that._x0, y0 = that._y0, x1 = that._x1, y1 = that._y1, dx = (x1 - x0) / 3;
    that._context.bezierCurveTo(x0 + dx, y0 + dx * t02, x1 - dx, y1 - dx * t12, x1, y1);
  }
  function MonotoneX(context) {
    this._context = context;
  }
  MonotoneX.prototype = {
    areaStart: function() {
      this._line = 0;
    },
    areaEnd: function() {
      this._line = NaN;
    },
    lineStart: function() {
      this._x0 = this._x1 = this._y0 = this._y1 = this._t0 = NaN;
      this._point = 0;
    },
    lineEnd: function() {
      switch (this._point) {
        case 2:
          this._context.lineTo(this._x1, this._y1);
          break;
        case 3:
          point4(this, this._t0, slope2(this, this._t0));
          break;
      }
      if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
      this._line = 1 - this._line;
    },
    point: function(x5, y5) {
      var t12 = NaN;
      x5 = +x5, y5 = +y5;
      if (x5 === this._x1 && y5 === this._y1) return;
      switch (this._point) {
        case 0:
          this._point = 1;
          this._line ? this._context.lineTo(x5, y5) : this._context.moveTo(x5, y5);
          break;
        case 1:
          this._point = 2;
          break;
        case 2:
          this._point = 3;
          point4(this, slope2(this, t12 = slope3(this, x5, y5)), t12);
          break;
        default:
          point4(this, this._t0, t12 = slope3(this, x5, y5));
          break;
      }
      this._x0 = this._x1, this._x1 = x5;
      this._y0 = this._y1, this._y1 = y5;
      this._t0 = t12;
    }
  };
  function MonotoneY(context) {
    this._context = new ReflectContext(context);
  }
  (MonotoneY.prototype = Object.create(MonotoneX.prototype)).point = function(x5, y5) {
    MonotoneX.prototype.point.call(this, y5, x5);
  };
  function ReflectContext(context) {
    this._context = context;
  }
  ReflectContext.prototype = {
    moveTo: function(x5, y5) {
      this._context.moveTo(y5, x5);
    },
    closePath: function() {
      this._context.closePath();
    },
    lineTo: function(x5, y5) {
      this._context.lineTo(y5, x5);
    },
    bezierCurveTo: function(x1, y1, x22, y22, x5, y5) {
      this._context.bezierCurveTo(y1, x1, y22, x22, y5, x5);
    }
  };
  function monotoneX(context) {
    return new MonotoneX(context);
  }
  function monotoneY(context) {
    return new MonotoneY(context);
  }

  // node_modules/d3-shape/src/curve/natural.js
  function Natural(context) {
    this._context = context;
  }
  Natural.prototype = {
    areaStart: function() {
      this._line = 0;
    },
    areaEnd: function() {
      this._line = NaN;
    },
    lineStart: function() {
      this._x = [];
      this._y = [];
    },
    lineEnd: function() {
      var x5 = this._x, y5 = this._y, n = x5.length;
      if (n) {
        this._line ? this._context.lineTo(x5[0], y5[0]) : this._context.moveTo(x5[0], y5[0]);
        if (n === 2) {
          this._context.lineTo(x5[1], y5[1]);
        } else {
          var px = controlPoints(x5), py = controlPoints(y5);
          for (var i0 = 0, i1 = 1; i1 < n; ++i0, ++i1) {
            this._context.bezierCurveTo(px[0][i0], py[0][i0], px[1][i0], py[1][i0], x5[i1], y5[i1]);
          }
        }
      }
      if (this._line || this._line !== 0 && n === 1) this._context.closePath();
      this._line = 1 - this._line;
      this._x = this._y = null;
    },
    point: function(x5, y5) {
      this._x.push(+x5);
      this._y.push(+y5);
    }
  };
  function controlPoints(x5) {
    var i, n = x5.length - 1, m2, a2 = new Array(n), b = new Array(n), r2 = new Array(n);
    a2[0] = 0, b[0] = 2, r2[0] = x5[0] + 2 * x5[1];
    for (i = 1; i < n - 1; ++i) a2[i] = 1, b[i] = 4, r2[i] = 4 * x5[i] + 2 * x5[i + 1];
    a2[n - 1] = 2, b[n - 1] = 7, r2[n - 1] = 8 * x5[n - 1] + x5[n];
    for (i = 1; i < n; ++i) m2 = a2[i] / b[i - 1], b[i] -= m2, r2[i] -= m2 * r2[i - 1];
    a2[n - 1] = r2[n - 1] / b[n - 1];
    for (i = n - 2; i >= 0; --i) a2[i] = (r2[i] - a2[i + 1]) / b[i];
    b[n - 1] = (x5[n] + a2[n - 1]) / 2;
    for (i = 0; i < n - 1; ++i) b[i] = 2 * x5[i + 1] - a2[i + 1];
    return [a2, b];
  }
  function natural_default(context) {
    return new Natural(context);
  }

  // node_modules/d3-shape/src/curve/step.js
  function Step(context, t) {
    this._context = context;
    this._t = t;
  }
  Step.prototype = {
    areaStart: function() {
      this._line = 0;
    },
    areaEnd: function() {
      this._line = NaN;
    },
    lineStart: function() {
      this._x = this._y = NaN;
      this._point = 0;
    },
    lineEnd: function() {
      if (0 < this._t && this._t < 1 && this._point === 2) this._context.lineTo(this._x, this._y);
      if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
      if (this._line >= 0) this._t = 1 - this._t, this._line = 1 - this._line;
    },
    point: function(x5, y5) {
      x5 = +x5, y5 = +y5;
      switch (this._point) {
        case 0:
          this._point = 1;
          this._line ? this._context.lineTo(x5, y5) : this._context.moveTo(x5, y5);
          break;
        case 1:
          this._point = 2;
        // falls through
        default: {
          if (this._t <= 0) {
            this._context.lineTo(this._x, y5);
            this._context.lineTo(x5, y5);
          } else {
            var x1 = this._x * (1 - this._t) + x5 * this._t;
            this._context.lineTo(x1, this._y);
            this._context.lineTo(x1, y5);
          }
          break;
        }
      }
      this._x = x5, this._y = y5;
    }
  };
  function step_default(context) {
    return new Step(context, 0.5);
  }
  function stepBefore(context) {
    return new Step(context, 0);
  }
  function stepAfter(context) {
    return new Step(context, 1);
  }

  // node_modules/d3-shape/src/offset/none.js
  function none_default(series, order) {
    if (!((n = series.length) > 1)) return;
    for (var i = 1, j, s0, s1 = series[order[0]], n, m2 = s1.length; i < n; ++i) {
      s0 = s1, s1 = series[order[i]];
      for (j = 0; j < m2; ++j) {
        s1[j][1] += s1[j][0] = isNaN(s0[j][1]) ? s0[j][0] : s0[j][1];
      }
    }
  }

  // node_modules/d3-shape/src/order/none.js
  function none_default2(series) {
    var n = series.length, o = new Array(n);
    while (--n >= 0) o[n] = n;
    return o;
  }

  // node_modules/d3-shape/src/stack.js
  function stackValue(d, key) {
    return d[key];
  }
  function stackSeries(key) {
    const series = [];
    series.key = key;
    return series;
  }
  function stack_default() {
    var keys = constant_default([]), order = none_default2, offset = none_default, value2 = stackValue;
    function stack(data) {
      var sz = Array.from(keys.apply(this, arguments), stackSeries), i, n = sz.length, j = -1, oz;
      for (const d of data) {
        for (i = 0, ++j; i < n; ++i) {
          (sz[i][j] = [0, +value2(d, sz[i].key, j, data)]).data = d;
        }
      }
      for (i = 0, oz = array_default(order(sz)); i < n; ++i) {
        sz[oz[i]].index = i;
      }
      offset(sz, oz);
      return sz;
    }
    stack.keys = function(_) {
      return arguments.length ? (keys = typeof _ === "function" ? _ : constant_default(Array.from(_)), stack) : keys;
    };
    stack.value = function(_) {
      return arguments.length ? (value2 = typeof _ === "function" ? _ : constant_default(+_), stack) : value2;
    };
    stack.order = function(_) {
      return arguments.length ? (order = _ == null ? none_default2 : typeof _ === "function" ? _ : constant_default(Array.from(_)), stack) : order;
    };
    stack.offset = function(_) {
      return arguments.length ? (offset = _ == null ? none_default : _, stack) : offset;
    };
    return stack;
  }

  // node_modules/d3-shape/src/offset/expand.js
  function expand_default(series, order) {
    if (!((n = series.length) > 0)) return;
    for (var i, n, j = 0, m2 = series[0].length, y5; j < m2; ++j) {
      for (y5 = i = 0; i < n; ++i) y5 += series[i][j][1] || 0;
      if (y5) for (i = 0; i < n; ++i) series[i][j][1] /= y5;
    }
    none_default(series, order);
  }

  // node_modules/d3-shape/src/offset/silhouette.js
  function silhouette_default(series, order) {
    if (!((n = series.length) > 0)) return;
    for (var j = 0, s0 = series[order[0]], n, m2 = s0.length; j < m2; ++j) {
      for (var i = 0, y5 = 0; i < n; ++i) y5 += series[i][j][1] || 0;
      s0[j][1] += s0[j][0] = -y5 / 2;
    }
    none_default(series, order);
  }

  // node_modules/d3-shape/src/offset/wiggle.js
  function wiggle_default(series, order) {
    if (!((n = series.length) > 0) || !((m2 = (s0 = series[order[0]]).length) > 0)) return;
    for (var y5 = 0, j = 1, s0, m2, n; j < m2; ++j) {
      for (var i = 0, s1 = 0, s2 = 0; i < n; ++i) {
        var si = series[order[i]], sij0 = si[j][1] || 0, sij1 = si[j - 1][1] || 0, s3 = (sij0 - sij1) / 2;
        for (var k = 0; k < i; ++k) {
          var sk = series[order[k]], skj0 = sk[j][1] || 0, skj1 = sk[j - 1][1] || 0;
          s3 += skj0 - skj1;
        }
        s1 += sij0, s2 += s3 * sij0;
      }
      s0[j - 1][1] += s0[j - 1][0] = y5;
      if (s1) y5 -= s2 / s1;
    }
    s0[j - 1][1] += s0[j - 1][0] = y5;
    none_default(series, order);
  }

  // node_modules/es-toolkit/dist/_internal/isUnsafeProperty.mjs
  function isUnsafeProperty(key) {
    return key === "__proto__";
  }

  // node_modules/es-toolkit/dist/compat/_internal/isDeepKey.mjs
  function isDeepKey(key) {
    switch (typeof key) {
      case "number":
      case "symbol":
        return false;
      case "string":
        return key.includes(".") || key.includes("[") || key.includes("]");
    }
  }

  // node_modules/es-toolkit/dist/compat/_internal/toKey.mjs
  function toKey(value2) {
    if (typeof value2 === "string" || typeof value2 === "symbol") return value2;
    if (Object.is(value2?.valueOf?.(), -0)) return "-0";
    return String(value2);
  }

  // node_modules/es-toolkit/dist/compat/util/toString.mjs
  function toString(value2) {
    if (value2 == null) return "";
    if (typeof value2 === "string") return value2;
    if (Array.isArray(value2)) return value2.map(toString).join(",");
    const result = String(value2);
    if (result === "0" && Object.is(Number(value2), -0)) return "-0";
    return result;
  }

  // node_modules/es-toolkit/dist/compat/util/toPath.mjs
  function toPath(deepKey) {
    if (Array.isArray(deepKey)) return deepKey.map(toKey);
    if (typeof deepKey === "symbol") return [deepKey];
    deepKey = toString(deepKey);
    const result = [];
    const length = deepKey.length;
    if (length === 0) return result;
    let index2 = 0;
    let key = "";
    let quoteChar = "";
    let bracket = false;
    if (deepKey.charCodeAt(0) === 46) result.push("");
    while (index2 < length) {
      const char = deepKey[index2];
      if (quoteChar) if (char === "\\" && index2 + 1 < length) {
        index2++;
        key += deepKey[index2];
      } else if (char === quoteChar) quoteChar = "";
      else key += char;
      else if (bracket) if (char === '"' || char === "'") quoteChar = char;
      else if (char === "]") {
        bracket = false;
        result.push(key);
        key = "";
      } else key += char;
      else if (char === "[") {
        bracket = true;
        if (key) {
          result.push(key);
          key = "";
        }
      } else if (char === ".") {
        if (key) {
          result.push(key);
          key = "";
        }
        const next = deepKey[index2 + 1];
        if (next === void 0 || next === ".") result.push("");
      } else key += char;
      index2++;
    }
    if (key) result.push(key);
    return result;
  }

  // node_modules/es-toolkit/dist/compat/object/get.mjs
  function get(object, path3, defaultValue) {
    if (object == null) return defaultValue;
    switch (typeof path3) {
      case "string": {
        if (isUnsafeProperty(path3)) return defaultValue;
        const result = object[path3];
        if (result === void 0) if (isDeepKey(path3) && !Object.hasOwn(object, path3)) return get(object, toPath(path3), defaultValue);
        else return defaultValue;
        return result;
      }
      case "number":
      case "symbol": {
        if (typeof path3 === "number") path3 = toKey(path3);
        const result = object[path3];
        if (result === void 0) return defaultValue;
        return result;
      }
      default: {
        if (Array.isArray(path3)) return getWithPath(object, path3, defaultValue);
        if (Object.is(path3?.valueOf(), -0)) path3 = "-0";
        else path3 = String(path3);
        if (isUnsafeProperty(path3)) return defaultValue;
        const result = object[path3];
        if (result === void 0) return defaultValue;
        return result;
      }
    }
  }
  function getWithPath(object, path3, defaultValue) {
    if (path3.length === 0) return defaultValue;
    let current2 = object;
    for (let index2 = 0; index2 < path3.length; index2++) {
      if (current2 == null) return defaultValue;
      if (isUnsafeProperty(path3[index2])) return defaultValue;
      current2 = current2[path3[index2]];
    }
    if (current2 === void 0) return defaultValue;
    return current2;
  }

  // node_modules/recharts/es6/util/round.js
  var defaultRoundPrecision = 4;
  function round(num) {
    var roundPrecision = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : defaultRoundPrecision;
    var factor = 10 ** roundPrecision;
    var rounded = Math.round(num * factor) / factor;
    if (Object.is(rounded, -0)) {
      return 0;
    }
    return rounded;
  }
  function roundTemplateLiteral(strings) {
    for (var _len = arguments.length, values = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      values[_key - 1] = arguments[_key];
    }
    return strings.reduce((result, string, i) => {
      var value2 = values[i - 1];
      if (typeof value2 === "string") {
        return result + value2 + string;
      }
      if (value2 !== void 0) {
        return result + round(value2) + string;
      }
      return result + string;
    }, "");
  }

  // node_modules/recharts/es6/util/DataUtils.js
  var mathSign = (value2) => {
    if (value2 === 0) {
      return 0;
    }
    if (value2 > 0) {
      return 1;
    }
    return -1;
  };
  var isNan = (value2) => {
    return typeof value2 == "number" && value2 != +value2;
  };
  var isPercent = (value2) => typeof value2 === "string" && value2.length > 1 && value2.indexOf("%") === value2.length - 1;
  var isNumber = (value2) => (typeof value2 === "number" || value2 instanceof Number) && !isNan(value2);
  var isNumOrStr = (value2) => isNumber(value2) || typeof value2 === "string";
  var idCounter = 0;
  var uniqueId = (prefix2) => {
    var id = ++idCounter;
    return "".concat(prefix2 || "").concat(id);
  };
  var getPercentValue = function getPercentValue2(percent, totalValue) {
    var defaultValue = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 0;
    var validate2 = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : false;
    if (!isNumber(percent) && typeof percent !== "string") {
      return defaultValue;
    }
    var value2;
    if (isPercent(percent)) {
      if (totalValue == null) {
        return defaultValue;
      }
      var index2 = percent.indexOf("%");
      value2 = totalValue * parseFloat(percent.slice(0, index2)) / 100;
    } else {
      value2 = +percent;
    }
    if (isNan(value2)) {
      value2 = defaultValue;
    }
    if (validate2 && totalValue != null && value2 > totalValue) {
      value2 = totalValue;
    }
    return value2;
  };
  var hasDuplicate = (ary2) => {
    if (!Array.isArray(ary2)) {
      return false;
    }
    var len = ary2.length;
    var cache = {};
    for (var i = 0; i < len; i++) {
      if (!cache[String(ary2[i])]) {
        cache[String(ary2[i])] = true;
      } else {
        return true;
      }
    }
    return false;
  };
  function interpolate(start, end, animationElapsedTime) {
    if (isNumber(start) && isNumber(end)) {
      return round(start + animationElapsedTime * (end - start));
    }
    return end;
  }
  function findEntryInArray(ary2, specifiedKey, specifiedValue) {
    if (!ary2 || !ary2.length) {
      return void 0;
    }
    return ary2.find((entry) => entry && (typeof specifiedKey === "function" ? specifiedKey(entry) : get(entry, specifiedKey)) === specifiedValue);
  }
  var isNullish = (value2) => {
    return value2 === null || typeof value2 === "undefined";
  };
  var upperFirst = (value2) => {
    if (isNullish(value2)) {
      return value2;
    }
    return "".concat(value2.charAt(0).toUpperCase()).concat(value2.slice(1));
  };
  function isNotNil(value2) {
    return value2 != null;
  }
  function noop() {
  }

  // node_modules/recharts/es6/util/types.js
  var import_react8 = __toESM(require_react());
  var isPolarCoordinate = (c2) => {
    return "radius" in c2 && "startAngle" in c2 && "endAngle" in c2;
  };
  var adaptEventHandlers = (props, newHandler) => {
    if (!props || typeof props === "function" || typeof props === "boolean") {
      return null;
    }
    var inputProps = props;
    if (/* @__PURE__ */ (0, import_react8.isValidElement)(props)) {
      inputProps = props.props;
    }
    if (typeof inputProps !== "object" && typeof inputProps !== "function") {
      return null;
    }
    var out = {};
    Object.keys(inputProps).forEach((key) => {
      if (isEventKey(key) && typeof inputProps[key] === "function") {
        out[key] = newHandler || ((e) => inputProps[key](inputProps, e));
      }
    });
    return out;
  };
  var getEventHandlerOfChild = (originalHandler, data, index2) => (e) => {
    originalHandler(data, index2, e);
    return null;
  };
  var adaptEventsOfChild = (props, data, index2) => {
    if (props === null || typeof props !== "object" && typeof props !== "function") {
      return null;
    }
    var out = null;
    Object.keys(props).forEach((key) => {
      var item = props[key];
      if (isEventKey(key) && typeof item === "function") {
        if (!out) out = {};
        out[key] = getEventHandlerOfChild(item, data, index2);
      }
    });
    return out;
  };

  // node_modules/recharts/es6/util/resolveDefaultProps.js
  function ownKeys(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys(Object(t), true).forEach(function(r3) {
        _defineProperty(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty(e, r2, t) {
    return (r2 = _toPropertyKey(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey(t) {
    var i = _toPrimitive(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  function resolveDefaultProps(realProps, defaultProps) {
    var resolvedProps = _objectSpread({}, realProps);
    var dp = defaultProps;
    var keys = Object.keys(defaultProps);
    var withDefaults = keys.reduce((acc, key) => {
      if (acc[key] === void 0 && dp[key] !== void 0) {
        acc[key] = dp[key];
      }
      return acc;
    }, resolvedProps);
    return withDefaults;
  }

  // node_modules/es-toolkit/dist/array/uniqBy.mjs
  function uniqBy(arr, mapper) {
    const map2 = /* @__PURE__ */ new Map();
    for (let i = 0; i < arr.length; i++) {
      const item = arr[i];
      const key = mapper(item, i, arr);
      if (!map2.has(key)) map2.set(key, item);
    }
    return Array.from(map2.values());
  }

  // node_modules/es-toolkit/dist/function/ary.mjs
  function ary(func, n) {
    return function(...args) {
      return func.apply(this, args.slice(0, n));
    };
  }

  // node_modules/es-toolkit/dist/function/identity.mjs
  function identity(x5) {
    return x5;
  }

  // node_modules/es-toolkit/dist/compat/object/property.mjs
  function property(path3) {
    return function(object) {
      return get(object, path3);
    };
  }

  // node_modules/es-toolkit/dist/predicate/isPrimitive.mjs
  function isPrimitive(value2) {
    return value2 == null || typeof value2 !== "object" && typeof value2 !== "function";
  }

  // node_modules/es-toolkit/dist/predicate/isTypedArray.mjs
  function isTypedArray(x5) {
    return ArrayBuffer.isView(x5) && !(x5 instanceof DataView);
  }

  // node_modules/es-toolkit/dist/compat/_internal/getSymbols.mjs
  function getSymbols(object) {
    return Object.getOwnPropertySymbols(object).filter((symbol) => Object.prototype.propertyIsEnumerable.call(object, symbol));
  }

  // node_modules/es-toolkit/dist/compat/_internal/getTag.mjs
  function getTag(value2) {
    if (value2 == null) return value2 === void 0 ? "[object Undefined]" : "[object Null]";
    return Object.prototype.toString.call(value2);
  }

  // node_modules/es-toolkit/dist/compat/_internal/tags.mjs
  var regexpTag = "[object RegExp]";
  var stringTag = "[object String]";
  var numberTag = "[object Number]";
  var booleanTag = "[object Boolean]";
  var argumentsTag = "[object Arguments]";
  var symbolTag = "[object Symbol]";
  var dateTag = "[object Date]";
  var mapTag = "[object Map]";
  var setTag = "[object Set]";
  var arrayTag = "[object Array]";
  var arrayBufferTag = "[object ArrayBuffer]";
  var objectTag = "[object Object]";
  var dataViewTag = "[object DataView]";
  var uint8ArrayTag = "[object Uint8Array]";
  var uint8ClampedArrayTag = "[object Uint8ClampedArray]";
  var uint16ArrayTag = "[object Uint16Array]";
  var uint32ArrayTag = "[object Uint32Array]";
  var int8ArrayTag = "[object Int8Array]";
  var int16ArrayTag = "[object Int16Array]";
  var int32ArrayTag = "[object Int32Array]";
  var float32ArrayTag = "[object Float32Array]";
  var float64ArrayTag = "[object Float64Array]";

  // node_modules/es-toolkit/dist/_internal/globalThis.mjs
  var globalThis_ = typeof globalThis === "object" && globalThis || typeof window === "object" && window || typeof self === "object" && self || typeof global === "object" && global || /* @__PURE__ */ (function() {
    return this;
  })();

  // node_modules/es-toolkit/dist/predicate/isBuffer.mjs
  function isBuffer(x5) {
    return typeof globalThis_.Buffer !== "undefined" && globalThis_.Buffer.isBuffer(x5);
  }

  // node_modules/es-toolkit/dist/object/cloneDeepWith.mjs
  function cloneDeepWith(obj, cloneValue) {
    return cloneDeepWithImpl(obj, void 0, obj, /* @__PURE__ */ new Map(), cloneValue);
  }
  function cloneDeepWithImpl(valueToClone, keyToClone, objectToClone, stack = /* @__PURE__ */ new Map(), cloneValue = void 0) {
    const cloned = cloneValue?.(valueToClone, keyToClone, objectToClone, stack);
    if (cloned !== void 0) return cloned;
    if (isPrimitive(valueToClone)) return valueToClone;
    if (stack.has(valueToClone)) return stack.get(valueToClone);
    if (Array.isArray(valueToClone)) {
      const result = new Array(valueToClone.length);
      stack.set(valueToClone, result);
      for (let i = 0; i < valueToClone.length; i++) result[i] = cloneDeepWithImpl(valueToClone[i], i, objectToClone, stack, cloneValue);
      if (Object.hasOwn(valueToClone, "index")) result.index = valueToClone.index;
      if (Object.hasOwn(valueToClone, "input")) result.input = valueToClone.input;
      return result;
    }
    if (valueToClone instanceof Date) return new Date(valueToClone.getTime());
    if (valueToClone instanceof RegExp) {
      const result = new RegExp(valueToClone.source, valueToClone.flags);
      result.lastIndex = valueToClone.lastIndex;
      return result;
    }
    if (valueToClone instanceof Map) {
      const result = /* @__PURE__ */ new Map();
      stack.set(valueToClone, result);
      for (const [key, value2] of valueToClone) result.set(key, cloneDeepWithImpl(value2, key, objectToClone, stack, cloneValue));
      return result;
    }
    if (valueToClone instanceof Set) {
      const result = /* @__PURE__ */ new Set();
      stack.set(valueToClone, result);
      for (const value2 of valueToClone) result.add(cloneDeepWithImpl(value2, void 0, objectToClone, stack, cloneValue));
      return result;
    }
    if (isBuffer(valueToClone)) return valueToClone.subarray();
    if (isTypedArray(valueToClone)) {
      const result = new (Object.getPrototypeOf(valueToClone)).constructor(valueToClone.length);
      stack.set(valueToClone, result);
      for (let i = 0; i < valueToClone.length; i++) result[i] = cloneDeepWithImpl(valueToClone[i], i, objectToClone, stack, cloneValue);
      return result;
    }
    if (valueToClone instanceof ArrayBuffer || typeof SharedArrayBuffer !== "undefined" && valueToClone instanceof SharedArrayBuffer) return valueToClone.slice(0);
    if (valueToClone instanceof DataView) {
      const result = new DataView(valueToClone.buffer.slice(0), valueToClone.byteOffset, valueToClone.byteLength);
      stack.set(valueToClone, result);
      copyProperties(result, valueToClone, objectToClone, stack, cloneValue);
      return result;
    }
    if (typeof File !== "undefined" && valueToClone instanceof File) {
      const result = new File([valueToClone], valueToClone.name, { type: valueToClone.type });
      stack.set(valueToClone, result);
      copyProperties(result, valueToClone, objectToClone, stack, cloneValue);
      return result;
    }
    if (typeof Blob !== "undefined" && valueToClone instanceof Blob) {
      const result = new Blob([valueToClone], { type: valueToClone.type });
      stack.set(valueToClone, result);
      copyProperties(result, valueToClone, objectToClone, stack, cloneValue);
      return result;
    }
    if (valueToClone instanceof Error) {
      const result = structuredClone(valueToClone);
      stack.set(valueToClone, result);
      result.message = valueToClone.message;
      result.name = valueToClone.name;
      result.stack = valueToClone.stack;
      result.cause = valueToClone.cause;
      result.constructor = valueToClone.constructor;
      copyProperties(result, valueToClone, objectToClone, stack, cloneValue);
      return result;
    }
    if (valueToClone instanceof Boolean) {
      const result = new Boolean(valueToClone.valueOf());
      stack.set(valueToClone, result);
      copyProperties(result, valueToClone, objectToClone, stack, cloneValue);
      return result;
    }
    if (valueToClone instanceof Number) {
      const result = new Number(valueToClone.valueOf());
      stack.set(valueToClone, result);
      copyProperties(result, valueToClone, objectToClone, stack, cloneValue);
      return result;
    }
    if (valueToClone instanceof String) {
      const result = new String(valueToClone.valueOf());
      stack.set(valueToClone, result);
      copyProperties(result, valueToClone, objectToClone, stack, cloneValue);
      return result;
    }
    if (typeof valueToClone === "object" && isCloneableObject(valueToClone)) {
      const result = Object.create(Object.getPrototypeOf(valueToClone));
      stack.set(valueToClone, result);
      copyProperties(result, valueToClone, objectToClone, stack, cloneValue);
      return result;
    }
    return valueToClone;
  }
  function copyProperties(target, source, objectToClone = target, stack, cloneValue) {
    const keys = [...Object.keys(source), ...getSymbols(source)];
    for (let i = 0; i < keys.length; i++) {
      const key = keys[i];
      const descriptor = Object.getOwnPropertyDescriptor(target, key);
      if (descriptor == null || descriptor.writable) target[key] = cloneDeepWithImpl(source[key], key, objectToClone, stack, cloneValue);
    }
  }
  function isCloneableObject(object) {
    switch (getTag(object)) {
      case argumentsTag:
      case arrayTag:
      case arrayBufferTag:
      case dataViewTag:
      case booleanTag:
      case dateTag:
      case float32ArrayTag:
      case float64ArrayTag:
      case int8ArrayTag:
      case int16ArrayTag:
      case int32ArrayTag:
      case mapTag:
      case numberTag:
      case objectTag:
      case regexpTag:
      case setTag:
      case stringTag:
      case symbolTag:
      case uint8ArrayTag:
      case uint8ClampedArrayTag:
      case uint16ArrayTag:
      case uint32ArrayTag:
        return true;
      default:
        return false;
    }
  }

  // node_modules/es-toolkit/dist/object/cloneDeep.mjs
  function cloneDeep(obj) {
    return cloneDeepWithImpl(obj, void 0, obj, /* @__PURE__ */ new Map(), void 0);
  }

  // node_modules/es-toolkit/dist/_internal/isEqualsSameValueZero.mjs
  function isEqualsSameValueZero(value2, other) {
    return value2 === other || Number.isNaN(value2) && Number.isNaN(other);
  }

  // node_modules/es-toolkit/dist/compat/predicate/isObject.mjs
  function isObject(value2) {
    return value2 !== null && (typeof value2 === "object" || typeof value2 === "function");
  }

  // node_modules/es-toolkit/dist/compat/predicate/isMatchWith.mjs
  function isMatchWith(target, source, compare) {
    if (typeof compare !== "function") return isMatchWith(target, source, () => void 0);
    return isMatchWithInternal(target, source, function doesMatch(objValue, srcValue, key, object, source2, stack) {
      const isEqual = compare(objValue, srcValue, key, object, source2, stack);
      if (isEqual !== void 0) return Boolean(isEqual);
      return isMatchWithInternal(objValue, srcValue, doesMatch, stack, false);
    }, /* @__PURE__ */ new Map(), true);
  }
  function isMatchWithInternal(target, source, compare, stack, isRoot = false) {
    if (source === target) return true;
    switch (typeof source) {
      case "object":
        return isObjectMatch(target, source, compare, stack);
      case "function":
        if (Object.keys(source).length > 0) return isMatchWithInternal(target, { ...source }, compare, stack, isRoot);
        return isEqualsSameValueZero(target, source);
      default:
        if (!isObject(target)) return isEqualsSameValueZero(target, source);
        if (isRoot) {
          if (typeof source === "string") return source === "";
          return true;
        }
        return isEqualsSameValueZero(target, source);
    }
  }
  function isObjectMatch(target, source, compare, stack) {
    if (source == null) return true;
    if (Array.isArray(source)) return isArrayMatch(target, source, compare, stack);
    if (source instanceof Map) return isMapMatch(target, source, compare, stack);
    if (source instanceof Set) return isSetMatch(target, source, compare, stack);
    const keys = Object.keys(source);
    if (target == null || isPrimitive(target)) return keys.length === 0;
    if (keys.length === 0) return true;
    if (stack?.has(source)) return stack.get(source) === target;
    stack?.set(source, target);
    try {
      for (let i = 0; i < keys.length; i++) {
        const key = keys[i];
        if (!isPrimitive(target) && !(key in target)) return false;
        if (source[key] === void 0 && target[key] !== void 0) return false;
        if (source[key] === null && target[key] !== null) return false;
        if (!compare(target[key], source[key], key, target, source, stack)) return false;
      }
      return true;
    } finally {
      stack?.delete(source);
    }
  }
  function isMapMatch(target, source, compare, stack) {
    if (source.size === 0) return true;
    if (!(target instanceof Map)) return false;
    for (const [key, sourceValue] of source.entries()) if (compare(target.get(key), sourceValue, key, target, source, stack) === false) return false;
    return true;
  }
  function isArrayMatch(target, source, compare, stack) {
    if (source.length === 0) return true;
    if (!Array.isArray(target)) return false;
    const countedIndex = /* @__PURE__ */ new Set();
    for (let i = 0; i < source.length; i++) {
      const sourceItem = source[i];
      let found = false;
      for (let j = 0; j < target.length; j++) {
        if (countedIndex.has(j)) continue;
        const targetItem = target[j];
        let matches3 = false;
        if (compare(targetItem, sourceItem, i, target, source, stack)) matches3 = true;
        if (matches3) {
          countedIndex.add(j);
          found = true;
          break;
        }
      }
      if (!found) return false;
    }
    return true;
  }
  function isSetMatch(target, source, compare, stack) {
    if (source.size === 0) return true;
    if (!(target instanceof Set)) return false;
    return isArrayMatch([...target], [...source], compare, stack);
  }

  // node_modules/es-toolkit/dist/compat/predicate/isMatch.mjs
  function isMatch(target, source) {
    return isMatchWith(target, source, () => void 0);
  }

  // node_modules/es-toolkit/dist/compat/predicate/matches.mjs
  function matches(source) {
    source = cloneDeep(source);
    return (target) => {
      return isMatch(target, source);
    };
  }

  // node_modules/es-toolkit/dist/compat/object/cloneDeepWith.mjs
  function cloneDeepWith2(obj, customizer) {
    return cloneDeepWith(obj, (value2, key, object, stack) => {
      const cloned = customizer?.(value2, key, object, stack);
      if (cloned !== void 0) return cloned;
      if (typeof obj !== "object") return;
      if (getTag(obj) === "[object Object]" && typeof obj.constructor !== "function") {
        const result = {};
        stack.set(obj, result);
        copyProperties(result, obj, object, stack);
        return result;
      }
      switch (Object.prototype.toString.call(obj)) {
        case numberTag:
        case stringTag:
        case booleanTag: {
          const result = new obj.constructor(obj?.valueOf());
          copyProperties(result, obj);
          return result;
        }
        case argumentsTag: {
          const result = {};
          copyProperties(result, obj);
          result.length = obj.length;
          result[Symbol.iterator] = obj[Symbol.iterator];
          return result;
        }
        default:
          return;
      }
    });
  }

  // node_modules/es-toolkit/dist/compat/object/cloneDeep.mjs
  function cloneDeep2(obj) {
    return cloneDeepWith2(obj);
  }

  // node_modules/es-toolkit/dist/compat/_internal/isIndex.mjs
  var IS_UNSIGNED_INTEGER = /^(?:0|[1-9]\d*)$/;
  function isIndex(value2, length = Number.MAX_SAFE_INTEGER) {
    switch (typeof value2) {
      case "number":
        return Number.isInteger(value2) && value2 >= 0 && value2 < length;
      case "symbol":
        return false;
      case "string":
        return IS_UNSIGNED_INTEGER.test(value2);
    }
  }

  // node_modules/es-toolkit/dist/compat/predicate/isArguments.mjs
  function isArguments(value2) {
    return value2 !== null && typeof value2 === "object" && getTag(value2) === "[object Arguments]";
  }

  // node_modules/es-toolkit/dist/compat/object/has.mjs
  function has(object, path3) {
    let resolvedPath;
    if (Array.isArray(path3)) resolvedPath = path3;
    else if (typeof path3 === "string" && isDeepKey(path3) && object?.[path3] == null) resolvedPath = toPath(path3);
    else resolvedPath = [path3];
    if (resolvedPath.length === 0) return false;
    let current2 = object;
    for (let i = 0; i < resolvedPath.length; i++) {
      const key = resolvedPath[i];
      if (current2 == null || !Object.hasOwn(current2, key)) {
        if (!((Array.isArray(current2) || isArguments(current2)) && isIndex(key) && key < current2.length)) return false;
      }
      current2 = current2[key];
    }
    return true;
  }

  // node_modules/es-toolkit/dist/compat/predicate/matchesProperty.mjs
  function matchesProperty(property2, source) {
    switch (typeof property2) {
      case "object":
        if (Object.is(property2?.valueOf(), -0)) property2 = "-0";
        break;
      case "number":
        property2 = toKey(property2);
        break;
    }
    source = cloneDeep2(source);
    return function(target) {
      const result = get(target, property2);
      if (result === void 0) return has(target, property2);
      if (source === void 0) return result === void 0;
      return isMatch(result, source);
    };
  }

  // node_modules/es-toolkit/dist/compat/util/iteratee.mjs
  function iteratee(value2) {
    if (value2 == null) return identity;
    switch (typeof value2) {
      case "function":
        return value2;
      case "object":
        if (Array.isArray(value2) && value2.length === 2) return matchesProperty(value2[0], value2[1]);
        return matches(value2);
      case "string":
      case "symbol":
      case "number":
        return property(value2);
    }
  }

  // node_modules/es-toolkit/dist/predicate/isLength.mjs
  function isLength(value2) {
    return Number.isSafeInteger(value2) && value2 >= 0;
  }

  // node_modules/es-toolkit/dist/compat/predicate/isArrayLike.mjs
  function isArrayLike(value2) {
    return value2 != null && typeof value2 !== "function" && isLength(value2.length);
  }

  // node_modules/es-toolkit/dist/compat/predicate/isObjectLike.mjs
  function isObjectLike(value2) {
    return typeof value2 === "object" && value2 !== null;
  }

  // node_modules/es-toolkit/dist/compat/predicate/isArrayLikeObject.mjs
  function isArrayLikeObject(value2) {
    return isObjectLike(value2) && isArrayLike(value2);
  }

  // node_modules/es-toolkit/dist/compat/array/uniqBy.mjs
  function uniqBy2(array, iteratee$1 = identity) {
    if (!isArrayLikeObject(array)) return [];
    return uniqBy(Array.from(array), ary(iteratee(iteratee$1), 1));
  }

  // node_modules/recharts/es6/util/payload/getUniqPayload.js
  function getUniqPayload(payload, option, defaultUniqBy2) {
    if (option === true) {
      return uniqBy2(payload, defaultUniqBy2);
    }
    if (typeof option === "function") {
      return uniqBy2(payload, option);
    }
    return payload;
  }

  // node_modules/recharts/es6/state/hooks.js
  var import_with_selector = __toESM(require_with_selector());
  var import_react10 = __toESM(require_react());

  // node_modules/recharts/es6/state/RechartsReduxContext.js
  var import_react9 = __toESM(require_react());
  var RechartsReduxContext = /* @__PURE__ */ (0, import_react9.createContext)(null);

  // node_modules/recharts/es6/state/hooks.js
  var noopDispatch = (a2) => a2;
  var useAppDispatch = () => {
    var context = (0, import_react10.useContext)(RechartsReduxContext);
    if (context) {
      return context.store.dispatch;
    }
    return noopDispatch;
  };
  var noop2 = () => {
  };
  var addNestedSubNoop = () => noop2;
  var refEquality = (a2, b) => a2 === b;
  function useAppSelector(selector) {
    var context = (0, import_react10.useContext)(RechartsReduxContext);
    var outOfContextSelector = (0, import_react10.useMemo)(() => {
      if (!context) {
        return noop2;
      }
      return (state) => {
        if (state == null) {
          return void 0;
        }
        return selector(state);
      };
    }, [context, selector]);
    return (0, import_with_selector.useSyncExternalStoreWithSelector)(context ? context.subscription.addNestedSub : addNestedSubNoop, context ? context.store.getState : noop2, context ? context.store.getState : noop2, outOfContextSelector, refEquality);
  }

  // node_modules/reselect/dist/reselect.mjs
  var runIdentityFunctionCheck = (resultFunc, inputSelectorsResults, outputSelectorResult) => {
    if (inputSelectorsResults.length === 1 && inputSelectorsResults[0] === outputSelectorResult) {
      let isInputSameAsOutput = false;
      try {
        const emptyObject = {};
        if (resultFunc(emptyObject) === emptyObject) isInputSameAsOutput = true;
      } catch {
      }
      if (isInputSameAsOutput) {
        let stack = void 0;
        try {
          throw new Error();
        } catch (e) {
          ;
          ({ stack } = e);
        }
        console.warn(
          "The result function returned its own inputs without modification. e.g\n`createSelector([state => state.todos], todos => todos)`\nThis could lead to inefficient memoization and unnecessary re-renders.\nEnsure transformation logic is in the result function, and extraction logic is in the input selectors.",
          { stack }
        );
      }
    }
  };
  var runInputStabilityCheck = (inputSelectorResultsObject, options, inputSelectorArgs) => {
    const { memoize, memoizeOptions } = options;
    const { inputSelectorResults, inputSelectorResultsCopy } = inputSelectorResultsObject;
    const createAnEmptyObject = memoize(() => ({}), ...memoizeOptions);
    const areInputSelectorResultsEqual = createAnEmptyObject.apply(null, inputSelectorResults) === createAnEmptyObject.apply(null, inputSelectorResultsCopy);
    if (!areInputSelectorResultsEqual) {
      let stack = void 0;
      try {
        throw new Error();
      } catch (e) {
        ;
        ({ stack } = e);
      }
      console.warn(
        "An input selector returned a different result when passed same arguments.\nThis means your output selector will likely run more frequently than intended.\nAvoid returning a new reference inside your input selector, e.g.\n`createSelector([state => state.todos.map(todo => todo.id)], todoIds => todoIds.length)`",
        {
          arguments: inputSelectorArgs,
          firstInputs: inputSelectorResults,
          secondInputs: inputSelectorResultsCopy,
          stack
        }
      );
    }
  };
  var globalDevModeChecks = {
    inputStabilityCheck: "once",
    identityFunctionCheck: "once"
  };
  function assertIsFunction(func, errorMessage = `expected a function, instead received ${typeof func}`) {
    if (typeof func !== "function") {
      throw new TypeError(errorMessage);
    }
  }
  function assertIsArrayOfFunctions(array, errorMessage = `expected all items to be functions, instead received the following types: `) {
    if (!array.every((item) => typeof item === "function")) {
      const itemTypes = array.map(
        (item) => typeof item === "function" ? `function ${item.name || "unnamed"}()` : typeof item
      ).join(", ");
      throw new TypeError(`${errorMessage}[${itemTypes}]`);
    }
  }
  var ensureIsArray = (item) => {
    return Array.isArray(item) ? item : [item];
  };
  function getDependencies(createSelectorArgs) {
    const dependencies = Array.isArray(createSelectorArgs[0]) ? createSelectorArgs[0] : createSelectorArgs;
    assertIsArrayOfFunctions(
      dependencies,
      `createSelector expects all input-selectors to be functions, but received the following types: `
    );
    return dependencies;
  }
  function collectInputSelectorResults(dependencies, inputSelectorArgs) {
    const inputSelectorResults = [];
    const { length } = dependencies;
    for (let i = 0; i < length; i++) {
      inputSelectorResults.push(dependencies[i].apply(null, inputSelectorArgs));
    }
    return inputSelectorResults;
  }
  var getDevModeChecksExecutionInfo = (firstRun, devModeChecks) => {
    const { identityFunctionCheck, inputStabilityCheck } = {
      ...globalDevModeChecks,
      ...devModeChecks
    };
    return {
      identityFunctionCheck: {
        shouldRun: identityFunctionCheck === "always" || identityFunctionCheck === "once" && firstRun,
        run: runIdentityFunctionCheck
      },
      inputStabilityCheck: {
        shouldRun: inputStabilityCheck === "always" || inputStabilityCheck === "once" && firstRun,
        run: runInputStabilityCheck
      }
    };
  };
  var StrongRef = class {
    constructor(value2) {
      this.value = value2;
    }
    deref() {
      return this.value;
    }
  };
  var getWeakRef = () => typeof WeakRef === "undefined" ? StrongRef : WeakRef;
  var Ref = /* @__PURE__ */ getWeakRef();
  var UNTERMINATED = 0;
  var TERMINATED = 1;
  function createCacheNode() {
    return {
      s: UNTERMINATED,
      v: void 0,
      o: null,
      p: null
    };
  }
  function maybeDeref(r2) {
    if (r2 instanceof Ref) {
      return r2.deref();
    }
    return r2;
  }
  function weakMapMemoize(func, options = {}) {
    let fnNode = createCacheNode();
    const { resultEqualityCheck } = options;
    let lastResult2;
    let resultsCount = 0;
    function memoized() {
      let cacheNode = fnNode;
      const { length } = arguments;
      for (let i = 0, l = length; i < l; i++) {
        const arg = arguments[i];
        if (typeof arg === "function" || typeof arg === "object" && arg !== null) {
          let objectCache = cacheNode.o;
          if (objectCache === null) {
            cacheNode.o = objectCache = /* @__PURE__ */ new WeakMap();
          }
          const objectNode = objectCache.get(arg);
          if (objectNode === void 0) {
            cacheNode = createCacheNode();
            objectCache.set(arg, cacheNode);
          } else {
            cacheNode = objectNode;
          }
        } else {
          let primitiveCache = cacheNode.p;
          if (primitiveCache === null) {
            cacheNode.p = primitiveCache = /* @__PURE__ */ new Map();
          }
          const primitiveNode = primitiveCache.get(arg);
          if (primitiveNode === void 0) {
            cacheNode = createCacheNode();
            primitiveCache.set(arg, cacheNode);
          } else {
            cacheNode = primitiveNode;
          }
        }
      }
      const terminatedNode = cacheNode;
      let result;
      if (cacheNode.s === TERMINATED) {
        result = cacheNode.v;
      } else {
        result = func.apply(null, arguments);
        resultsCount++;
        if (resultEqualityCheck) {
          const lastResultValue = maybeDeref(lastResult2);
          if (lastResultValue != null && resultEqualityCheck(lastResultValue, result)) {
            result = lastResultValue;
            resultsCount !== 0 && resultsCount--;
          }
          const needsWeakRef = typeof result === "object" && result !== null || typeof result === "function";
          lastResult2 = needsWeakRef ? /* @__PURE__ */ new Ref(result) : result;
        }
      }
      terminatedNode.s = TERMINATED;
      terminatedNode.v = result;
      return result;
    }
    memoized.clearCache = () => {
      fnNode = createCacheNode();
      memoized.resetResultsCount();
    };
    memoized.resultsCount = () => resultsCount;
    memoized.resetResultsCount = () => {
      resultsCount = 0;
    };
    return memoized;
  }
  function createSelectorCreator(memoizeOrOptions, ...memoizeOptionsFromArgs) {
    const createSelectorCreatorOptions = typeof memoizeOrOptions === "function" ? {
      memoize: memoizeOrOptions,
      memoizeOptions: memoizeOptionsFromArgs
    } : memoizeOrOptions;
    const createSelector2 = (...createSelectorArgs) => {
      let recomputations = 0;
      let dependencyRecomputations = 0;
      let lastResult2;
      let directlyPassedOptions = {};
      let resultFunc = createSelectorArgs.pop();
      if (typeof resultFunc === "object") {
        directlyPassedOptions = resultFunc;
        resultFunc = createSelectorArgs.pop();
      }
      assertIsFunction(
        resultFunc,
        `createSelector expects an output function after the inputs, but received: [${typeof resultFunc}]`
      );
      const combinedOptions = {
        ...createSelectorCreatorOptions,
        ...directlyPassedOptions
      };
      const {
        memoize,
        memoizeOptions = [],
        argsMemoize = weakMapMemoize,
        argsMemoizeOptions = []
      } = combinedOptions;
      const finalMemoizeOptions = ensureIsArray(memoizeOptions);
      const finalArgsMemoizeOptions = ensureIsArray(argsMemoizeOptions);
      const dependencies = getDependencies(createSelectorArgs);
      const memoizedResultFunc = memoize(function recomputationWrapper() {
        recomputations++;
        return resultFunc.apply(
          null,
          arguments
        );
      }, ...finalMemoizeOptions);
      let firstRun = true;
      const selector = argsMemoize(function dependenciesChecker() {
        dependencyRecomputations++;
        const inputSelectorResults = collectInputSelectorResults(
          dependencies,
          arguments
        );
        lastResult2 = memoizedResultFunc.apply(null, inputSelectorResults);
        if (true) {
          const { devModeChecks = {} } = combinedOptions;
          const { identityFunctionCheck, inputStabilityCheck } = getDevModeChecksExecutionInfo(firstRun, devModeChecks);
          if (identityFunctionCheck.shouldRun) {
            identityFunctionCheck.run(
              resultFunc,
              inputSelectorResults,
              lastResult2
            );
          }
          if (inputStabilityCheck.shouldRun) {
            const inputSelectorResultsCopy = collectInputSelectorResults(
              dependencies,
              arguments
            );
            inputStabilityCheck.run(
              { inputSelectorResults, inputSelectorResultsCopy },
              { memoize, memoizeOptions: finalMemoizeOptions },
              arguments
            );
          }
          if (firstRun) firstRun = false;
        }
        return lastResult2;
      }, ...finalArgsMemoizeOptions);
      return Object.assign(selector, {
        resultFunc,
        memoizedResultFunc,
        dependencies,
        dependencyRecomputations: () => dependencyRecomputations,
        resetDependencyRecomputations: () => {
          dependencyRecomputations = 0;
        },
        lastResult: () => lastResult2,
        recomputations: () => recomputations,
        resetRecomputations: () => {
          recomputations = 0;
        },
        memoize,
        argsMemoize
      });
    };
    Object.assign(createSelector2, {
      withTypes: () => createSelector2
    });
    return createSelector2;
  }
  var createSelector = /* @__PURE__ */ createSelectorCreator(weakMapMemoize);

  // node_modules/es-toolkit/dist/array/flatten.mjs
  function flatten(arr, depth = 1) {
    const result = [];
    const flooredDepth = Math.floor(depth);
    const recursive = (arr2, currentDepth) => {
      for (let i = 0; i < arr2.length; i++) {
        const item = arr2[i];
        if (Array.isArray(item) && currentDepth < flooredDepth) recursive(item, currentDepth + 1);
        else result.push(item);
      }
    };
    recursive(arr, 0);
    return result;
  }

  // node_modules/es-toolkit/dist/compat/_internal/isIterateeCall.mjs
  function isIterateeCall(value2, index2, object) {
    if (!isObject(object)) return false;
    if (typeof index2 === "number" && isArrayLike(object) && isIndex(index2) && index2 < object.length || typeof index2 === "string" && index2 in object) return isEqualsSameValueZero(object[index2], value2);
    return false;
  }

  // node_modules/es-toolkit/dist/compat/_internal/compareValues.mjs
  function getPriority(a2) {
    if (typeof a2 === "symbol") return 1;
    if (a2 === null) return 2;
    if (a2 === void 0) return 3;
    if (a2 !== a2) return 4;
    return 0;
  }
  var compareValues = (a2, b, order) => {
    if (a2 !== b) {
      const aPriority = getPriority(a2);
      const bPriority = getPriority(b);
      if (aPriority === bPriority && aPriority === 0) {
        if (a2 < b) return order === "desc" ? 1 : -1;
        if (a2 > b) return order === "desc" ? -1 : 1;
      }
      return order === "desc" ? bPriority - aPriority : aPriority - bPriority;
    }
    return 0;
  };

  // node_modules/es-toolkit/dist/compat/predicate/isSymbol.mjs
  function isSymbol(value2) {
    return typeof value2 === "symbol" || value2 instanceof Symbol;
  }

  // node_modules/es-toolkit/dist/compat/_internal/isKey.mjs
  var regexIsDeepProp = /\.|\[(?:[^[\]]*|(["'])(?:(?!\1)[^\\]|\\.)*?\1)\]/;
  var regexIsPlainProp = /^\w*$/;
  function isKey(value2, object) {
    if (Array.isArray(value2)) return false;
    if (typeof value2 === "number" || typeof value2 === "boolean" || value2 == null || isSymbol(value2)) return true;
    return typeof value2 === "string" && (regexIsPlainProp.test(value2) || !regexIsDeepProp.test(value2)) || object != null && Object.hasOwn(object, value2);
  }

  // node_modules/es-toolkit/dist/compat/array/orderBy.mjs
  function orderBy(collection, criteria, orders, guard) {
    if (collection == null) return [];
    orders = guard ? void 0 : orders;
    if (!Array.isArray(collection)) collection = Object.values(collection);
    if (!Array.isArray(criteria)) criteria = criteria == null ? [null] : [criteria];
    if (criteria.length === 0) criteria = [null];
    if (!Array.isArray(orders)) orders = orders == null ? [] : [orders];
    orders = orders.map((order) => String(order));
    const getValueByNestedPath = (object, path3) => {
      let target = object;
      for (let i = 0; i < path3.length && target != null; ++i) target = target[path3[i]];
      return target;
    };
    const getValueByCriterion = (criterion, object) => {
      if (object == null || criterion == null) return object;
      if (typeof criterion === "object" && "key" in criterion) {
        if (Object.hasOwn(object, criterion.key)) return object[criterion.key];
        return getValueByNestedPath(object, criterion.path);
      }
      if (typeof criterion === "function") return criterion(object);
      if (Array.isArray(criterion)) return getValueByNestedPath(object, criterion);
      if (typeof object === "object") return object[criterion];
      return object;
    };
    const preparedCriteria = criteria.map((criterion) => {
      if (Array.isArray(criterion) && criterion.length === 1) criterion = criterion[0];
      if (criterion == null || typeof criterion === "function" || Array.isArray(criterion) || isKey(criterion)) return criterion;
      return {
        key: criterion,
        path: toPath(criterion)
      };
    });
    return collection.map((item) => ({
      original: item,
      criteria: preparedCriteria.map((criterion) => getValueByCriterion(criterion, item))
    })).slice().sort((a2, b) => {
      for (let i = 0; i < preparedCriteria.length; i++) {
        const comparedResult = compareValues(a2.criteria[i], b.criteria[i], orders[i]);
        if (comparedResult !== 0) return comparedResult;
      }
      return 0;
    }).map((item) => item.original);
  }

  // node_modules/es-toolkit/dist/compat/array/sortBy.mjs
  function sortBy(collection, ...criteria) {
    const length = criteria.length;
    if (length > 1 && isIterateeCall(collection, criteria[0], criteria[1])) criteria = [];
    else if (length > 2 && isIterateeCall(criteria[0], criteria[1], criteria[2])) criteria = [criteria[0]];
    return orderBy(collection, flatten(criteria), ["asc"]);
  }

  // node_modules/recharts/es6/state/selectors/legendSelectors.js
  var selectLegendSettings = (state) => state.legend.settings;
  var selectLegendSize = (state) => state.legend.size;
  var selectAllLegendPayload2DArray = (state) => state.legend.payload;
  var selectLegendPayload = createSelector([selectAllLegendPayload2DArray, selectLegendSettings], (payloads, _ref2) => {
    var itemSorter = _ref2.itemSorter;
    var flat = payloads.flat(1);
    return itemSorter ? sortBy(flat, itemSorter) : flat;
  });

  // node_modules/recharts/es6/util/useElementOffset.js
  var import_react11 = __toESM(require_react());
  function _slicedToArray(r2, e) {
    return _arrayWithHoles(r2) || _iterableToArrayLimit(r2, e) || _unsupportedIterableToArray(r2, e) || _nonIterableRest();
  }
  function _nonIterableRest() {
    throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  function _unsupportedIterableToArray(r2, a2) {
    if (r2) {
      if ("string" == typeof r2) return _arrayLikeToArray(r2, a2);
      var t = {}.toString.call(r2).slice(8, -1);
      return "Object" === t && r2.constructor && (t = r2.constructor.name), "Map" === t || "Set" === t ? Array.from(r2) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r2, a2) : void 0;
    }
  }
  function _arrayLikeToArray(r2, a2) {
    (null == a2 || a2 > r2.length) && (a2 = r2.length);
    for (var e = 0, n = Array(a2); e < a2; e++) n[e] = r2[e];
    return n;
  }
  function _iterableToArrayLimit(r2, l) {
    var t = null == r2 ? null : "undefined" != typeof Symbol && r2[Symbol.iterator] || r2["@@iterator"];
    if (null != t) {
      var e, n, i, u, a2 = [], f = true, o = false;
      try {
        if (i = (t = t.call(r2)).next, 0 === l) {
          if (Object(t) !== t) return;
          f = false;
        } else for (; !(f = (e = i.call(t)).done) && (a2.push(e.value), a2.length !== l); f = true) ;
      } catch (r3) {
        o = true, n = r3;
      } finally {
        try {
          if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return;
        } finally {
          if (o) throw n;
        }
      }
      return a2;
    }
  }
  function _arrayWithHoles(r2) {
    if (Array.isArray(r2)) return r2;
  }
  var EPS = 1;
  function hasSignificantChange(a2, b) {
    return Math.abs(a2.height - b.height) > EPS || Math.abs(a2.left - b.left) > EPS || Math.abs(a2.top - b.top) > EPS || Math.abs(a2.width - b.width) > EPS;
  }
  function readElementOffset(node) {
    var rect = node.getBoundingClientRect();
    return {
      height: rect.height,
      left: rect.left,
      top: rect.top,
      width: rect.width
    };
  }
  function useElementOffset() {
    var extraDependencies = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : [];
    var _useState = (0, import_react11.useState)({
      height: 0,
      left: 0,
      top: 0,
      width: 0
    }), _useState2 = _slicedToArray(_useState, 2), lastBoundingBox = _useState2[0], setLastBoundingBox = _useState2[1];
    var observerRef = (0, import_react11.useRef)(null);
    var lastBoundingBoxRef = (0, import_react11.useRef)(lastBoundingBox);
    lastBoundingBoxRef.current = lastBoundingBox;
    var updateBoundingBox = (0, import_react11.useCallback)(
      (node) => {
        if (observerRef.current != null) {
          observerRef.current.disconnect();
          observerRef.current = null;
        }
        if (node != null) {
          var box = readElementOffset(node);
          if (hasSignificantChange(box, lastBoundingBoxRef.current)) {
            setLastBoundingBox(box);
          }
          if (typeof ResizeObserver !== "undefined") {
            var observer = new ResizeObserver(() => {
              var newBox = readElementOffset(node);
              if (hasSignificantChange(newBox, lastBoundingBoxRef.current)) {
                setLastBoundingBox(newBox);
              }
            });
            observer.observe(node);
            observerRef.current = observer;
          }
        }
      },
      // eslint-disable-next-line react-hooks/exhaustive-deps
      [...extraDependencies]
    );
    (0, import_react11.useEffect)(() => {
      return () => {
        var _observerRef$current;
        (_observerRef$current = observerRef.current) === null || _observerRef$current === void 0 || _observerRef$current.disconnect();
      };
    }, []);
    return [lastBoundingBox, updateBoundingBox];
  }

  // node_modules/recharts/es6/context/chartLayoutContext.js
  var import_react14 = __toESM(require_react());

  // node_modules/redux/dist/redux.mjs
  var $$observable = /* @__PURE__ */ (() => typeof Symbol === "function" && Symbol.observable || "@@observable")();
  var symbol_observable_default = $$observable;
  var randomString = () => Math.random().toString(36).substring(7).split("").join(".");
  var ActionTypes = {
    INIT: `@@redux/INIT${/* @__PURE__ */ randomString()}`,
    REPLACE: `@@redux/REPLACE${/* @__PURE__ */ randomString()}`,
    PROBE_UNKNOWN_ACTION: () => `@@redux/PROBE_UNKNOWN_ACTION${randomString()}`
  };
  var actionTypes_default = ActionTypes;
  function isPlainObject(obj) {
    if (typeof obj !== "object" || obj === null)
      return false;
    let proto = obj;
    while (Object.getPrototypeOf(proto) !== null) {
      proto = Object.getPrototypeOf(proto);
    }
    return Object.getPrototypeOf(obj) === proto || Object.getPrototypeOf(obj) === null;
  }
  function miniKindOf(val) {
    if (val === void 0)
      return "undefined";
    if (val === null)
      return "null";
    const type = typeof val;
    switch (type) {
      case "boolean":
      case "string":
      case "number":
      case "symbol":
      case "function": {
        return type;
      }
    }
    if (Array.isArray(val))
      return "array";
    if (isDate(val))
      return "date";
    if (isError(val))
      return "error";
    const constructorName = ctorName(val);
    switch (constructorName) {
      case "Symbol":
      case "Promise":
      case "WeakMap":
      case "WeakSet":
      case "Map":
      case "Set":
        return constructorName;
    }
    return Object.prototype.toString.call(val).slice(8, -1).toLowerCase().replace(/\s/g, "");
  }
  function ctorName(val) {
    return typeof val.constructor === "function" ? val.constructor.name : null;
  }
  function isError(val) {
    return val instanceof Error || typeof val.message === "string" && val.constructor && typeof val.constructor.stackTraceLimit === "number";
  }
  function isDate(val) {
    if (val instanceof Date)
      return true;
    return typeof val.toDateString === "function" && typeof val.getDate === "function" && typeof val.setDate === "function";
  }
  function kindOf(val) {
    let typeOfVal = typeof val;
    if (true) {
      typeOfVal = miniKindOf(val);
    }
    return typeOfVal;
  }
  function createStore(reducer, preloadedState, enhancer) {
    if (typeof reducer !== "function") {
      throw new Error(false ? formatProdErrorMessage(2) : `Expected the root reducer to be a function. Instead, received: '${kindOf(reducer)}'`);
    }
    if (typeof preloadedState === "function" && typeof enhancer === "function" || typeof enhancer === "function" && typeof arguments[3] === "function") {
      throw new Error(false ? formatProdErrorMessage(0) : "It looks like you are passing several store enhancers to createStore(). This is not supported. Instead, compose them together to a single function. See https://redux.js.org/tutorials/fundamentals/part-4-store#creating-a-store-with-enhancers for an example.");
    }
    if (typeof preloadedState === "function" && typeof enhancer === "undefined") {
      enhancer = preloadedState;
      preloadedState = void 0;
    }
    if (typeof enhancer !== "undefined") {
      if (typeof enhancer !== "function") {
        throw new Error(false ? formatProdErrorMessage(1) : `Expected the enhancer to be a function. Instead, received: '${kindOf(enhancer)}'`);
      }
      return enhancer(createStore)(reducer, preloadedState);
    }
    let currentReducer = reducer;
    let currentState = preloadedState;
    let currentListeners = /* @__PURE__ */ new Map();
    let nextListeners = currentListeners;
    let listenerIdCounter = 0;
    let isDispatching = false;
    function ensureCanMutateNextListeners() {
      if (nextListeners === currentListeners) {
        nextListeners = /* @__PURE__ */ new Map();
        currentListeners.forEach((listener2, key) => {
          nextListeners.set(key, listener2);
        });
      }
    }
    function getState() {
      if (isDispatching) {
        throw new Error(false ? formatProdErrorMessage(3) : "You may not call store.getState() while the reducer is executing. The reducer has already received the state as an argument. Pass it down from the top reducer instead of reading it from the store.");
      }
      return currentState;
    }
    function subscribe(listener2) {
      if (typeof listener2 !== "function") {
        throw new Error(false ? formatProdErrorMessage(4) : `Expected the listener to be a function. Instead, received: '${kindOf(listener2)}'`);
      }
      if (isDispatching) {
        throw new Error(false ? formatProdErrorMessage(5) : "You may not call store.subscribe() while the reducer is executing. If you would like to be notified after the store has been updated, subscribe from a component and invoke store.getState() in the callback to access the latest state. See https://redux.js.org/api/store#subscribelistener for more details.");
      }
      let isSubscribed = true;
      ensureCanMutateNextListeners();
      const listenerId = listenerIdCounter++;
      nextListeners.set(listenerId, listener2);
      return function unsubscribe() {
        if (!isSubscribed) {
          return;
        }
        if (isDispatching) {
          throw new Error(false ? formatProdErrorMessage(6) : "You may not unsubscribe from a store listener while the reducer is executing. See https://redux.js.org/api/store#subscribelistener for more details.");
        }
        isSubscribed = false;
        ensureCanMutateNextListeners();
        nextListeners.delete(listenerId);
        currentListeners = null;
      };
    }
    function dispatch2(action) {
      if (!isPlainObject(action)) {
        throw new Error(false ? formatProdErrorMessage(7) : `Actions must be plain objects. Instead, the actual type was: '${kindOf(action)}'. You may need to add middleware to your store setup to handle dispatching other values, such as 'redux-thunk' to handle dispatching functions. See https://redux.js.org/tutorials/fundamentals/part-4-store#middleware and https://redux.js.org/tutorials/fundamentals/part-6-async-logic#using-the-redux-thunk-middleware for examples.`);
      }
      if (typeof action.type === "undefined") {
        throw new Error(false ? formatProdErrorMessage(8) : 'Actions may not have an undefined "type" property. You may have misspelled an action type string constant.');
      }
      if (typeof action.type !== "string") {
        throw new Error(false ? formatProdErrorMessage(17) : `Action "type" property must be a string. Instead, the actual type was: '${kindOf(action.type)}'. Value was: '${action.type}' (stringified)`);
      }
      if (isDispatching) {
        throw new Error(false ? formatProdErrorMessage(9) : "Reducers may not dispatch actions.");
      }
      try {
        isDispatching = true;
        currentState = currentReducer(currentState, action);
      } finally {
        isDispatching = false;
      }
      const listeners = currentListeners = nextListeners;
      listeners.forEach((listener2) => {
        listener2();
      });
      return action;
    }
    function replaceReducer(nextReducer) {
      if (typeof nextReducer !== "function") {
        throw new Error(false ? formatProdErrorMessage(10) : `Expected the nextReducer to be a function. Instead, received: '${kindOf(nextReducer)}`);
      }
      currentReducer = nextReducer;
      dispatch2({
        type: actionTypes_default.REPLACE
      });
    }
    function observable() {
      const outerSubscribe = subscribe;
      return {
        /**
         * The minimal observable subscription method.
         * @param observer Any object that can be used as an observer.
         * The observer object should have a `next` method.
         * @returns An object with an `unsubscribe` method that can
         * be used to unsubscribe the observable from the store, and prevent further
         * emission of values from the observable.
         */
        subscribe(observer) {
          if (typeof observer !== "object" || observer === null) {
            throw new Error(false ? formatProdErrorMessage(11) : `Expected the observer to be an object. Instead, received: '${kindOf(observer)}'`);
          }
          function observeState() {
            const observerAsObserver = observer;
            if (observerAsObserver.next) {
              observerAsObserver.next(getState());
            }
          }
          observeState();
          const unsubscribe = outerSubscribe(observeState);
          return {
            unsubscribe
          };
        },
        [symbol_observable_default]() {
          return this;
        }
      };
    }
    dispatch2({
      type: actionTypes_default.INIT
    });
    const store = {
      dispatch: dispatch2,
      subscribe,
      getState,
      replaceReducer,
      [symbol_observable_default]: observable
    };
    return store;
  }
  function warning(message) {
    if (typeof console !== "undefined" && typeof console.error === "function") {
      console.error(message);
    }
    try {
      throw new Error(message);
    } catch (e) {
    }
  }
  function getUnexpectedStateShapeWarningMessage(inputState, reducers2, action, unexpectedKeyCache) {
    const reducerKeys = Object.keys(reducers2);
    const argumentName = action && action.type === actionTypes_default.INIT ? "preloadedState argument passed to createStore" : "previous state received by the reducer";
    if (reducerKeys.length === 0) {
      return "Store does not have a valid reducer. Make sure the argument passed to combineReducers is an object whose values are reducers.";
    }
    if (!isPlainObject(inputState)) {
      return `The ${argumentName} has unexpected type of "${kindOf(inputState)}". Expected argument to be an object with the following keys: "${reducerKeys.join('", "')}"`;
    }
    const unexpectedKeys = Object.keys(inputState).filter((key) => !reducers2.hasOwnProperty(key) && !unexpectedKeyCache[key]);
    unexpectedKeys.forEach((key) => {
      unexpectedKeyCache[key] = true;
    });
    if (action && action.type === actionTypes_default.REPLACE)
      return;
    if (unexpectedKeys.length > 0) {
      return `Unexpected ${unexpectedKeys.length > 1 ? "keys" : "key"} "${unexpectedKeys.join('", "')}" found in ${argumentName}. Expected to find one of the known reducer keys instead: "${reducerKeys.join('", "')}". Unexpected keys will be ignored.`;
    }
  }
  function assertReducerShape(reducers2) {
    Object.keys(reducers2).forEach((key) => {
      const reducer = reducers2[key];
      const initialState15 = reducer(void 0, {
        type: actionTypes_default.INIT
      });
      if (typeof initialState15 === "undefined") {
        throw new Error(false ? formatProdErrorMessage(12) : `The slice reducer for key "${key}" returned undefined during initialization. If the state passed to the reducer is undefined, you must explicitly return the initial state. The initial state may not be undefined. If you don't want to set a value for this reducer, you can use null instead of undefined.`);
      }
      if (typeof reducer(void 0, {
        type: actionTypes_default.PROBE_UNKNOWN_ACTION()
      }) === "undefined") {
        throw new Error(false ? formatProdErrorMessage(13) : `The slice reducer for key "${key}" returned undefined when probed with a random type. Don't try to handle '${actionTypes_default.INIT}' or other actions in "redux/*" namespace. They are considered private. Instead, you must return the current state for any unknown actions, unless it is undefined, in which case you must return the initial state, regardless of the action type. The initial state may not be undefined, but can be null.`);
      }
    });
  }
  function combineReducers(reducers2) {
    const reducerKeys = Object.keys(reducers2);
    const finalReducers = {};
    for (let i = 0; i < reducerKeys.length; i++) {
      const key = reducerKeys[i];
      if (true) {
        if (typeof reducers2[key] === "undefined") {
          warning(`No reducer provided for key "${key}"`);
        }
      }
      if (typeof reducers2[key] === "function") {
        finalReducers[key] = reducers2[key];
      }
    }
    const finalReducerKeys = Object.keys(finalReducers);
    let unexpectedKeyCache;
    if (true) {
      unexpectedKeyCache = {};
    }
    let shapeAssertionError;
    try {
      assertReducerShape(finalReducers);
    } catch (e) {
      shapeAssertionError = e;
    }
    return function combination(state = {}, action) {
      if (shapeAssertionError) {
        throw shapeAssertionError;
      }
      if (true) {
        const warningMessage = getUnexpectedStateShapeWarningMessage(state, finalReducers, action, unexpectedKeyCache);
        if (warningMessage) {
          warning(warningMessage);
        }
      }
      let hasChanged = false;
      const nextState = {};
      for (let i = 0; i < finalReducerKeys.length; i++) {
        const key = finalReducerKeys[i];
        const reducer = finalReducers[key];
        const previousStateForKey = state[key];
        const nextStateForKey = reducer(previousStateForKey, action);
        if (typeof nextStateForKey === "undefined") {
          const actionType = action && action.type;
          throw new Error(false ? formatProdErrorMessage(14) : `When called with an action of type ${actionType ? `"${String(actionType)}"` : "(unknown type)"}, the slice reducer for key "${key}" returned undefined. To ignore an action, you must explicitly return the previous state. If you want this reducer to hold no value, you can return null instead of undefined.`);
        }
        nextState[key] = nextStateForKey;
        hasChanged = hasChanged || nextStateForKey !== previousStateForKey;
      }
      hasChanged = hasChanged || finalReducerKeys.length !== Object.keys(state).length;
      return hasChanged ? nextState : state;
    };
  }
  function compose(...funcs) {
    if (funcs.length === 0) {
      return (arg) => arg;
    }
    if (funcs.length === 1) {
      return funcs[0];
    }
    return funcs.reduce((a2, b) => (...args) => a2(b(...args)));
  }
  function applyMiddleware(...middlewares) {
    return (createStore2) => (reducer, preloadedState) => {
      const store = createStore2(reducer, preloadedState);
      let dispatch2 = () => {
        throw new Error(false ? formatProdErrorMessage(15) : "Dispatching while constructing your middleware is not allowed. Other middleware would not be applied to this dispatch.");
      };
      const middlewareAPI = {
        getState: store.getState,
        dispatch: (action, ...args) => dispatch2(action, ...args)
      };
      const chain = middlewares.map((middleware) => middleware(middlewareAPI));
      dispatch2 = compose(...chain)(store.dispatch);
      return {
        ...store,
        dispatch: dispatch2
      };
    };
  }
  function isAction(action) {
    return isPlainObject(action) && "type" in action && typeof action.type === "string";
  }

  // node_modules/immer/dist/immer.mjs
  var NOTHING = Symbol.for("immer-nothing");
  var DRAFTABLE = Symbol.for("immer-draftable");
  var DRAFT_STATE = Symbol.for("immer-state");
  var errors = true ? [
    // All error codes, starting by 0:
    function(plugin) {
      return `The plugin for '${plugin}' has not been loaded into Immer. To enable the plugin, import and call \`enable${plugin}()\` when initializing your application.`;
    },
    function(thing) {
      return `produce can only be called on things that are draftable: plain objects, arrays, Map, Set or classes that are marked with '[immerable]: true'. Got '${thing}'`;
    },
    "This object has been frozen and should not be mutated",
    function(data) {
      return "Cannot use a proxy that has been revoked. Did you pass an object from inside an immer function to an async process? " + data;
    },
    "An immer producer returned a new value *and* modified its draft. Either return a new value *or* modify the draft.",
    "Immer forbids circular references",
    "The first or second argument to `produce` must be a function",
    "The third argument to `produce` must be a function or undefined",
    "First argument to `createDraft` must be a plain object, an array, or an immerable object",
    "First argument to `finishDraft` must be a draft returned by `createDraft`",
    function(thing) {
      return `'current' expects a draft, got: ${thing}`;
    },
    "Object.defineProperty() cannot be used on an Immer draft",
    "Object.setPrototypeOf() cannot be used on an Immer draft",
    "Immer only supports deleting array indices",
    "Immer only supports setting array indices and the 'length' property",
    function(thing) {
      return `'original' expects a draft, got: ${thing}`;
    }
    // Note: if more errors are added, the errorOffset in Patches.ts should be increased
    // See Patches.ts for additional errors
  ] : [];
  function die(error, ...args) {
    if (true) {
      const e = errors[error];
      const msg = isFunction(e) ? e.apply(null, args) : e;
      throw new Error(`[Immer] ${msg}`);
    }
    throw new Error(
      `[Immer] minified error nr: ${error}. Full error at: https://bit.ly/3cXEKWf`
    );
  }
  var O = Object;
  var getPrototypeOf = O.getPrototypeOf;
  var CONSTRUCTOR = "constructor";
  var PROTOTYPE = "prototype";
  var CONFIGURABLE = "configurable";
  var ENUMERABLE = "enumerable";
  var WRITABLE = "writable";
  var VALUE = "value";
  var isDraft = (value2) => !!value2 && !!value2[DRAFT_STATE];
  function isDraftable(value2) {
    if (!value2)
      return false;
    return isPlainObject2(value2) || isArray(value2) || !!value2[DRAFTABLE] || !!value2[CONSTRUCTOR]?.[DRAFTABLE] || isMap(value2) || isSet(value2);
  }
  var objectCtorString = O[PROTOTYPE][CONSTRUCTOR].toString();
  var cachedCtorStrings = /* @__PURE__ */ new WeakMap();
  function isPlainObject2(value2) {
    if (!value2 || !isObjectish(value2))
      return false;
    const proto = getPrototypeOf(value2);
    if (proto === null || proto === O[PROTOTYPE])
      return true;
    const Ctor = O.hasOwnProperty.call(proto, CONSTRUCTOR) && proto[CONSTRUCTOR];
    if (Ctor === Object)
      return true;
    if (!isFunction(Ctor))
      return false;
    let ctorString = cachedCtorStrings.get(Ctor);
    if (ctorString === void 0) {
      ctorString = Function.toString.call(Ctor);
      cachedCtorStrings.set(Ctor, ctorString);
    }
    return ctorString === objectCtorString;
  }
  function each(obj, iter, strict = true) {
    if (getArchtype(obj) === 0) {
      const keys = strict ? Reflect.ownKeys(obj) : O.keys(obj);
      keys.forEach((key) => {
        iter(key, obj[key], obj);
      });
    } else {
      obj.forEach((entry, index2) => iter(index2, entry, obj));
    }
  }
  function getArchtype(thing) {
    const state = thing[DRAFT_STATE];
    return state ? state.type_ : isArray(thing) ? 1 : isMap(thing) ? 2 : isSet(thing) ? 3 : 0;
  }
  var has2 = (thing, prop, type = getArchtype(thing)) => type === 2 ? thing.has(prop) : O[PROTOTYPE].hasOwnProperty.call(thing, prop);
  var get2 = (thing, prop, type = getArchtype(thing)) => (
    // @ts-ignore
    type === 2 ? thing.get(prop) : thing[prop]
  );
  var set = (thing, propOrOldValue, value2, type = getArchtype(thing)) => {
    if (type === 2)
      thing.set(propOrOldValue, value2);
    else if (type === 3) {
      thing.add(value2);
    } else
      thing[propOrOldValue] = value2;
  };
  function is(x5, y5) {
    if (x5 === y5) {
      return x5 !== 0 || 1 / x5 === 1 / y5;
    } else {
      return x5 !== x5 && y5 !== y5;
    }
  }
  var isArray = Array.isArray;
  var isMap = (target) => target instanceof Map;
  var isSet = (target) => target instanceof Set;
  var isObjectish = (target) => typeof target === "object";
  var isFunction = (target) => typeof target === "function";
  var isBoolean = (target) => typeof target === "boolean";
  function isArrayIndex(value2) {
    const n = +value2;
    return Number.isInteger(n) && String(n) === value2;
  }
  var latest = (state) => state.copy_ || state.base_;
  var getFinalValue = (state) => state.modified_ ? state.copy_ : state.base_;
  function shallowCopy(base, strict) {
    if (isMap(base)) {
      return new Map(base);
    }
    if (isSet(base)) {
      return new Set(base);
    }
    if (isArray(base))
      return Array[PROTOTYPE].slice.call(base);
    const isPlain2 = isPlainObject2(base);
    if (strict === true || strict === "class_only" && !isPlain2) {
      const descriptors = O.getOwnPropertyDescriptors(base);
      delete descriptors[DRAFT_STATE];
      let keys = Reflect.ownKeys(descriptors);
      for (let i = 0; i < keys.length; i++) {
        const key = keys[i];
        const desc = descriptors[key];
        if (desc[WRITABLE] === false) {
          desc[WRITABLE] = true;
          desc[CONFIGURABLE] = true;
        }
        if (desc.get || desc.set)
          descriptors[key] = {
            [CONFIGURABLE]: true,
            [WRITABLE]: true,
            // could live with !!desc.set as well here...
            [ENUMERABLE]: desc[ENUMERABLE],
            [VALUE]: base[key]
          };
      }
      return O.create(getPrototypeOf(base), descriptors);
    } else {
      const proto = getPrototypeOf(base);
      if (proto !== null && isPlain2) {
        return { ...base };
      }
      const obj = O.create(proto);
      return O.assign(obj, base);
    }
  }
  function freeze(obj, deep = false) {
    if (isFrozen(obj) || isDraft(obj) || !isDraftable(obj))
      return obj;
    if (getArchtype(obj) > 1) {
      O.defineProperties(obj, {
        set: dontMutateMethodOverride,
        add: dontMutateMethodOverride,
        clear: dontMutateMethodOverride,
        delete: dontMutateMethodOverride
      });
    }
    O.freeze(obj);
    if (deep)
      each(
        obj,
        (_key, value2) => {
          freeze(value2, true);
        },
        false
      );
    return obj;
  }
  function dontMutateFrozenCollections() {
    die(2);
  }
  var dontMutateMethodOverride = {
    [VALUE]: dontMutateFrozenCollections
  };
  function isFrozen(obj) {
    if (obj === null || !isObjectish(obj))
      return true;
    return O.isFrozen(obj);
  }
  var PluginMapSet = "MapSet";
  var PluginPatches = "Patches";
  var PluginArrayMethods = "ArrayMethods";
  var plugins = {};
  function getPlugin(pluginKey) {
    const plugin = plugins[pluginKey];
    if (!plugin) {
      die(0, pluginKey);
    }
    return plugin;
  }
  var isPluginLoaded = (pluginKey) => !!plugins[pluginKey];
  var currentScope;
  var getCurrentScope = () => currentScope;
  var createScope = (parent_, immer_) => ({
    drafts_: [],
    parent_,
    immer_,
    // Whenever the modified draft contains a draft from another scope, we
    // need to prevent auto-freezing so the unowned draft can be finalized.
    canAutoFreeze_: true,
    unfinalizedDrafts_: 0,
    handledSet_: /* @__PURE__ */ new Set(),
    processedForPatches_: /* @__PURE__ */ new Set(),
    mapSetPlugin_: isPluginLoaded(PluginMapSet) ? getPlugin(PluginMapSet) : void 0,
    arrayMethodsPlugin_: isPluginLoaded(PluginArrayMethods) ? getPlugin(PluginArrayMethods) : void 0
  });
  function usePatchesInScope(scope, patchListener) {
    if (patchListener) {
      scope.patchPlugin_ = getPlugin(PluginPatches);
      scope.patches_ = [];
      scope.inversePatches_ = [];
      scope.patchListener_ = patchListener;
    }
  }
  function revokeScope(scope) {
    leaveScope(scope);
    scope.drafts_.forEach(revokeDraft);
    scope.drafts_ = null;
  }
  function leaveScope(scope) {
    if (scope === currentScope) {
      currentScope = scope.parent_;
    }
  }
  var enterScope = (immer2) => currentScope = createScope(currentScope, immer2);
  function revokeDraft(draft) {
    const state = draft[DRAFT_STATE];
    if (state.type_ === 0 || state.type_ === 1)
      state.revoke_();
    else
      state.revoked_ = true;
  }
  function processResult(result, scope) {
    scope.unfinalizedDrafts_ = scope.drafts_.length;
    const baseDraft = scope.drafts_[0];
    const isReplaced = result !== void 0 && result !== baseDraft;
    if (isReplaced) {
      if (baseDraft[DRAFT_STATE].modified_) {
        revokeScope(scope);
        die(4);
      }
      if (isDraftable(result)) {
        result = finalize(scope, result);
      }
      const { patchPlugin_ } = scope;
      if (patchPlugin_) {
        patchPlugin_.generateReplacementPatches_(
          baseDraft[DRAFT_STATE].base_,
          result,
          scope
        );
      }
    } else {
      result = finalize(scope, baseDraft);
    }
    maybeFreeze(scope, result, true);
    revokeScope(scope);
    if (scope.patches_) {
      scope.patchListener_(scope.patches_, scope.inversePatches_);
    }
    return result !== NOTHING ? result : void 0;
  }
  function finalize(rootScope, value2) {
    if (isFrozen(value2))
      return value2;
    const state = value2[DRAFT_STATE];
    if (!state) {
      const finalValue = handleValue(value2, rootScope.handledSet_, rootScope);
      return finalValue;
    }
    if (!isSameScope(state, rootScope)) {
      return value2;
    }
    if (!state.modified_) {
      return state.base_;
    }
    if (!state.finalized_) {
      const { callbacks_ } = state;
      if (callbacks_) {
        while (callbacks_.length > 0) {
          const callback = callbacks_.pop();
          callback(rootScope);
        }
      }
      generatePatchesAndFinalize(state, rootScope);
    }
    return state.copy_;
  }
  function maybeFreeze(scope, value2, deep = false) {
    if (!scope.parent_ && scope.immer_.autoFreeze_ && scope.canAutoFreeze_) {
      freeze(value2, deep);
    }
  }
  function markStateFinalized(state) {
    state.finalized_ = true;
    state.scope_.unfinalizedDrafts_--;
  }
  var isSameScope = (state, rootScope) => state.scope_ === rootScope;
  var EMPTY_LOCATIONS_RESULT = [];
  function updateDraftInParent(parent, draftValue, finalizedValue, originalKey) {
    const parentCopy = latest(parent);
    const parentType = parent.type_;
    if (originalKey !== void 0) {
      const currentValue = get2(parentCopy, originalKey, parentType);
      if (currentValue === draftValue) {
        set(parentCopy, originalKey, finalizedValue, parentType);
        return;
      }
    }
    if (!parent.draftLocations_) {
      const draftLocations = parent.draftLocations_ = /* @__PURE__ */ new Map();
      each(parentCopy, (key, value2) => {
        if (isDraft(value2)) {
          const keys = draftLocations.get(value2) || [];
          keys.push(key);
          draftLocations.set(value2, keys);
        }
      });
    }
    const locations = parent.draftLocations_.get(draftValue) ?? EMPTY_LOCATIONS_RESULT;
    for (const location of locations) {
      set(parentCopy, location, finalizedValue, parentType);
    }
  }
  function registerChildFinalizationCallback(parent, child, key) {
    parent.callbacks_.push(function childCleanup(rootScope) {
      const state = child;
      if (!state || !isSameScope(state, rootScope)) {
        return;
      }
      rootScope.mapSetPlugin_?.fixSetContents(state);
      const finalizedValue = getFinalValue(state);
      updateDraftInParent(parent, state.draft_ ?? state, finalizedValue, key);
      generatePatchesAndFinalize(state, rootScope);
    });
  }
  function generatePatchesAndFinalize(state, rootScope) {
    const shouldFinalize = state.modified_ && !state.finalized_ && (state.type_ === 3 || state.type_ === 1 && state.allIndicesReassigned_ || (state.assigned_?.size ?? 0) > 0);
    if (shouldFinalize) {
      const { patchPlugin_ } = rootScope;
      if (patchPlugin_) {
        const basePath = patchPlugin_.getPath(state);
        if (basePath) {
          patchPlugin_.generatePatches_(state, basePath, rootScope);
        }
      }
      markStateFinalized(state);
    }
  }
  function handleCrossReference(target, key, value2) {
    const { scope_ } = target;
    if (isDraft(value2)) {
      const state = value2[DRAFT_STATE];
      if (isSameScope(state, scope_)) {
        state.callbacks_.push(function crossReferenceCleanup() {
          prepareCopy(target);
          const finalizedValue = getFinalValue(state);
          updateDraftInParent(target, value2, finalizedValue, key);
        });
      }
    } else if (isDraftable(value2)) {
      target.callbacks_.push(function nestedDraftCleanup() {
        const targetCopy = latest(target);
        if (target.type_ === 3) {
          if (targetCopy.has(value2)) {
            handleValue(value2, scope_.handledSet_, scope_);
          }
        } else {
          if (get2(targetCopy, key, target.type_) === value2) {
            if (scope_.drafts_.length > 1 && (target.assigned_.get(key) ?? false) === true && target.copy_) {
              handleValue(
                get2(target.copy_, key, target.type_),
                scope_.handledSet_,
                scope_
              );
            }
          }
        }
      });
    }
  }
  function handleValue(target, handledSet, rootScope) {
    if (!rootScope.immer_.autoFreeze_ && rootScope.unfinalizedDrafts_ < 1) {
      return target;
    }
    if (isDraft(target) || handledSet.has(target) || !isDraftable(target) || isFrozen(target)) {
      return target;
    }
    handledSet.add(target);
    each(target, (key, value2) => {
      if (isDraft(value2)) {
        const state = value2[DRAFT_STATE];
        if (isSameScope(state, rootScope)) {
          const updatedValue = getFinalValue(state);
          set(target, key, updatedValue, target.type_);
          markStateFinalized(state);
        }
      } else if (isDraftable(value2)) {
        handleValue(value2, handledSet, rootScope);
      }
    });
    return target;
  }
  function createProxyProxy(base, parent) {
    const baseIsArray = isArray(base);
    const state = {
      type_: baseIsArray ? 1 : 0,
      // Track which produce call this is associated with.
      scope_: parent ? parent.scope_ : getCurrentScope(),
      // True for both shallow and deep changes.
      modified_: false,
      // Used during finalization.
      finalized_: false,
      // Track which properties have been assigned (true) or deleted (false).
      // actually instantiated in `prepareCopy()`
      assigned_: void 0,
      // The parent draft state.
      parent_: parent,
      // The base state.
      base_: base,
      // The base proxy.
      draft_: null,
      // set below
      // The base copy with any updated values.
      copy_: null,
      // Called by the `produce` function.
      revoke_: null,
      isManual_: false,
      // `callbacks` actually gets assigned in `createProxy`
      callbacks_: void 0
    };
    let target = state;
    let traps = objectTraps;
    if (baseIsArray) {
      target = [state];
      traps = arrayTraps;
    }
    const { revoke, proxy } = Proxy.revocable(target, traps);
    state.draft_ = proxy;
    state.revoke_ = revoke;
    return [proxy, state];
  }
  var objectTraps = {
    get(state, prop) {
      if (prop === DRAFT_STATE)
        return state;
      let arrayPlugin = state.scope_.arrayMethodsPlugin_;
      const isArrayWithStringProp = state.type_ === 1 && typeof prop === "string";
      if (isArrayWithStringProp) {
        if (arrayPlugin?.isArrayOperationMethod(prop)) {
          return arrayPlugin.createMethodInterceptor(state, prop);
        }
      }
      const source = latest(state);
      if (!has2(source, prop, state.type_)) {
        return readPropFromProto(state, source, prop);
      }
      const value2 = source[prop];
      if (state.finalized_ || !isDraftable(value2)) {
        return value2;
      }
      if (isArrayWithStringProp && state.operationMethod && arrayPlugin?.isMutatingArrayMethod(
        state.operationMethod
      ) && isArrayIndex(prop)) {
        return value2;
      }
      if (value2 === peek(state.base_, prop) || isRelocatedBaseRef(state, prop, value2)) {
        prepareCopy(state);
        const childKey = state.type_ === 1 ? +prop : prop;
        const childDraft = createProxy(state.scope_, value2, state, childKey);
        return state.copy_[childKey] = childDraft;
      }
      return value2;
    },
    has(state, prop) {
      return prop in latest(state);
    },
    ownKeys(state) {
      return Reflect.ownKeys(latest(state));
    },
    set(state, prop, value2) {
      const desc = getDescriptorFromProto(latest(state), prop);
      if (desc?.set) {
        desc.set.call(state.draft_, value2);
        return true;
      }
      if (!state.modified_) {
        const current2 = peek(latest(state), prop);
        const currentState = current2?.[DRAFT_STATE];
        if (currentState && currentState.base_ === value2) {
          state.copy_[prop] = value2;
          state.assigned_.set(prop, false);
          return true;
        }
        if (is(value2, current2) && (value2 !== void 0 || has2(state.base_, prop, state.type_)))
          return true;
        prepareCopy(state);
        markChanged(state);
      }
      if (state.copy_[prop] === value2 && // special case: handle new props with value 'undefined'
      (value2 !== void 0 || has2(state.copy_, prop, state.type_)) || // special case: NaN
      Number.isNaN(value2) && Number.isNaN(state.copy_[prop]))
        return true;
      state.copy_[prop] = value2;
      state.assigned_.set(prop, true);
      handleCrossReference(state, prop, value2);
      return true;
    },
    deleteProperty(state, prop) {
      prepareCopy(state);
      if (peek(state.base_, prop) !== void 0 || prop in state.base_) {
        state.assigned_.set(prop, false);
        markChanged(state);
      } else {
        state.assigned_.delete(prop);
      }
      if (state.copy_) {
        delete state.copy_[prop];
      }
      return true;
    },
    // Note: We never coerce `desc.value` into an Immer draft, because we can't make
    // the same guarantee in ES5 mode.
    getOwnPropertyDescriptor(state, prop) {
      const owner = latest(state);
      const desc = Reflect.getOwnPropertyDescriptor(owner, prop);
      if (!desc)
        return desc;
      return {
        [WRITABLE]: true,
        [CONFIGURABLE]: state.type_ !== 1 || prop !== "length",
        [ENUMERABLE]: desc[ENUMERABLE],
        [VALUE]: owner[prop]
      };
    },
    defineProperty() {
      die(11);
    },
    getPrototypeOf(state) {
      return getPrototypeOf(state.base_);
    },
    setPrototypeOf() {
      die(12);
    }
  };
  var arrayTraps = {};
  for (let key in objectTraps) {
    let fn = objectTraps[key];
    arrayTraps[key] = function() {
      const args = arguments;
      args[0] = args[0][0];
      return fn.apply(this, args);
    };
  }
  arrayTraps.deleteProperty = function(state, prop) {
    if (isNaN(parseInt(prop)))
      die(13);
    return arrayTraps.set.call(this, state, prop, void 0);
  };
  arrayTraps.set = function(state, prop, value2) {
    if (prop !== "length" && isNaN(parseInt(prop)))
      die(14);
    return objectTraps.set.call(this, state[0], prop, value2, state[0]);
  };
  function peek(draft, prop) {
    const state = draft[DRAFT_STATE];
    const source = state ? latest(state) : draft;
    return source[prop];
  }
  function isRelocatedBaseRef(state, prop, value2) {
    if (state.type_ !== 1 || !state.allIndicesReassigned_ || state.assigned_?.get(prop) || !isDraftable(value2) || value2[DRAFT_STATE]) {
      return false;
    }
    return state.baseRefs_.has(value2);
  }
  function readPropFromProto(state, source, prop) {
    const desc = getDescriptorFromProto(source, prop);
    return desc ? VALUE in desc ? desc[VALUE] : (
      // This is a very special case, if the prop is a getter defined by the
      // prototype, we should invoke it with the draft as context!
      desc.get?.call(state.draft_)
    ) : void 0;
  }
  function getDescriptorFromProto(source, prop) {
    if (!(prop in source))
      return void 0;
    let proto = getPrototypeOf(source);
    while (proto) {
      const desc = Object.getOwnPropertyDescriptor(proto, prop);
      if (desc)
        return desc;
      proto = getPrototypeOf(proto);
    }
    return void 0;
  }
  function markChanged(state) {
    if (!state.modified_) {
      state.modified_ = true;
      if (state.parent_) {
        markChanged(state.parent_);
      }
    }
  }
  function prepareCopy(state) {
    if (!state.copy_) {
      state.assigned_ = /* @__PURE__ */ new Map();
      state.copy_ = shallowCopy(
        state.base_,
        state.scope_.immer_.useStrictShallowCopy_
      );
    }
  }
  var Immer2 = class {
    constructor(config) {
      this.autoFreeze_ = true;
      this.useStrictShallowCopy_ = false;
      this.useStrictIteration_ = false;
      this.produce = (base, recipe, patchListener) => {
        if (isFunction(base) && !isFunction(recipe)) {
          const defaultBase = recipe;
          recipe = base;
          const self2 = this;
          return function curriedProduce(base2 = defaultBase, ...args) {
            return self2.produce(base2, (draft) => recipe.call(this, draft, ...args));
          };
        }
        if (!isFunction(recipe))
          die(6);
        if (patchListener !== void 0 && !isFunction(patchListener))
          die(7);
        let result;
        if (isDraftable(base)) {
          const scope = enterScope(this);
          const proxy = createProxy(scope, base, void 0);
          let hasError = true;
          try {
            result = recipe(proxy);
            hasError = false;
          } finally {
            if (hasError)
              revokeScope(scope);
            else
              leaveScope(scope);
          }
          usePatchesInScope(scope, patchListener);
          return processResult(result, scope);
        } else if (!base || !isObjectish(base)) {
          result = recipe(base);
          if (result === void 0)
            result = base;
          if (result === NOTHING)
            result = void 0;
          if (this.autoFreeze_)
            freeze(result, true);
          if (patchListener) {
            const p = [];
            const ip = [];
            getPlugin(PluginPatches).generateReplacementPatches_(base, result, {
              patches_: p,
              inversePatches_: ip
            });
            patchListener(p, ip);
          }
          return result;
        } else
          die(1, base);
      };
      this.produceWithPatches = (base, recipe) => {
        if (isFunction(base)) {
          return (state, ...args) => this.produceWithPatches(state, (draft) => base(draft, ...args));
        }
        let patches, inversePatches;
        const result = this.produce(base, recipe, (p, ip) => {
          patches = p;
          inversePatches = ip;
        });
        return [result, patches, inversePatches];
      };
      if (isBoolean(config?.autoFreeze))
        this.setAutoFreeze(config.autoFreeze);
      if (isBoolean(config?.useStrictShallowCopy))
        this.setUseStrictShallowCopy(config.useStrictShallowCopy);
      if (isBoolean(config?.useStrictIteration))
        this.setUseStrictIteration(config.useStrictIteration);
    }
    createDraft(base) {
      if (!isDraftable(base))
        die(8);
      if (isDraft(base))
        base = current(base);
      const scope = enterScope(this);
      const proxy = createProxy(scope, base, void 0);
      proxy[DRAFT_STATE].isManual_ = true;
      leaveScope(scope);
      return proxy;
    }
    finishDraft(draft, patchListener) {
      const state = draft && draft[DRAFT_STATE];
      if (!state || !state.isManual_)
        die(9);
      const { scope_: scope } = state;
      usePatchesInScope(scope, patchListener);
      return processResult(void 0, scope);
    }
    /**
     * Pass true to automatically freeze all copies created by Immer.
     *
     * By default, auto-freezing is enabled.
     */
    setAutoFreeze(value2) {
      this.autoFreeze_ = value2;
    }
    /**
     * Pass true to enable strict shallow copy.
     *
     * By default, immer does not copy the object descriptors such as getter, setter and non-enumrable properties.
     */
    setUseStrictShallowCopy(value2) {
      this.useStrictShallowCopy_ = value2;
    }
    /**
     * Pass false to use faster iteration that skips non-enumerable properties
     * but still handles symbols for compatibility.
     *
     * By default, strict iteration is enabled (includes all own properties).
     */
    setUseStrictIteration(value2) {
      this.useStrictIteration_ = value2;
    }
    shouldUseStrictIteration() {
      return this.useStrictIteration_;
    }
    applyPatches(base, patches) {
      let i;
      for (i = patches.length - 1; i >= 0; i--) {
        const patch = patches[i];
        if (patch.path.length === 0 && patch.op === "replace") {
          base = patch.value;
          break;
        }
      }
      if (i > -1) {
        patches = patches.slice(i + 1);
      }
      const applyPatchesImpl = getPlugin(PluginPatches).applyPatches_;
      if (isDraft(base)) {
        return applyPatchesImpl(base, patches);
      }
      return this.produce(
        base,
        (draft) => applyPatchesImpl(draft, patches)
      );
    }
  };
  function createProxy(rootScope, value2, parent, key) {
    const [draft, state] = isMap(value2) ? getPlugin(PluginMapSet).proxyMap_(value2, parent) : isSet(value2) ? getPlugin(PluginMapSet).proxySet_(value2, parent) : createProxyProxy(value2, parent);
    const scope = parent?.scope_ ?? getCurrentScope();
    scope.drafts_.push(draft);
    state.callbacks_ = parent?.callbacks_ ?? [];
    state.key_ = key;
    if (parent && key !== void 0) {
      registerChildFinalizationCallback(parent, state, key);
    } else {
      state.callbacks_.push(function rootDraftCleanup(rootScope2) {
        rootScope2.mapSetPlugin_?.fixSetContents(state);
        const { patchPlugin_ } = rootScope2;
        if (state.modified_ && patchPlugin_) {
          patchPlugin_.generatePatches_(state, [], rootScope2);
        }
      });
    }
    return draft;
  }
  function current(value2) {
    if (!isDraft(value2))
      die(10, value2);
    return currentImpl(value2);
  }
  function currentImpl(value2) {
    if (!isDraftable(value2) || isFrozen(value2))
      return value2;
    const state = value2[DRAFT_STATE];
    let copy3;
    let strict = true;
    if (state) {
      if (!state.modified_)
        return state.base_;
      state.finalized_ = true;
      copy3 = shallowCopy(value2, state.scope_.immer_.useStrictShallowCopy_);
      strict = state.scope_.immer_.shouldUseStrictIteration();
    } else {
      copy3 = shallowCopy(value2, true);
    }
    each(
      copy3,
      (key, childValue) => {
        set(copy3, key, currentImpl(childValue));
      },
      strict
    );
    if (state) {
      state.finalized_ = false;
    }
    return copy3;
  }
  var immer = new Immer2();
  var produce = immer.produce;
  var castDraft = (value2) => value2;

  // node_modules/redux-thunk/dist/redux-thunk.mjs
  function createThunkMiddleware(extraArgument) {
    const middleware = ({ dispatch: dispatch2, getState }) => (next) => (action) => {
      if (typeof action === "function") {
        return action(dispatch2, getState, extraArgument);
      }
      return next(action);
    };
    return middleware;
  }
  var thunk = createThunkMiddleware();
  var withExtraArgument = createThunkMiddleware;

  // node_modules/@reduxjs/toolkit/dist/redux-toolkit.modern.mjs
  var composeWithDevTools = typeof window !== "undefined" && window.__REDUX_DEVTOOLS_EXTENSION_COMPOSE__ ? window.__REDUX_DEVTOOLS_EXTENSION_COMPOSE__ : function() {
    if (arguments.length === 0) return void 0;
    if (typeof arguments[0] === "object") return compose;
    return compose.apply(null, arguments);
  };
  var devToolsEnhancer = typeof window !== "undefined" && window.__REDUX_DEVTOOLS_EXTENSION__ ? window.__REDUX_DEVTOOLS_EXTENSION__ : function() {
    return function(noop32) {
      return noop32;
    };
  };
  var hasMatchFunction = (v) => {
    return v && typeof v.match === "function";
  };
  function createAction(type, prepareAction) {
    function actionCreator(...args) {
      if (prepareAction) {
        let prepared = prepareAction(...args);
        if (!prepared) {
          throw new Error(false ? formatProdErrorMessage(0) : "prepareAction did not return an object");
        }
        return {
          type,
          payload: prepared.payload,
          ..."meta" in prepared && {
            meta: prepared.meta
          },
          ..."error" in prepared && {
            error: prepared.error
          }
        };
      }
      return {
        type,
        payload: args[0]
      };
    }
    actionCreator.toString = () => `${type}`;
    actionCreator.type = type;
    actionCreator.match = (action) => isAction(action) && action.type === type;
    return actionCreator;
  }
  function isActionCreator(action) {
    return typeof action === "function" && "type" in action && // hasMatchFunction only wants Matchers but I don't see the point in rewriting it
    hasMatchFunction(action);
  }
  function getMessage(type) {
    const splitType = type ? `${type}`.split("/") : [];
    const actionName = splitType[splitType.length - 1] || "actionCreator";
    return `Detected an action creator with type "${type || "unknown"}" being dispatched.
Make sure you're calling the action creator before dispatching, i.e. \`dispatch(${actionName}())\` instead of \`dispatch(${actionName})\`. This is necessary even if the action has no payload.`;
  }
  function createActionCreatorInvariantMiddleware(options = {}) {
    if (false) {
      return () => (next) => (action) => next(action);
    }
    const {
      isActionCreator: isActionCreator2 = isActionCreator
    } = options;
    return () => (next) => (action) => {
      if (isActionCreator2(action)) {
        console.warn(getMessage(action.type));
      }
      return next(action);
    };
  }
  function getTimeMeasureUtils(maxDelay, fnName) {
    let elapsed = 0;
    return {
      measureTime(fn) {
        const started = Date.now();
        try {
          return fn();
        } finally {
          const finished = Date.now();
          elapsed += finished - started;
        }
      },
      warnIfExceeded() {
        if (elapsed > maxDelay) {
          console.warn(`${fnName} took ${elapsed}ms, which is more than the warning threshold of ${maxDelay}ms. 
If your state or actions are very large, you may want to disable the middleware as it might cause too much of a slowdown in development mode. See https://redux-toolkit.js.org/api/getDefaultMiddleware for instructions.
It is disabled in production builds, so you don't need to worry about that.`);
        }
      }
    };
  }
  var Tuple = class _Tuple extends Array {
    constructor(...items) {
      super(...items);
      Object.setPrototypeOf(this, _Tuple.prototype);
    }
    static get [Symbol.species]() {
      return _Tuple;
    }
    concat(...arr) {
      return super.concat.apply(this, arr);
    }
    prepend(...arr) {
      if (arr.length === 1 && Array.isArray(arr[0])) {
        return new _Tuple(...arr[0].concat(this));
      }
      return new _Tuple(...arr.concat(this));
    }
  };
  function freezeDraftable(val) {
    return isDraftable(val) ? produce(val, () => {
    }) : val;
  }
  function getOrInsertComputed(map2, key, compute) {
    if (map2.has(key)) return map2.get(key);
    return map2.set(key, compute(key)).get(key);
  }
  function isImmutableDefault(value2) {
    return typeof value2 !== "object" || value2 == null || Object.isFrozen(value2);
  }
  function trackForMutations(isImmutable, ignoredPaths, obj) {
    const trackedProperties = trackProperties(isImmutable, ignoredPaths, obj);
    return {
      detectMutations() {
        return detectMutations(isImmutable, ignoredPaths, trackedProperties, obj);
      }
    };
  }
  function trackProperties(isImmutable, ignoredPaths = [], obj, path3 = "", checkedObjects = /* @__PURE__ */ new Set()) {
    const tracked = {
      value: obj
    };
    if (!isImmutable(obj) && !checkedObjects.has(obj)) {
      checkedObjects.add(obj);
      tracked.children = {};
      const hasIgnoredPaths = ignoredPaths.length > 0;
      for (const key in obj) {
        const nestedPath = path3 ? path3 + "." + key : key;
        if (hasIgnoredPaths) {
          const hasMatches = ignoredPaths.some((ignored) => {
            if (ignored instanceof RegExp) {
              return ignored.test(nestedPath);
            }
            return nestedPath === ignored;
          });
          if (hasMatches) {
            continue;
          }
        }
        tracked.children[key] = trackProperties(isImmutable, ignoredPaths, obj[key], nestedPath);
      }
    }
    return tracked;
  }
  function detectMutations(isImmutable, ignoredPaths = [], trackedProperty, obj, sameParentRef = false, path3 = "") {
    const prevObj = trackedProperty ? trackedProperty.value : void 0;
    const sameRef = prevObj === obj;
    if (sameParentRef && !sameRef && !Number.isNaN(obj)) {
      return {
        wasMutated: true,
        path: path3
      };
    }
    if (isImmutable(prevObj) || isImmutable(obj)) {
      return {
        wasMutated: false
      };
    }
    const keysToDetect = {};
    for (let key in trackedProperty.children) {
      keysToDetect[key] = true;
    }
    for (let key in obj) {
      keysToDetect[key] = true;
    }
    const hasIgnoredPaths = ignoredPaths.length > 0;
    for (let key in keysToDetect) {
      const nestedPath = path3 ? path3 + "." + key : key;
      if (hasIgnoredPaths) {
        const hasMatches = ignoredPaths.some((ignored) => {
          if (ignored instanceof RegExp) {
            return ignored.test(nestedPath);
          }
          return nestedPath === ignored;
        });
        if (hasMatches) {
          continue;
        }
      }
      const result = detectMutations(isImmutable, ignoredPaths, trackedProperty.children[key], obj[key], sameRef, nestedPath);
      if (result.wasMutated) {
        return result;
      }
    }
    return {
      wasMutated: false
    };
  }
  function createImmutableStateInvariantMiddleware(options = {}) {
    if (false) {
      return () => (next) => (action) => next(action);
    } else {
      let stringify2 = function(obj, serializer, indent, decycler) {
        return JSON.stringify(obj, getSerialize2(serializer, decycler), indent);
      }, getSerialize2 = function(serializer, decycler) {
        let stack = [], keys = [];
        if (!decycler) decycler = function(_, value2) {
          if (stack[0] === value2) return "[Circular ~]";
          return "[Circular ~." + keys.slice(0, stack.indexOf(value2)).join(".") + "]";
        };
        return function(key, value2) {
          if (stack.length > 0) {
            var thisPos = stack.indexOf(this);
            ~thisPos ? stack.splice(thisPos + 1) : stack.push(this);
            ~thisPos ? keys.splice(thisPos, Infinity, key) : keys.push(key);
            if (~stack.indexOf(value2)) value2 = decycler.call(this, key, value2);
          } else stack.push(value2);
          return serializer == null ? value2 : serializer.call(this, key, value2);
        };
      };
      var stringify = stringify2, getSerialize = getSerialize2;
      let {
        isImmutable = isImmutableDefault,
        ignoredPaths,
        warnAfter = 32
      } = options;
      const track = trackForMutations.bind(null, isImmutable, ignoredPaths);
      return ({
        getState
      }) => {
        let state = getState();
        let tracker = track(state);
        let result;
        return (next) => (action) => {
          const measureUtils = getTimeMeasureUtils(warnAfter, "ImmutableStateInvariantMiddleware");
          measureUtils.measureTime(() => {
            state = getState();
            result = tracker.detectMutations();
            tracker = track(state);
            if (result.wasMutated) {
              throw new Error(false ? formatProdErrorMessage(19) : `A state mutation was detected between dispatches, in the path '${result.path || ""}'.  This may cause incorrect behavior. (https://redux.js.org/style-guide/style-guide#do-not-mutate-state)`);
            }
          });
          const dispatchedAction = next(action);
          measureUtils.measureTime(() => {
            state = getState();
            result = tracker.detectMutations();
            tracker = track(state);
            if (result.wasMutated) {
              throw new Error(false ? formatProdErrorMessage(20) : `A state mutation was detected inside a dispatch, in the path: ${result.path || ""}. Take a look at the reducer(s) handling the action ${stringify2(action)}. (https://redux.js.org/style-guide/style-guide#do-not-mutate-state)`);
            }
          });
          measureUtils.warnIfExceeded();
          return dispatchedAction;
        };
      };
    }
  }
  function isPlain(val) {
    const type = typeof val;
    return val == null || type === "string" || type === "boolean" || type === "number" || Array.isArray(val) || isPlainObject(val);
  }
  function findNonSerializableValue(value2, path3 = "", isSerializable = isPlain, getEntries, ignoredPaths = [], cache) {
    let foundNestedSerializable;
    if (!isSerializable(value2)) {
      return {
        keyPath: path3 || "<root>",
        value: value2
      };
    }
    if (typeof value2 !== "object" || value2 === null) {
      return false;
    }
    if (cache?.has(value2)) return false;
    const entries = getEntries != null ? getEntries(value2) : Object.entries(value2);
    const hasIgnoredPaths = ignoredPaths.length > 0;
    for (const [key, nestedValue] of entries) {
      const nestedPath = path3 ? path3 + "." + key : key;
      if (hasIgnoredPaths) {
        const hasMatches = ignoredPaths.some((ignored) => {
          if (ignored instanceof RegExp) {
            return ignored.test(nestedPath);
          }
          return nestedPath === ignored;
        });
        if (hasMatches) {
          continue;
        }
      }
      if (!isSerializable(nestedValue)) {
        return {
          keyPath: nestedPath,
          value: nestedValue
        };
      }
      if (typeof nestedValue === "object") {
        foundNestedSerializable = findNonSerializableValue(nestedValue, nestedPath, isSerializable, getEntries, ignoredPaths, cache);
        if (foundNestedSerializable) {
          return foundNestedSerializable;
        }
      }
    }
    if (cache && isNestedFrozen(value2)) cache.add(value2);
    return false;
  }
  function isNestedFrozen(value2) {
    if (!Object.isFrozen(value2)) return false;
    for (const nestedValue of Object.values(value2)) {
      if (typeof nestedValue !== "object" || nestedValue === null) continue;
      if (!isNestedFrozen(nestedValue)) return false;
    }
    return true;
  }
  function createSerializableStateInvariantMiddleware(options = {}) {
    if (false) {
      return () => (next) => (action) => next(action);
    } else {
      const {
        isSerializable = isPlain,
        getEntries,
        ignoredActions = [],
        ignoredActionPaths = ["meta.arg", "meta.baseQueryMeta"],
        ignoredPaths = [],
        warnAfter = 32,
        ignoreState = false,
        ignoreActions = false,
        disableCache = false
      } = options;
      const cache = !disableCache && WeakSet ? /* @__PURE__ */ new WeakSet() : void 0;
      return (storeAPI) => (next) => (action) => {
        if (!isAction(action)) {
          return next(action);
        }
        const result = next(action);
        const measureUtils = getTimeMeasureUtils(warnAfter, "SerializableStateInvariantMiddleware");
        if (!ignoreActions && !(ignoredActions.length && ignoredActions.indexOf(action.type) !== -1)) {
          measureUtils.measureTime(() => {
            const foundActionNonSerializableValue = findNonSerializableValue(action, "", isSerializable, getEntries, ignoredActionPaths, cache);
            if (foundActionNonSerializableValue) {
              const {
                keyPath,
                value: value2
              } = foundActionNonSerializableValue;
              console.error(`A non-serializable value was detected in an action, in the path: \`${keyPath}\`. Value:`, value2, "\nTake a look at the logic that dispatched this action: ", action, "\n(See https://redux.js.org/faq/actions#why-should-type-be-a-string-or-at-least-serializable-why-should-my-action-types-be-constants)", "\n(To allow non-serializable values see: https://redux-toolkit.js.org/usage/usage-guide#working-with-non-serializable-data)");
            }
          });
        }
        if (!ignoreState) {
          measureUtils.measureTime(() => {
            const state = storeAPI.getState();
            const foundStateNonSerializableValue = findNonSerializableValue(state, "", isSerializable, getEntries, ignoredPaths, cache);
            if (foundStateNonSerializableValue) {
              const {
                keyPath,
                value: value2
              } = foundStateNonSerializableValue;
              console.error(`A non-serializable value was detected in the state, in the path: \`${keyPath}\`. Value:`, value2, `
Take a look at the reducer(s) handling this action type: ${action.type}.
(See https://redux.js.org/faq/organizing-state#can-i-put-functions-promises-or-other-non-serializable-items-in-my-store-state)`);
            }
          });
          measureUtils.warnIfExceeded();
        }
        return result;
      };
    }
  }
  function isBoolean2(x5) {
    return typeof x5 === "boolean";
  }
  var buildGetDefaultMiddleware = () => function getDefaultMiddleware(options) {
    const {
      thunk: thunk2 = true,
      immutableCheck = true,
      serializableCheck = true,
      actionCreatorCheck = true
    } = options ?? {};
    let middlewareArray = new Tuple();
    if (thunk2) {
      if (isBoolean2(thunk2)) {
        middlewareArray.push(thunk);
      } else {
        middlewareArray.push(withExtraArgument(thunk2.extraArgument));
      }
    }
    if (true) {
      if (immutableCheck) {
        let immutableOptions = {};
        if (!isBoolean2(immutableCheck)) {
          immutableOptions = immutableCheck;
        }
        middlewareArray.unshift(createImmutableStateInvariantMiddleware(immutableOptions));
      }
      if (serializableCheck) {
        let serializableOptions = {};
        if (!isBoolean2(serializableCheck)) {
          serializableOptions = serializableCheck;
        }
        middlewareArray.push(createSerializableStateInvariantMiddleware(serializableOptions));
      }
      if (actionCreatorCheck) {
        let actionCreatorOptions = {};
        if (!isBoolean2(actionCreatorCheck)) {
          actionCreatorOptions = actionCreatorCheck;
        }
        middlewareArray.unshift(createActionCreatorInvariantMiddleware(actionCreatorOptions));
      }
    }
    return middlewareArray;
  };
  var SHOULD_AUTOBATCH = "RTK_autoBatch";
  var prepareAutoBatched = () => (payload) => ({
    payload,
    meta: {
      [SHOULD_AUTOBATCH]: true
    }
  });
  var createQueueWithTimer = (timeout2) => {
    return (notify) => {
      setTimeout(notify, timeout2);
    };
  };
  var createRafWithFallbackTimer = (raf, timeout2) => {
    return (notify) => {
      let called = false;
      const callback = () => {
        if (called) return;
        called = true;
        cancelAnimationFrame(rafId4);
        clearTimeout(timerId);
        notify();
      };
      const rafId4 = raf(callback);
      const timerId = setTimeout(callback, timeout2);
    };
  };
  var autoBatchEnhancer = (options = {
    type: "raf"
  }) => (next) => (...args) => {
    const store = next(...args);
    let notifying = true;
    let shouldNotifyAtEndOfTick = false;
    let notificationQueued = false;
    const listeners = /* @__PURE__ */ new Set();
    const queueCallback = options.type === "tick" ? queueMicrotask : options.type === "raf" ? (
      // requestAnimationFrame won't exist in SSR environments. Fall back to a vague approximation just to keep from erroring.
      typeof window !== "undefined" && window.requestAnimationFrame ? createRafWithFallbackTimer(window.requestAnimationFrame, 100) : createQueueWithTimer(10)
    ) : options.type === "callback" ? options.queueNotification : createQueueWithTimer(options.timeout);
    const notifyListeners = () => {
      notificationQueued = false;
      if (shouldNotifyAtEndOfTick) {
        shouldNotifyAtEndOfTick = false;
        listeners.forEach((l) => l());
      }
    };
    return Object.assign({}, store, {
      // Override the base `store.subscribe` method to keep original listeners
      // from running if we're delaying notifications
      subscribe(listener2) {
        const wrappedListener = () => notifying && listener2();
        const unsubscribe = store.subscribe(wrappedListener);
        listeners.add(listener2);
        return () => {
          unsubscribe();
          listeners.delete(listener2);
        };
      },
      // Override the base `store.dispatch` method so that we can check actions
      // for the `shouldAutoBatch` flag and determine if batching is active
      dispatch(action) {
        try {
          notifying = !action?.meta?.[SHOULD_AUTOBATCH];
          shouldNotifyAtEndOfTick = !notifying;
          if (shouldNotifyAtEndOfTick) {
            if (!notificationQueued) {
              notificationQueued = true;
              queueCallback(notifyListeners);
            }
          }
          return store.dispatch(action);
        } finally {
          notifying = true;
        }
      }
    });
  };
  var buildGetDefaultEnhancers = (middlewareEnhancer) => function getDefaultEnhancers(options) {
    const {
      autoBatch = true
    } = options ?? {};
    let enhancerArray = new Tuple(middlewareEnhancer);
    if (autoBatch) {
      enhancerArray.push(autoBatchEnhancer(typeof autoBatch === "object" ? autoBatch : void 0));
    }
    return enhancerArray;
  };
  function configureStore(options) {
    const getDefaultMiddleware = buildGetDefaultMiddleware();
    const {
      reducer = void 0,
      middleware,
      devTools = true,
      duplicateMiddlewareCheck = true,
      preloadedState = void 0,
      enhancers = void 0
    } = options || {};
    let rootReducer2;
    if (typeof reducer === "function") {
      rootReducer2 = reducer;
    } else if (isPlainObject(reducer)) {
      rootReducer2 = combineReducers(reducer);
    } else {
      throw new Error(false ? formatProdErrorMessage(1) : "`reducer` is a required argument, and must be a function or an object of functions that can be passed to combineReducers");
    }
    if (middleware && typeof middleware !== "function") {
      throw new Error(false ? formatProdErrorMessage(2) : "`middleware` field must be a callback");
    }
    let finalMiddleware;
    if (typeof middleware === "function") {
      finalMiddleware = middleware(getDefaultMiddleware);
      if (!Array.isArray(finalMiddleware)) {
        throw new Error(false ? formatProdErrorMessage(3) : "when using a middleware builder function, an array of middleware must be returned");
      }
    } else {
      finalMiddleware = getDefaultMiddleware();
    }
    if (finalMiddleware.some((item) => typeof item !== "function")) {
      throw new Error(false ? formatProdErrorMessage(4) : "each middleware provided to configureStore must be a function");
    }
    if (duplicateMiddlewareCheck) {
      let middlewareReferences = /* @__PURE__ */ new Set();
      finalMiddleware.forEach((middleware2) => {
        if (middlewareReferences.has(middleware2)) {
          throw new Error(false ? formatProdErrorMessage(42) : "Duplicate middleware references found when creating the store. Ensure that each middleware is only included once.");
        }
        middlewareReferences.add(middleware2);
      });
    }
    let finalCompose = compose;
    if (devTools) {
      finalCompose = composeWithDevTools({
        // Enable capture of stack traces for dispatched Redux actions
        trace: true,
        ...typeof devTools === "object" && devTools
      });
    }
    const middlewareEnhancer = applyMiddleware(...finalMiddleware);
    const getDefaultEnhancers = buildGetDefaultEnhancers(middlewareEnhancer);
    if (enhancers && typeof enhancers !== "function") {
      throw new Error(false ? formatProdErrorMessage(5) : "`enhancers` field must be a callback");
    }
    let storeEnhancers = typeof enhancers === "function" ? enhancers(getDefaultEnhancers) : getDefaultEnhancers();
    if (!Array.isArray(storeEnhancers)) {
      throw new Error(false ? formatProdErrorMessage(6) : "`enhancers` callback must return an array");
    }
    if (storeEnhancers.some((item) => typeof item !== "function")) {
      throw new Error(false ? formatProdErrorMessage(7) : "each enhancer provided to configureStore must be a function");
    }
    if (finalMiddleware.length && !storeEnhancers.includes(middlewareEnhancer)) {
      console.error("middlewares were provided, but middleware enhancer was not included in final enhancers - make sure to call `getDefaultEnhancers`");
    }
    const composedEnhancer = finalCompose(...storeEnhancers);
    return createStore(rootReducer2, preloadedState, composedEnhancer);
  }
  function executeReducerBuilderCallback(builderCallback) {
    const actionsMap = {};
    const actionMatchers = [];
    let defaultCaseReducer;
    const builder = {
      addCase(typeOrActionCreator, reducer) {
        if (true) {
          if (actionMatchers.length > 0) {
            throw new Error(false ? formatProdErrorMessage(26) : "`builder.addCase` should only be called before calling `builder.addMatcher`");
          }
          if (defaultCaseReducer) {
            throw new Error(false ? formatProdErrorMessage(27) : "`builder.addCase` should only be called before calling `builder.addDefaultCase`");
          }
        }
        const type = typeof typeOrActionCreator === "string" ? typeOrActionCreator : typeOrActionCreator.type;
        if (!type) {
          throw new Error(false ? formatProdErrorMessage(28) : "`builder.addCase` cannot be called with an empty action type");
        }
        if (type in actionsMap) {
          throw new Error(false ? formatProdErrorMessage(29) : `\`builder.addCase\` cannot be called with two reducers for the same action type '${type}'`);
        }
        actionsMap[type] = reducer;
        return builder;
      },
      addAsyncThunk(asyncThunk, reducers2) {
        if (true) {
          if (defaultCaseReducer) {
            throw new Error(false ? formatProdErrorMessage(43) : "`builder.addAsyncThunk` should only be called before calling `builder.addDefaultCase`");
          }
        }
        if (reducers2.pending) actionsMap[asyncThunk.pending.type] = reducers2.pending;
        if (reducers2.rejected) actionsMap[asyncThunk.rejected.type] = reducers2.rejected;
        if (reducers2.fulfilled) actionsMap[asyncThunk.fulfilled.type] = reducers2.fulfilled;
        if (reducers2.settled) actionMatchers.push({
          matcher: asyncThunk.settled,
          reducer: reducers2.settled
        });
        return builder;
      },
      addMatcher(matcher, reducer) {
        if (true) {
          if (defaultCaseReducer) {
            throw new Error(false ? formatProdErrorMessage(30) : "`builder.addMatcher` should only be called before calling `builder.addDefaultCase`");
          }
        }
        actionMatchers.push({
          matcher,
          reducer
        });
        return builder;
      },
      addDefaultCase(reducer) {
        if (true) {
          if (defaultCaseReducer) {
            throw new Error(false ? formatProdErrorMessage(31) : "`builder.addDefaultCase` can only be called once");
          }
        }
        defaultCaseReducer = reducer;
        return builder;
      }
    };
    builderCallback(builder);
    return [actionsMap, actionMatchers, defaultCaseReducer];
  }
  function isStateFunction(x5) {
    return typeof x5 === "function";
  }
  function createReducer(initialState15, mapOrBuilderCallback) {
    if (true) {
      if (typeof mapOrBuilderCallback === "object") {
        throw new Error(false ? formatProdErrorMessage(8) : "The object notation for `createReducer` has been removed. Please use the 'builder callback' notation instead: https://redux-toolkit.js.org/api/createReducer");
      }
    }
    let [actionsMap, finalActionMatchers, finalDefaultCaseReducer] = executeReducerBuilderCallback(mapOrBuilderCallback);
    let getInitialState;
    if (isStateFunction(initialState15)) {
      getInitialState = () => freezeDraftable(initialState15());
    } else {
      const frozenInitialState = freezeDraftable(initialState15);
      getInitialState = () => frozenInitialState;
    }
    function reducer(state = getInitialState(), action) {
      let caseReducers = [actionsMap[action.type], ...finalActionMatchers.filter(({
        matcher
      }) => matcher(action)).map(({
        reducer: reducer2
      }) => reducer2)];
      if (caseReducers.filter((cr) => !!cr).length === 0) {
        caseReducers = [finalDefaultCaseReducer];
      }
      return caseReducers.reduce((previousState, caseReducer) => {
        if (caseReducer) {
          if (isDraft(previousState)) {
            const draft = previousState;
            const result = caseReducer(draft, action);
            if (result === void 0) {
              return previousState;
            }
            return result;
          } else if (!isDraftable(previousState)) {
            const result = caseReducer(previousState, action);
            if (result === void 0) {
              if (previousState === null) {
                return previousState;
              }
              throw Error("A case reducer on a non-draftable value must not return undefined");
            }
            return result;
          } else {
            return produce(previousState, (draft) => {
              return caseReducer(draft, action);
            });
          }
        }
        return previousState;
      }, state);
    }
    reducer.getInitialState = getInitialState;
    return reducer;
  }
  var matches2 = (matcher, action) => {
    if (hasMatchFunction(matcher)) {
      return matcher.match(action);
    } else {
      return matcher(action);
    }
  };
  function isAnyOf(...matchers) {
    return (action) => {
      return matchers.some((matcher) => matches2(matcher, action));
    };
  }
  var urlAlphabet = "ModuleSymbhasOwnPr-0123456789ABCDEFGHNRVfgctiUvz_KqYTJkLxpZXIjQW";
  var nanoid = (size = 21) => {
    let id = "";
    let i = size;
    while (i--) {
      id += urlAlphabet[Math.random() * 64 | 0];
    }
    return id;
  };
  var commonProperties = ["name", "message", "stack", "code"];
  var RejectWithValue = class {
    constructor(payload, meta) {
      this.payload = payload;
      this.meta = meta;
    }
    payload;
    meta;
    /*
    type-only property to distinguish between RejectWithValue and FulfillWithMeta
    does not exist at runtime
    */
    _type;
  };
  var FulfillWithMeta = class {
    constructor(payload, meta) {
      this.payload = payload;
      this.meta = meta;
    }
    payload;
    meta;
    /*
    type-only property to distinguish between RejectWithValue and FulfillWithMeta
    does not exist at runtime
    */
    _type;
  };
  var miniSerializeError = (value2) => {
    if (typeof value2 === "object" && value2 !== null) {
      const simpleError = {};
      for (const property2 of commonProperties) {
        if (typeof value2[property2] === "string") {
          simpleError[property2] = value2[property2];
        }
      }
      return simpleError;
    }
    return {
      message: String(value2)
    };
  };
  var externalAbortMessage = "External signal was aborted";
  var createAsyncThunk = /* @__PURE__ */ (() => {
    function createAsyncThunk2(typePrefix, payloadCreator, options) {
      const fulfilled = createAction(typePrefix + "/fulfilled", (payload, requestId, arg, meta) => ({
        payload,
        meta: {
          ...meta || {},
          arg,
          requestId,
          requestStatus: "fulfilled"
        }
      }));
      const pending = createAction(typePrefix + "/pending", (requestId, arg, meta) => ({
        payload: void 0,
        meta: {
          ...meta || {},
          arg,
          requestId,
          requestStatus: "pending"
        }
      }));
      const rejected = createAction(typePrefix + "/rejected", (error, requestId, arg, payload, meta) => ({
        payload,
        error: (options && options.serializeError || miniSerializeError)(error || "Rejected"),
        meta: {
          ...meta || {},
          arg,
          requestId,
          rejectedWithValue: !!payload,
          requestStatus: "rejected",
          aborted: error?.name === "AbortError",
          condition: error?.name === "ConditionError"
        }
      }));
      function actionCreator(arg, {
        signal
      } = {}) {
        return (dispatch2, getState, extra) => {
          const requestId = options?.idGenerator ? options.idGenerator(arg) : nanoid();
          const abortController = new AbortController();
          let abortHandler;
          let abortReason;
          function abort(reason) {
            abortReason = reason;
            abortController.abort();
          }
          if (signal) {
            if (signal.aborted) {
              abort(externalAbortMessage);
            } else {
              signal.addEventListener("abort", () => abort(externalAbortMessage), {
                once: true
              });
            }
          }
          const promise = (async function() {
            let finalAction;
            try {
              let conditionResult = options?.condition?.(arg, {
                getState,
                extra
              });
              if (isThenable(conditionResult)) {
                conditionResult = await conditionResult;
              }
              if (conditionResult === false || abortController.signal.aborted) {
                throw {
                  name: "ConditionError",
                  message: "Aborted due to condition callback returning false."
                };
              }
              const abortedPromise = new Promise((_, reject) => {
                abortHandler = () => {
                  reject({
                    name: "AbortError",
                    message: abortReason || "Aborted"
                  });
                };
                abortController.signal.addEventListener("abort", abortHandler, {
                  once: true
                });
              });
              dispatch2(pending(requestId, arg, options?.getPendingMeta?.({
                requestId,
                arg
              }, {
                getState,
                extra
              })));
              finalAction = await Promise.race([abortedPromise, Promise.resolve(payloadCreator(arg, {
                dispatch: dispatch2,
                getState,
                extra,
                requestId,
                signal: abortController.signal,
                abort,
                rejectWithValue: ((value2, meta) => {
                  return new RejectWithValue(value2, meta);
                }),
                fulfillWithValue: ((value2, meta) => {
                  return new FulfillWithMeta(value2, meta);
                })
              })).then((result) => {
                if (result instanceof RejectWithValue) {
                  throw result;
                }
                if (result instanceof FulfillWithMeta) {
                  return fulfilled(result.payload, requestId, arg, result.meta);
                }
                return fulfilled(result, requestId, arg);
              })]);
            } catch (err) {
              finalAction = err instanceof RejectWithValue ? rejected(null, requestId, arg, err.payload, err.meta) : rejected(err, requestId, arg);
            } finally {
              if (abortHandler) {
                abortController.signal.removeEventListener("abort", abortHandler);
              }
            }
            const skipDispatch = options && !options.dispatchConditionRejection && rejected.match(finalAction) && finalAction.meta.condition;
            if (!skipDispatch) {
              dispatch2(finalAction);
            }
            return finalAction;
          })();
          return Object.assign(promise, {
            abort,
            requestId,
            arg,
            unwrap() {
              return promise.then(unwrapResult);
            }
          });
        };
      }
      return Object.assign(actionCreator, {
        pending,
        rejected,
        fulfilled,
        settled: isAnyOf(rejected, fulfilled),
        typePrefix
      });
    }
    createAsyncThunk2.withTypes = () => createAsyncThunk2;
    return createAsyncThunk2;
  })();
  function unwrapResult(action) {
    if (action.meta && action.meta.rejectedWithValue) {
      throw action.payload;
    }
    if (action.error) {
      throw action.error;
    }
    return action.payload;
  }
  function isThenable(value2) {
    return value2 !== null && typeof value2 === "object" && typeof value2.then === "function";
  }
  var asyncThunkSymbol = /* @__PURE__ */ Symbol.for("rtk-slice-createasyncthunk");
  var asyncThunkCreator = {
    [asyncThunkSymbol]: createAsyncThunk
  };
  function getType(slice4, actionKey) {
    return `${slice4}/${actionKey}`;
  }
  function buildCreateSlice({
    creators
  } = {}) {
    const cAT = creators?.asyncThunk?.[asyncThunkSymbol];
    return function createSlice2(options) {
      const {
        name,
        reducerPath = name
      } = options;
      if (!name) {
        throw new Error(false ? formatProdErrorMessage(11) : "`name` is a required option for createSlice");
      }
      if (typeof process !== "undefined" && true) {
        if (options.initialState === void 0) {
          console.error("You must provide an `initialState` value that is not `undefined`. You may have misspelled `initialState`");
        }
      }
      const reducers2 = (typeof options.reducers === "function" ? options.reducers(buildReducerCreators()) : options.reducers) || {};
      const reducerNames = Object.keys(reducers2);
      const context = {
        sliceCaseReducersByName: {},
        sliceCaseReducersByType: {},
        actionCreators: {},
        sliceMatchers: []
      };
      const contextMethods = {
        addCase(typeOrActionCreator, reducer2) {
          const type = typeof typeOrActionCreator === "string" ? typeOrActionCreator : typeOrActionCreator.type;
          if (!type) {
            throw new Error(false ? formatProdErrorMessage(12) : "`context.addCase` cannot be called with an empty action type");
          }
          if (type in context.sliceCaseReducersByType) {
            throw new Error(false ? formatProdErrorMessage(13) : "`context.addCase` cannot be called with two reducers for the same action type: " + type);
          }
          context.sliceCaseReducersByType[type] = reducer2;
          return contextMethods;
        },
        addMatcher(matcher, reducer2) {
          context.sliceMatchers.push({
            matcher,
            reducer: reducer2
          });
          return contextMethods;
        },
        exposeAction(name2, actionCreator) {
          context.actionCreators[name2] = actionCreator;
          return contextMethods;
        },
        exposeCaseReducer(name2, reducer2) {
          context.sliceCaseReducersByName[name2] = reducer2;
          return contextMethods;
        }
      };
      reducerNames.forEach((reducerName) => {
        const reducerDefinition = reducers2[reducerName];
        const reducerDetails = {
          reducerName,
          type: getType(name, reducerName),
          createNotation: typeof options.reducers === "function"
        };
        if (isAsyncThunkSliceReducerDefinition(reducerDefinition)) {
          handleThunkCaseReducerDefinition(reducerDetails, reducerDefinition, contextMethods, cAT);
        } else {
          handleNormalReducerDefinition(reducerDetails, reducerDefinition, contextMethods);
        }
      });
      function buildReducer() {
        if (true) {
          if (typeof options.extraReducers === "object") {
            throw new Error(false ? formatProdErrorMessage(14) : "The object notation for `createSlice.extraReducers` has been removed. Please use the 'builder callback' notation instead: https://redux-toolkit.js.org/api/createSlice");
          }
        }
        const [extraReducers = {}, actionMatchers = [], defaultCaseReducer = void 0] = typeof options.extraReducers === "function" ? executeReducerBuilderCallback(options.extraReducers) : [options.extraReducers];
        const finalCaseReducers = {
          ...extraReducers,
          ...context.sliceCaseReducersByType
        };
        return createReducer(options.initialState, (builder) => {
          for (let key in finalCaseReducers) {
            builder.addCase(key, finalCaseReducers[key]);
          }
          for (let sM of context.sliceMatchers) {
            builder.addMatcher(sM.matcher, sM.reducer);
          }
          for (let m2 of actionMatchers) {
            builder.addMatcher(m2.matcher, m2.reducer);
          }
          if (defaultCaseReducer) {
            builder.addDefaultCase(defaultCaseReducer);
          }
        });
      }
      const selectSelf = (state) => state;
      const injectedSelectorCache = /* @__PURE__ */ new Map();
      const injectedStateCache = /* @__PURE__ */ new WeakMap();
      let _reducer;
      function reducer(state, action) {
        if (!_reducer) _reducer = buildReducer();
        return _reducer(state, action);
      }
      function getInitialState() {
        if (!_reducer) _reducer = buildReducer();
        return _reducer.getInitialState();
      }
      function makeSelectorProps(reducerPath2, injected = false) {
        function selectSlice(state) {
          let sliceState = state[reducerPath2];
          if (typeof sliceState === "undefined") {
            if (injected) {
              sliceState = getOrInsertComputed(injectedStateCache, selectSlice, getInitialState);
            } else if (true) {
              throw new Error(false ? formatProdErrorMessage(15) : "selectSlice returned undefined for an uninjected slice reducer");
            }
          }
          return sliceState;
        }
        function getSelectors(selectState = selectSelf) {
          const selectorCache = getOrInsertComputed(injectedSelectorCache, injected, () => /* @__PURE__ */ new WeakMap());
          return getOrInsertComputed(selectorCache, selectState, () => {
            const map2 = {};
            for (const [name2, selector] of Object.entries(options.selectors ?? {})) {
              map2[name2] = wrapSelector(selector, selectState, () => getOrInsertComputed(injectedStateCache, selectState, getInitialState), injected);
            }
            return map2;
          });
        }
        return {
          reducerPath: reducerPath2,
          getSelectors,
          get selectors() {
            return getSelectors(selectSlice);
          },
          selectSlice
        };
      }
      const slice4 = {
        name,
        reducer,
        actions: context.actionCreators,
        caseReducers: context.sliceCaseReducersByName,
        getInitialState,
        ...makeSelectorProps(reducerPath),
        injectInto(injectable, {
          reducerPath: pathOpt,
          ...config
        } = {}) {
          const newReducerPath = pathOpt ?? reducerPath;
          injectable.inject({
            reducerPath: newReducerPath,
            reducer
          }, config);
          return {
            ...slice4,
            ...makeSelectorProps(newReducerPath, true)
          };
        }
      };
      return slice4;
    };
  }
  function wrapSelector(selector, selectState, getInitialState, injected) {
    function wrapper(rootState, ...args) {
      let sliceState = selectState(rootState);
      if (typeof sliceState === "undefined") {
        if (injected) {
          sliceState = getInitialState();
        } else if (true) {
          throw new Error(false ? formatProdErrorMessage(16) : "selectState returned undefined for an uninjected slice reducer");
        }
      }
      return selector(sliceState, ...args);
    }
    wrapper.unwrapped = selector;
    return wrapper;
  }
  var createSlice = /* @__PURE__ */ buildCreateSlice();
  function buildReducerCreators() {
    function asyncThunk(payloadCreator, config) {
      return {
        _reducerDefinitionType: "asyncThunk",
        payloadCreator,
        ...config
      };
    }
    asyncThunk.withTypes = () => asyncThunk;
    return {
      reducer(caseReducer) {
        return Object.assign({
          // hack so the wrapping function has the same name as the original
          // we need to create a wrapper so the `reducerDefinitionType` is not assigned to the original
          [caseReducer.name](...args) {
            return caseReducer(...args);
          }
        }[caseReducer.name], {
          _reducerDefinitionType: "reducer"
          /* reducer */
        });
      },
      preparedReducer(prepare, reducer) {
        return {
          _reducerDefinitionType: "reducerWithPrepare",
          prepare,
          reducer
        };
      },
      asyncThunk
    };
  }
  function handleNormalReducerDefinition({
    type,
    reducerName,
    createNotation
  }, maybeReducerWithPrepare, context) {
    let caseReducer;
    let prepareCallback;
    if ("reducer" in maybeReducerWithPrepare) {
      if (createNotation && !isCaseReducerWithPrepareDefinition(maybeReducerWithPrepare)) {
        throw new Error(false ? formatProdErrorMessage(17) : "Please use the `create.preparedReducer` notation for prepared action creators with the `create` notation.");
      }
      caseReducer = maybeReducerWithPrepare.reducer;
      prepareCallback = maybeReducerWithPrepare.prepare;
    } else {
      caseReducer = maybeReducerWithPrepare;
    }
    context.addCase(type, caseReducer).exposeCaseReducer(reducerName, caseReducer).exposeAction(reducerName, prepareCallback ? createAction(type, prepareCallback) : createAction(type));
  }
  function isAsyncThunkSliceReducerDefinition(reducerDefinition) {
    return reducerDefinition._reducerDefinitionType === "asyncThunk";
  }
  function isCaseReducerWithPrepareDefinition(reducerDefinition) {
    return reducerDefinition._reducerDefinitionType === "reducerWithPrepare";
  }
  function handleThunkCaseReducerDefinition({
    type,
    reducerName
  }, reducerDefinition, context, cAT) {
    if (!cAT) {
      throw new Error(false ? formatProdErrorMessage(18) : "Cannot use `create.asyncThunk` in the built-in `createSlice`. Use `buildCreateSlice({ creators: { asyncThunk: asyncThunkCreator } })` to create a customised version of `createSlice`.");
    }
    const {
      payloadCreator,
      fulfilled,
      pending,
      rejected,
      settled,
      options
    } = reducerDefinition;
    const thunk2 = cAT(type, payloadCreator, options);
    context.exposeAction(reducerName, thunk2);
    if (fulfilled) {
      context.addCase(thunk2.fulfilled, fulfilled);
    }
    if (pending) {
      context.addCase(thunk2.pending, pending);
    }
    if (rejected) {
      context.addCase(thunk2.rejected, rejected);
    }
    if (settled) {
      context.addMatcher(thunk2.settled, settled);
    }
    context.exposeCaseReducer(reducerName, {
      fulfilled: fulfilled || noop3,
      pending: pending || noop3,
      rejected: rejected || noop3,
      settled: settled || noop3
    });
  }
  function noop3() {
  }
  var task = "task";
  var listener = "listener";
  var completed = "completed";
  var cancelled = "cancelled";
  var taskCancelled = `task-${cancelled}`;
  var taskCompleted = `task-${completed}`;
  var listenerCancelled = `${listener}-${cancelled}`;
  var listenerCompleted = `${listener}-${completed}`;
  var TaskAbortError = class {
    constructor(code) {
      this.code = code;
      this.message = `${task} ${cancelled} (reason: ${code})`;
    }
    code;
    name = "TaskAbortError";
    message;
  };
  var assertFunction = (func, expected) => {
    if (typeof func !== "function") {
      throw new TypeError(false ? formatProdErrorMessage(32) : `${expected} is not a function`);
    }
  };
  var noop22 = () => {
  };
  var catchRejection = (promise, onError = noop22) => {
    promise.catch(onError);
    return promise;
  };
  var addAbortSignalListener = (abortSignal, callback) => {
    abortSignal.addEventListener("abort", callback, {
      once: true
    });
    return () => abortSignal.removeEventListener("abort", callback);
  };
  var validateActive = (signal) => {
    if (signal.aborted) {
      throw new TaskAbortError(signal.reason);
    }
  };
  function raceWithSignal(signal, promise) {
    let cleanup = noop22;
    return new Promise((resolve, reject) => {
      const notifyRejection = () => reject(new TaskAbortError(signal.reason));
      if (signal.aborted) {
        notifyRejection();
        return;
      }
      cleanup = addAbortSignalListener(signal, notifyRejection);
      promise.finally(() => cleanup()).then(resolve, reject);
    }).finally(() => {
      cleanup = noop22;
    });
  }
  var runTask = async (task2, cleanUp) => {
    try {
      await Promise.resolve();
      const value2 = await task2();
      return {
        status: "ok",
        value: value2
      };
    } catch (error) {
      return {
        status: error instanceof TaskAbortError ? "cancelled" : "rejected",
        error
      };
    } finally {
      cleanUp?.();
    }
  };
  var createPause = (signal) => {
    return (promise) => {
      return catchRejection(raceWithSignal(signal, promise).then((output) => {
        validateActive(signal);
        return output;
      }));
    };
  };
  var createDelay = (signal) => {
    const pause = createPause(signal);
    return (timeoutMs) => {
      return pause(new Promise((resolve) => setTimeout(resolve, timeoutMs)));
    };
  };
  var {
    assign
  } = Object;
  var INTERNAL_NIL_TOKEN = {};
  var alm = "listenerMiddleware";
  var createFork = (parentAbortSignal, parentBlockingPromises) => {
    const linkControllers = (controller) => addAbortSignalListener(parentAbortSignal, () => controller.abort(parentAbortSignal.reason));
    return (taskExecutor, opts) => {
      assertFunction(taskExecutor, "taskExecutor");
      const childAbortController = new AbortController();
      linkControllers(childAbortController);
      const result = runTask(async () => {
        validateActive(parentAbortSignal);
        validateActive(childAbortController.signal);
        const result2 = await taskExecutor({
          pause: createPause(childAbortController.signal),
          delay: createDelay(childAbortController.signal),
          signal: childAbortController.signal
        });
        validateActive(childAbortController.signal);
        return result2;
      }, () => childAbortController.abort(taskCompleted));
      if (opts?.autoJoin) {
        parentBlockingPromises.push(result.catch(noop22));
      }
      return {
        result: createPause(parentAbortSignal)(result),
        cancel() {
          childAbortController.abort(taskCancelled);
        }
      };
    };
  };
  var createTakePattern = (startListening, signal) => {
    const take = async (predicate, timeout2) => {
      validateActive(signal);
      let unsubscribe = () => {
      };
      const tuplePromise = new Promise((resolve, reject) => {
        let stopListening = startListening({
          predicate,
          effect: (action, listenerApi) => {
            listenerApi.unsubscribe();
            resolve([action, listenerApi.getState(), listenerApi.getOriginalState()]);
          }
        });
        unsubscribe = () => {
          stopListening();
          reject();
        };
      });
      const promises = [tuplePromise];
      if (timeout2 != null) {
        promises.push(new Promise((resolve) => setTimeout(resolve, timeout2, null)));
      }
      try {
        const output = await raceWithSignal(signal, Promise.race(promises));
        validateActive(signal);
        return output;
      } finally {
        unsubscribe();
      }
    };
    return ((predicate, timeout2) => catchRejection(take(predicate, timeout2)));
  };
  var getListenerEntryPropsFrom = (options) => {
    let {
      type,
      actionCreator,
      matcher,
      predicate,
      effect
    } = options;
    if (type) {
      predicate = createAction(type).match;
    } else if (actionCreator) {
      type = actionCreator.type;
      predicate = actionCreator.match;
    } else if (matcher) {
      predicate = matcher;
    } else if (predicate) {
    } else {
      throw new Error(false ? formatProdErrorMessage(21) : "Creating or removing a listener requires one of the known fields for matching an action");
    }
    assertFunction(effect, "options.listener");
    return {
      predicate,
      type,
      effect
    };
  };
  var createListenerEntry = /* @__PURE__ */ assign((options) => {
    const {
      type,
      predicate,
      effect
    } = getListenerEntryPropsFrom(options);
    const entry = {
      id: nanoid(),
      effect,
      type,
      predicate,
      pending: /* @__PURE__ */ new Set(),
      unsubscribe: () => {
        throw new Error(false ? formatProdErrorMessage(22) : "Unsubscribe not initialized");
      }
    };
    return entry;
  }, {
    withTypes: () => createListenerEntry
  });
  var findListenerEntry = (listenerMap, options) => {
    const {
      type,
      effect,
      predicate
    } = getListenerEntryPropsFrom(options);
    return Array.from(listenerMap.values()).find((entry) => {
      const matchPredicateOrType = typeof type === "string" ? entry.type === type : entry.predicate === predicate;
      return matchPredicateOrType && entry.effect === effect;
    });
  };
  var cancelActiveListeners = (entry) => {
    entry.pending.forEach((controller) => {
      controller.abort(listenerCancelled);
    });
  };
  var createClearListenerMiddleware = (listenerMap, executingListeners) => {
    return () => {
      for (const listener2 of executingListeners.keys()) {
        cancelActiveListeners(listener2);
      }
      listenerMap.clear();
    };
  };
  var safelyNotifyError = (errorHandler, errorToNotify, errorInfo) => {
    try {
      errorHandler(errorToNotify, errorInfo);
    } catch (errorHandlerError) {
      setTimeout(() => {
        throw errorHandlerError;
      }, 0);
    }
  };
  var addListener = /* @__PURE__ */ assign(/* @__PURE__ */ createAction(`${alm}/add`), {
    withTypes: () => addListener
  });
  var clearAllListeners = /* @__PURE__ */ createAction(`${alm}/removeAll`);
  var removeListener = /* @__PURE__ */ assign(/* @__PURE__ */ createAction(`${alm}/remove`), {
    withTypes: () => removeListener
  });
  var defaultErrorHandler = (...args) => {
    console.error(`${alm}/error`, ...args);
  };
  var createListenerMiddleware = (middlewareOptions = {}) => {
    const listenerMap = /* @__PURE__ */ new Map();
    const executingListeners = /* @__PURE__ */ new Map();
    const trackExecutingListener = (entry) => {
      const count = executingListeners.get(entry) ?? 0;
      executingListeners.set(entry, count + 1);
    };
    const untrackExecutingListener = (entry) => {
      const count = executingListeners.get(entry) ?? 1;
      if (count === 1) {
        executingListeners.delete(entry);
      } else {
        executingListeners.set(entry, count - 1);
      }
    };
    const {
      extra,
      onError = defaultErrorHandler
    } = middlewareOptions;
    assertFunction(onError, "onError");
    const insertEntry = (entry) => {
      entry.unsubscribe = () => listenerMap.delete(entry.id);
      listenerMap.set(entry.id, entry);
      return (cancelOptions) => {
        entry.unsubscribe();
        if (cancelOptions?.cancelActive) {
          cancelActiveListeners(entry);
        }
      };
    };
    const startListening = ((options) => {
      const entry = findListenerEntry(listenerMap, options) ?? createListenerEntry(options);
      return insertEntry(entry);
    });
    assign(startListening, {
      withTypes: () => startListening
    });
    const stopListening = (options) => {
      const entry = findListenerEntry(listenerMap, options);
      if (entry) {
        entry.unsubscribe();
        if (options.cancelActive) {
          cancelActiveListeners(entry);
        }
      }
      return !!entry;
    };
    assign(stopListening, {
      withTypes: () => stopListening
    });
    const notifyListener = async (entry, action, api, getOriginalState) => {
      const internalTaskController = new AbortController();
      const take = createTakePattern(startListening, internalTaskController.signal);
      const autoJoinPromises = [];
      try {
        entry.pending.add(internalTaskController);
        trackExecutingListener(entry);
        await Promise.resolve(entry.effect(
          action,
          // Use assign() rather than ... to avoid extra helper functions added to bundle
          assign({}, api, {
            getOriginalState,
            condition: (predicate, timeout2) => take(predicate, timeout2).then(Boolean),
            take,
            delay: createDelay(internalTaskController.signal),
            pause: createPause(internalTaskController.signal),
            extra,
            signal: internalTaskController.signal,
            fork: createFork(internalTaskController.signal, autoJoinPromises),
            unsubscribe: entry.unsubscribe,
            subscribe: () => {
              listenerMap.set(entry.id, entry);
            },
            cancelActiveListeners: () => {
              entry.pending.forEach((controller, _, set3) => {
                if (controller !== internalTaskController) {
                  controller.abort(listenerCancelled);
                  set3.delete(controller);
                }
              });
            },
            cancel: () => {
              internalTaskController.abort(listenerCancelled);
              entry.pending.delete(internalTaskController);
            },
            throwIfCancelled: () => {
              validateActive(internalTaskController.signal);
            }
          })
        ));
      } catch (listenerError) {
        if (!(listenerError instanceof TaskAbortError)) {
          safelyNotifyError(onError, listenerError, {
            raisedBy: "effect"
          });
        }
      } finally {
        await Promise.all(autoJoinPromises);
        internalTaskController.abort(listenerCompleted);
        untrackExecutingListener(entry);
        entry.pending.delete(internalTaskController);
      }
    };
    const clearListenerMiddleware = createClearListenerMiddleware(listenerMap, executingListeners);
    const middleware = (api) => (next) => (action) => {
      if (!isAction(action)) {
        return next(action);
      }
      if (addListener.match(action)) {
        return startListening(action.payload);
      }
      if (clearAllListeners.match(action)) {
        clearListenerMiddleware();
        return;
      }
      if (removeListener.match(action)) {
        return stopListening(action.payload);
      }
      let originalState = api.getState();
      const getOriginalState = () => {
        if (originalState === INTERNAL_NIL_TOKEN) {
          throw new Error(false ? formatProdErrorMessage(23) : `${alm}: getOriginalState can only be called synchronously`);
        }
        return originalState;
      };
      let result;
      try {
        result = next(action);
        if (listenerMap.size > 0) {
          const currentState = api.getState();
          const listenerEntries = Array.from(listenerMap.values());
          for (const entry of listenerEntries) {
            let runListener = false;
            try {
              runListener = entry.predicate(action, currentState, originalState);
            } catch (predicateError) {
              runListener = false;
              safelyNotifyError(onError, predicateError, {
                raisedBy: "predicate"
              });
            }
            if (!runListener) {
              continue;
            }
            notifyListener(entry, action, api, getOriginalState);
          }
        }
      } finally {
        originalState = INTERNAL_NIL_TOKEN;
      }
      return result;
    };
    return {
      middleware,
      startListening,
      stopListening,
      clearListeners: clearListenerMiddleware
    };
  };

  // node_modules/recharts/es6/state/layoutSlice.js
  var initialState = {
    layoutType: "horizontal",
    width: 0,
    height: 0,
    margin: {
      top: 5,
      right: 5,
      bottom: 5,
      left: 5
    },
    scale: 1
  };
  var chartLayoutSlice = createSlice({
    name: "chartLayout",
    initialState,
    reducers: {
      setLayout(state, action) {
        state.layoutType = action.payload;
      },
      setChartSize(state, action) {
        state.width = action.payload.width;
        state.height = action.payload.height;
      },
      setMargin(state, action) {
        var _action$payload$top, _action$payload$right, _action$payload$botto, _action$payload$left;
        state.margin.top = (_action$payload$top = action.payload.top) !== null && _action$payload$top !== void 0 ? _action$payload$top : 0;
        state.margin.right = (_action$payload$right = action.payload.right) !== null && _action$payload$right !== void 0 ? _action$payload$right : 0;
        state.margin.bottom = (_action$payload$botto = action.payload.bottom) !== null && _action$payload$botto !== void 0 ? _action$payload$botto : 0;
        state.margin.left = (_action$payload$left = action.payload.left) !== null && _action$payload$left !== void 0 ? _action$payload$left : 0;
      },
      setScale(state, action) {
        state.scale = action.payload;
      }
    }
  });
  var _chartLayoutSlice$act = chartLayoutSlice.actions;
  var setMargin = _chartLayoutSlice$act.setMargin;
  var setLayout = _chartLayoutSlice$act.setLayout;
  var setChartSize = _chartLayoutSlice$act.setChartSize;
  var setScale = _chartLayoutSlice$act.setScale;
  var chartLayoutReducer = chartLayoutSlice.reducer;

  // node_modules/recharts/es6/util/getSliced.js
  function getSliced(arr, startIndex, endIndex) {
    if (!Array.isArray(arr)) {
      return arr;
    }
    if (arr && startIndex + endIndex !== 0) {
      return arr.slice(startIndex, endIndex + 1);
    }
    return arr;
  }

  // node_modules/recharts/es6/util/isWellBehavedNumber.js
  function isWellBehavedNumber(n) {
    return Number.isFinite(n);
  }
  function isPositiveNumber(n) {
    return typeof n === "number" && n > 0 && Number.isFinite(n);
  }

  // node_modules/recharts/es6/util/ChartUtils.js
  function ownKeys2(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread2(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys2(Object(t), true).forEach(function(r3) {
        _defineProperty2(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys2(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty2(e, r2, t) {
    return (r2 = _toPropertyKey2(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey2(t) {
    var i = _toPrimitive2(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive2(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  function getValueByDataKey(obj, dataKey, defaultValue) {
    if (isNullish(obj) || isNullish(dataKey)) {
      return defaultValue;
    }
    if (isNumOrStr(dataKey)) {
      return get(obj, dataKey, defaultValue);
    }
    if (typeof dataKey === "function") {
      return dataKey(obj);
    }
    return defaultValue;
  }
  var appendOffsetOfLegend = (offset, legendSettings, legendSize) => {
    if (legendSettings && legendSize) {
      var boxWidth = legendSize.width, boxHeight = legendSize.height;
      var align = legendSettings.align, verticalAlign = legendSettings.verticalAlign, layout = legendSettings.layout;
      if ((layout === "vertical" || layout === "horizontal" && verticalAlign === "middle") && align !== "center" && isNumber(offset[align])) {
        return _objectSpread2(_objectSpread2({}, offset), {}, {
          [align]: offset[align] + (boxWidth || 0)
        });
      }
      if ((layout === "horizontal" || layout === "vertical" && align === "center") && verticalAlign !== "middle" && isNumber(offset[verticalAlign])) {
        return _objectSpread2(_objectSpread2({}, offset), {}, {
          [verticalAlign]: offset[verticalAlign] + (boxHeight || 0)
        });
      }
    }
    return offset;
  };
  var isCategoricalAxis = (layout, axisType) => layout === "horizontal" && axisType === "xAxis" || layout === "vertical" && axisType === "yAxis" || layout === "centric" && axisType === "angleAxis" || layout === "radial" && axisType === "radiusAxis";
  var getCoordinatesOfGrid = (ticks, minValue, maxValue, syncWithTicks) => {
    if (syncWithTicks) {
      return ticks.map((entry) => entry.coordinate);
    }
    var hasMin, hasMax;
    var values = ticks.map((entry) => {
      if (entry.coordinate === minValue) {
        hasMin = true;
      }
      if (entry.coordinate === maxValue) {
        hasMax = true;
      }
      return entry.coordinate;
    });
    if (!hasMin) {
      values.push(minValue);
    }
    if (!hasMax) {
      values.push(maxValue);
    }
    return values;
  };
  var getTicksOfAxis = (axis, isGrid, isAll) => {
    if (!axis) {
      return null;
    }
    var duplicateDomain = axis.duplicateDomain, type = axis.type, range3 = axis.range, scale = axis.scale, realScaleType = axis.realScaleType, isCategorical = axis.isCategorical, categoricalDomain = axis.categoricalDomain, tickCount = axis.tickCount, ticks = axis.ticks, niceTicks = axis.niceTicks, axisType = axis.axisType;
    if (!scale) {
      return null;
    }
    var offsetForBand = realScaleType === "scaleBand" && scale.bandwidth ? scale.bandwidth() / 2 : 2;
    var offset = (isGrid || isAll) && type === "category" && scale.bandwidth ? scale.bandwidth() / offsetForBand : 0;
    offset = axisType === "angleAxis" && range3 && range3.length >= 2 ? mathSign(range3[0] - range3[1]) * 2 * offset : offset;
    if (isGrid && (ticks || niceTicks)) {
      var result = (ticks || niceTicks || []).map((entry, index2) => {
        var scaleContent = duplicateDomain ? duplicateDomain.indexOf(entry) : entry;
        var scaled = scale.map(scaleContent);
        if (!isWellBehavedNumber(scaled)) {
          return null;
        }
        return {
          // If the scaleContent is not a number, the coordinate will be NaN.
          // That could be the case for example with a PointScale and a string as domain.
          coordinate: scaled + offset,
          value: entry,
          offset,
          index: index2
        };
      }).filter(isNotNil);
      return result;
    }
    if (isCategorical && categoricalDomain) {
      return categoricalDomain.map((entry, index2) => {
        var scaled = scale.map(entry);
        if (!isWellBehavedNumber(scaled)) {
          return null;
        }
        return {
          coordinate: scaled + offset,
          value: entry,
          index: index2,
          offset
        };
      }).filter(isNotNil);
    }
    if (scale.ticks && !isAll && tickCount != null) {
      return scale.ticks(tickCount).map((entry, index2) => {
        var scaled = scale.map(entry);
        if (!isWellBehavedNumber(scaled)) {
          return null;
        }
        return {
          coordinate: scaled + offset,
          value: entry,
          index: index2,
          offset
        };
      }).filter(isNotNil);
    }
    return scale.domain().map((entry, index2) => {
      var scaled = scale.map(entry);
      if (!isWellBehavedNumber(scaled)) {
        return null;
      }
      return {
        coordinate: scaled + offset,
        // @ts-expect-error can't use Date as an index
        value: duplicateDomain ? duplicateDomain[entry] : entry,
        index: index2,
        offset
      };
    }).filter(isNotNil);
  };
  var truncateByDomain = (value2, domain) => {
    if (!domain || domain.length !== 2 || !isNumber(domain[0]) || !isNumber(domain[1])) {
      return value2;
    }
    var minValue = Math.min(domain[0], domain[1]);
    var maxValue = Math.max(domain[0], domain[1]);
    var result = [value2[0], value2[1]];
    if (!isNumber(value2[0]) || value2[0] < minValue) {
      result[0] = minValue;
    }
    if (!isNumber(value2[1]) || value2[1] > maxValue) {
      result[1] = maxValue;
    }
    if (result[0] > maxValue) {
      result[0] = maxValue;
    }
    if (result[1] < minValue) {
      result[1] = minValue;
    }
    return result;
  };
  var offsetSign = (series) => {
    var _series$;
    var n = series.length;
    if (n <= 0) {
      return;
    }
    var m2 = (_series$ = series[0]) === null || _series$ === void 0 ? void 0 : _series$.length;
    if (m2 == null || m2 <= 0) {
      return;
    }
    for (var j = 0; j < m2; ++j) {
      var positive = 0;
      var negative = 0;
      for (var i = 0; i < n; ++i) {
        var row = series[i];
        var col = row === null || row === void 0 ? void 0 : row[j];
        if (col == null) {
          continue;
        }
        var series1 = col[1];
        var series0 = col[0];
        var value2 = isNan(series1) ? series0 : series1;
        if (value2 >= 0) {
          col[0] = positive;
          positive += value2;
          col[1] = positive;
        } else {
          col[0] = negative;
          negative += value2;
          col[1] = negative;
        }
      }
    }
  };
  var offsetPositive = (series) => {
    var _series$2;
    var n = series.length;
    if (n <= 0) {
      return;
    }
    var m2 = (_series$2 = series[0]) === null || _series$2 === void 0 ? void 0 : _series$2.length;
    if (m2 == null || m2 <= 0) {
      return;
    }
    for (var j = 0; j < m2; ++j) {
      var positive = 0;
      for (var i = 0; i < n; ++i) {
        var row = series[i];
        var col = row === null || row === void 0 ? void 0 : row[j];
        if (col == null) {
          continue;
        }
        var value2 = isNan(col[1]) ? col[0] : col[1];
        if (value2 >= 0) {
          col[0] = positive;
          positive += value2;
          col[1] = positive;
        } else {
          col[0] = 0;
          col[1] = 0;
        }
      }
    }
  };
  var STACK_OFFSET_MAP = {
    sign: offsetSign,
    // @ts-expect-error definitelytyped types are incorrect
    expand: expand_default,
    // @ts-expect-error definitelytyped types are incorrect
    none: none_default,
    // @ts-expect-error definitelytyped types are incorrect
    silhouette: silhouette_default,
    // @ts-expect-error definitelytyped types are incorrect
    wiggle: wiggle_default,
    positive: offsetPositive
  };
  var getStackedData = (data, dataKeys, offsetType) => {
    var _STACK_OFFSET_MAP$off;
    var offsetAccessor = (_STACK_OFFSET_MAP$off = STACK_OFFSET_MAP[offsetType]) !== null && _STACK_OFFSET_MAP$off !== void 0 ? _STACK_OFFSET_MAP$off : none_default;
    var stack = stack_default().keys(dataKeys).value((d, key) => Number(getValueByDataKey(d, key, 0))).order(none_default2).offset(offsetAccessor);
    var result = stack(data);
    result.forEach((series, seriesIndex) => {
      series.forEach((point6, pointIndex) => {
        var value2 = getValueByDataKey(data[pointIndex], dataKeys[seriesIndex], 0);
        if (Array.isArray(value2) && value2.length === 2 && isNumber(value2[0]) && isNumber(value2[1])) {
          point6[0] = value2[0];
          point6[1] = value2[1];
        }
      });
    });
    return result;
  };
  function getNormalizedStackId(publicStackId) {
    return publicStackId == null ? void 0 : String(publicStackId);
  }
  function getCateCoordinateOfLine(_ref2) {
    var axis = _ref2.axis, ticks = _ref2.ticks, bandSize = _ref2.bandSize, entry = _ref2.entry, index2 = _ref2.index, dataKey = _ref2.dataKey;
    if (axis.type === "category") {
      if (!axis.allowDuplicatedCategory && axis.dataKey && !isNullish(entry[axis.dataKey])) {
        var matchedTick = findEntryInArray(ticks, "value", entry[axis.dataKey]);
        if (matchedTick) {
          return matchedTick.coordinate + bandSize / 2;
        }
      }
      return ticks !== null && ticks !== void 0 && ticks[index2] ? ticks[index2].coordinate + bandSize / 2 : null;
    }
    var value2 = getValueByDataKey(entry, !isNullish(dataKey) ? dataKey : axis.dataKey);
    var scaled = axis.scale.map(value2);
    if (!isNumber(scaled)) {
      return null;
    }
    return scaled;
  }
  var getCateCoordinateOfBar = (_ref2) => {
    var axis = _ref2.axis, ticks = _ref2.ticks, offset = _ref2.offset, bandSize = _ref2.bandSize, entry = _ref2.entry, index2 = _ref2.index;
    if (axis.type === "category") {
      return ticks[index2] ? ticks[index2].coordinate + offset : null;
    }
    var value2 = getValueByDataKey(entry, axis.dataKey, axis.scale.domain()[index2]);
    if (isNullish(value2)) {
      return null;
    }
    var scaled = axis.scale.map(value2);
    if (!isNumber(scaled)) {
      return null;
    }
    return scaled - bandSize / 2 + offset;
  };
  var getBaseValueOfBar = (_ref3) => {
    var numericAxis = _ref3.numericAxis;
    var domain = numericAxis.scale.domain();
    if (numericAxis.type === "number") {
      var minValue = Math.min(domain[0], domain[1]);
      var maxValue = Math.max(domain[0], domain[1]);
      if (minValue <= 0 && maxValue >= 0) {
        return 0;
      }
      if (maxValue < 0) {
        return maxValue;
      }
      return minValue;
    }
    return domain[0];
  };
  var getDomainOfSingle = (data) => {
    var flat = data.flat(2).filter(isNumber);
    return [Math.min(...flat), Math.max(...flat)];
  };
  var makeDomainFinite = (domain) => {
    return [domain[0] === Infinity ? 0 : domain[0], domain[1] === -Infinity ? 0 : domain[1]];
  };
  var getDomainOfStackGroups = (stackGroups, startIndex, endIndex) => {
    if (stackGroups == null || Object.keys(stackGroups).length === 0) {
      return void 0;
    }
    return makeDomainFinite(Object.keys(stackGroups).reduce((result, stackId) => {
      var group = stackGroups[stackId];
      if (!group) {
        return result;
      }
      var stackedData = group.stackedData;
      var domain = stackedData.reduce((res, entry) => {
        var sliced = getSliced(entry, startIndex, endIndex);
        var s = getDomainOfSingle(sliced);
        if (!isWellBehavedNumber(s[0]) || !isWellBehavedNumber(s[1])) {
          return res;
        }
        return [Math.min(res[0], s[0]), Math.max(res[1], s[1])];
      }, [Infinity, -Infinity]);
      return [Math.min(domain[0], result[0]), Math.max(domain[1], result[1])];
    }, [Infinity, -Infinity]));
  };
  var MIN_VALUE_REG = /^dataMin[\s]*-[\s]*([0-9]+([.]{1}[0-9]+){0,1})$/;
  var MAX_VALUE_REG = /^dataMax[\s]*\+[\s]*([0-9]+([.]{1}[0-9]+){0,1})$/;
  var getBandSizeOfAxis = (axis, ticks, isBar) => {
    if (axis && axis.scale && axis.scale.bandwidth) {
      var bandWidth = axis.scale.bandwidth();
      if (!isBar || bandWidth > 0) {
        return bandWidth;
      }
    }
    if (axis && ticks && ticks.length >= 2) {
      var orderedTicks = sortBy(ticks, (o) => o.coordinate);
      var bandSize = Infinity;
      for (var i = 1, len = orderedTicks.length; i < len; i++) {
        var cur = orderedTicks[i];
        var prev = orderedTicks[i - 1];
        bandSize = Math.min(((cur === null || cur === void 0 ? void 0 : cur.coordinate) || 0) - ((prev === null || prev === void 0 ? void 0 : prev.coordinate) || 0), bandSize);
      }
      return bandSize === Infinity ? 0 : bandSize;
    }
    return isBar ? void 0 : 0;
  };
  function getTooltipEntry(_ref4) {
    var tooltipEntrySettings = _ref4.tooltipEntrySettings, dataKey = _ref4.dataKey, payload = _ref4.payload, value2 = _ref4.value, name = _ref4.name;
    return _objectSpread2(_objectSpread2({}, tooltipEntrySettings), {}, {
      dataKey,
      payload,
      value: value2,
      name
    });
  }
  function getTooltipNameProp(nameFromItem, dataKey) {
    if (nameFromItem != null) {
      return String(nameFromItem);
    }
    if (typeof dataKey === "string") {
      return dataKey;
    }
    return void 0;
  }
  var calculateCartesianTooltipPos = (coordinate, layout) => {
    if (layout === "horizontal") {
      return coordinate.relativeX;
    }
    if (layout === "vertical") {
      return coordinate.relativeY;
    }
    return void 0;
  };
  var calculatePolarTooltipPos = (rangeObj, layout) => {
    if (layout === "centric") {
      return rangeObj.angle;
    }
    return rangeObj.radius;
  };

  // node_modules/recharts/es6/state/selectors/containerSelectors.js
  var selectChartWidth = (state) => state.layout.width;
  var selectChartHeight = (state) => state.layout.height;
  var selectContainerScale = (state) => state.layout.scale;
  var selectMargin = (state) => state.layout.margin;

  // node_modules/recharts/es6/state/selectors/selectAllAxes.js
  var selectAllXAxes = createSelector((state) => state.cartesianAxis.xAxis, (xAxisMap) => {
    return Object.values(xAxisMap);
  });
  var selectAllYAxes = createSelector((state) => state.cartesianAxis.yAxis, (yAxisMap) => {
    return Object.values(yAxisMap);
  });

  // node_modules/recharts/es6/util/Constants.js
  var DATA_ITEM_INDEX_ATTRIBUTE_NAME = "data-recharts-item-index";
  var DATA_ITEM_GRAPHICAL_ITEM_ID_ATTRIBUTE_NAME = "data-recharts-item-id";
  var DEFAULT_Y_AXIS_WIDTH = 60;

  // node_modules/recharts/es6/state/selectors/selectChartOffsetInternal.js
  function ownKeys3(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread3(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys3(Object(t), true).forEach(function(r3) {
        _defineProperty3(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys3(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty3(e, r2, t) {
    return (r2 = _toPropertyKey3(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey3(t) {
    var i = _toPrimitive3(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive3(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  var selectBrushHeight = (state) => state.brush.height;
  function selectLeftAxesOffset(state) {
    var yAxes = selectAllYAxes(state);
    return yAxes.reduce((result, entry) => {
      if (entry.orientation === "left" && !entry.mirror && !entry.hide) {
        var width = typeof entry.width === "number" ? entry.width : DEFAULT_Y_AXIS_WIDTH;
        return result + width;
      }
      return result;
    }, 0);
  }
  function selectRightAxesOffset(state) {
    var yAxes = selectAllYAxes(state);
    return yAxes.reduce((result, entry) => {
      if (entry.orientation === "right" && !entry.mirror && !entry.hide) {
        var width = typeof entry.width === "number" ? entry.width : DEFAULT_Y_AXIS_WIDTH;
        return result + width;
      }
      return result;
    }, 0);
  }
  function selectTopAxesOffset(state) {
    var xAxes = selectAllXAxes(state);
    return xAxes.reduce((result, entry) => {
      if (entry.orientation === "top" && !entry.mirror && !entry.hide) {
        return result + entry.height;
      }
      return result;
    }, 0);
  }
  function selectBottomAxesOffset(state) {
    var xAxes = selectAllXAxes(state);
    return xAxes.reduce((result, entry) => {
      if (entry.orientation === "bottom" && !entry.mirror && !entry.hide) {
        return result + entry.height;
      }
      return result;
    }, 0);
  }
  var selectChartOffsetInternal = createSelector([selectChartWidth, selectChartHeight, selectMargin, selectBrushHeight, selectLeftAxesOffset, selectRightAxesOffset, selectTopAxesOffset, selectBottomAxesOffset, selectLegendSettings, selectLegendSize], (chartWidth, chartHeight, margin, brushHeight, leftAxesOffset, rightAxesOffset, topAxesOffset, bottomAxesOffset, legendSettings, legendSize) => {
    var offsetH = {
      left: (margin.left || 0) + leftAxesOffset,
      right: (margin.right || 0) + rightAxesOffset
    };
    var offsetV = {
      top: (margin.top || 0) + topAxesOffset,
      bottom: (margin.bottom || 0) + bottomAxesOffset
    };
    var offset = _objectSpread3(_objectSpread3({}, offsetV), offsetH);
    var brushBottom = offset.bottom;
    offset.bottom += brushHeight;
    offset = appendOffsetOfLegend(offset, legendSettings, legendSize);
    var offsetWidth = chartWidth - offset.left - offset.right;
    var offsetHeight = chartHeight - offset.top - offset.bottom;
    return _objectSpread3(_objectSpread3({
      brushBottom
    }, offset), {}, {
      // never return negative values for height and width
      width: Math.max(offsetWidth, 0),
      height: Math.max(offsetHeight, 0)
    });
  });
  var selectChartViewBox = createSelector(selectChartOffsetInternal, (offset) => ({
    x: offset.left,
    y: offset.top,
    width: offset.width,
    height: offset.height
  }));
  var selectAxisViewBox = createSelector(selectChartWidth, selectChartHeight, (width, height) => ({
    x: 0,
    y: 0,
    width,
    height
  }));

  // node_modules/recharts/es6/context/PanoramaContext.js
  var React3 = __toESM(require_react());
  var import_react12 = __toESM(require_react());
  var PanoramaContext = /* @__PURE__ */ (0, import_react12.createContext)(null);
  var useIsPanorama = () => (0, import_react12.useContext)(PanoramaContext) != null;

  // node_modules/recharts/es6/state/selectors/brushSelectors.js
  var selectBrushSettings = (state) => state.brush;
  var selectBrushDimensions = createSelector([selectBrushSettings, selectChartOffsetInternal, selectMargin], (brushSettings, offset, margin) => ({
    height: brushSettings.height,
    x: isNumber(brushSettings.x) ? brushSettings.x : offset.left,
    y: isNumber(brushSettings.y) ? brushSettings.y : offset.top + offset.height + offset.brushBottom - ((margin === null || margin === void 0 ? void 0 : margin.bottom) || 0),
    width: isNumber(brushSettings.width) ? brushSettings.width : offset.width
  }));

  // node_modules/recharts/es6/component/ResponsiveContainer.js
  var React4 = __toESM(require_react());
  var import_react13 = __toESM(require_react());

  // node_modules/recharts/es6/util/LogUtils.js
  var isDev = true;
  var warn2 = function warn3(condition, format2) {
    for (var _len = arguments.length, args = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
      args[_key - 2] = arguments[_key];
    }
    if (isDev && typeof console !== "undefined" && console.warn) {
      if (format2 === void 0) {
        console.warn("LogUtils requires an error message argument");
      }
      if (!condition) {
        if (format2 === void 0) {
          console.warn("Minified exception occurred; use the non-minified dev environment for the full error message and additional helpful warnings.");
        } else {
          var argIndex = 0;
          console.warn(format2.replace(/%s/g, () => args[argIndex++]));
        }
      }
    }
  };

  // node_modules/recharts/es6/component/responsiveContainerUtils.js
  var defaultResponsiveContainerProps = {
    width: "100%",
    height: "100%",
    debounce: 0,
    minWidth: 0,
    initialDimension: {
      width: -1,
      height: -1
    }
  };

  // node_modules/recharts/es6/component/ResponsiveContainer.js
  var ResponsiveContainerContext = /* @__PURE__ */ (0, import_react13.createContext)(defaultResponsiveContainerProps.initialDimension);
  var useResponsiveContainerContext = () => (0, import_react13.useContext)(ResponsiveContainerContext);

  // node_modules/recharts/es6/context/chartLayoutContext.js
  function cartesianViewBoxToTrapezoid(box) {
    if (!box) {
      return void 0;
    }
    return {
      x: box.x,
      y: box.y,
      upperWidth: "upperWidth" in box ? box.upperWidth : box.width,
      lowerWidth: "lowerWidth" in box ? box.lowerWidth : box.width,
      width: box.width,
      height: box.height
    };
  }
  var useViewBox = () => {
    var _useAppSelector;
    var panorama = useIsPanorama();
    var rootViewBox = useAppSelector(selectChartViewBox);
    var brushDimensions = useAppSelector(selectBrushDimensions);
    var brushPadding = (_useAppSelector = useAppSelector(selectBrushSettings)) === null || _useAppSelector === void 0 ? void 0 : _useAppSelector.padding;
    if (!panorama || !brushDimensions || !brushPadding) {
      return rootViewBox;
    }
    return {
      width: brushDimensions.width - brushPadding.left - brushPadding.right,
      height: brushDimensions.height - brushPadding.top - brushPadding.bottom,
      x: brushPadding.left,
      y: brushPadding.top
    };
  };
  var manyComponentsThrowErrorsIfOffsetIsUndefined = {
    top: 0,
    bottom: 0,
    left: 0,
    right: 0,
    width: 0,
    height: 0,
    brushBottom: 0
  };
  var useOffsetInternal = () => {
    var _useAppSelector2;
    return (_useAppSelector2 = useAppSelector(selectChartOffsetInternal)) !== null && _useAppSelector2 !== void 0 ? _useAppSelector2 : manyComponentsThrowErrorsIfOffsetIsUndefined;
  };
  var useChartWidth = () => {
    return useAppSelector(selectChartWidth);
  };
  var useChartHeight = () => {
    return useAppSelector(selectChartHeight);
  };
  var selectChartLayout = (state) => state.layout.layoutType;
  var useChartLayout = () => useAppSelector(selectChartLayout);
  var useCartesianChartLayout = () => {
    var layout = useChartLayout();
    if (layout === "horizontal" || layout === "vertical") {
      return layout;
    }
    return void 0;
  };
  var selectPolarChartLayout = (state) => {
    var layout = state.layout.layoutType;
    if (layout === "centric" || layout === "radial") {
      return layout;
    }
    return void 0;
  };
  var usePolarChartLayout = () => {
    return useAppSelector(selectPolarChartLayout);
  };
  var useIsInChartContext = () => {
    var layout = useChartLayout();
    return layout !== void 0;
  };
  var ReportChartSize = (props) => {
    var dispatch2 = useAppDispatch();
    var isPanorama = useIsPanorama();
    var widthFromProps = props.width, heightFromProps = props.height;
    var responsiveContainerCalculations = useResponsiveContainerContext();
    var width = widthFromProps;
    var height = heightFromProps;
    if (responsiveContainerCalculations) {
      width = responsiveContainerCalculations.width > 0 ? responsiveContainerCalculations.width : widthFromProps;
      height = responsiveContainerCalculations.height > 0 ? responsiveContainerCalculations.height : heightFromProps;
    }
    (0, import_react14.useEffect)(() => {
      if (!isPanorama && isPositiveNumber(width) && isPositiveNumber(height)) {
        dispatch2(setChartSize({
          width,
          height
        }));
      }
    }, [dispatch2, isPanorama, width, height]);
    return null;
  };

  // node_modules/recharts/es6/state/legendSlice.js
  var initialState2 = {
    settings: {
      layout: "horizontal",
      align: "center",
      verticalAlign: "bottom",
      itemSorter: "value"
    },
    size: {
      width: 0,
      height: 0
    },
    payload: []
  };
  var legendSlice = createSlice({
    name: "legend",
    initialState: initialState2,
    reducers: {
      setLegendSize(state, action) {
        state.size.width = action.payload.width;
        state.size.height = action.payload.height;
      },
      setLegendSettings(state, action) {
        state.settings.align = action.payload.align;
        state.settings.layout = action.payload.layout;
        state.settings.verticalAlign = action.payload.verticalAlign;
        state.settings.itemSorter = action.payload.itemSorter;
      },
      addLegendPayload: {
        reducer(state, action) {
          state.payload.push(castDraft(action.payload));
        },
        prepare: prepareAutoBatched()
      },
      replaceLegendPayload: {
        reducer(state, action) {
          var _action$payload = action.payload, prev = _action$payload.prev, next = _action$payload.next;
          var index2 = current(state).payload.indexOf(castDraft(prev));
          if (index2 > -1) {
            state.payload[index2] = castDraft(next);
          }
        },
        prepare: prepareAutoBatched()
      },
      removeLegendPayload: {
        reducer(state, action) {
          var index2 = current(state).payload.indexOf(castDraft(action.payload));
          if (index2 > -1) {
            state.payload.splice(index2, 1);
          }
        },
        prepare: prepareAutoBatched()
      }
    }
  });
  var _legendSlice$actions = legendSlice.actions;
  var setLegendSize = _legendSlice$actions.setLegendSize;
  var setLegendSettings = _legendSlice$actions.setLegendSettings;
  var addLegendPayload = _legendSlice$actions.addLegendPayload;
  var replaceLegendPayload = _legendSlice$actions.replaceLegendPayload;
  var removeLegendPayload = _legendSlice$actions.removeLegendPayload;
  var legendReducer = legendSlice.reducer;

  // node_modules/react-redux/dist/react-redux.mjs
  var React5 = __toESM(require_react(), 1);
  var import_with_selector2 = __toESM(require_with_selector2(), 1);
  var REACT_FORWARD_REF_TYPE = /* @__PURE__ */ Symbol.for("react.forward_ref");
  var REACT_MEMO_TYPE = /* @__PURE__ */ Symbol.for("react.memo");
  var ForwardRef = REACT_FORWARD_REF_TYPE;
  var Memo = REACT_MEMO_TYPE;
  function defaultNoopBatch(callback) {
    callback();
  }
  function createListenerCollection() {
    let first = null;
    let last = null;
    return {
      clear() {
        first = null;
        last = null;
      },
      notify() {
        defaultNoopBatch(() => {
          let listener2 = first;
          while (listener2) {
            listener2.callback();
            listener2 = listener2.next;
          }
        });
      },
      get() {
        const listeners = [];
        let listener2 = first;
        while (listener2) {
          listeners.push(listener2);
          listener2 = listener2.next;
        }
        return listeners;
      },
      subscribe(callback) {
        let isSubscribed = true;
        const listener2 = last = {
          callback,
          next: null,
          prev: last
        };
        if (listener2.prev) {
          listener2.prev.next = listener2;
        } else {
          first = listener2;
        }
        return function unsubscribe() {
          if (!isSubscribed || first === null) return;
          isSubscribed = false;
          if (listener2.next) {
            listener2.next.prev = listener2.prev;
          } else {
            last = listener2.prev;
          }
          if (listener2.prev) {
            listener2.prev.next = listener2.next;
          } else {
            first = listener2.next;
          }
        };
      }
    };
  }
  var nullListeners = {
    notify() {
    },
    get: () => []
  };
  function createSubscription(store, parentSub) {
    let unsubscribe;
    let listeners = nullListeners;
    let subscriptionsAmount = 0;
    let selfSubscribed = false;
    function addNestedSub(listener2) {
      trySubscribe();
      const cleanupListener = listeners.subscribe(listener2);
      let removed = false;
      return () => {
        if (!removed) {
          removed = true;
          cleanupListener();
          tryUnsubscribe();
        }
      };
    }
    function notifyNestedSubs() {
      listeners.notify();
    }
    function handleChangeWrapper() {
      if (subscription.onStateChange) {
        subscription.onStateChange();
      }
    }
    function isSubscribed() {
      return selfSubscribed;
    }
    function trySubscribe() {
      subscriptionsAmount++;
      if (!unsubscribe) {
        unsubscribe = parentSub ? parentSub.addNestedSub(handleChangeWrapper) : store.subscribe(handleChangeWrapper);
        listeners = createListenerCollection();
      }
    }
    function tryUnsubscribe() {
      subscriptionsAmount--;
      if (unsubscribe && subscriptionsAmount === 0) {
        unsubscribe();
        unsubscribe = void 0;
        listeners.clear();
        listeners = nullListeners;
      }
    }
    function trySubscribeSelf() {
      if (!selfSubscribed) {
        selfSubscribed = true;
        trySubscribe();
      }
    }
    function tryUnsubscribeSelf() {
      if (selfSubscribed) {
        selfSubscribed = false;
        tryUnsubscribe();
      }
    }
    const subscription = {
      addNestedSub,
      notifyNestedSubs,
      handleChangeWrapper,
      isSubscribed,
      trySubscribe: trySubscribeSelf,
      tryUnsubscribe: tryUnsubscribeSelf,
      getListeners: () => listeners
    };
    return subscription;
  }
  var canUseDOM = () => !!(typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined");
  var isDOM = /* @__PURE__ */ canUseDOM();
  var isRunningInReactNative = () => typeof navigator !== "undefined" && navigator.product === "ReactNative";
  var isReactNative = /* @__PURE__ */ isRunningInReactNative();
  var getUseIsomorphicLayoutEffect = () => isDOM || isReactNative ? React5.useLayoutEffect : React5.useEffect;
  var useIsomorphicLayoutEffect = /* @__PURE__ */ getUseIsomorphicLayoutEffect();
  function is2(x5, y5) {
    if (x5 === y5) {
      return x5 !== 0 || y5 !== 0 || 1 / x5 === 1 / y5;
    } else {
      return x5 !== x5 && y5 !== y5;
    }
  }
  function shallowEqual(objA, objB) {
    if (is2(objA, objB)) return true;
    if (typeof objA !== "object" || objA === null || typeof objB !== "object" || objB === null) {
      return false;
    }
    const keysA = Object.keys(objA);
    const keysB = Object.keys(objB);
    if (keysA.length !== keysB.length) return false;
    for (let i = 0; i < keysA.length; i++) {
      if (!Object.prototype.hasOwnProperty.call(objB, keysA[i]) || !is2(objA[keysA[i]], objB[keysA[i]])) {
        return false;
      }
    }
    return true;
  }
  var FORWARD_REF_STATICS = {
    $$typeof: true,
    render: true,
    defaultProps: true,
    displayName: true,
    propTypes: true
  };
  var MEMO_STATICS = {
    $$typeof: true,
    compare: true,
    defaultProps: true,
    displayName: true,
    propTypes: true,
    type: true
  };
  var TYPE_STATICS = {
    [ForwardRef]: FORWARD_REF_STATICS,
    [Memo]: MEMO_STATICS
  };
  var objectPrototype = Object.prototype;
  var ContextKey = /* @__PURE__ */ Symbol.for(`react-redux-context`);
  var gT = typeof globalThis !== "undefined" ? globalThis : (
    /* fall back to a per-module scope (pre-8.1 behaviour) if `globalThis` is not available */
    {}
  );
  function getContext() {
    if (!React5.createContext) return {};
    const contextMap = gT[ContextKey] ??= /* @__PURE__ */ new Map();
    let realContext = contextMap.get(React5.createContext);
    if (!realContext) {
      realContext = React5.createContext(
        null
      );
      if (true) {
        realContext.displayName = "ReactRedux";
      }
      contextMap.set(React5.createContext, realContext);
    }
    return realContext;
  }
  var ReactReduxContext = /* @__PURE__ */ getContext();
  function Provider(providerProps) {
    const { children, context, serverState, store } = providerProps;
    const contextValue = React5.useMemo(() => {
      const subscription = createSubscription(store);
      const baseContextValue = {
        store,
        subscription,
        getServerState: serverState ? () => serverState : void 0
      };
      if (false) {
        return baseContextValue;
      } else {
        const { identityFunctionCheck = "once", stabilityCheck = "once" } = providerProps;
        return /* @__PURE__ */ Object.assign(baseContextValue, {
          stabilityCheck,
          identityFunctionCheck
        });
      }
    }, [store, serverState]);
    const previousState = React5.useMemo(() => store.getState(), [store]);
    useIsomorphicLayoutEffect(() => {
      const { subscription } = contextValue;
      subscription.onStateChange = subscription.notifyNestedSubs;
      subscription.trySubscribe();
      if (previousState !== store.getState()) {
        subscription.notifyNestedSubs();
      }
      return () => {
        subscription.tryUnsubscribe();
        subscription.onStateChange = void 0;
      };
    }, [contextValue, previousState]);
    const Context = context || ReactReduxContext;
    return /* @__PURE__ */ React5.createElement(Context.Provider, { value: contextValue }, children);
  }
  var Provider_default = Provider;

  // node_modules/recharts/es6/util/propsAreEqual.js
  var propsToShallowCompare = /* @__PURE__ */ new Set([
    "axisLine",
    "tickLine",
    "activeBar",
    "activeDot",
    "activeLabel",
    "activeShape",
    "allowEscapeViewBox",
    "background",
    "cursor",
    "dot",
    "label",
    "line",
    "margin",
    "padding",
    "position",
    "shape",
    "style",
    "tick",
    "wrapperStyle",
    // radius can be an array of 4 numbers, easy to compare shallowly
    "radius",
    "throttledEvents"
  ]);
  function sameValueZero(x5, y5) {
    if (x5 == null && y5 == null) {
      return true;
    }
    if (typeof x5 === "number" && typeof y5 === "number") {
      return x5 === y5 || x5 !== x5 && y5 !== y5;
    }
    return x5 === y5;
  }
  function propsAreEqual(prevProps, nextProps) {
    var allKeys = /* @__PURE__ */ new Set([...Object.keys(prevProps), ...Object.keys(nextProps)]);
    for (var key of allKeys) {
      if (propsToShallowCompare.has(key)) {
        if (prevProps[key] == null && nextProps[key] == null) {
          continue;
        }
        if (!shallowEqual(prevProps[key], nextProps[key])) {
          return false;
        }
      } else if (!sameValueZero(prevProps[key], nextProps[key])) {
        return false;
      }
    }
    return true;
  }

  // node_modules/recharts/es6/component/Tooltip.js
  var React13 = __toESM(require_react());
  var import_react24 = __toESM(require_react());
  var import_react_dom2 = __toESM(require_react_dom());

  // node_modules/recharts/es6/component/DefaultTooltipContent.js
  var React6 = __toESM(require_react());
  function _extends3() {
    return _extends3 = Object.assign ? Object.assign.bind() : function(n) {
      for (var e = 1; e < arguments.length; e++) {
        var t = arguments[e];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends3.apply(null, arguments);
  }
  function ownKeys4(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread4(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys4(Object(t), true).forEach(function(r3) {
        _defineProperty4(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys4(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty4(e, r2, t) {
    return (r2 = _toPropertyKey4(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey4(t) {
    var i = _toPrimitive4(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive4(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  function _slicedToArray2(r2, e) {
    return _arrayWithHoles2(r2) || _iterableToArrayLimit2(r2, e) || _unsupportedIterableToArray2(r2, e) || _nonIterableRest2();
  }
  function _nonIterableRest2() {
    throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  function _unsupportedIterableToArray2(r2, a2) {
    if (r2) {
      if ("string" == typeof r2) return _arrayLikeToArray2(r2, a2);
      var t = {}.toString.call(r2).slice(8, -1);
      return "Object" === t && r2.constructor && (t = r2.constructor.name), "Map" === t || "Set" === t ? Array.from(r2) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray2(r2, a2) : void 0;
    }
  }
  function _arrayLikeToArray2(r2, a2) {
    (null == a2 || a2 > r2.length) && (a2 = r2.length);
    for (var e = 0, n = Array(a2); e < a2; e++) n[e] = r2[e];
    return n;
  }
  function _iterableToArrayLimit2(r2, l) {
    var t = null == r2 ? null : "undefined" != typeof Symbol && r2[Symbol.iterator] || r2["@@iterator"];
    if (null != t) {
      var e, n, i, u, a2 = [], f = true, o = false;
      try {
        if (i = (t = t.call(r2)).next, 0 === l) {
          if (Object(t) !== t) return;
          f = false;
        } else for (; !(f = (e = i.call(t)).done) && (a2.push(e.value), a2.length !== l); f = true) ;
      } catch (r3) {
        o = true, n = r3;
      } finally {
        try {
          if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return;
        } finally {
          if (o) throw n;
        }
      }
      return a2;
    }
  }
  function _arrayWithHoles2(r2) {
    if (Array.isArray(r2)) return r2;
  }
  function defaultFormatter(value2) {
    return Array.isArray(value2) && isNumOrStr(value2[0]) && isNumOrStr(value2[1]) ? value2.join(" ~ ") : value2;
  }
  var defaultDefaultTooltipContentProps = {
    separator: " : ",
    contentStyle: {
      margin: 0,
      padding: 10,
      backgroundColor: "#fff",
      border: "1px solid #ccc",
      whiteSpace: "nowrap"
    },
    itemStyle: {
      display: "block",
      paddingTop: 4,
      paddingBottom: 4,
      color: "#000"
    },
    labelStyle: {},
    accessibilityLayer: false
  };
  function lodashLikeSortBy(array, itemSorter) {
    if (itemSorter == null) {
      return array;
    }
    return sortBy(array, itemSorter);
  }
  var DefaultTooltipContent = (props) => {
    var _props$separator = props.separator, separator = _props$separator === void 0 ? defaultDefaultTooltipContentProps.separator : _props$separator, contentStyle = props.contentStyle, itemStyle = props.itemStyle, _props$labelStyle = props.labelStyle, labelStyle = _props$labelStyle === void 0 ? defaultDefaultTooltipContentProps.labelStyle : _props$labelStyle, payload = props.payload, formatter = props.formatter, itemSorter = props.itemSorter, wrapperClassName = props.wrapperClassName, labelClassName = props.labelClassName, label = props.label, labelFormatter = props.labelFormatter, _props$accessibilityL = props.accessibilityLayer, accessibilityLayer = _props$accessibilityL === void 0 ? defaultDefaultTooltipContentProps.accessibilityLayer : _props$accessibilityL;
    var renderContent2 = () => {
      if (payload && payload.length) {
        var listStyle = {
          padding: 0,
          margin: 0
        };
        var sortedPayload = lodashLikeSortBy(payload, itemSorter);
        var items = sortedPayload.map((entry, i) => {
          if (!entry || entry.type === "none") {
            return null;
          }
          var finalFormatter = entry.formatter || formatter || defaultFormatter;
          var value2 = entry.value, name = entry.name;
          var finalValue = value2;
          var finalName = name;
          if (finalFormatter) {
            var formatted = finalFormatter(value2, name, entry, i, payload);
            if (Array.isArray(formatted)) {
              var _formatted = _slicedToArray2(formatted, 2);
              finalValue = _formatted[0];
              finalName = _formatted[1];
            } else if (formatted != null) {
              finalValue = formatted;
            } else {
              return null;
            }
          }
          var finalItemStyle = _objectSpread4(_objectSpread4({}, defaultDefaultTooltipContentProps.itemStyle), {}, {
            color: entry.color || defaultDefaultTooltipContentProps.itemStyle.color
          }, itemStyle);
          return /* @__PURE__ */ React6.createElement("li", {
            className: "recharts-tooltip-item",
            key: "tooltip-item-".concat(i),
            style: finalItemStyle
          }, isNumOrStr(finalName) ? /* @__PURE__ */ React6.createElement("span", {
            className: "recharts-tooltip-item-name"
          }, finalName) : null, isNumOrStr(finalName) ? /* @__PURE__ */ React6.createElement("span", {
            className: "recharts-tooltip-item-separator"
          }, separator) : null, /* @__PURE__ */ React6.createElement("span", {
            className: "recharts-tooltip-item-value"
          }, finalValue), /* @__PURE__ */ React6.createElement("span", {
            className: "recharts-tooltip-item-unit"
          }, entry.unit || ""));
        });
        return /* @__PURE__ */ React6.createElement("ul", {
          className: "recharts-tooltip-item-list",
          style: listStyle
        }, items);
      }
      return null;
    };
    var finalStyle = _objectSpread4(_objectSpread4({}, defaultDefaultTooltipContentProps.contentStyle), contentStyle);
    var finalLabelStyle = _objectSpread4({
      margin: 0
    }, labelStyle);
    var hasLabel = !isNullish(label);
    var finalLabel = hasLabel ? label : "";
    var wrapperCN = clsx("recharts-default-tooltip", wrapperClassName);
    var labelCN = clsx("recharts-tooltip-label", labelClassName);
    if (hasLabel && labelFormatter && payload !== void 0 && payload !== null) {
      finalLabel = labelFormatter(label, payload);
    }
    var accessibilityAttributes = accessibilityLayer ? {
      role: "status",
      "aria-live": "assertive"
    } : {};
    return /* @__PURE__ */ React6.createElement("div", _extends3({
      className: wrapperCN,
      style: finalStyle
    }, accessibilityAttributes), /* @__PURE__ */ React6.createElement("p", {
      className: labelCN,
      style: finalLabelStyle
    }, /* @__PURE__ */ React6.isValidElement(finalLabel) ? finalLabel : "".concat(finalLabel)), renderContent2());
  };

  // node_modules/recharts/es6/component/TooltipBoundingBox.js
  var React7 = __toESM(require_react());

  // node_modules/recharts/es6/util/tooltip/translate.js
  var CSS_CLASS_PREFIX = "recharts-tooltip-wrapper";
  var TOOLTIP_HIDDEN = {
    visibility: "hidden"
  };
  function getTooltipCSSClassName(_ref2) {
    var coordinate = _ref2.coordinate, translateX = _ref2.translateX, translateY = _ref2.translateY;
    return clsx(CSS_CLASS_PREFIX, {
      ["".concat(CSS_CLASS_PREFIX, "-right")]: isNumber(translateX) && coordinate && isNumber(coordinate.x) && translateX >= coordinate.x,
      ["".concat(CSS_CLASS_PREFIX, "-left")]: isNumber(translateX) && coordinate && isNumber(coordinate.x) && translateX < coordinate.x,
      ["".concat(CSS_CLASS_PREFIX, "-bottom")]: isNumber(translateY) && coordinate && isNumber(coordinate.y) && translateY >= coordinate.y,
      ["".concat(CSS_CLASS_PREFIX, "-top")]: isNumber(translateY) && coordinate && isNumber(coordinate.y) && translateY < coordinate.y
    });
  }
  function getTooltipTranslateXY(_ref2) {
    var allowEscapeViewBox = _ref2.allowEscapeViewBox, coordinate = _ref2.coordinate, key = _ref2.key, offset = _ref2.offset, position = _ref2.position, reverseDirection = _ref2.reverseDirection, tooltipDimension = _ref2.tooltipDimension, viewBox = _ref2.viewBox, viewBoxDimension = _ref2.viewBoxDimension;
    if (position && isNumber(position[key])) {
      return position[key];
    }
    var negative = coordinate[key] - tooltipDimension - (offset > 0 ? offset : 0);
    var positive = coordinate[key] + offset;
    if (allowEscapeViewBox[key]) {
      return reverseDirection[key] ? negative : positive;
    }
    var viewBoxKey = viewBox[key];
    if (viewBoxKey == null) {
      return 0;
    }
    if (reverseDirection[key]) {
      var _tooltipBoundary = negative;
      var _viewBoxBoundary = viewBoxKey;
      if (_tooltipBoundary < _viewBoxBoundary) {
        return Math.max(positive, viewBoxKey);
      }
      return Math.max(negative, viewBoxKey);
    }
    if (viewBoxDimension == null) {
      return 0;
    }
    var tooltipBoundary = positive + tooltipDimension;
    var viewBoxBoundary = viewBoxKey + viewBoxDimension;
    if (tooltipBoundary > viewBoxBoundary) {
      return Math.max(negative, viewBoxKey);
    }
    return Math.max(positive, viewBoxKey);
  }
  function getTransformStyle(_ref3) {
    var translateX = _ref3.translateX, translateY = _ref3.translateY, useTranslate3d = _ref3.useTranslate3d;
    return {
      transform: useTranslate3d ? "translate3d(".concat(translateX, "px, ").concat(translateY, "px, 0)") : "translate(".concat(translateX, "px, ").concat(translateY, "px)")
    };
  }
  function getTooltipTranslate(_ref4) {
    var allowEscapeViewBox = _ref4.allowEscapeViewBox, coordinate = _ref4.coordinate, offsetTop = _ref4.offsetTop, offsetLeft = _ref4.offsetLeft, position = _ref4.position, reverseDirection = _ref4.reverseDirection, tooltipBox = _ref4.tooltipBox, useTranslate3d = _ref4.useTranslate3d, viewBox = _ref4.viewBox;
    var cssProperties, translateX, translateY;
    if (tooltipBox.height > 0 && tooltipBox.width > 0 && coordinate) {
      translateX = getTooltipTranslateXY({
        allowEscapeViewBox,
        coordinate,
        key: "x",
        offset: offsetLeft,
        position,
        reverseDirection,
        tooltipDimension: tooltipBox.width,
        viewBox,
        viewBoxDimension: viewBox.width
      });
      translateY = getTooltipTranslateXY({
        allowEscapeViewBox,
        coordinate,
        key: "y",
        offset: offsetTop,
        position,
        reverseDirection,
        tooltipDimension: tooltipBox.height,
        viewBox,
        viewBoxDimension: viewBox.height
      });
      cssProperties = getTransformStyle({
        translateX,
        translateY,
        useTranslate3d
      });
    } else {
      cssProperties = TOOLTIP_HIDDEN;
    }
    return {
      cssProperties,
      cssClasses: getTooltipCSSClassName({
        translateX,
        translateY,
        coordinate
      })
    };
  }

  // node_modules/recharts/es6/util/usePrefersReducedMotion.js
  var import_react15 = __toESM(require_react());

  // node_modules/recharts/es6/util/Global.js
  var parseIsSsrByDefault = () => !(typeof window !== "undefined" && window.document && Boolean(window.document.createElement) && window.setTimeout);
  var Global = {
    devToolsEnabled: true,
    isSsr: parseIsSsrByDefault()
  };

  // node_modules/recharts/es6/util/usePrefersReducedMotion.js
  function _slicedToArray3(r2, e) {
    return _arrayWithHoles3(r2) || _iterableToArrayLimit3(r2, e) || _unsupportedIterableToArray3(r2, e) || _nonIterableRest3();
  }
  function _nonIterableRest3() {
    throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  function _unsupportedIterableToArray3(r2, a2) {
    if (r2) {
      if ("string" == typeof r2) return _arrayLikeToArray3(r2, a2);
      var t = {}.toString.call(r2).slice(8, -1);
      return "Object" === t && r2.constructor && (t = r2.constructor.name), "Map" === t || "Set" === t ? Array.from(r2) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray3(r2, a2) : void 0;
    }
  }
  function _arrayLikeToArray3(r2, a2) {
    (null == a2 || a2 > r2.length) && (a2 = r2.length);
    for (var e = 0, n = Array(a2); e < a2; e++) n[e] = r2[e];
    return n;
  }
  function _iterableToArrayLimit3(r2, l) {
    var t = null == r2 ? null : "undefined" != typeof Symbol && r2[Symbol.iterator] || r2["@@iterator"];
    if (null != t) {
      var e, n, i, u, a2 = [], f = true, o = false;
      try {
        if (i = (t = t.call(r2)).next, 0 === l) {
          if (Object(t) !== t) return;
          f = false;
        } else for (; !(f = (e = i.call(t)).done) && (a2.push(e.value), a2.length !== l); f = true) ;
      } catch (r3) {
        o = true, n = r3;
      } finally {
        try {
          if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return;
        } finally {
          if (o) throw n;
        }
      }
      return a2;
    }
  }
  function _arrayWithHoles3(r2) {
    if (Array.isArray(r2)) return r2;
  }
  function usePrefersReducedMotion() {
    var _useState = (0, import_react15.useState)(() => {
      if (Global.isSsr) {
        return false;
      }
      if (!window.matchMedia) {
        return false;
      }
      return window.matchMedia("(prefers-reduced-motion: reduce)").matches;
    }), _useState2 = _slicedToArray3(_useState, 2), prefersReducedMotion = _useState2[0], setPrefersReducedMotion = _useState2[1];
    (0, import_react15.useEffect)(() => {
      if (!window.matchMedia) {
        return;
      }
      var mediaQuery = window.matchMedia("(prefers-reduced-motion: reduce)");
      var handleChange = () => {
        setPrefersReducedMotion(mediaQuery.matches);
      };
      mediaQuery.addEventListener("change", handleChange);
      return () => {
        mediaQuery.removeEventListener("change", handleChange);
      };
    }, []);
    return prefersReducedMotion;
  }

  // node_modules/recharts/es6/component/TooltipBoundingBox.js
  function ownKeys5(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread5(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys5(Object(t), true).forEach(function(r3) {
        _defineProperty5(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys5(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty5(e, r2, t) {
    return (r2 = _toPropertyKey5(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey5(t) {
    var i = _toPrimitive5(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive5(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  function _slicedToArray4(r2, e) {
    return _arrayWithHoles4(r2) || _iterableToArrayLimit4(r2, e) || _unsupportedIterableToArray4(r2, e) || _nonIterableRest4();
  }
  function _nonIterableRest4() {
    throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  function _unsupportedIterableToArray4(r2, a2) {
    if (r2) {
      if ("string" == typeof r2) return _arrayLikeToArray4(r2, a2);
      var t = {}.toString.call(r2).slice(8, -1);
      return "Object" === t && r2.constructor && (t = r2.constructor.name), "Map" === t || "Set" === t ? Array.from(r2) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray4(r2, a2) : void 0;
    }
  }
  function _arrayLikeToArray4(r2, a2) {
    (null == a2 || a2 > r2.length) && (a2 = r2.length);
    for (var e = 0, n = Array(a2); e < a2; e++) n[e] = r2[e];
    return n;
  }
  function _iterableToArrayLimit4(r2, l) {
    var t = null == r2 ? null : "undefined" != typeof Symbol && r2[Symbol.iterator] || r2["@@iterator"];
    if (null != t) {
      var e, n, i, u, a2 = [], f = true, o = false;
      try {
        if (i = (t = t.call(r2)).next, 0 === l) {
          if (Object(t) !== t) return;
          f = false;
        } else for (; !(f = (e = i.call(t)).done) && (a2.push(e.value), a2.length !== l); f = true) ;
      } catch (r3) {
        o = true, n = r3;
      } finally {
        try {
          if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return;
        } finally {
          if (o) throw n;
        }
      }
      return a2;
    }
  }
  function _arrayWithHoles4(r2) {
    if (Array.isArray(r2)) return r2;
  }
  function resolveTransitionProperty(args) {
    if (args.prefersReducedMotion && args.isAnimationActive === "auto") {
      return void 0;
    }
    if (args.isAnimationActive && args.active) {
      var easing = typeof args.animationEasing === "string" ? args.animationEasing : "ease";
      return "transform ".concat(args.animationDuration, "ms ").concat(easing);
    }
    return void 0;
  }
  function TooltipBoundingBoxImpl(props) {
    var _props$coordinate3, _props$coordinate4, _props$coordinate$x2, _props$coordinate5, _props$coordinate$y2, _props$coordinate6;
    var prefersReducedMotion = usePrefersReducedMotion();
    var _React$useState = React7.useState(() => ({
      dismissed: false,
      dismissedAtCoordinate: {
        x: 0,
        y: 0
      }
    })), _React$useState2 = _slicedToArray4(_React$useState, 2), state = _React$useState2[0], setState = _React$useState2[1];
    React7.useEffect(() => {
      var handleKeyDown = (event) => {
        if (event.key === "Escape") {
          var _props$coordinate$x, _props$coordinate, _props$coordinate$y, _props$coordinate2;
          setState({
            dismissed: true,
            dismissedAtCoordinate: {
              x: (_props$coordinate$x = (_props$coordinate = props.coordinate) === null || _props$coordinate === void 0 ? void 0 : _props$coordinate.x) !== null && _props$coordinate$x !== void 0 ? _props$coordinate$x : 0,
              y: (_props$coordinate$y = (_props$coordinate2 = props.coordinate) === null || _props$coordinate2 === void 0 ? void 0 : _props$coordinate2.y) !== null && _props$coordinate$y !== void 0 ? _props$coordinate$y : 0
            }
          });
        }
      };
      document.addEventListener("keydown", handleKeyDown);
      return () => {
        document.removeEventListener("keydown", handleKeyDown);
      };
    }, [(_props$coordinate3 = props.coordinate) === null || _props$coordinate3 === void 0 ? void 0 : _props$coordinate3.x, (_props$coordinate4 = props.coordinate) === null || _props$coordinate4 === void 0 ? void 0 : _props$coordinate4.y]);
    if (state.dismissed && (((_props$coordinate$x2 = (_props$coordinate5 = props.coordinate) === null || _props$coordinate5 === void 0 ? void 0 : _props$coordinate5.x) !== null && _props$coordinate$x2 !== void 0 ? _props$coordinate$x2 : 0) !== state.dismissedAtCoordinate.x || ((_props$coordinate$y2 = (_props$coordinate6 = props.coordinate) === null || _props$coordinate6 === void 0 ? void 0 : _props$coordinate6.y) !== null && _props$coordinate$y2 !== void 0 ? _props$coordinate$y2 : 0) !== state.dismissedAtCoordinate.y)) {
      setState(_objectSpread5(_objectSpread5({}, state), {}, {
        dismissed: false
      }));
    }
    var _getTooltipTranslate = getTooltipTranslate({
      allowEscapeViewBox: props.allowEscapeViewBox,
      coordinate: props.coordinate,
      offsetLeft: typeof props.offset === "number" ? props.offset : props.offset.x,
      offsetTop: typeof props.offset === "number" ? props.offset : props.offset.y,
      position: props.position,
      reverseDirection: props.reverseDirection,
      tooltipBox: {
        height: props.lastBoundingBox.height,
        width: props.lastBoundingBox.width
      },
      useTranslate3d: props.useTranslate3d,
      viewBox: props.viewBox
    }), cssClasses = _getTooltipTranslate.cssClasses, cssProperties = _getTooltipTranslate.cssProperties;
    var positionStyle = props.hasPortalFromProps ? {} : _objectSpread5(_objectSpread5({
      transition: resolveTransitionProperty({
        prefersReducedMotion,
        isAnimationActive: props.isAnimationActive,
        active: props.active,
        animationDuration: props.animationDuration,
        animationEasing: props.animationEasing
      })
    }, cssProperties), {}, {
      pointerEvents: "none",
      position: "absolute",
      top: 0,
      left: 0
    });
    var outerStyle = _objectSpread5(_objectSpread5({}, positionStyle), {}, {
      visibility: !state.dismissed && props.active && props.hasPayload ? "visible" : "hidden"
    }, props.wrapperStyle);
    return /* @__PURE__ */ React7.createElement("div", {
      // @ts-expect-error typescript library does not recognize xmlns attribute, but it's required for an HTML chunk inside SVG.
      xmlns: "http://www.w3.org/1999/xhtml",
      tabIndex: -1,
      className: cssClasses,
      style: outerStyle,
      ref: props.innerRef
    }, props.children);
  }
  var TooltipBoundingBox = /* @__PURE__ */ React7.memo(TooltipBoundingBoxImpl);

  // node_modules/recharts/es6/context/accessibilityContext.js
  var useAccessibilityLayer = () => {
    var _useAppSelector;
    return (_useAppSelector = useAppSelector((state) => state.rootProps.accessibilityLayer)) !== null && _useAppSelector !== void 0 ? _useAppSelector : true;
  };

  // node_modules/recharts/es6/component/Cursor.js
  var React12 = __toESM(require_react());
  var import_react21 = __toESM(require_react());

  // node_modules/recharts/es6/shape/Curve.js
  var React8 = __toESM(require_react());
  function _extends4() {
    return _extends4 = Object.assign ? Object.assign.bind() : function(n) {
      for (var e = 1; e < arguments.length; e++) {
        var t = arguments[e];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends4.apply(null, arguments);
  }
  function ownKeys6(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread6(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys6(Object(t), true).forEach(function(r3) {
        _defineProperty6(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys6(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty6(e, r2, t) {
    return (r2 = _toPropertyKey6(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey6(t) {
    var i = _toPrimitive6(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive6(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  var CURVE_FACTORIES = {
    curveBasisClosed: basisClosed_default,
    curveBasisOpen: basisOpen_default,
    curveBasis: basis_default,
    curveBumpX: bumpX,
    curveBumpY: bumpY,
    curveLinearClosed: linearClosed_default,
    curveLinear: linear_default,
    curveMonotoneX: monotoneX,
    curveMonotoneY: monotoneY,
    curveNatural: natural_default,
    curveStep: step_default,
    curveStepAfter: stepAfter,
    curveStepBefore: stepBefore
  };
  var defined = (p) => isWellBehavedNumber(p.x) && isWellBehavedNumber(p.y);
  var areaDefined = (d) => d.base != null && defined(d.base) && defined(d);
  var getX = (p) => p.x;
  var getY = (p) => p.y;
  var getCurveFactory = (type, layout) => {
    if (typeof type === "function") {
      return type;
    }
    var name = "curve".concat(upperFirst(type));
    if ((name === "curveMonotone" || name === "curveBump") && layout) {
      var factory = CURVE_FACTORIES["".concat(name).concat(layout === "vertical" ? "Y" : "X")];
      if (factory) {
        return factory;
      }
    }
    return CURVE_FACTORIES[name] || linear_default;
  };
  var defaultCurveProps = {
    connectNulls: false,
    type: "linear"
  };
  var getPath = (_ref2) => {
    var _ref$type = _ref2.type, type = _ref$type === void 0 ? defaultCurveProps.type : _ref$type, _ref$points = _ref2.points, points = _ref$points === void 0 ? [] : _ref$points, baseLine = _ref2.baseLine, layout = _ref2.layout, _ref$connectNulls = _ref2.connectNulls, connectNulls = _ref$connectNulls === void 0 ? defaultCurveProps.connectNulls : _ref$connectNulls;
    var curveFactory = getCurveFactory(type, layout);
    var formatPoints = connectNulls ? points.filter(defined) : points;
    if (Array.isArray(baseLine)) {
      var _lineFunction;
      var areaPoints = points.map((entry, index2) => _objectSpread6(_objectSpread6({}, entry), {}, {
        base: baseLine[index2]
      }));
      if (layout === "vertical") {
        _lineFunction = area_default().y(getY).x1(getX).x0((d) => d.base.x);
      } else {
        _lineFunction = area_default().x(getX).y1(getY).y0((d) => d.base.y);
      }
      var _nullableLineFunction = _lineFunction.defined(areaDefined).curve(curveFactory);
      var finalPoints = connectNulls ? areaPoints.filter(areaDefined) : areaPoints;
      return _nullableLineFunction(finalPoints);
    }
    var lineFunction;
    if (layout === "vertical" && isNumber(baseLine)) {
      lineFunction = area_default().y(getY).x1(getX).x0(baseLine);
    } else if (isNumber(baseLine)) {
      lineFunction = area_default().x(getX).y1(getY).y0(baseLine);
    } else {
      lineFunction = line_default().x(getX).y(getY);
    }
    var nullableLineFunction = lineFunction.defined(defined).curve(curveFactory);
    return nullableLineFunction(formatPoints);
  };
  var Curve = (props) => {
    var className = props.className, points = props.points, path3 = props.path, pathRef = props.pathRef;
    var layout = useChartLayout();
    if ((!points || !points.length) && !path3) {
      return null;
    }
    var getPathInput = {
      type: props.type,
      points: props.points,
      baseLine: props.baseLine,
      layout: props.layout || layout,
      connectNulls: props.connectNulls
    };
    var realPath = points && points.length ? getPath(getPathInput) : path3;
    return /* @__PURE__ */ React8.createElement("path", _extends4({}, svgPropertiesNoEvents(props), adaptEventHandlers(props), {
      className: clsx("recharts-curve", className),
      d: realPath === null ? void 0 : realPath,
      ref: pathRef
    }));
  };

  // node_modules/recharts/es6/shape/Cross.js
  var React9 = __toESM(require_react());
  var _excluded3 = ["x", "y", "top", "left", "width", "height", "className"];
  function _extends5() {
    return _extends5 = Object.assign ? Object.assign.bind() : function(n) {
      for (var e = 1; e < arguments.length; e++) {
        var t = arguments[e];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends5.apply(null, arguments);
  }
  function ownKeys7(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread7(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys7(Object(t), true).forEach(function(r3) {
        _defineProperty7(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys7(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty7(e, r2, t) {
    return (r2 = _toPropertyKey7(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey7(t) {
    var i = _toPrimitive7(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive7(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  function _objectWithoutProperties3(e, t) {
    if (null == e) return {};
    var o, r2, i = _objectWithoutPropertiesLoose3(e, t);
    if (Object.getOwnPropertySymbols) {
      var n = Object.getOwnPropertySymbols(e);
      for (r2 = 0; r2 < n.length; r2++) o = n[r2], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
    }
    return i;
  }
  function _objectWithoutPropertiesLoose3(r2, e) {
    if (null == r2) return {};
    var t = {};
    for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
      if (-1 !== e.indexOf(n)) continue;
      t[n] = r2[n];
    }
    return t;
  }
  var getPath2 = (x5, y5, width, height, top, left2) => {
    return "M".concat(x5, ",").concat(top, "v").concat(height, "M").concat(left2, ",").concat(y5, "h").concat(width);
  };
  var Cross = (_ref2) => {
    var _ref$x = _ref2.x, x5 = _ref$x === void 0 ? 0 : _ref$x, _ref$y = _ref2.y, y5 = _ref$y === void 0 ? 0 : _ref$y, _ref$top = _ref2.top, top = _ref$top === void 0 ? 0 : _ref$top, _ref$left = _ref2.left, left2 = _ref$left === void 0 ? 0 : _ref$left, _ref$width = _ref2.width, width = _ref$width === void 0 ? 0 : _ref$width, _ref$height = _ref2.height, height = _ref$height === void 0 ? 0 : _ref$height, className = _ref2.className, rest = _objectWithoutProperties3(_ref2, _excluded3);
    var props = _objectSpread7({
      x: x5,
      y: y5,
      top,
      left: left2,
      width,
      height
    }, rest);
    if (!isNumber(x5) || !isNumber(y5) || !isNumber(width) || !isNumber(height) || !isNumber(top) || !isNumber(left2)) {
      return null;
    }
    return /* @__PURE__ */ React9.createElement("path", _extends5({}, svgPropertiesAndEvents(props), {
      className: clsx("recharts-cross", className),
      d: getPath2(x5, y5, width, height, top, left2)
    }));
  };

  // node_modules/recharts/es6/util/cursor/getCursorRectangle.js
  function getCursorRectangle(layout, activeCoordinate, offset, tooltipAxisBandSize) {
    var halfSize = tooltipAxisBandSize / 2;
    return {
      stroke: "none",
      fill: "#ccc",
      x: layout === "horizontal" ? activeCoordinate.x - halfSize : offset.left + 0.5,
      y: layout === "horizontal" ? offset.top + 0.5 : activeCoordinate.y - halfSize,
      width: layout === "horizontal" ? tooltipAxisBandSize : offset.width - 1,
      height: layout === "horizontal" ? offset.height - 1 : tooltipAxisBandSize
    };
  }

  // node_modules/recharts/es6/shape/Rectangle.js
  var React10 = __toESM(require_react());
  var import_react19 = __toESM(require_react());

  // node_modules/recharts/es6/animation/JavascriptAnimate.js
  var import_react17 = __toESM(require_react());

  // node_modules/recharts/es6/animation/easing.js
  var ACCURACY = 1e-4;
  var cubicBezierFactor = (c1, c2) => [0, 3 * c1, 3 * c2 - 6 * c1, 3 * c1 - 3 * c2 + 1];
  var evaluatePolynomial = (params, animationElapsedTime) => params.map((param, i) => param * animationElapsedTime ** i).reduce((pre, curr) => pre + curr);
  var cubicBezier = (c1, c2) => (animationElapsedTime) => {
    var params = cubicBezierFactor(c1, c2);
    return evaluatePolynomial(params, animationElapsedTime);
  };
  var derivativeCubicBezier = (c1, c2) => (animationElapsedTime) => {
    var params = cubicBezierFactor(c1, c2);
    var newParams = [...params.map((param, i) => param * i).slice(1), 0];
    return evaluatePolynomial(newParams, animationElapsedTime);
  };
  var parseCubicBezier = (easing) => {
    var _easingParts$;
    var easingParts = easing.split("(");
    if (easingParts.length !== 2 || easingParts[0] !== "cubic-bezier") {
      return null;
    }
    var numbers2 = (_easingParts$ = easingParts[1]) === null || _easingParts$ === void 0 || (_easingParts$ = _easingParts$.split(")")[0]) === null || _easingParts$ === void 0 ? void 0 : _easingParts$.split(",");
    if (numbers2 == null || numbers2.length !== 4) {
      return null;
    }
    var coords = numbers2.map((x5) => parseFloat(x5));
    return [coords[0], coords[1], coords[2], coords[3]];
  };
  var getBezierCoordinates = function getBezierCoordinates2() {
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    if (args.length === 1) {
      switch (args[0]) {
        case "linear":
          return [0, 0, 1, 1];
        case "ease":
          return [0.25, 0.1, 0.25, 1];
        case "ease-in":
          return [0.42, 0, 1, 1];
        case "ease-out":
          return [0.42, 0, 0.58, 1];
        case "ease-in-out":
          return [0, 0, 0.58, 1];
        default: {
          var easing = parseCubicBezier(args[0]);
          if (easing) {
            return easing;
          }
        }
      }
    }
    if (args.length === 4) {
      return args;
    }
    return [0, 0, 1, 1];
  };
  var createBezierEasing = (x1, y1, x22, y22) => {
    var curveX = cubicBezier(x1, x22);
    var curveY = cubicBezier(y1, y22);
    var derCurveX = derivativeCubicBezier(x1, x22);
    var rangeValue = (value2) => {
      if (value2 > 1) {
        return 1;
      }
      if (value2 < 0) {
        return 0;
      }
      return value2;
    };
    var bezier = (_animationElapsedTime) => {
      var animationElapsedTime = _animationElapsedTime > 1 ? 1 : _animationElapsedTime;
      var x5 = animationElapsedTime;
      for (var i = 0; i < 8; ++i) {
        var evalT = curveX(x5) - animationElapsedTime;
        var derVal = derCurveX(x5);
        if (Math.abs(evalT - animationElapsedTime) < ACCURACY || derVal < ACCURACY) {
          return curveY(x5);
        }
        x5 = rangeValue(x5 - evalT / derVal);
      }
      return curveY(x5);
    };
    bezier.isStepper = false;
    return bezier;
  };
  var configBezier = function configBezier2() {
    return createBezierEasing(...getBezierCoordinates(...arguments));
  };
  var createSpringEasing = function createSpringEasing2() {
    var config = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    var _config$stiff = config.stiff, stiff = _config$stiff === void 0 ? 100 : _config$stiff, _config$damping = config.damping, damping = _config$damping === void 0 ? 8 : _config$damping, _config$dt = config.dt, dt = _config$dt === void 0 ? 16.67 : _config$dt;
    var destX = 1;
    var positions = [0];
    var currX = 0;
    var currV = 0;
    var maxIterations = 1e4;
    var iterations = 0;
    while (iterations < maxIterations) {
      var FSpring = -(currX - destX) * stiff;
      var FDamping = currV * damping;
      currV += (FSpring - FDamping) * dt / 1e3;
      currX += currV * dt / 1e3;
      positions.push(currX);
      if (Math.abs(currX - destX) < ACCURACY && Math.abs(currV) < ACCURACY) {
        break;
      }
      iterations++;
    }
    positions[positions.length - 1] = destX;
    var maxIndex = positions.length - 1;
    return (t) => {
      var _positions$index, _positions, _positions$index2;
      if (t <= 0) return 0;
      if (t >= 1) return destX;
      var exactFrame = t * maxIndex;
      var index2 = Math.floor(exactFrame);
      var fraction = exactFrame - index2;
      return ((_positions$index = positions[index2]) !== null && _positions$index !== void 0 ? _positions$index : 0) + (((_positions = positions[index2 + 1]) !== null && _positions !== void 0 ? _positions : 0) - ((_positions$index2 = positions[index2]) !== null && _positions$index2 !== void 0 ? _positions$index2 : 0)) * fraction;
    };
  };
  var createEasingFunction = (easing) => {
    if (typeof easing === "string") {
      switch (easing) {
        case "ease":
        case "ease-in-out":
        case "ease-out":
        case "ease-in":
        case "linear":
          return configBezier(easing);
        case "spring":
          return createSpringEasing();
        default:
          if (easing.split("(")[0] === "cubic-bezier") {
            return configBezier(easing);
          }
      }
    }
    if (typeof easing === "function") {
      return easing;
    }
    return null;
  };

  // node_modules/recharts/es6/animation/useAnimationController.js
  var import_react16 = __toESM(require_react());

  // node_modules/recharts/es6/animation/AnimationControllerImpl.js
  var animationControllerImpl = (timeoutController, animationHandle, listener2) => {
    var cancellable;
    var nextUpdate = (now2) => {
      var timeRemaining = animationHandle.tick(now2);
      if (animationHandle.getState() === "active") {
        listener2(animationHandle.getInterpolated());
        if (animationHandle.getProgress() === 1) {
          animationHandle.complete();
          cancellable = void 0;
          return;
        }
        cancellable = timeoutController.setTimeout(nextUpdate, timeRemaining);
        return;
      }
      cancellable = timeoutController.setTimeout(nextUpdate, timeRemaining);
    };
    cancellable = timeoutController.setTimeout(nextUpdate, 0);
    return () => {
      var _cancellable;
      return (_cancellable = cancellable) === null || _cancellable === void 0 ? void 0 : _cancellable();
    };
  };

  // node_modules/recharts/es6/animation/useAnimationController.js
  var AnimationControllerContext = /* @__PURE__ */ (0, import_react16.createContext)(animationControllerImpl);
  var AnimationControllerProvider = AnimationControllerContext.Provider;
  function useAnimationController(animationControllerFromProps) {
    var animationControllerFromContext = (0, import_react16.useContext)(AnimationControllerContext);
    return (0, import_react16.useMemo)(() => animationControllerFromProps !== null && animationControllerFromProps !== void 0 ? animationControllerFromProps : animationControllerFromContext, [animationControllerFromProps, animationControllerFromContext]);
  }

  // node_modules/recharts/es6/animation/AnimationHandle.js
  function _defineProperty8(e, r2, t) {
    return (r2 = _toPropertyKey8(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey8(t) {
    var i = _toPrimitive8(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive8(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  var INIT = "init";
  var PENDING = "pending";
  var ACTIVE = "active";
  var COMPLETED = "completed";
  function duration(time2) {
    return Math.max(0, time2);
  }
  var RechartsAnimation = class {
    /**
     * Returns the absolute time after the animationBegin delay has been completed,
     * and when the animationDuration started ticking.
     */
    getAnimationStartedTime() {
      return this.animationStartedTime;
    }
    /**
     * Returns the absolute time of when the animation began - now it will wait for {animationBegin} ms before the transition starts
     */
    getBeginStartedTime() {
      return this.beginStartedTime;
    }
    constructor(param) {
      var _param$onAnimationSta;
      _defineProperty8(this, "state", INIT);
      this.animationId = param.animationId;
      this.onAnimationEnd = param.onAnimationEnd;
      this.animationDuration = duration(param.animationDuration);
      this.animationBegin = duration(param.animationBegin);
      this.progress = 0;
      this.from = param.from;
      this.to = param.to;
      this.easing = param.easing;
      (_param$onAnimationSta = param.onAnimationStart) === null || _param$onAnimationSta === void 0 || _param$onAnimationSta.call(param);
    }
    /**
     * Returns the state machine current state
     * - `init`:       animation had just been created. It immediately calls `onAnimationStart`
     * - `pending`:    animation is now paused for `animationBegin` milliseconds until the transition begins
     * - `active`:     animation is transitioning items on screen
     * - `completed`:  animation has completed its transition and executed `onAnimationEnd`.
     *                 This state is final and the animation is no longer allowed to transition to other states.
     */
    getState() {
      return this.state;
    }
    /**
     * Returns the easing input or function
     */
    getEasing() {
      return this.easing;
    }
    /**
     * Returns the configuration - the duration of the transition.
     * Does not change in time, does not change when state changes, this is a static value.
     */
    getAnimationDuration() {
      return this.animationDuration;
    }
    /**
     * Sets the current time of the animation. The animation sets its internal state and progress accordingly.
     * This is current, absolute time; not additive!
     * This allows you to essentially "travel back in time" based on the value you pass in here.
     *
     * Returns the (relative) time remaining until the current activity is over.
     * Meaning: if the state is in a middle of a delay, returns the time left until the delay is finished.
     * If the state is in the middle of a transition, returns time left until that transition is complete.
     * This is useful because it's the same number you can take and put into setTimeout(fn, X)
     * as that's how much time we need to wait until the next state transition happens.
     */
    tick(now2) {
      if (this.getState() === INIT) {
        this.state = PENDING;
        this.beginStartedTime = now2;
        return this.animationBegin;
      }
      if (this.getState() === PENDING) {
        if (this.beginStartedTime == null) {
          throw new Error();
        }
        var _timeElapsed = now2 - this.beginStartedTime;
        if (_timeElapsed >= this.animationBegin) {
          this.state = ACTIVE;
          this.animationStartedTime = now2;
          return this.nextAnimationUpdate(0);
        }
        return duration(this.animationBegin - _timeElapsed);
      }
      if (this.getState() === ACTIVE) {
        if (this.animationStartedTime == null) {
          throw new Error();
        }
        var _timeElapsed2 = now2 - this.animationStartedTime;
        this.setProgress(_timeElapsed2 / this.animationDuration);
        return this.nextAnimationUpdate(_timeElapsed2);
      }
      return 0;
    }
    setProgress(newProgress) {
      this.progress = Math.min(1, Math.max(0, newProgress));
    }
    /**
     * Returns an abstract "progress" which is number between 0 and 1 which shows the distance of transition.
     * This progress depends on the animation state:
     * - `init`: 0
     * - `pending`: 0
     * - `active`: transitioning between [0, 1] based on the time elapsed
     * - `completed`: 1
     *
     * The progress is hard-capped to be between 0 and 1 (inclusive) to avoid overshooting caused by coarse timers.
     * For this reason, the easing function must be applied _after_ this animation state,
     * so that one has a chance to construct dynamic "overshoot" animations.
     *
     * The progress is linear with time.
     * If you wish for easing, use `getInterpolated()` instead.
     */
    getProgress() {
      return this.progress;
    }
    /**
     * Completes the animation. Completed animation:
     * - cannot be manipulated anymore
     * - its progress is set to 1
     * - tick function doesn't do anything
     * - getState() always returns 'completed'
     */
    complete() {
      this.progress = 1;
      if (this.state === "active") {
        var _this$onAnimationEnd;
        (_this$onAnimationEnd = this.onAnimationEnd) === null || _this$onAnimationEnd === void 0 || _this$onAnimationEnd.call(this);
      }
      this.state = COMPLETED;
    }
    /**
     * Returns the starting value of the animation.
     * Does not include progress, easing, interpolation, none of that - just the static starting value
     */
    getFrom() {
      return this.from;
    }
    /**
     * Returns the end value of the animation.
     * Does not include progress, easing, interpolation, none of that - just the static end value
     */
    getTo() {
      return this.to;
    }
    /**
     * Unique identifier of an animation
     */
    getAnimationId() {
      return this.animationId;
    }
    /**
     * Returns the configuration - the duration of delay in between animation initialization, and transition.
     * Does not change in time, does not change when state changes, this is a static value.
     */
    getAnimationBegin() {
      return this.animationBegin;
    }
    /**
     * Returns value of the transition at the current time.
     * The exact details differ based on the animation type
     */
    /**
     * Returns the duration of time of when the controller should ask for the next update
     */
  };
  var JavascriptAnimation = class extends RechartsAnimation {
    // eslint-disable-next-line class-methods-use-this
    nextAnimationUpdate() {
      return 0;
    }
    /**
     * Returns value of the animation after its easing function had been applied.
     * This value, unlike getProgress(), can escape the [0..1] range
     * because this is entirely within the easing function control. Spring typically does this.
     */
    getInterpolated() {
      return this.easing(interpolate(this.getFrom(), this.getTo(), this.getProgress()));
    }
  };

  // node_modules/recharts/es6/animation/timeoutController.js
  var RequestAnimationFrameTimeoutController = class {
    setTimeout(callback) {
      var delay = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 0;
      var startTime = performance.now();
      var requestId = null;
      var executeCallback = (now2) => {
        if (now2 - startTime >= delay) {
          callback(now2);
        } else {
          requestId = requestAnimationFrame(executeCallback);
        }
      };
      requestId = requestAnimationFrame(executeCallback);
      return () => {
        if (requestId != null) {
          cancelAnimationFrame(requestId);
        }
      };
    }
  };

  // node_modules/recharts/es6/animation/JavascriptAnimate.js
  function _slicedToArray5(r2, e) {
    return _arrayWithHoles5(r2) || _iterableToArrayLimit5(r2, e) || _unsupportedIterableToArray5(r2, e) || _nonIterableRest5();
  }
  function _nonIterableRest5() {
    throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  function _unsupportedIterableToArray5(r2, a2) {
    if (r2) {
      if ("string" == typeof r2) return _arrayLikeToArray5(r2, a2);
      var t = {}.toString.call(r2).slice(8, -1);
      return "Object" === t && r2.constructor && (t = r2.constructor.name), "Map" === t || "Set" === t ? Array.from(r2) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray5(r2, a2) : void 0;
    }
  }
  function _arrayLikeToArray5(r2, a2) {
    (null == a2 || a2 > r2.length) && (a2 = r2.length);
    for (var e = 0, n = Array(a2); e < a2; e++) n[e] = r2[e];
    return n;
  }
  function _iterableToArrayLimit5(r2, l) {
    var t = null == r2 ? null : "undefined" != typeof Symbol && r2[Symbol.iterator] || r2["@@iterator"];
    if (null != t) {
      var e, n, i, u, a2 = [], f = true, o = false;
      try {
        if (i = (t = t.call(r2)).next, 0 === l) {
          if (Object(t) !== t) return;
          f = false;
        } else for (; !(f = (e = i.call(t)).done) && (a2.push(e.value), a2.length !== l); f = true) ;
      } catch (r3) {
        o = true, n = r3;
      } finally {
        try {
          if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return;
        } finally {
          if (o) throw n;
        }
      }
      return a2;
    }
  }
  function _arrayWithHoles5(r2) {
    if (Array.isArray(r2)) return r2;
  }
  var defaultJavascriptAnimateProps = {
    begin: 0,
    duration: 1e3,
    easing: "ease",
    isActive: true,
    canBegin: true,
    onAnimationEnd: () => {
    },
    onAnimationStart: () => {
    }
  };
  var from = 0;
  var to = 1;
  function JavascriptAnimate(outsideProps) {
    var props = resolveDefaultProps(outsideProps, defaultJavascriptAnimateProps);
    var animationId = props.animationId, isActiveProp = props.isActive, canBegin = props.canBegin, duration2 = props.duration, easing = props.easing, begin = props.begin, onAnimationEnd = props.onAnimationEnd, onAnimationStart = props.onAnimationStart, children = props.children;
    var prefersReducedMotion = usePrefersReducedMotion();
    var isActive = isActiveProp === "auto" ? !Global.isSsr && !prefersReducedMotion : isActiveProp;
    var animationController = useAnimationController(props.animationController);
    var _useState = (0, import_react17.useState)(isActive ? from : to), _useState2 = _slicedToArray5(_useState, 2), style = _useState2[0], setStyle = _useState2[1];
    (0, import_react17.useEffect)(() => {
      if (!isActive) {
        setStyle(to);
      }
    }, [isActive]);
    (0, import_react17.useEffect)(() => {
      var easingFunction = createEasingFunction(easing);
      if (!isActive || !canBegin || easingFunction == null) {
        return noop;
      }
      var timeoutController = new RequestAnimationFrameTimeoutController();
      var animation = new JavascriptAnimation({
        animationId,
        easing: easingFunction,
        animationDuration: duration2,
        animationBegin: begin,
        onAnimationStart,
        onAnimationEnd,
        from,
        to
      });
      return animationController(timeoutController, animation, setStyle);
    }, [animationController, animationId, isActive, canBegin, duration2, easing, begin, onAnimationStart, onAnimationEnd]);
    return children(Number(style));
  }

  // node_modules/recharts/es6/util/useAnimationId.js
  var import_react18 = __toESM(require_react());
  function useAnimationId(input) {
    var prefix2 = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : "animation-";
    var animationId = (0, import_react18.useRef)(uniqueId(prefix2));
    var prevProps = (0, import_react18.useRef)(input);
    if (prevProps.current !== input) {
      animationId.current = uniqueId(prefix2);
      prevProps.current = input;
    }
    return animationId.current;
  }

  // node_modules/recharts/es6/animation/util.js
  var getDashCase = (name) => name.replace(/([A-Z])/g, (v) => "-".concat(v.toLowerCase()));
  var getTransitionVal = (props, duration2, easing) => props.map((prop) => "".concat(getDashCase(prop), " ").concat(duration2, "ms ").concat(easing)).join(",");

  // node_modules/recharts/es6/shape/Rectangle.js
  var _excluded4 = ["radius"];
  var _excluded22 = ["radius"];
  var _templateObject;
  var _templateObject2;
  var _templateObject3;
  var _templateObject4;
  var _templateObject5;
  var _templateObject6;
  var _templateObject7;
  var _templateObject8;
  var _templateObject9;
  var _templateObject0;
  function ownKeys8(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread8(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys8(Object(t), true).forEach(function(r3) {
        _defineProperty9(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys8(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty9(e, r2, t) {
    return (r2 = _toPropertyKey9(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey9(t) {
    var i = _toPrimitive9(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive9(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  function _extends6() {
    return _extends6 = Object.assign ? Object.assign.bind() : function(n) {
      for (var e = 1; e < arguments.length; e++) {
        var t = arguments[e];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends6.apply(null, arguments);
  }
  function _objectWithoutProperties4(e, t) {
    if (null == e) return {};
    var o, r2, i = _objectWithoutPropertiesLoose4(e, t);
    if (Object.getOwnPropertySymbols) {
      var n = Object.getOwnPropertySymbols(e);
      for (r2 = 0; r2 < n.length; r2++) o = n[r2], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
    }
    return i;
  }
  function _objectWithoutPropertiesLoose4(r2, e) {
    if (null == r2) return {};
    var t = {};
    for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
      if (-1 !== e.indexOf(n)) continue;
      t[n] = r2[n];
    }
    return t;
  }
  function _slicedToArray6(r2, e) {
    return _arrayWithHoles6(r2) || _iterableToArrayLimit6(r2, e) || _unsupportedIterableToArray6(r2, e) || _nonIterableRest6();
  }
  function _nonIterableRest6() {
    throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  function _unsupportedIterableToArray6(r2, a2) {
    if (r2) {
      if ("string" == typeof r2) return _arrayLikeToArray6(r2, a2);
      var t = {}.toString.call(r2).slice(8, -1);
      return "Object" === t && r2.constructor && (t = r2.constructor.name), "Map" === t || "Set" === t ? Array.from(r2) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray6(r2, a2) : void 0;
    }
  }
  function _arrayLikeToArray6(r2, a2) {
    (null == a2 || a2 > r2.length) && (a2 = r2.length);
    for (var e = 0, n = Array(a2); e < a2; e++) n[e] = r2[e];
    return n;
  }
  function _iterableToArrayLimit6(r2, l) {
    var t = null == r2 ? null : "undefined" != typeof Symbol && r2[Symbol.iterator] || r2["@@iterator"];
    if (null != t) {
      var e, n, i, u, a2 = [], f = true, o = false;
      try {
        if (i = (t = t.call(r2)).next, 0 === l) {
          if (Object(t) !== t) return;
          f = false;
        } else for (; !(f = (e = i.call(t)).done) && (a2.push(e.value), a2.length !== l); f = true) ;
      } catch (r3) {
        o = true, n = r3;
      } finally {
        try {
          if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return;
        } finally {
          if (o) throw n;
        }
      }
      return a2;
    }
  }
  function _arrayWithHoles6(r2) {
    if (Array.isArray(r2)) return r2;
  }
  function _taggedTemplateLiteral(e, t) {
    return t || (t = e.slice(0)), Object.freeze(Object.defineProperties(e, { raw: { value: Object.freeze(t) } }));
  }
  var getRectanglePath = (x5, y5, width, height, radius) => {
    var roundedWidth = round(width);
    var roundedHeight = round(height);
    var maxRadius = Math.min(Math.abs(roundedWidth) / 2, Math.abs(roundedHeight) / 2);
    var ySign = roundedHeight >= 0 ? 1 : -1;
    var xSign = roundedWidth >= 0 ? 1 : -1;
    var clockWise = roundedHeight >= 0 && roundedWidth >= 0 || roundedHeight < 0 && roundedWidth < 0 ? 1 : 0;
    var path3;
    if (maxRadius > 0 && Array.isArray(radius)) {
      var newRadius = [0, 0, 0, 0];
      for (var i = 0, len = 4; i < len; i++) {
        var _radius$i;
        var r2 = (_radius$i = radius[i]) !== null && _radius$i !== void 0 ? _radius$i : 0;
        newRadius[i] = r2 > maxRadius ? maxRadius : r2;
      }
      path3 = roundTemplateLiteral(_templateObject || (_templateObject = _taggedTemplateLiteral(["M", ",", ""])), x5, y5 + ySign * newRadius[0]);
      if (newRadius[0] > 0) {
        path3 += roundTemplateLiteral(_templateObject2 || (_templateObject2 = _taggedTemplateLiteral(["A ", ",", ",0,0,", ",", ",", ""])), newRadius[0], newRadius[0], clockWise, x5 + xSign * newRadius[0], y5);
      }
      path3 += roundTemplateLiteral(_templateObject3 || (_templateObject3 = _taggedTemplateLiteral(["L ", ",", ""])), x5 + width - xSign * newRadius[1], y5);
      if (newRadius[1] > 0) {
        path3 += roundTemplateLiteral(_templateObject4 || (_templateObject4 = _taggedTemplateLiteral(["A ", ",", ",0,0,", ",\n        ", ",", ""])), newRadius[1], newRadius[1], clockWise, x5 + width, y5 + ySign * newRadius[1]);
      }
      path3 += roundTemplateLiteral(_templateObject5 || (_templateObject5 = _taggedTemplateLiteral(["L ", ",", ""])), x5 + width, y5 + height - ySign * newRadius[2]);
      if (newRadius[2] > 0) {
        path3 += roundTemplateLiteral(_templateObject6 || (_templateObject6 = _taggedTemplateLiteral(["A ", ",", ",0,0,", ",\n        ", ",", ""])), newRadius[2], newRadius[2], clockWise, x5 + width - xSign * newRadius[2], y5 + height);
      }
      path3 += roundTemplateLiteral(_templateObject7 || (_templateObject7 = _taggedTemplateLiteral(["L ", ",", ""])), x5 + xSign * newRadius[3], y5 + height);
      if (newRadius[3] > 0) {
        path3 += roundTemplateLiteral(_templateObject8 || (_templateObject8 = _taggedTemplateLiteral(["A ", ",", ",0,0,", ",\n        ", ",", ""])), newRadius[3], newRadius[3], clockWise, x5, y5 + height - ySign * newRadius[3]);
      }
      path3 += "Z";
    } else if (maxRadius > 0 && radius === +radius && radius > 0) {
      var _newRadius = Math.min(maxRadius, radius);
      path3 = roundTemplateLiteral(_templateObject9 || (_templateObject9 = _taggedTemplateLiteral(["M ", ",", "\n            A ", ",", ",0,0,", ",", ",", "\n            L ", ",", "\n            A ", ",", ",0,0,", ",", ",", "\n            L ", ",", "\n            A ", ",", ",0,0,", ",", ",", "\n            L ", ",", "\n            A ", ",", ",0,0,", ",", ",", " Z"])), x5, y5 + ySign * _newRadius, _newRadius, _newRadius, clockWise, x5 + xSign * _newRadius, y5, x5 + width - xSign * _newRadius, y5, _newRadius, _newRadius, clockWise, x5 + width, y5 + ySign * _newRadius, x5 + width, y5 + height - ySign * _newRadius, _newRadius, _newRadius, clockWise, x5 + width - xSign * _newRadius, y5 + height, x5 + xSign * _newRadius, y5 + height, _newRadius, _newRadius, clockWise, x5, y5 + height - ySign * _newRadius);
    } else {
      path3 = roundTemplateLiteral(_templateObject0 || (_templateObject0 = _taggedTemplateLiteral(["M ", ",", " h ", " v ", " h ", " Z"])), x5, y5, width, height, -width);
    }
    return path3;
  };
  var defaultRectangleProps = {
    x: 0,
    y: 0,
    width: 0,
    height: 0,
    radius: 0,
    isAnimationActive: false,
    isUpdateAnimationActive: false,
    animationBegin: 0,
    animationDuration: 1500,
    animationEasing: "ease"
  };
  var Rectangle = (rectangleProps) => {
    var props = resolveDefaultProps(rectangleProps, defaultRectangleProps);
    var pathRef = (0, import_react19.useRef)(null);
    var _useState = (0, import_react19.useState)(-1), _useState2 = _slicedToArray6(_useState, 2), totalLength = _useState2[0], setTotalLength = _useState2[1];
    (0, import_react19.useEffect)(() => {
      if (pathRef.current && pathRef.current.getTotalLength) {
        try {
          var pathTotalLength = pathRef.current.getTotalLength();
          if (pathTotalLength) {
            setTotalLength(pathTotalLength);
          }
        } catch (_unused) {
        }
      }
    }, []);
    var x5 = props.x, y5 = props.y, width = props.width, height = props.height, radius = props.radius, className = props.className;
    var animationEasing = props.animationEasing, animationDuration = props.animationDuration, animationBegin = props.animationBegin, isAnimationActive = props.isAnimationActive, isUpdateAnimationActive = props.isUpdateAnimationActive;
    var prevWidthRef = (0, import_react19.useRef)(width);
    var prevHeightRef = (0, import_react19.useRef)(height);
    var prevXRef = (0, import_react19.useRef)(x5);
    var prevYRef = (0, import_react19.useRef)(y5);
    var animationIdInput = (0, import_react19.useMemo)(() => ({
      x: x5,
      y: y5,
      width,
      height,
      radius
    }), [x5, y5, width, height, radius]);
    var animationId = useAnimationId(animationIdInput, "rectangle-");
    if (x5 !== +x5 || y5 !== +y5 || width !== +width || height !== +height || width === 0 || height === 0) {
      return null;
    }
    var layerClass = clsx("recharts-rectangle", className);
    if (!isUpdateAnimationActive) {
      var _svgPropertiesAndEven = svgPropertiesAndEvents(props), _ = _svgPropertiesAndEven.radius, otherPathProps = _objectWithoutProperties4(_svgPropertiesAndEven, _excluded4);
      return /* @__PURE__ */ React10.createElement("path", _extends6({}, otherPathProps, {
        x: round(x5),
        y: round(y5),
        width: round(width),
        height: round(height),
        radius: typeof radius === "number" ? radius : void 0,
        className: layerClass,
        d: getRectanglePath(x5, y5, width, height, radius)
      }));
    }
    var prevWidth = prevWidthRef.current;
    var prevHeight = prevHeightRef.current;
    var prevX = prevXRef.current;
    var prevY = prevYRef.current;
    var from2 = "0px ".concat(totalLength === -1 ? 1 : totalLength, "px");
    var to2 = "".concat(totalLength, "px ").concat(totalLength, "px");
    var transition = getTransitionVal(["strokeDasharray"], animationDuration, typeof animationEasing === "string" ? animationEasing : defaultRectangleProps.animationEasing);
    return /* @__PURE__ */ React10.createElement(JavascriptAnimate, {
      animationId,
      key: animationId,
      canBegin: totalLength > 0,
      duration: animationDuration,
      easing: animationEasing,
      isActive: isUpdateAnimationActive,
      begin: animationBegin
    }, (animationElapsedTime) => {
      var currWidth = interpolate(prevWidth, width, animationElapsedTime);
      var currHeight = interpolate(prevHeight, height, animationElapsedTime);
      var currX = interpolate(prevX, x5, animationElapsedTime);
      var currY = interpolate(prevY, y5, animationElapsedTime);
      if (pathRef.current) {
        prevWidthRef.current = currWidth;
        prevHeightRef.current = currHeight;
        prevXRef.current = currX;
        prevYRef.current = currY;
      }
      var animationStyle;
      if (!isAnimationActive) {
        animationStyle = {
          strokeDasharray: to2
        };
      } else if (animationElapsedTime > 0) {
        animationStyle = {
          transition,
          strokeDasharray: to2
        };
      } else {
        animationStyle = {
          strokeDasharray: from2
        };
      }
      var _svgPropertiesAndEven2 = svgPropertiesAndEvents(props), _2 = _svgPropertiesAndEven2.radius, otherPathProps2 = _objectWithoutProperties4(_svgPropertiesAndEven2, _excluded22);
      return /* @__PURE__ */ React10.createElement("path", _extends6({}, otherPathProps2, {
        radius: typeof radius === "number" ? radius : void 0,
        className: layerClass,
        d: getRectanglePath(currX, currY, currWidth, currHeight, radius),
        ref: pathRef,
        style: _objectSpread8(_objectSpread8({}, animationStyle), props.style)
      }));
    });
  };

  // node_modules/recharts/es6/util/PolarUtils.js
  function ownKeys9(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread9(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys9(Object(t), true).forEach(function(r3) {
        _defineProperty10(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys9(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty10(e, r2, t) {
    return (r2 = _toPropertyKey10(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey10(t) {
    var i = _toPrimitive10(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive10(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  var RADIAN = Math.PI / 180;
  var radianToDegree = (angleInRadian) => angleInRadian * 180 / Math.PI;
  var polarToCartesian = (cx, cy, radius, angle) => ({
    x: cx + Math.cos(-RADIAN * angle) * radius,
    y: cy + Math.sin(-RADIAN * angle) * radius
  });
  var getMaxRadius = function getMaxRadius2(width, height) {
    var offset = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {
      top: 0,
      right: 0,
      bottom: 0,
      left: 0,
      width: 0,
      height: 0,
      brushBottom: 0
    };
    return Math.min(Math.abs(width - (offset.left || 0) - (offset.right || 0)), Math.abs(height - (offset.top || 0) - (offset.bottom || 0))) / 2;
  };
  var distanceBetweenPoints = (point6, anotherPoint) => {
    var x1 = point6.x, y1 = point6.y;
    var x22 = anotherPoint.x, y22 = anotherPoint.y;
    return Math.sqrt((x1 - x22) ** 2 + (y1 - y22) ** 2);
  };
  var getAngleOfPoint = (_ref2, _ref22) => {
    var x5 = _ref2.x, y5 = _ref2.y;
    var cx = _ref22.cx, cy = _ref22.cy;
    var radius = distanceBetweenPoints({
      x: x5,
      y: y5
    }, {
      x: cx,
      y: cy
    });
    if (radius <= 0) {
      return {
        radius,
        angle: 0
      };
    }
    var cos3 = (x5 - cx) / radius;
    var angleInRadian = Math.acos(cos3);
    if (y5 > cy) {
      angleInRadian = 2 * Math.PI - angleInRadian;
    }
    return {
      radius,
      angle: radianToDegree(angleInRadian),
      angleInRadian
    };
  };
  var formatAngleOfSector = (_ref3) => {
    var startAngle = _ref3.startAngle, endAngle = _ref3.endAngle;
    var startCnt = Math.floor(startAngle / 360);
    var endCnt = Math.floor(endAngle / 360);
    var min3 = Math.min(startCnt, endCnt);
    return {
      startAngle: startAngle - min3 * 360,
      endAngle: endAngle - min3 * 360
    };
  };
  var reverseFormatAngleOfSector = (angle, _ref4) => {
    var startAngle = _ref4.startAngle, endAngle = _ref4.endAngle;
    var startCnt = Math.floor(startAngle / 360);
    var endCnt = Math.floor(endAngle / 360);
    var min3 = Math.min(startCnt, endCnt);
    return angle + min3 * 360;
  };
  var inRangeOfSector = (_ref5, viewBox) => {
    var x5 = _ref5.relativeX, y5 = _ref5.relativeY;
    var _getAngleOfPoint = getAngleOfPoint({
      x: x5,
      y: y5
    }, viewBox), radius = _getAngleOfPoint.radius, angle = _getAngleOfPoint.angle;
    var innerRadius = viewBox.innerRadius, outerRadius = viewBox.outerRadius;
    if (radius < innerRadius || radius > outerRadius) {
      return null;
    }
    if (radius === 0) {
      return null;
    }
    var _formatAngleOfSector = formatAngleOfSector(viewBox), startAngle = _formatAngleOfSector.startAngle, endAngle = _formatAngleOfSector.endAngle;
    var formatAngle = angle;
    var inRange;
    if (startAngle <= endAngle) {
      while (formatAngle > endAngle) {
        formatAngle -= 360;
      }
      while (formatAngle < startAngle) {
        formatAngle += 360;
      }
      inRange = formatAngle >= startAngle && formatAngle <= endAngle;
    } else {
      while (formatAngle > startAngle) {
        formatAngle -= 360;
      }
      while (formatAngle < endAngle) {
        formatAngle += 360;
      }
      inRange = formatAngle >= endAngle && formatAngle <= startAngle;
    }
    if (inRange) {
      return _objectSpread9(_objectSpread9({}, viewBox), {}, {
        radius,
        angle: reverseFormatAngleOfSector(formatAngle, viewBox)
      });
    }
    return null;
  };

  // node_modules/recharts/es6/util/cursor/getRadialCursorPoints.js
  function getRadialCursorPoints(activeCoordinate) {
    var cx = activeCoordinate.cx, cy = activeCoordinate.cy, radius = activeCoordinate.radius, startAngle = activeCoordinate.startAngle, endAngle = activeCoordinate.endAngle;
    var startPoint = polarToCartesian(cx, cy, radius, startAngle);
    var endPoint = polarToCartesian(cx, cy, radius, endAngle);
    return {
      points: [startPoint, endPoint],
      cx,
      cy,
      radius,
      startAngle,
      endAngle
    };
  }

  // node_modules/recharts/es6/shape/Sector.js
  var React11 = __toESM(require_react());
  var _templateObject10;
  var _templateObject22;
  var _templateObject32;
  var _templateObject42;
  var _templateObject52;
  var _templateObject62;
  var _templateObject72;
  function _extends7() {
    return _extends7 = Object.assign ? Object.assign.bind() : function(n) {
      for (var e = 1; e < arguments.length; e++) {
        var t = arguments[e];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends7.apply(null, arguments);
  }
  function _taggedTemplateLiteral2(e, t) {
    return t || (t = e.slice(0)), Object.freeze(Object.defineProperties(e, { raw: { value: Object.freeze(t) } }));
  }
  var getDeltaAngle = (startAngle, endAngle) => {
    var sign2 = mathSign(endAngle - startAngle);
    var deltaAngle = Math.min(Math.abs(endAngle - startAngle), 359.999);
    return sign2 * deltaAngle;
  };
  var getTangentCircle = (_ref2) => {
    var cx = _ref2.cx, cy = _ref2.cy, radius = _ref2.radius, angle = _ref2.angle, sign2 = _ref2.sign, isExternal = _ref2.isExternal, cornerRadius = _ref2.cornerRadius, cornerIsExternal = _ref2.cornerIsExternal;
    var centerRadius = cornerRadius * (isExternal ? 1 : -1) + radius;
    var theta = Math.asin(cornerRadius / centerRadius) / RADIAN;
    var centerAngle = cornerIsExternal ? angle : angle + sign2 * theta;
    var center2 = polarToCartesian(cx, cy, centerRadius, centerAngle);
    var circleTangency = polarToCartesian(cx, cy, radius, centerAngle);
    var lineTangencyAngle = cornerIsExternal ? angle - sign2 * theta : angle;
    var lineTangency = polarToCartesian(cx, cy, centerRadius * Math.cos(theta * RADIAN), lineTangencyAngle);
    return {
      center: center2,
      circleTangency,
      lineTangency,
      theta
    };
  };
  var getSectorPath = (_ref2) => {
    var cx = _ref2.cx, cy = _ref2.cy, innerRadius = _ref2.innerRadius, outerRadius = _ref2.outerRadius, startAngle = _ref2.startAngle, endAngle = _ref2.endAngle;
    var angle = getDeltaAngle(startAngle, endAngle);
    var tempEndAngle = startAngle + angle;
    var outerStartPoint = polarToCartesian(cx, cy, outerRadius, startAngle);
    var outerEndPoint = polarToCartesian(cx, cy, outerRadius, tempEndAngle);
    var path3 = roundTemplateLiteral(_templateObject10 || (_templateObject10 = _taggedTemplateLiteral2(["M ", ",", "\n    A ", ",", ",0,\n    ", ",", ",\n    ", ",", "\n  "])), outerStartPoint.x, outerStartPoint.y, outerRadius, outerRadius, +(Math.abs(angle) > 180), +(startAngle > tempEndAngle), outerEndPoint.x, outerEndPoint.y);
    if (innerRadius > 0) {
      var innerStartPoint = polarToCartesian(cx, cy, innerRadius, startAngle);
      var innerEndPoint = polarToCartesian(cx, cy, innerRadius, tempEndAngle);
      path3 += roundTemplateLiteral(_templateObject22 || (_templateObject22 = _taggedTemplateLiteral2(["L ", ",", "\n            A ", ",", ",0,\n            ", ",", ",\n            ", ",", " Z"])), innerEndPoint.x, innerEndPoint.y, innerRadius, innerRadius, +(Math.abs(angle) > 180), +(startAngle <= tempEndAngle), innerStartPoint.x, innerStartPoint.y);
    } else {
      path3 += roundTemplateLiteral(_templateObject32 || (_templateObject32 = _taggedTemplateLiteral2(["L ", ",", " Z"])), cx, cy);
    }
    return path3;
  };
  var getSectorWithCorner = (_ref3) => {
    var cx = _ref3.cx, cy = _ref3.cy, innerRadius = _ref3.innerRadius, outerRadius = _ref3.outerRadius, cornerRadius = _ref3.cornerRadius, forceCornerRadius = _ref3.forceCornerRadius, cornerIsExternal = _ref3.cornerIsExternal, startAngle = _ref3.startAngle, endAngle = _ref3.endAngle;
    var sign2 = mathSign(endAngle - startAngle);
    var _getTangentCircle = getTangentCircle({
      cx,
      cy,
      radius: outerRadius,
      angle: startAngle,
      sign: sign2,
      cornerRadius,
      cornerIsExternal
    }), soct = _getTangentCircle.circleTangency, solt = _getTangentCircle.lineTangency, sot = _getTangentCircle.theta;
    var _getTangentCircle2 = getTangentCircle({
      cx,
      cy,
      radius: outerRadius,
      angle: endAngle,
      sign: -sign2,
      cornerRadius,
      cornerIsExternal
    }), eoct = _getTangentCircle2.circleTangency, eolt = _getTangentCircle2.lineTangency, eot = _getTangentCircle2.theta;
    var outerArcAngle = cornerIsExternal ? Math.abs(startAngle - endAngle) : Math.abs(startAngle - endAngle) - sot - eot;
    if (outerArcAngle < 0) {
      if (forceCornerRadius) {
        return roundTemplateLiteral(_templateObject42 || (_templateObject42 = _taggedTemplateLiteral2(["M ", ",", "\n        a", ",", ",0,0,1,", ",0\n        a", ",", ",0,0,1,", ",0\n      "])), solt.x, solt.y, cornerRadius, cornerRadius, cornerRadius * 2, cornerRadius, cornerRadius, -cornerRadius * 2);
      }
      return getSectorPath({
        cx,
        cy,
        innerRadius,
        outerRadius,
        startAngle,
        endAngle
      });
    }
    var path3 = roundTemplateLiteral(_templateObject52 || (_templateObject52 = _taggedTemplateLiteral2(["M ", ",", "\n    A", ",", ",0,0,", ",", ",", "\n    A", ",", ",0,", ",", ",", ",", "\n    A", ",", ",0,0,", ",", ",", "\n  "])), solt.x, solt.y, cornerRadius, cornerRadius, +(sign2 < 0), soct.x, soct.y, outerRadius, outerRadius, +(outerArcAngle > 180), +(sign2 < 0), eoct.x, eoct.y, cornerRadius, cornerRadius, +(sign2 < 0), eolt.x, eolt.y);
    if (innerRadius > 0) {
      var _getTangentCircle3 = getTangentCircle({
        cx,
        cy,
        radius: innerRadius,
        angle: startAngle,
        sign: sign2,
        isExternal: true,
        cornerRadius,
        cornerIsExternal
      }), sict = _getTangentCircle3.circleTangency, silt = _getTangentCircle3.lineTangency, sit = _getTangentCircle3.theta;
      var _getTangentCircle4 = getTangentCircle({
        cx,
        cy,
        radius: innerRadius,
        angle: endAngle,
        sign: -sign2,
        isExternal: true,
        cornerRadius,
        cornerIsExternal
      }), eict = _getTangentCircle4.circleTangency, eilt = _getTangentCircle4.lineTangency, eit = _getTangentCircle4.theta;
      var innerArcAngle = cornerIsExternal ? Math.abs(startAngle - endAngle) : Math.abs(startAngle - endAngle) - sit - eit;
      if (innerArcAngle < 0 && cornerRadius === 0) {
        return "".concat(path3, "L").concat(cx, ",").concat(cy, "Z");
      }
      path3 += roundTemplateLiteral(_templateObject62 || (_templateObject62 = _taggedTemplateLiteral2(["L", ",", "\n      A", ",", ",0,0,", ",", ",", "\n      A", ",", ",0,", ",", ",", ",", "\n      A", ",", ",0,0,", ",", ",", "Z"])), eilt.x, eilt.y, cornerRadius, cornerRadius, +(sign2 < 0), eict.x, eict.y, innerRadius, innerRadius, +(innerArcAngle > 180), +(sign2 > 0), sict.x, sict.y, cornerRadius, cornerRadius, +(sign2 < 0), silt.x, silt.y);
    } else {
      path3 += roundTemplateLiteral(_templateObject72 || (_templateObject72 = _taggedTemplateLiteral2(["L", ",", "Z"])), cx, cy);
    }
    return path3;
  };
  var defaultSectorProps = {
    cx: 0,
    cy: 0,
    innerRadius: 0,
    outerRadius: 0,
    startAngle: 0,
    endAngle: 0,
    cornerRadius: 0,
    forceCornerRadius: false,
    cornerIsExternal: false
  };
  var Sector = (sectorProps) => {
    var props = resolveDefaultProps(sectorProps, defaultSectorProps);
    var cx = props.cx, cy = props.cy, innerRadius = props.innerRadius, outerRadius = props.outerRadius, cornerRadius = props.cornerRadius, forceCornerRadius = props.forceCornerRadius, cornerIsExternal = props.cornerIsExternal, startAngle = props.startAngle, endAngle = props.endAngle, className = props.className;
    if (outerRadius < innerRadius || startAngle === endAngle) {
      return null;
    }
    var layerClass = clsx("recharts-sector", className);
    var deltaRadius = outerRadius - innerRadius;
    var cr = getPercentValue(cornerRadius, deltaRadius, 0, true);
    var path3;
    if (cr > 0 && Math.abs(startAngle - endAngle) < 360) {
      path3 = getSectorWithCorner({
        cx,
        cy,
        innerRadius,
        outerRadius,
        cornerRadius: Math.min(cr, deltaRadius / 2),
        forceCornerRadius,
        cornerIsExternal,
        startAngle,
        endAngle
      });
    } else {
      path3 = getSectorPath({
        cx,
        cy,
        innerRadius,
        outerRadius,
        startAngle,
        endAngle
      });
    }
    return /* @__PURE__ */ React11.createElement("path", _extends7({}, svgPropertiesAndEvents(props), {
      className: layerClass,
      d: path3
    }));
  };

  // node_modules/recharts/es6/util/cursor/getCursorPoints.js
  function getCursorPoints(layout, activeCoordinate, offset) {
    if (layout === "horizontal") {
      return [{
        x: activeCoordinate.x,
        y: offset.top
      }, {
        x: activeCoordinate.x,
        y: offset.top + offset.height
      }];
    }
    if (layout === "vertical") {
      return [{
        x: offset.left,
        y: activeCoordinate.y
      }, {
        x: offset.left + offset.width,
        y: activeCoordinate.y
      }];
    }
    if (isPolarCoordinate(activeCoordinate)) {
      if (layout === "centric") {
        var cx = activeCoordinate.cx, cy = activeCoordinate.cy, innerRadius = activeCoordinate.innerRadius, outerRadius = activeCoordinate.outerRadius, angle = activeCoordinate.angle;
        var innerPoint = polarToCartesian(cx, cy, innerRadius, angle);
        var outerPoint = polarToCartesian(cx, cy, outerRadius, angle);
        return [{
          x: innerPoint.x,
          y: innerPoint.y
        }, {
          x: outerPoint.x,
          y: outerPoint.y
        }];
      }
      return getRadialCursorPoints(activeCoordinate);
    }
    return void 0;
  }

  // node_modules/es-toolkit/dist/compat/util/toNumber.mjs
  function toNumber(value2) {
    if (isSymbol(value2)) return NaN;
    return Number(value2);
  }

  // node_modules/es-toolkit/dist/compat/util/toFinite.mjs
  function toFinite(value2) {
    if (!value2) return value2 === 0 ? value2 : 0;
    value2 = toNumber(value2);
    if (value2 === Infinity || value2 === -Infinity) return (value2 < 0 ? -1 : 1) * Number.MAX_VALUE;
    return value2 === value2 ? value2 : 0;
  }

  // node_modules/es-toolkit/dist/compat/math/range.mjs
  function range(start, end, step) {
    if (step && typeof step !== "number" && isIterateeCall(start, end, step)) end = step = void 0;
    start = toFinite(start);
    if (end === void 0) {
      end = start;
      start = 0;
    } else end = toFinite(end);
    step = step === void 0 ? start < end ? 1 : -1 : toFinite(step);
    const length = Math.max(Math.ceil((end - start) / (step || 1)), 0);
    const result = new Array(length);
    for (let index2 = 0; index2 < length; index2++) {
      result[index2] = start;
      start += step;
    }
    return result;
  }

  // node_modules/recharts/es6/state/selectors/dataSelectors.js
  var selectChartDataWithIndexes = (state) => state.chartData;
  var selectChartDataAndAlwaysIgnoreIndexes = createSelector([selectChartDataWithIndexes], (dataState) => {
    var dataEndIndex = dataState.chartData != null ? dataState.chartData.length - 1 : 0;
    return {
      chartData: dataState.chartData,
      computedData: dataState.computedData,
      dataEndIndex,
      dataStartIndex: 0
    };
  });
  var selectChartDataWithIndexesIfNotInPanoramaPosition4 = (state, _unused1, _unused2, isPanorama) => {
    if (isPanorama) {
      return selectChartDataAndAlwaysIgnoreIndexes(state);
    }
    return selectChartDataWithIndexes(state);
  };
  var selectChartDataWithIndexesIfNotInPanoramaPosition3 = (state, _unused1, isPanorama) => {
    if (isPanorama) {
      return selectChartDataAndAlwaysIgnoreIndexes(state);
    }
    return selectChartDataWithIndexes(state);
  };
  var selectChartDataSliceIfNotInPanorama = createSelector([selectChartDataWithIndexesIfNotInPanoramaPosition4], (_ref2) => {
    var chartData = _ref2.chartData, dataStartIndex = _ref2.dataStartIndex, dataEndIndex = _ref2.dataEndIndex;
    return chartData != null ? chartData.slice(dataStartIndex, dataEndIndex + 1) : [];
  });
  var selectChartDataSliceIgnoringIndexes = createSelector([selectChartDataAndAlwaysIgnoreIndexes], (_ref2) => {
    var chartData = _ref2.chartData, dataStartIndex = _ref2.dataStartIndex, dataEndIndex = _ref2.dataEndIndex;
    return chartData != null ? chartData.slice(dataStartIndex, dataEndIndex + 1) : [];
  });
  var selectChartDataSliceWithIndexes = createSelector([selectChartDataWithIndexes], (_ref3) => {
    var chartData = _ref3.chartData, dataStartIndex = _ref3.dataStartIndex, dataEndIndex = _ref3.dataEndIndex;
    return chartData != null ? chartData.slice(dataStartIndex, dataEndIndex + 1) : [];
  });

  // node_modules/recharts/es6/util/isDomainSpecifiedByUser.js
  function _slicedToArray7(r2, e) {
    return _arrayWithHoles7(r2) || _iterableToArrayLimit7(r2, e) || _unsupportedIterableToArray7(r2, e) || _nonIterableRest7();
  }
  function _nonIterableRest7() {
    throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  function _unsupportedIterableToArray7(r2, a2) {
    if (r2) {
      if ("string" == typeof r2) return _arrayLikeToArray7(r2, a2);
      var t = {}.toString.call(r2).slice(8, -1);
      return "Object" === t && r2.constructor && (t = r2.constructor.name), "Map" === t || "Set" === t ? Array.from(r2) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray7(r2, a2) : void 0;
    }
  }
  function _arrayLikeToArray7(r2, a2) {
    (null == a2 || a2 > r2.length) && (a2 = r2.length);
    for (var e = 0, n = Array(a2); e < a2; e++) n[e] = r2[e];
    return n;
  }
  function _iterableToArrayLimit7(r2, l) {
    var t = null == r2 ? null : "undefined" != typeof Symbol && r2[Symbol.iterator] || r2["@@iterator"];
    if (null != t) {
      var e, n, i, u, a2 = [], f = true, o = false;
      try {
        if (i = (t = t.call(r2)).next, 0 === l) {
          if (Object(t) !== t) return;
          f = false;
        } else for (; !(f = (e = i.call(t)).done) && (a2.push(e.value), a2.length !== l); f = true) ;
      } catch (r3) {
        o = true, n = r3;
      } finally {
        try {
          if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return;
        } finally {
          if (o) throw n;
        }
      }
      return a2;
    }
  }
  function _arrayWithHoles7(r2) {
    if (Array.isArray(r2)) return r2;
  }
  function isWellFormedNumberDomain(v) {
    if (Array.isArray(v) && v.length === 2) {
      var _v = _slicedToArray7(v, 2), min3 = _v[0], max4 = _v[1];
      if (isWellBehavedNumber(min3) && isWellBehavedNumber(max4)) {
        return true;
      }
    }
    return false;
  }
  function extendDomain(providedDomain, boundaryDomain, allowDataOverflow) {
    if (allowDataOverflow) {
      return providedDomain;
    }
    return [Math.min(providedDomain[0], boundaryDomain[0]), Math.max(providedDomain[1], boundaryDomain[1])];
  }
  function numericalDomainSpecifiedWithoutRequiringData(userDomain, allowDataOverflow) {
    if (!allowDataOverflow) {
      return void 0;
    }
    if (typeof userDomain === "function") {
      return void 0;
    }
    if (Array.isArray(userDomain) && userDomain.length === 2) {
      var _userDomain = _slicedToArray7(userDomain, 2), providedMin = _userDomain[0], providedMax = _userDomain[1];
      var finalMin, finalMax;
      if (isWellBehavedNumber(providedMin)) {
        finalMin = providedMin;
      } else if (typeof providedMin === "function") {
        return void 0;
      }
      if (isWellBehavedNumber(providedMax)) {
        finalMax = providedMax;
      } else if (typeof providedMax === "function") {
        return void 0;
      }
      var candidate = [finalMin, finalMax];
      if (isWellFormedNumberDomain(candidate)) {
        return candidate;
      }
    }
    return void 0;
  }
  function parseNumericalUserDomain(userDomain, dataDomain, allowDataOverflow) {
    if (!allowDataOverflow && dataDomain == null) {
      return void 0;
    }
    if (typeof userDomain === "function" && dataDomain != null) {
      try {
        var result = userDomain(dataDomain, allowDataOverflow);
        if (isWellFormedNumberDomain(result)) {
          return extendDomain(result, dataDomain, allowDataOverflow);
        }
      } catch (_unused) {
      }
    }
    if (Array.isArray(userDomain) && userDomain.length === 2) {
      var _userDomain2 = _slicedToArray7(userDomain, 2), providedMin = _userDomain2[0], providedMax = _userDomain2[1];
      var finalMin, finalMax;
      if (providedMin === "auto") {
        if (dataDomain != null) {
          finalMin = Math.min(...dataDomain);
        }
      } else if (isNumber(providedMin)) {
        finalMin = providedMin;
      } else if (typeof providedMin === "function") {
        try {
          if (dataDomain != null) {
            finalMin = providedMin(dataDomain === null || dataDomain === void 0 ? void 0 : dataDomain[0]);
          }
        } catch (_unused2) {
        }
      } else if (typeof providedMin === "string" && MIN_VALUE_REG.test(providedMin)) {
        var match = MIN_VALUE_REG.exec(providedMin);
        if (match == null || match[1] == null || dataDomain == null) {
          finalMin = void 0;
        } else {
          var value2 = +match[1];
          finalMin = dataDomain[0] - value2;
        }
      } else {
        finalMin = dataDomain === null || dataDomain === void 0 ? void 0 : dataDomain[0];
      }
      if (providedMax === "auto") {
        if (dataDomain != null) {
          finalMax = Math.max(...dataDomain);
        }
      } else if (isNumber(providedMax)) {
        finalMax = providedMax;
      } else if (typeof providedMax === "function") {
        try {
          if (dataDomain != null) {
            finalMax = providedMax(dataDomain === null || dataDomain === void 0 ? void 0 : dataDomain[1]);
          }
        } catch (_unused3) {
        }
      } else if (typeof providedMax === "string" && MAX_VALUE_REG.test(providedMax)) {
        var _match = MAX_VALUE_REG.exec(providedMax);
        if (_match == null || _match[1] == null || dataDomain == null) {
          finalMax = void 0;
        } else {
          var _value = +_match[1];
          finalMax = dataDomain[1] + _value;
        }
      } else {
        finalMax = dataDomain === null || dataDomain === void 0 ? void 0 : dataDomain[1];
      }
      var candidate = [finalMin, finalMax];
      if (isWellFormedNumberDomain(candidate)) {
        if (dataDomain == null) {
          return candidate;
        }
        return extendDomain(candidate, dataDomain, allowDataOverflow);
      }
    }
    return void 0;
  }

  // node_modules/recharts/es6/util/scale/getNiceTickValues.js
  var import_decimal2 = __toESM(require_decimal());

  // node_modules/recharts/es6/util/scale/util/arithmetic.js
  var import_decimal = __toESM(require_decimal());
  function getDigitCount(value2) {
    var result;
    if (value2 === 0) {
      result = 1;
    } else {
      result = Math.floor(new import_decimal.default(value2).abs().log(10).toNumber()) + 1;
    }
    return result;
  }
  function rangeStep(start, end, step) {
    var num = new import_decimal.default(start);
    var i = 0;
    var result = [];
    while (num.lt(end) && i < 1e5) {
      result.push(num.toNumber());
      num = num.add(step);
      i++;
    }
    return result;
  }

  // node_modules/recharts/es6/util/scale/getNiceTickValues.js
  function _slicedToArray8(r2, e) {
    return _arrayWithHoles8(r2) || _iterableToArrayLimit8(r2, e) || _unsupportedIterableToArray8(r2, e) || _nonIterableRest8();
  }
  function _nonIterableRest8() {
    throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  function _unsupportedIterableToArray8(r2, a2) {
    if (r2) {
      if ("string" == typeof r2) return _arrayLikeToArray8(r2, a2);
      var t = {}.toString.call(r2).slice(8, -1);
      return "Object" === t && r2.constructor && (t = r2.constructor.name), "Map" === t || "Set" === t ? Array.from(r2) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray8(r2, a2) : void 0;
    }
  }
  function _arrayLikeToArray8(r2, a2) {
    (null == a2 || a2 > r2.length) && (a2 = r2.length);
    for (var e = 0, n = Array(a2); e < a2; e++) n[e] = r2[e];
    return n;
  }
  function _iterableToArrayLimit8(r2, l) {
    var t = null == r2 ? null : "undefined" != typeof Symbol && r2[Symbol.iterator] || r2["@@iterator"];
    if (null != t) {
      var e, n, i, u, a2 = [], f = true, o = false;
      try {
        if (i = (t = t.call(r2)).next, 0 === l) {
          if (Object(t) !== t) return;
          f = false;
        } else for (; !(f = (e = i.call(t)).done) && (a2.push(e.value), a2.length !== l); f = true) ;
      } catch (r3) {
        o = true, n = r3;
      } finally {
        try {
          if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return;
        } finally {
          if (o) throw n;
        }
      }
      return a2;
    }
  }
  function _arrayWithHoles8(r2) {
    if (Array.isArray(r2)) return r2;
  }
  var getValidInterval = (_ref2) => {
    var _ref22 = _slicedToArray8(_ref2, 2), min3 = _ref22[0], max4 = _ref22[1];
    var validMin = min3, validMax = max4;
    if (min3 > max4) {
      validMin = max4;
      validMax = min3;
    }
    return [validMin, validMax];
  };
  var getAdaptiveStep = (roughStep, allowDecimals, correctionFactor) => {
    if (roughStep.lte(0)) {
      return new import_decimal2.default(0);
    }
    var digitCount = getDigitCount(roughStep.toNumber());
    var digitCountValue = new import_decimal2.default(10).pow(digitCount);
    var stepRatio = roughStep.div(digitCountValue);
    var stepRatioScale = digitCount !== 1 ? 0.05 : 0.1;
    var amendStepRatio = new import_decimal2.default(Math.ceil(stepRatio.div(stepRatioScale).toNumber())).add(correctionFactor).mul(stepRatioScale);
    var formatStep = amendStepRatio.mul(digitCountValue);
    return allowDecimals ? new import_decimal2.default(formatStep.toNumber()) : new import_decimal2.default(Math.ceil(formatStep.toNumber()));
  };
  var getSnap125Step = (roughStep, allowDecimals, correctionFactor) => {
    var _NICE_STEPS$niceIdx;
    if (roughStep.lte(0)) {
      return new import_decimal2.default(0);
    }
    var NICE_STEPS = [1, 2, 2.5, 5];
    var roughNum = roughStep.toNumber();
    var exponent = Math.floor(new import_decimal2.default(roughNum).abs().log(10).toNumber());
    var magnitude = new import_decimal2.default(10).pow(exponent);
    var normalized = roughStep.div(magnitude).toNumber();
    var niceIdx = NICE_STEPS.findIndex((s) => s >= normalized - 1e-10);
    if (niceIdx === -1) {
      magnitude = magnitude.mul(10);
      niceIdx = 0;
    }
    niceIdx += correctionFactor;
    if (niceIdx >= NICE_STEPS.length) {
      var extraMag = Math.floor(niceIdx / NICE_STEPS.length);
      niceIdx %= NICE_STEPS.length;
      magnitude = magnitude.mul(new import_decimal2.default(10).pow(extraMag));
    }
    var niceStep = (_NICE_STEPS$niceIdx = NICE_STEPS[niceIdx]) !== null && _NICE_STEPS$niceIdx !== void 0 ? _NICE_STEPS$niceIdx : 1;
    var formatStep = new import_decimal2.default(niceStep).mul(magnitude);
    return allowDecimals ? formatStep : new import_decimal2.default(Math.ceil(formatStep.toNumber()));
  };
  var getTickOfSingleValue = (value2, tickCount, allowDecimals) => {
    var step = new import_decimal2.default(1);
    var middle = new import_decimal2.default(value2);
    if (!middle.isint() && allowDecimals) {
      var absVal = Math.abs(value2);
      if (absVal < 1) {
        step = new import_decimal2.default(10).pow(getDigitCount(value2) - 1);
        middle = new import_decimal2.default(Math.floor(middle.div(step).toNumber())).mul(step);
      } else if (absVal > 1) {
        middle = new import_decimal2.default(Math.floor(value2));
      }
    } else if (value2 === 0) {
      middle = new import_decimal2.default(Math.floor((tickCount - 1) / 2));
    } else if (!allowDecimals) {
      middle = new import_decimal2.default(Math.floor(value2));
    }
    var middleIndex = Math.floor((tickCount - 1) / 2);
    var ticks = [];
    for (var i = 0; i < tickCount; i++) {
      ticks.push(middle.add(new import_decimal2.default(i - middleIndex).mul(step)).toNumber());
    }
    return ticks;
  };
  var _calculateStep = function calculateStep(min3, max4, tickCount, allowDecimals) {
    var correctionFactor = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : 0;
    var stepFn = arguments.length > 5 && arguments[5] !== void 0 ? arguments[5] : getAdaptiveStep;
    if (!Number.isFinite((max4 - min3) / (tickCount - 1))) {
      return {
        step: new import_decimal2.default(0),
        tickMin: new import_decimal2.default(0),
        tickMax: new import_decimal2.default(0)
      };
    }
    var step = stepFn(new import_decimal2.default(max4).sub(min3).div(tickCount - 1), allowDecimals, correctionFactor);
    var middle;
    if (min3 <= 0 && max4 >= 0) {
      middle = new import_decimal2.default(0);
    } else {
      middle = new import_decimal2.default(min3).add(max4).div(2);
      middle = middle.sub(new import_decimal2.default(middle).mod(step));
    }
    var belowCount = Math.ceil(middle.sub(min3).div(step).toNumber());
    var upCount = Math.ceil(new import_decimal2.default(max4).sub(middle).div(step).toNumber());
    var scaleCount = belowCount + upCount + 1;
    if (scaleCount > tickCount) {
      return _calculateStep(min3, max4, tickCount, allowDecimals, correctionFactor + 1, stepFn);
    }
    if (scaleCount < tickCount) {
      upCount = max4 > 0 ? upCount + (tickCount - scaleCount) : upCount;
      belowCount = max4 > 0 ? belowCount : belowCount + (tickCount - scaleCount);
    }
    return {
      step,
      tickMin: middle.sub(new import_decimal2.default(belowCount).mul(step)),
      tickMax: middle.add(new import_decimal2.default(upCount).mul(step))
    };
  };
  var getNiceTickValues = function getNiceTickValues2(_ref3) {
    var _ref4 = _slicedToArray8(_ref3, 2), min3 = _ref4[0], max4 = _ref4[1];
    var tickCount = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 6;
    var allowDecimals = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : true;
    var niceTicksMode = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : "auto";
    var count = Math.max(tickCount, 2);
    var _getValidInterval = getValidInterval([min3, max4]), _getValidInterval2 = _slicedToArray8(_getValidInterval, 2), cormin = _getValidInterval2[0], cormax = _getValidInterval2[1];
    if (cormin === -Infinity || cormax === Infinity) {
      var _values = cormax === Infinity ? [cormin, ...Array(tickCount - 1).fill(Infinity)] : [...Array(tickCount - 1).fill(-Infinity), cormax];
      return min3 > max4 ? _values.reverse() : _values;
    }
    if (cormin === cormax) {
      return getTickOfSingleValue(cormin, tickCount, allowDecimals);
    }
    var stepFn = niceTicksMode === "snap125" ? getSnap125Step : getAdaptiveStep;
    var _calculateStep2 = _calculateStep(cormin, cormax, count, allowDecimals, 0, stepFn), step = _calculateStep2.step, tickMin = _calculateStep2.tickMin, tickMax = _calculateStep2.tickMax;
    var values = rangeStep(tickMin, tickMax.add(new import_decimal2.default(0.1).mul(step)), step);
    return min3 > max4 ? values.reverse() : values;
  };
  var getTickValuesFixedDomain = function getTickValuesFixedDomain2(_ref5, tickCount) {
    var _ref6 = _slicedToArray8(_ref5, 2), min3 = _ref6[0], max4 = _ref6[1];
    var allowDecimals = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : true;
    var niceTicksMode = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : "auto";
    var _getValidInterval3 = getValidInterval([min3, max4]), _getValidInterval4 = _slicedToArray8(_getValidInterval3, 2), cormin = _getValidInterval4[0], cormax = _getValidInterval4[1];
    if (cormin === -Infinity || cormax === Infinity) {
      return [min3, max4];
    }
    if (cormin === cormax) {
      return [cormin];
    }
    var stepFn = niceTicksMode === "snap125" ? getSnap125Step : getAdaptiveStep;
    var count = Math.max(tickCount, 2);
    var step = stepFn(new import_decimal2.default(cormax).sub(cormin).div(count - 1), allowDecimals, 0);
    var values = [...rangeStep(new import_decimal2.default(cormin), new import_decimal2.default(cormax), step), cormax];
    if (allowDecimals === false) {
      values = values.map((value2) => Math.round(value2));
      var last = values.length - 1;
      if (last > 0 && values[last] === values[last - 1]) {
        values = values.slice(0, last);
      }
    }
    return min3 > max4 ? values.reverse() : values;
  };

  // node_modules/recharts/es6/state/selectors/rootPropsSelectors.js
  var selectRootMaxBarSize = (state) => state.rootProps.maxBarSize;
  var selectBarGap = (state) => state.rootProps.barGap;
  var selectBarCategoryGap = (state) => state.rootProps.barCategoryGap;
  var selectRootBarSize = (state) => state.rootProps.barSize;
  var selectStackOffsetType = (state) => state.rootProps.stackOffset;
  var selectReverseStackOrder = (state) => state.rootProps.reverseStackOrder;
  var selectChartName = (state) => state.options.chartName;
  var selectSyncId = (state) => state.rootProps.syncId;
  var selectSyncMethod = (state) => state.rootProps.syncMethod;
  var selectEventEmitter = (state) => state.options.eventEmitter;
  var selectChartBaseValue = (state) => state.rootProps.baseValue;

  // node_modules/recharts/es6/zIndex/DefaultZIndexes.js
  var DefaultZIndexes = {
    /**
     * CartesianGrid and PolarGrid
     */
    grid: -100,
    /**
     * Background of Bar and RadialBar.
     * This is not visible by default but can be enabled by setting background={true} on Bar or RadialBar.
     */
    barBackground: -50,
    /*
     * other chart elements or custom elements without specific zIndex
     * render in here, at zIndex 0
     */
    /**
     * Area, Pie, Radar, and ReferenceArea
     */
    area: 100,
    /**
     * Cursor is embedded inside Tooltip and controlled by it.
     * The Tooltip itself has a separate portal and is not included in the zIndex system;
     * Cursor is the decoration inside the chart area. CursorRectangle is a rectangle box.
     * It renders below bar so that in a stacked bar chart the cursor rectangle does not hide the other bars.
     */
    cursorRectangle: 200,
    /**
     * Bar and RadialBar
     */
    bar: 300,
    /**
     * Line and ReferenceLine, and ErrorBor
     */
    line: 400,
    /**
     * XAxis and YAxis and PolarAngleAxis and PolarRadiusAxis ticks and lines and children
     */
    axis: 500,
    /**
     * Scatter and ReferenceDot,
     * and Dots of Line and Area and Radar if they have dot=true
     */
    scatter: 600,
    /**
     * Hovering over a Bar or RadialBar renders a highlight rectangle
     */
    activeBar: 1e3,
    /**
     * Cursor is embedded inside Tooltip and controlled by it.
     * The Tooltip itself has a separate portal and is not included in the zIndex system;
     * Cursor is the decoration inside the chart area, usually a cross or a box.
     * CursorLine is a line cursor rendered in Line, Area, Scatter, Radar charts.
     * It renders above the Line and Scatter so that it is always visible.
     * It renders below active dot so that the dot is always visible and shows the current point.
     * We're also assuming that the active dot is small enough that it does not fully cover the cursor line.
     *
     * This also applies to the radial cursor in RadialBarChart.
     */
    cursorLine: 1100,
    /**
     * Hovering over a Point in Line, Area, Scatter, Radar renders a highlight dot
     */
    activeDot: 1200,
    /**
     * LabelList and Label, including Axis labels
     */
    label: 2e3
  };

  // node_modules/recharts/es6/polar/defaultPolarAngleAxisProps.js
  var defaultPolarAngleAxisProps = {
    allowDecimals: false,
    allowDuplicatedCategory: true,
    // if I set this to false then Tooltip synchronisation stops working in Radar, wtf
    allowDataOverflow: false,
    angle: 0,
    angleAxisId: 0,
    axisLine: true,
    axisLineType: "polygon",
    cx: 0,
    cy: 0,
    hide: false,
    includeHidden: false,
    label: false,
    niceTicks: "auto",
    orientation: "outer",
    reversed: false,
    scale: "auto",
    tick: true,
    tickLine: true,
    tickSize: 8,
    type: "auto",
    zIndex: DefaultZIndexes.axis
  };

  // node_modules/recharts/es6/polar/defaultPolarRadiusAxisProps.js
  var defaultPolarRadiusAxisProps = {
    allowDataOverflow: false,
    allowDecimals: false,
    allowDuplicatedCategory: true,
    angle: 0,
    axisLine: true,
    includeHidden: false,
    hide: false,
    niceTicks: "auto",
    label: false,
    orientation: "right",
    radiusAxisId: 0,
    reversed: false,
    scale: "auto",
    stroke: "#ccc",
    tick: true,
    tickCount: 5,
    tickLine: true,
    type: "auto",
    zIndex: DefaultZIndexes.axis
  };

  // node_modules/recharts/es6/state/selectors/combiners/combineAxisRangeWithReverse.js
  var combineAxisRangeWithReverse = (axisSettings, axisRange) => {
    if (!axisSettings || !axisRange) {
      return void 0;
    }
    if (axisSettings !== null && axisSettings !== void 0 && axisSettings.reversed) {
      return [axisRange[1], axisRange[0]];
    }
    return axisRange;
  };

  // node_modules/recharts/es6/util/getAxisTypeBasedOnLayout.js
  function getAxisTypeBasedOnLayout(layout, axisType, axisDomainType) {
    if (axisDomainType !== "auto") {
      return axisDomainType;
    }
    if (layout == null) {
      return void 0;
    }
    return isCategoricalAxis(layout, axisType) ? "category" : "number";
  }

  // node_modules/recharts/es6/state/selectors/polarAxisSelectors.js
  function ownKeys10(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread10(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys10(Object(t), true).forEach(function(r3) {
        _defineProperty11(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys10(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty11(e, r2, t) {
    return (r2 = _toPropertyKey11(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey11(t) {
    var i = _toPrimitive11(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive11(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  var implicitAngleAxis = {
    allowDataOverflow: defaultPolarAngleAxisProps.allowDataOverflow,
    allowDecimals: defaultPolarAngleAxisProps.allowDecimals,
    allowDuplicatedCategory: false,
    // defaultPolarAngleAxisProps.allowDuplicatedCategory has it set to true but the actual axis rendering ignores the prop because reasons,
    dataKey: void 0,
    domain: void 0,
    id: defaultPolarAngleAxisProps.angleAxisId,
    includeHidden: false,
    name: void 0,
    reversed: defaultPolarAngleAxisProps.reversed,
    scale: defaultPolarAngleAxisProps.scale,
    tick: defaultPolarAngleAxisProps.tick,
    tickCount: void 0,
    ticks: void 0,
    type: defaultPolarAngleAxisProps.type,
    unit: void 0,
    niceTicks: "auto"
  };
  var implicitRadiusAxis = {
    allowDataOverflow: defaultPolarRadiusAxisProps.allowDataOverflow,
    allowDecimals: defaultPolarRadiusAxisProps.allowDecimals,
    allowDuplicatedCategory: defaultPolarRadiusAxisProps.allowDuplicatedCategory,
    dataKey: void 0,
    domain: void 0,
    id: defaultPolarRadiusAxisProps.radiusAxisId,
    includeHidden: defaultPolarRadiusAxisProps.includeHidden,
    name: void 0,
    reversed: defaultPolarRadiusAxisProps.reversed,
    scale: defaultPolarRadiusAxisProps.scale,
    tick: defaultPolarRadiusAxisProps.tick,
    tickCount: defaultPolarRadiusAxisProps.tickCount,
    ticks: void 0,
    type: defaultPolarRadiusAxisProps.type,
    unit: void 0,
    niceTicks: "auto"
  };
  var selectAngleAxisNoDefaults = (state, angleAxisId) => {
    if (angleAxisId == null) {
      return void 0;
    }
    return state.polarAxis.angleAxis[angleAxisId];
  };
  var selectAngleAxis = createSelector([selectAngleAxisNoDefaults, selectPolarChartLayout], (angleAxisSettings, layout) => {
    var _getAxisTypeBasedOnLa;
    if (angleAxisSettings != null) {
      return angleAxisSettings;
    }
    var evaluatedType = (_getAxisTypeBasedOnLa = getAxisTypeBasedOnLayout(layout, "angleAxis", implicitAngleAxis.type)) !== null && _getAxisTypeBasedOnLa !== void 0 ? _getAxisTypeBasedOnLa : "category";
    return _objectSpread10(_objectSpread10({}, implicitAngleAxis), {}, {
      type: evaluatedType
    });
  });
  var selectRadiusAxisNoDefaults = (state, radiusAxisId) => {
    return state.polarAxis.radiusAxis[radiusAxisId];
  };
  var selectRadiusAxis = createSelector([selectRadiusAxisNoDefaults, selectPolarChartLayout], (radiusAxisSettings, layout) => {
    var _getAxisTypeBasedOnLa2;
    if (radiusAxisSettings != null) {
      return radiusAxisSettings;
    }
    var evaluatedType = (_getAxisTypeBasedOnLa2 = getAxisTypeBasedOnLayout(layout, "radiusAxis", implicitRadiusAxis.type)) !== null && _getAxisTypeBasedOnLa2 !== void 0 ? _getAxisTypeBasedOnLa2 : "category";
    return _objectSpread10(_objectSpread10({}, implicitRadiusAxis), {}, {
      type: evaluatedType
    });
  });
  var selectPolarOptions = (state) => state.polarOptions;
  var selectMaxRadius = createSelector([selectChartWidth, selectChartHeight, selectChartOffsetInternal], getMaxRadius);
  var selectInnerRadius = createSelector([selectPolarOptions, selectMaxRadius], (polarChartOptions, maxRadius) => {
    if (polarChartOptions == null) {
      return void 0;
    }
    return getPercentValue(polarChartOptions.innerRadius, maxRadius, 0);
  });
  var selectOuterRadius = createSelector([selectPolarOptions, selectMaxRadius], (polarChartOptions, maxRadius) => {
    if (polarChartOptions == null) {
      return void 0;
    }
    return getPercentValue(polarChartOptions.outerRadius, maxRadius, maxRadius * 0.8);
  });
  var combineAngleAxisRange = (polarOptions) => {
    if (polarOptions == null) {
      return [0, 0];
    }
    var startAngle = polarOptions.startAngle, endAngle = polarOptions.endAngle;
    return [startAngle, endAngle];
  };
  var selectAngleAxisRange = createSelector([selectPolarOptions], combineAngleAxisRange);
  var selectAngleAxisRangeWithReversed = createSelector([selectAngleAxis, selectAngleAxisRange], combineAxisRangeWithReverse);
  var selectRadiusAxisRange = createSelector([selectMaxRadius, selectInnerRadius, selectOuterRadius], (maxRadius, innerRadius, outerRadius) => {
    if (maxRadius == null || innerRadius == null || outerRadius == null) {
      return void 0;
    }
    return [innerRadius, outerRadius];
  });
  var selectRadiusAxisRangeWithReversed = createSelector([selectRadiusAxis, selectRadiusAxisRange], combineAxisRangeWithReverse);
  var selectPolarViewBox = createSelector([selectChartLayout, selectPolarOptions, selectInnerRadius, selectOuterRadius, selectChartWidth, selectChartHeight], (layout, polarOptions, innerRadius, outerRadius, width, height) => {
    if (layout !== "centric" && layout !== "radial" || polarOptions == null || innerRadius == null || outerRadius == null) {
      return void 0;
    }
    var cx = polarOptions.cx, cy = polarOptions.cy, startAngle = polarOptions.startAngle, endAngle = polarOptions.endAngle;
    return {
      cx: getPercentValue(cx, width, width / 2),
      cy: getPercentValue(cy, height, height / 2),
      innerRadius,
      outerRadius,
      startAngle,
      endAngle,
      clockWise: false
      // this property look useful, why not use it?
    };
  });

  // node_modules/recharts/es6/state/selectors/pickAxisType.js
  var pickAxisType = (_state, axisType) => axisType;

  // node_modules/recharts/es6/state/selectors/pickAxisId.js
  var pickAxisId = (_state, _axisType, axisId) => axisId;

  // node_modules/recharts/es6/util/stacks/getStackSeriesIdentifier.js
  function getStackSeriesIdentifier(graphicalItem) {
    return graphicalItem === null || graphicalItem === void 0 ? void 0 : graphicalItem.id;
  }

  // node_modules/recharts/es6/state/selectors/combiners/combineDisplayedStackedData.js
  function combineDisplayedStackedData(stackedGraphicalItems, _ref2, tooltipAxisSettings) {
    var _ref$chartData = _ref2.chartData, chartData = _ref$chartData === void 0 ? [] : _ref$chartData;
    var allowDuplicatedCategory = tooltipAxisSettings.allowDuplicatedCategory, tooltipDataKey = tooltipAxisSettings.dataKey;
    var knownItemsByDataKey = /* @__PURE__ */ new Map();
    stackedGraphicalItems.forEach((item) => {
      var _item$data;
      var resolvedData = (_item$data = item.data) !== null && _item$data !== void 0 ? _item$data : chartData;
      if (resolvedData == null || resolvedData.length === 0) {
        return;
      }
      var stackIdentifier = getStackSeriesIdentifier(item);
      resolvedData.forEach((entry, index2) => {
        var tooltipValue = tooltipDataKey == null || allowDuplicatedCategory ? index2 : String(getValueByDataKey(entry, tooltipDataKey, null));
        var numericValue = getValueByDataKey(entry, item.dataKey, 0);
        var curr;
        if (knownItemsByDataKey.has(tooltipValue)) {
          curr = knownItemsByDataKey.get(tooltipValue);
        } else {
          curr = {};
        }
        Object.assign(curr, {
          [stackIdentifier]: numericValue
        });
        knownItemsByDataKey.set(tooltipValue, curr);
      });
    });
    return Array.from(knownItemsByDataKey.values());
  }

  // node_modules/recharts/es6/state/types/StackedGraphicalItem.js
  function isStacked(graphicalItem) {
    return "stackId" in graphicalItem && graphicalItem.stackId != null && graphicalItem.dataKey != null;
  }

  // node_modules/recharts/es6/state/selectors/numberDomainEqualityCheck.js
  var numberDomainEqualityCheck = (a2, b) => {
    if (a2 === b) {
      return true;
    }
    if (a2 == null || b == null) {
      return false;
    }
    return a2[0] === b[0] && a2[1] === b[1];
  };

  // node_modules/recharts/es6/state/selectors/arrayEqualityCheck.js
  function emptyArraysAreEqualCheck(a2, b) {
    if (Array.isArray(a2) && Array.isArray(b) && a2.length === 0 && b.length === 0) {
      return true;
    }
    return a2 === b;
  }
  function arrayContentsAreEqualCheck(a2, b) {
    if (a2.length === b.length) {
      for (var i = 0; i < a2.length; i++) {
        if (a2[i] !== b[i]) {
          return false;
        }
      }
      return true;
    }
    return false;
  }

  // node_modules/recharts/es6/state/selectors/selectTooltipAxisType.js
  var selectTooltipAxisType = (state) => {
    var layout = selectChartLayout(state);
    if (layout === "horizontal") {
      return "xAxis";
    }
    if (layout === "vertical") {
      return "yAxis";
    }
    if (layout === "centric") {
      return "angleAxis";
    }
    return "radiusAxis";
  };

  // node_modules/recharts/es6/state/selectors/selectTooltipAxisId.js
  var selectTooltipAxisId = (state) => state.tooltip.settings.axisId;

  // node_modules/recharts/es6/util/scale/RechartsScale.js
  function rechartsScaleFactory(d3Scale) {
    if (d3Scale == null) {
      return void 0;
    }
    var ticksFn = d3Scale.ticks;
    var bandwidthFn = d3Scale.bandwidth;
    var d3Range = d3Scale.range();
    var range3 = [Math.min(...d3Range), Math.max(...d3Range)];
    return {
      domain: () => d3Scale.domain(),
      range: (function(_range) {
        function range4() {
          return _range.apply(this, arguments);
        }
        range4.toString = function() {
          return _range.toString();
        };
        return range4;
      })(() => range3),
      rangeMin: () => range3[0],
      rangeMax: () => range3[1],
      isInRange(value2) {
        var first = range3[0];
        var last = range3[1];
        return first <= last ? value2 >= first && value2 <= last : value2 >= last && value2 <= first;
      },
      bandwidth: bandwidthFn ? () => bandwidthFn.call(d3Scale) : void 0,
      ticks: ticksFn ? (count) => ticksFn.call(d3Scale, count) : void 0,
      map: (input, options) => {
        var baseValue = d3Scale(input);
        if (baseValue == null) {
          return void 0;
        }
        if (d3Scale.bandwidth && options !== null && options !== void 0 && options.position) {
          var bandWidth = d3Scale.bandwidth();
          switch (options.position) {
            case "middle":
              baseValue += bandWidth / 2;
              break;
            case "end":
              baseValue += bandWidth;
              break;
            default:
              break;
          }
        }
        return baseValue;
      }
    };
  }

  // node_modules/recharts/es6/state/selectors/combiners/combineCheckedDomain.js
  var combineCheckedDomain = (realScaleType, axisDomain) => {
    if (axisDomain == null) {
      return void 0;
    }
    switch (realScaleType) {
      case "linear": {
        if (!isWellFormedNumberDomain(axisDomain)) {
          var min3, max4;
          for (var i = 0; i < axisDomain.length; i++) {
            var value2 = axisDomain[i];
            if (!isWellBehavedNumber(value2)) {
              continue;
            }
            if (min3 === void 0 || value2 < min3) {
              min3 = value2;
            }
            if (max4 === void 0 || value2 > max4) {
              max4 = value2;
            }
          }
          if (min3 !== void 0 && max4 !== void 0) {
            return [min3, max4];
          }
          return void 0;
        }
        return axisDomain;
      }
      default:
        return axisDomain;
    }
  };

  // node_modules/victory-vendor/es/d3-scale.js
  var d3_scale_exports = {};
  __export(d3_scale_exports, {
    scaleBand: () => band,
    scaleDiverging: () => diverging,
    scaleDivergingLog: () => divergingLog,
    scaleDivergingPow: () => divergingPow,
    scaleDivergingSqrt: () => divergingSqrt,
    scaleDivergingSymlog: () => divergingSymlog,
    scaleIdentity: () => identity3,
    scaleImplicit: () => implicit,
    scaleLinear: () => linear2,
    scaleLog: () => log2,
    scaleOrdinal: () => ordinal,
    scalePoint: () => point5,
    scalePow: () => pow,
    scaleQuantile: () => quantile2,
    scaleQuantize: () => quantize,
    scaleRadial: () => radial,
    scaleSequential: () => sequential,
    scaleSequentialLog: () => sequentialLog,
    scaleSequentialPow: () => sequentialPow,
    scaleSequentialQuantile: () => sequentialQuantile,
    scaleSequentialSqrt: () => sequentialSqrt,
    scaleSequentialSymlog: () => sequentialSymlog,
    scaleSqrt: () => sqrt2,
    scaleSymlog: () => symlog,
    scaleThreshold: () => threshold,
    scaleTime: () => time,
    scaleUtc: () => utcTime,
    tickFormat: () => tickFormat
  });

  // node_modules/d3-array/src/ascending.js
  function ascending_default(a2, b) {
    return a2 < b ? -1 : a2 > b ? 1 : a2 >= b ? 0 : NaN;
  }

  // node_modules/d3-array/src/bisector.js
  function bisector_default(f) {
    let delta = f;
    let compare = f;
    if (f.length === 1) {
      delta = (d, x5) => f(d) - x5;
      compare = ascendingComparator(f);
    }
    function left2(a2, x5, lo, hi) {
      if (lo == null) lo = 0;
      if (hi == null) hi = a2.length;
      while (lo < hi) {
        const mid = lo + hi >>> 1;
        if (compare(a2[mid], x5) < 0) lo = mid + 1;
        else hi = mid;
      }
      return lo;
    }
    function right2(a2, x5, lo, hi) {
      if (lo == null) lo = 0;
      if (hi == null) hi = a2.length;
      while (lo < hi) {
        const mid = lo + hi >>> 1;
        if (compare(a2[mid], x5) > 0) hi = mid;
        else lo = mid + 1;
      }
      return lo;
    }
    function center2(a2, x5, lo, hi) {
      if (lo == null) lo = 0;
      if (hi == null) hi = a2.length;
      const i = left2(a2, x5, lo, hi - 1);
      return i > lo && delta(a2[i - 1], x5) > -delta(a2[i], x5) ? i - 1 : i;
    }
    return { left: left2, center: center2, right: right2 };
  }
  function ascendingComparator(f) {
    return (d, x5) => ascending_default(f(d), x5);
  }

  // node_modules/d3-array/src/number.js
  function number_default(x5) {
    return x5 === null ? NaN : +x5;
  }
  function* numbers(values, valueof) {
    if (valueof === void 0) {
      for (let value2 of values) {
        if (value2 != null && (value2 = +value2) >= value2) {
          yield value2;
        }
      }
    } else {
      let index2 = -1;
      for (let value2 of values) {
        if ((value2 = valueof(value2, ++index2, values)) != null && (value2 = +value2) >= value2) {
          yield value2;
        }
      }
    }
  }

  // node_modules/d3-array/src/bisect.js
  var ascendingBisect = bisector_default(ascending_default);
  var bisectRight = ascendingBisect.right;
  var bisectLeft = ascendingBisect.left;
  var bisectCenter = bisector_default(number_default).center;
  var bisect_default = bisectRight;

  // node_modules/internmap/src/index.js
  var InternMap = class extends Map {
    constructor(entries, key = keyof) {
      super();
      Object.defineProperties(this, { _intern: { value: /* @__PURE__ */ new Map() }, _key: { value: key } });
      if (entries != null) for (const [key2, value2] of entries) this.set(key2, value2);
    }
    get(key) {
      return super.get(intern_get(this, key));
    }
    has(key) {
      return super.has(intern_get(this, key));
    }
    set(key, value2) {
      return super.set(intern_set(this, key), value2);
    }
    delete(key) {
      return super.delete(intern_delete(this, key));
    }
  };
  function intern_get({ _intern, _key }, value2) {
    const key = _key(value2);
    return _intern.has(key) ? _intern.get(key) : value2;
  }
  function intern_set({ _intern, _key }, value2) {
    const key = _key(value2);
    if (_intern.has(key)) return _intern.get(key);
    _intern.set(key, value2);
    return value2;
  }
  function intern_delete({ _intern, _key }, value2) {
    const key = _key(value2);
    if (_intern.has(key)) {
      value2 = _intern.get(value2);
      _intern.delete(key);
    }
    return value2;
  }
  function keyof(value2) {
    return value2 !== null && typeof value2 === "object" ? value2.valueOf() : value2;
  }

  // node_modules/d3-array/src/ticks.js
  var e10 = Math.sqrt(50);
  var e5 = Math.sqrt(10);
  var e2 = Math.sqrt(2);
  function ticks_default(start, stop, count) {
    var reverse, i = -1, n, ticks, step;
    stop = +stop, start = +start, count = +count;
    if (start === stop && count > 0) return [start];
    if (reverse = stop < start) n = start, start = stop, stop = n;
    if ((step = tickIncrement(start, stop, count)) === 0 || !isFinite(step)) return [];
    if (step > 0) {
      let r0 = Math.round(start / step), r1 = Math.round(stop / step);
      if (r0 * step < start) ++r0;
      if (r1 * step > stop) --r1;
      ticks = new Array(n = r1 - r0 + 1);
      while (++i < n) ticks[i] = (r0 + i) * step;
    } else {
      step = -step;
      let r0 = Math.round(start * step), r1 = Math.round(stop * step);
      if (r0 / step < start) ++r0;
      if (r1 / step > stop) --r1;
      ticks = new Array(n = r1 - r0 + 1);
      while (++i < n) ticks[i] = (r0 + i) / step;
    }
    if (reverse) ticks.reverse();
    return ticks;
  }
  function tickIncrement(start, stop, count) {
    var step = (stop - start) / Math.max(0, count), power = Math.floor(Math.log(step) / Math.LN10), error = step / Math.pow(10, power);
    return power >= 0 ? (error >= e10 ? 10 : error >= e5 ? 5 : error >= e2 ? 2 : 1) * Math.pow(10, power) : -Math.pow(10, -power) / (error >= e10 ? 10 : error >= e5 ? 5 : error >= e2 ? 2 : 1);
  }
  function tickStep(start, stop, count) {
    var step0 = Math.abs(stop - start) / Math.max(0, count), step1 = Math.pow(10, Math.floor(Math.log(step0) / Math.LN10)), error = step0 / step1;
    if (error >= e10) step1 *= 10;
    else if (error >= e5) step1 *= 5;
    else if (error >= e2) step1 *= 2;
    return stop < start ? -step1 : step1;
  }

  // node_modules/d3-array/src/max.js
  function max2(values, valueof) {
    let max4;
    if (valueof === void 0) {
      for (const value2 of values) {
        if (value2 != null && (max4 < value2 || max4 === void 0 && value2 >= value2)) {
          max4 = value2;
        }
      }
    } else {
      let index2 = -1;
      for (let value2 of values) {
        if ((value2 = valueof(value2, ++index2, values)) != null && (max4 < value2 || max4 === void 0 && value2 >= value2)) {
          max4 = value2;
        }
      }
    }
    return max4;
  }

  // node_modules/d3-array/src/min.js
  function min2(values, valueof) {
    let min3;
    if (valueof === void 0) {
      for (const value2 of values) {
        if (value2 != null && (min3 > value2 || min3 === void 0 && value2 >= value2)) {
          min3 = value2;
        }
      }
    } else {
      let index2 = -1;
      for (let value2 of values) {
        if ((value2 = valueof(value2, ++index2, values)) != null && (min3 > value2 || min3 === void 0 && value2 >= value2)) {
          min3 = value2;
        }
      }
    }
    return min3;
  }

  // node_modules/d3-array/src/quickselect.js
  function quickselect(array, k, left2 = 0, right2 = array.length - 1, compare = ascending_default) {
    while (right2 > left2) {
      if (right2 - left2 > 600) {
        const n = right2 - left2 + 1;
        const m2 = k - left2 + 1;
        const z = Math.log(n);
        const s = 0.5 * Math.exp(2 * z / 3);
        const sd = 0.5 * Math.sqrt(z * s * (n - s) / n) * (m2 - n / 2 < 0 ? -1 : 1);
        const newLeft = Math.max(left2, Math.floor(k - m2 * s / n + sd));
        const newRight = Math.min(right2, Math.floor(k + (n - m2) * s / n + sd));
        quickselect(array, k, newLeft, newRight, compare);
      }
      const t = array[k];
      let i = left2;
      let j = right2;
      swap(array, left2, k);
      if (compare(array[right2], t) > 0) swap(array, left2, right2);
      while (i < j) {
        swap(array, i, j), ++i, --j;
        while (compare(array[i], t) < 0) ++i;
        while (compare(array[j], t) > 0) --j;
      }
      if (compare(array[left2], t) === 0) swap(array, left2, j);
      else ++j, swap(array, j, right2);
      if (j <= k) left2 = j + 1;
      if (k <= j) right2 = j - 1;
    }
    return array;
  }
  function swap(array, i, j) {
    const t = array[i];
    array[i] = array[j];
    array[j] = t;
  }

  // node_modules/d3-array/src/quantile.js
  function quantile(values, p, valueof) {
    values = Float64Array.from(numbers(values, valueof));
    if (!(n = values.length)) return;
    if ((p = +p) <= 0 || n < 2) return min2(values);
    if (p >= 1) return max2(values);
    var n, i = (n - 1) * p, i0 = Math.floor(i), value0 = max2(quickselect(values, i0).subarray(0, i0 + 1)), value1 = min2(values.subarray(i0 + 1));
    return value0 + (value1 - value0) * (i - i0);
  }
  function quantileSorted(values, p, valueof = number_default) {
    if (!(n = values.length)) return;
    if ((p = +p) <= 0 || n < 2) return +valueof(values[0], 0, values);
    if (p >= 1) return +valueof(values[n - 1], n - 1, values);
    var n, i = (n - 1) * p, i0 = Math.floor(i), value0 = +valueof(values[i0], i0, values), value1 = +valueof(values[i0 + 1], i0 + 1, values);
    return value0 + (value1 - value0) * (i - i0);
  }

  // node_modules/d3-array/src/range.js
  function range_default(start, stop, step) {
    start = +start, stop = +stop, step = (n = arguments.length) < 2 ? (stop = start, start = 0, 1) : n < 3 ? 1 : +step;
    var i = -1, n = Math.max(0, Math.ceil((stop - start) / step)) | 0, range3 = new Array(n);
    while (++i < n) {
      range3[i] = start + i * step;
    }
    return range3;
  }

  // node_modules/d3-array/src/sum.js
  function sum(values, valueof) {
    let sum3 = 0;
    if (valueof === void 0) {
      for (let value2 of values) {
        if (value2 = +value2) {
          sum3 += value2;
        }
      }
    } else {
      let index2 = -1;
      for (let value2 of values) {
        if (value2 = +valueof(value2, ++index2, values)) {
          sum3 += value2;
        }
      }
    }
    return sum3;
  }

  // node_modules/d3-scale/src/init.js
  function initRange(domain, range3) {
    switch (arguments.length) {
      case 0:
        break;
      case 1:
        this.range(domain);
        break;
      default:
        this.range(range3).domain(domain);
        break;
    }
    return this;
  }
  function initInterpolator(domain, interpolator) {
    switch (arguments.length) {
      case 0:
        break;
      case 1: {
        if (typeof domain === "function") this.interpolator(domain);
        else this.range(domain);
        break;
      }
      default: {
        this.domain(domain);
        if (typeof interpolator === "function") this.interpolator(interpolator);
        else this.range(interpolator);
        break;
      }
    }
    return this;
  }

  // node_modules/d3-scale/src/ordinal.js
  var implicit = Symbol("implicit");
  function ordinal() {
    var index2 = new InternMap(), domain = [], range3 = [], unknown = implicit;
    function scale(d) {
      let i = index2.get(d);
      if (i === void 0) {
        if (unknown !== implicit) return unknown;
        index2.set(d, i = domain.push(d) - 1);
      }
      return range3[i % range3.length];
    }
    scale.domain = function(_) {
      if (!arguments.length) return domain.slice();
      domain = [], index2 = new InternMap();
      for (const value2 of _) {
        if (index2.has(value2)) continue;
        index2.set(value2, domain.push(value2) - 1);
      }
      return scale;
    };
    scale.range = function(_) {
      return arguments.length ? (range3 = Array.from(_), scale) : range3.slice();
    };
    scale.unknown = function(_) {
      return arguments.length ? (unknown = _, scale) : unknown;
    };
    scale.copy = function() {
      return ordinal(domain, range3).unknown(unknown);
    };
    initRange.apply(scale, arguments);
    return scale;
  }

  // node_modules/d3-scale/src/band.js
  function band() {
    var scale = ordinal().unknown(void 0), domain = scale.domain, ordinalRange = scale.range, r0 = 0, r1 = 1, step, bandwidth, round2 = false, paddingInner = 0, paddingOuter = 0, align = 0.5;
    delete scale.unknown;
    function rescale() {
      var n = domain().length, reverse = r1 < r0, start = reverse ? r1 : r0, stop = reverse ? r0 : r1;
      step = (stop - start) / Math.max(1, n - paddingInner + paddingOuter * 2);
      if (round2) step = Math.floor(step);
      start += (stop - start - step * (n - paddingInner)) * align;
      bandwidth = step * (1 - paddingInner);
      if (round2) start = Math.round(start), bandwidth = Math.round(bandwidth);
      var values = range_default(n).map(function(i) {
        return start + step * i;
      });
      return ordinalRange(reverse ? values.reverse() : values);
    }
    scale.domain = function(_) {
      return arguments.length ? (domain(_), rescale()) : domain();
    };
    scale.range = function(_) {
      return arguments.length ? ([r0, r1] = _, r0 = +r0, r1 = +r1, rescale()) : [r0, r1];
    };
    scale.rangeRound = function(_) {
      return [r0, r1] = _, r0 = +r0, r1 = +r1, round2 = true, rescale();
    };
    scale.bandwidth = function() {
      return bandwidth;
    };
    scale.step = function() {
      return step;
    };
    scale.round = function(_) {
      return arguments.length ? (round2 = !!_, rescale()) : round2;
    };
    scale.padding = function(_) {
      return arguments.length ? (paddingInner = Math.min(1, paddingOuter = +_), rescale()) : paddingInner;
    };
    scale.paddingInner = function(_) {
      return arguments.length ? (paddingInner = Math.min(1, _), rescale()) : paddingInner;
    };
    scale.paddingOuter = function(_) {
      return arguments.length ? (paddingOuter = +_, rescale()) : paddingOuter;
    };
    scale.align = function(_) {
      return arguments.length ? (align = Math.max(0, Math.min(1, _)), rescale()) : align;
    };
    scale.copy = function() {
      return band(domain(), [r0, r1]).round(round2).paddingInner(paddingInner).paddingOuter(paddingOuter).align(align);
    };
    return initRange.apply(rescale(), arguments);
  }
  function pointish(scale) {
    var copy3 = scale.copy;
    scale.padding = scale.paddingOuter;
    delete scale.paddingInner;
    delete scale.paddingOuter;
    scale.copy = function() {
      return pointish(copy3());
    };
    return scale;
  }
  function point5() {
    return pointish(band.apply(null, arguments).paddingInner(1));
  }

  // node_modules/d3-color/src/define.js
  function define_default(constructor, factory, prototype) {
    constructor.prototype = factory.prototype = prototype;
    prototype.constructor = constructor;
  }
  function extend(parent, definition) {
    var prototype = Object.create(parent.prototype);
    for (var key in definition) prototype[key] = definition[key];
    return prototype;
  }

  // node_modules/d3-color/src/color.js
  function Color() {
  }
  var darker = 0.7;
  var brighter = 1 / darker;
  var reI = "\\s*([+-]?\\d+)\\s*";
  var reN = "\\s*([+-]?(?:\\d*\\.)?\\d+(?:[eE][+-]?\\d+)?)\\s*";
  var reP = "\\s*([+-]?(?:\\d*\\.)?\\d+(?:[eE][+-]?\\d+)?)%\\s*";
  var reHex = /^#([0-9a-f]{3,8})$/;
  var reRgbInteger = new RegExp(`^rgb\\(${reI},${reI},${reI}\\)$`);
  var reRgbPercent = new RegExp(`^rgb\\(${reP},${reP},${reP}\\)$`);
  var reRgbaInteger = new RegExp(`^rgba\\(${reI},${reI},${reI},${reN}\\)$`);
  var reRgbaPercent = new RegExp(`^rgba\\(${reP},${reP},${reP},${reN}\\)$`);
  var reHslPercent = new RegExp(`^hsl\\(${reN},${reP},${reP}\\)$`);
  var reHslaPercent = new RegExp(`^hsla\\(${reN},${reP},${reP},${reN}\\)$`);
  var named = {
    aliceblue: 15792383,
    antiquewhite: 16444375,
    aqua: 65535,
    aquamarine: 8388564,
    azure: 15794175,
    beige: 16119260,
    bisque: 16770244,
    black: 0,
    blanchedalmond: 16772045,
    blue: 255,
    blueviolet: 9055202,
    brown: 10824234,
    burlywood: 14596231,
    cadetblue: 6266528,
    chartreuse: 8388352,
    chocolate: 13789470,
    coral: 16744272,
    cornflowerblue: 6591981,
    cornsilk: 16775388,
    crimson: 14423100,
    cyan: 65535,
    darkblue: 139,
    darkcyan: 35723,
    darkgoldenrod: 12092939,
    darkgray: 11119017,
    darkgreen: 25600,
    darkgrey: 11119017,
    darkkhaki: 12433259,
    darkmagenta: 9109643,
    darkolivegreen: 5597999,
    darkorange: 16747520,
    darkorchid: 10040012,
    darkred: 9109504,
    darksalmon: 15308410,
    darkseagreen: 9419919,
    darkslateblue: 4734347,
    darkslategray: 3100495,
    darkslategrey: 3100495,
    darkturquoise: 52945,
    darkviolet: 9699539,
    deeppink: 16716947,
    deepskyblue: 49151,
    dimgray: 6908265,
    dimgrey: 6908265,
    dodgerblue: 2003199,
    firebrick: 11674146,
    floralwhite: 16775920,
    forestgreen: 2263842,
    fuchsia: 16711935,
    gainsboro: 14474460,
    ghostwhite: 16316671,
    gold: 16766720,
    goldenrod: 14329120,
    gray: 8421504,
    green: 32768,
    greenyellow: 11403055,
    grey: 8421504,
    honeydew: 15794160,
    hotpink: 16738740,
    indianred: 13458524,
    indigo: 4915330,
    ivory: 16777200,
    khaki: 15787660,
    lavender: 15132410,
    lavenderblush: 16773365,
    lawngreen: 8190976,
    lemonchiffon: 16775885,
    lightblue: 11393254,
    lightcoral: 15761536,
    lightcyan: 14745599,
    lightgoldenrodyellow: 16448210,
    lightgray: 13882323,
    lightgreen: 9498256,
    lightgrey: 13882323,
    lightpink: 16758465,
    lightsalmon: 16752762,
    lightseagreen: 2142890,
    lightskyblue: 8900346,
    lightslategray: 7833753,
    lightslategrey: 7833753,
    lightsteelblue: 11584734,
    lightyellow: 16777184,
    lime: 65280,
    limegreen: 3329330,
    linen: 16445670,
    magenta: 16711935,
    maroon: 8388608,
    mediumaquamarine: 6737322,
    mediumblue: 205,
    mediumorchid: 12211667,
    mediumpurple: 9662683,
    mediumseagreen: 3978097,
    mediumslateblue: 8087790,
    mediumspringgreen: 64154,
    mediumturquoise: 4772300,
    mediumvioletred: 13047173,
    midnightblue: 1644912,
    mintcream: 16121850,
    mistyrose: 16770273,
    moccasin: 16770229,
    navajowhite: 16768685,
    navy: 128,
    oldlace: 16643558,
    olive: 8421376,
    olivedrab: 7048739,
    orange: 16753920,
    orangered: 16729344,
    orchid: 14315734,
    palegoldenrod: 15657130,
    palegreen: 10025880,
    paleturquoise: 11529966,
    palevioletred: 14381203,
    papayawhip: 16773077,
    peachpuff: 16767673,
    peru: 13468991,
    pink: 16761035,
    plum: 14524637,
    powderblue: 11591910,
    purple: 8388736,
    rebeccapurple: 6697881,
    red: 16711680,
    rosybrown: 12357519,
    royalblue: 4286945,
    saddlebrown: 9127187,
    salmon: 16416882,
    sandybrown: 16032864,
    seagreen: 3050327,
    seashell: 16774638,
    sienna: 10506797,
    silver: 12632256,
    skyblue: 8900331,
    slateblue: 6970061,
    slategray: 7372944,
    slategrey: 7372944,
    snow: 16775930,
    springgreen: 65407,
    steelblue: 4620980,
    tan: 13808780,
    teal: 32896,
    thistle: 14204888,
    tomato: 16737095,
    turquoise: 4251856,
    violet: 15631086,
    wheat: 16113331,
    white: 16777215,
    whitesmoke: 16119285,
    yellow: 16776960,
    yellowgreen: 10145074
  };
  define_default(Color, color, {
    copy(channels) {
      return Object.assign(new this.constructor(), this, channels);
    },
    displayable() {
      return this.rgb().displayable();
    },
    hex: color_formatHex,
    // Deprecated! Use color.formatHex.
    formatHex: color_formatHex,
    formatHex8: color_formatHex8,
    formatHsl: color_formatHsl,
    formatRgb: color_formatRgb,
    toString: color_formatRgb
  });
  function color_formatHex() {
    return this.rgb().formatHex();
  }
  function color_formatHex8() {
    return this.rgb().formatHex8();
  }
  function color_formatHsl() {
    return hslConvert(this).formatHsl();
  }
  function color_formatRgb() {
    return this.rgb().formatRgb();
  }
  function color(format2) {
    var m2, l;
    format2 = (format2 + "").trim().toLowerCase();
    return (m2 = reHex.exec(format2)) ? (l = m2[1].length, m2 = parseInt(m2[1], 16), l === 6 ? rgbn(m2) : l === 3 ? new Rgb(m2 >> 8 & 15 | m2 >> 4 & 240, m2 >> 4 & 15 | m2 & 240, (m2 & 15) << 4 | m2 & 15, 1) : l === 8 ? rgba(m2 >> 24 & 255, m2 >> 16 & 255, m2 >> 8 & 255, (m2 & 255) / 255) : l === 4 ? rgba(m2 >> 12 & 15 | m2 >> 8 & 240, m2 >> 8 & 15 | m2 >> 4 & 240, m2 >> 4 & 15 | m2 & 240, ((m2 & 15) << 4 | m2 & 15) / 255) : null) : (m2 = reRgbInteger.exec(format2)) ? new Rgb(m2[1], m2[2], m2[3], 1) : (m2 = reRgbPercent.exec(format2)) ? new Rgb(m2[1] * 255 / 100, m2[2] * 255 / 100, m2[3] * 255 / 100, 1) : (m2 = reRgbaInteger.exec(format2)) ? rgba(m2[1], m2[2], m2[3], m2[4]) : (m2 = reRgbaPercent.exec(format2)) ? rgba(m2[1] * 255 / 100, m2[2] * 255 / 100, m2[3] * 255 / 100, m2[4]) : (m2 = reHslPercent.exec(format2)) ? hsla(m2[1], m2[2] / 100, m2[3] / 100, 1) : (m2 = reHslaPercent.exec(format2)) ? hsla(m2[1], m2[2] / 100, m2[3] / 100, m2[4]) : named.hasOwnProperty(format2) ? rgbn(named[format2]) : format2 === "transparent" ? new Rgb(NaN, NaN, NaN, 0) : null;
  }
  function rgbn(n) {
    return new Rgb(n >> 16 & 255, n >> 8 & 255, n & 255, 1);
  }
  function rgba(r2, g, b, a2) {
    if (a2 <= 0) r2 = g = b = NaN;
    return new Rgb(r2, g, b, a2);
  }
  function rgbConvert(o) {
    if (!(o instanceof Color)) o = color(o);
    if (!o) return new Rgb();
    o = o.rgb();
    return new Rgb(o.r, o.g, o.b, o.opacity);
  }
  function rgb(r2, g, b, opacity) {
    return arguments.length === 1 ? rgbConvert(r2) : new Rgb(r2, g, b, opacity == null ? 1 : opacity);
  }
  function Rgb(r2, g, b, opacity) {
    this.r = +r2;
    this.g = +g;
    this.b = +b;
    this.opacity = +opacity;
  }
  define_default(Rgb, rgb, extend(Color, {
    brighter(k) {
      k = k == null ? brighter : Math.pow(brighter, k);
      return new Rgb(this.r * k, this.g * k, this.b * k, this.opacity);
    },
    darker(k) {
      k = k == null ? darker : Math.pow(darker, k);
      return new Rgb(this.r * k, this.g * k, this.b * k, this.opacity);
    },
    rgb() {
      return this;
    },
    clamp() {
      return new Rgb(clampi(this.r), clampi(this.g), clampi(this.b), clampa(this.opacity));
    },
    displayable() {
      return -0.5 <= this.r && this.r < 255.5 && (-0.5 <= this.g && this.g < 255.5) && (-0.5 <= this.b && this.b < 255.5) && (0 <= this.opacity && this.opacity <= 1);
    },
    hex: rgb_formatHex,
    // Deprecated! Use color.formatHex.
    formatHex: rgb_formatHex,
    formatHex8: rgb_formatHex8,
    formatRgb: rgb_formatRgb,
    toString: rgb_formatRgb
  }));
  function rgb_formatHex() {
    return `#${hex(this.r)}${hex(this.g)}${hex(this.b)}`;
  }
  function rgb_formatHex8() {
    return `#${hex(this.r)}${hex(this.g)}${hex(this.b)}${hex((isNaN(this.opacity) ? 1 : this.opacity) * 255)}`;
  }
  function rgb_formatRgb() {
    const a2 = clampa(this.opacity);
    return `${a2 === 1 ? "rgb(" : "rgba("}${clampi(this.r)}, ${clampi(this.g)}, ${clampi(this.b)}${a2 === 1 ? ")" : `, ${a2})`}`;
  }
  function clampa(opacity) {
    return isNaN(opacity) ? 1 : Math.max(0, Math.min(1, opacity));
  }
  function clampi(value2) {
    return Math.max(0, Math.min(255, Math.round(value2) || 0));
  }
  function hex(value2) {
    value2 = clampi(value2);
    return (value2 < 16 ? "0" : "") + value2.toString(16);
  }
  function hsla(h, s, l, a2) {
    if (a2 <= 0) h = s = l = NaN;
    else if (l <= 0 || l >= 1) h = s = NaN;
    else if (s <= 0) h = NaN;
    return new Hsl(h, s, l, a2);
  }
  function hslConvert(o) {
    if (o instanceof Hsl) return new Hsl(o.h, o.s, o.l, o.opacity);
    if (!(o instanceof Color)) o = color(o);
    if (!o) return new Hsl();
    if (o instanceof Hsl) return o;
    o = o.rgb();
    var r2 = o.r / 255, g = o.g / 255, b = o.b / 255, min3 = Math.min(r2, g, b), max4 = Math.max(r2, g, b), h = NaN, s = max4 - min3, l = (max4 + min3) / 2;
    if (s) {
      if (r2 === max4) h = (g - b) / s + (g < b) * 6;
      else if (g === max4) h = (b - r2) / s + 2;
      else h = (r2 - g) / s + 4;
      s /= l < 0.5 ? max4 + min3 : 2 - max4 - min3;
      h *= 60;
    } else {
      s = l > 0 && l < 1 ? 0 : h;
    }
    return new Hsl(h, s, l, o.opacity);
  }
  function hsl(h, s, l, opacity) {
    return arguments.length === 1 ? hslConvert(h) : new Hsl(h, s, l, opacity == null ? 1 : opacity);
  }
  function Hsl(h, s, l, opacity) {
    this.h = +h;
    this.s = +s;
    this.l = +l;
    this.opacity = +opacity;
  }
  define_default(Hsl, hsl, extend(Color, {
    brighter(k) {
      k = k == null ? brighter : Math.pow(brighter, k);
      return new Hsl(this.h, this.s, this.l * k, this.opacity);
    },
    darker(k) {
      k = k == null ? darker : Math.pow(darker, k);
      return new Hsl(this.h, this.s, this.l * k, this.opacity);
    },
    rgb() {
      var h = this.h % 360 + (this.h < 0) * 360, s = isNaN(h) || isNaN(this.s) ? 0 : this.s, l = this.l, m2 = l + (l < 0.5 ? l : 1 - l) * s, m1 = 2 * l - m2;
      return new Rgb(
        hsl2rgb(h >= 240 ? h - 240 : h + 120, m1, m2),
        hsl2rgb(h, m1, m2),
        hsl2rgb(h < 120 ? h + 240 : h - 120, m1, m2),
        this.opacity
      );
    },
    clamp() {
      return new Hsl(clamph(this.h), clampt(this.s), clampt(this.l), clampa(this.opacity));
    },
    displayable() {
      return (0 <= this.s && this.s <= 1 || isNaN(this.s)) && (0 <= this.l && this.l <= 1) && (0 <= this.opacity && this.opacity <= 1);
    },
    formatHsl() {
      const a2 = clampa(this.opacity);
      return `${a2 === 1 ? "hsl(" : "hsla("}${clamph(this.h)}, ${clampt(this.s) * 100}%, ${clampt(this.l) * 100}%${a2 === 1 ? ")" : `, ${a2})`}`;
    }
  }));
  function clamph(value2) {
    value2 = (value2 || 0) % 360;
    return value2 < 0 ? value2 + 360 : value2;
  }
  function clampt(value2) {
    return Math.max(0, Math.min(1, value2 || 0));
  }
  function hsl2rgb(h, m1, m2) {
    return (h < 60 ? m1 + (m2 - m1) * h / 60 : h < 180 ? m2 : h < 240 ? m1 + (m2 - m1) * (240 - h) / 60 : m1) * 255;
  }

  // node_modules/d3-interpolate/src/basis.js
  function basis(t12, v0, v1, v2, v3) {
    var t2 = t12 * t12, t3 = t2 * t12;
    return ((1 - 3 * t12 + 3 * t2 - t3) * v0 + (4 - 6 * t2 + 3 * t3) * v1 + (1 + 3 * t12 + 3 * t2 - 3 * t3) * v2 + t3 * v3) / 6;
  }
  function basis_default2(values) {
    var n = values.length - 1;
    return function(t) {
      var i = t <= 0 ? t = 0 : t >= 1 ? (t = 1, n - 1) : Math.floor(t * n), v1 = values[i], v2 = values[i + 1], v0 = i > 0 ? values[i - 1] : 2 * v1 - v2, v3 = i < n - 1 ? values[i + 2] : 2 * v2 - v1;
      return basis((t - i / n) * n, v0, v1, v2, v3);
    };
  }

  // node_modules/d3-interpolate/src/basisClosed.js
  function basisClosed_default2(values) {
    var n = values.length;
    return function(t) {
      var i = Math.floor(((t %= 1) < 0 ? ++t : t) * n), v0 = values[(i + n - 1) % n], v1 = values[i % n], v2 = values[(i + 1) % n], v3 = values[(i + 2) % n];
      return basis((t - i / n) * n, v0, v1, v2, v3);
    };
  }

  // node_modules/d3-interpolate/src/constant.js
  var constant_default2 = (x5) => () => x5;

  // node_modules/d3-interpolate/src/color.js
  function linear(a2, d) {
    return function(t) {
      return a2 + t * d;
    };
  }
  function exponential(a2, b, y5) {
    return a2 = Math.pow(a2, y5), b = Math.pow(b, y5) - a2, y5 = 1 / y5, function(t) {
      return Math.pow(a2 + t * b, y5);
    };
  }
  function gamma(y5) {
    return (y5 = +y5) === 1 ? nogamma : function(a2, b) {
      return b - a2 ? exponential(a2, b, y5) : constant_default2(isNaN(a2) ? b : a2);
    };
  }
  function nogamma(a2, b) {
    var d = b - a2;
    return d ? linear(a2, d) : constant_default2(isNaN(a2) ? b : a2);
  }

  // node_modules/d3-interpolate/src/rgb.js
  var rgb_default = (function rgbGamma(y5) {
    var color2 = gamma(y5);
    function rgb2(start, end) {
      var r2 = color2((start = rgb(start)).r, (end = rgb(end)).r), g = color2(start.g, end.g), b = color2(start.b, end.b), opacity = nogamma(start.opacity, end.opacity);
      return function(t) {
        start.r = r2(t);
        start.g = g(t);
        start.b = b(t);
        start.opacity = opacity(t);
        return start + "";
      };
    }
    rgb2.gamma = rgbGamma;
    return rgb2;
  })(1);
  function rgbSpline(spline) {
    return function(colors) {
      var n = colors.length, r2 = new Array(n), g = new Array(n), b = new Array(n), i, color2;
      for (i = 0; i < n; ++i) {
        color2 = rgb(colors[i]);
        r2[i] = color2.r || 0;
        g[i] = color2.g || 0;
        b[i] = color2.b || 0;
      }
      r2 = spline(r2);
      g = spline(g);
      b = spline(b);
      color2.opacity = 1;
      return function(t) {
        color2.r = r2(t);
        color2.g = g(t);
        color2.b = b(t);
        return color2 + "";
      };
    };
  }
  var rgbBasis = rgbSpline(basis_default2);
  var rgbBasisClosed = rgbSpline(basisClosed_default2);

  // node_modules/d3-interpolate/src/numberArray.js
  function numberArray_default(a2, b) {
    if (!b) b = [];
    var n = a2 ? Math.min(b.length, a2.length) : 0, c2 = b.slice(), i;
    return function(t) {
      for (i = 0; i < n; ++i) c2[i] = a2[i] * (1 - t) + b[i] * t;
      return c2;
    };
  }
  function isNumberArray(x5) {
    return ArrayBuffer.isView(x5) && !(x5 instanceof DataView);
  }

  // node_modules/d3-interpolate/src/array.js
  function genericArray(a2, b) {
    var nb = b ? b.length : 0, na = a2 ? Math.min(nb, a2.length) : 0, x5 = new Array(na), c2 = new Array(nb), i;
    for (i = 0; i < na; ++i) x5[i] = value_default(a2[i], b[i]);
    for (; i < nb; ++i) c2[i] = b[i];
    return function(t) {
      for (i = 0; i < na; ++i) c2[i] = x5[i](t);
      return c2;
    };
  }

  // node_modules/d3-interpolate/src/date.js
  function date_default(a2, b) {
    var d = /* @__PURE__ */ new Date();
    return a2 = +a2, b = +b, function(t) {
      return d.setTime(a2 * (1 - t) + b * t), d;
    };
  }

  // node_modules/d3-interpolate/src/number.js
  function number_default2(a2, b) {
    return a2 = +a2, b = +b, function(t) {
      return a2 * (1 - t) + b * t;
    };
  }

  // node_modules/d3-interpolate/src/object.js
  function object_default(a2, b) {
    var i = {}, c2 = {}, k;
    if (a2 === null || typeof a2 !== "object") a2 = {};
    if (b === null || typeof b !== "object") b = {};
    for (k in b) {
      if (k in a2) {
        i[k] = value_default(a2[k], b[k]);
      } else {
        c2[k] = b[k];
      }
    }
    return function(t) {
      for (k in i) c2[k] = i[k](t);
      return c2;
    };
  }

  // node_modules/d3-interpolate/src/string.js
  var reA = /[-+]?(?:\d+\.?\d*|\.?\d+)(?:[eE][-+]?\d+)?/g;
  var reB = new RegExp(reA.source, "g");
  function zero(b) {
    return function() {
      return b;
    };
  }
  function one(b) {
    return function(t) {
      return b(t) + "";
    };
  }
  function string_default(a2, b) {
    var bi = reA.lastIndex = reB.lastIndex = 0, am, bm, bs, i = -1, s = [], q = [];
    a2 = a2 + "", b = b + "";
    while ((am = reA.exec(a2)) && (bm = reB.exec(b))) {
      if ((bs = bm.index) > bi) {
        bs = b.slice(bi, bs);
        if (s[i]) s[i] += bs;
        else s[++i] = bs;
      }
      if ((am = am[0]) === (bm = bm[0])) {
        if (s[i]) s[i] += bm;
        else s[++i] = bm;
      } else {
        s[++i] = null;
        q.push({ i, x: number_default2(am, bm) });
      }
      bi = reB.lastIndex;
    }
    if (bi < b.length) {
      bs = b.slice(bi);
      if (s[i]) s[i] += bs;
      else s[++i] = bs;
    }
    return s.length < 2 ? q[0] ? one(q[0].x) : zero(b) : (b = q.length, function(t) {
      for (var i2 = 0, o; i2 < b; ++i2) s[(o = q[i2]).i] = o.x(t);
      return s.join("");
    });
  }

  // node_modules/d3-interpolate/src/value.js
  function value_default(a2, b) {
    var t = typeof b, c2;
    return b == null || t === "boolean" ? constant_default2(b) : (t === "number" ? number_default2 : t === "string" ? (c2 = color(b)) ? (b = c2, rgb_default) : string_default : b instanceof color ? rgb_default : b instanceof Date ? date_default : isNumberArray(b) ? numberArray_default : Array.isArray(b) ? genericArray : typeof b.valueOf !== "function" && typeof b.toString !== "function" || isNaN(b) ? object_default : number_default2)(a2, b);
  }

  // node_modules/d3-interpolate/src/round.js
  function round_default(a2, b) {
    return a2 = +a2, b = +b, function(t) {
      return Math.round(a2 * (1 - t) + b * t);
    };
  }

  // node_modules/d3-interpolate/src/piecewise.js
  function piecewise(interpolate2, values) {
    if (values === void 0) values = interpolate2, interpolate2 = value_default;
    var i = 0, n = values.length - 1, v = values[0], I = new Array(n < 0 ? 0 : n);
    while (i < n) I[i] = interpolate2(v, v = values[++i]);
    return function(t) {
      var i2 = Math.max(0, Math.min(n - 1, Math.floor(t *= n)));
      return I[i2](t - i2);
    };
  }

  // node_modules/d3-scale/src/constant.js
  function constants(x5) {
    return function() {
      return x5;
    };
  }

  // node_modules/d3-scale/src/number.js
  function number(x5) {
    return +x5;
  }

  // node_modules/d3-scale/src/continuous.js
  var unit = [0, 1];
  function identity2(x5) {
    return x5;
  }
  function normalize(a2, b) {
    return (b -= a2 = +a2) ? function(x5) {
      return (x5 - a2) / b;
    } : constants(isNaN(b) ? NaN : 0.5);
  }
  function clamper(a2, b) {
    var t;
    if (a2 > b) t = a2, a2 = b, b = t;
    return function(x5) {
      return Math.max(a2, Math.min(b, x5));
    };
  }
  function bimap(domain, range3, interpolate2) {
    var d0 = domain[0], d1 = domain[1], r0 = range3[0], r1 = range3[1];
    if (d1 < d0) d0 = normalize(d1, d0), r0 = interpolate2(r1, r0);
    else d0 = normalize(d0, d1), r0 = interpolate2(r0, r1);
    return function(x5) {
      return r0(d0(x5));
    };
  }
  function polymap(domain, range3, interpolate2) {
    var j = Math.min(domain.length, range3.length) - 1, d = new Array(j), r2 = new Array(j), i = -1;
    if (domain[j] < domain[0]) {
      domain = domain.slice().reverse();
      range3 = range3.slice().reverse();
    }
    while (++i < j) {
      d[i] = normalize(domain[i], domain[i + 1]);
      r2[i] = interpolate2(range3[i], range3[i + 1]);
    }
    return function(x5) {
      var i2 = bisect_default(domain, x5, 1, j) - 1;
      return r2[i2](d[i2](x5));
    };
  }
  function copy(source, target) {
    return target.domain(source.domain()).range(source.range()).interpolate(source.interpolate()).clamp(source.clamp()).unknown(source.unknown());
  }
  function transformer() {
    var domain = unit, range3 = unit, interpolate2 = value_default, transform, untransform, unknown, clamp = identity2, piecewise2, output, input;
    function rescale() {
      var n = Math.min(domain.length, range3.length);
      if (clamp !== identity2) clamp = clamper(domain[0], domain[n - 1]);
      piecewise2 = n > 2 ? polymap : bimap;
      output = input = null;
      return scale;
    }
    function scale(x5) {
      return x5 == null || isNaN(x5 = +x5) ? unknown : (output || (output = piecewise2(domain.map(transform), range3, interpolate2)))(transform(clamp(x5)));
    }
    scale.invert = function(y5) {
      return clamp(untransform((input || (input = piecewise2(range3, domain.map(transform), number_default2)))(y5)));
    };
    scale.domain = function(_) {
      return arguments.length ? (domain = Array.from(_, number), rescale()) : domain.slice();
    };
    scale.range = function(_) {
      return arguments.length ? (range3 = Array.from(_), rescale()) : range3.slice();
    };
    scale.rangeRound = function(_) {
      return range3 = Array.from(_), interpolate2 = round_default, rescale();
    };
    scale.clamp = function(_) {
      return arguments.length ? (clamp = _ ? true : identity2, rescale()) : clamp !== identity2;
    };
    scale.interpolate = function(_) {
      return arguments.length ? (interpolate2 = _, rescale()) : interpolate2;
    };
    scale.unknown = function(_) {
      return arguments.length ? (unknown = _, scale) : unknown;
    };
    return function(t, u) {
      transform = t, untransform = u;
      return rescale();
    };
  }
  function continuous() {
    return transformer()(identity2, identity2);
  }

  // node_modules/d3-format/src/formatDecimal.js
  function formatDecimal_default(x5) {
    return Math.abs(x5 = Math.round(x5)) >= 1e21 ? x5.toLocaleString("en").replace(/,/g, "") : x5.toString(10);
  }
  function formatDecimalParts(x5, p) {
    if (!isFinite(x5) || x5 === 0) return null;
    var i = (x5 = p ? x5.toExponential(p - 1) : x5.toExponential()).indexOf("e"), coefficient = x5.slice(0, i);
    return [
      coefficient.length > 1 ? coefficient[0] + coefficient.slice(2) : coefficient,
      +x5.slice(i + 1)
    ];
  }

  // node_modules/d3-format/src/exponent.js
  function exponent_default(x5) {
    return x5 = formatDecimalParts(Math.abs(x5)), x5 ? x5[1] : NaN;
  }

  // node_modules/d3-format/src/formatGroup.js
  function formatGroup_default(grouping, thousands) {
    return function(value2, width) {
      var i = value2.length, t = [], j = 0, g = grouping[0], length = 0;
      while (i > 0 && g > 0) {
        if (length + g + 1 > width) g = Math.max(1, width - length);
        t.push(value2.substring(i -= g, i + g));
        if ((length += g + 1) > width) break;
        g = grouping[j = (j + 1) % grouping.length];
      }
      return t.reverse().join(thousands);
    };
  }

  // node_modules/d3-format/src/formatNumerals.js
  function formatNumerals_default(numerals) {
    return function(value2) {
      return value2.replace(/[0-9]/g, function(i) {
        return numerals[+i];
      });
    };
  }

  // node_modules/d3-format/src/formatSpecifier.js
  var re = /^(?:(.)?([<>=^]))?([+\-( ])?([$#])?(0)?(\d+)?(,)?(\.\d+)?(~)?([a-z%])?$/i;
  function formatSpecifier(specifier) {
    if (!(match = re.exec(specifier))) throw new Error("invalid format: " + specifier);
    var match;
    return new FormatSpecifier({
      fill: match[1],
      align: match[2],
      sign: match[3],
      symbol: match[4],
      zero: match[5],
      width: match[6],
      comma: match[7],
      precision: match[8] && match[8].slice(1),
      trim: match[9],
      type: match[10]
    });
  }
  formatSpecifier.prototype = FormatSpecifier.prototype;
  function FormatSpecifier(specifier) {
    this.fill = specifier.fill === void 0 ? " " : specifier.fill + "";
    this.align = specifier.align === void 0 ? ">" : specifier.align + "";
    this.sign = specifier.sign === void 0 ? "-" : specifier.sign + "";
    this.symbol = specifier.symbol === void 0 ? "" : specifier.symbol + "";
    this.zero = !!specifier.zero;
    this.width = specifier.width === void 0 ? void 0 : +specifier.width;
    this.comma = !!specifier.comma;
    this.precision = specifier.precision === void 0 ? void 0 : +specifier.precision;
    this.trim = !!specifier.trim;
    this.type = specifier.type === void 0 ? "" : specifier.type + "";
  }
  FormatSpecifier.prototype.toString = function() {
    return this.fill + this.align + this.sign + this.symbol + (this.zero ? "0" : "") + (this.width === void 0 ? "" : Math.max(1, this.width | 0)) + (this.comma ? "," : "") + (this.precision === void 0 ? "" : "." + Math.max(0, this.precision | 0)) + (this.trim ? "~" : "") + this.type;
  };

  // node_modules/d3-format/src/formatTrim.js
  function formatTrim_default(s) {
    out: for (var n = s.length, i = 1, i0 = -1, i1; i < n; ++i) {
      switch (s[i]) {
        case ".":
          i0 = i1 = i;
          break;
        case "0":
          if (i0 === 0) i0 = i;
          i1 = i;
          break;
        default:
          if (!+s[i]) break out;
          if (i0 > 0) i0 = 0;
          break;
      }
    }
    return i0 > 0 ? s.slice(0, i0) + s.slice(i1 + 1) : s;
  }

  // node_modules/d3-format/src/formatPrefixAuto.js
  var prefixExponent;
  function formatPrefixAuto_default(x5, p) {
    var d = formatDecimalParts(x5, p);
    if (!d) return prefixExponent = void 0, x5.toPrecision(p);
    var coefficient = d[0], exponent = d[1], i = exponent - (prefixExponent = Math.max(-8, Math.min(8, Math.floor(exponent / 3))) * 3) + 1, n = coefficient.length;
    return i === n ? coefficient : i > n ? coefficient + new Array(i - n + 1).join("0") : i > 0 ? coefficient.slice(0, i) + "." + coefficient.slice(i) : "0." + new Array(1 - i).join("0") + formatDecimalParts(x5, Math.max(0, p + i - 1))[0];
  }

  // node_modules/d3-format/src/formatRounded.js
  function formatRounded_default(x5, p) {
    var d = formatDecimalParts(x5, p);
    if (!d) return x5 + "";
    var coefficient = d[0], exponent = d[1];
    return exponent < 0 ? "0." + new Array(-exponent).join("0") + coefficient : coefficient.length > exponent + 1 ? coefficient.slice(0, exponent + 1) + "." + coefficient.slice(exponent + 1) : coefficient + new Array(exponent - coefficient.length + 2).join("0");
  }

  // node_modules/d3-format/src/formatTypes.js
  var formatTypes_default = {
    "%": (x5, p) => (x5 * 100).toFixed(p),
    "b": (x5) => Math.round(x5).toString(2),
    "c": (x5) => x5 + "",
    "d": formatDecimal_default,
    "e": (x5, p) => x5.toExponential(p),
    "f": (x5, p) => x5.toFixed(p),
    "g": (x5, p) => x5.toPrecision(p),
    "o": (x5) => Math.round(x5).toString(8),
    "p": (x5, p) => formatRounded_default(x5 * 100, p),
    "r": formatRounded_default,
    "s": formatPrefixAuto_default,
    "X": (x5) => Math.round(x5).toString(16).toUpperCase(),
    "x": (x5) => Math.round(x5).toString(16)
  };

  // node_modules/d3-format/src/identity.js
  function identity_default(x5) {
    return x5;
  }

  // node_modules/d3-format/src/locale.js
  var map = Array.prototype.map;
  var prefixes = ["y", "z", "a", "f", "p", "n", "µ", "m", "", "k", "M", "G", "T", "P", "E", "Z", "Y"];
  function locale_default(locale3) {
    var group = locale3.grouping === void 0 || locale3.thousands === void 0 ? identity_default : formatGroup_default(map.call(locale3.grouping, Number), locale3.thousands + ""), currencyPrefix = locale3.currency === void 0 ? "" : locale3.currency[0] + "", currencySuffix = locale3.currency === void 0 ? "" : locale3.currency[1] + "", decimal = locale3.decimal === void 0 ? "." : locale3.decimal + "", numerals = locale3.numerals === void 0 ? identity_default : formatNumerals_default(map.call(locale3.numerals, String)), percent = locale3.percent === void 0 ? "%" : locale3.percent + "", minus = locale3.minus === void 0 ? "−" : locale3.minus + "", nan = locale3.nan === void 0 ? "NaN" : locale3.nan + "";
    function newFormat(specifier, options) {
      specifier = formatSpecifier(specifier);
      var fill = specifier.fill, align = specifier.align, sign2 = specifier.sign, symbol = specifier.symbol, zero2 = specifier.zero, width = specifier.width, comma = specifier.comma, precision = specifier.precision, trim = specifier.trim, type = specifier.type;
      if (type === "n") comma = true, type = "g";
      else if (!formatTypes_default[type]) precision === void 0 && (precision = 12), trim = true, type = "g";
      if (zero2 || fill === "0" && align === "=") zero2 = true, fill = "0", align = "=";
      var prefix2 = (options && options.prefix !== void 0 ? options.prefix : "") + (symbol === "$" ? currencyPrefix : symbol === "#" && /[boxX]/.test(type) ? "0" + type.toLowerCase() : ""), suffix2 = (symbol === "$" ? currencySuffix : /[%p]/.test(type) ? percent : "") + (options && options.suffix !== void 0 ? options.suffix : "");
      var formatType = formatTypes_default[type], maybeSuffix = /[defgprs%]/.test(type);
      precision = precision === void 0 ? 6 : /[gprs]/.test(type) ? Math.max(1, Math.min(21, precision)) : Math.max(0, Math.min(20, precision));
      function format2(value2) {
        var valuePrefix = prefix2, valueSuffix = suffix2, i, n, c2;
        if (type === "c") {
          valueSuffix = formatType(value2) + valueSuffix;
          value2 = "";
        } else {
          value2 = +value2;
          var valueNegative = value2 < 0 || 1 / value2 < 0;
          value2 = isNaN(value2) ? nan : formatType(Math.abs(value2), precision);
          if (trim) value2 = formatTrim_default(value2);
          if (valueNegative && +value2 === 0 && sign2 !== "+") valueNegative = false;
          valuePrefix = (valueNegative ? sign2 === "(" ? sign2 : minus : sign2 === "-" || sign2 === "(" ? "" : sign2) + valuePrefix;
          valueSuffix = (type === "s" && !isNaN(value2) && prefixExponent !== void 0 ? prefixes[8 + prefixExponent / 3] : "") + valueSuffix + (valueNegative && sign2 === "(" ? ")" : "");
          if (maybeSuffix) {
            i = -1, n = value2.length;
            while (++i < n) {
              if (c2 = value2.charCodeAt(i), 48 > c2 || c2 > 57) {
                valueSuffix = (c2 === 46 ? decimal + value2.slice(i + 1) : value2.slice(i)) + valueSuffix;
                value2 = value2.slice(0, i);
                break;
              }
            }
          }
        }
        if (comma && !zero2) value2 = group(value2, Infinity);
        var length = valuePrefix.length + value2.length + valueSuffix.length, padding = length < width ? new Array(width - length + 1).join(fill) : "";
        if (comma && zero2) value2 = group(padding + value2, padding.length ? width - valueSuffix.length : Infinity), padding = "";
        switch (align) {
          case "<":
            value2 = valuePrefix + value2 + valueSuffix + padding;
            break;
          case "=":
            value2 = valuePrefix + padding + value2 + valueSuffix;
            break;
          case "^":
            value2 = padding.slice(0, length = padding.length >> 1) + valuePrefix + value2 + valueSuffix + padding.slice(length);
            break;
          default:
            value2 = padding + valuePrefix + value2 + valueSuffix;
            break;
        }
        return numerals(value2);
      }
      format2.toString = function() {
        return specifier + "";
      };
      return format2;
    }
    function formatPrefix2(specifier, value2) {
      var e = Math.max(-8, Math.min(8, Math.floor(exponent_default(value2) / 3))) * 3, k = Math.pow(10, -e), f = newFormat((specifier = formatSpecifier(specifier), specifier.type = "f", specifier), { suffix: prefixes[8 + e / 3] });
      return function(value3) {
        return f(k * value3);
      };
    }
    return {
      format: newFormat,
      formatPrefix: formatPrefix2
    };
  }

  // node_modules/d3-format/src/defaultLocale.js
  var locale;
  var format;
  var formatPrefix;
  defaultLocale({
    thousands: ",",
    grouping: [3],
    currency: ["$", ""]
  });
  function defaultLocale(definition) {
    locale = locale_default(definition);
    format = locale.format;
    formatPrefix = locale.formatPrefix;
    return locale;
  }

  // node_modules/d3-format/src/precisionFixed.js
  function precisionFixed_default(step) {
    return Math.max(0, -exponent_default(Math.abs(step)));
  }

  // node_modules/d3-format/src/precisionPrefix.js
  function precisionPrefix_default(step, value2) {
    return Math.max(0, Math.max(-8, Math.min(8, Math.floor(exponent_default(value2) / 3))) * 3 - exponent_default(Math.abs(step)));
  }

  // node_modules/d3-format/src/precisionRound.js
  function precisionRound_default(step, max4) {
    step = Math.abs(step), max4 = Math.abs(max4) - step;
    return Math.max(0, exponent_default(max4) - exponent_default(step)) + 1;
  }

  // node_modules/d3-scale/src/tickFormat.js
  function tickFormat(start, stop, count, specifier) {
    var step = tickStep(start, stop, count), precision;
    specifier = formatSpecifier(specifier == null ? ",f" : specifier);
    switch (specifier.type) {
      case "s": {
        var value2 = Math.max(Math.abs(start), Math.abs(stop));
        if (specifier.precision == null && !isNaN(precision = precisionPrefix_default(step, value2))) specifier.precision = precision;
        return formatPrefix(specifier, value2);
      }
      case "":
      case "e":
      case "g":
      case "p":
      case "r": {
        if (specifier.precision == null && !isNaN(precision = precisionRound_default(step, Math.max(Math.abs(start), Math.abs(stop))))) specifier.precision = precision - (specifier.type === "e");
        break;
      }
      case "f":
      case "%": {
        if (specifier.precision == null && !isNaN(precision = precisionFixed_default(step))) specifier.precision = precision - (specifier.type === "%") * 2;
        break;
      }
    }
    return format(specifier);
  }

  // node_modules/d3-scale/src/linear.js
  function linearish(scale) {
    var domain = scale.domain;
    scale.ticks = function(count) {
      var d = domain();
      return ticks_default(d[0], d[d.length - 1], count == null ? 10 : count);
    };
    scale.tickFormat = function(count, specifier) {
      var d = domain();
      return tickFormat(d[0], d[d.length - 1], count == null ? 10 : count, specifier);
    };
    scale.nice = function(count) {
      if (count == null) count = 10;
      var d = domain();
      var i0 = 0;
      var i1 = d.length - 1;
      var start = d[i0];
      var stop = d[i1];
      var prestep;
      var step;
      var maxIter = 10;
      if (stop < start) {
        step = start, start = stop, stop = step;
        step = i0, i0 = i1, i1 = step;
      }
      while (maxIter-- > 0) {
        step = tickIncrement(start, stop, count);
        if (step === prestep) {
          d[i0] = start;
          d[i1] = stop;
          return domain(d);
        } else if (step > 0) {
          start = Math.floor(start / step) * step;
          stop = Math.ceil(stop / step) * step;
        } else if (step < 0) {
          start = Math.ceil(start * step) / step;
          stop = Math.floor(stop * step) / step;
        } else {
          break;
        }
        prestep = step;
      }
      return scale;
    };
    return scale;
  }
  function linear2() {
    var scale = continuous();
    scale.copy = function() {
      return copy(scale, linear2());
    };
    initRange.apply(scale, arguments);
    return linearish(scale);
  }

  // node_modules/d3-scale/src/identity.js
  function identity3(domain) {
    var unknown;
    function scale(x5) {
      return x5 == null || isNaN(x5 = +x5) ? unknown : x5;
    }
    scale.invert = scale;
    scale.domain = scale.range = function(_) {
      return arguments.length ? (domain = Array.from(_, number), scale) : domain.slice();
    };
    scale.unknown = function(_) {
      return arguments.length ? (unknown = _, scale) : unknown;
    };
    scale.copy = function() {
      return identity3(domain).unknown(unknown);
    };
    domain = arguments.length ? Array.from(domain, number) : [0, 1];
    return linearish(scale);
  }

  // node_modules/d3-scale/src/nice.js
  function nice(domain, interval2) {
    domain = domain.slice();
    var i0 = 0, i1 = domain.length - 1, x0 = domain[i0], x1 = domain[i1], t;
    if (x1 < x0) {
      t = i0, i0 = i1, i1 = t;
      t = x0, x0 = x1, x1 = t;
    }
    domain[i0] = interval2.floor(x0);
    domain[i1] = interval2.ceil(x1);
    return domain;
  }

  // node_modules/d3-scale/src/log.js
  function transformLog(x5) {
    return Math.log(x5);
  }
  function transformExp(x5) {
    return Math.exp(x5);
  }
  function transformLogn(x5) {
    return -Math.log(-x5);
  }
  function transformExpn(x5) {
    return -Math.exp(-x5);
  }
  function pow10(x5) {
    return isFinite(x5) ? +("1e" + x5) : x5 < 0 ? 0 : x5;
  }
  function powp(base) {
    return base === 10 ? pow10 : base === Math.E ? Math.exp : (x5) => Math.pow(base, x5);
  }
  function logp(base) {
    return base === Math.E ? Math.log : base === 10 && Math.log10 || base === 2 && Math.log2 || (base = Math.log(base), (x5) => Math.log(x5) / base);
  }
  function reflect(f) {
    return (x5, k) => -f(-x5, k);
  }
  function loggish(transform) {
    const scale = transform(transformLog, transformExp);
    const domain = scale.domain;
    let base = 10;
    let logs;
    let pows;
    function rescale() {
      logs = logp(base), pows = powp(base);
      if (domain()[0] < 0) {
        logs = reflect(logs), pows = reflect(pows);
        transform(transformLogn, transformExpn);
      } else {
        transform(transformLog, transformExp);
      }
      return scale;
    }
    scale.base = function(_) {
      return arguments.length ? (base = +_, rescale()) : base;
    };
    scale.domain = function(_) {
      return arguments.length ? (domain(_), rescale()) : domain();
    };
    scale.ticks = (count) => {
      const d = domain();
      let u = d[0];
      let v = d[d.length - 1];
      const r2 = v < u;
      if (r2) [u, v] = [v, u];
      let i = logs(u);
      let j = logs(v);
      let k;
      let t;
      const n = count == null ? 10 : +count;
      let z = [];
      if (!(base % 1) && j - i < n) {
        i = Math.floor(i), j = Math.ceil(j);
        if (u > 0) for (; i <= j; ++i) {
          for (k = 1; k < base; ++k) {
            t = i < 0 ? k / pows(-i) : k * pows(i);
            if (t < u) continue;
            if (t > v) break;
            z.push(t);
          }
        }
        else for (; i <= j; ++i) {
          for (k = base - 1; k >= 1; --k) {
            t = i > 0 ? k / pows(-i) : k * pows(i);
            if (t < u) continue;
            if (t > v) break;
            z.push(t);
          }
        }
        if (z.length * 2 < n) z = ticks_default(u, v, n);
      } else {
        z = ticks_default(i, j, Math.min(j - i, n)).map(pows);
      }
      return r2 ? z.reverse() : z;
    };
    scale.tickFormat = (count, specifier) => {
      if (count == null) count = 10;
      if (specifier == null) specifier = base === 10 ? "s" : ",";
      if (typeof specifier !== "function") {
        if (!(base % 1) && (specifier = formatSpecifier(specifier)).precision == null) specifier.trim = true;
        specifier = format(specifier);
      }
      if (count === Infinity) return specifier;
      const k = Math.max(1, base * count / scale.ticks().length);
      return (d) => {
        let i = d / pows(Math.round(logs(d)));
        if (i * base < base - 0.5) i *= base;
        return i <= k ? specifier(d) : "";
      };
    };
    scale.nice = () => {
      return domain(nice(domain(), {
        floor: (x5) => pows(Math.floor(logs(x5))),
        ceil: (x5) => pows(Math.ceil(logs(x5)))
      }));
    };
    return scale;
  }
  function log2() {
    const scale = loggish(transformer()).domain([1, 10]);
    scale.copy = () => copy(scale, log2()).base(scale.base());
    initRange.apply(scale, arguments);
    return scale;
  }

  // node_modules/d3-scale/src/symlog.js
  function transformSymlog(c2) {
    return function(x5) {
      return Math.sign(x5) * Math.log1p(Math.abs(x5 / c2));
    };
  }
  function transformSymexp(c2) {
    return function(x5) {
      return Math.sign(x5) * Math.expm1(Math.abs(x5)) * c2;
    };
  }
  function symlogish(transform) {
    var c2 = 1, scale = transform(transformSymlog(c2), transformSymexp(c2));
    scale.constant = function(_) {
      return arguments.length ? transform(transformSymlog(c2 = +_), transformSymexp(c2)) : c2;
    };
    return linearish(scale);
  }
  function symlog() {
    var scale = symlogish(transformer());
    scale.copy = function() {
      return copy(scale, symlog()).constant(scale.constant());
    };
    return initRange.apply(scale, arguments);
  }

  // node_modules/d3-scale/src/pow.js
  function transformPow(exponent) {
    return function(x5) {
      return x5 < 0 ? -Math.pow(-x5, exponent) : Math.pow(x5, exponent);
    };
  }
  function transformSqrt(x5) {
    return x5 < 0 ? -Math.sqrt(-x5) : Math.sqrt(x5);
  }
  function transformSquare(x5) {
    return x5 < 0 ? -x5 * x5 : x5 * x5;
  }
  function powish(transform) {
    var scale = transform(identity2, identity2), exponent = 1;
    function rescale() {
      return exponent === 1 ? transform(identity2, identity2) : exponent === 0.5 ? transform(transformSqrt, transformSquare) : transform(transformPow(exponent), transformPow(1 / exponent));
    }
    scale.exponent = function(_) {
      return arguments.length ? (exponent = +_, rescale()) : exponent;
    };
    return linearish(scale);
  }
  function pow() {
    var scale = powish(transformer());
    scale.copy = function() {
      return copy(scale, pow()).exponent(scale.exponent());
    };
    initRange.apply(scale, arguments);
    return scale;
  }
  function sqrt2() {
    return pow.apply(null, arguments).exponent(0.5);
  }

  // node_modules/d3-scale/src/radial.js
  function square(x5) {
    return Math.sign(x5) * x5 * x5;
  }
  function unsquare(x5) {
    return Math.sign(x5) * Math.sqrt(Math.abs(x5));
  }
  function radial() {
    var squared = continuous(), range3 = [0, 1], round2 = false, unknown;
    function scale(x5) {
      var y5 = unsquare(squared(x5));
      return isNaN(y5) ? unknown : round2 ? Math.round(y5) : y5;
    }
    scale.invert = function(y5) {
      return squared.invert(square(y5));
    };
    scale.domain = function(_) {
      return arguments.length ? (squared.domain(_), scale) : squared.domain();
    };
    scale.range = function(_) {
      return arguments.length ? (squared.range((range3 = Array.from(_, number)).map(square)), scale) : range3.slice();
    };
    scale.rangeRound = function(_) {
      return scale.range(_).round(true);
    };
    scale.round = function(_) {
      return arguments.length ? (round2 = !!_, scale) : round2;
    };
    scale.clamp = function(_) {
      return arguments.length ? (squared.clamp(_), scale) : squared.clamp();
    };
    scale.unknown = function(_) {
      return arguments.length ? (unknown = _, scale) : unknown;
    };
    scale.copy = function() {
      return radial(squared.domain(), range3).round(round2).clamp(squared.clamp()).unknown(unknown);
    };
    initRange.apply(scale, arguments);
    return linearish(scale);
  }

  // node_modules/d3-scale/src/quantile.js
  function quantile2() {
    var domain = [], range3 = [], thresholds = [], unknown;
    function rescale() {
      var i = 0, n = Math.max(1, range3.length);
      thresholds = new Array(n - 1);
      while (++i < n) thresholds[i - 1] = quantileSorted(domain, i / n);
      return scale;
    }
    function scale(x5) {
      return x5 == null || isNaN(x5 = +x5) ? unknown : range3[bisect_default(thresholds, x5)];
    }
    scale.invertExtent = function(y5) {
      var i = range3.indexOf(y5);
      return i < 0 ? [NaN, NaN] : [
        i > 0 ? thresholds[i - 1] : domain[0],
        i < thresholds.length ? thresholds[i] : domain[domain.length - 1]
      ];
    };
    scale.domain = function(_) {
      if (!arguments.length) return domain.slice();
      domain = [];
      for (let d of _) if (d != null && !isNaN(d = +d)) domain.push(d);
      domain.sort(ascending_default);
      return rescale();
    };
    scale.range = function(_) {
      return arguments.length ? (range3 = Array.from(_), rescale()) : range3.slice();
    };
    scale.unknown = function(_) {
      return arguments.length ? (unknown = _, scale) : unknown;
    };
    scale.quantiles = function() {
      return thresholds.slice();
    };
    scale.copy = function() {
      return quantile2().domain(domain).range(range3).unknown(unknown);
    };
    return initRange.apply(scale, arguments);
  }

  // node_modules/d3-scale/src/quantize.js
  function quantize() {
    var x0 = 0, x1 = 1, n = 1, domain = [0.5], range3 = [0, 1], unknown;
    function scale(x5) {
      return x5 != null && x5 <= x5 ? range3[bisect_default(domain, x5, 0, n)] : unknown;
    }
    function rescale() {
      var i = -1;
      domain = new Array(n);
      while (++i < n) domain[i] = ((i + 1) * x1 - (i - n) * x0) / (n + 1);
      return scale;
    }
    scale.domain = function(_) {
      return arguments.length ? ([x0, x1] = _, x0 = +x0, x1 = +x1, rescale()) : [x0, x1];
    };
    scale.range = function(_) {
      return arguments.length ? (n = (range3 = Array.from(_)).length - 1, rescale()) : range3.slice();
    };
    scale.invertExtent = function(y5) {
      var i = range3.indexOf(y5);
      return i < 0 ? [NaN, NaN] : i < 1 ? [x0, domain[0]] : i >= n ? [domain[n - 1], x1] : [domain[i - 1], domain[i]];
    };
    scale.unknown = function(_) {
      return arguments.length ? (unknown = _, scale) : scale;
    };
    scale.thresholds = function() {
      return domain.slice();
    };
    scale.copy = function() {
      return quantize().domain([x0, x1]).range(range3).unknown(unknown);
    };
    return initRange.apply(linearish(scale), arguments);
  }

  // node_modules/d3-scale/src/threshold.js
  function threshold() {
    var domain = [0.5], range3 = [0, 1], unknown, n = 1;
    function scale(x5) {
      return x5 != null && x5 <= x5 ? range3[bisect_default(domain, x5, 0, n)] : unknown;
    }
    scale.domain = function(_) {
      return arguments.length ? (domain = Array.from(_), n = Math.min(domain.length, range3.length - 1), scale) : domain.slice();
    };
    scale.range = function(_) {
      return arguments.length ? (range3 = Array.from(_), n = Math.min(domain.length, range3.length - 1), scale) : range3.slice();
    };
    scale.invertExtent = function(y5) {
      var i = range3.indexOf(y5);
      return [domain[i - 1], domain[i]];
    };
    scale.unknown = function(_) {
      return arguments.length ? (unknown = _, scale) : unknown;
    };
    scale.copy = function() {
      return threshold().domain(domain).range(range3).unknown(unknown);
    };
    return initRange.apply(scale, arguments);
  }

  // node_modules/d3-time/src/interval.js
  var t0 = /* @__PURE__ */ new Date();
  var t1 = /* @__PURE__ */ new Date();
  function timeInterval(floori, offseti, count, field) {
    function interval2(date2) {
      return floori(date2 = arguments.length === 0 ? /* @__PURE__ */ new Date() : /* @__PURE__ */ new Date(+date2)), date2;
    }
    interval2.floor = (date2) => {
      return floori(date2 = /* @__PURE__ */ new Date(+date2)), date2;
    };
    interval2.ceil = (date2) => {
      return floori(date2 = new Date(date2 - 1)), offseti(date2, 1), floori(date2), date2;
    };
    interval2.round = (date2) => {
      const d0 = interval2(date2), d1 = interval2.ceil(date2);
      return date2 - d0 < d1 - date2 ? d0 : d1;
    };
    interval2.offset = (date2, step) => {
      return offseti(date2 = /* @__PURE__ */ new Date(+date2), step == null ? 1 : Math.floor(step)), date2;
    };
    interval2.range = (start, stop, step) => {
      const range3 = [];
      start = interval2.ceil(start);
      step = step == null ? 1 : Math.floor(step);
      if (!(start < stop) || !(step > 0)) return range3;
      let previous;
      do
        range3.push(previous = /* @__PURE__ */ new Date(+start)), offseti(start, step), floori(start);
      while (previous < start && start < stop);
      return range3;
    };
    interval2.filter = (test) => {
      return timeInterval((date2) => {
        if (date2 >= date2) while (floori(date2), !test(date2)) date2.setTime(date2 - 1);
      }, (date2, step) => {
        if (date2 >= date2) {
          if (step < 0) while (++step <= 0) {
            while (offseti(date2, -1), !test(date2)) {
            }
          }
          else while (--step >= 0) {
            while (offseti(date2, 1), !test(date2)) {
            }
          }
        }
      });
    };
    if (count) {
      interval2.count = (start, end) => {
        t0.setTime(+start), t1.setTime(+end);
        floori(t0), floori(t1);
        return Math.floor(count(t0, t1));
      };
      interval2.every = (step) => {
        step = Math.floor(step);
        return !isFinite(step) || !(step > 0) ? null : !(step > 1) ? interval2 : interval2.filter(field ? (d) => field(d) % step === 0 : (d) => interval2.count(0, d) % step === 0);
      };
    }
    return interval2;
  }

  // node_modules/d3-time/src/millisecond.js
  var millisecond = timeInterval(() => {
  }, (date2, step) => {
    date2.setTime(+date2 + step);
  }, (start, end) => {
    return end - start;
  });
  millisecond.every = (k) => {
    k = Math.floor(k);
    if (!isFinite(k) || !(k > 0)) return null;
    if (!(k > 1)) return millisecond;
    return timeInterval((date2) => {
      date2.setTime(Math.floor(date2 / k) * k);
    }, (date2, step) => {
      date2.setTime(+date2 + step * k);
    }, (start, end) => {
      return (end - start) / k;
    });
  };
  var milliseconds = millisecond.range;

  // node_modules/d3-time/src/duration.js
  var durationSecond = 1e3;
  var durationMinute = durationSecond * 60;
  var durationHour = durationMinute * 60;
  var durationDay = durationHour * 24;
  var durationWeek = durationDay * 7;
  var durationMonth = durationDay * 30;
  var durationYear = durationDay * 365;

  // node_modules/d3-time/src/second.js
  var second = timeInterval((date2) => {
    date2.setTime(date2 - date2.getMilliseconds());
  }, (date2, step) => {
    date2.setTime(+date2 + step * durationSecond);
  }, (start, end) => {
    return (end - start) / durationSecond;
  }, (date2) => {
    return date2.getUTCSeconds();
  });
  var seconds = second.range;

  // node_modules/d3-time/src/minute.js
  var timeMinute = timeInterval((date2) => {
    date2.setTime(date2 - date2.getMilliseconds() - date2.getSeconds() * durationSecond);
  }, (date2, step) => {
    date2.setTime(+date2 + step * durationMinute);
  }, (start, end) => {
    return (end - start) / durationMinute;
  }, (date2) => {
    return date2.getMinutes();
  });
  var timeMinutes = timeMinute.range;
  var utcMinute = timeInterval((date2) => {
    date2.setUTCSeconds(0, 0);
  }, (date2, step) => {
    date2.setTime(+date2 + step * durationMinute);
  }, (start, end) => {
    return (end - start) / durationMinute;
  }, (date2) => {
    return date2.getUTCMinutes();
  });
  var utcMinutes = utcMinute.range;

  // node_modules/d3-time/src/hour.js
  var timeHour = timeInterval((date2) => {
    date2.setTime(date2 - date2.getMilliseconds() - date2.getSeconds() * durationSecond - date2.getMinutes() * durationMinute);
  }, (date2, step) => {
    date2.setTime(+date2 + step * durationHour);
  }, (start, end) => {
    return (end - start) / durationHour;
  }, (date2) => {
    return date2.getHours();
  });
  var timeHours = timeHour.range;
  var utcHour = timeInterval((date2) => {
    date2.setUTCMinutes(0, 0, 0);
  }, (date2, step) => {
    date2.setTime(+date2 + step * durationHour);
  }, (start, end) => {
    return (end - start) / durationHour;
  }, (date2) => {
    return date2.getUTCHours();
  });
  var utcHours = utcHour.range;

  // node_modules/d3-time/src/day.js
  var timeDay = timeInterval(
    (date2) => date2.setHours(0, 0, 0, 0),
    (date2, step) => date2.setDate(date2.getDate() + step),
    (start, end) => (end - start - (end.getTimezoneOffset() - start.getTimezoneOffset()) * durationMinute) / durationDay,
    (date2) => date2.getDate() - 1
  );
  var timeDays = timeDay.range;
  var utcDay = timeInterval((date2) => {
    date2.setUTCHours(0, 0, 0, 0);
  }, (date2, step) => {
    date2.setUTCDate(date2.getUTCDate() + step);
  }, (start, end) => {
    return (end - start) / durationDay;
  }, (date2) => {
    return date2.getUTCDate() - 1;
  });
  var utcDays = utcDay.range;
  var unixDay = timeInterval((date2) => {
    date2.setUTCHours(0, 0, 0, 0);
  }, (date2, step) => {
    date2.setUTCDate(date2.getUTCDate() + step);
  }, (start, end) => {
    return (end - start) / durationDay;
  }, (date2) => {
    return Math.floor(date2 / durationDay);
  });
  var unixDays = unixDay.range;

  // node_modules/d3-time/src/week.js
  function timeWeekday(i) {
    return timeInterval((date2) => {
      date2.setDate(date2.getDate() - (date2.getDay() + 7 - i) % 7);
      date2.setHours(0, 0, 0, 0);
    }, (date2, step) => {
      date2.setDate(date2.getDate() + step * 7);
    }, (start, end) => {
      return (end - start - (end.getTimezoneOffset() - start.getTimezoneOffset()) * durationMinute) / durationWeek;
    });
  }
  var timeSunday = timeWeekday(0);
  var timeMonday = timeWeekday(1);
  var timeTuesday = timeWeekday(2);
  var timeWednesday = timeWeekday(3);
  var timeThursday = timeWeekday(4);
  var timeFriday = timeWeekday(5);
  var timeSaturday = timeWeekday(6);
  var timeSundays = timeSunday.range;
  var timeMondays = timeMonday.range;
  var timeTuesdays = timeTuesday.range;
  var timeWednesdays = timeWednesday.range;
  var timeThursdays = timeThursday.range;
  var timeFridays = timeFriday.range;
  var timeSaturdays = timeSaturday.range;
  function utcWeekday(i) {
    return timeInterval((date2) => {
      date2.setUTCDate(date2.getUTCDate() - (date2.getUTCDay() + 7 - i) % 7);
      date2.setUTCHours(0, 0, 0, 0);
    }, (date2, step) => {
      date2.setUTCDate(date2.getUTCDate() + step * 7);
    }, (start, end) => {
      return (end - start) / durationWeek;
    });
  }
  var utcSunday = utcWeekday(0);
  var utcMonday = utcWeekday(1);
  var utcTuesday = utcWeekday(2);
  var utcWednesday = utcWeekday(3);
  var utcThursday = utcWeekday(4);
  var utcFriday = utcWeekday(5);
  var utcSaturday = utcWeekday(6);
  var utcSundays = utcSunday.range;
  var utcMondays = utcMonday.range;
  var utcTuesdays = utcTuesday.range;
  var utcWednesdays = utcWednesday.range;
  var utcThursdays = utcThursday.range;
  var utcFridays = utcFriday.range;
  var utcSaturdays = utcSaturday.range;

  // node_modules/d3-time/src/month.js
  var timeMonth = timeInterval((date2) => {
    date2.setDate(1);
    date2.setHours(0, 0, 0, 0);
  }, (date2, step) => {
    date2.setMonth(date2.getMonth() + step);
  }, (start, end) => {
    return end.getMonth() - start.getMonth() + (end.getFullYear() - start.getFullYear()) * 12;
  }, (date2) => {
    return date2.getMonth();
  });
  var timeMonths = timeMonth.range;
  var utcMonth = timeInterval((date2) => {
    date2.setUTCDate(1);
    date2.setUTCHours(0, 0, 0, 0);
  }, (date2, step) => {
    date2.setUTCMonth(date2.getUTCMonth() + step);
  }, (start, end) => {
    return end.getUTCMonth() - start.getUTCMonth() + (end.getUTCFullYear() - start.getUTCFullYear()) * 12;
  }, (date2) => {
    return date2.getUTCMonth();
  });
  var utcMonths = utcMonth.range;

  // node_modules/d3-time/src/year.js
  var timeYear = timeInterval((date2) => {
    date2.setMonth(0, 1);
    date2.setHours(0, 0, 0, 0);
  }, (date2, step) => {
    date2.setFullYear(date2.getFullYear() + step);
  }, (start, end) => {
    return end.getFullYear() - start.getFullYear();
  }, (date2) => {
    return date2.getFullYear();
  });
  timeYear.every = (k) => {
    return !isFinite(k = Math.floor(k)) || !(k > 0) ? null : timeInterval((date2) => {
      date2.setFullYear(Math.floor(date2.getFullYear() / k) * k);
      date2.setMonth(0, 1);
      date2.setHours(0, 0, 0, 0);
    }, (date2, step) => {
      date2.setFullYear(date2.getFullYear() + step * k);
    });
  };
  var timeYears = timeYear.range;
  var utcYear = timeInterval((date2) => {
    date2.setUTCMonth(0, 1);
    date2.setUTCHours(0, 0, 0, 0);
  }, (date2, step) => {
    date2.setUTCFullYear(date2.getUTCFullYear() + step);
  }, (start, end) => {
    return end.getUTCFullYear() - start.getUTCFullYear();
  }, (date2) => {
    return date2.getUTCFullYear();
  });
  utcYear.every = (k) => {
    return !isFinite(k = Math.floor(k)) || !(k > 0) ? null : timeInterval((date2) => {
      date2.setUTCFullYear(Math.floor(date2.getUTCFullYear() / k) * k);
      date2.setUTCMonth(0, 1);
      date2.setUTCHours(0, 0, 0, 0);
    }, (date2, step) => {
      date2.setUTCFullYear(date2.getUTCFullYear() + step * k);
    });
  };
  var utcYears = utcYear.range;

  // node_modules/d3-time/src/ticks.js
  function ticker(year, month, week, day, hour, minute) {
    const tickIntervals = [
      [second, 1, durationSecond],
      [second, 5, 5 * durationSecond],
      [second, 15, 15 * durationSecond],
      [second, 30, 30 * durationSecond],
      [minute, 1, durationMinute],
      [minute, 5, 5 * durationMinute],
      [minute, 15, 15 * durationMinute],
      [minute, 30, 30 * durationMinute],
      [hour, 1, durationHour],
      [hour, 3, 3 * durationHour],
      [hour, 6, 6 * durationHour],
      [hour, 12, 12 * durationHour],
      [day, 1, durationDay],
      [day, 2, 2 * durationDay],
      [week, 1, durationWeek],
      [month, 1, durationMonth],
      [month, 3, 3 * durationMonth],
      [year, 1, durationYear]
    ];
    function ticks(start, stop, count) {
      const reverse = stop < start;
      if (reverse) [start, stop] = [stop, start];
      const interval2 = count && typeof count.range === "function" ? count : tickInterval(start, stop, count);
      const ticks2 = interval2 ? interval2.range(start, +stop + 1) : [];
      return reverse ? ticks2.reverse() : ticks2;
    }
    function tickInterval(start, stop, count) {
      const target = Math.abs(stop - start) / count;
      const i = bisector_default(([, , step2]) => step2).right(tickIntervals, target);
      if (i === tickIntervals.length) return year.every(tickStep(start / durationYear, stop / durationYear, count));
      if (i === 0) return millisecond.every(Math.max(tickStep(start, stop, count), 1));
      const [t, step] = tickIntervals[target / tickIntervals[i - 1][2] < tickIntervals[i][2] / target ? i - 1 : i];
      return t.every(step);
    }
    return [ticks, tickInterval];
  }
  var [utcTicks, utcTickInterval] = ticker(utcYear, utcMonth, utcSunday, unixDay, utcHour, utcMinute);
  var [timeTicks, timeTickInterval] = ticker(timeYear, timeMonth, timeSunday, timeDay, timeHour, timeMinute);

  // node_modules/d3-time-format/src/locale.js
  function localDate(d) {
    if (0 <= d.y && d.y < 100) {
      var date2 = new Date(-1, d.m, d.d, d.H, d.M, d.S, d.L);
      date2.setFullYear(d.y);
      return date2;
    }
    return new Date(d.y, d.m, d.d, d.H, d.M, d.S, d.L);
  }
  function utcDate(d) {
    if (0 <= d.y && d.y < 100) {
      var date2 = new Date(Date.UTC(-1, d.m, d.d, d.H, d.M, d.S, d.L));
      date2.setUTCFullYear(d.y);
      return date2;
    }
    return new Date(Date.UTC(d.y, d.m, d.d, d.H, d.M, d.S, d.L));
  }
  function newDate(y5, m2, d) {
    return { y: y5, m: m2, d, H: 0, M: 0, S: 0, L: 0 };
  }
  function formatLocale(locale3) {
    var locale_dateTime = locale3.dateTime, locale_date = locale3.date, locale_time = locale3.time, locale_periods = locale3.periods, locale_weekdays = locale3.days, locale_shortWeekdays = locale3.shortDays, locale_months = locale3.months, locale_shortMonths = locale3.shortMonths;
    var periodRe = formatRe(locale_periods), periodLookup = formatLookup(locale_periods), weekdayRe = formatRe(locale_weekdays), weekdayLookup = formatLookup(locale_weekdays), shortWeekdayRe = formatRe(locale_shortWeekdays), shortWeekdayLookup = formatLookup(locale_shortWeekdays), monthRe = formatRe(locale_months), monthLookup = formatLookup(locale_months), shortMonthRe = formatRe(locale_shortMonths), shortMonthLookup = formatLookup(locale_shortMonths);
    var formats = {
      "a": formatShortWeekday,
      "A": formatWeekday,
      "b": formatShortMonth,
      "B": formatMonth,
      "c": null,
      "d": formatDayOfMonth,
      "e": formatDayOfMonth,
      "f": formatMicroseconds,
      "g": formatYearISO,
      "G": formatFullYearISO,
      "H": formatHour24,
      "I": formatHour12,
      "j": formatDayOfYear,
      "L": formatMilliseconds,
      "m": formatMonthNumber,
      "M": formatMinutes2,
      "p": formatPeriod,
      "q": formatQuarter,
      "Q": formatUnixTimestamp,
      "s": formatUnixTimestampSeconds,
      "S": formatSeconds,
      "u": formatWeekdayNumberMonday,
      "U": formatWeekNumberSunday,
      "V": formatWeekNumberISO,
      "w": formatWeekdayNumberSunday,
      "W": formatWeekNumberMonday,
      "x": null,
      "X": null,
      "y": formatYear,
      "Y": formatFullYear,
      "Z": formatZone,
      "%": formatLiteralPercent
    };
    var utcFormats = {
      "a": formatUTCShortWeekday,
      "A": formatUTCWeekday,
      "b": formatUTCShortMonth,
      "B": formatUTCMonth,
      "c": null,
      "d": formatUTCDayOfMonth,
      "e": formatUTCDayOfMonth,
      "f": formatUTCMicroseconds,
      "g": formatUTCYearISO,
      "G": formatUTCFullYearISO,
      "H": formatUTCHour24,
      "I": formatUTCHour12,
      "j": formatUTCDayOfYear,
      "L": formatUTCMilliseconds,
      "m": formatUTCMonthNumber,
      "M": formatUTCMinutes,
      "p": formatUTCPeriod,
      "q": formatUTCQuarter,
      "Q": formatUnixTimestamp,
      "s": formatUnixTimestampSeconds,
      "S": formatUTCSeconds,
      "u": formatUTCWeekdayNumberMonday,
      "U": formatUTCWeekNumberSunday,
      "V": formatUTCWeekNumberISO,
      "w": formatUTCWeekdayNumberSunday,
      "W": formatUTCWeekNumberMonday,
      "x": null,
      "X": null,
      "y": formatUTCYear,
      "Y": formatUTCFullYear,
      "Z": formatUTCZone,
      "%": formatLiteralPercent
    };
    var parses = {
      "a": parseShortWeekday,
      "A": parseWeekday,
      "b": parseShortMonth,
      "B": parseMonth,
      "c": parseLocaleDateTime,
      "d": parseDayOfMonth,
      "e": parseDayOfMonth,
      "f": parseMicroseconds,
      "g": parseYear,
      "G": parseFullYear,
      "H": parseHour24,
      "I": parseHour24,
      "j": parseDayOfYear,
      "L": parseMilliseconds,
      "m": parseMonthNumber,
      "M": parseMinutes,
      "p": parsePeriod,
      "q": parseQuarter,
      "Q": parseUnixTimestamp,
      "s": parseUnixTimestampSeconds,
      "S": parseSeconds,
      "u": parseWeekdayNumberMonday,
      "U": parseWeekNumberSunday,
      "V": parseWeekNumberISO,
      "w": parseWeekdayNumberSunday,
      "W": parseWeekNumberMonday,
      "x": parseLocaleDate,
      "X": parseLocaleTime,
      "y": parseYear,
      "Y": parseFullYear,
      "Z": parseZone,
      "%": parseLiteralPercent
    };
    formats.x = newFormat(locale_date, formats);
    formats.X = newFormat(locale_time, formats);
    formats.c = newFormat(locale_dateTime, formats);
    utcFormats.x = newFormat(locale_date, utcFormats);
    utcFormats.X = newFormat(locale_time, utcFormats);
    utcFormats.c = newFormat(locale_dateTime, utcFormats);
    function newFormat(specifier, formats2) {
      return function(date2) {
        var string = [], i = -1, j = 0, n = specifier.length, c2, pad2, format2;
        if (!(date2 instanceof Date)) date2 = /* @__PURE__ */ new Date(+date2);
        while (++i < n) {
          if (specifier.charCodeAt(i) === 37) {
            string.push(specifier.slice(j, i));
            if ((pad2 = pads[c2 = specifier.charAt(++i)]) != null) c2 = specifier.charAt(++i);
            else pad2 = c2 === "e" ? " " : "0";
            if (format2 = formats2[c2]) c2 = format2(date2, pad2);
            string.push(c2);
            j = i + 1;
          }
        }
        string.push(specifier.slice(j, i));
        return string.join("");
      };
    }
    function newParse(specifier, Z) {
      return function(string) {
        var d = newDate(1900, void 0, 1), i = parseSpecifier(d, specifier, string += "", 0), week, day;
        if (i != string.length) return null;
        if ("Q" in d) return new Date(d.Q);
        if ("s" in d) return new Date(d.s * 1e3 + ("L" in d ? d.L : 0));
        if (Z && !("Z" in d)) d.Z = 0;
        if ("p" in d) d.H = d.H % 12 + d.p * 12;
        if (d.m === void 0) d.m = "q" in d ? d.q : 0;
        if ("V" in d) {
          if (d.V < 1 || d.V > 53) return null;
          if (!("w" in d)) d.w = 1;
          if ("Z" in d) {
            week = utcDate(newDate(d.y, 0, 1)), day = week.getUTCDay();
            week = day > 4 || day === 0 ? utcMonday.ceil(week) : utcMonday(week);
            week = utcDay.offset(week, (d.V - 1) * 7);
            d.y = week.getUTCFullYear();
            d.m = week.getUTCMonth();
            d.d = week.getUTCDate() + (d.w + 6) % 7;
          } else {
            week = localDate(newDate(d.y, 0, 1)), day = week.getDay();
            week = day > 4 || day === 0 ? timeMonday.ceil(week) : timeMonday(week);
            week = timeDay.offset(week, (d.V - 1) * 7);
            d.y = week.getFullYear();
            d.m = week.getMonth();
            d.d = week.getDate() + (d.w + 6) % 7;
          }
        } else if ("W" in d || "U" in d) {
          if (!("w" in d)) d.w = "u" in d ? d.u % 7 : "W" in d ? 1 : 0;
          day = "Z" in d ? utcDate(newDate(d.y, 0, 1)).getUTCDay() : localDate(newDate(d.y, 0, 1)).getDay();
          d.m = 0;
          d.d = "W" in d ? (d.w + 6) % 7 + d.W * 7 - (day + 5) % 7 : d.w + d.U * 7 - (day + 6) % 7;
        }
        if ("Z" in d) {
          d.H += d.Z / 100 | 0;
          d.M += d.Z % 100;
          return utcDate(d);
        }
        return localDate(d);
      };
    }
    function parseSpecifier(d, specifier, string, j) {
      var i = 0, n = specifier.length, m2 = string.length, c2, parse;
      while (i < n) {
        if (j >= m2) return -1;
        c2 = specifier.charCodeAt(i++);
        if (c2 === 37) {
          c2 = specifier.charAt(i++);
          parse = parses[c2 in pads ? specifier.charAt(i++) : c2];
          if (!parse || (j = parse(d, string, j)) < 0) return -1;
        } else if (c2 != string.charCodeAt(j++)) {
          return -1;
        }
      }
      return j;
    }
    function parsePeriod(d, string, i) {
      var n = periodRe.exec(string.slice(i));
      return n ? (d.p = periodLookup.get(n[0].toLowerCase()), i + n[0].length) : -1;
    }
    function parseShortWeekday(d, string, i) {
      var n = shortWeekdayRe.exec(string.slice(i));
      return n ? (d.w = shortWeekdayLookup.get(n[0].toLowerCase()), i + n[0].length) : -1;
    }
    function parseWeekday(d, string, i) {
      var n = weekdayRe.exec(string.slice(i));
      return n ? (d.w = weekdayLookup.get(n[0].toLowerCase()), i + n[0].length) : -1;
    }
    function parseShortMonth(d, string, i) {
      var n = shortMonthRe.exec(string.slice(i));
      return n ? (d.m = shortMonthLookup.get(n[0].toLowerCase()), i + n[0].length) : -1;
    }
    function parseMonth(d, string, i) {
      var n = monthRe.exec(string.slice(i));
      return n ? (d.m = monthLookup.get(n[0].toLowerCase()), i + n[0].length) : -1;
    }
    function parseLocaleDateTime(d, string, i) {
      return parseSpecifier(d, locale_dateTime, string, i);
    }
    function parseLocaleDate(d, string, i) {
      return parseSpecifier(d, locale_date, string, i);
    }
    function parseLocaleTime(d, string, i) {
      return parseSpecifier(d, locale_time, string, i);
    }
    function formatShortWeekday(d) {
      return locale_shortWeekdays[d.getDay()];
    }
    function formatWeekday(d) {
      return locale_weekdays[d.getDay()];
    }
    function formatShortMonth(d) {
      return locale_shortMonths[d.getMonth()];
    }
    function formatMonth(d) {
      return locale_months[d.getMonth()];
    }
    function formatPeriod(d) {
      return locale_periods[+(d.getHours() >= 12)];
    }
    function formatQuarter(d) {
      return 1 + ~~(d.getMonth() / 3);
    }
    function formatUTCShortWeekday(d) {
      return locale_shortWeekdays[d.getUTCDay()];
    }
    function formatUTCWeekday(d) {
      return locale_weekdays[d.getUTCDay()];
    }
    function formatUTCShortMonth(d) {
      return locale_shortMonths[d.getUTCMonth()];
    }
    function formatUTCMonth(d) {
      return locale_months[d.getUTCMonth()];
    }
    function formatUTCPeriod(d) {
      return locale_periods[+(d.getUTCHours() >= 12)];
    }
    function formatUTCQuarter(d) {
      return 1 + ~~(d.getUTCMonth() / 3);
    }
    return {
      format: function(specifier) {
        var f = newFormat(specifier += "", formats);
        f.toString = function() {
          return specifier;
        };
        return f;
      },
      parse: function(specifier) {
        var p = newParse(specifier += "", false);
        p.toString = function() {
          return specifier;
        };
        return p;
      },
      utcFormat: function(specifier) {
        var f = newFormat(specifier += "", utcFormats);
        f.toString = function() {
          return specifier;
        };
        return f;
      },
      utcParse: function(specifier) {
        var p = newParse(specifier += "", true);
        p.toString = function() {
          return specifier;
        };
        return p;
      }
    };
  }
  var pads = { "-": "", "_": " ", "0": "0" };
  var numberRe = /^\s*\d+/;
  var percentRe = /^%/;
  var requoteRe = /[\\^$*+?|[\]().{}]/g;
  function pad(value2, fill, width) {
    var sign2 = value2 < 0 ? "-" : "", string = (sign2 ? -value2 : value2) + "", length = string.length;
    return sign2 + (length < width ? new Array(width - length + 1).join(fill) + string : string);
  }
  function requote(s) {
    return s.replace(requoteRe, "\\$&");
  }
  function formatRe(names) {
    return new RegExp("^(?:" + names.map(requote).join("|") + ")", "i");
  }
  function formatLookup(names) {
    return new Map(names.map((name, i) => [name.toLowerCase(), i]));
  }
  function parseWeekdayNumberSunday(d, string, i) {
    var n = numberRe.exec(string.slice(i, i + 1));
    return n ? (d.w = +n[0], i + n[0].length) : -1;
  }
  function parseWeekdayNumberMonday(d, string, i) {
    var n = numberRe.exec(string.slice(i, i + 1));
    return n ? (d.u = +n[0], i + n[0].length) : -1;
  }
  function parseWeekNumberSunday(d, string, i) {
    var n = numberRe.exec(string.slice(i, i + 2));
    return n ? (d.U = +n[0], i + n[0].length) : -1;
  }
  function parseWeekNumberISO(d, string, i) {
    var n = numberRe.exec(string.slice(i, i + 2));
    return n ? (d.V = +n[0], i + n[0].length) : -1;
  }
  function parseWeekNumberMonday(d, string, i) {
    var n = numberRe.exec(string.slice(i, i + 2));
    return n ? (d.W = +n[0], i + n[0].length) : -1;
  }
  function parseFullYear(d, string, i) {
    var n = numberRe.exec(string.slice(i, i + 4));
    return n ? (d.y = +n[0], i + n[0].length) : -1;
  }
  function parseYear(d, string, i) {
    var n = numberRe.exec(string.slice(i, i + 2));
    return n ? (d.y = +n[0] + (+n[0] > 68 ? 1900 : 2e3), i + n[0].length) : -1;
  }
  function parseZone(d, string, i) {
    var n = /^(Z)|([+-]\d\d)(?::?(\d\d))?/.exec(string.slice(i, i + 6));
    return n ? (d.Z = n[1] ? 0 : -(n[2] + (n[3] || "00")), i + n[0].length) : -1;
  }
  function parseQuarter(d, string, i) {
    var n = numberRe.exec(string.slice(i, i + 1));
    return n ? (d.q = n[0] * 3 - 3, i + n[0].length) : -1;
  }
  function parseMonthNumber(d, string, i) {
    var n = numberRe.exec(string.slice(i, i + 2));
    return n ? (d.m = n[0] - 1, i + n[0].length) : -1;
  }
  function parseDayOfMonth(d, string, i) {
    var n = numberRe.exec(string.slice(i, i + 2));
    return n ? (d.d = +n[0], i + n[0].length) : -1;
  }
  function parseDayOfYear(d, string, i) {
    var n = numberRe.exec(string.slice(i, i + 3));
    return n ? (d.m = 0, d.d = +n[0], i + n[0].length) : -1;
  }
  function parseHour24(d, string, i) {
    var n = numberRe.exec(string.slice(i, i + 2));
    return n ? (d.H = +n[0], i + n[0].length) : -1;
  }
  function parseMinutes(d, string, i) {
    var n = numberRe.exec(string.slice(i, i + 2));
    return n ? (d.M = +n[0], i + n[0].length) : -1;
  }
  function parseSeconds(d, string, i) {
    var n = numberRe.exec(string.slice(i, i + 2));
    return n ? (d.S = +n[0], i + n[0].length) : -1;
  }
  function parseMilliseconds(d, string, i) {
    var n = numberRe.exec(string.slice(i, i + 3));
    return n ? (d.L = +n[0], i + n[0].length) : -1;
  }
  function parseMicroseconds(d, string, i) {
    var n = numberRe.exec(string.slice(i, i + 6));
    return n ? (d.L = Math.floor(n[0] / 1e3), i + n[0].length) : -1;
  }
  function parseLiteralPercent(d, string, i) {
    var n = percentRe.exec(string.slice(i, i + 1));
    return n ? i + n[0].length : -1;
  }
  function parseUnixTimestamp(d, string, i) {
    var n = numberRe.exec(string.slice(i));
    return n ? (d.Q = +n[0], i + n[0].length) : -1;
  }
  function parseUnixTimestampSeconds(d, string, i) {
    var n = numberRe.exec(string.slice(i));
    return n ? (d.s = +n[0], i + n[0].length) : -1;
  }
  function formatDayOfMonth(d, p) {
    return pad(d.getDate(), p, 2);
  }
  function formatHour24(d, p) {
    return pad(d.getHours(), p, 2);
  }
  function formatHour12(d, p) {
    return pad(d.getHours() % 12 || 12, p, 2);
  }
  function formatDayOfYear(d, p) {
    return pad(1 + timeDay.count(timeYear(d), d), p, 3);
  }
  function formatMilliseconds(d, p) {
    return pad(d.getMilliseconds(), p, 3);
  }
  function formatMicroseconds(d, p) {
    return formatMilliseconds(d, p) + "000";
  }
  function formatMonthNumber(d, p) {
    return pad(d.getMonth() + 1, p, 2);
  }
  function formatMinutes2(d, p) {
    return pad(d.getMinutes(), p, 2);
  }
  function formatSeconds(d, p) {
    return pad(d.getSeconds(), p, 2);
  }
  function formatWeekdayNumberMonday(d) {
    var day = d.getDay();
    return day === 0 ? 7 : day;
  }
  function formatWeekNumberSunday(d, p) {
    return pad(timeSunday.count(timeYear(d) - 1, d), p, 2);
  }
  function dISO(d) {
    var day = d.getDay();
    return day >= 4 || day === 0 ? timeThursday(d) : timeThursday.ceil(d);
  }
  function formatWeekNumberISO(d, p) {
    d = dISO(d);
    return pad(timeThursday.count(timeYear(d), d) + (timeYear(d).getDay() === 4), p, 2);
  }
  function formatWeekdayNumberSunday(d) {
    return d.getDay();
  }
  function formatWeekNumberMonday(d, p) {
    return pad(timeMonday.count(timeYear(d) - 1, d), p, 2);
  }
  function formatYear(d, p) {
    return pad(d.getFullYear() % 100, p, 2);
  }
  function formatYearISO(d, p) {
    d = dISO(d);
    return pad(d.getFullYear() % 100, p, 2);
  }
  function formatFullYear(d, p) {
    return pad(d.getFullYear() % 1e4, p, 4);
  }
  function formatFullYearISO(d, p) {
    var day = d.getDay();
    d = day >= 4 || day === 0 ? timeThursday(d) : timeThursday.ceil(d);
    return pad(d.getFullYear() % 1e4, p, 4);
  }
  function formatZone(d) {
    var z = d.getTimezoneOffset();
    return (z > 0 ? "-" : (z *= -1, "+")) + pad(z / 60 | 0, "0", 2) + pad(z % 60, "0", 2);
  }
  function formatUTCDayOfMonth(d, p) {
    return pad(d.getUTCDate(), p, 2);
  }
  function formatUTCHour24(d, p) {
    return pad(d.getUTCHours(), p, 2);
  }
  function formatUTCHour12(d, p) {
    return pad(d.getUTCHours() % 12 || 12, p, 2);
  }
  function formatUTCDayOfYear(d, p) {
    return pad(1 + utcDay.count(utcYear(d), d), p, 3);
  }
  function formatUTCMilliseconds(d, p) {
    return pad(d.getUTCMilliseconds(), p, 3);
  }
  function formatUTCMicroseconds(d, p) {
    return formatUTCMilliseconds(d, p) + "000";
  }
  function formatUTCMonthNumber(d, p) {
    return pad(d.getUTCMonth() + 1, p, 2);
  }
  function formatUTCMinutes(d, p) {
    return pad(d.getUTCMinutes(), p, 2);
  }
  function formatUTCSeconds(d, p) {
    return pad(d.getUTCSeconds(), p, 2);
  }
  function formatUTCWeekdayNumberMonday(d) {
    var dow = d.getUTCDay();
    return dow === 0 ? 7 : dow;
  }
  function formatUTCWeekNumberSunday(d, p) {
    return pad(utcSunday.count(utcYear(d) - 1, d), p, 2);
  }
  function UTCdISO(d) {
    var day = d.getUTCDay();
    return day >= 4 || day === 0 ? utcThursday(d) : utcThursday.ceil(d);
  }
  function formatUTCWeekNumberISO(d, p) {
    d = UTCdISO(d);
    return pad(utcThursday.count(utcYear(d), d) + (utcYear(d).getUTCDay() === 4), p, 2);
  }
  function formatUTCWeekdayNumberSunday(d) {
    return d.getUTCDay();
  }
  function formatUTCWeekNumberMonday(d, p) {
    return pad(utcMonday.count(utcYear(d) - 1, d), p, 2);
  }
  function formatUTCYear(d, p) {
    return pad(d.getUTCFullYear() % 100, p, 2);
  }
  function formatUTCYearISO(d, p) {
    d = UTCdISO(d);
    return pad(d.getUTCFullYear() % 100, p, 2);
  }
  function formatUTCFullYear(d, p) {
    return pad(d.getUTCFullYear() % 1e4, p, 4);
  }
  function formatUTCFullYearISO(d, p) {
    var day = d.getUTCDay();
    d = day >= 4 || day === 0 ? utcThursday(d) : utcThursday.ceil(d);
    return pad(d.getUTCFullYear() % 1e4, p, 4);
  }
  function formatUTCZone() {
    return "+0000";
  }
  function formatLiteralPercent() {
    return "%";
  }
  function formatUnixTimestamp(d) {
    return +d;
  }
  function formatUnixTimestampSeconds(d) {
    return Math.floor(+d / 1e3);
  }

  // node_modules/d3-time-format/src/defaultLocale.js
  var locale2;
  var timeFormat;
  var timeParse;
  var utcFormat;
  var utcParse;
  defaultLocale2({
    dateTime: "%x, %X",
    date: "%-m/%-d/%Y",
    time: "%-I:%M:%S %p",
    periods: ["AM", "PM"],
    days: ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"],
    shortDays: ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"],
    months: ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"],
    shortMonths: ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"]
  });
  function defaultLocale2(definition) {
    locale2 = formatLocale(definition);
    timeFormat = locale2.format;
    timeParse = locale2.parse;
    utcFormat = locale2.utcFormat;
    utcParse = locale2.utcParse;
    return locale2;
  }

  // node_modules/d3-scale/src/time.js
  function date(t) {
    return new Date(t);
  }
  function number2(t) {
    return t instanceof Date ? +t : +/* @__PURE__ */ new Date(+t);
  }
  function calendar(ticks, tickInterval, year, month, week, day, hour, minute, second2, format2) {
    var scale = continuous(), invert = scale.invert, domain = scale.domain;
    var formatMillisecond = format2(".%L"), formatSecond = format2(":%S"), formatMinute = format2("%I:%M"), formatHour = format2("%I %p"), formatDay = format2("%a %d"), formatWeek = format2("%b %d"), formatMonth = format2("%B"), formatYear2 = format2("%Y");
    function tickFormat2(date2) {
      return (second2(date2) < date2 ? formatMillisecond : minute(date2) < date2 ? formatSecond : hour(date2) < date2 ? formatMinute : day(date2) < date2 ? formatHour : month(date2) < date2 ? week(date2) < date2 ? formatDay : formatWeek : year(date2) < date2 ? formatMonth : formatYear2)(date2);
    }
    scale.invert = function(y5) {
      return new Date(invert(y5));
    };
    scale.domain = function(_) {
      return arguments.length ? domain(Array.from(_, number2)) : domain().map(date);
    };
    scale.ticks = function(interval2) {
      var d = domain();
      return ticks(d[0], d[d.length - 1], interval2 == null ? 10 : interval2);
    };
    scale.tickFormat = function(count, specifier) {
      return specifier == null ? tickFormat2 : format2(specifier);
    };
    scale.nice = function(interval2) {
      var d = domain();
      if (!interval2 || typeof interval2.range !== "function") interval2 = tickInterval(d[0], d[d.length - 1], interval2 == null ? 10 : interval2);
      return interval2 ? domain(nice(d, interval2)) : scale;
    };
    scale.copy = function() {
      return copy(scale, calendar(ticks, tickInterval, year, month, week, day, hour, minute, second2, format2));
    };
    return scale;
  }
  function time() {
    return initRange.apply(calendar(timeTicks, timeTickInterval, timeYear, timeMonth, timeSunday, timeDay, timeHour, timeMinute, second, timeFormat).domain([new Date(2e3, 0, 1), new Date(2e3, 0, 2)]), arguments);
  }

  // node_modules/d3-scale/src/utcTime.js
  function utcTime() {
    return initRange.apply(calendar(utcTicks, utcTickInterval, utcYear, utcMonth, utcSunday, utcDay, utcHour, utcMinute, second, utcFormat).domain([Date.UTC(2e3, 0, 1), Date.UTC(2e3, 0, 2)]), arguments);
  }

  // node_modules/d3-scale/src/sequential.js
  function transformer2() {
    var x0 = 0, x1 = 1, t02, t12, k10, transform, interpolator = identity2, clamp = false, unknown;
    function scale(x5) {
      return x5 == null || isNaN(x5 = +x5) ? unknown : interpolator(k10 === 0 ? 0.5 : (x5 = (transform(x5) - t02) * k10, clamp ? Math.max(0, Math.min(1, x5)) : x5));
    }
    scale.domain = function(_) {
      return arguments.length ? ([x0, x1] = _, t02 = transform(x0 = +x0), t12 = transform(x1 = +x1), k10 = t02 === t12 ? 0 : 1 / (t12 - t02), scale) : [x0, x1];
    };
    scale.clamp = function(_) {
      return arguments.length ? (clamp = !!_, scale) : clamp;
    };
    scale.interpolator = function(_) {
      return arguments.length ? (interpolator = _, scale) : interpolator;
    };
    function range3(interpolate2) {
      return function(_) {
        var r0, r1;
        return arguments.length ? ([r0, r1] = _, interpolator = interpolate2(r0, r1), scale) : [interpolator(0), interpolator(1)];
      };
    }
    scale.range = range3(value_default);
    scale.rangeRound = range3(round_default);
    scale.unknown = function(_) {
      return arguments.length ? (unknown = _, scale) : unknown;
    };
    return function(t) {
      transform = t, t02 = t(x0), t12 = t(x1), k10 = t02 === t12 ? 0 : 1 / (t12 - t02);
      return scale;
    };
  }
  function copy2(source, target) {
    return target.domain(source.domain()).interpolator(source.interpolator()).clamp(source.clamp()).unknown(source.unknown());
  }
  function sequential() {
    var scale = linearish(transformer2()(identity2));
    scale.copy = function() {
      return copy2(scale, sequential());
    };
    return initInterpolator.apply(scale, arguments);
  }
  function sequentialLog() {
    var scale = loggish(transformer2()).domain([1, 10]);
    scale.copy = function() {
      return copy2(scale, sequentialLog()).base(scale.base());
    };
    return initInterpolator.apply(scale, arguments);
  }
  function sequentialSymlog() {
    var scale = symlogish(transformer2());
    scale.copy = function() {
      return copy2(scale, sequentialSymlog()).constant(scale.constant());
    };
    return initInterpolator.apply(scale, arguments);
  }
  function sequentialPow() {
    var scale = powish(transformer2());
    scale.copy = function() {
      return copy2(scale, sequentialPow()).exponent(scale.exponent());
    };
    return initInterpolator.apply(scale, arguments);
  }
  function sequentialSqrt() {
    return sequentialPow.apply(null, arguments).exponent(0.5);
  }

  // node_modules/d3-scale/src/sequentialQuantile.js
  function sequentialQuantile() {
    var domain = [], interpolator = identity2;
    function scale(x5) {
      if (x5 != null && !isNaN(x5 = +x5)) return interpolator((bisect_default(domain, x5, 1) - 1) / (domain.length - 1));
    }
    scale.domain = function(_) {
      if (!arguments.length) return domain.slice();
      domain = [];
      for (let d of _) if (d != null && !isNaN(d = +d)) domain.push(d);
      domain.sort(ascending_default);
      return scale;
    };
    scale.interpolator = function(_) {
      return arguments.length ? (interpolator = _, scale) : interpolator;
    };
    scale.range = function() {
      return domain.map((d, i) => interpolator(i / (domain.length - 1)));
    };
    scale.quantiles = function(n) {
      return Array.from({ length: n + 1 }, (_, i) => quantile(domain, i / n));
    };
    scale.copy = function() {
      return sequentialQuantile(interpolator).domain(domain);
    };
    return initInterpolator.apply(scale, arguments);
  }

  // node_modules/d3-scale/src/diverging.js
  function transformer3() {
    var x0 = 0, x1 = 0.5, x22 = 1, s = 1, t02, t12, t2, k10, k21, interpolator = identity2, transform, clamp = false, unknown;
    function scale(x5) {
      return isNaN(x5 = +x5) ? unknown : (x5 = 0.5 + ((x5 = +transform(x5)) - t12) * (s * x5 < s * t12 ? k10 : k21), interpolator(clamp ? Math.max(0, Math.min(1, x5)) : x5));
    }
    scale.domain = function(_) {
      return arguments.length ? ([x0, x1, x22] = _, t02 = transform(x0 = +x0), t12 = transform(x1 = +x1), t2 = transform(x22 = +x22), k10 = t02 === t12 ? 0 : 0.5 / (t12 - t02), k21 = t12 === t2 ? 0 : 0.5 / (t2 - t12), s = t12 < t02 ? -1 : 1, scale) : [x0, x1, x22];
    };
    scale.clamp = function(_) {
      return arguments.length ? (clamp = !!_, scale) : clamp;
    };
    scale.interpolator = function(_) {
      return arguments.length ? (interpolator = _, scale) : interpolator;
    };
    function range3(interpolate2) {
      return function(_) {
        var r0, r1, r2;
        return arguments.length ? ([r0, r1, r2] = _, interpolator = piecewise(interpolate2, [r0, r1, r2]), scale) : [interpolator(0), interpolator(0.5), interpolator(1)];
      };
    }
    scale.range = range3(value_default);
    scale.rangeRound = range3(round_default);
    scale.unknown = function(_) {
      return arguments.length ? (unknown = _, scale) : unknown;
    };
    return function(t) {
      transform = t, t02 = t(x0), t12 = t(x1), t2 = t(x22), k10 = t02 === t12 ? 0 : 0.5 / (t12 - t02), k21 = t12 === t2 ? 0 : 0.5 / (t2 - t12), s = t12 < t02 ? -1 : 1;
      return scale;
    };
  }
  function diverging() {
    var scale = linearish(transformer3()(identity2));
    scale.copy = function() {
      return copy2(scale, diverging());
    };
    return initInterpolator.apply(scale, arguments);
  }
  function divergingLog() {
    var scale = loggish(transformer3()).domain([0.1, 1, 10]);
    scale.copy = function() {
      return copy2(scale, divergingLog()).base(scale.base());
    };
    return initInterpolator.apply(scale, arguments);
  }
  function divergingSymlog() {
    var scale = symlogish(transformer3());
    scale.copy = function() {
      return copy2(scale, divergingSymlog()).constant(scale.constant());
    };
    return initInterpolator.apply(scale, arguments);
  }
  function divergingPow() {
    var scale = powish(transformer3());
    scale.copy = function() {
      return copy2(scale, divergingPow()).exponent(scale.exponent());
    };
    return initInterpolator.apply(scale, arguments);
  }
  function divergingSqrt() {
    return divergingPow.apply(null, arguments).exponent(0.5);
  }

  // node_modules/recharts/es6/state/selectors/combiners/combineConfiguredScale.js
  function getD3ScaleFromType(realScaleType) {
    var scales = d3_scale_exports;
    if (realScaleType in scales && typeof scales[realScaleType] === "function") {
      return scales[realScaleType]();
    }
    var name = "scale".concat(upperFirst(realScaleType));
    if (name in scales && typeof scales[name] === "function") {
      return scales[name]();
    }
    return void 0;
  }
  function combineConfiguredScaleInternal(scale, axisDomain, axisRange) {
    if (typeof scale === "function") {
      return scale.copy().domain(axisDomain).range(axisRange);
    }
    if (scale == null) {
      return void 0;
    }
    var d3ScaleFunction = getD3ScaleFromType(scale);
    if (d3ScaleFunction == null) {
      return void 0;
    }
    d3ScaleFunction.domain(axisDomain).range(axisRange);
    return d3ScaleFunction;
  }
  function combineConfiguredScale(axis, realScaleType, axisDomain, axisRange) {
    if (axisDomain == null || axisRange == null) {
      return void 0;
    }
    if (typeof axis.scale === "function") {
      return combineConfiguredScaleInternal(axis.scale, axisDomain, axisRange);
    }
    return combineConfiguredScaleInternal(realScaleType, axisDomain, axisRange);
  }

  // node_modules/recharts/es6/state/selectors/combiners/combineRealScaleType.js
  function getD3ScaleName(name) {
    return "scale".concat(upperFirst(name));
  }
  function isSupportedScaleName(name) {
    return getD3ScaleName(name) in d3_scale_exports;
  }
  var combineRealScaleType = (axisConfig, hasBar, chartType) => {
    if (axisConfig == null) {
      return void 0;
    }
    var scale = axisConfig.scale, type = axisConfig.type;
    if (scale === "auto") {
      if (type === "category" && chartType && (chartType.indexOf("LineChart") >= 0 || chartType.indexOf("AreaChart") >= 0 || chartType.indexOf("ComposedChart") >= 0 && !hasBar)) {
        return "point";
      }
      if (type === "category") {
        return "band";
      }
      return "linear";
    }
    if (typeof scale === "string") {
      return isSupportedScaleName(scale) ? scale : "point";
    }
    return void 0;
  };

  // node_modules/recharts/es6/util/scale/createCategoricalInverse.js
  function bisect(haystack, needle) {
    var lo = 0;
    var hi = haystack.length;
    var ascending = haystack[0] < haystack[haystack.length - 1];
    while (lo < hi) {
      var mid = Math.floor((lo + hi) / 2);
      if (ascending ? haystack[mid] < needle : haystack[mid] > needle) {
        lo = mid + 1;
      } else {
        hi = mid;
      }
    }
    return lo;
  }
  function createCategoricalInverse(scale, allDataPointsOnAxis) {
    if (!scale) {
      return void 0;
    }
    var domain = allDataPointsOnAxis !== null && allDataPointsOnAxis !== void 0 ? allDataPointsOnAxis : scale.domain();
    var pixelPositions = domain.map((d) => {
      var _scale;
      return (_scale = scale(d)) !== null && _scale !== void 0 ? _scale : 0;
    });
    var range3 = scale.range();
    if (domain.length === 0 || range3.length < 2) {
      return void 0;
    }
    return (pixelValue) => {
      var _pixelPositions, _pixelPositions$index;
      var index2 = bisect(pixelPositions, pixelValue);
      if (index2 <= 0) {
        return domain[0];
      }
      if (index2 >= domain.length) {
        return domain[domain.length - 1];
      }
      var leftPixel = (_pixelPositions = pixelPositions[index2 - 1]) !== null && _pixelPositions !== void 0 ? _pixelPositions : 0;
      var rightPixel = (_pixelPositions$index = pixelPositions[index2]) !== null && _pixelPositions$index !== void 0 ? _pixelPositions$index : 0;
      if (Math.abs(pixelValue - leftPixel) <= Math.abs(pixelValue - rightPixel)) {
        return domain[index2 - 1];
      }
      return domain[index2];
    };
  }

  // node_modules/recharts/es6/state/selectors/combiners/combineInverseScaleFunction.js
  function combineInverseScaleFunction(configuredScale) {
    if (configuredScale == null) {
      return void 0;
    }
    if ("invert" in configuredScale && typeof configuredScale.invert === "function") {
      return configuredScale.invert.bind(configuredScale);
    }
    return createCategoricalInverse(configuredScale, void 0);
  }

  // node_modules/recharts/es6/state/selectors/axisSelectors.js
  function ownKeys11(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread11(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys11(Object(t), true).forEach(function(r3) {
        _defineProperty12(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys11(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty12(e, r2, t) {
    return (r2 = _toPropertyKey12(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey12(t) {
    var i = _toPrimitive12(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive12(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  function _slicedToArray9(r2, e) {
    return _arrayWithHoles9(r2) || _iterableToArrayLimit9(r2, e) || _unsupportedIterableToArray9(r2, e) || _nonIterableRest9();
  }
  function _nonIterableRest9() {
    throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  function _unsupportedIterableToArray9(r2, a2) {
    if (r2) {
      if ("string" == typeof r2) return _arrayLikeToArray9(r2, a2);
      var t = {}.toString.call(r2).slice(8, -1);
      return "Object" === t && r2.constructor && (t = r2.constructor.name), "Map" === t || "Set" === t ? Array.from(r2) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray9(r2, a2) : void 0;
    }
  }
  function _arrayLikeToArray9(r2, a2) {
    (null == a2 || a2 > r2.length) && (a2 = r2.length);
    for (var e = 0, n = Array(a2); e < a2; e++) n[e] = r2[e];
    return n;
  }
  function _iterableToArrayLimit9(r2, l) {
    var t = null == r2 ? null : "undefined" != typeof Symbol && r2[Symbol.iterator] || r2["@@iterator"];
    if (null != t) {
      var e, n, i, u, a2 = [], f = true, o = false;
      try {
        if (i = (t = t.call(r2)).next, 0 === l) {
          if (Object(t) !== t) return;
          f = false;
        } else for (; !(f = (e = i.call(t)).done) && (a2.push(e.value), a2.length !== l); f = true) ;
      } catch (r3) {
        o = true, n = r3;
      } finally {
        try {
          if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return;
        } finally {
          if (o) throw n;
        }
      }
      return a2;
    }
  }
  function _arrayWithHoles9(r2) {
    if (Array.isArray(r2)) return r2;
  }
  var defaultNumericDomain = [0, "auto"];
  var implicitXAxis = {
    allowDataOverflow: false,
    allowDecimals: true,
    allowDuplicatedCategory: true,
    angle: 0,
    dataKey: void 0,
    domain: void 0,
    height: 30,
    hide: true,
    id: 0,
    includeHidden: false,
    interval: "preserveEnd",
    minTickGap: 5,
    mirror: false,
    name: void 0,
    orientation: "bottom",
    padding: {
      left: 0,
      right: 0
    },
    reversed: false,
    scale: "auto",
    tick: true,
    tickCount: 5,
    tickFormatter: void 0,
    ticks: void 0,
    type: "category",
    unit: void 0,
    niceTicks: "auto"
  };
  var selectXAxisSettingsNoDefaults = (state, axisId) => {
    return state.cartesianAxis.xAxis[axisId];
  };
  var selectXAxisSettings = (state, axisId) => {
    var axis = selectXAxisSettingsNoDefaults(state, axisId);
    if (axis == null) {
      return implicitXAxis;
    }
    return axis;
  };
  var implicitYAxis = {
    allowDataOverflow: false,
    allowDecimals: true,
    allowDuplicatedCategory: true,
    angle: 0,
    dataKey: void 0,
    domain: defaultNumericDomain,
    hide: true,
    id: 0,
    includeHidden: false,
    interval: "preserveEnd",
    minTickGap: 5,
    mirror: false,
    name: void 0,
    orientation: "left",
    padding: {
      top: 0,
      bottom: 0
    },
    reversed: false,
    scale: "auto",
    tick: true,
    tickCount: 5,
    tickFormatter: void 0,
    ticks: void 0,
    type: "number",
    unit: void 0,
    niceTicks: "auto",
    width: DEFAULT_Y_AXIS_WIDTH
  };
  var selectYAxisSettingsNoDefaults = (state, axisId) => {
    return state.cartesianAxis.yAxis[axisId];
  };
  var selectYAxisSettings = (state, axisId) => {
    var axis = selectYAxisSettingsNoDefaults(state, axisId);
    if (axis == null) {
      return implicitYAxis;
    }
    return axis;
  };
  var implicitZAxis = {
    domain: [0, "auto"],
    includeHidden: false,
    reversed: false,
    allowDataOverflow: false,
    allowDuplicatedCategory: false,
    dataKey: void 0,
    id: 0,
    name: "",
    range: [64, 64],
    scale: "auto",
    type: "number",
    unit: ""
  };
  var selectZAxisSettings = (state, axisId) => {
    var axis = state.cartesianAxis.zAxis[axisId];
    if (axis == null) {
      return implicitZAxis;
    }
    return axis;
  };
  var selectBaseAxis = (state, axisType, axisId) => {
    switch (axisType) {
      case "xAxis": {
        return selectXAxisSettings(state, axisId);
      }
      case "yAxis": {
        return selectYAxisSettings(state, axisId);
      }
      case "zAxis": {
        return selectZAxisSettings(state, axisId);
      }
      case "angleAxis": {
        return selectAngleAxis(state, axisId);
      }
      case "radiusAxis": {
        return selectRadiusAxis(state, axisId);
      }
      default:
        throw new Error("Unexpected axis type: ".concat(axisType));
    }
  };
  var selectCartesianAxisSettings = (state, axisType, axisId) => {
    switch (axisType) {
      case "xAxis": {
        return selectXAxisSettings(state, axisId);
      }
      case "yAxis": {
        return selectYAxisSettings(state, axisId);
      }
      default:
        throw new Error("Unexpected axis type: ".concat(axisType));
    }
  };
  var selectRenderableAxisSettings = (state, axisType, axisId) => {
    switch (axisType) {
      case "xAxis": {
        return selectXAxisSettings(state, axisId);
      }
      case "yAxis": {
        return selectYAxisSettings(state, axisId);
      }
      case "angleAxis": {
        return selectAngleAxis(state, axisId);
      }
      case "radiusAxis": {
        return selectRadiusAxis(state, axisId);
      }
      default:
        throw new Error("Unexpected axis type: ".concat(axisType));
    }
  };
  var selectHasBar = (state) => state.graphicalItems.cartesianItems.some((item) => item.type === "bar") || state.graphicalItems.polarItems.some((item) => item.type === "radialBar");
  function itemAxisPredicate(axisType, axisId) {
    return (item) => {
      switch (axisType) {
        case "xAxis":
          return "xAxisId" in item && item.xAxisId === axisId;
        case "yAxis":
          return "yAxisId" in item && item.yAxisId === axisId;
        case "zAxis":
          return "zAxisId" in item && item.zAxisId === axisId;
        case "angleAxis":
          return "angleAxisId" in item && item.angleAxisId === axisId;
        case "radiusAxis":
          return "radiusAxisId" in item && item.radiusAxisId === axisId;
        default:
          return false;
      }
    };
  }
  var selectUnfilteredCartesianItems = (state) => state.graphicalItems.cartesianItems;
  var selectAxisPredicate = createSelector([pickAxisType, pickAxisId], itemAxisPredicate);
  var combineGraphicalItemsSettings = (graphicalItems, axisSettings, axisPredicate) => graphicalItems.filter(axisPredicate).filter((item) => {
    if ((axisSettings === null || axisSettings === void 0 ? void 0 : axisSettings.includeHidden) === true) {
      return true;
    }
    return !item.hide;
  });
  var selectCartesianItemsSettings = createSelector([selectUnfilteredCartesianItems, selectBaseAxis, selectAxisPredicate], combineGraphicalItemsSettings, {
    memoizeOptions: {
      resultEqualityCheck: emptyArraysAreEqualCheck
    }
  });
  var selectStackedCartesianItemsSettings = createSelector([selectCartesianItemsSettings], (cartesianItems) => {
    return cartesianItems.filter((item) => item.type === "area" || item.type === "bar").filter(isStacked);
  });
  var filterGraphicalNotStackedItems = (cartesianItems) => cartesianItems.filter((item) => !("stackId" in item) || item.stackId === void 0);
  var selectCartesianItemsSettingsExceptStacked = createSelector([selectCartesianItemsSettings], filterGraphicalNotStackedItems);
  var combineGraphicalItemsData = (cartesianItems) => cartesianItems.map((item) => item.data).filter(Boolean).flat(1);
  var selectAnyCartesianItemsUsesChartData = createSelector([selectCartesianItemsSettings], (items) => items.some((item) => !item.data));
  var selectCartesianGraphicalItemsData = createSelector([selectCartesianItemsSettings], combineGraphicalItemsData, {
    memoizeOptions: {
      resultEqualityCheck: emptyArraysAreEqualCheck
    }
  });
  var combineDisplayedData = (graphicalItemsData, _ref2) => {
    var _ref$chartData = _ref2.chartData, chartData = _ref$chartData === void 0 ? [] : _ref$chartData, dataStartIndex = _ref2.dataStartIndex, dataEndIndex = _ref2.dataEndIndex;
    if (graphicalItemsData.length > 0) {
      return graphicalItemsData;
    }
    return chartData.slice(dataStartIndex, dataEndIndex + 1);
  };
  var selectDisplayedData = createSelector([selectCartesianGraphicalItemsData, selectChartDataWithIndexesIfNotInPanoramaPosition4], combineDisplayedData);
  var combineAppliedValues = (data, axisSettings, items) => {
    if ((axisSettings === null || axisSettings === void 0 ? void 0 : axisSettings.dataKey) != null) {
      return data.map((item) => ({
        value: getValueByDataKey(item, axisSettings.dataKey)
      }));
    }
    if (items.length > 0) {
      return items.map((item) => item.dataKey).flatMap((dataKey) => data.map((entry) => ({
        value: getValueByDataKey(entry, dataKey)
      })));
    }
    return data.map((entry) => ({
      value: entry
    }));
  };
  var combineAllAppliedValues = (displayedData, axisSettings, items, _ref2, anyItemUsesChartData, graphicalItemsData) => {
    var _ref2$chartData = _ref2.chartData, chartData = _ref2$chartData === void 0 ? [] : _ref2$chartData, dataStartIndex = _ref2.dataStartIndex, dataEndIndex = _ref2.dataEndIndex;
    var appliedValues = combineAppliedValues(displayedData, axisSettings, items);
    if (anyItemUsesChartData && (axisSettings === null || axisSettings === void 0 ? void 0 : axisSettings.dataKey) != null && graphicalItemsData.length > 0) {
      var chartDataSlice2 = chartData.slice(dataStartIndex, dataEndIndex + 1);
      var chartAppliedValues = chartDataSlice2.map((item) => ({
        value: getValueByDataKey(item, axisSettings.dataKey)
      })).filter((av) => av.value != null);
      return [...chartAppliedValues, ...appliedValues];
    }
    return appliedValues;
  };
  var selectAllAppliedValues = createSelector([selectDisplayedData, selectBaseAxis, selectCartesianItemsSettings, selectChartDataWithIndexesIfNotInPanoramaPosition4, selectAnyCartesianItemsUsesChartData, selectCartesianGraphicalItemsData], combineAllAppliedValues);
  function makeNumber(val) {
    if (isNumOrStr(val) || val instanceof Date) {
      var n = Number(val);
      if (isWellBehavedNumber(n)) {
        return n;
      }
    }
    return void 0;
  }
  function makeDomain(val) {
    if (Array.isArray(val)) {
      var attempt = [makeNumber(val[0]), makeNumber(val[1])];
      if (isWellFormedNumberDomain(attempt)) {
        return attempt;
      }
      return void 0;
    }
    var n = makeNumber(val);
    if (n == null) {
      return void 0;
    }
    return [n, n];
  }
  function onlyAllowNumbers(data) {
    return data.map(makeNumber).filter(isNotNil);
  }
  function sortBy2(a2, b) {
    var aNum = makeNumber(a2);
    var bNum = makeNumber(b);
    if (aNum == null && bNum == null) {
      return 0;
    }
    if (aNum == null) {
      return -1;
    }
    if (bNum == null) {
      return 1;
    }
    return aNum - bNum;
  }
  var selectSortedDataPoints = createSelector([selectAllAppliedValues], (appliedData) => {
    return appliedData === null || appliedData === void 0 ? void 0 : appliedData.map((item) => item.value).sort(sortBy2);
  });
  function isErrorBarRelevantForAxisType(axisType, errorBar) {
    switch (axisType) {
      case "xAxis":
        return errorBar.direction === "x";
      case "yAxis":
        return errorBar.direction === "y";
      default:
        return false;
    }
  }
  function getErrorDomainByDataKey(entry, appliedValue, relevantErrorBars) {
    if (!relevantErrorBars) {
      return [];
    }
    if (!relevantErrorBars.length) {
      return [];
    }
    var appliedNumericValue;
    if (typeof appliedValue === "number" && !isNan(appliedValue)) {
      appliedNumericValue = appliedValue;
    } else if (Array.isArray(appliedValue)) {
      var numericRangeValues = onlyAllowNumbers(appliedValue);
      if (numericRangeValues.length > 0) {
        appliedNumericValue = Math.max(...numericRangeValues);
      }
    }
    if (appliedNumericValue == null) {
      return [];
    }
    return onlyAllowNumbers(relevantErrorBars.flatMap((eb) => {
      var errorValue = getValueByDataKey(entry, eb.dataKey);
      var lowBound, highBound;
      if (Array.isArray(errorValue)) {
        var _errorValue = _slicedToArray9(errorValue, 2);
        lowBound = _errorValue[0];
        highBound = _errorValue[1];
      } else {
        lowBound = highBound = errorValue;
      }
      if (!isWellBehavedNumber(lowBound) || !isWellBehavedNumber(highBound)) {
        return void 0;
      }
      return [appliedNumericValue - lowBound, appliedNumericValue + highBound];
    }));
  }
  var selectTooltipAxis = (state) => {
    var axisType = selectTooltipAxisType(state);
    var axisId = selectTooltipAxisId(state);
    return selectRenderableAxisSettings(state, axisType, axisId);
  };
  var selectTooltipAxisDataKey = createSelector([selectTooltipAxis], (axis) => axis === null || axis === void 0 ? void 0 : axis.dataKey);
  var selectDisplayedStackedData = createSelector([selectStackedCartesianItemsSettings, selectChartDataWithIndexesIfNotInPanoramaPosition4, selectTooltipAxis], combineDisplayedStackedData);
  var combineStackGroups = (displayedData, items, stackOffsetType, reverseStackOrder) => {
    var initialItemsGroups = {};
    var itemsGroup = items.reduce((acc, item) => {
      if (item.stackId == null) {
        return acc;
      }
      var stack = acc[item.stackId];
      if (stack == null) {
        stack = [];
      }
      stack.push(item);
      acc[item.stackId] = stack;
      return acc;
    }, initialItemsGroups);
    return Object.fromEntries(Object.entries(itemsGroup).map((_ref3) => {
      var _ref4 = _slicedToArray9(_ref3, 2), stackId = _ref4[0], graphicalItems = _ref4[1];
      var orderedGraphicalItems = reverseStackOrder ? [...graphicalItems].reverse() : graphicalItems;
      var dataKeys = orderedGraphicalItems.map(getStackSeriesIdentifier);
      return [stackId, {
        // @ts-expect-error getStackedData requires that the input is array of objects, Recharts does not test for that
        stackedData: getStackedData(displayedData, dataKeys, stackOffsetType),
        graphicalItems: orderedGraphicalItems
      }];
    }));
  };
  var selectStackGroups = createSelector([selectDisplayedStackedData, selectStackedCartesianItemsSettings, selectStackOffsetType, selectReverseStackOrder], combineStackGroups);
  var combineDomainOfStackGroups = (stackGroups, _ref5, axisType, domainFromUserPreference) => {
    var dataStartIndex = _ref5.dataStartIndex, dataEndIndex = _ref5.dataEndIndex;
    if (domainFromUserPreference != null) {
      return void 0;
    }
    if (axisType === "zAxis") {
      return void 0;
    }
    return getDomainOfStackGroups(stackGroups, dataStartIndex, dataEndIndex);
  };
  var selectAllowsDataOverflow = createSelector([selectBaseAxis], (axisSettings) => axisSettings.allowDataOverflow);
  var getDomainDefinition = (axisSettings) => {
    var _axisSettings$domain;
    if (axisSettings == null || !("domain" in axisSettings)) {
      return defaultNumericDomain;
    }
    if (axisSettings.domain != null) {
      return axisSettings.domain;
    }
    if ("ticks" in axisSettings && axisSettings.ticks != null) {
      if (axisSettings.type === "number") {
        var allValues = onlyAllowNumbers(axisSettings.ticks);
        return [Math.min(...allValues), Math.max(...allValues)];
      }
      if (axisSettings.type === "category") {
        return axisSettings.ticks.map(String);
      }
    }
    return (_axisSettings$domain = axisSettings === null || axisSettings === void 0 ? void 0 : axisSettings.domain) !== null && _axisSettings$domain !== void 0 ? _axisSettings$domain : defaultNumericDomain;
  };
  var selectDomainDefinition = createSelector([selectBaseAxis], getDomainDefinition);
  var selectDomainFromUserPreference = createSelector([selectDomainDefinition, selectAllowsDataOverflow], numericalDomainSpecifiedWithoutRequiringData);
  var selectDomainOfStackGroups = createSelector([selectStackGroups, selectChartDataWithIndexes, pickAxisType, selectDomainFromUserPreference], combineDomainOfStackGroups, {
    memoizeOptions: {
      resultEqualityCheck: numberDomainEqualityCheck
    }
  });
  var selectAllErrorBarSettings = (state) => state.errorBars;
  var combineRelevantErrorBarSettings = (cartesianItemsSettings, allErrorBarSettings, axisType) => {
    return cartesianItemsSettings.flatMap((item) => {
      return allErrorBarSettings[item.id];
    }).filter(Boolean).filter((e) => {
      return isErrorBarRelevantForAxisType(axisType, e);
    });
  };
  var mergeDomains = function mergeDomains2() {
    for (var _len = arguments.length, domains = new Array(_len), _key = 0; _key < _len; _key++) {
      domains[_key] = arguments[_key];
    }
    var allDomains = domains.filter(Boolean);
    if (allDomains.length === 0) {
      return void 0;
    }
    var allValues = allDomains.flat();
    var min3 = Math.min(...allValues);
    var max4 = Math.max(...allValues);
    return [min3, max4];
  };
  var combineDomainOfAllAppliedNumericalValuesIncludingErrorValues = function combineDomainOfAllAppliedNumericalValuesIncludingErrorValues2(displayedData, axisSettings, items, errorBars, axisType) {
    var chartDataSlice2 = arguments.length > 5 && arguments[5] !== void 0 ? arguments[5] : [];
    var lowerEnd, upperEnd;
    if (items.length > 0) {
      items.forEach((item) => {
        var _errorBars$item$id;
        var itemData = item.data != null ? [...item.data] : chartDataSlice2;
        var relevantErrorBars = (_errorBars$item$id = errorBars[item.id]) === null || _errorBars$item$id === void 0 ? void 0 : _errorBars$item$id.filter((errorBar) => isErrorBarRelevantForAxisType(axisType, errorBar));
        itemData.forEach((entry) => {
          var _axisSettings$dataKey;
          var valueByDataKey = getValueByDataKey(entry, (_axisSettings$dataKey = axisSettings.dataKey) !== null && _axisSettings$dataKey !== void 0 ? _axisSettings$dataKey : item.dataKey);
          var errorDomain = getErrorDomainByDataKey(entry, valueByDataKey, relevantErrorBars);
          if (errorDomain.length >= 2) {
            var localLower = Math.min(...errorDomain);
            var localUpper = Math.max(...errorDomain);
            if (lowerEnd == null || localLower < lowerEnd) {
              lowerEnd = localLower;
            }
            if (upperEnd == null || localUpper > upperEnd) {
              upperEnd = localUpper;
            }
          }
          var dataValueDomain = makeDomain(valueByDataKey);
          if (dataValueDomain != null) {
            lowerEnd = lowerEnd == null ? dataValueDomain[0] : Math.min(lowerEnd, dataValueDomain[0]);
            upperEnd = upperEnd == null ? dataValueDomain[1] : Math.max(upperEnd, dataValueDomain[1]);
          }
        });
      });
    }
    if ((axisSettings === null || axisSettings === void 0 ? void 0 : axisSettings.dataKey) != null && items.length === 0) {
      displayedData.forEach((item) => {
        var dataValueDomain = makeDomain(getValueByDataKey(item, axisSettings.dataKey));
        if (dataValueDomain != null) {
          lowerEnd = lowerEnd == null ? dataValueDomain[0] : Math.min(lowerEnd, dataValueDomain[0]);
          upperEnd = upperEnd == null ? dataValueDomain[1] : Math.max(upperEnd, dataValueDomain[1]);
        }
      });
    }
    if (isWellBehavedNumber(lowerEnd) && isWellBehavedNumber(upperEnd)) {
      return [lowerEnd, upperEnd];
    }
    return void 0;
  };
  var selectDomainOfAllAppliedNumericalValuesIncludingErrorValues = createSelector([selectDisplayedData, selectBaseAxis, selectCartesianItemsSettingsExceptStacked, selectAllErrorBarSettings, pickAxisType, selectChartDataSliceIfNotInPanorama], combineDomainOfAllAppliedNumericalValuesIncludingErrorValues, {
    memoizeOptions: {
      resultEqualityCheck: numberDomainEqualityCheck
    }
  });
  function onlyAllowNumbersAndStringsAndDates(item) {
    var value2 = item.value;
    if (isNumOrStr(value2) || value2 instanceof Date) {
      return value2;
    }
    return void 0;
  }
  var computeDomainOfTypeCategory = (allDataSquished, axisSettings, isCategorical) => {
    var categoricalDomain = allDataSquished.map(onlyAllowNumbersAndStringsAndDates).filter((v) => v != null);
    if (isCategorical && (axisSettings.dataKey == null || axisSettings.allowDuplicatedCategory && hasDuplicate(categoricalDomain))) {
      return range(0, allDataSquished.length);
    }
    if (axisSettings.allowDuplicatedCategory) {
      return categoricalDomain;
    }
    return Array.from(new Set(categoricalDomain));
  };
  var selectReferenceDots = (state) => state.referenceElements.dots;
  var filterReferenceElements = (elements, axisType, axisId) => {
    return elements.filter((el) => el.ifOverflow === "extendDomain").filter((el) => {
      if (axisType === "xAxis") {
        return el.xAxisId === axisId;
      }
      return el.yAxisId === axisId;
    });
  };
  var selectReferenceDotsByAxis = createSelector([selectReferenceDots, pickAxisType, pickAxisId], filterReferenceElements);
  var selectReferenceAreas = (state) => state.referenceElements.areas;
  var selectReferenceAreasByAxis = createSelector([selectReferenceAreas, pickAxisType, pickAxisId], filterReferenceElements);
  var selectReferenceLines = (state) => state.referenceElements.lines;
  var selectReferenceLinesByAxis = createSelector([selectReferenceLines, pickAxisType, pickAxisId], filterReferenceElements);
  var combineDotsDomain = (dots, axisType) => {
    if (dots == null) {
      return void 0;
    }
    var allCoords = onlyAllowNumbers(dots.map((dot) => axisType === "xAxis" ? dot.x : dot.y));
    if (allCoords.length === 0) {
      return void 0;
    }
    return [Math.min(...allCoords), Math.max(...allCoords)];
  };
  var selectReferenceDotsDomain = createSelector(selectReferenceDotsByAxis, pickAxisType, combineDotsDomain);
  var combineAreasDomain = (areas, axisType) => {
    if (areas == null) {
      return void 0;
    }
    var allCoords = onlyAllowNumbers(areas.flatMap((area) => [axisType === "xAxis" ? area.x1 : area.y1, axisType === "xAxis" ? area.x2 : area.y2]));
    if (allCoords.length === 0) {
      return void 0;
    }
    return [Math.min(...allCoords), Math.max(...allCoords)];
  };
  var selectReferenceAreasDomain = createSelector([selectReferenceAreasByAxis, pickAxisType], combineAreasDomain);
  function extractXCoordinates(line) {
    var _line$segment;
    if (line.x != null) {
      return onlyAllowNumbers([line.x]);
    }
    var segmentCoordinates = (_line$segment = line.segment) === null || _line$segment === void 0 ? void 0 : _line$segment.map((s) => s.x);
    if (segmentCoordinates == null || segmentCoordinates.length === 0) {
      return [];
    }
    return onlyAllowNumbers(segmentCoordinates);
  }
  function extractYCoordinates(line) {
    var _line$segment2;
    if (line.y != null) {
      return onlyAllowNumbers([line.y]);
    }
    var segmentCoordinates = (_line$segment2 = line.segment) === null || _line$segment2 === void 0 ? void 0 : _line$segment2.map((s) => s.y);
    if (segmentCoordinates == null || segmentCoordinates.length === 0) {
      return [];
    }
    return onlyAllowNumbers(segmentCoordinates);
  }
  var combineLinesDomain = (lines, axisType) => {
    if (lines == null) {
      return void 0;
    }
    var allCoords = lines.flatMap((line) => axisType === "xAxis" ? extractXCoordinates(line) : extractYCoordinates(line));
    if (allCoords.length === 0) {
      return void 0;
    }
    return [Math.min(...allCoords), Math.max(...allCoords)];
  };
  var selectReferenceLinesDomain = createSelector([selectReferenceLinesByAxis, pickAxisType], combineLinesDomain);
  var selectReferenceElementsDomain = createSelector(selectReferenceDotsDomain, selectReferenceLinesDomain, selectReferenceAreasDomain, (dotsDomain, linesDomain, areasDomain) => {
    return mergeDomains(dotsDomain, areasDomain, linesDomain);
  });
  var combineNumericalDomain = (axisSettings, domainDefinition, domainFromUserPreference, domainOfStackGroups, dataAndErrorBarsDomain, referenceElementsDomain, layout, axisType, numericTicksDomain) => {
    if (domainFromUserPreference != null) {
      return domainFromUserPreference;
    }
    var shouldIncludeDomainOfStackGroups = layout === "vertical" && axisType === "xAxis" || layout === "horizontal" && axisType === "yAxis";
    var mergedDomains = shouldIncludeDomainOfStackGroups ? mergeDomains(domainOfStackGroups, referenceElementsDomain, dataAndErrorBarsDomain) : mergeDomains(referenceElementsDomain, dataAndErrorBarsDomain);
    var parsedDomain = parseNumericalUserDomain(domainDefinition, mergedDomains, axisSettings.allowDataOverflow);
    if (parsedDomain != null) {
      return parsedDomain;
    }
    if (axisSettings.allowDataOverflow && mergedDomains == null && numericTicksDomain != null) {
      return numericTicksDomain;
    }
    return parsedDomain;
  };
  var combineNumericTicksDomain = (axisSettings) => {
    if (axisSettings == null || axisSettings.type !== "number" || !("ticks" in axisSettings) || axisSettings.ticks == null) {
      return void 0;
    }
    var numericTicks = onlyAllowNumbers(axisSettings.ticks);
    if (numericTicks.length === 0) {
      return void 0;
    }
    return [Math.min(...numericTicks), Math.max(...numericTicks)];
  };
  var selectNumericTicksDomain = createSelector([selectBaseAxis], combineNumericTicksDomain, {
    memoizeOptions: {
      resultEqualityCheck: numberDomainEqualityCheck
    }
  });
  var selectNumericalDomain = createSelector([selectBaseAxis, selectDomainDefinition, selectDomainFromUserPreference, selectDomainOfStackGroups, selectDomainOfAllAppliedNumericalValuesIncludingErrorValues, selectReferenceElementsDomain, selectChartLayout, pickAxisType, selectNumericTicksDomain], combineNumericalDomain, {
    memoizeOptions: {
      resultEqualityCheck: numberDomainEqualityCheck
    }
  });
  var expandDomain = [0, 1];
  var combineAxisDomain = (axisSettings, layout, displayedData, allAppliedValues, stackOffsetType, axisType, numericalDomain) => {
    if ((axisSettings == null || displayedData == null || displayedData.length === 0) && numericalDomain === void 0) {
      return void 0;
    }
    var dataKey = axisSettings.dataKey, type = axisSettings.type;
    var isCategorical = isCategoricalAxis(layout, axisType);
    if (isCategorical && dataKey == null) {
      var _displayedData$length;
      return range(0, (_displayedData$length = displayedData === null || displayedData === void 0 ? void 0 : displayedData.length) !== null && _displayedData$length !== void 0 ? _displayedData$length : 0);
    }
    if (type === "category") {
      return computeDomainOfTypeCategory(allAppliedValues, axisSettings, isCategorical);
    }
    if (stackOffsetType === "expand" && !isCategorical) {
      return expandDomain;
    }
    return numericalDomain;
  };
  var selectAxisDomain = createSelector([selectBaseAxis, selectChartLayout, selectDisplayedData, selectAllAppliedValues, selectStackOffsetType, pickAxisType, selectNumericalDomain], combineAxisDomain);
  var selectRealScaleType = createSelector([selectBaseAxis, selectHasBar, selectChartName], combineRealScaleType);
  var combineNiceTicks = (axisDomain, axisSettings, realScaleType) => {
    var niceTicks = axisSettings.niceTicks;
    if (niceTicks === "none") {
      return void 0;
    }
    var domainDefinition = getDomainDefinition(axisSettings);
    var hasDomainAutoKeyword = Array.isArray(domainDefinition) && (domainDefinition[0] === "auto" || domainDefinition[1] === "auto");
    if ((niceTicks === "snap125" || niceTicks === "adaptive") && axisSettings != null && axisSettings.tickCount && isWellFormedNumberDomain(axisDomain)) {
      if (hasDomainAutoKeyword) {
        return getNiceTickValues(axisDomain, axisSettings.tickCount, axisSettings.allowDecimals, niceTicks);
      }
      if (axisSettings.type === "number") {
        return getTickValuesFixedDomain(axisDomain, axisSettings.tickCount, axisSettings.allowDecimals, niceTicks);
      }
    }
    if (niceTicks === "auto" && realScaleType === "linear" && axisSettings != null && axisSettings.tickCount) {
      if (hasDomainAutoKeyword && isWellFormedNumberDomain(axisDomain)) {
        return getNiceTickValues(axisDomain, axisSettings.tickCount, axisSettings.allowDecimals, "adaptive");
      }
      if (axisSettings.type === "number" && isWellFormedNumberDomain(axisDomain)) {
        return getTickValuesFixedDomain(axisDomain, axisSettings.tickCount, axisSettings.allowDecimals, "adaptive");
      }
    }
    return void 0;
  };
  var selectNiceTicks = createSelector([selectAxisDomain, selectRenderableAxisSettings, selectRealScaleType], combineNiceTicks);
  var combineAxisDomainWithNiceTicks = (axisSettings, domain, niceTicks, axisType) => {
    if (
      /*
       * Angle axis for some reason uses nice ticks when rendering axis tick labels,
       * but doesn't use nice ticks for extending domain like all the other axes do.
       * Not really sure why? Is there a good reason,
       * or is it just because someone added support for nice ticks to the other axes and forgot this one?
       */
      axisType !== "angleAxis" && (axisSettings === null || axisSettings === void 0 ? void 0 : axisSettings.type) === "number" && isWellFormedNumberDomain(domain) && Array.isArray(niceTicks) && niceTicks.length > 0
    ) {
      var _niceTicks$, _niceTicks;
      var minFromDomain = domain[0];
      var minFromTicks = (_niceTicks$ = niceTicks[0]) !== null && _niceTicks$ !== void 0 ? _niceTicks$ : 0;
      var maxFromDomain = domain[1];
      var maxFromTicks = (_niceTicks = niceTicks[niceTicks.length - 1]) !== null && _niceTicks !== void 0 ? _niceTicks : 0;
      return [Math.min(minFromDomain, minFromTicks), Math.max(maxFromDomain, maxFromTicks)];
    }
    return domain;
  };
  var selectAxisDomainIncludingNiceTicks = createSelector([selectBaseAxis, selectAxisDomain, selectNiceTicks, pickAxisType], combineAxisDomainWithNiceTicks);
  var selectSmallestDistanceBetweenValues = createSelector(selectAllAppliedValues, selectBaseAxis, (allDataSquished, axisSettings) => {
    if (!axisSettings || axisSettings.type !== "number") {
      return void 0;
    }
    var smallestDistanceBetweenValues = Infinity;
    var sortedValues = Array.from(onlyAllowNumbers(allDataSquished.map((d) => d.value))).sort((a2, b) => a2 - b);
    var first = sortedValues[0];
    var last = sortedValues[sortedValues.length - 1];
    if (first == null || last == null) {
      return Infinity;
    }
    var diff = last - first;
    if (diff === 0) {
      return Infinity;
    }
    for (var i = 0; i < sortedValues.length - 1; i++) {
      var curr = sortedValues[i];
      var next = sortedValues[i + 1];
      if (curr == null || next == null) {
        continue;
      }
      var distance = next - curr;
      smallestDistanceBetweenValues = Math.min(smallestDistanceBetweenValues, distance);
    }
    return smallestDistanceBetweenValues / diff;
  });
  var selectCalculatedPadding = createSelector(selectSmallestDistanceBetweenValues, selectChartLayout, selectBarCategoryGap, selectChartOffsetInternal, (_1, _2, _3, _4, padding) => padding, (smallestDistanceInPercent, layout, barCategoryGap, offset, padding) => {
    if (!isWellBehavedNumber(smallestDistanceInPercent)) {
      return 0;
    }
    var rangeWidth = layout === "vertical" ? offset.height : offset.width;
    if (padding === "gap") {
      return smallestDistanceInPercent * rangeWidth / 2;
    }
    if (padding === "no-gap") {
      var gap = getPercentValue(barCategoryGap, smallestDistanceInPercent * rangeWidth);
      var halfBand = smallestDistanceInPercent * rangeWidth / 2;
      return halfBand - gap - (halfBand - gap) / rangeWidth * gap;
    }
    return 0;
  });
  var selectCalculatedXAxisPadding = (state, axisId, isPanorama) => {
    var xAxisSettings = selectXAxisSettings(state, axisId);
    if (xAxisSettings == null || typeof xAxisSettings.padding !== "string") {
      return 0;
    }
    return selectCalculatedPadding(state, "xAxis", axisId, isPanorama, xAxisSettings.padding);
  };
  var selectCalculatedYAxisPadding = (state, axisId, isPanorama) => {
    var yAxisSettings = selectYAxisSettings(state, axisId);
    if (yAxisSettings == null || typeof yAxisSettings.padding !== "string") {
      return 0;
    }
    return selectCalculatedPadding(state, "yAxis", axisId, isPanorama, yAxisSettings.padding);
  };
  var selectXAxisPadding = createSelector(selectXAxisSettings, selectCalculatedXAxisPadding, (xAxisSettings, calculated) => {
    var _padding$left, _padding$right;
    if (xAxisSettings == null) {
      return {
        left: 0,
        right: 0
      };
    }
    var padding = xAxisSettings.padding;
    if (typeof padding === "string") {
      return {
        left: calculated,
        right: calculated
      };
    }
    return {
      left: ((_padding$left = padding.left) !== null && _padding$left !== void 0 ? _padding$left : 0) + calculated,
      right: ((_padding$right = padding.right) !== null && _padding$right !== void 0 ? _padding$right : 0) + calculated
    };
  });
  var selectYAxisPadding = createSelector(selectYAxisSettings, selectCalculatedYAxisPadding, (yAxisSettings, calculated) => {
    var _padding$top, _padding$bottom;
    if (yAxisSettings == null) {
      return {
        top: 0,
        bottom: 0
      };
    }
    var padding = yAxisSettings.padding;
    if (typeof padding === "string") {
      return {
        top: calculated,
        bottom: calculated
      };
    }
    return {
      top: ((_padding$top = padding.top) !== null && _padding$top !== void 0 ? _padding$top : 0) + calculated,
      bottom: ((_padding$bottom = padding.bottom) !== null && _padding$bottom !== void 0 ? _padding$bottom : 0) + calculated
    };
  });
  var selectXAxisRange = createSelector([selectChartOffsetInternal, selectXAxisPadding, selectBrushDimensions, selectBrushSettings, (_state, _axisId, isPanorama) => isPanorama], (offset, padding, brushDimensions, _ref6, isPanorama) => {
    var brushPadding = _ref6.padding;
    if (isPanorama) {
      return [brushPadding.left, brushDimensions.width - brushPadding.right];
    }
    return [offset.left + padding.left, offset.left + offset.width - padding.right];
  });
  var selectYAxisRange = createSelector([selectChartOffsetInternal, selectChartLayout, selectYAxisPadding, selectBrushDimensions, selectBrushSettings, (_state, _axisId, isPanorama) => isPanorama], (offset, layout, padding, brushDimensions, _ref7, isPanorama) => {
    var brushPadding = _ref7.padding;
    if (isPanorama) {
      return [brushDimensions.height - brushPadding.bottom, brushPadding.top];
    }
    if (layout === "horizontal") {
      return [offset.top + offset.height - padding.bottom, offset.top + padding.top];
    }
    return [offset.top + padding.top, offset.top + offset.height - padding.bottom];
  });
  var selectAxisRange = (state, axisType, axisId, isPanorama) => {
    var _selectZAxisSettings;
    switch (axisType) {
      case "xAxis":
        return selectXAxisRange(state, axisId, isPanorama);
      case "yAxis":
        return selectYAxisRange(state, axisId, isPanorama);
      case "zAxis":
        return (_selectZAxisSettings = selectZAxisSettings(state, axisId)) === null || _selectZAxisSettings === void 0 ? void 0 : _selectZAxisSettings.range;
      case "angleAxis":
        return selectAngleAxisRange(state);
      case "radiusAxis":
        return selectRadiusAxisRange(state, axisId);
      default:
        return void 0;
    }
  };
  var selectAxisRangeWithReverse = createSelector([selectBaseAxis, selectAxisRange], combineAxisRangeWithReverse);
  var selectCheckedAxisDomain = createSelector([selectRealScaleType, selectAxisDomainIncludingNiceTicks], combineCheckedDomain);
  var selectConfiguredScale = createSelector([selectBaseAxis, selectRealScaleType, selectCheckedAxisDomain, selectAxisRangeWithReverse], combineConfiguredScale);
  var combineCategoricalDomain = (layout, appliedValues, axis, axisType) => {
    if (axis == null || axis.dataKey == null) {
      return void 0;
    }
    var type = axis.type, scale = axis.scale;
    var isCategorical = isCategoricalAxis(layout, axisType);
    if (isCategorical && (type === "number" || scale !== "auto")) {
      return appliedValues.map((d) => d.value);
    }
    return void 0;
  };
  var selectCategoricalDomain = createSelector([selectChartLayout, selectAllAppliedValues, selectRenderableAxisSettings, pickAxisType], combineCategoricalDomain);
  var selectAxisScale = createSelector([selectConfiguredScale], rechartsScaleFactory);
  var selectAxisInverseScale = createSelector([selectConfiguredScale], combineInverseScaleFunction);
  var selectAxisInverseDataSnapScale = createSelector([selectConfiguredScale, selectSortedDataPoints], createCategoricalInverse);
  var selectErrorBarsSettings = createSelector([selectCartesianItemsSettings, selectAllErrorBarSettings, pickAxisType], combineRelevantErrorBarSettings);
  function compareIds(a2, b) {
    if (a2.id < b.id) {
      return -1;
    }
    if (a2.id > b.id) {
      return 1;
    }
    return 0;
  }
  var pickAxisOrientation = (_state, orientation) => orientation;
  var pickMirror = (_state, _orientation, mirror) => mirror;
  var selectAllXAxesWithOffsetType = createSelector(selectAllXAxes, pickAxisOrientation, pickMirror, (allAxes, orientation, mirror) => allAxes.filter((axis) => axis.orientation === orientation).filter((axis) => axis.mirror === mirror).sort(compareIds));
  var selectAllYAxesWithOffsetType = createSelector(selectAllYAxes, pickAxisOrientation, pickMirror, (allAxes, orientation, mirror) => allAxes.filter((axis) => axis.orientation === orientation).filter((axis) => axis.mirror === mirror).sort(compareIds));
  var getXAxisSize = (offset, axisSettings) => {
    return {
      width: offset.width,
      height: axisSettings.height
    };
  };
  var getYAxisSize = (offset, axisSettings) => {
    var width = typeof axisSettings.width === "number" ? axisSettings.width : DEFAULT_Y_AXIS_WIDTH;
    return {
      width,
      height: offset.height
    };
  };
  var selectXAxisSize = createSelector(selectChartOffsetInternal, selectXAxisSettings, getXAxisSize);
  var combineXAxisPositionStartingPoint = (offset, orientation, chartHeight) => {
    switch (orientation) {
      case "top":
        return offset.top;
      case "bottom":
        return chartHeight - offset.bottom;
      default:
        return 0;
    }
  };
  var combineYAxisPositionStartingPoint = (offset, orientation, chartWidth) => {
    switch (orientation) {
      case "left":
        return offset.left;
      case "right":
        return chartWidth - offset.right;
      default:
        return 0;
    }
  };
  var selectAllXAxesOffsetSteps = createSelector(selectChartHeight, selectChartOffsetInternal, selectAllXAxesWithOffsetType, pickAxisOrientation, pickMirror, (chartHeight, offset, allAxesWithSameOffsetType, orientation, mirror) => {
    var steps = {};
    var position;
    allAxesWithSameOffsetType.forEach((axis) => {
      var axisSize = getXAxisSize(offset, axis);
      if (position == null) {
        position = combineXAxisPositionStartingPoint(offset, orientation, chartHeight);
      }
      var needSpace = orientation === "top" && !mirror || orientation === "bottom" && mirror;
      steps[axis.id] = position - Number(needSpace) * axisSize.height;
      position += (needSpace ? -1 : 1) * axisSize.height;
    });
    return steps;
  });
  var selectAllYAxesOffsetSteps = createSelector(selectChartWidth, selectChartOffsetInternal, selectAllYAxesWithOffsetType, pickAxisOrientation, pickMirror, (chartWidth, offset, allAxesWithSameOffsetType, orientation, mirror) => {
    var steps = {};
    var position;
    allAxesWithSameOffsetType.forEach((axis) => {
      var axisSize = getYAxisSize(offset, axis);
      if (position == null) {
        position = combineYAxisPositionStartingPoint(offset, orientation, chartWidth);
      }
      var needSpace = orientation === "left" && !mirror || orientation === "right" && mirror;
      steps[axis.id] = position - Number(needSpace) * axisSize.width;
      position += (needSpace ? -1 : 1) * axisSize.width;
    });
    return steps;
  });
  var selectXAxisOffsetSteps = (state, axisId) => {
    var axisSettings = selectXAxisSettings(state, axisId);
    if (axisSettings == null) {
      return void 0;
    }
    return selectAllXAxesOffsetSteps(state, axisSettings.orientation, axisSettings.mirror);
  };
  var selectXAxisPosition = createSelector([selectChartOffsetInternal, selectXAxisSettings, selectXAxisOffsetSteps, (_, axisId) => axisId], (offset, axisSettings, allSteps, axisId) => {
    if (axisSettings == null) {
      return void 0;
    }
    var stepOfThisAxis = allSteps === null || allSteps === void 0 ? void 0 : allSteps[axisId];
    if (stepOfThisAxis == null) {
      return {
        x: offset.left,
        y: 0
      };
    }
    return {
      x: offset.left,
      y: stepOfThisAxis
    };
  });
  var selectYAxisOffsetSteps = (state, axisId) => {
    var axisSettings = selectYAxisSettings(state, axisId);
    if (axisSettings == null) {
      return void 0;
    }
    return selectAllYAxesOffsetSteps(state, axisSettings.orientation, axisSettings.mirror);
  };
  var selectYAxisPosition = createSelector([selectChartOffsetInternal, selectYAxisSettings, selectYAxisOffsetSteps, (_, axisId) => axisId], (offset, axisSettings, allSteps, axisId) => {
    if (axisSettings == null) {
      return void 0;
    }
    var stepOfThisAxis = allSteps === null || allSteps === void 0 ? void 0 : allSteps[axisId];
    if (stepOfThisAxis == null) {
      return {
        x: 0,
        y: offset.top
      };
    }
    return {
      x: stepOfThisAxis,
      y: offset.top
    };
  });
  var selectYAxisSize = createSelector(selectChartOffsetInternal, selectYAxisSettings, (offset, axisSettings) => {
    var width = typeof axisSettings.width === "number" ? axisSettings.width : DEFAULT_Y_AXIS_WIDTH;
    return {
      width,
      height: offset.height
    };
  });
  var selectCartesianAxisSize = (state, axisType, axisId) => {
    switch (axisType) {
      case "xAxis": {
        return selectXAxisSize(state, axisId).width;
      }
      case "yAxis": {
        return selectYAxisSize(state, axisId).height;
      }
      default: {
        return void 0;
      }
    }
  };
  var combineDuplicateDomain = (chartLayout, appliedValues, axis, axisType) => {
    if (axis == null) {
      return void 0;
    }
    var allowDuplicatedCategory = axis.allowDuplicatedCategory, type = axis.type, dataKey = axis.dataKey;
    var isCategorical = isCategoricalAxis(chartLayout, axisType);
    var allData = appliedValues.map((av) => av.value);
    var validData = allData.filter((v) => v != null);
    if (dataKey && isCategorical && type === "category" && allowDuplicatedCategory && hasDuplicate(validData)) {
      return allData;
    }
    return void 0;
  };
  var selectDuplicateDomain = createSelector([selectChartLayout, selectAllAppliedValues, selectBaseAxis, pickAxisType], combineDuplicateDomain);
  var selectAxisPropsNeededForCartesianGridTicksGenerator = createSelector([selectChartLayout, selectCartesianAxisSettings, selectRealScaleType, selectAxisScale, selectDuplicateDomain, selectCategoricalDomain, selectAxisRange, selectNiceTicks, pickAxisType], (layout, axis, realScaleType, scale, duplicateDomain, categoricalDomain, axisRange, niceTicks, axisType) => {
    if (axis == null) {
      return void 0;
    }
    var isCategorical = isCategoricalAxis(layout, axisType);
    return {
      angle: axis.angle,
      interval: axis.interval,
      minTickGap: axis.minTickGap,
      orientation: axis.orientation,
      tick: axis.tick,
      tickCount: axis.tickCount,
      tickFormatter: axis.tickFormatter,
      ticks: axis.ticks,
      type: axis.type,
      unit: axis.unit,
      axisType,
      categoricalDomain,
      duplicateDomain,
      isCategorical,
      niceTicks,
      range: axisRange,
      realScaleType,
      scale
    };
  });
  var combineAxisTicks = (layout, axis, realScaleType, scale, niceTicks, axisRange, duplicateDomain, categoricalDomain, axisType) => {
    if (axis == null || scale == null) {
      return void 0;
    }
    var isCategorical = isCategoricalAxis(layout, axisType);
    var type = axis.type, ticks = axis.ticks, tickCount = axis.tickCount;
    var offsetForBand = (
      // @ts-expect-error This is testing for `scaleBand` but for band axis the type is reported as `band` so this looks like a dead code with a workaround elsewhere?
      realScaleType === "scaleBand" && typeof scale.bandwidth === "function" ? scale.bandwidth() / 2 : 2
    );
    var offset = type === "category" && scale.bandwidth ? scale.bandwidth() / offsetForBand : 0;
    offset = axisType === "angleAxis" && axisRange != null && axisRange.length >= 2 ? mathSign(axisRange[0] - axisRange[1]) * 2 * offset : offset;
    var ticksOrNiceTicks = ticks || niceTicks;
    if (ticksOrNiceTicks) {
      return ticksOrNiceTicks.map((entry, index2) => {
        var scaleContent = duplicateDomain ? duplicateDomain.indexOf(entry) : entry;
        var scaled = scale.map(scaleContent);
        if (!isWellBehavedNumber(scaled)) {
          return null;
        }
        return {
          index: index2,
          coordinate: scaled + offset,
          value: entry,
          offset
        };
      }).filter(isNotNil);
    }
    if (isCategorical && categoricalDomain) {
      return categoricalDomain.map((entry, index2) => {
        var scaled = scale.map(entry);
        if (!isWellBehavedNumber(scaled)) {
          return null;
        }
        return {
          coordinate: scaled + offset,
          value: entry,
          index: index2,
          offset
        };
      }).filter(isNotNil);
    }
    if (scale.ticks) {
      return scale.ticks(tickCount).map((entry, index2) => {
        var scaled = scale.map(entry);
        if (!isWellBehavedNumber(scaled)) {
          return null;
        }
        return {
          coordinate: scaled + offset,
          value: entry,
          index: index2,
          offset
        };
      }).filter(isNotNil);
    }
    return scale.domain().map((entry, index2) => {
      var scaled = scale.map(entry);
      if (!isWellBehavedNumber(scaled)) {
        return null;
      }
      return {
        coordinate: scaled + offset,
        // @ts-expect-error can't use Date as index
        value: duplicateDomain ? duplicateDomain[entry] : entry,
        index: index2,
        offset
      };
    }).filter(isNotNil);
  };
  var selectTicksOfAxis = createSelector([selectChartLayout, selectRenderableAxisSettings, selectRealScaleType, selectAxisScale, selectNiceTicks, selectAxisRange, selectDuplicateDomain, selectCategoricalDomain, pickAxisType], combineAxisTicks);
  var combineGraphicalItemTicks = (layout, axis, scale, axisRange, duplicateDomain, categoricalDomain, axisType) => {
    if (axis == null || scale == null || axisRange == null || axisRange[0] === axisRange[1]) {
      return void 0;
    }
    var isCategorical = isCategoricalAxis(layout, axisType);
    var tickCount = axis.tickCount;
    var offset = 0;
    offset = axisType === "angleAxis" && (axisRange === null || axisRange === void 0 ? void 0 : axisRange.length) >= 2 ? mathSign(axisRange[0] - axisRange[1]) * 2 * offset : offset;
    if (isCategorical && categoricalDomain) {
      return categoricalDomain.map((entry, index2) => {
        var scaled = scale.map(entry);
        if (!isWellBehavedNumber(scaled)) {
          return null;
        }
        return {
          coordinate: scaled + offset,
          value: entry,
          index: index2,
          offset
        };
      }).filter(isNotNil);
    }
    if (scale.ticks) {
      return scale.ticks(tickCount).map((entry, index2) => {
        var scaled = scale.map(entry);
        if (!isWellBehavedNumber(scaled)) {
          return null;
        }
        return {
          coordinate: scaled + offset,
          value: entry,
          index: index2,
          offset
        };
      }).filter(isNotNil);
    }
    return scale.domain().map((entry, index2) => {
      var scaled = scale.map(entry);
      if (!isWellBehavedNumber(scaled)) {
        return null;
      }
      return {
        coordinate: scaled + offset,
        // @ts-expect-error can't use unknown as index
        value: duplicateDomain ? duplicateDomain[entry] : entry,
        index: index2,
        offset
      };
    }).filter(isNotNil);
  };
  var selectTicksOfGraphicalItem = createSelector([selectChartLayout, selectRenderableAxisSettings, selectAxisScale, selectAxisRange, selectDuplicateDomain, selectCategoricalDomain, pickAxisType], combineGraphicalItemTicks);
  var selectAxisWithScale = createSelector(selectBaseAxis, selectAxisScale, (axis, scale) => {
    if (axis == null || scale == null) {
      return void 0;
    }
    return _objectSpread11(_objectSpread11({}, axis), {}, {
      scale
    });
  });
  var selectZAxisConfiguredScale = createSelector([selectBaseAxis, selectRealScaleType, selectAxisDomain, selectAxisRangeWithReverse], combineConfiguredScale);
  var selectZAxisScale = createSelector([selectZAxisConfiguredScale], rechartsScaleFactory);
  var selectZAxisWithScale = createSelector((state, _axisType, axisId) => selectZAxisSettings(state, axisId), selectZAxisScale, (axis, scale) => {
    if (axis == null || scale == null) {
      return void 0;
    }
    return _objectSpread11(_objectSpread11({}, axis), {}, {
      scale
    });
  });
  var selectChartDirection = createSelector([selectChartLayout, selectAllXAxes, selectAllYAxes], (layout, allXAxes, allYAxes) => {
    switch (layout) {
      case "horizontal": {
        return allXAxes.some((axis) => axis.reversed) ? "right-to-left" : "left-to-right";
      }
      case "vertical": {
        return allYAxes.some((axis) => axis.reversed) ? "bottom-to-top" : "top-to-bottom";
      }
      // TODO: make this better. For now, right arrow triggers "forward", left arrow "back"
      // however, the tooltip moves an unintuitive direction because of how the indices are rendered
      case "centric":
      case "radial": {
        return "left-to-right";
      }
      default: {
        return void 0;
      }
    }
  });
  var selectRenderedTicksOfAxis = (state, axisType, axisId) => {
    var _state$renderedTicks$;
    return (_state$renderedTicks$ = state.renderedTicks[axisType]) === null || _state$renderedTicks$ === void 0 ? void 0 : _state$renderedTicks$[axisId];
  };
  var selectAxisInverseTickSnapScale = createSelector([selectRenderedTicksOfAxis], (ticks) => {
    if (!ticks || ticks.length === 0) {
      return void 0;
    }
    return (pixelValue) => {
      var _closestTick;
      var minDistance = Infinity;
      var closestTick = ticks[0];
      for (var tick of ticks) {
        var distance = Math.abs(tick.coordinate - pixelValue);
        if (distance < minDistance) {
          minDistance = distance;
          closestTick = tick;
        }
      }
      return (_closestTick = closestTick) === null || _closestTick === void 0 ? void 0 : _closestTick.value;
    };
  });

  // node_modules/recharts/es6/state/selectors/selectTooltipEventType.js
  var selectDefaultTooltipEventType = (state) => state.options.defaultTooltipEventType;
  var selectValidateTooltipEventTypes = (state) => state.options.validateTooltipEventTypes;
  function combineTooltipEventType(shared, defaultTooltipEventType, validateTooltipEventTypes) {
    if (shared == null) {
      return defaultTooltipEventType;
    }
    var eventType = shared ? "axis" : "item";
    if (validateTooltipEventTypes == null) {
      return defaultTooltipEventType;
    }
    return validateTooltipEventTypes.includes(eventType) ? eventType : defaultTooltipEventType;
  }
  function selectTooltipEventType(state, shared) {
    var defaultTooltipEventType = selectDefaultTooltipEventType(state);
    var validateTooltipEventTypes = selectValidateTooltipEventTypes(state);
    return combineTooltipEventType(shared, defaultTooltipEventType, validateTooltipEventTypes);
  }
  function useTooltipEventType(shared) {
    return useAppSelector((state) => selectTooltipEventType(state, shared));
  }

  // node_modules/recharts/es6/state/selectors/combiners/combineActiveLabel.js
  var combineActiveLabel = (tooltipTicks, activeIndex) => {
    var _tooltipTicks$n;
    var n = Number(activeIndex);
    if (isNan(n) || activeIndex == null) {
      return void 0;
    }
    return n >= 0 ? tooltipTicks === null || tooltipTicks === void 0 || (_tooltipTicks$n = tooltipTicks[n]) === null || _tooltipTicks$n === void 0 ? void 0 : _tooltipTicks$n.value : void 0;
  };

  // node_modules/recharts/es6/state/selectors/selectTooltipSettings.js
  var selectTooltipSettings = (state) => state.tooltip.settings;

  // node_modules/recharts/es6/state/tooltipSlice.js
  var noInteraction = {
    active: false,
    index: null,
    dataKey: void 0,
    graphicalItemId: void 0,
    coordinate: void 0
  };
  var initialState3 = {
    itemInteraction: {
      click: noInteraction,
      hover: noInteraction
    },
    axisInteraction: {
      click: noInteraction,
      hover: noInteraction
    },
    keyboardInteraction: noInteraction,
    syncInteraction: {
      active: false,
      index: null,
      dataKey: void 0,
      label: void 0,
      coordinate: void 0,
      sourceViewBox: void 0,
      graphicalItemId: void 0
    },
    tooltipItemPayloads: [],
    settings: {
      shared: void 0,
      trigger: "hover",
      axisId: 0,
      active: false,
      defaultIndex: void 0
    }
  };
  var tooltipSlice = createSlice({
    name: "tooltip",
    initialState: initialState3,
    reducers: {
      addTooltipEntrySettings: {
        reducer(state, action) {
          state.tooltipItemPayloads.push(castDraft(action.payload));
        },
        prepare: prepareAutoBatched()
      },
      replaceTooltipEntrySettings: {
        reducer(state, action) {
          var _action$payload = action.payload, prev = _action$payload.prev, next = _action$payload.next;
          var index2 = current(state).tooltipItemPayloads.indexOf(castDraft(prev));
          if (index2 > -1) {
            state.tooltipItemPayloads[index2] = castDraft(next);
          }
        },
        prepare: prepareAutoBatched()
      },
      removeTooltipEntrySettings: {
        reducer(state, action) {
          var index2 = current(state).tooltipItemPayloads.indexOf(castDraft(action.payload));
          if (index2 > -1) {
            state.tooltipItemPayloads.splice(index2, 1);
          }
        },
        prepare: prepareAutoBatched()
      },
      setTooltipSettingsState(state, action) {
        state.settings = action.payload;
      },
      setActiveMouseOverItemIndex(state, action) {
        state.syncInteraction.active = false;
        state.syncInteraction.sourceViewBox = void 0;
        state.keyboardInteraction.active = false;
        state.itemInteraction.hover.active = true;
        state.itemInteraction.hover.index = action.payload.activeIndex;
        state.itemInteraction.hover.dataKey = action.payload.activeDataKey;
        state.itemInteraction.hover.graphicalItemId = action.payload.activeGraphicalItemId;
        state.itemInteraction.hover.coordinate = action.payload.activeCoordinate;
      },
      mouseLeaveChart(state) {
        state.itemInteraction.hover.active = false;
        state.axisInteraction.hover.active = false;
      },
      mouseLeaveItem(state) {
        state.itemInteraction.hover.active = false;
      },
      setActiveClickItemIndex(state, action) {
        state.syncInteraction.active = false;
        state.syncInteraction.sourceViewBox = void 0;
        state.itemInteraction.click.active = true;
        state.keyboardInteraction.active = false;
        state.itemInteraction.click.index = action.payload.activeIndex;
        state.itemInteraction.click.dataKey = action.payload.activeDataKey;
        state.itemInteraction.click.graphicalItemId = action.payload.activeGraphicalItemId;
        state.itemInteraction.click.coordinate = action.payload.activeCoordinate;
      },
      setMouseOverAxisIndex(state, action) {
        state.syncInteraction.active = false;
        state.syncInteraction.sourceViewBox = void 0;
        state.axisInteraction.hover.active = true;
        state.keyboardInteraction.active = false;
        state.axisInteraction.hover.index = action.payload.activeIndex;
        state.axisInteraction.hover.dataKey = action.payload.activeDataKey;
        state.axisInteraction.hover.coordinate = action.payload.activeCoordinate;
      },
      setMouseClickAxisIndex(state, action) {
        state.syncInteraction.active = false;
        state.syncInteraction.sourceViewBox = void 0;
        state.keyboardInteraction.active = false;
        state.axisInteraction.click.active = true;
        state.axisInteraction.click.index = action.payload.activeIndex;
        state.axisInteraction.click.dataKey = action.payload.activeDataKey;
        state.axisInteraction.click.coordinate = action.payload.activeCoordinate;
      },
      setSyncInteraction(state, action) {
        state.syncInteraction = action.payload;
      },
      setKeyboardInteraction(state, action) {
        state.keyboardInteraction.active = action.payload.active;
        state.keyboardInteraction.index = action.payload.activeIndex;
        state.keyboardInteraction.coordinate = action.payload.activeCoordinate;
      }
    }
  });
  var _tooltipSlice$actions = tooltipSlice.actions;
  var addTooltipEntrySettings = _tooltipSlice$actions.addTooltipEntrySettings;
  var replaceTooltipEntrySettings = _tooltipSlice$actions.replaceTooltipEntrySettings;
  var removeTooltipEntrySettings = _tooltipSlice$actions.removeTooltipEntrySettings;
  var setTooltipSettingsState = _tooltipSlice$actions.setTooltipSettingsState;
  var setActiveMouseOverItemIndex = _tooltipSlice$actions.setActiveMouseOverItemIndex;
  var mouseLeaveItem = _tooltipSlice$actions.mouseLeaveItem;
  var mouseLeaveChart = _tooltipSlice$actions.mouseLeaveChart;
  var setActiveClickItemIndex = _tooltipSlice$actions.setActiveClickItemIndex;
  var setMouseOverAxisIndex = _tooltipSlice$actions.setMouseOverAxisIndex;
  var setMouseClickAxisIndex = _tooltipSlice$actions.setMouseClickAxisIndex;
  var setSyncInteraction = _tooltipSlice$actions.setSyncInteraction;
  var setKeyboardInteraction = _tooltipSlice$actions.setKeyboardInteraction;
  var tooltipReducer = tooltipSlice.reducer;

  // node_modules/recharts/es6/state/selectors/combiners/combineTooltipInteractionState.js
  function ownKeys12(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread12(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys12(Object(t), true).forEach(function(r3) {
        _defineProperty13(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys12(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty13(e, r2, t) {
    return (r2 = _toPropertyKey13(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey13(t) {
    var i = _toPrimitive13(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive13(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  function chooseAppropriateMouseInteraction(tooltipState, tooltipEventType, trigger) {
    if (tooltipEventType === "axis") {
      if (trigger === "click") {
        return tooltipState.axisInteraction.click;
      }
      return tooltipState.axisInteraction.hover;
    }
    if (trigger === "click") {
      return tooltipState.itemInteraction.click;
    }
    return tooltipState.itemInteraction.hover;
  }
  function hasBeenActivePreviously(tooltipInteractionState) {
    return tooltipInteractionState.index != null;
  }
  var combineTooltipInteractionState = (tooltipState, tooltipEventType, trigger, defaultIndex) => {
    if (tooltipEventType == null) {
      return noInteraction;
    }
    var appropriateMouseInteraction = chooseAppropriateMouseInteraction(tooltipState, tooltipEventType, trigger);
    if (appropriateMouseInteraction == null) {
      return noInteraction;
    }
    if (appropriateMouseInteraction.active) {
      return appropriateMouseInteraction;
    }
    if (tooltipState.keyboardInteraction.active) {
      return tooltipState.keyboardInteraction;
    }
    if (tooltipState.syncInteraction.active && tooltipState.syncInteraction.index != null) {
      return tooltipState.syncInteraction;
    }
    var activeFromProps = tooltipState.settings.active === true;
    if (hasBeenActivePreviously(appropriateMouseInteraction)) {
      if (activeFromProps) {
        return _objectSpread12(_objectSpread12({}, appropriateMouseInteraction), {}, {
          active: true
        });
      }
    } else if (defaultIndex != null) {
      return {
        active: true,
        coordinate: void 0,
        dataKey: void 0,
        index: defaultIndex,
        graphicalItemId: void 0
      };
    }
    return _objectSpread12(_objectSpread12({}, noInteraction), {}, {
      coordinate: appropriateMouseInteraction.coordinate
    });
  };

  // node_modules/recharts/es6/state/selectors/combiners/combineActiveTooltipIndex.js
  function toFiniteNumber(value2) {
    if (typeof value2 === "number") {
      return Number.isFinite(value2) ? value2 : void 0;
    }
    if (value2 instanceof Date) {
      var numericValue = value2.valueOf();
      return Number.isFinite(numericValue) ? numericValue : void 0;
    }
    var parsed = Number(value2);
    return Number.isFinite(parsed) ? parsed : void 0;
  }
  function isValueWithinNumberDomain(value2, domain) {
    var numericValue = toFiniteNumber(value2);
    var lowerBound = domain[0];
    var upperBound = domain[1];
    if (numericValue === void 0) {
      return false;
    }
    var min3 = Math.min(lowerBound, upperBound);
    var max4 = Math.max(lowerBound, upperBound);
    return numericValue >= min3 && numericValue <= max4;
  }
  function isValueWithinDomain(entry, axisDataKey, domain) {
    if (domain == null || axisDataKey == null) {
      return true;
    }
    var value2 = getValueByDataKey(entry, axisDataKey);
    if (value2 == null) {
      return true;
    }
    if (!isWellFormedNumberDomain(domain)) {
      return true;
    }
    return isValueWithinNumberDomain(value2, domain);
  }
  var combineActiveTooltipIndex = (tooltipInteraction, chartData, axisDataKey, domain) => {
    var desiredIndex = tooltipInteraction === null || tooltipInteraction === void 0 ? void 0 : tooltipInteraction.index;
    if (desiredIndex == null) {
      return null;
    }
    var indexAsNumber = Number(desiredIndex);
    if (!isWellBehavedNumber(indexAsNumber)) {
      return desiredIndex;
    }
    var lowerLimit = 0;
    var upperLimit = Infinity;
    if (chartData.length > 0) {
      upperLimit = chartData.length - 1;
    }
    var clampedIndex = Math.max(lowerLimit, Math.min(indexAsNumber, upperLimit));
    var entry = chartData[clampedIndex];
    if (entry == null) {
      return String(clampedIndex);
    }
    if (!isValueWithinDomain(entry, axisDataKey, domain)) {
      return null;
    }
    return String(clampedIndex);
  };

  // node_modules/recharts/es6/state/selectors/combiners/combineCoordinateForDefaultIndex.js
  var combineCoordinateForDefaultIndex = (width, height, layout, offset, tooltipTicks, defaultIndex, tooltipConfigurations) => {
    if (defaultIndex == null) {
      return void 0;
    }
    var firstConfiguration = tooltipConfigurations[0];
    var maybePosition = firstConfiguration === null || firstConfiguration === void 0 ? void 0 : firstConfiguration.getPosition(defaultIndex);
    if (maybePosition != null) {
      return maybePosition;
    }
    var tick = tooltipTicks === null || tooltipTicks === void 0 ? void 0 : tooltipTicks[Number(defaultIndex)];
    if (!tick) {
      return void 0;
    }
    switch (layout) {
      case "horizontal": {
        return {
          x: tick.coordinate,
          y: (offset.top + height) / 2
        };
      }
      default: {
        return {
          x: (offset.left + width) / 2,
          y: tick.coordinate
        };
      }
    }
  };

  // node_modules/recharts/es6/state/selectors/combiners/combineTooltipPayloadConfigurations.js
  var combineTooltipPayloadConfigurations = (tooltipState, tooltipEventType, trigger, defaultIndex) => {
    if (tooltipEventType === "axis") {
      return tooltipState.tooltipItemPayloads;
    }
    if (tooltipState.tooltipItemPayloads.length === 0) {
      return [];
    }
    var filterByGraphicalItemId;
    if (trigger === "hover") {
      filterByGraphicalItemId = tooltipState.itemInteraction.hover.graphicalItemId;
    } else {
      filterByGraphicalItemId = tooltipState.itemInteraction.click.graphicalItemId;
    }
    if (tooltipState.syncInteraction.active && filterByGraphicalItemId == null) {
      return tooltipState.tooltipItemPayloads;
    }
    if (filterByGraphicalItemId == null && (defaultIndex != null || tooltipState.keyboardInteraction.active)) {
      var firstItemPayload = tooltipState.tooltipItemPayloads[0];
      if (firstItemPayload != null) {
        return [firstItemPayload];
      }
      return [];
    }
    return tooltipState.tooltipItemPayloads.filter((tpc) => {
      var _tpc$settings;
      return ((_tpc$settings = tpc.settings) === null || _tpc$settings === void 0 ? void 0 : _tpc$settings.graphicalItemId) === filterByGraphicalItemId;
    });
  };

  // node_modules/recharts/es6/state/selectors/selectTooltipPayloadSearcher.js
  var selectTooltipPayloadSearcher = (state) => state.options.tooltipPayloadSearcher;

  // node_modules/recharts/es6/state/selectors/selectTooltipState.js
  var selectTooltipState = (state) => state.tooltip;

  // node_modules/recharts/es6/state/selectors/combiners/combineTooltipPayload.js
  function ownKeys13(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread13(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys13(Object(t), true).forEach(function(r3) {
        _defineProperty14(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys13(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty14(e, r2, t) {
    return (r2 = _toPropertyKey14(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey14(t) {
    var i = _toPrimitive14(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive14(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  function parseName(value2) {
    if (typeof value2 === "string" || typeof value2 === "number") {
      return value2;
    }
    return void 0;
  }
  function parseUnit(value2) {
    if (typeof value2 === "string" || typeof value2 === "number" || typeof value2 === "boolean") {
      return value2;
    }
    return void 0;
  }
  function parseDataKey(value2) {
    if (typeof value2 === "string" || typeof value2 === "number") {
      return value2;
    }
    if (typeof value2 === "function") {
      return (obj) => value2(obj);
    }
    return void 0;
  }
  function parseColor(value2) {
    if (typeof value2 === "string") {
      return value2;
    }
    return void 0;
  }
  function parseTooltipPayloadItem(item) {
    if (item == null || typeof item !== "object") {
      return void 0;
    }
    var name = "name" in item ? parseName(item.name) : void 0;
    var unit2 = "unit" in item ? parseUnit(item.unit) : void 0;
    var dataKey = "dataKey" in item ? parseDataKey(item.dataKey) : void 0;
    var payload = "payload" in item ? item.payload : void 0;
    var color2 = "color" in item ? parseColor(item.color) : void 0;
    var fill = "fill" in item ? parseColor(item.fill) : void 0;
    return {
      name,
      unit: unit2,
      dataKey,
      payload,
      color: color2,
      fill
    };
  }
  function selectFinalData(dataDefinedOnItem, dataDefinedOnChart) {
    if (dataDefinedOnItem != null) {
      return dataDefinedOnItem;
    }
    return dataDefinedOnChart;
  }
  var combineTooltipPayload = (tooltipPayloadConfigurations, activeIndex, chartDataState, tooltipAxisDataKey, activeLabel, tooltipPayloadSearcher, tooltipEventType) => {
    if (activeIndex == null || tooltipPayloadSearcher == null) {
      return void 0;
    }
    var chartData = chartDataState.chartData, computedData = chartDataState.computedData, dataStartIndex = chartDataState.dataStartIndex, dataEndIndex = chartDataState.dataEndIndex;
    var init = [];
    return tooltipPayloadConfigurations.reduce((agg, _ref2) => {
      var _settings$dataKey;
      var dataDefinedOnItem = _ref2.dataDefinedOnItem, settings = _ref2.settings;
      var finalData = selectFinalData(dataDefinedOnItem, chartData);
      var sliced = Array.isArray(finalData) ? getSliced(finalData, dataStartIndex, dataEndIndex) : finalData;
      var finalDataKey = (_settings$dataKey = settings === null || settings === void 0 ? void 0 : settings.dataKey) !== null && _settings$dataKey !== void 0 ? _settings$dataKey : tooltipAxisDataKey;
      var finalNameKey = settings === null || settings === void 0 ? void 0 : settings.nameKey;
      var tooltipPayload;
      if (tooltipAxisDataKey && Array.isArray(sliced) && /*
       * findEntryInArray won't work for Scatter because Scatter provides an array of arrays
       * as tooltip payloads and findEntryInArray is not prepared to handle that.
       * Sad but also ScatterChart only allows 'item' tooltipEventType
       * and also this is only a problem if there are multiple Scatters and each has its own data array
       * so let's fix that some other time.
       */
      !Array.isArray(sliced[0]) && /*
       * If the tooltipEventType is 'axis', we should search for the dataKey in the sliced data
       * because thanks to allowDuplicatedCategory=false, the order of elements in the array
       * no longer matches the order of elements in the original data
       * and so we need to search by the active dataKey + label rather than by index.
       *
       * The same happens if multiple graphical items are present in the chart
       * and each of them has its own data array. Those arrays get concatenated
       * and again the tooltip index no longer matches the original data.
       *
       * On the other hand the tooltipEventType 'item' should always search by index
       * because we get the index from interacting over the individual elements
       * which is always accurate, irrespective of the allowDuplicatedCategory setting.
       */
      tooltipEventType === "axis") {
        tooltipPayload = findEntryInArray(sliced, tooltipAxisDataKey, activeLabel);
      } else {
        tooltipPayload = tooltipPayloadSearcher(sliced, activeIndex, computedData, finalNameKey);
      }
      if (Array.isArray(tooltipPayload)) {
        tooltipPayload.forEach((item) => {
          var _parsedItem$color, _parsedItem$fill;
          var parsedItem = parseTooltipPayloadItem(item);
          var itemName = parsedItem === null || parsedItem === void 0 ? void 0 : parsedItem.name;
          var itemDataKey = parsedItem === null || parsedItem === void 0 ? void 0 : parsedItem.dataKey;
          var itemPayload = parsedItem === null || parsedItem === void 0 ? void 0 : parsedItem.payload;
          var newSettings = _objectSpread13(_objectSpread13({}, settings), {}, {
            name: itemName,
            unit: parsedItem === null || parsedItem === void 0 ? void 0 : parsedItem.unit,
            // Preserve item-level color/fill from graphical items.
            color: (_parsedItem$color = parsedItem === null || parsedItem === void 0 ? void 0 : parsedItem.color) !== null && _parsedItem$color !== void 0 ? _parsedItem$color : settings === null || settings === void 0 ? void 0 : settings.color,
            fill: (_parsedItem$fill = parsedItem === null || parsedItem === void 0 ? void 0 : parsedItem.fill) !== null && _parsedItem$fill !== void 0 ? _parsedItem$fill : settings === null || settings === void 0 ? void 0 : settings.fill
          });
          agg.push(getTooltipEntry({
            tooltipEntrySettings: newSettings,
            dataKey: itemDataKey,
            payload: itemPayload,
            value: getValueByDataKey(itemPayload, itemDataKey),
            name: itemName == null ? void 0 : String(itemName)
          }));
        });
      } else {
        var _getValueByDataKey;
        agg.push(getTooltipEntry({
          tooltipEntrySettings: settings,
          dataKey: finalDataKey,
          payload: tooltipPayload,
          // getValueByDataKey does not validate the output type
          value: getValueByDataKey(tooltipPayload, finalDataKey),
          // getValueByDataKey does not validate the output type
          name: (_getValueByDataKey = getValueByDataKey(tooltipPayload, finalNameKey)) !== null && _getValueByDataKey !== void 0 ? _getValueByDataKey : settings === null || settings === void 0 ? void 0 : settings.name
        }));
      }
      return agg;
    }, init);
  };

  // node_modules/recharts/es6/state/selectors/tooltipSelectors.js
  var selectTooltipAxisRealScaleType = createSelector([selectTooltipAxis, selectHasBar, selectChartName], combineRealScaleType);
  var selectAllUnfilteredGraphicalItems = createSelector([(state) => state.graphicalItems.cartesianItems, (state) => state.graphicalItems.polarItems], (cartesianItems, polarItems) => [...cartesianItems, ...polarItems]);
  var selectTooltipAxisPredicate = createSelector([selectTooltipAxisType, selectTooltipAxisId], itemAxisPredicate);
  var selectAllGraphicalItemsSettings = createSelector([selectAllUnfilteredGraphicalItems, selectTooltipAxis, selectTooltipAxisPredicate], combineGraphicalItemsSettings, {
    memoizeOptions: {
      resultEqualityCheck: emptyArraysAreEqualCheck
    }
  });
  var selectAllStackedGraphicalItemsSettings = createSelector([selectAllGraphicalItemsSettings], (graphicalItems) => graphicalItems.filter(isStacked));
  var selectTooltipGraphicalItemsData = createSelector([selectAllGraphicalItemsSettings], combineGraphicalItemsData, {
    memoizeOptions: {
      resultEqualityCheck: emptyArraysAreEqualCheck
    }
  });
  var selectAnyTooltipItemUsesChartData = createSelector([selectAllGraphicalItemsSettings], (items) => items.some((item) => !item.data));
  var selectTooltipDisplayedData = createSelector([selectTooltipGraphicalItemsData, selectChartDataWithIndexes], combineDisplayedData);
  var selectTooltipStackedData = createSelector([selectAllStackedGraphicalItemsSettings, selectChartDataWithIndexes, selectTooltipAxis], combineDisplayedStackedData);
  var selectAllTooltipAppliedValues = createSelector([selectTooltipDisplayedData, selectTooltipAxis, selectAllGraphicalItemsSettings, selectChartDataWithIndexes, selectAnyTooltipItemUsesChartData, selectTooltipGraphicalItemsData], combineAllAppliedValues);
  var selectTooltipAxisDomainDefinition = createSelector([selectTooltipAxis], getDomainDefinition);
  var selectTooltipDataOverflow = createSelector([selectTooltipAxis], (axisSettings) => axisSettings.allowDataOverflow);
  var selectTooltipDomainFromUserPreferences = createSelector([selectTooltipAxisDomainDefinition, selectTooltipDataOverflow], numericalDomainSpecifiedWithoutRequiringData);
  var selectAllStackedGraphicalItems = createSelector([selectAllGraphicalItemsSettings], (graphicalItems) => graphicalItems.filter(isStacked));
  var selectTooltipStackGroups = createSelector([selectTooltipStackedData, selectAllStackedGraphicalItems, selectStackOffsetType, selectReverseStackOrder], combineStackGroups);
  var selectTooltipDomainOfStackGroups = createSelector([selectTooltipStackGroups, selectChartDataWithIndexes, selectTooltipAxisType, selectTooltipDomainFromUserPreferences], combineDomainOfStackGroups);
  var selectTooltipItemsSettingsExceptStacked = createSelector([selectAllGraphicalItemsSettings], filterGraphicalNotStackedItems);
  var selectDomainOfAllAppliedNumericalValuesIncludingErrorValues2 = createSelector([selectTooltipDisplayedData, selectTooltipAxis, selectTooltipItemsSettingsExceptStacked, selectAllErrorBarSettings, selectTooltipAxisType, selectChartDataSliceWithIndexes], combineDomainOfAllAppliedNumericalValuesIncludingErrorValues, {
    memoizeOptions: {
      resultEqualityCheck: numberDomainEqualityCheck
    }
  });
  var selectReferenceDotsByTooltipAxis = createSelector([selectReferenceDots, selectTooltipAxisType, selectTooltipAxisId], filterReferenceElements);
  var selectTooltipReferenceDotsDomain = createSelector([selectReferenceDotsByTooltipAxis, selectTooltipAxisType], combineDotsDomain);
  var selectReferenceAreasByTooltipAxis = createSelector([selectReferenceAreas, selectTooltipAxisType, selectTooltipAxisId], filterReferenceElements);
  var selectTooltipReferenceAreasDomain = createSelector([selectReferenceAreasByTooltipAxis, selectTooltipAxisType], combineAreasDomain);
  var selectReferenceLinesByTooltipAxis = createSelector([selectReferenceLines, selectTooltipAxisType, selectTooltipAxisId], filterReferenceElements);
  var selectTooltipReferenceLinesDomain = createSelector([selectReferenceLinesByTooltipAxis, selectTooltipAxisType], combineLinesDomain);
  var selectTooltipReferenceElementsDomain = createSelector([selectTooltipReferenceDotsDomain, selectTooltipReferenceLinesDomain, selectTooltipReferenceAreasDomain], mergeDomains);
  var selectTooltipNumericalDomain = createSelector([selectTooltipAxis, selectTooltipAxisDomainDefinition, selectTooltipDomainFromUserPreferences, selectTooltipDomainOfStackGroups, selectDomainOfAllAppliedNumericalValuesIncludingErrorValues2, selectTooltipReferenceElementsDomain, selectChartLayout, selectTooltipAxisType], combineNumericalDomain);
  var selectTooltipAxisDomain = createSelector([selectTooltipAxis, selectChartLayout, selectTooltipDisplayedData, selectAllTooltipAppliedValues, selectStackOffsetType, selectTooltipAxisType, selectTooltipNumericalDomain], combineAxisDomain);
  var selectTooltipNiceTicks = createSelector([selectTooltipAxisDomain, selectTooltipAxis, selectTooltipAxisRealScaleType], combineNiceTicks);
  var selectTooltipAxisDomainIncludingNiceTicks = createSelector([selectTooltipAxis, selectTooltipAxisDomain, selectTooltipNiceTicks, selectTooltipAxisType], combineAxisDomainWithNiceTicks);
  var selectTooltipAxisRange = (state) => {
    var axisType = selectTooltipAxisType(state);
    var axisId = selectTooltipAxisId(state);
    var isPanorama = false;
    return selectAxisRange(state, axisType, axisId, isPanorama);
  };
  var selectTooltipAxisRangeWithReverse = createSelector([selectTooltipAxis, selectTooltipAxisRange], combineAxisRangeWithReverse);
  var selectTooltipConfiguredScale = createSelector([selectTooltipAxis, selectTooltipAxisRealScaleType, selectTooltipAxisDomainIncludingNiceTicks, selectTooltipAxisRangeWithReverse], combineConfiguredScale);
  var selectTooltipAxisScale = createSelector([selectTooltipConfiguredScale], rechartsScaleFactory);
  var selectTooltipDuplicateDomain = createSelector([selectChartLayout, selectAllTooltipAppliedValues, selectTooltipAxis, selectTooltipAxisType], combineDuplicateDomain);
  var selectTooltipCategoricalDomain = createSelector([selectChartLayout, selectAllTooltipAppliedValues, selectTooltipAxis, selectTooltipAxisType], combineCategoricalDomain);
  var combineTicksOfTooltipAxis = (layout, axis, realScaleType, scale, range3, duplicateDomain, categoricalDomain, axisType) => {
    if (!axis) {
      return void 0;
    }
    var type = axis.type;
    var isCategorical = isCategoricalAxis(layout, axisType);
    if (!scale) {
      return void 0;
    }
    var offsetForBand = realScaleType === "scaleBand" && scale.bandwidth ? scale.bandwidth() / 2 : 2;
    var offset = type === "category" && scale.bandwidth ? scale.bandwidth() / offsetForBand : 0;
    offset = axisType === "angleAxis" && range3 != null && (range3 === null || range3 === void 0 ? void 0 : range3.length) >= 2 ? mathSign(range3[0] - range3[1]) * 2 * offset : offset;
    if (isCategorical && categoricalDomain) {
      return categoricalDomain.map((entry, index2) => {
        var scaled = scale.map(entry);
        if (!isWellBehavedNumber(scaled)) {
          return null;
        }
        return {
          coordinate: scaled + offset,
          value: entry,
          index: index2,
          offset
        };
      }).filter(isNotNil);
    }
    return scale.domain().map((entry, index2) => {
      var scaled = scale.map(entry);
      if (!isWellBehavedNumber(scaled)) {
        return null;
      }
      return {
        coordinate: scaled + offset,
        // @ts-expect-error can't use Date as an index
        value: duplicateDomain ? duplicateDomain[entry] : entry,
        index: index2,
        offset
      };
    }).filter(isNotNil);
  };
  var selectTooltipAxisTicks = createSelector([selectChartLayout, selectTooltipAxis, selectTooltipAxisRealScaleType, selectTooltipAxisScale, selectTooltipAxisRange, selectTooltipDuplicateDomain, selectTooltipCategoricalDomain, selectTooltipAxisType], combineTicksOfTooltipAxis);
  var selectTooltipEventType2 = createSelector([selectDefaultTooltipEventType, selectValidateTooltipEventTypes, selectTooltipSettings], (defaultTooltipEventType, validateTooltipEventType, settings) => combineTooltipEventType(settings.shared, defaultTooltipEventType, validateTooltipEventType));
  var selectTooltipTrigger = (state) => state.tooltip.settings.trigger;
  var selectDefaultIndex = (state) => state.tooltip.settings.defaultIndex;
  var selectTooltipInteractionState = createSelector([selectTooltipState, selectTooltipEventType2, selectTooltipTrigger, selectDefaultIndex], combineTooltipInteractionState);
  var selectActiveTooltipIndex = createSelector([selectTooltipInteractionState, selectTooltipDisplayedData, selectTooltipAxisDataKey, selectTooltipAxisDomain], combineActiveTooltipIndex);
  var selectActiveLabel = createSelector([selectTooltipAxisTicks, selectActiveTooltipIndex], combineActiveLabel);
  var selectActiveTooltipDataKey = createSelector([selectTooltipInteractionState], (tooltipInteraction) => {
    if (!tooltipInteraction) {
      return void 0;
    }
    return tooltipInteraction.dataKey;
  });
  var selectActiveTooltipGraphicalItemId = createSelector([selectTooltipInteractionState], (tooltipInteraction) => {
    if (!tooltipInteraction) {
      return void 0;
    }
    return tooltipInteraction.graphicalItemId;
  });
  var selectTooltipPayloadConfigurations = createSelector([selectTooltipState, selectTooltipEventType2, selectTooltipTrigger, selectDefaultIndex], combineTooltipPayloadConfigurations);
  var selectTooltipCoordinateForDefaultIndex = createSelector([selectChartWidth, selectChartHeight, selectChartLayout, selectChartOffsetInternal, selectTooltipAxisTicks, selectDefaultIndex, selectTooltipPayloadConfigurations], combineCoordinateForDefaultIndex);
  var selectActiveTooltipCoordinate = createSelector([selectTooltipInteractionState, selectTooltipCoordinateForDefaultIndex], (tooltipInteractionState, defaultIndexCoordinate) => {
    if (tooltipInteractionState !== null && tooltipInteractionState !== void 0 && tooltipInteractionState.coordinate) {
      return tooltipInteractionState.coordinate;
    }
    return defaultIndexCoordinate;
  });
  var selectIsTooltipActive = createSelector([selectTooltipInteractionState], (tooltipInteractionState) => {
    var _tooltipInteractionSt;
    return (_tooltipInteractionSt = tooltipInteractionState === null || tooltipInteractionState === void 0 ? void 0 : tooltipInteractionState.active) !== null && _tooltipInteractionSt !== void 0 ? _tooltipInteractionSt : false;
  });
  var selectActiveTooltipPayload = createSelector([selectTooltipPayloadConfigurations, selectActiveTooltipIndex, selectChartDataWithIndexes, selectTooltipAxisDataKey, selectActiveLabel, selectTooltipPayloadSearcher, selectTooltipEventType2], combineTooltipPayload);
  var selectActiveTooltipDataPoints = createSelector([selectActiveTooltipPayload], (payload) => {
    if (payload == null) {
      return void 0;
    }
    var dataPoints = payload.map((p) => p.payload).filter((p) => p != null);
    return Array.from(new Set(dataPoints));
  });

  // node_modules/recharts/es6/context/useTooltipAxis.js
  function ownKeys14(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread14(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys14(Object(t), true).forEach(function(r3) {
        _defineProperty15(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys14(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty15(e, r2, t) {
    return (r2 = _toPropertyKey15(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey15(t) {
    var i = _toPrimitive15(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive15(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  var useTooltipAxis = () => useAppSelector(selectTooltipAxis);
  var useTooltipAxisBandSize = () => {
    var tooltipAxis = useTooltipAxis();
    var tooltipTicks = useAppSelector(selectTooltipAxisTicks);
    var tooltipAxisScale = useAppSelector(selectTooltipAxisScale);
    if (!tooltipAxis || !tooltipAxisScale) {
      return getBandSizeOfAxis(void 0, tooltipTicks);
    }
    return getBandSizeOfAxis(_objectSpread14(_objectSpread14({}, tooltipAxis), {}, {
      scale: tooltipAxisScale
    }), tooltipTicks);
  };

  // node_modules/recharts/es6/util/getActiveCoordinate.js
  function ownKeys15(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread15(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys15(Object(t), true).forEach(function(r3) {
        _defineProperty16(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys15(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty16(e, r2, t) {
    return (r2 = _toPropertyKey16(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey16(t) {
    var i = _toPrimitive16(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive16(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  var getActiveCartesianCoordinate = (layout, tooltipTicks, activeIndex, pointer) => {
    var entry = tooltipTicks.find((tick) => tick && tick.index === activeIndex);
    if (entry) {
      if (layout === "horizontal") {
        return {
          x: entry.coordinate,
          y: pointer.relativeY
        };
      }
      if (layout === "vertical") {
        return {
          x: pointer.relativeX,
          y: entry.coordinate
        };
      }
    }
    return {
      x: 0,
      y: 0
    };
  };
  var getActivePolarCoordinate = (layout, tooltipTicks, activeIndex, rangeObj) => {
    var entry = tooltipTicks.find((tick) => tick && tick.index === activeIndex);
    if (entry) {
      if (layout === "centric") {
        var _angle = entry.coordinate;
        var _radius = rangeObj.radius;
        return _objectSpread15(_objectSpread15(_objectSpread15({}, rangeObj), polarToCartesian(rangeObj.cx, rangeObj.cy, _radius, _angle)), {}, {
          angle: _angle,
          radius: _radius
        });
      }
      var radius = entry.coordinate;
      var angle = rangeObj.angle;
      return _objectSpread15(_objectSpread15(_objectSpread15({}, rangeObj), polarToCartesian(rangeObj.cx, rangeObj.cy, radius, angle)), {}, {
        angle,
        radius
      });
    }
    return {
      angle: 0,
      clockWise: false,
      cx: 0,
      cy: 0,
      endAngle: 0,
      innerRadius: 0,
      outerRadius: 0,
      radius: 0,
      startAngle: 0,
      x: 0,
      y: 0
    };
  };
  function isInCartesianRange(pointer, offset) {
    var x5 = pointer.relativeX, y5 = pointer.relativeY;
    return x5 >= offset.left && x5 <= offset.left + offset.width && y5 >= offset.top && y5 <= offset.top + offset.height;
  }
  var calculateActiveTickIndex = (coordinate, ticks, unsortedTicks, axisType, range3) => {
    var _ticks$length;
    var len = (_ticks$length = ticks === null || ticks === void 0 ? void 0 : ticks.length) !== null && _ticks$length !== void 0 ? _ticks$length : 0;
    if (len <= 1 || coordinate == null) {
      return 0;
    }
    if (axisType === "angleAxis" && range3 != null && Math.abs(Math.abs(range3[1] - range3[0]) - 360) <= 1e-6) {
      for (var i = 0; i < len; i++) {
        var _unsortedTicks, _unsortedTicks2, _unsortedTicks$i, _unsortedTicks$, _unsortedTicks3;
        var before = i > 0 ? (_unsortedTicks = unsortedTicks[i - 1]) === null || _unsortedTicks === void 0 ? void 0 : _unsortedTicks.coordinate : (_unsortedTicks2 = unsortedTicks[len - 1]) === null || _unsortedTicks2 === void 0 ? void 0 : _unsortedTicks2.coordinate;
        var cur = (_unsortedTicks$i = unsortedTicks[i]) === null || _unsortedTicks$i === void 0 ? void 0 : _unsortedTicks$i.coordinate;
        var after = i >= len - 1 ? (_unsortedTicks$ = unsortedTicks[0]) === null || _unsortedTicks$ === void 0 ? void 0 : _unsortedTicks$.coordinate : (_unsortedTicks3 = unsortedTicks[i + 1]) === null || _unsortedTicks3 === void 0 ? void 0 : _unsortedTicks3.coordinate;
        var sameDirectionCoord = void 0;
        if (before == null || cur == null || after == null) {
          continue;
        }
        if (mathSign(cur - before) !== mathSign(after - cur)) {
          var diffInterval = [];
          if (mathSign(after - cur) === mathSign(range3[1] - range3[0])) {
            sameDirectionCoord = after;
            var curInRange = cur + range3[1] - range3[0];
            diffInterval[0] = Math.min(curInRange, (curInRange + before) / 2);
            diffInterval[1] = Math.max(curInRange, (curInRange + before) / 2);
          } else {
            sameDirectionCoord = before;
            var afterInRange = after + range3[1] - range3[0];
            diffInterval[0] = Math.min(cur, (afterInRange + cur) / 2);
            diffInterval[1] = Math.max(cur, (afterInRange + cur) / 2);
          }
          var sameInterval = [Math.min(cur, (sameDirectionCoord + cur) / 2), Math.max(cur, (sameDirectionCoord + cur) / 2)];
          if (coordinate > sameInterval[0] && coordinate <= sameInterval[1] || coordinate >= diffInterval[0] && coordinate <= diffInterval[1]) {
            var _unsortedTicks$i2;
            return (_unsortedTicks$i2 = unsortedTicks[i]) === null || _unsortedTicks$i2 === void 0 ? void 0 : _unsortedTicks$i2.index;
          }
        } else {
          var minValue = Math.min(before, after);
          var maxValue = Math.max(before, after);
          if (coordinate > (minValue + cur) / 2 && coordinate <= (maxValue + cur) / 2) {
            var _unsortedTicks$i3;
            return (_unsortedTicks$i3 = unsortedTicks[i]) === null || _unsortedTicks$i3 === void 0 ? void 0 : _unsortedTicks$i3.index;
          }
        }
      }
    } else if (ticks) {
      for (var _i = 0; _i < len; _i++) {
        var curr = ticks[_i];
        if (curr == null) {
          continue;
        }
        var next = ticks[_i + 1];
        var prev = ticks[_i - 1];
        if (_i === 0 && next != null && coordinate <= (curr.coordinate + next.coordinate) / 2) {
          return curr.index;
        }
        if (_i === len - 1 && prev != null && coordinate > (curr.coordinate + prev.coordinate) / 2) {
          return curr.index;
        }
        if (_i > 0 && _i < len - 1 && prev != null && next != null && coordinate > (curr.coordinate + prev.coordinate) / 2 && coordinate <= (curr.coordinate + next.coordinate) / 2) {
          return curr.index;
        }
      }
    }
    return -1;
  };

  // node_modules/recharts/es6/state/selectors/selectors.js
  var useChartName = () => {
    return useAppSelector(selectChartName);
  };
  var pickTooltipEventType = (_state, tooltipEventType) => tooltipEventType;
  var pickTrigger = (_state, _tooltipEventType, trigger) => trigger;
  var pickDefaultIndex = (_state, _tooltipEventType, _trigger, defaultIndex) => defaultIndex;
  var selectOrderedTooltipTicks = createSelector(selectTooltipAxisTicks, (ticks) => sortBy(ticks, (o) => o.coordinate));
  var selectTooltipInteractionState2 = createSelector([selectTooltipState, pickTooltipEventType, pickTrigger, pickDefaultIndex], combineTooltipInteractionState);
  var selectActiveIndex = createSelector([selectTooltipInteractionState2, selectTooltipDisplayedData, selectTooltipAxisDataKey, selectTooltipAxisDomain], combineActiveTooltipIndex);
  var selectTooltipDataKey = (state, tooltipEventType, trigger) => {
    if (tooltipEventType == null) {
      return void 0;
    }
    var tooltipState = selectTooltipState(state);
    if (tooltipEventType === "axis") {
      if (trigger === "hover") {
        return tooltipState.axisInteraction.hover.dataKey;
      }
      return tooltipState.axisInteraction.click.dataKey;
    }
    if (trigger === "hover") {
      return tooltipState.itemInteraction.hover.dataKey;
    }
    return tooltipState.itemInteraction.click.dataKey;
  };
  var selectTooltipPayloadConfigurations2 = createSelector([selectTooltipState, pickTooltipEventType, pickTrigger, pickDefaultIndex], combineTooltipPayloadConfigurations);
  var selectCoordinateForDefaultIndex = createSelector([selectChartWidth, selectChartHeight, selectChartLayout, selectChartOffsetInternal, selectTooltipAxisTicks, pickDefaultIndex, selectTooltipPayloadConfigurations2], combineCoordinateForDefaultIndex);
  var selectActiveCoordinate = createSelector([selectTooltipInteractionState2, selectCoordinateForDefaultIndex], (tooltipInteractionState, defaultIndexCoordinate) => {
    var _tooltipInteractionSt;
    return (_tooltipInteractionSt = tooltipInteractionState.coordinate) !== null && _tooltipInteractionSt !== void 0 ? _tooltipInteractionSt : defaultIndexCoordinate;
  });
  var selectActiveLabel2 = createSelector([selectTooltipAxisTicks, selectActiveIndex], combineActiveLabel);
  var selectTooltipPayload = createSelector([selectTooltipPayloadConfigurations2, selectActiveIndex, selectChartDataWithIndexes, selectTooltipAxisDataKey, selectActiveLabel2, selectTooltipPayloadSearcher, pickTooltipEventType], combineTooltipPayload);
  var selectIsTooltipActive2 = createSelector([selectTooltipInteractionState2, selectActiveIndex], (tooltipInteractionState, activeIndex) => {
    return {
      isActive: tooltipInteractionState.active && activeIndex != null,
      activeIndex
    };
  });
  var combineActiveCartesianProps = (chartEvent, layout, tooltipAxisType, tooltipAxisRange, tooltipTicks, orderedTooltipTicks, offset) => {
    if (!chartEvent || !tooltipAxisType || !tooltipAxisRange || !tooltipTicks) {
      return void 0;
    }
    if (!isInCartesianRange(chartEvent, offset)) {
      return void 0;
    }
    var pos = calculateCartesianTooltipPos(chartEvent, layout);
    var activeIndex = calculateActiveTickIndex(pos, orderedTooltipTicks, tooltipTicks, tooltipAxisType, tooltipAxisRange);
    var activeCoordinate = getActiveCartesianCoordinate(layout, tooltipTicks, activeIndex, chartEvent);
    return {
      activeIndex: String(activeIndex),
      activeCoordinate
    };
  };
  var combineActivePolarProps = (chartEvent, layout, polarViewBox, tooltipAxisType, tooltipAxisRange, tooltipTicks, orderedTooltipTicks) => {
    if (!chartEvent || !tooltipAxisType || !tooltipAxisRange || !tooltipTicks || !polarViewBox) {
      return void 0;
    }
    var rangeObj = inRangeOfSector(chartEvent, polarViewBox);
    if (!rangeObj) {
      return void 0;
    }
    var pos = calculatePolarTooltipPos(rangeObj, layout);
    var activeIndex = calculateActiveTickIndex(pos, orderedTooltipTicks, tooltipTicks, tooltipAxisType, tooltipAxisRange);
    var activeCoordinate = getActivePolarCoordinate(layout, tooltipTicks, activeIndex, rangeObj);
    return {
      activeIndex: String(activeIndex),
      activeCoordinate
    };
  };
  var combineActiveProps = (chartEvent, layout, polarViewBox, tooltipAxisType, tooltipAxisRange, tooltipTicks, orderedTooltipTicks, offset) => {
    if (!chartEvent || !layout || !tooltipAxisType || !tooltipAxisRange || !tooltipTicks) {
      return void 0;
    }
    if (layout === "horizontal" || layout === "vertical") {
      return combineActiveCartesianProps(chartEvent, layout, tooltipAxisType, tooltipAxisRange, tooltipTicks, orderedTooltipTicks, offset);
    }
    return combineActivePolarProps(chartEvent, layout, polarViewBox, tooltipAxisType, tooltipAxisRange, tooltipTicks, orderedTooltipTicks);
  };

  // node_modules/recharts/es6/zIndex/ZIndexLayer.js
  var import_react20 = __toESM(require_react());
  var import_react_dom = __toESM(require_react_dom());

  // node_modules/recharts/es6/zIndex/zIndexSelectors.js
  var selectZIndexPortalElement = createSelector((state) => state.zIndex.zIndexMap, (_, zIndex) => zIndex, (_, _zIndex, isPanorama) => isPanorama, (zIndexMap, zIndex, isPanorama) => {
    if (zIndex == null) {
      return void 0;
    }
    var entry = zIndexMap[zIndex];
    if (entry == null) {
      return void 0;
    }
    if (isPanorama) {
      return entry.panoramaElement;
    }
    return entry.element;
  });
  var selectAllRegisteredZIndexes = createSelector((state) => state.zIndex.zIndexMap, (zIndexMap) => {
    var allNumbers = Object.keys(zIndexMap).map((zIndexStr) => parseInt(zIndexStr, 10)).concat(Object.values(DefaultZIndexes));
    var uniqueNumbers = Array.from(new Set(allNumbers));
    return uniqueNumbers.sort((a2, b) => a2 - b);
  }, {
    memoizeOptions: {
      resultEqualityCheck: arrayContentsAreEqualCheck
    }
  });

  // node_modules/recharts/es6/state/zIndexSlice.js
  function ownKeys16(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread16(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys16(Object(t), true).forEach(function(r3) {
        _defineProperty17(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys16(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty17(e, r2, t) {
    return (r2 = _toPropertyKey17(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey17(t) {
    var i = _toPrimitive17(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive17(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  var seed = {};
  var initialState4 = {
    zIndexMap: Object.values(DefaultZIndexes).reduce((acc, current2) => _objectSpread16(_objectSpread16({}, acc), {}, {
      [current2]: {
        element: void 0,
        panoramaElement: void 0,
        consumers: 0
      }
    }), seed)
  };
  var defaultZIndexSet = new Set(Object.values(DefaultZIndexes));
  function isDefaultZIndex(zIndex) {
    return defaultZIndexSet.has(zIndex);
  }
  var zIndexSlice = createSlice({
    name: "zIndex",
    initialState: initialState4,
    reducers: {
      registerZIndexPortal: {
        reducer: (state, action) => {
          var zIndex = action.payload.zIndex;
          if (state.zIndexMap[zIndex]) {
            state.zIndexMap[zIndex].consumers += 1;
          } else {
            state.zIndexMap[zIndex] = {
              consumers: 1,
              element: void 0,
              panoramaElement: void 0
            };
          }
        },
        prepare: prepareAutoBatched()
      },
      unregisterZIndexPortal: {
        reducer: (state, action) => {
          var zIndex = action.payload.zIndex;
          if (state.zIndexMap[zIndex]) {
            state.zIndexMap[zIndex].consumers -= 1;
            if (state.zIndexMap[zIndex].consumers <= 0 && !isDefaultZIndex(zIndex)) {
              delete state.zIndexMap[zIndex];
            }
          }
        },
        prepare: prepareAutoBatched()
      },
      registerZIndexPortalElement: {
        reducer: (state, action) => {
          var _action$payload = action.payload, zIndex = _action$payload.zIndex, element = _action$payload.element, isPanorama = _action$payload.isPanorama;
          if (state.zIndexMap[zIndex]) {
            if (isPanorama) {
              state.zIndexMap[zIndex].panoramaElement = castDraft(element);
            } else {
              state.zIndexMap[zIndex].element = castDraft(element);
            }
          } else {
            state.zIndexMap[zIndex] = {
              consumers: 0,
              element: isPanorama ? void 0 : castDraft(element),
              panoramaElement: isPanorama ? castDraft(element) : void 0
            };
          }
        },
        prepare: prepareAutoBatched()
      },
      unregisterZIndexPortalElement: {
        reducer: (state, action) => {
          var zIndex = action.payload.zIndex;
          if (state.zIndexMap[zIndex]) {
            if (action.payload.isPanorama) {
              state.zIndexMap[zIndex].panoramaElement = void 0;
            } else {
              state.zIndexMap[zIndex].element = void 0;
            }
          }
        },
        prepare: prepareAutoBatched()
      }
    }
  });
  var _zIndexSlice$actions = zIndexSlice.actions;
  var registerZIndexPortal = _zIndexSlice$actions.registerZIndexPortal;
  var unregisterZIndexPortal = _zIndexSlice$actions.unregisterZIndexPortal;
  var registerZIndexPortalElement = _zIndexSlice$actions.registerZIndexPortalElement;
  var unregisterZIndexPortalElement = _zIndexSlice$actions.unregisterZIndexPortalElement;
  var zIndexReducer = zIndexSlice.reducer;

  // node_modules/recharts/es6/zIndex/ZIndexLayer.js
  function ZIndexLayer(_ref2) {
    var zIndex = _ref2.zIndex, children = _ref2.children;
    var isInChartContext = useIsInChartContext();
    var shouldRenderInPortal = isInChartContext && zIndex !== void 0 && zIndex !== 0;
    var isPanorama = useIsPanorama();
    var lastPortalElementRef = (0, import_react20.useRef)(void 0);
    var registeredZIndexesRef = (0, import_react20.useRef)(/* @__PURE__ */ new Set());
    var dispatch2 = useAppDispatch();
    var portalElement = useAppSelector((state) => selectZIndexPortalElement(state, zIndex, isPanorama));
    (0, import_react20.useLayoutEffect)(() => {
      if (!shouldRenderInPortal) {
        var registered = registeredZIndexesRef.current;
        registered.forEach((z) => {
          dispatch2(unregisterZIndexPortal({
            zIndex: z
          }));
        });
        registered.clear();
        lastPortalElementRef.current = void 0;
        return;
      }
      if (!registeredZIndexesRef.current.has(zIndex)) {
        dispatch2(registerZIndexPortal({
          zIndex
        }));
        registeredZIndexesRef.current.add(zIndex);
      }
      if (portalElement) {
        lastPortalElementRef.current = portalElement;
        var _registered = registeredZIndexesRef.current;
        _registered.forEach((z) => {
          if (z !== zIndex) {
            dispatch2(unregisterZIndexPortal({
              zIndex: z
            }));
            _registered.delete(z);
          }
        });
      }
    }, [dispatch2, zIndex, shouldRenderInPortal, portalElement]);
    (0, import_react20.useLayoutEffect)(() => {
      var registered = registeredZIndexesRef.current;
      return () => {
        registered.forEach((z) => {
          dispatch2(unregisterZIndexPortal({
            zIndex: z
          }));
        });
        registered.clear();
      };
    }, [dispatch2]);
    if (!shouldRenderInPortal) {
      return children;
    }
    var targetElement = portalElement !== null && portalElement !== void 0 ? portalElement : lastPortalElementRef.current;
    if (!targetElement) {
      return null;
    }
    return /* @__PURE__ */ (0, import_react_dom.createPortal)(children, targetElement);
  }

  // node_modules/recharts/es6/component/Cursor.js
  function _extends8() {
    return _extends8 = Object.assign ? Object.assign.bind() : function(n) {
      for (var e = 1; e < arguments.length; e++) {
        var t = arguments[e];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends8.apply(null, arguments);
  }
  function ownKeys17(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread17(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys17(Object(t), true).forEach(function(r3) {
        _defineProperty18(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys17(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty18(e, r2, t) {
    return (r2 = _toPropertyKey18(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey18(t) {
    var i = _toPrimitive18(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive18(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  function RenderCursor(_ref2) {
    var cursor = _ref2.cursor, cursorComp = _ref2.cursorComp, cursorProps = _ref2.cursorProps;
    if (/* @__PURE__ */ (0, import_react21.isValidElement)(cursor)) {
      return /* @__PURE__ */ (0, import_react21.cloneElement)(cursor, cursorProps);
    }
    return /* @__PURE__ */ (0, import_react21.createElement)(cursorComp, cursorProps);
  }
  function CursorInternal(props) {
    var _props$zIndex;
    var coordinate = props.coordinate, payload = props.payload, index2 = props.index, offset = props.offset, tooltipAxisBandSize = props.tooltipAxisBandSize, layout = props.layout, cursor = props.cursor, tooltipEventType = props.tooltipEventType, chartName = props.chartName;
    var activeCoordinate = coordinate;
    var activePayload = payload;
    var activeTooltipIndex = index2;
    if (!cursor || !activeCoordinate || chartName !== "ScatterChart" && tooltipEventType !== "axis") {
      return null;
    }
    var restProps, cursorComp, preferredZIndex;
    if (chartName === "ScatterChart") {
      restProps = activeCoordinate;
      cursorComp = Cross;
      preferredZIndex = DefaultZIndexes.cursorLine;
    } else if (chartName === "BarChart") {
      restProps = getCursorRectangle(layout, activeCoordinate, offset, tooltipAxisBandSize);
      cursorComp = Rectangle;
      preferredZIndex = DefaultZIndexes.cursorRectangle;
    } else if (layout === "radial" && isPolarCoordinate(activeCoordinate)) {
      var _getRadialCursorPoint = getRadialCursorPoints(activeCoordinate), cx = _getRadialCursorPoint.cx, cy = _getRadialCursorPoint.cy, radius = _getRadialCursorPoint.radius, startAngle = _getRadialCursorPoint.startAngle, endAngle = _getRadialCursorPoint.endAngle;
      restProps = {
        cx,
        cy,
        startAngle,
        endAngle,
        innerRadius: radius,
        outerRadius: radius
      };
      cursorComp = Sector;
      preferredZIndex = DefaultZIndexes.cursorLine;
    } else {
      restProps = {
        points: getCursorPoints(layout, activeCoordinate, offset)
      };
      cursorComp = Curve;
      preferredZIndex = DefaultZIndexes.cursorLine;
    }
    var extraClassName = typeof cursor === "object" && "className" in cursor ? cursor.className : void 0;
    var cursorProps = _objectSpread17(_objectSpread17(_objectSpread17(_objectSpread17({
      stroke: "#ccc",
      pointerEvents: "none"
    }, offset), restProps), svgPropertiesNoEventsFromUnknown(cursor)), {}, {
      payload: activePayload,
      payloadIndex: activeTooltipIndex,
      className: clsx("recharts-tooltip-cursor", extraClassName)
    });
    return /* @__PURE__ */ React12.createElement(ZIndexLayer, {
      zIndex: (_props$zIndex = props.zIndex) !== null && _props$zIndex !== void 0 ? _props$zIndex : preferredZIndex
    }, /* @__PURE__ */ React12.createElement(RenderCursor, {
      cursor,
      cursorComp,
      cursorProps
    }));
  }
  function Cursor(props) {
    var tooltipAxisBandSize = useTooltipAxisBandSize();
    var offset = useOffsetInternal();
    var layout = useChartLayout();
    var chartName = useChartName();
    if (tooltipAxisBandSize == null || offset == null || layout == null || chartName == null) {
      return null;
    }
    return /* @__PURE__ */ React12.createElement(CursorInternal, _extends8({}, props, {
      offset,
      layout,
      tooltipAxisBandSize,
      chartName
    }));
  }

  // node_modules/recharts/es6/context/tooltipPortalContext.js
  var import_react22 = __toESM(require_react());
  var TooltipPortalContext = /* @__PURE__ */ (0, import_react22.createContext)(null);
  var useTooltipPortal = () => (0, import_react22.useContext)(TooltipPortalContext);

  // node_modules/recharts/es6/synchronisation/useChartSynchronisation.js
  var import_react23 = __toESM(require_react());

  // node_modules/eventemitter3/index.mjs
  var import_index = __toESM(require_eventemitter3(), 1);
  var eventemitter3_default = import_index.default;

  // node_modules/recharts/es6/util/Events.js
  var eventCenter = new eventemitter3_default();
  var TOOLTIP_SYNC_EVENT = "recharts.syncEvent.tooltip";
  var BRUSH_SYNC_EVENT = "recharts.syncEvent.brush";

  // node_modules/recharts/es6/state/optionsSlice.js
  var arrayTooltipSearcher = (data, strIndex) => {
    if (!strIndex) return void 0;
    if (!Array.isArray(data)) return void 0;
    var numIndex = Number.parseInt(strIndex, 10);
    if (isNan(numIndex)) {
      return void 0;
    }
    return data[numIndex];
  };
  var initialState5 = {
    chartName: "",
    tooltipPayloadSearcher: () => void 0,
    eventEmitter: void 0,
    defaultTooltipEventType: "axis"
  };
  var optionsSlice = createSlice({
    name: "options",
    initialState: initialState5,
    reducers: {
      createEventEmitter: (state) => {
        if (state.eventEmitter == null) {
          state.eventEmitter = Symbol("rechartsEventEmitter");
        }
      }
    }
  });
  var optionsReducer = optionsSlice.reducer;
  var createEventEmitter = optionsSlice.actions.createEventEmitter;

  // node_modules/recharts/es6/synchronisation/syncSelectors.js
  function selectSynchronisedTooltipState(state) {
    return state.tooltip.syncInteraction;
  }

  // node_modules/recharts/es6/state/chartDataSlice.js
  var initialChartDataState = {
    chartData: void 0,
    computedData: void 0,
    dataStartIndex: 0,
    dataEndIndex: 0
  };
  var chartDataSlice = createSlice({
    name: "chartData",
    initialState: initialChartDataState,
    reducers: {
      setChartData(state, action) {
        state.chartData = castDraft(action.payload);
        if (action.payload == null) {
          state.dataStartIndex = 0;
          state.dataEndIndex = 0;
          return;
        }
        if (action.payload.length > 0 && state.dataEndIndex !== action.payload.length - 1) {
          state.dataEndIndex = action.payload.length - 1;
        }
      },
      setComputedData(state, action) {
        state.computedData = action.payload;
      },
      setDataStartEndIndexes(state, action) {
        var _action$payload = action.payload, startIndex = _action$payload.startIndex, endIndex = _action$payload.endIndex;
        if (startIndex != null) {
          state.dataStartIndex = startIndex;
        }
        if (endIndex != null) {
          state.dataEndIndex = endIndex;
        }
      }
    }
  });
  var _chartDataSlice$actio = chartDataSlice.actions;
  var setChartData = _chartDataSlice$actio.setChartData;
  var setDataStartEndIndexes = _chartDataSlice$actio.setDataStartEndIndexes;
  var setComputedData = _chartDataSlice$actio.setComputedData;
  var chartDataReducer = chartDataSlice.reducer;

  // node_modules/recharts/es6/synchronisation/useChartSynchronisation.js
  var _excluded5 = ["x", "y"];
  function ownKeys18(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread18(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys18(Object(t), true).forEach(function(r3) {
        _defineProperty19(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys18(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty19(e, r2, t) {
    return (r2 = _toPropertyKey19(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey19(t) {
    var i = _toPrimitive19(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive19(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  function _objectWithoutProperties5(e, t) {
    if (null == e) return {};
    var o, r2, i = _objectWithoutPropertiesLoose5(e, t);
    if (Object.getOwnPropertySymbols) {
      var n = Object.getOwnPropertySymbols(e);
      for (r2 = 0; r2 < n.length; r2++) o = n[r2], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
    }
    return i;
  }
  function _objectWithoutPropertiesLoose5(r2, e) {
    if (null == r2) return {};
    var t = {};
    for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
      if (-1 !== e.indexOf(n)) continue;
      t[n] = r2[n];
    }
    return t;
  }
  function useTooltipSyncEventsListener() {
    var mySyncId = useAppSelector(selectSyncId);
    var myEventEmitter = useAppSelector(selectEventEmitter);
    var dispatch2 = useAppDispatch();
    var syncMethod = useAppSelector(selectSyncMethod);
    var tooltipTicks = useAppSelector(selectTooltipAxisTicks);
    var layout = useChartLayout();
    var viewBox = useViewBox();
    var className = useAppSelector((state) => state.rootProps.className);
    (0, import_react23.useEffect)(() => {
      if (mySyncId == null) {
        return noop;
      }
      var listener2 = (incomingSyncId, action, emitter) => {
        if (myEventEmitter === emitter) {
          return;
        }
        if (mySyncId !== incomingSyncId) {
          return;
        }
        if (action.payload.active === false) {
          dispatch2(setSyncInteraction({
            active: false,
            coordinate: void 0,
            dataKey: void 0,
            index: null,
            label: void 0,
            sourceViewBox: void 0,
            graphicalItemId: void 0
          }));
          return;
        }
        if (syncMethod === "index") {
          var _action$payload;
          if (viewBox && action !== null && action !== void 0 && (_action$payload = action.payload) !== null && _action$payload !== void 0 && _action$payload.coordinate && action.payload.sourceViewBox) {
            var _action$payload$coord = action.payload.coordinate, _x = _action$payload$coord.x, _y = _action$payload$coord.y, otherCoordinateProps = _objectWithoutProperties5(_action$payload$coord, _excluded5);
            var _action$payload$sourc = action.payload.sourceViewBox, sourceX = _action$payload$sourc.x, sourceY = _action$payload$sourc.y, sourceWidth = _action$payload$sourc.width, sourceHeight = _action$payload$sourc.height;
            var scaledCoordinate = _objectSpread18(_objectSpread18({}, otherCoordinateProps), {}, {
              x: viewBox.x + (sourceWidth ? (_x - sourceX) / sourceWidth : 0) * viewBox.width,
              y: viewBox.y + (sourceHeight ? (_y - sourceY) / sourceHeight : 0) * viewBox.height
            });
            dispatch2(_objectSpread18(_objectSpread18({}, action), {}, {
              payload: _objectSpread18(_objectSpread18({}, action.payload), {}, {
                coordinate: scaledCoordinate
              })
            }));
          } else {
            dispatch2(action);
          }
          return;
        }
        if (tooltipTicks == null) {
          return;
        }
        var activeTick;
        if (typeof syncMethod === "function") {
          var syncMethodParam = {
            activeTooltipIndex: action.payload.index == null ? void 0 : Number(action.payload.index),
            isTooltipActive: action.payload.active,
            activeIndex: action.payload.index == null ? void 0 : Number(action.payload.index),
            activeLabel: action.payload.label,
            activeDataKey: action.payload.dataKey,
            activeCoordinate: action.payload.coordinate
          };
          var activeTooltipIndex = syncMethod(tooltipTicks, syncMethodParam);
          activeTick = tooltipTicks[activeTooltipIndex];
        } else if (syncMethod === "value") {
          activeTick = tooltipTicks.find((tick) => String(tick.value) === action.payload.label);
        }
        var coordinate = action.payload.coordinate;
        if (coordinate == null || viewBox == null) {
          dispatch2(setSyncInteraction({
            active: false,
            coordinate: void 0,
            dataKey: void 0,
            index: null,
            label: void 0,
            sourceViewBox: void 0,
            graphicalItemId: void 0
          }));
          return;
        }
        if (activeTick == null) {
          dispatch2(setSyncInteraction({
            active: false,
            coordinate: void 0,
            dataKey: void 0,
            index: null,
            label: void 0,
            sourceViewBox: action.payload.sourceViewBox,
            graphicalItemId: void 0
          }));
          return;
        }
        var x5 = coordinate.x, y5 = coordinate.y;
        var validateChartX = Math.min(x5, viewBox.x + viewBox.width);
        var validateChartY = Math.min(y5, viewBox.y + viewBox.height);
        var activeCoordinate = {
          x: layout === "horizontal" ? activeTick.coordinate : validateChartX,
          y: layout === "horizontal" ? validateChartY : activeTick.coordinate
        };
        var syncAction = setSyncInteraction({
          active: action.payload.active,
          coordinate: activeCoordinate,
          dataKey: action.payload.dataKey,
          index: String(activeTick.index),
          label: action.payload.label,
          sourceViewBox: action.payload.sourceViewBox,
          graphicalItemId: action.payload.graphicalItemId
        });
        dispatch2(syncAction);
      };
      eventCenter.on(TOOLTIP_SYNC_EVENT, listener2);
      return () => {
        eventCenter.off(TOOLTIP_SYNC_EVENT, listener2);
      };
    }, [className, dispatch2, myEventEmitter, mySyncId, syncMethod, tooltipTicks, layout, viewBox]);
  }
  function useBrushSyncEventsListener() {
    var mySyncId = useAppSelector(selectSyncId);
    var myEventEmitter = useAppSelector(selectEventEmitter);
    var dispatch2 = useAppDispatch();
    (0, import_react23.useEffect)(() => {
      if (mySyncId == null) {
        return noop;
      }
      var listener2 = (incomingSyncId, action, emitter) => {
        if (myEventEmitter === emitter) {
          return;
        }
        if (mySyncId === incomingSyncId) {
          dispatch2(setDataStartEndIndexes(action));
        }
      };
      eventCenter.on(BRUSH_SYNC_EVENT, listener2);
      return () => {
        eventCenter.off(BRUSH_SYNC_EVENT, listener2);
      };
    }, [dispatch2, myEventEmitter, mySyncId]);
  }
  function useSynchronisedEventsFromOtherCharts() {
    var dispatch2 = useAppDispatch();
    (0, import_react23.useEffect)(() => {
      dispatch2(createEventEmitter());
    }, [dispatch2]);
    useTooltipSyncEventsListener();
    useBrushSyncEventsListener();
  }
  function useTooltipChartSynchronisation(tooltipEventType, trigger, activeCoordinate, activeLabel, activeIndex, isTooltipActive) {
    var activeDataKey = useAppSelector((state) => selectTooltipDataKey(state, tooltipEventType, trigger));
    var activeGraphicalItemId = useAppSelector(selectActiveTooltipGraphicalItemId);
    var eventEmitterSymbol = useAppSelector(selectEventEmitter);
    var syncId = useAppSelector(selectSyncId);
    var syncMethod = useAppSelector(selectSyncMethod);
    var tooltipState = useAppSelector(selectSynchronisedTooltipState);
    var isReceivingSynchronisation = (tooltipState === null || tooltipState === void 0 ? void 0 : tooltipState.sourceViewBox) != null;
    var viewBox = useViewBox();
    (0, import_react23.useEffect)(() => {
      if (isReceivingSynchronisation) {
        return;
      }
      if (syncId == null) {
        return;
      }
      if (eventEmitterSymbol == null) {
        return;
      }
      var syncAction = setSyncInteraction({
        active: isTooltipActive,
        coordinate: activeCoordinate,
        dataKey: activeDataKey,
        index: activeIndex,
        label: typeof activeLabel === "number" ? String(activeLabel) : activeLabel,
        sourceViewBox: viewBox,
        graphicalItemId: activeGraphicalItemId
      });
      eventCenter.emit(TOOLTIP_SYNC_EVENT, syncId, syncAction, eventEmitterSymbol);
    }, [isReceivingSynchronisation, activeCoordinate, activeDataKey, activeGraphicalItemId, activeIndex, activeLabel, eventEmitterSymbol, syncId, syncMethod, isTooltipActive, viewBox]);
  }

  // node_modules/recharts/es6/component/Tooltip.js
  function ownKeys19(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread19(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys19(Object(t), true).forEach(function(r3) {
        _defineProperty20(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys19(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty20(e, r2, t) {
    return (r2 = _toPropertyKey20(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey20(t) {
    var i = _toPrimitive20(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive20(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  function _slicedToArray10(r2, e) {
    return _arrayWithHoles10(r2) || _iterableToArrayLimit10(r2, e) || _unsupportedIterableToArray10(r2, e) || _nonIterableRest10();
  }
  function _nonIterableRest10() {
    throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  function _unsupportedIterableToArray10(r2, a2) {
    if (r2) {
      if ("string" == typeof r2) return _arrayLikeToArray10(r2, a2);
      var t = {}.toString.call(r2).slice(8, -1);
      return "Object" === t && r2.constructor && (t = r2.constructor.name), "Map" === t || "Set" === t ? Array.from(r2) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray10(r2, a2) : void 0;
    }
  }
  function _arrayLikeToArray10(r2, a2) {
    (null == a2 || a2 > r2.length) && (a2 = r2.length);
    for (var e = 0, n = Array(a2); e < a2; e++) n[e] = r2[e];
    return n;
  }
  function _iterableToArrayLimit10(r2, l) {
    var t = null == r2 ? null : "undefined" != typeof Symbol && r2[Symbol.iterator] || r2["@@iterator"];
    if (null != t) {
      var e, n, i, u, a2 = [], f = true, o = false;
      try {
        if (i = (t = t.call(r2)).next, 0 === l) {
          if (Object(t) !== t) return;
          f = false;
        } else for (; !(f = (e = i.call(t)).done) && (a2.push(e.value), a2.length !== l); f = true) ;
      } catch (r3) {
        o = true, n = r3;
      } finally {
        try {
          if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return;
        } finally {
          if (o) throw n;
        }
      }
      return a2;
    }
  }
  function _arrayWithHoles10(r2) {
    if (Array.isArray(r2)) return r2;
  }
  function defaultUniqBy(entry) {
    return entry.dataKey;
  }
  function renderContent(content, props) {
    if (/* @__PURE__ */ React13.isValidElement(content)) {
      return /* @__PURE__ */ React13.cloneElement(content, props);
    }
    if (typeof content === "function") {
      return /* @__PURE__ */ React13.createElement(content, props);
    }
    return /* @__PURE__ */ React13.createElement(DefaultTooltipContent, props);
  }
  var emptyPayload = [];
  var defaultTooltipProps = {
    allowEscapeViewBox: {
      x: false,
      y: false
    },
    animationDuration: 400,
    animationEasing: "ease",
    axisId: 0,
    contentStyle: {},
    cursor: true,
    filterNull: true,
    includeHidden: false,
    isAnimationActive: "auto",
    itemSorter: "name",
    itemStyle: {},
    labelStyle: {},
    offset: 10,
    reverseDirection: {
      x: false,
      y: false
    },
    separator: " : ",
    trigger: "hover",
    useTranslate3d: false,
    wrapperStyle: {}
  };
  function Tooltip(outsideProps) {
    var _useAppSelector, _ref2;
    var props = resolveDefaultProps(outsideProps, defaultTooltipProps);
    var activeFromProps = props.active, allowEscapeViewBox = props.allowEscapeViewBox, animationDuration = props.animationDuration, animationEasing = props.animationEasing, content = props.content, filterNull = props.filterNull, isAnimationActive = props.isAnimationActive, offset = props.offset, payloadUniqBy = props.payloadUniqBy, position = props.position, reverseDirection = props.reverseDirection, useTranslate3d = props.useTranslate3d, wrapperStyle = props.wrapperStyle, cursor = props.cursor, shared = props.shared, trigger = props.trigger, defaultIndex = props.defaultIndex, portalFromProps = props.portal, axisId = props.axisId;
    var dispatch2 = useAppDispatch();
    var defaultIndexAsString = typeof defaultIndex === "number" ? String(defaultIndex) : defaultIndex;
    (0, import_react24.useEffect)(() => {
      dispatch2(setTooltipSettingsState({
        shared,
        trigger,
        axisId,
        active: activeFromProps,
        defaultIndex: defaultIndexAsString
      }));
    }, [dispatch2, shared, trigger, axisId, activeFromProps, defaultIndexAsString]);
    var viewBox = useViewBox();
    var accessibilityLayer = useAccessibilityLayer();
    var tooltipEventType = useTooltipEventType(shared);
    var _ref3 = (_useAppSelector = useAppSelector((state) => selectIsTooltipActive2(state, tooltipEventType, trigger, defaultIndexAsString))) !== null && _useAppSelector !== void 0 ? _useAppSelector : {}, activeIndex = _ref3.activeIndex, isActive = _ref3.isActive;
    var payloadFromRedux = useAppSelector((state) => selectTooltipPayload(state, tooltipEventType, trigger, defaultIndexAsString));
    var labelFromRedux = useAppSelector((state) => selectActiveLabel2(state, tooltipEventType, trigger, defaultIndexAsString));
    var coordinate = useAppSelector((state) => selectActiveCoordinate(state, tooltipEventType, trigger, defaultIndexAsString));
    var payload = payloadFromRedux;
    var tooltipPortalFromContext = useTooltipPortal();
    var finalIsActive = (_ref2 = activeFromProps !== null && activeFromProps !== void 0 ? activeFromProps : isActive) !== null && _ref2 !== void 0 ? _ref2 : false;
    var _useElementOffset = useElementOffset([payload, finalIsActive]), _useElementOffset2 = _slicedToArray10(_useElementOffset, 2), lastBoundingBox = _useElementOffset2[0], updateBoundingBox = _useElementOffset2[1];
    var finalLabel = tooltipEventType === "axis" ? labelFromRedux : void 0;
    useTooltipChartSynchronisation(tooltipEventType, trigger, coordinate, finalLabel, activeIndex, finalIsActive);
    var tooltipPortal = portalFromProps !== null && portalFromProps !== void 0 ? portalFromProps : tooltipPortalFromContext;
    if (tooltipPortal == null || viewBox == null || tooltipEventType == null) {
      return null;
    }
    var finalPayload = payload !== null && payload !== void 0 ? payload : emptyPayload;
    if (!finalIsActive) {
      finalPayload = emptyPayload;
    }
    if (filterNull && finalPayload.length) {
      finalPayload = getUniqPayload(finalPayload.filter((entry) => entry.value != null && (entry.hide !== true || props.includeHidden)), payloadUniqBy, defaultUniqBy);
    }
    var hasPayload = finalPayload.length > 0;
    var tooltipContentProps = _objectSpread19(_objectSpread19({}, props), {}, {
      payload: finalPayload,
      label: finalLabel,
      active: finalIsActive,
      activeIndex,
      coordinate,
      accessibilityLayer
    });
    var tooltipElement = /* @__PURE__ */ React13.createElement(TooltipBoundingBox, {
      allowEscapeViewBox,
      animationDuration,
      animationEasing,
      isAnimationActive,
      active: finalIsActive,
      coordinate,
      hasPayload,
      offset,
      position,
      reverseDirection,
      useTranslate3d,
      viewBox,
      wrapperStyle,
      lastBoundingBox,
      innerRef: updateBoundingBox,
      hasPortalFromProps: Boolean(portalFromProps)
    }, renderContent(content, tooltipContentProps));
    return /* @__PURE__ */ React13.createElement(React13.Fragment, null, /* @__PURE__ */ (0, import_react_dom2.createPortal)(tooltipElement, tooltipPortal), finalIsActive && /* @__PURE__ */ React13.createElement(Cursor, {
      cursor,
      tooltipEventType,
      coordinate,
      payload: finalPayload,
      index: activeIndex
    }));
  }

  // node_modules/recharts/es6/component/Cell.js
  var Cell = (_props) => null;
  Cell.displayName = "Cell";

  // node_modules/recharts/es6/component/Text.js
  var React14 = __toESM(require_react());
  var import_react25 = __toESM(require_react());

  // node_modules/recharts/es6/util/LRUCache.js
  function _defineProperty21(e, r2, t) {
    return (r2 = _toPropertyKey21(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey21(t) {
    var i = _toPrimitive21(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive21(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  var LRUCache = class {
    constructor(maxSize) {
      _defineProperty21(this, "cache", /* @__PURE__ */ new Map());
      this.maxSize = maxSize;
    }
    get(key) {
      var value2 = this.cache.get(key);
      if (value2 !== void 0) {
        this.cache.delete(key);
        this.cache.set(key, value2);
      }
      return value2;
    }
    set(key, value2) {
      if (this.cache.has(key)) {
        this.cache.delete(key);
      } else if (this.cache.size >= this.maxSize) {
        var firstKey = this.cache.keys().next().value;
        if (firstKey != null) {
          this.cache.delete(firstKey);
        }
      }
      this.cache.set(key, value2);
    }
    clear() {
      this.cache.clear();
    }
    size() {
      return this.cache.size;
    }
  };

  // node_modules/recharts/es6/util/DOMUtils.js
  function ownKeys20(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread20(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys20(Object(t), true).forEach(function(r3) {
        _defineProperty22(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys20(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty22(e, r2, t) {
    return (r2 = _toPropertyKey22(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey22(t) {
    var i = _toPrimitive22(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive22(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  var defaultConfig = {
    cacheSize: 2e3,
    enableCache: true
  };
  var currentConfig = _objectSpread20({}, defaultConfig);
  var stringCache = new LRUCache(currentConfig.cacheSize);
  var SPAN_STYLE = {
    position: "absolute",
    top: "-20000px",
    left: 0,
    padding: 0,
    margin: 0,
    border: "none",
    whiteSpace: "pre"
  };
  var MEASUREMENT_SPAN_ID = "recharts_measurement_span";
  function createCacheKey(text, style) {
    var fontSize = style.fontSize || "";
    var fontFamily = style.fontFamily || "";
    var fontWeight = style.fontWeight || "";
    var fontStyle = style.fontStyle || "";
    var letterSpacing = style.letterSpacing || "";
    var textTransform = style.textTransform || "";
    return "".concat(text, "|").concat(fontSize, "|").concat(fontFamily, "|").concat(fontWeight, "|").concat(fontStyle, "|").concat(letterSpacing, "|").concat(textTransform);
  }
  var measureTextWithDOM = (text, style) => {
    try {
      var measurementSpan = document.getElementById(MEASUREMENT_SPAN_ID);
      if (!measurementSpan) {
        measurementSpan = document.createElement("span");
        measurementSpan.setAttribute("id", MEASUREMENT_SPAN_ID);
        measurementSpan.setAttribute("aria-hidden", "true");
        document.body.appendChild(measurementSpan);
      }
      Object.assign(measurementSpan.style, SPAN_STYLE, style);
      measurementSpan.textContent = "".concat(text);
      var rect = measurementSpan.getBoundingClientRect();
      return {
        width: rect.width,
        height: rect.height
      };
    } catch (_unused) {
      return {
        width: 0,
        height: 0
      };
    }
  };
  var getStringSize = function getStringSize2(text) {
    var style = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    if (text === void 0 || text === null || Global.isSsr) {
      return {
        width: 0,
        height: 0
      };
    }
    if (!currentConfig.enableCache) {
      return measureTextWithDOM(text, style);
    }
    var cacheKey = createCacheKey(text, style);
    var cachedResult = stringCache.get(cacheKey);
    if (cachedResult) {
      return cachedResult;
    }
    var result = measureTextWithDOM(text, style);
    stringCache.set(cacheKey, result);
    return result;
  };

  // node_modules/recharts/es6/util/ReduceCSSCalc.js
  var _DecimalCSS;
  function _slicedToArray11(r2, e) {
    return _arrayWithHoles11(r2) || _iterableToArrayLimit11(r2, e) || _unsupportedIterableToArray11(r2, e) || _nonIterableRest11();
  }
  function _nonIterableRest11() {
    throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  function _unsupportedIterableToArray11(r2, a2) {
    if (r2) {
      if ("string" == typeof r2) return _arrayLikeToArray11(r2, a2);
      var t = {}.toString.call(r2).slice(8, -1);
      return "Object" === t && r2.constructor && (t = r2.constructor.name), "Map" === t || "Set" === t ? Array.from(r2) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray11(r2, a2) : void 0;
    }
  }
  function _arrayLikeToArray11(r2, a2) {
    (null == a2 || a2 > r2.length) && (a2 = r2.length);
    for (var e = 0, n = Array(a2); e < a2; e++) n[e] = r2[e];
    return n;
  }
  function _iterableToArrayLimit11(r2, l) {
    var t = null == r2 ? null : "undefined" != typeof Symbol && r2[Symbol.iterator] || r2["@@iterator"];
    if (null != t) {
      var e, n, i, u, a2 = [], f = true, o = false;
      try {
        if (i = (t = t.call(r2)).next, 0 === l) {
          if (Object(t) !== t) return;
          f = false;
        } else for (; !(f = (e = i.call(t)).done) && (a2.push(e.value), a2.length !== l); f = true) ;
      } catch (r3) {
        o = true, n = r3;
      } finally {
        try {
          if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return;
        } finally {
          if (o) throw n;
        }
      }
      return a2;
    }
  }
  function _arrayWithHoles11(r2) {
    if (Array.isArray(r2)) return r2;
  }
  function _defineProperty23(e, r2, t) {
    return (r2 = _toPropertyKey23(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey23(t) {
    var i = _toPrimitive23(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive23(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  var MULTIPLY_OR_DIVIDE_REGEX = /(-?\d+(?:\.\d+)?[a-zA-Z%]*)([*/])(-?\d+(?:\.\d+)?[a-zA-Z%]*)/;
  var ADD_OR_SUBTRACT_REGEX = /(-?\d+(?:\.\d+)?[a-zA-Z%]*)([+-])(-?\d+(?:\.\d+)?[a-zA-Z%]*)/;
  var CSS_LENGTH_UNIT_REGEX = /^(px|cm|vh|vw|em|rem|%|mm|in|pt|pc|ex|ch|vmin|vmax|Q)$/;
  var NUM_SPLIT_REGEX = /(-?\d+(?:\.\d+)?)([a-zA-Z%]+)?/;
  var CONVERSION_RATES = {
    cm: 96 / 2.54,
    mm: 96 / 25.4,
    pt: 96 / 72,
    pc: 96 / 6,
    in: 96,
    Q: 96 / (2.54 * 40),
    px: 1
  };
  var FIXED_CSS_LENGTH_UNITS = ["cm", "mm", "pt", "pc", "in", "Q", "px"];
  function isSupportedUnit(unit2) {
    return FIXED_CSS_LENGTH_UNITS.includes(unit2);
  }
  var STR_NAN = "NaN";
  function convertToPx(value2, unit2) {
    return value2 * CONVERSION_RATES[unit2];
  }
  var DecimalCSS = class _DecimalCSS2 {
    static parse(str) {
      var _NUM_SPLIT_REGEX$exec;
      var _ref2 = (_NUM_SPLIT_REGEX$exec = NUM_SPLIT_REGEX.exec(str)) !== null && _NUM_SPLIT_REGEX$exec !== void 0 ? _NUM_SPLIT_REGEX$exec : [], _ref22 = _slicedToArray11(_ref2, 3), numStr = _ref22[1], unit2 = _ref22[2];
      if (numStr == null) {
        return _DecimalCSS2.NaN;
      }
      return new _DecimalCSS2(parseFloat(numStr), unit2 !== null && unit2 !== void 0 ? unit2 : "");
    }
    constructor(num, unit2) {
      this.num = num;
      this.unit = unit2;
      this.num = num;
      this.unit = unit2;
      if (isNan(num)) {
        this.unit = "";
      }
      if (unit2 !== "" && !CSS_LENGTH_UNIT_REGEX.test(unit2)) {
        this.num = NaN;
        this.unit = "";
      }
      if (isSupportedUnit(unit2)) {
        this.num = convertToPx(num, unit2);
        this.unit = "px";
      }
    }
    add(other) {
      if (this.unit !== other.unit) {
        return new _DecimalCSS2(NaN, "");
      }
      return new _DecimalCSS2(this.num + other.num, this.unit);
    }
    subtract(other) {
      if (this.unit !== other.unit) {
        return new _DecimalCSS2(NaN, "");
      }
      return new _DecimalCSS2(this.num - other.num, this.unit);
    }
    multiply(other) {
      if (this.unit !== "" && other.unit !== "" && this.unit !== other.unit) {
        return new _DecimalCSS2(NaN, "");
      }
      return new _DecimalCSS2(this.num * other.num, this.unit || other.unit);
    }
    divide(other) {
      if (this.unit !== "" && other.unit !== "" && this.unit !== other.unit) {
        return new _DecimalCSS2(NaN, "");
      }
      return new _DecimalCSS2(this.num / other.num, this.unit || other.unit);
    }
    toString() {
      return "".concat(this.num).concat(this.unit);
    }
    isNaN() {
      return isNan(this.num);
    }
  };
  _DecimalCSS = DecimalCSS;
  _defineProperty23(DecimalCSS, "NaN", new _DecimalCSS(NaN, ""));
  function calculateArithmetic(expr) {
    if (expr == null || expr.includes(STR_NAN)) {
      return STR_NAN;
    }
    var newExpr = expr;
    while (newExpr.includes("*") || newExpr.includes("/")) {
      var _MULTIPLY_OR_DIVIDE_R;
      var _ref3 = (_MULTIPLY_OR_DIVIDE_R = MULTIPLY_OR_DIVIDE_REGEX.exec(newExpr)) !== null && _MULTIPLY_OR_DIVIDE_R !== void 0 ? _MULTIPLY_OR_DIVIDE_R : [], _ref4 = _slicedToArray11(_ref3, 4), leftOperand = _ref4[1], operator = _ref4[2], rightOperand = _ref4[3];
      var lTs = DecimalCSS.parse(leftOperand !== null && leftOperand !== void 0 ? leftOperand : "");
      var rTs = DecimalCSS.parse(rightOperand !== null && rightOperand !== void 0 ? rightOperand : "");
      var result = operator === "*" ? lTs.multiply(rTs) : lTs.divide(rTs);
      if (result.isNaN()) {
        return STR_NAN;
      }
      newExpr = newExpr.replace(MULTIPLY_OR_DIVIDE_REGEX, result.toString());
    }
    while (newExpr.includes("+") || /.-\d+(?:\.\d+)?/.test(newExpr)) {
      var _ADD_OR_SUBTRACT_REGE;
      var _ref5 = (_ADD_OR_SUBTRACT_REGE = ADD_OR_SUBTRACT_REGEX.exec(newExpr)) !== null && _ADD_OR_SUBTRACT_REGE !== void 0 ? _ADD_OR_SUBTRACT_REGE : [], _ref6 = _slicedToArray11(_ref5, 4), _leftOperand = _ref6[1], _operator = _ref6[2], _rightOperand = _ref6[3];
      var _lTs = DecimalCSS.parse(_leftOperand !== null && _leftOperand !== void 0 ? _leftOperand : "");
      var _rTs = DecimalCSS.parse(_rightOperand !== null && _rightOperand !== void 0 ? _rightOperand : "");
      var _result = _operator === "+" ? _lTs.add(_rTs) : _lTs.subtract(_rTs);
      if (_result.isNaN()) {
        return STR_NAN;
      }
      newExpr = newExpr.replace(ADD_OR_SUBTRACT_REGEX, _result.toString());
    }
    return newExpr;
  }
  var PARENTHESES_REGEX = /\(([^()]*)\)/;
  function calculateParentheses(expr) {
    var newExpr = expr;
    var match;
    while ((match = PARENTHESES_REGEX.exec(newExpr)) != null) {
      var _match = match, _match2 = _slicedToArray11(_match, 2), parentheticalExpression = _match2[1];
      newExpr = newExpr.replace(PARENTHESES_REGEX, calculateArithmetic(parentheticalExpression));
    }
    return newExpr;
  }
  function evaluateExpression(expression) {
    var newExpr = expression.replace(/\s+/g, "");
    newExpr = calculateParentheses(newExpr);
    newExpr = calculateArithmetic(newExpr);
    return newExpr;
  }
  function safeEvaluateExpression(expression) {
    try {
      return evaluateExpression(expression);
    } catch (_unused) {
      return STR_NAN;
    }
  }
  function reduceCSSCalc(expression) {
    var result = safeEvaluateExpression(expression.slice(5, -1));
    if (result === STR_NAN) {
      return "";
    }
    return result;
  }

  // node_modules/recharts/es6/component/Text.js
  var _excluded6 = ["x", "y", "lineHeight", "capHeight", "fill", "scaleToFit", "textAnchor", "verticalAnchor"];
  var _excluded23 = ["dx", "dy", "angle", "className", "breakAll"];
  function _extends9() {
    return _extends9 = Object.assign ? Object.assign.bind() : function(n) {
      for (var e = 1; e < arguments.length; e++) {
        var t = arguments[e];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends9.apply(null, arguments);
  }
  function _objectWithoutProperties6(e, t) {
    if (null == e) return {};
    var o, r2, i = _objectWithoutPropertiesLoose6(e, t);
    if (Object.getOwnPropertySymbols) {
      var n = Object.getOwnPropertySymbols(e);
      for (r2 = 0; r2 < n.length; r2++) o = n[r2], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
    }
    return i;
  }
  function _objectWithoutPropertiesLoose6(r2, e) {
    if (null == r2) return {};
    var t = {};
    for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
      if (-1 !== e.indexOf(n)) continue;
      t[n] = r2[n];
    }
    return t;
  }
  function _slicedToArray12(r2, e) {
    return _arrayWithHoles12(r2) || _iterableToArrayLimit12(r2, e) || _unsupportedIterableToArray12(r2, e) || _nonIterableRest12();
  }
  function _nonIterableRest12() {
    throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  function _unsupportedIterableToArray12(r2, a2) {
    if (r2) {
      if ("string" == typeof r2) return _arrayLikeToArray12(r2, a2);
      var t = {}.toString.call(r2).slice(8, -1);
      return "Object" === t && r2.constructor && (t = r2.constructor.name), "Map" === t || "Set" === t ? Array.from(r2) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray12(r2, a2) : void 0;
    }
  }
  function _arrayLikeToArray12(r2, a2) {
    (null == a2 || a2 > r2.length) && (a2 = r2.length);
    for (var e = 0, n = Array(a2); e < a2; e++) n[e] = r2[e];
    return n;
  }
  function _iterableToArrayLimit12(r2, l) {
    var t = null == r2 ? null : "undefined" != typeof Symbol && r2[Symbol.iterator] || r2["@@iterator"];
    if (null != t) {
      var e, n, i, u, a2 = [], f = true, o = false;
      try {
        if (i = (t = t.call(r2)).next, 0 === l) {
          if (Object(t) !== t) return;
          f = false;
        } else for (; !(f = (e = i.call(t)).done) && (a2.push(e.value), a2.length !== l); f = true) ;
      } catch (r3) {
        o = true, n = r3;
      } finally {
        try {
          if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return;
        } finally {
          if (o) throw n;
        }
      }
      return a2;
    }
  }
  function _arrayWithHoles12(r2) {
    if (Array.isArray(r2)) return r2;
  }
  var BREAKING_SPACES = /[ \f\n\r\t\v\u2028\u2029]+/;
  var calculateWordWidths = (_ref2) => {
    var children = _ref2.children, breakAll = _ref2.breakAll, style = _ref2.style;
    try {
      var words = [];
      if (!isNullish(children)) {
        if (breakAll) {
          words = children.toString().split("");
        } else {
          words = children.toString().split(BREAKING_SPACES);
        }
      }
      var wordsWithComputedWidth = words.map((word) => ({
        word,
        width: getStringSize(word, style).width
      }));
      var spaceWidth = breakAll ? 0 : getStringSize(" ", style).width;
      return {
        wordsWithComputedWidth,
        spaceWidth
      };
    } catch (_unused) {
      return null;
    }
  };
  function isValidTextAnchor(value2) {
    return value2 === "start" || value2 === "middle" || value2 === "end" || value2 === "inherit";
  }
  function isRenderableText(val) {
    return isNullish(val) || typeof val === "string" || typeof val === "number" || typeof val === "boolean";
  }
  var calculate = (words, lineWidth, spaceWidth, scaleToFit) => words.reduce((result, _ref2) => {
    var word = _ref2.word, width = _ref2.width;
    var currentLine = result[result.length - 1];
    if (currentLine && width != null && (lineWidth == null || scaleToFit || currentLine.width + width + spaceWidth < Number(lineWidth))) {
      currentLine.words.push(word);
      currentLine.width += width + spaceWidth;
    } else {
      var newLine = {
        words: [word],
        width
      };
      result.push(newLine);
    }
    return result;
  }, []);
  var findLongestLine = (words) => words.reduce((a2, b) => a2.width > b.width ? a2 : b);
  var suffix = "…";
  var checkOverflow = (text, index2, breakAll, style, maxLines, lineWidth, spaceWidth, scaleToFit) => {
    var tempText = text.slice(0, index2);
    var words = calculateWordWidths({
      breakAll,
      style,
      children: tempText + suffix
    });
    if (!words) {
      return [false, []];
    }
    var result = calculate(words.wordsWithComputedWidth, lineWidth, spaceWidth, scaleToFit);
    var doesOverflow = result.length > maxLines || findLongestLine(result).width > Number(lineWidth);
    return [doesOverflow, result];
  };
  var calculateWordsByLines = (_ref3, initialWordsWithComputedWith, spaceWidth, lineWidth, scaleToFit) => {
    var maxLines = _ref3.maxLines, children = _ref3.children, style = _ref3.style, breakAll = _ref3.breakAll;
    var shouldLimitLines = isNumber(maxLines);
    var text = String(children);
    var originalResult = calculate(initialWordsWithComputedWith, lineWidth, spaceWidth, scaleToFit);
    if (!shouldLimitLines || scaleToFit) {
      return originalResult;
    }
    var overflows = originalResult.length > maxLines || findLongestLine(originalResult).width > Number(lineWidth);
    if (!overflows) {
      return originalResult;
    }
    var start = 0;
    var end = text.length - 1;
    var iterations = 0;
    var trimmedResult;
    while (start <= end && iterations <= text.length - 1) {
      var middle = Math.floor((start + end) / 2);
      var prev = middle - 1;
      var _checkOverflow = checkOverflow(text, prev, breakAll, style, maxLines, lineWidth, spaceWidth, scaleToFit), _checkOverflow2 = _slicedToArray12(_checkOverflow, 2), doesPrevOverflow = _checkOverflow2[0], result = _checkOverflow2[1];
      var _checkOverflow3 = checkOverflow(text, middle, breakAll, style, maxLines, lineWidth, spaceWidth, scaleToFit), _checkOverflow4 = _slicedToArray12(_checkOverflow3, 1), doesMiddleOverflow = _checkOverflow4[0];
      if (!doesPrevOverflow && !doesMiddleOverflow) {
        start = middle + 1;
      }
      if (doesPrevOverflow && doesMiddleOverflow) {
        end = middle - 1;
      }
      if (!doesPrevOverflow && doesMiddleOverflow) {
        trimmedResult = result;
        break;
      }
      iterations++;
    }
    return trimmedResult || originalResult;
  };
  var getWordsWithoutCalculate = (children) => {
    var words = !isNullish(children) ? children.toString().split(BREAKING_SPACES) : [];
    return [{
      words,
      width: void 0
    }];
  };
  var getWordsByLines = (_ref4) => {
    var width = _ref4.width, scaleToFit = _ref4.scaleToFit, children = _ref4.children, style = _ref4.style, breakAll = _ref4.breakAll, maxLines = _ref4.maxLines;
    if ((width || scaleToFit) && !Global.isSsr) {
      var wordsWithComputedWidth, spaceWidth;
      var wordWidths = calculateWordWidths({
        breakAll,
        children,
        style
      });
      if (wordWidths) {
        var wcw = wordWidths.wordsWithComputedWidth, sw = wordWidths.spaceWidth;
        wordsWithComputedWidth = wcw;
        spaceWidth = sw;
      } else {
        return getWordsWithoutCalculate(children);
      }
      return calculateWordsByLines({
        breakAll,
        children,
        maxLines,
        style
      }, wordsWithComputedWidth, spaceWidth, width, Boolean(scaleToFit));
    }
    return getWordsWithoutCalculate(children);
  };
  var DEFAULT_FILL = "#808080";
  var textDefaultProps = {
    angle: 0,
    breakAll: false,
    // Magic number from d3
    capHeight: "0.71em",
    fill: DEFAULT_FILL,
    lineHeight: "1em",
    scaleToFit: false,
    textAnchor: "start",
    // Maintain compat with existing charts / default SVG behavior
    verticalAnchor: "end",
    x: 0,
    y: 0
  };
  var Text = /* @__PURE__ */ (0, import_react25.forwardRef)((outsideProps, ref) => {
    var _resolveDefaultProps = resolveDefaultProps(outsideProps, textDefaultProps), propsX = _resolveDefaultProps.x, propsY = _resolveDefaultProps.y, lineHeight = _resolveDefaultProps.lineHeight, capHeight = _resolveDefaultProps.capHeight, fill = _resolveDefaultProps.fill, scaleToFit = _resolveDefaultProps.scaleToFit, textAnchor = _resolveDefaultProps.textAnchor, verticalAnchor = _resolveDefaultProps.verticalAnchor, props = _objectWithoutProperties6(_resolveDefaultProps, _excluded6);
    var wordsByLines = (0, import_react25.useMemo)(() => {
      return getWordsByLines({
        breakAll: props.breakAll,
        children: props.children,
        maxLines: props.maxLines,
        scaleToFit,
        style: props.style,
        width: props.width
      });
    }, [props.breakAll, props.children, props.maxLines, scaleToFit, props.style, props.width]);
    var dx = props.dx, dy = props.dy, angle = props.angle, className = props.className, breakAll = props.breakAll, textProps = _objectWithoutProperties6(props, _excluded23);
    if (!isNumOrStr(propsX) || !isNumOrStr(propsY) || wordsByLines.length === 0) {
      return null;
    }
    var x5 = Number(propsX) + (isNumber(dx) ? dx : 0);
    var y5 = Number(propsY) + (isNumber(dy) ? dy : 0);
    if (!isWellBehavedNumber(x5) || !isWellBehavedNumber(y5)) {
      return null;
    }
    var startDy;
    switch (verticalAnchor) {
      case "start":
        startDy = reduceCSSCalc("calc(".concat(capHeight, ")"));
        break;
      case "middle":
        startDy = reduceCSSCalc("calc(".concat((wordsByLines.length - 1) / 2, " * -").concat(lineHeight, " + (").concat(capHeight, " / 2))"));
        break;
      default:
        startDy = reduceCSSCalc("calc(".concat(wordsByLines.length - 1, " * -").concat(lineHeight, ")"));
        break;
    }
    var transforms = [];
    var firstLine = wordsByLines[0];
    if (scaleToFit && firstLine != null) {
      var lineWidth = firstLine.width;
      var width = props.width;
      transforms.push("scale(".concat(isNumber(width) && isNumber(lineWidth) ? width / lineWidth : 1, ")"));
    }
    if (angle) {
      transforms.push("rotate(".concat(angle, ", ").concat(x5, ", ").concat(y5, ")"));
    }
    if (transforms.length) {
      textProps.transform = transforms.join(" ");
    }
    return /* @__PURE__ */ React14.createElement("text", _extends9({}, svgPropertiesAndEvents(textProps), {
      ref,
      x: x5,
      y: y5,
      className: clsx("recharts-text", className),
      textAnchor,
      fill: fill.includes("url") ? DEFAULT_FILL : fill
    }), wordsByLines.map((line, index2) => {
      var words = line.words.join(breakAll ? "" : " ");
      return (
        // duplicate words will cause duplicate keys which is why we add the array index here
        /* @__PURE__ */ React14.createElement("tspan", {
          x: x5,
          dy: index2 === 0 ? startDy : lineHeight,
          key: "".concat(words, "-").concat(index2)
        }, words)
      );
    }));
  });
  Text.displayName = "Text";

  // node_modules/recharts/es6/component/Label.js
  var React15 = __toESM(require_react());
  var import_react26 = __toESM(require_react());

  // node_modules/recharts/es6/cartesian/getCartesianPosition.js
  function ownKeys21(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread21(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys21(Object(t), true).forEach(function(r3) {
        _defineProperty24(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys21(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty24(e, r2, t) {
    return (r2 = _toPropertyKey24(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey24(t) {
    var i = _toPrimitive24(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive24(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  var getCartesianPosition = (options) => {
    var viewBox = options.viewBox, position = options.position, _options$offset = options.offset, offset = _options$offset === void 0 ? 0 : _options$offset, parentViewBoxFromOptions = options.parentViewBox, clamp = options.clamp;
    var _cartesianViewBoxToTr = cartesianViewBoxToTrapezoid(viewBox), x5 = _cartesianViewBoxToTr.x, y5 = _cartesianViewBoxToTr.y, height = _cartesianViewBoxToTr.height, upperWidth = _cartesianViewBoxToTr.upperWidth, lowerWidth = _cartesianViewBoxToTr.lowerWidth;
    var upperX = x5;
    var lowerX = x5 + (upperWidth - lowerWidth) / 2;
    var middleX = (upperX + lowerX) / 2;
    var midHeightWidth = (upperWidth + lowerWidth) / 2;
    var centerX = upperX + upperWidth / 2;
    var verticalSign = height >= 0 ? 1 : -1;
    var verticalOffset = verticalSign * offset;
    var verticalEnd = verticalSign > 0 ? "end" : "start";
    var verticalStart = verticalSign > 0 ? "start" : "end";
    var horizontalSign = upperWidth >= 0 ? 1 : -1;
    var horizontalOffset = horizontalSign * offset;
    var horizontalEnd = horizontalSign > 0 ? "end" : "start";
    var horizontalStart = horizontalSign > 0 ? "start" : "end";
    var parentViewBox = parentViewBoxFromOptions;
    if (position === "top") {
      var result = {
        x: upperX + upperWidth / 2,
        y: y5 - verticalOffset,
        horizontalAnchor: "middle",
        verticalAnchor: verticalEnd
      };
      if (clamp && parentViewBox) {
        result.height = Math.max(y5 - parentViewBox.y, 0);
        result.width = upperWidth;
      }
      return result;
    }
    if (position === "bottom") {
      var _result = {
        x: lowerX + lowerWidth / 2,
        y: y5 + height + verticalOffset,
        horizontalAnchor: "middle",
        verticalAnchor: verticalStart
      };
      if (clamp && parentViewBox) {
        _result.height = Math.max(parentViewBox.y + parentViewBox.height - (y5 + height), 0);
        _result.width = lowerWidth;
      }
      return _result;
    }
    if (position === "left") {
      var _result2 = {
        x: middleX - horizontalOffset,
        y: y5 + height / 2,
        horizontalAnchor: horizontalEnd,
        verticalAnchor: "middle"
      };
      if (clamp && parentViewBox) {
        _result2.width = Math.max(_result2.x - parentViewBox.x, 0);
        _result2.height = height;
      }
      return _result2;
    }
    if (position === "right") {
      var _result3 = {
        x: middleX + midHeightWidth + horizontalOffset,
        y: y5 + height / 2,
        horizontalAnchor: horizontalStart,
        verticalAnchor: "middle"
      };
      if (clamp && parentViewBox) {
        _result3.width = Math.max(parentViewBox.x + parentViewBox.width - _result3.x, 0);
        _result3.height = height;
      }
      return _result3;
    }
    var sizeAttrs = clamp && parentViewBox ? {
      width: midHeightWidth,
      height
    } : {};
    if (position === "insideLeft") {
      return _objectSpread21({
        x: middleX + horizontalOffset,
        y: y5 + height / 2,
        horizontalAnchor: horizontalStart,
        verticalAnchor: "middle"
      }, sizeAttrs);
    }
    if (position === "insideRight") {
      return _objectSpread21({
        x: middleX + midHeightWidth - horizontalOffset,
        y: y5 + height / 2,
        horizontalAnchor: horizontalEnd,
        verticalAnchor: "middle"
      }, sizeAttrs);
    }
    if (position === "insideTop") {
      return _objectSpread21({
        x: upperX + upperWidth / 2,
        y: y5 + verticalOffset,
        horizontalAnchor: "middle",
        verticalAnchor: verticalStart
      }, sizeAttrs);
    }
    if (position === "insideBottom") {
      return _objectSpread21({
        x: lowerX + lowerWidth / 2,
        y: y5 + height - verticalOffset,
        horizontalAnchor: "middle",
        verticalAnchor: verticalEnd
      }, sizeAttrs);
    }
    if (position === "insideTopLeft") {
      return _objectSpread21({
        x: upperX + horizontalOffset,
        y: y5 + verticalOffset,
        horizontalAnchor: horizontalStart,
        verticalAnchor: verticalStart
      }, sizeAttrs);
    }
    if (position === "insideTopRight") {
      return _objectSpread21({
        x: upperX + upperWidth - horizontalOffset,
        y: y5 + verticalOffset,
        horizontalAnchor: horizontalEnd,
        verticalAnchor: verticalStart
      }, sizeAttrs);
    }
    if (position === "insideBottomLeft") {
      return _objectSpread21({
        x: lowerX + horizontalOffset,
        y: y5 + height - verticalOffset,
        horizontalAnchor: horizontalStart,
        verticalAnchor: verticalEnd
      }, sizeAttrs);
    }
    if (position === "insideBottomRight") {
      return _objectSpread21({
        x: lowerX + lowerWidth - horizontalOffset,
        y: y5 + height - verticalOffset,
        horizontalAnchor: horizontalEnd,
        verticalAnchor: verticalEnd
      }, sizeAttrs);
    }
    if (!!position && typeof position === "object" && (isNumber(position.x) || isPercent(position.x)) && (isNumber(position.y) || isPercent(position.y))) {
      return _objectSpread21({
        x: x5 + getPercentValue(position.x, midHeightWidth),
        y: y5 + getPercentValue(position.y, height),
        horizontalAnchor: "end",
        verticalAnchor: "end"
      }, sizeAttrs);
    }
    return _objectSpread21({
      x: centerX,
      y: y5 + height / 2,
      horizontalAnchor: "middle",
      verticalAnchor: "middle"
    }, sizeAttrs);
  };

  // node_modules/recharts/es6/component/Label.js
  var _excluded7 = ["labelRef"];
  var _excluded24 = ["content"];
  function _objectWithoutProperties7(e, t) {
    if (null == e) return {};
    var o, r2, i = _objectWithoutPropertiesLoose7(e, t);
    if (Object.getOwnPropertySymbols) {
      var n = Object.getOwnPropertySymbols(e);
      for (r2 = 0; r2 < n.length; r2++) o = n[r2], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
    }
    return i;
  }
  function _objectWithoutPropertiesLoose7(r2, e) {
    if (null == r2) return {};
    var t = {};
    for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
      if (-1 !== e.indexOf(n)) continue;
      t[n] = r2[n];
    }
    return t;
  }
  function ownKeys22(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread22(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys22(Object(t), true).forEach(function(r3) {
        _defineProperty25(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys22(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty25(e, r2, t) {
    return (r2 = _toPropertyKey25(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey25(t) {
    var i = _toPrimitive25(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive25(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  function _extends10() {
    return _extends10 = Object.assign ? Object.assign.bind() : function(n) {
      for (var e = 1; e < arguments.length; e++) {
        var t = arguments[e];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends10.apply(null, arguments);
  }
  var CartesianLabelContext = /* @__PURE__ */ (0, import_react26.createContext)(null);
  var CartesianLabelContextProvider = (_ref2) => {
    var x5 = _ref2.x, y5 = _ref2.y, upperWidth = _ref2.upperWidth, lowerWidth = _ref2.lowerWidth, width = _ref2.width, height = _ref2.height, children = _ref2.children;
    var viewBox = (0, import_react26.useMemo)(() => ({
      x: x5,
      y: y5,
      upperWidth,
      lowerWidth,
      width,
      height
    }), [x5, y5, upperWidth, lowerWidth, width, height]);
    return /* @__PURE__ */ React15.createElement(CartesianLabelContext.Provider, {
      value: viewBox
    }, children);
  };
  var useCartesianLabelContext = () => {
    var labelChildContext = (0, import_react26.useContext)(CartesianLabelContext);
    var chartContext = useViewBox();
    return labelChildContext || (chartContext ? cartesianViewBoxToTrapezoid(chartContext) : void 0);
  };
  var PolarLabelContext = /* @__PURE__ */ (0, import_react26.createContext)(null);
  var usePolarLabelContext = () => {
    var labelChildContext = (0, import_react26.useContext)(PolarLabelContext);
    var chartContext = useAppSelector(selectPolarViewBox);
    return labelChildContext || chartContext;
  };
  var getLabel = (props) => {
    var value2 = props.value, formatter = props.formatter;
    var label = isNullish(props.children) ? value2 : props.children;
    if (typeof formatter === "function") {
      return formatter(label);
    }
    return label;
  };
  var isLabelContentAFunction = (content) => {
    return content != null && typeof content === "function";
  };
  var getDeltaAngle2 = (startAngle, endAngle) => {
    var sign2 = mathSign(endAngle - startAngle);
    var deltaAngle = Math.min(Math.abs(endAngle - startAngle), 360);
    return sign2 * deltaAngle;
  };
  var renderRadialLabel = (labelProps, position, label, attrs, viewBox) => {
    var offset = labelProps.offset, className = labelProps.className;
    var cx = viewBox.cx, cy = viewBox.cy, innerRadius = viewBox.innerRadius, outerRadius = viewBox.outerRadius, startAngle = viewBox.startAngle, endAngle = viewBox.endAngle, clockWise = viewBox.clockWise;
    var radius = (innerRadius + outerRadius) / 2;
    var deltaAngle = getDeltaAngle2(startAngle, endAngle);
    var sign2 = deltaAngle >= 0 ? 1 : -1;
    var labelAngle, direction;
    switch (position) {
      case "insideStart":
        labelAngle = startAngle + sign2 * offset;
        direction = clockWise;
        break;
      case "insideEnd":
        labelAngle = endAngle - sign2 * offset;
        direction = !clockWise;
        break;
      case "end":
        labelAngle = endAngle + sign2 * offset;
        direction = clockWise;
        break;
      default:
        throw new Error("Unsupported position ".concat(position));
    }
    direction = deltaAngle <= 0 ? direction : !direction;
    var startPoint = polarToCartesian(cx, cy, radius, labelAngle);
    var endPoint = polarToCartesian(cx, cy, radius, labelAngle + (direction ? 1 : -1) * 359);
    var path3 = "M".concat(startPoint.x, ",").concat(startPoint.y, "\n    A").concat(radius, ",").concat(radius, ",0,1,").concat(direction ? 0 : 1, ",\n    ").concat(endPoint.x, ",").concat(endPoint.y);
    var id = isNullish(labelProps.id) ? uniqueId("recharts-radial-line-") : labelProps.id;
    return /* @__PURE__ */ React15.createElement("text", _extends10({}, attrs, {
      dominantBaseline: "central",
      className: clsx("recharts-radial-bar-label", className)
    }), /* @__PURE__ */ React15.createElement("defs", null, /* @__PURE__ */ React15.createElement("path", {
      id,
      d: path3
    })), /* @__PURE__ */ React15.createElement("textPath", {
      xlinkHref: "#".concat(id)
    }, label));
  };
  var getAttrsOfPolarLabel = (viewBox, offset, position) => {
    var cx = viewBox.cx, cy = viewBox.cy, innerRadius = viewBox.innerRadius, outerRadius = viewBox.outerRadius, startAngle = viewBox.startAngle, endAngle = viewBox.endAngle;
    var midAngle = (startAngle + endAngle) / 2;
    if (position === "outside") {
      var _polarToCartesian = polarToCartesian(cx, cy, outerRadius + offset, midAngle), _x = _polarToCartesian.x, _y = _polarToCartesian.y;
      return {
        x: _x,
        y: _y,
        textAnchor: _x >= cx ? "start" : "end",
        verticalAnchor: "middle"
      };
    }
    if (position === "center") {
      return {
        x: cx,
        y: cy,
        textAnchor: "middle",
        verticalAnchor: "middle"
      };
    }
    if (position === "centerTop") {
      return {
        x: cx,
        y: cy,
        textAnchor: "middle",
        verticalAnchor: "start"
      };
    }
    if (position === "centerBottom") {
      return {
        x: cx,
        y: cy,
        textAnchor: "middle",
        verticalAnchor: "end"
      };
    }
    var r2 = (innerRadius + outerRadius) / 2;
    var _polarToCartesian2 = polarToCartesian(cx, cy, r2, midAngle), x5 = _polarToCartesian2.x, y5 = _polarToCartesian2.y;
    return {
      x: x5,
      y: y5,
      textAnchor: "middle",
      verticalAnchor: "middle"
    };
  };
  var isPolar = (viewBox) => viewBox != null && "cx" in viewBox && isNumber(viewBox.cx);
  var defaultLabelProps = {
    angle: 0,
    offset: 5,
    zIndex: DefaultZIndexes.label,
    position: "middle",
    textBreakAll: false
  };
  function polarViewBoxToTrapezoid(viewBox) {
    if (!isPolar(viewBox)) {
      return viewBox;
    }
    var cx = viewBox.cx, cy = viewBox.cy, outerRadius = viewBox.outerRadius;
    var diameter = outerRadius * 2;
    return {
      x: cx - outerRadius,
      y: cy - outerRadius,
      width: diameter,
      upperWidth: diameter,
      lowerWidth: diameter,
      height: diameter
    };
  }
  function Label(outerProps) {
    var props = resolveDefaultProps(outerProps, defaultLabelProps);
    var viewBoxFromProps = props.viewBox, parentViewBox = props.parentViewBox, position = props.position, value2 = props.value, children = props.children, content = props.content, _props$className = props.className, className = _props$className === void 0 ? "" : _props$className, textBreakAll = props.textBreakAll, labelRef = props.labelRef;
    var polarViewBox = usePolarLabelContext();
    var cartesianViewBox = useCartesianLabelContext();
    var resolvedViewBox = position === "center" ? cartesianViewBox : polarViewBox !== null && polarViewBox !== void 0 ? polarViewBox : cartesianViewBox;
    var viewBox, label, positionAttrs;
    if (viewBoxFromProps == null) {
      viewBox = resolvedViewBox;
    } else if (isPolar(viewBoxFromProps)) {
      viewBox = viewBoxFromProps;
    } else {
      viewBox = cartesianViewBoxToTrapezoid(viewBoxFromProps);
    }
    var cartesianBox = polarViewBoxToTrapezoid(viewBox);
    if (!viewBox || isNullish(value2) && isNullish(children) && !/* @__PURE__ */ (0, import_react26.isValidElement)(content) && typeof content !== "function") {
      return null;
    }
    var propsWithViewBox = _objectSpread22(_objectSpread22({}, props), {}, {
      viewBox
    });
    if (/* @__PURE__ */ (0, import_react26.isValidElement)(content)) {
      var _ = propsWithViewBox.labelRef, propsWithoutLabelRef = _objectWithoutProperties7(propsWithViewBox, _excluded7);
      return /* @__PURE__ */ (0, import_react26.cloneElement)(content, propsWithoutLabelRef);
    }
    if (typeof content === "function") {
      var _2 = propsWithViewBox.content, propsForContent = _objectWithoutProperties7(propsWithViewBox, _excluded24);
      label = /* @__PURE__ */ (0, import_react26.createElement)(content, propsForContent);
      if (/* @__PURE__ */ (0, import_react26.isValidElement)(label)) {
        return label;
      }
    } else {
      label = getLabel(props);
    }
    var attrs = svgPropertiesAndEvents(props);
    if (isPolar(viewBox)) {
      if (position === "insideStart" || position === "insideEnd" || position === "end") {
        return renderRadialLabel(props, position, label, attrs, viewBox);
      }
      positionAttrs = getAttrsOfPolarLabel(viewBox, props.offset, props.position);
    } else {
      if (!cartesianBox) {
        return null;
      }
      var cartesianResult = getCartesianPosition({
        viewBox: cartesianBox,
        position,
        offset: props.offset,
        parentViewBox: isPolar(parentViewBox) ? void 0 : parentViewBox,
        clamp: true
      });
      positionAttrs = _objectSpread22(_objectSpread22({
        x: cartesianResult.x,
        y: cartesianResult.y,
        textAnchor: cartesianResult.horizontalAnchor,
        verticalAnchor: cartesianResult.verticalAnchor
      }, cartesianResult.width !== void 0 ? {
        width: cartesianResult.width
      } : {}), cartesianResult.height !== void 0 ? {
        height: cartesianResult.height
      } : {});
    }
    return /* @__PURE__ */ React15.createElement(ZIndexLayer, {
      zIndex: props.zIndex
    }, /* @__PURE__ */ React15.createElement(Text, _extends10({
      ref: labelRef,
      className: clsx("recharts-label", className)
    }, attrs, positionAttrs, {
      /*
       * textAnchor is decided by default based on the `position`
       * but we allow overriding via props for precise control.
       */
      textAnchor: isValidTextAnchor(attrs.textAnchor) ? attrs.textAnchor : positionAttrs.textAnchor,
      breakAll: textBreakAll
    }), label));
  }
  Label.displayName = "Label";
  var parseLabel = (label, viewBox, labelRef) => {
    if (!label) {
      return null;
    }
    var commonProps = {
      viewBox,
      labelRef
    };
    if (label === true) {
      return /* @__PURE__ */ React15.createElement(Label, _extends10({
        key: "label-implicit"
      }, commonProps));
    }
    if (isNumOrStr(label)) {
      return /* @__PURE__ */ React15.createElement(Label, _extends10({
        key: "label-implicit",
        value: label
      }, commonProps));
    }
    if (/* @__PURE__ */ (0, import_react26.isValidElement)(label)) {
      if (label.type === Label) {
        return /* @__PURE__ */ (0, import_react26.cloneElement)(label, _objectSpread22({
          key: "label-implicit"
        }, commonProps));
      }
      return /* @__PURE__ */ React15.createElement(Label, _extends10({
        key: "label-implicit",
        content: label
      }, commonProps));
    }
    if (isLabelContentAFunction(label)) {
      return /* @__PURE__ */ React15.createElement(Label, _extends10({
        key: "label-implicit",
        content: label
      }, commonProps));
    }
    if (label && typeof label === "object") {
      return /* @__PURE__ */ React15.createElement(Label, _extends10({}, label, {
        key: "label-implicit"
      }, commonProps));
    }
    return null;
  };
  function CartesianLabelFromLabelProp(_ref3) {
    var label = _ref3.label, labelRef = _ref3.labelRef;
    var viewBox = useCartesianLabelContext();
    return parseLabel(label, viewBox, labelRef) || null;
  }

  // node_modules/recharts/es6/component/LabelList.js
  var React16 = __toESM(require_react());
  var import_react27 = __toESM(require_react());
  var _excluded8 = ["valueAccessor"];
  var _excluded25 = ["dataKey", "clockWise", "id", "textBreakAll", "zIndex"];
  function _extends11() {
    return _extends11 = Object.assign ? Object.assign.bind() : function(n) {
      for (var e = 1; e < arguments.length; e++) {
        var t = arguments[e];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends11.apply(null, arguments);
  }
  function _objectWithoutProperties8(e, t) {
    if (null == e) return {};
    var o, r2, i = _objectWithoutPropertiesLoose8(e, t);
    if (Object.getOwnPropertySymbols) {
      var n = Object.getOwnPropertySymbols(e);
      for (r2 = 0; r2 < n.length; r2++) o = n[r2], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
    }
    return i;
  }
  function _objectWithoutPropertiesLoose8(r2, e) {
    if (null == r2) return {};
    var t = {};
    for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
      if (-1 !== e.indexOf(n)) continue;
      t[n] = r2[n];
    }
    return t;
  }
  var defaultAccessor = (entry) => {
    var val = Array.isArray(entry.value) ? entry.value[entry.value.length - 1] : entry.value;
    if (isRenderableText(val)) {
      return val;
    }
    return void 0;
  };
  var CartesianLabelListContext = /* @__PURE__ */ (0, import_react27.createContext)(void 0);
  var CartesianLabelListContextProvider = CartesianLabelListContext.Provider;
  var PolarLabelListContext = /* @__PURE__ */ (0, import_react27.createContext)(void 0);
  var PolarLabelListContextProvider = PolarLabelListContext.Provider;
  function useCartesianLabelListContext() {
    return (0, import_react27.useContext)(CartesianLabelListContext);
  }
  function usePolarLabelListContext() {
    return (0, import_react27.useContext)(PolarLabelListContext);
  }
  function LabelList(_ref2) {
    var _ref$valueAccessor = _ref2.valueAccessor, valueAccessor = _ref$valueAccessor === void 0 ? defaultAccessor : _ref$valueAccessor, restProps = _objectWithoutProperties8(_ref2, _excluded8);
    var dataKey = restProps.dataKey, clockWise = restProps.clockWise, id = restProps.id, textBreakAll = restProps.textBreakAll, zIndex = restProps.zIndex, others = _objectWithoutProperties8(restProps, _excluded25);
    var cartesianData = useCartesianLabelListContext();
    var polarData = usePolarLabelListContext();
    var data = cartesianData || polarData;
    if (!data || !data.length) {
      return null;
    }
    return /* @__PURE__ */ React16.createElement(ZIndexLayer, {
      zIndex: zIndex !== null && zIndex !== void 0 ? zIndex : DefaultZIndexes.label
    }, /* @__PURE__ */ React16.createElement(Layer, {
      className: "recharts-label-list"
    }, data.map((entry, index2) => {
      var _restProps$fill;
      var value2 = isNullish(dataKey) ? valueAccessor(entry, index2) : getValueByDataKey(entry.payload, dataKey);
      var idProps = isNullish(id) ? {} : {
        id: "".concat(id, "-").concat(index2)
      };
      return /* @__PURE__ */ React16.createElement(Label, _extends11({
        key: "label-".concat(index2)
      }, svgPropertiesAndEvents(entry), others, idProps, {
        /*
         * Prefer to use the explicit fill from LabelList props.
         * Only in an absence of that, fall back to the fill of the entry.
         * The entry fill can be quite difficult to see especially in Bar, Pie, RadialBar in inside positions.
         * On the other hand it's quite convenient in Scatter, Line, or when the position is outside the Bar, Pie filled shapes.
         */
        fill: (_restProps$fill = restProps.fill) !== null && _restProps$fill !== void 0 ? _restProps$fill : entry.fill,
        parentViewBox: entry.parentViewBox,
        value: value2,
        textBreakAll,
        viewBox: entry.viewBox,
        index: index2,
        zIndex: 0
      }));
    })));
  }
  LabelList.displayName = "LabelList";
  function LabelListFromLabelProp(_ref2) {
    var label = _ref2.label;
    if (!label) {
      return null;
    }
    if (label === true) {
      return /* @__PURE__ */ React16.createElement(LabelList, {
        key: "labelList-implicit"
      });
    }
    if (/* @__PURE__ */ React16.isValidElement(label) || isLabelContentAFunction(label)) {
      return /* @__PURE__ */ React16.createElement(LabelList, {
        key: "labelList-implicit",
        content: label
      });
    }
    if (typeof label === "object") {
      return /* @__PURE__ */ React16.createElement(LabelList, _extends11({
        key: "labelList-implicit"
      }, label, {
        type: String(label.type)
      }));
    }
    return null;
  }

  // node_modules/recharts/es6/shape/Dot.js
  var React17 = __toESM(require_react());
  function _extends12() {
    return _extends12 = Object.assign ? Object.assign.bind() : function(n) {
      for (var e = 1; e < arguments.length; e++) {
        var t = arguments[e];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends12.apply(null, arguments);
  }
  var Dot = (props) => {
    var cx = props.cx, cy = props.cy, r2 = props.r, className = props.className;
    var layerClass = clsx("recharts-dot", className);
    if (isNumber(cx) && isNumber(cy) && isNumber(r2)) {
      return /* @__PURE__ */ React17.createElement("circle", _extends12({}, svgPropertiesNoEvents(props), adaptEventHandlers(props), {
        className: layerClass,
        cx,
        cy,
        r: r2
      }));
    }
    return null;
  };

  // node_modules/recharts/es6/state/selectors/polarSelectors.js
  var selectUnfilteredPolarItems = (state) => state.graphicalItems.polarItems;
  var selectAxisPredicate2 = createSelector([pickAxisType, pickAxisId], itemAxisPredicate);
  var selectPolarItemsSettings = createSelector([selectUnfilteredPolarItems, selectBaseAxis, selectAxisPredicate2], combineGraphicalItemsSettings);
  var selectPolarGraphicalItemsData = createSelector([selectPolarItemsSettings], combineGraphicalItemsData);
  var selectPolarDisplayedData = createSelector([selectPolarGraphicalItemsData, selectChartDataAndAlwaysIgnoreIndexes], combineDisplayedData);
  var selectPolarAppliedValues = createSelector([selectPolarDisplayedData, selectBaseAxis, selectPolarItemsSettings], combineAppliedValues);
  var selectAllPolarAppliedNumericalValues = createSelector([selectPolarDisplayedData, selectBaseAxis, selectPolarItemsSettings], (data, axisSettings, items) => {
    if (items.length > 0) {
      return data.flatMap((entry) => {
        return items.flatMap((item) => {
          var _axisSettings$dataKey;
          var valueByDataKey = getValueByDataKey(entry, (_axisSettings$dataKey = axisSettings.dataKey) !== null && _axisSettings$dataKey !== void 0 ? _axisSettings$dataKey : item.dataKey);
          return {
            value: valueByDataKey,
            errorDomain: []
            // polar charts do not have error bars
          };
        });
      }).filter(Boolean);
    }
    if ((axisSettings === null || axisSettings === void 0 ? void 0 : axisSettings.dataKey) != null) {
      return data.map((item) => ({
        value: getValueByDataKey(item, axisSettings.dataKey),
        errorDomain: []
      }));
    }
    return data.map((entry) => ({
      value: entry,
      errorDomain: []
    }));
  });
  var unsupportedInPolarChart = () => void 0;
  var selectDomainOfAllPolarAppliedNumericalValues = createSelector([selectPolarDisplayedData, selectBaseAxis, selectPolarItemsSettings, selectAllErrorBarSettings, pickAxisType, selectChartDataSliceIgnoringIndexes], combineDomainOfAllAppliedNumericalValuesIncludingErrorValues);
  var selectPolarNumericalDomain = createSelector([selectBaseAxis, selectDomainDefinition, selectDomainFromUserPreference, unsupportedInPolarChart, selectDomainOfAllPolarAppliedNumericalValues, unsupportedInPolarChart, selectChartLayout, pickAxisType], combineNumericalDomain);
  var selectPolarAxisDomain = createSelector([selectBaseAxis, selectChartLayout, selectPolarDisplayedData, selectPolarAppliedValues, selectStackOffsetType, pickAxisType, selectPolarNumericalDomain], combineAxisDomain);
  var selectPolarNiceTicks = createSelector([selectPolarAxisDomain, selectRenderableAxisSettings, selectRealScaleType], combineNiceTicks);
  var selectPolarAxisDomainIncludingNiceTicks = createSelector([selectBaseAxis, selectPolarAxisDomain, selectPolarNiceTicks, pickAxisType], combineAxisDomainWithNiceTicks);
  var selectPolarAxisCheckedDomain = createSelector([selectRealScaleType, selectPolarAxisDomainIncludingNiceTicks], combineCheckedDomain);

  // node_modules/recharts/es6/state/polarAxisSlice.js
  var initialState6 = {
    radiusAxis: {},
    angleAxis: {}
  };
  var polarAxisSlice = createSlice({
    name: "polarAxis",
    initialState: initialState6,
    reducers: {
      addRadiusAxis(state, action) {
        state.radiusAxis[action.payload.id] = castDraft(action.payload);
      },
      removeRadiusAxis(state, action) {
        delete state.radiusAxis[action.payload.id];
      },
      addAngleAxis(state, action) {
        state.angleAxis[action.payload.id] = castDraft(action.payload);
      },
      removeAngleAxis(state, action) {
        delete state.angleAxis[action.payload.id];
      }
    }
  });
  var _polarAxisSlice$actio = polarAxisSlice.actions;
  var addRadiusAxis = _polarAxisSlice$actio.addRadiusAxis;
  var removeRadiusAxis = _polarAxisSlice$actio.removeRadiusAxis;
  var addAngleAxis = _polarAxisSlice$actio.addAngleAxis;
  var removeAngleAxis = _polarAxisSlice$actio.removeAngleAxis;
  var polarAxisReducer = polarAxisSlice.reducer;

  // node_modules/recharts/es6/util/getClassNameFromUnknown.js
  function getClassNameFromUnknown(u) {
    if (u && typeof u === "object" && "className" in u && typeof u.className === "string") {
      return u.className;
    }
    return "";
  }

  // node_modules/recharts/es6/polar/Pie.js
  var React22 = __toESM(require_react());
  var import_react36 = __toESM(require_react());

  // node_modules/recharts/es6/state/selectors/pieSelectors.js
  function ownKeys23(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread23(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys23(Object(t), true).forEach(function(r3) {
        _defineProperty26(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys23(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty26(e, r2, t) {
    return (r2 = _toPropertyKey26(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey26(t) {
    var i = _toPrimitive26(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive26(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  var pickId = (_state, id) => id;
  var selectSynchronisedPieSettings = createSelector([selectUnfilteredPolarItems, pickId], (graphicalItems, id) => graphicalItems.filter((item) => item.type === "pie").find((item) => item.id === id));
  var emptyArray = [];
  var pickCells = (_state, _id, cells) => {
    if ((cells === null || cells === void 0 ? void 0 : cells.length) === 0) {
      return emptyArray;
    }
    return cells;
  };
  var selectDisplayedData2 = createSelector([selectChartDataAndAlwaysIgnoreIndexes, selectSynchronisedPieSettings, pickCells], (_ref2, pieSettings, cells) => {
    var chartData = _ref2.chartData;
    if (pieSettings == null) {
      return void 0;
    }
    var displayedData;
    if ((pieSettings === null || pieSettings === void 0 ? void 0 : pieSettings.data) != null && pieSettings.data.length > 0) {
      displayedData = pieSettings.data;
    } else {
      displayedData = chartData;
    }
    if ((!displayedData || !displayedData.length) && cells != null) {
      displayedData = cells.map((cell) => _objectSpread23(_objectSpread23({}, pieSettings.presentationProps), cell.props));
    }
    if (displayedData == null) {
      return void 0;
    }
    return displayedData;
  });
  var selectPieLegend = createSelector([selectDisplayedData2, selectSynchronisedPieSettings, pickCells], (displayedData, pieSettings, cells) => {
    if (displayedData == null || pieSettings == null) {
      return void 0;
    }
    return displayedData.map((entry, i) => {
      var _cells$i;
      var name = getValueByDataKey(entry, pieSettings.nameKey, pieSettings.name);
      var color2;
      if (cells !== null && cells !== void 0 && (_cells$i = cells[i]) !== null && _cells$i !== void 0 && (_cells$i = _cells$i.props) !== null && _cells$i !== void 0 && _cells$i.fill) {
        color2 = cells[i].props.fill;
      } else if (typeof entry === "object" && entry != null && "fill" in entry) {
        color2 = entry.fill;
      } else {
        color2 = pieSettings.fill;
      }
      return {
        value: getTooltipNameProp(name, pieSettings.dataKey),
        dataKey: pieSettings.dataKey,
        color: color2,
        // @ts-expect-error Legend payload.payload says it wants objects but our data can be unknown
        payload: entry,
        type: pieSettings.legendType
      };
    });
  });
  var selectPieSectors = createSelector([selectDisplayedData2, selectSynchronisedPieSettings, pickCells, selectChartOffsetInternal], (displayedData, pieSettings, cells, offset) => {
    if (pieSettings == null || displayedData == null) {
      return void 0;
    }
    return computePieSectors({
      offset,
      pieSettings,
      displayedData,
      cells
    });
  });

  // node_modules/recharts/es6/util/ReactUtils.js
  var import_react28 = __toESM(require_react());
  var import_react_is = __toESM(require_react_is());
  var getDisplayName = (Comp) => {
    if (typeof Comp === "string") {
      return Comp;
    }
    if (!Comp) {
      return "";
    }
    return Comp.displayName || Comp.name || "Component";
  };
  var lastChildren = null;
  var lastResult = null;
  var toArray = (children) => {
    if (children === lastChildren && Array.isArray(lastResult)) {
      return lastResult;
    }
    var result = [];
    import_react28.Children.forEach(children, (child) => {
      if (isNullish(child)) return;
      if ((0, import_react_is.isFragment)(child)) {
        result = result.concat(toArray(child.props.children));
      } else {
        result.push(child);
      }
    });
    lastResult = result;
    lastChildren = children;
    return result;
  };
  function findAllByType(children, type) {
    var result = [];
    var types = [];
    if (Array.isArray(type)) {
      types = type.map((t) => getDisplayName(t));
    } else {
      types = [getDisplayName(type)];
    }
    toArray(children).forEach((child) => {
      var childType = get(child, "type.displayName") || get(child, "type.name");
      if (childType && types.indexOf(childType) !== -1) {
        result.push(child);
      }
    });
    return result;
  }
  var isClipDot = (dot) => {
    if (dot && typeof dot === "object" && "clipDot" in dot) {
      return Boolean(dot.clipDot);
    }
    return true;
  };

  // node_modules/recharts/es6/util/ActiveShapeUtils.js
  var React18 = __toESM(require_react());
  var import_react29 = __toESM(require_react());
  function ownKeys24(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread24(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys24(Object(t), true).forEach(function(r3) {
        _defineProperty27(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys24(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty27(e, r2, t) {
    return (r2 = _toPropertyKey27(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey27(t) {
    var i = _toPrimitive27(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive27(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  function mergeShapeProps(option, props) {
    return _objectSpread24(_objectSpread24({}, props), option);
  }
  function getPropsFromShapeOption(option) {
    if (/* @__PURE__ */ (0, import_react29.isValidElement)(option)) {
      return option.props;
    }
    return option;
  }
  function renderWithShapeElement(option, props) {
    return /* @__PURE__ */ (0, import_react29.cloneElement)(option, mergeShapeProps(getPropsFromShapeOption(option), props));
  }
  function getShapeIndex(shapeProps) {
    if (!("index" in shapeProps)) {
      return void 0;
    }
    var index2 = shapeProps.index;
    return typeof index2 === "number" || typeof index2 === "string" ? index2 : void 0;
  }
  function isActiveShape(shapeProps) {
    return "isActive" in shapeProps && shapeProps.isActive === true;
  }
  function Shape(_ref2) {
    var option = _ref2.option, DefaultShape = _ref2.DefaultShape, shapeProps = _ref2.shapeProps, _ref$activeClassName = _ref2.activeClassName, activeClassName = _ref$activeClassName === void 0 ? "recharts-active-shape" : _ref$activeClassName, _ref$inActiveClassNam = _ref2.inActiveClassName, inActiveClassName = _ref$inActiveClassNam === void 0 ? "recharts-shape" : _ref$inActiveClassNam;
    var index2 = getShapeIndex(shapeProps);
    var shape;
    if (/* @__PURE__ */ (0, import_react29.isValidElement)(option)) {
      shape = renderWithShapeElement(option, shapeProps);
    } else if (option === DefaultShape) {
      shape = /* @__PURE__ */ React18.createElement(DefaultShape, shapeProps);
    } else if (typeof option === "function") {
      shape = option(shapeProps, index2);
    } else if (typeof option === "object") {
      shape = /* @__PURE__ */ React18.createElement(DefaultShape, mergeShapeProps(option, shapeProps));
    } else {
      shape = /* @__PURE__ */ React18.createElement(DefaultShape, shapeProps);
    }
    if (isActiveShape(shapeProps)) {
      return /* @__PURE__ */ React18.createElement(Layer, {
        className: activeClassName
      }, shape);
    }
    return /* @__PURE__ */ React18.createElement(Layer, {
      className: inActiveClassName
    }, shape);
  }

  // node_modules/recharts/es6/context/tooltipContext.js
  var useMouseEnterItemDispatch = (onMouseEnterFromProps, dataKey, graphicalItemId) => {
    var dispatch2 = useAppDispatch();
    return (data, index2) => (event) => {
      onMouseEnterFromProps === null || onMouseEnterFromProps === void 0 || onMouseEnterFromProps(data, index2, event);
      dispatch2(setActiveMouseOverItemIndex({
        activeIndex: String(index2),
        activeDataKey: dataKey,
        activeCoordinate: data.tooltipPosition,
        activeGraphicalItemId: graphicalItemId
      }));
    };
  };
  var useMouseLeaveItemDispatch = (onMouseLeaveFromProps) => {
    var dispatch2 = useAppDispatch();
    return (data, index2) => (event) => {
      onMouseLeaveFromProps === null || onMouseLeaveFromProps === void 0 || onMouseLeaveFromProps(data, index2, event);
      dispatch2(mouseLeaveItem());
    };
  };
  var useMouseClickItemDispatch = (onMouseClickFromProps, dataKey, graphicalItemId) => {
    var dispatch2 = useAppDispatch();
    return (data, index2) => (event) => {
      onMouseClickFromProps === null || onMouseClickFromProps === void 0 || onMouseClickFromProps(data, index2, event);
      dispatch2(setActiveClickItemIndex({
        activeIndex: String(index2),
        activeDataKey: dataKey,
        activeCoordinate: data.tooltipPosition,
        activeGraphicalItemId: graphicalItemId
      }));
    };
  };

  // node_modules/recharts/es6/state/SetTooltipEntrySettings.js
  var import_react30 = __toESM(require_react());
  function SetTooltipEntrySettings(_ref2) {
    var tooltipEntrySettings = _ref2.tooltipEntrySettings;
    var dispatch2 = useAppDispatch();
    var isPanorama = useIsPanorama();
    var prevSettingsRef = (0, import_react30.useRef)(null);
    (0, import_react30.useLayoutEffect)(() => {
      if (isPanorama) {
        return;
      }
      if (prevSettingsRef.current === null) {
        dispatch2(addTooltipEntrySettings(tooltipEntrySettings));
      } else if (prevSettingsRef.current !== tooltipEntrySettings) {
        dispatch2(replaceTooltipEntrySettings({
          prev: prevSettingsRef.current,
          next: tooltipEntrySettings
        }));
      }
      prevSettingsRef.current = tooltipEntrySettings;
    }, [tooltipEntrySettings, dispatch2, isPanorama]);
    (0, import_react30.useLayoutEffect)(() => {
      return () => {
        if (prevSettingsRef.current) {
          dispatch2(removeTooltipEntrySettings(prevSettingsRef.current));
          prevSettingsRef.current = null;
        }
      };
    }, [dispatch2]);
    return null;
  }

  // node_modules/recharts/es6/state/SetLegendPayload.js
  var import_react31 = __toESM(require_react());
  function SetLegendPayload(_ref2) {
    var legendPayload = _ref2.legendPayload;
    var dispatch2 = useAppDispatch();
    var isPanorama = useIsPanorama();
    var prevPayloadRef = (0, import_react31.useRef)(null);
    (0, import_react31.useLayoutEffect)(() => {
      if (isPanorama) {
        return;
      }
      if (prevPayloadRef.current === null) {
        dispatch2(addLegendPayload(legendPayload));
      } else if (prevPayloadRef.current !== legendPayload) {
        dispatch2(replaceLegendPayload({
          prev: prevPayloadRef.current,
          next: legendPayload
        }));
      }
      prevPayloadRef.current = legendPayload;
    }, [dispatch2, isPanorama, legendPayload]);
    (0, import_react31.useLayoutEffect)(() => {
      return () => {
        if (prevPayloadRef.current) {
          dispatch2(removeLegendPayload(prevPayloadRef.current));
          prevPayloadRef.current = null;
        }
      };
    }, [dispatch2]);
    return null;
  }
  function SetPolarLegendPayload(_ref2) {
    var legendPayload = _ref2.legendPayload;
    var dispatch2 = useAppDispatch();
    var layout = useAppSelector(selectChartLayout);
    var prevPayloadRef = (0, import_react31.useRef)(null);
    (0, import_react31.useLayoutEffect)(() => {
      if (layout !== "centric" && layout !== "radial") {
        return;
      }
      if (prevPayloadRef.current === null) {
        dispatch2(addLegendPayload(legendPayload));
      } else if (prevPayloadRef.current !== legendPayload) {
        dispatch2(replaceLegendPayload({
          prev: prevPayloadRef.current,
          next: legendPayload
        }));
      }
      prevPayloadRef.current = legendPayload;
    }, [dispatch2, layout, legendPayload]);
    (0, import_react31.useLayoutEffect)(() => {
      return () => {
        if (prevPayloadRef.current) {
          dispatch2(removeLegendPayload(prevPayloadRef.current));
          prevPayloadRef.current = null;
        }
      };
    }, [dispatch2]);
    return null;
  }

  // node_modules/recharts/es6/animation/AnimatedItems.js
  var React19 = __toESM(require_react());
  var import_react33 = __toESM(require_react());

  // node_modules/recharts/es6/animation/matchBy.js
  function _slicedToArray13(r2, e) {
    return _arrayWithHoles13(r2) || _iterableToArrayLimit13(r2, e) || _unsupportedIterableToArray13(r2, e) || _nonIterableRest13();
  }
  function _nonIterableRest13() {
    throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  function _unsupportedIterableToArray13(r2, a2) {
    if (r2) {
      if ("string" == typeof r2) return _arrayLikeToArray13(r2, a2);
      var t = {}.toString.call(r2).slice(8, -1);
      return "Object" === t && r2.constructor && (t = r2.constructor.name), "Map" === t || "Set" === t ? Array.from(r2) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray13(r2, a2) : void 0;
    }
  }
  function _arrayLikeToArray13(r2, a2) {
    (null == a2 || a2 > r2.length) && (a2 = r2.length);
    for (var e = 0, n = Array(a2); e < a2; e++) n[e] = r2[e];
    return n;
  }
  function _iterableToArrayLimit13(r2, l) {
    var t = null == r2 ? null : "undefined" != typeof Symbol && r2[Symbol.iterator] || r2["@@iterator"];
    if (null != t) {
      var e, n, i, u, a2 = [], f = true, o = false;
      try {
        if (i = (t = t.call(r2)).next, 0 === l) {
          if (Object(t) !== t) return;
          f = false;
        } else for (; !(f = (e = i.call(t)).done) && (a2.push(e.value), a2.length !== l); f = true) ;
      } catch (r3) {
        o = true, n = r3;
      } finally {
        try {
          if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return;
        } finally {
          if (o) throw n;
        }
      }
      return a2;
    }
  }
  function _arrayWithHoles13(r2) {
    if (Array.isArray(r2)) return r2;
  }
  var matchByIndex = "index";
  var matchAppend = "append";
  function tagAlignedItems(alignedPrevItems, nextItems) {
    var removedPrevItems = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : [];
    var tagged = [];
    for (var prev of removedPrevItems) {
      tagged.push({
        status: "removed",
        prev
      });
    }
    for (var i = 0; i < nextItems.length; i++) {
      var _prev = alignedPrevItems[i];
      var next = nextItems[i];
      if (_prev != null) {
        tagged.push({
          status: "matched",
          prev: _prev,
          next
        });
      } else {
        tagged.push({
          status: "added",
          next
        });
      }
    }
    return tagged;
  }
  function matchByIndexImpl(prevItems, nextItems) {
    var factor = prevItems.length / nextItems.length;
    var alignedPrevItems = nextItems.map((_, i) => prevItems[Math.floor(i * factor)]);
    return tagAlignedItems(alignedPrevItems, nextItems);
  }
  function matchAppendImpl(prevItems, nextItems) {
    var alignedPrevItems = nextItems.map((_, i) => prevItems[i]);
    return tagAlignedItems(alignedPrevItems, nextItems);
  }
  function buildPrevKeyMap(prevItems, matchBy) {
    var prevMap = /* @__PURE__ */ new Map();
    for (var i = 0; i < prevItems.length; i++) {
      var _item = prevItems[i];
      if (_item == null) continue;
      var key = matchBy(_item, i);
      if (key != null && !prevMap.has(key)) {
        prevMap.set(key, _item);
      }
    }
    return prevMap;
  }
  function matchByKey(prevItems, nextItems, matchBy) {
    var prevMap = buildPrevKeyMap(prevItems, matchBy);
    var matchedKeys = /* @__PURE__ */ new Set();
    var alignedPrevItems = nextItems.map((next, i) => {
      var key2 = matchBy(next, i);
      if (key2 != null) {
        var prev = prevMap.get(key2);
        if (prev !== void 0) {
          matchedKeys.add(key2);
          return prev;
        }
      }
      return void 0;
    });
    var removedPrevItems = [];
    for (var _ref3 of prevMap) {
      var _ref2 = _slicedToArray13(_ref3, 2);
      var key = _ref2[0];
      var _item2 = _ref2[1];
      if (!matchedKeys.has(key)) {
        removedPrevItems.push(_item2);
      }
    }
    return tagAlignedItems(alignedPrevItems, nextItems, removedPrevItems);
  }
  function matchAnimationItems(prevItems, nextItems, matchBy) {
    if (nextItems == null) {
      return null;
    }
    if (prevItems == null) {
      return nextItems.map((next) => ({
        status: "added",
        next
      }));
    }
    if (matchBy === matchByIndex) {
      return matchByIndexImpl(prevItems, nextItems);
    }
    if (matchBy === matchAppend) {
      return matchAppendImpl(prevItems, nextItems);
    }
    return matchByKey(prevItems, nextItems, matchBy);
  }

  // node_modules/recharts/es6/animation/useAnimationStartSnapshot.js
  var import_react32 = __toESM(require_react());
  function useAnimationStartSnapshot(animationInput, previousValueRef) {
    var previousAnimationInputRef = (0, import_react32.useRef)(animationInput);
    var startValueRef = (0, import_react32.useRef)(previousValueRef.current);
    var isReadyToCommitRef = (0, import_react32.useRef)(true);
    if (previousAnimationInputRef.current !== animationInput) {
      previousAnimationInputRef.current = animationInput;
      startValueRef.current = previousValueRef.current;
      isReadyToCommitRef.current = false;
    }
    var syncStepValue = (0, import_react32.useCallback)(function(stepValue, animationElapsedTime) {
      var canCommit = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : true;
      if (animationElapsedTime === 0) {
        isReadyToCommitRef.current = true;
        return;
      }
      if (animationElapsedTime === 1) {
        startValueRef.current = stepValue;
      }
      if (animationElapsedTime > 0 && isReadyToCommitRef.current && canCommit) {
        previousValueRef.current = stepValue;
      }
    }, [previousValueRef]);
    return {
      startValue: startValueRef.current,
      syncStepValue
    };
  }

  // node_modules/recharts/es6/animation/AnimatedItems.js
  function _slicedToArray14(r2, e) {
    return _arrayWithHoles14(r2) || _iterableToArrayLimit14(r2, e) || _unsupportedIterableToArray14(r2, e) || _nonIterableRest14();
  }
  function _nonIterableRest14() {
    throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  function _unsupportedIterableToArray14(r2, a2) {
    if (r2) {
      if ("string" == typeof r2) return _arrayLikeToArray14(r2, a2);
      var t = {}.toString.call(r2).slice(8, -1);
      return "Object" === t && r2.constructor && (t = r2.constructor.name), "Map" === t || "Set" === t ? Array.from(r2) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray14(r2, a2) : void 0;
    }
  }
  function _arrayLikeToArray14(r2, a2) {
    (null == a2 || a2 > r2.length) && (a2 = r2.length);
    for (var e = 0, n = Array(a2); e < a2; e++) n[e] = r2[e];
    return n;
  }
  function _iterableToArrayLimit14(r2, l) {
    var t = null == r2 ? null : "undefined" != typeof Symbol && r2[Symbol.iterator] || r2["@@iterator"];
    if (null != t) {
      var e, n, i, u, a2 = [], f = true, o = false;
      try {
        if (i = (t = t.call(r2)).next, 0 === l) {
          if (Object(t) !== t) return;
          f = false;
        } else for (; !(f = (e = i.call(t)).done) && (a2.push(e.value), a2.length !== l); f = true) ;
      } catch (r3) {
        o = true, n = r3;
      } finally {
        try {
          if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return;
        } finally {
          if (o) throw n;
        }
      }
      return a2;
    }
  }
  function _arrayWithHoles14(r2) {
    if (Array.isArray(r2)) return r2;
  }
  function useAnimationCallbacks(onAnimationStart, onAnimationEnd) {
    var _useState = (0, import_react33.useState)(false), _useState2 = _slicedToArray14(_useState, 2), isAnimating = _useState2[0], setIsAnimating = _useState2[1];
    var handleAnimationStart = (0, import_react33.useCallback)(() => {
      if (typeof onAnimationStart === "function") {
        onAnimationStart();
      }
      setIsAnimating(true);
    }, [onAnimationStart]);
    var handleAnimationEnd = (0, import_react33.useCallback)(() => {
      if (typeof onAnimationEnd === "function") {
        onAnimationEnd();
      }
      setIsAnimating(false);
    }, [onAnimationEnd]);
    return {
      isAnimating,
      handleAnimationStart,
      handleAnimationEnd
    };
  }
  function AnimatedItems(props) {
    var _animationStartItems$;
    var animationInput = props.animationInput, animationIdPrefix = props.animationIdPrefix, items = props.items, previousItemsRef = props.previousItemsRef, isAnimationActive = props.isAnimationActive, animationBegin = props.animationBegin, animationDuration = props.animationDuration, animationEasing = props.animationEasing, onAnimationStart = props.onAnimationStart, onAnimationEnd = props.onAnimationEnd, animationInterpolateFn = props.animationInterpolateFn, animationMatchBy = props.animationMatchBy, shouldUpdatePreviousRef = props.shouldUpdatePreviousRef, children = props.children, layout = props.layout;
    var animationId = useAnimationId(animationInput, animationIdPrefix);
    var animationStartItems = useAnimationStartSnapshot(animationId, previousItemsRef);
    var rawPrevItems = (_animationStartItems$ = animationStartItems.startValue) !== null && _animationStartItems$ !== void 0 ? _animationStartItems$ : null;
    var animationItems = matchAnimationItems(rawPrevItems, items, animationMatchBy !== null && animationMatchBy !== void 0 ? animationMatchBy : matchByIndex);
    return /* @__PURE__ */ React19.createElement(JavascriptAnimate, {
      animationId,
      begin: animationBegin,
      duration: animationDuration,
      isActive: isAnimationActive,
      easing: animationEasing,
      onAnimationEnd,
      onAnimationStart,
      key: animationId
    }, (animationElapsedTime) => {
      var isEntrance = rawPrevItems == null;
      var stepData = items == null ? items : animationInterpolateFn(animationItems, animationElapsedTime, layout);
      var canUpdate = shouldUpdatePreviousRef ? shouldUpdatePreviousRef(animationElapsedTime) : animationElapsedTime > 0;
      animationStartItems.syncStepValue(stepData, animationElapsedTime, canUpdate);
      if (stepData == null) {
        return null;
      }
      return children(stepData, animationElapsedTime, isEntrance);
    });
  }

  // node_modules/recharts/es6/context/RegisterGraphicalItemId.js
  var React21 = __toESM(require_react());
  var import_react34 = __toESM(require_react());

  // node_modules/recharts/es6/util/useId.js
  var React20 = __toESM(require_react());
  var _ref;
  function _slicedToArray15(r2, e) {
    return _arrayWithHoles15(r2) || _iterableToArrayLimit15(r2, e) || _unsupportedIterableToArray15(r2, e) || _nonIterableRest15();
  }
  function _nonIterableRest15() {
    throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  function _unsupportedIterableToArray15(r2, a2) {
    if (r2) {
      if ("string" == typeof r2) return _arrayLikeToArray15(r2, a2);
      var t = {}.toString.call(r2).slice(8, -1);
      return "Object" === t && r2.constructor && (t = r2.constructor.name), "Map" === t || "Set" === t ? Array.from(r2) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray15(r2, a2) : void 0;
    }
  }
  function _arrayLikeToArray15(r2, a2) {
    (null == a2 || a2 > r2.length) && (a2 = r2.length);
    for (var e = 0, n = Array(a2); e < a2; e++) n[e] = r2[e];
    return n;
  }
  function _iterableToArrayLimit15(r2, l) {
    var t = null == r2 ? null : "undefined" != typeof Symbol && r2[Symbol.iterator] || r2["@@iterator"];
    if (null != t) {
      var e, n, i, u, a2 = [], f = true, o = false;
      try {
        if (i = (t = t.call(r2)).next, 0 === l) {
          if (Object(t) !== t) return;
          f = false;
        } else for (; !(f = (e = i.call(t)).done) && (a2.push(e.value), a2.length !== l); f = true) ;
      } catch (r3) {
        o = true, n = r3;
      } finally {
        try {
          if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return;
        } finally {
          if (o) throw n;
        }
      }
      return a2;
    }
  }
  function _arrayWithHoles15(r2) {
    if (Array.isArray(r2)) return r2;
  }
  var useIdFallback = () => {
    var _React$useState = React20.useState(() => uniqueId("uid-")), _React$useState2 = _slicedToArray15(_React$useState, 1), id = _React$useState2[0];
    return id;
  };
  var useId = (_ref = React20["useId".toString()]) !== null && _ref !== void 0 ? _ref : useIdFallback;

  // node_modules/recharts/es6/util/useUniqueId.js
  function useUniqueId(prefix2, customId) {
    var generatedId = useId();
    if (customId) {
      return customId;
    }
    return prefix2 ? "".concat(prefix2, "-").concat(generatedId) : generatedId;
  }

  // node_modules/recharts/es6/context/RegisterGraphicalItemId.js
  var GraphicalItemIdContext = /* @__PURE__ */ (0, import_react34.createContext)(void 0);
  var RegisterGraphicalItemId = (_ref2) => {
    var id = _ref2.id, type = _ref2.type, children = _ref2.children;
    var resolvedId = useUniqueId("recharts-".concat(type), id);
    return /* @__PURE__ */ React21.createElement(GraphicalItemIdContext.Provider, {
      value: resolvedId
    }, children(resolvedId));
  };

  // node_modules/recharts/es6/state/SetGraphicalItem.js
  var import_react35 = __toESM(require_react());

  // node_modules/recharts/es6/state/graphicalItemsSlice.js
  var initialState7 = {
    cartesianItems: [],
    polarItems: []
  };
  var graphicalItemsSlice = createSlice({
    name: "graphicalItems",
    initialState: initialState7,
    reducers: {
      addCartesianGraphicalItem: {
        reducer(state, action) {
          state.cartesianItems.push(castDraft(action.payload));
        },
        prepare: prepareAutoBatched()
      },
      replaceCartesianGraphicalItem: {
        reducer(state, action) {
          var _action$payload = action.payload, prev = _action$payload.prev, next = _action$payload.next;
          var index2 = current(state).cartesianItems.indexOf(castDraft(prev));
          if (index2 > -1) {
            state.cartesianItems[index2] = castDraft(next);
          }
        },
        prepare: prepareAutoBatched()
      },
      removeCartesianGraphicalItem: {
        reducer(state, action) {
          var index2 = current(state).cartesianItems.indexOf(castDraft(action.payload));
          if (index2 > -1) {
            state.cartesianItems.splice(index2, 1);
          }
        },
        prepare: prepareAutoBatched()
      },
      addPolarGraphicalItem: {
        reducer(state, action) {
          state.polarItems.push(castDraft(action.payload));
        },
        prepare: prepareAutoBatched()
      },
      removePolarGraphicalItem: {
        reducer(state, action) {
          var index2 = current(state).polarItems.indexOf(castDraft(action.payload));
          if (index2 > -1) {
            state.polarItems.splice(index2, 1);
          }
        },
        prepare: prepareAutoBatched()
      },
      replacePolarGraphicalItem: {
        reducer(state, action) {
          var _action$payload2 = action.payload, prev = _action$payload2.prev, next = _action$payload2.next;
          var index2 = current(state).polarItems.indexOf(castDraft(prev));
          if (index2 > -1) {
            state.polarItems[index2] = castDraft(next);
          }
        },
        prepare: prepareAutoBatched()
      }
    }
  });
  var _graphicalItemsSlice$ = graphicalItemsSlice.actions;
  var addCartesianGraphicalItem = _graphicalItemsSlice$.addCartesianGraphicalItem;
  var replaceCartesianGraphicalItem = _graphicalItemsSlice$.replaceCartesianGraphicalItem;
  var removeCartesianGraphicalItem = _graphicalItemsSlice$.removeCartesianGraphicalItem;
  var addPolarGraphicalItem = _graphicalItemsSlice$.addPolarGraphicalItem;
  var removePolarGraphicalItem = _graphicalItemsSlice$.removePolarGraphicalItem;
  var replacePolarGraphicalItem = _graphicalItemsSlice$.replacePolarGraphicalItem;
  var graphicalItemsReducer = graphicalItemsSlice.reducer;

  // node_modules/recharts/es6/state/SetGraphicalItem.js
  var SetCartesianGraphicalItemImpl = (props) => {
    var dispatch2 = useAppDispatch();
    var prevPropsRef = (0, import_react35.useRef)(null);
    (0, import_react35.useLayoutEffect)(() => {
      if (prevPropsRef.current === null) {
        dispatch2(addCartesianGraphicalItem(props));
      } else if (prevPropsRef.current !== props) {
        dispatch2(replaceCartesianGraphicalItem({
          prev: prevPropsRef.current,
          next: props
        }));
      }
      prevPropsRef.current = props;
    }, [dispatch2, props]);
    (0, import_react35.useLayoutEffect)(() => {
      return () => {
        if (prevPropsRef.current) {
          dispatch2(removeCartesianGraphicalItem(prevPropsRef.current));
          prevPropsRef.current = null;
        }
      };
    }, [dispatch2]);
    return null;
  };
  var SetCartesianGraphicalItem = /* @__PURE__ */ (0, import_react35.memo)(SetCartesianGraphicalItemImpl);
  var SetPolarGraphicalItemImpl = (props) => {
    var dispatch2 = useAppDispatch();
    var prevPropsRef = (0, import_react35.useRef)(null);
    (0, import_react35.useLayoutEffect)(() => {
      if (prevPropsRef.current === null) {
        dispatch2(addPolarGraphicalItem(props));
      } else if (prevPropsRef.current !== props) {
        dispatch2(replacePolarGraphicalItem({
          prev: prevPropsRef.current,
          next: props
        }));
      }
      prevPropsRef.current = props;
    }, [dispatch2, props]);
    (0, import_react35.useLayoutEffect)(() => {
      return () => {
        if (prevPropsRef.current) {
          dispatch2(removePolarGraphicalItem(prevPropsRef.current));
          prevPropsRef.current = null;
        }
      };
    }, [dispatch2]);
    return null;
  };
  var SetPolarGraphicalItem = /* @__PURE__ */ (0, import_react35.memo)(SetPolarGraphicalItemImpl);

  // node_modules/recharts/es6/polar/Pie.js
  var _excluded9 = ["key"];
  var _excluded26 = ["onMouseEnter", "onClick", "onMouseLeave"];
  var _excluded32 = ["id"];
  var _excluded42 = ["id"];
  function _extends13() {
    return _extends13 = Object.assign ? Object.assign.bind() : function(n) {
      for (var e = 1; e < arguments.length; e++) {
        var t = arguments[e];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends13.apply(null, arguments);
  }
  function _objectWithoutProperties9(e, t) {
    if (null == e) return {};
    var o, r2, i = _objectWithoutPropertiesLoose9(e, t);
    if (Object.getOwnPropertySymbols) {
      var n = Object.getOwnPropertySymbols(e);
      for (r2 = 0; r2 < n.length; r2++) o = n[r2], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
    }
    return i;
  }
  function _objectWithoutPropertiesLoose9(r2, e) {
    if (null == r2) return {};
    var t = {};
    for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
      if (-1 !== e.indexOf(n)) continue;
      t[n] = r2[n];
    }
    return t;
  }
  function ownKeys25(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread25(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys25(Object(t), true).forEach(function(r3) {
        _defineProperty28(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys25(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty28(e, r2, t) {
    return (r2 = _toPropertyKey28(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey28(t) {
    var i = _toPrimitive28(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive28(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  var defaultPieSectorShape = Sector;
  function SetPiePayloadLegend(props) {
    var cells = (0, import_react36.useMemo)(() => findAllByType(props.children, Cell), [props.children]);
    var legendPayload = useAppSelector((state) => selectPieLegend(state, props.id, cells));
    if (legendPayload == null) {
      return null;
    }
    return /* @__PURE__ */ React22.createElement(SetPolarLegendPayload, {
      legendPayload
    });
  }
  function getActiveShapeFill(activeShape) {
    if (activeShape == null || typeof activeShape === "boolean" || typeof activeShape === "function") {
      return void 0;
    }
    if (/* @__PURE__ */ React22.isValidElement(activeShape)) {
      var _activeShape$props;
      var _fill = (_activeShape$props = activeShape.props) === null || _activeShape$props === void 0 ? void 0 : _activeShape$props.fill;
      return typeof _fill === "string" ? _fill : void 0;
    }
    var fill = activeShape.fill;
    return typeof fill === "string" ? fill : void 0;
  }
  var SetPieTooltipEntrySettings = /* @__PURE__ */ React22.memo((_ref2) => {
    var dataKey = _ref2.dataKey, nameKey = _ref2.nameKey, sectors = _ref2.sectors, stroke = _ref2.stroke, strokeWidth = _ref2.strokeWidth, fill = _ref2.fill, name = _ref2.name, hide = _ref2.hide, tooltipType = _ref2.tooltipType, formatter = _ref2.formatter, id = _ref2.id, activeShape = _ref2.activeShape;
    var activeShapeFill = getActiveShapeFill(activeShape);
    var tooltipDataDefinedOnItem = sectors.map((sector) => {
      var sectorTooltipPayload = sector.tooltipPayload;
      if (activeShapeFill == null || sectorTooltipPayload == null) {
        return sectorTooltipPayload;
      }
      return sectorTooltipPayload.map((item) => _objectSpread25(_objectSpread25({}, item), {}, {
        color: activeShapeFill,
        fill: activeShapeFill
      }));
    });
    var tooltipEntrySettings = {
      dataDefinedOnItem: tooltipDataDefinedOnItem,
      getPosition: (index2) => {
        var _sectors$Number;
        return (_sectors$Number = sectors[Number(index2)]) === null || _sectors$Number === void 0 ? void 0 : _sectors$Number.tooltipPosition;
      },
      settings: {
        stroke,
        strokeWidth,
        fill,
        dataKey,
        nameKey,
        name: getTooltipNameProp(name, dataKey),
        hide,
        type: tooltipType,
        color: fill,
        unit: "",
        // why doesn't Pie support unit?
        formatter,
        graphicalItemId: id
      }
    };
    return /* @__PURE__ */ React22.createElement(SetTooltipEntrySettings, {
      tooltipEntrySettings
    });
  });
  var getTextAnchor = (x5, cx) => {
    if (x5 > cx) {
      return "start";
    }
    if (x5 < cx) {
      return "end";
    }
    return "middle";
  };
  var getOuterRadius = (dataPoint, outerRadius, maxPieRadius) => {
    if (typeof outerRadius === "function") {
      return getPercentValue(outerRadius(dataPoint), maxPieRadius, maxPieRadius * 0.8);
    }
    return getPercentValue(outerRadius, maxPieRadius, maxPieRadius * 0.8);
  };
  var parseCoordinateOfPie = (pieSettings, offset, dataPoint) => {
    var top = offset.top, left2 = offset.left, width = offset.width, height = offset.height;
    var maxPieRadius = getMaxRadius(width, height);
    var cx = left2 + getPercentValue(pieSettings.cx, width, width / 2);
    var cy = top + getPercentValue(pieSettings.cy, height, height / 2);
    var innerRadius = getPercentValue(pieSettings.innerRadius, maxPieRadius, 0);
    var outerRadius = getOuterRadius(dataPoint, pieSettings.outerRadius, maxPieRadius);
    var maxRadius = pieSettings.maxRadius || Math.sqrt(width * width + height * height) / 2;
    return {
      cx,
      cy,
      innerRadius,
      outerRadius,
      maxRadius
    };
  };
  var parseDeltaAngle = (startAngle, endAngle) => {
    var sign2 = mathSign(endAngle - startAngle);
    var deltaAngle = Math.min(Math.abs(endAngle - startAngle), 360);
    return sign2 * deltaAngle;
  };
  var renderLabelLineItem = (option, props) => {
    if (/* @__PURE__ */ React22.isValidElement(option)) {
      return /* @__PURE__ */ React22.cloneElement(option, props);
    }
    if (typeof option === "function") {
      return option(props);
    }
    var className = clsx("recharts-pie-label-line", typeof option !== "boolean" ? option.className : "");
    var key = props.key, otherProps = _objectWithoutProperties9(props, _excluded9);
    return /* @__PURE__ */ React22.createElement(Curve, _extends13({}, otherProps, {
      type: "linear",
      className
    }));
  };
  var renderLabelItem = (option, props, value2) => {
    if (/* @__PURE__ */ React22.isValidElement(option)) {
      return /* @__PURE__ */ React22.cloneElement(option, props);
    }
    var label = value2;
    if (typeof option === "function") {
      label = option(props);
      if (/* @__PURE__ */ React22.isValidElement(label)) {
        return label;
      }
    }
    var className = clsx("recharts-pie-label-text", getClassNameFromUnknown(option));
    return /* @__PURE__ */ React22.createElement(Text, _extends13({}, props, {
      alignmentBaseline: "middle",
      className
    }), label);
  };
  function PieLabels(_ref2) {
    var sectors = _ref2.sectors, props = _ref2.props, showLabels = _ref2.showLabels;
    var label = props.label, labelLine = props.labelLine, dataKey = props.dataKey;
    if (!showLabels || !label || !sectors) {
      return null;
    }
    var pieProps = svgPropertiesNoEvents(props);
    var customLabelProps = svgPropertiesNoEventsFromUnknown(label);
    var customLabelLineProps = svgPropertiesNoEventsFromUnknown(labelLine);
    var offsetRadius = typeof label === "object" && "offsetRadius" in label && typeof label.offsetRadius === "number" && label.offsetRadius || 20;
    var labels = sectors.map((entry, i) => {
      var midAngle = (entry.startAngle + entry.endAngle) / 2;
      var endPoint = polarToCartesian(entry.cx, entry.cy, entry.outerRadius + offsetRadius, midAngle);
      var labelProps = _objectSpread25(_objectSpread25(_objectSpread25(_objectSpread25({}, pieProps), entry), {}, {
        // @ts-expect-error customLabelProps is contributing unknown props
        stroke: "none"
      }, customLabelProps), {}, {
        index: i,
        textAnchor: getTextAnchor(endPoint.x, entry.cx)
      }, endPoint);
      var lineProps = _objectSpread25(_objectSpread25(_objectSpread25(_objectSpread25({}, pieProps), entry), {}, {
        // @ts-expect-error customLabelLineProps is contributing unknown props
        fill: "none",
        // @ts-expect-error customLabelLineProps is contributing unknown props
        stroke: entry.fill
      }, customLabelLineProps), {}, {
        index: i,
        points: [polarToCartesian(entry.cx, entry.cy, entry.outerRadius, midAngle), endPoint],
        key: "line"
      });
      return /* @__PURE__ */ React22.createElement(ZIndexLayer, {
        zIndex: DefaultZIndexes.label,
        key: "label-".concat(entry.startAngle, "-").concat(entry.endAngle, "-").concat(entry.midAngle, "-").concat(i)
      }, /* @__PURE__ */ React22.createElement(Layer, null, labelLine && renderLabelLineItem(labelLine, lineProps), renderLabelItem(label, labelProps, getValueByDataKey(entry, dataKey))));
    });
    return /* @__PURE__ */ React22.createElement(Layer, {
      className: "recharts-pie-labels"
    }, labels);
  }
  function PieLabelList(_ref3) {
    var sectors = _ref3.sectors, props = _ref3.props, showLabels = _ref3.showLabels;
    var label = props.label;
    if (typeof label === "object" && label != null && "position" in label) {
      return /* @__PURE__ */ React22.createElement(LabelListFromLabelProp, {
        label
      });
    }
    return /* @__PURE__ */ React22.createElement(PieLabels, {
      sectors,
      props,
      showLabels
    });
  }
  function PieSectors(props) {
    var sectors = props.sectors, activeShape = props.activeShape, inactiveShapeProp = props.inactiveShape, allOtherPieProps = props.allOtherPieProps, shape = props.shape, id = props.id, animationElapsedTime = props.animationElapsedTime, isAnimating = props.isAnimating, isEntrance = props.isEntrance;
    var activeIndex = useAppSelector(selectActiveTooltipIndex);
    var activeDataKey = useAppSelector(selectActiveTooltipDataKey);
    var activeGraphicalItemId = useAppSelector(selectActiveTooltipGraphicalItemId);
    var onMouseEnterFromProps = allOtherPieProps.onMouseEnter, onItemClickFromProps = allOtherPieProps.onClick, onMouseLeaveFromProps = allOtherPieProps.onMouseLeave, restOfAllOtherProps = _objectWithoutProperties9(allOtherPieProps, _excluded26);
    var onMouseEnterFromContext = useMouseEnterItemDispatch(onMouseEnterFromProps, allOtherPieProps.dataKey, id);
    var onMouseLeaveFromContext = useMouseLeaveItemDispatch(onMouseLeaveFromProps);
    var onClickFromContext = useMouseClickItemDispatch(onItemClickFromProps, allOtherPieProps.dataKey, id);
    if (sectors == null || sectors.length === 0) {
      return null;
    }
    return /* @__PURE__ */ React22.createElement(React22.Fragment, null, sectors.map((entry, i) => {
      if ((entry === null || entry === void 0 ? void 0 : entry.startAngle) === 0 && (entry === null || entry === void 0 ? void 0 : entry.endAngle) === 0 && sectors.length !== 1) return null;
      var graphicalItemMatches = activeGraphicalItemId == null || activeGraphicalItemId === id;
      var isActive = String(i) === activeIndex && (activeDataKey == null || allOtherPieProps.dataKey === activeDataKey) && graphicalItemMatches;
      var inactiveShape = activeIndex ? inactiveShapeProp : null;
      var sectorOptions = activeShape && isActive ? activeShape : inactiveShape;
      var sectorProps = _objectSpread25(_objectSpread25({}, entry), {}, {
        stroke: entry.stroke,
        tabIndex: -1,
        index: i,
        isActive,
        animationElapsedTime,
        isAnimating,
        isEntrance,
        [DATA_ITEM_INDEX_ATTRIBUTE_NAME]: i,
        [DATA_ITEM_GRAPHICAL_ITEM_ID_ATTRIBUTE_NAME]: id
      });
      return /* @__PURE__ */ React22.createElement(Layer, _extends13({
        key: "sector-".concat(entry === null || entry === void 0 ? void 0 : entry.startAngle, "-").concat(entry === null || entry === void 0 ? void 0 : entry.endAngle, "-").concat(entry.midAngle, "-").concat(i),
        tabIndex: -1,
        className: "recharts-pie-sector"
      }, adaptEventsOfChild(restOfAllOtherProps, entry, i), {
        onMouseEnter: onMouseEnterFromContext(entry, i),
        onMouseLeave: onMouseLeaveFromContext(entry, i),
        onClick: onClickFromContext(entry, i)
      }), /* @__PURE__ */ React22.createElement(Shape, {
        option: sectorOptions !== null && sectorOptions !== void 0 ? sectorOptions : shape,
        DefaultShape: defaultPieSectorShape,
        shapeProps: sectorProps
      }));
    }));
  }
  function computePieSectors(_ref4) {
    var _pieSettings$paddingA;
    var pieSettings = _ref4.pieSettings, displayedData = _ref4.displayedData, cells = _ref4.cells, offset = _ref4.offset;
    var cornerRadius = pieSettings.cornerRadius, startAngle = pieSettings.startAngle, endAngle = pieSettings.endAngle, dataKey = pieSettings.dataKey, nameKey = pieSettings.nameKey, tooltipType = pieSettings.tooltipType;
    var minAngle = Math.abs(pieSettings.minAngle);
    var deltaAngle = parseDeltaAngle(startAngle, endAngle);
    var absDeltaAngle = Math.abs(deltaAngle);
    var paddingAngle = displayedData.length <= 1 ? 0 : (_pieSettings$paddingA = pieSettings.paddingAngle) !== null && _pieSettings$paddingA !== void 0 ? _pieSettings$paddingA : 0;
    var notZeroItemCount = displayedData.filter((entry) => getValueByDataKey(entry, dataKey, 0) !== 0).length;
    var totalPaddingAngle = (absDeltaAngle >= 360 ? notZeroItemCount : notZeroItemCount - 1) * paddingAngle;
    var sum3 = displayedData.reduce((result, entry) => {
      var val = getValueByDataKey(entry, dataKey, 0);
      return result + (isNumber(val) ? val : 0);
    }, 0);
    var needsMinAngleAdjustment = minAngle > 0 && sum3 > 0 && displayedData.some((entry) => {
      var val = getValueByDataKey(entry, dataKey, 0);
      var percent = (isNumber(val) ? val : 0) / sum3;
      return val !== 0 && percent * absDeltaAngle < minAngle;
    });
    var effectiveMinAngle = needsMinAngleAdjustment ? minAngle : 0;
    var realTotalAngle = absDeltaAngle - notZeroItemCount * effectiveMinAngle - totalPaddingAngle;
    var sectors;
    if (sum3 > 0) {
      var prev;
      sectors = displayedData.map((entry, i) => {
        var val = getValueByDataKey(entry, dataKey, 0);
        var name = getValueByDataKey(entry, nameKey, i);
        var coordinate = parseCoordinateOfPie(pieSettings, offset, entry);
        var percent = (isNumber(val) ? val : 0) / sum3;
        var tempStartAngle;
        var entryWithCellInfo = _objectSpread25(_objectSpread25({}, entry), cells && cells[i] && cells[i].props);
        var sectorColor = entryWithCellInfo != null && "fill" in entryWithCellInfo && typeof entryWithCellInfo.fill === "string" ? entryWithCellInfo.fill : pieSettings.fill;
        if (i) {
          tempStartAngle = prev.endAngle + mathSign(deltaAngle) * paddingAngle * (val !== 0 ? 1 : 0);
        } else {
          tempStartAngle = startAngle;
        }
        var tempEndAngle = tempStartAngle + mathSign(deltaAngle) * ((val !== 0 ? effectiveMinAngle : 0) + percent * realTotalAngle);
        var midAngle = (tempStartAngle + tempEndAngle) / 2;
        var middleRadius = (coordinate.innerRadius + coordinate.outerRadius) / 2;
        var tooltipPayload = [{
          name,
          value: val,
          payload: entryWithCellInfo,
          dataKey,
          type: tooltipType,
          color: sectorColor,
          fill: sectorColor,
          graphicalItemId: pieSettings.id
        }];
        var tooltipPosition = polarToCartesian(coordinate.cx, coordinate.cy, middleRadius, midAngle);
        prev = _objectSpread25(_objectSpread25(_objectSpread25(_objectSpread25({}, pieSettings.presentationProps), {}, {
          percent,
          cornerRadius: typeof cornerRadius === "string" ? parseFloat(cornerRadius) : cornerRadius,
          name,
          tooltipPayload,
          midAngle,
          middleRadius,
          tooltipPosition
        }, entryWithCellInfo), coordinate), {}, {
          value: val,
          dataKey,
          startAngle: tempStartAngle,
          endAngle: tempEndAngle,
          payload: entryWithCellInfo,
          paddingAngle: val !== 0 ? mathSign(deltaAngle) * paddingAngle : 0
        });
        return prev;
      });
    }
    return sectors;
  }
  function PieLabelListProvider(_ref5) {
    var showLabels = _ref5.showLabels, sectors = _ref5.sectors, children = _ref5.children;
    var labelListEntries = (0, import_react36.useMemo)(() => {
      if (!showLabels || !sectors) {
        return [];
      }
      return sectors.map((entry) => ({
        value: entry.value,
        payload: entry.payload,
        clockWise: false,
        parentViewBox: void 0,
        viewBox: {
          cx: entry.cx,
          cy: entry.cy,
          innerRadius: entry.innerRadius,
          outerRadius: entry.outerRadius,
          startAngle: entry.startAngle,
          endAngle: entry.endAngle,
          clockWise: false
        },
        fill: entry.fill
      }));
    }, [sectors, showLabels]);
    return /* @__PURE__ */ React22.createElement(PolarLabelListContextProvider, {
      value: showLabels ? labelListEntries : void 0
    }, children);
  }
  var defaultPieAnimateItems = (items, animationElapsedTime) => {
    if (items == null) return [];
    var stepData = [];
    var firstNonRemoved = items.find((item) => item.status !== "removed");
    var curAngle = firstNonRemoved ? firstNonRemoved.next.startAngle : 0;
    items.forEach((item, index2) => {
      if (item.status === "removed") return;
      var paddingAngle = index2 > 0 ? get(item.next, "paddingAngle", 0) : 0;
      if (item.status === "matched") {
        var angle = interpolate(item.prev.endAngle - item.prev.startAngle, item.next.endAngle - item.next.startAngle, animationElapsedTime);
        var latest2 = _objectSpread25(_objectSpread25({}, item.next), {}, {
          startAngle: curAngle + paddingAngle,
          endAngle: curAngle + angle + paddingAngle
        });
        stepData.push(latest2);
        curAngle = latest2.endAngle;
      } else {
        var deltaAngle = interpolate(0, item.next.endAngle - item.next.startAngle, animationElapsedTime);
        var _latest = _objectSpread25(_objectSpread25({}, item.next), {}, {
          startAngle: curAngle + paddingAngle,
          endAngle: curAngle + deltaAngle + paddingAngle
        });
        stepData.push(_latest);
        curAngle = _latest.endAngle;
      }
    });
    return stepData;
  };
  function SectorsWithAnimation(_ref6) {
    var props = _ref6.props, previousSectorsRef = _ref6.previousSectorsRef, id = _ref6.id;
    var sectors = props.sectors, activeShape = props.activeShape, inactiveShape = props.inactiveShape, animationInterpolateFn = props.animationInterpolateFn;
    var _useAnimationCallback = useAnimationCallbacks(props.onAnimationStart, props.onAnimationEnd), isAnimating = _useAnimationCallback.isAnimating, handleAnimationStart = _useAnimationCallback.handleAnimationStart, handleAnimationEnd = _useAnimationCallback.handleAnimationEnd;
    var layout = usePolarChartLayout();
    if (layout == null) return null;
    return /* @__PURE__ */ React22.createElement(PieLabelListProvider, {
      showLabels: !isAnimating,
      sectors
    }, /* @__PURE__ */ React22.createElement(AnimatedItems, {
      animationInput: props,
      animationIdPrefix: "recharts-pie-",
      items: sectors,
      previousItemsRef: previousSectorsRef,
      isAnimationActive: props.isAnimationActive,
      animationBegin: props.animationBegin,
      animationDuration: props.animationDuration,
      animationEasing: props.animationEasing,
      onAnimationStart: handleAnimationStart,
      onAnimationEnd: handleAnimationEnd,
      animationInterpolateFn,
      animationMatchBy: props.animationMatchBy,
      layout
    }, (stepData, animationElapsedTime, isEntrance) => /* @__PURE__ */ React22.createElement(Layer, null, /* @__PURE__ */ React22.createElement(PieSectors, {
      sectors: stepData,
      activeShape,
      inactiveShape,
      allOtherPieProps: props,
      shape: props.shape,
      id,
      animationElapsedTime,
      isAnimating: isAnimating || animationElapsedTime < 1,
      isEntrance
    }))), /* @__PURE__ */ React22.createElement(PieLabelList, {
      showLabels: !isAnimating,
      sectors,
      props
    }), props.children);
  }
  var defaultPieProps = {
    animationBegin: 400,
    animationDuration: 1500,
    animationEasing: "ease",
    animationInterpolateFn: defaultPieAnimateItems,
    animationMatchBy: matchAppend,
    cx: "50%",
    cy: "50%",
    dataKey: "value",
    endAngle: 360,
    fill: "#808080",
    hide: false,
    innerRadius: 0,
    isAnimationActive: "auto",
    label: false,
    labelLine: true,
    legendType: "rect",
    minAngle: 0,
    nameKey: "name",
    outerRadius: "80%",
    paddingAngle: 0,
    rootTabIndex: 0,
    shape: defaultPieSectorShape,
    startAngle: 0,
    stroke: "#fff",
    zIndex: DefaultZIndexes.area
  };
  function PieImpl(props) {
    var id = props.id, propsWithoutId = _objectWithoutProperties9(props, _excluded32);
    var hide = props.hide, className = props.className, rootTabIndex = props.rootTabIndex;
    var cells = (0, import_react36.useMemo)(() => findAllByType(props.children, Cell), [props.children]);
    var sectors = useAppSelector((state) => selectPieSectors(state, id, cells));
    var previousSectorsRef = (0, import_react36.useRef)(null);
    var layerClass = clsx("recharts-pie", className);
    if (hide || sectors == null) {
      previousSectorsRef.current = null;
      return /* @__PURE__ */ React22.createElement(Layer, {
        tabIndex: rootTabIndex,
        className: layerClass
      });
    }
    return /* @__PURE__ */ React22.createElement(ZIndexLayer, {
      zIndex: props.zIndex
    }, /* @__PURE__ */ React22.createElement(SetPieTooltipEntrySettings, {
      dataKey: props.dataKey,
      nameKey: props.nameKey,
      sectors,
      stroke: props.stroke,
      strokeWidth: props.strokeWidth,
      fill: props.fill,
      name: props.name,
      hide: props.hide,
      tooltipType: props.tooltipType,
      formatter: props.formatter,
      id,
      activeShape: props.activeShape
    }), /* @__PURE__ */ React22.createElement(Layer, {
      tabIndex: rootTabIndex,
      className: layerClass
    }, /* @__PURE__ */ React22.createElement(SectorsWithAnimation, {
      props: _objectSpread25(_objectSpread25({}, propsWithoutId), {}, {
        sectors
      }),
      previousSectorsRef,
      id
    })));
  }
  function PieFn(outsideProps) {
    var props = resolveDefaultProps(outsideProps, defaultPieProps);
    var externalId = props.id, propsWithoutId = _objectWithoutProperties9(props, _excluded42);
    var presentationProps = svgPropertiesNoEvents(propsWithoutId);
    return /* @__PURE__ */ React22.createElement(RegisterGraphicalItemId, {
      id: externalId,
      type: "pie"
    }, (id) => /* @__PURE__ */ React22.createElement(React22.Fragment, null, /* @__PURE__ */ React22.createElement(SetPolarGraphicalItem, {
      type: "pie",
      id,
      data: propsWithoutId.data,
      dataKey: propsWithoutId.dataKey,
      hide: propsWithoutId.hide,
      angleAxisId: 0,
      radiusAxisId: 0,
      name: propsWithoutId.name,
      nameKey: propsWithoutId.nameKey,
      tooltipType: propsWithoutId.tooltipType,
      legendType: propsWithoutId.legendType,
      fill: propsWithoutId.fill,
      cx: propsWithoutId.cx,
      cy: propsWithoutId.cy,
      startAngle: propsWithoutId.startAngle,
      endAngle: propsWithoutId.endAngle,
      paddingAngle: propsWithoutId.paddingAngle,
      minAngle: propsWithoutId.minAngle,
      innerRadius: propsWithoutId.innerRadius,
      outerRadius: propsWithoutId.outerRadius,
      cornerRadius: propsWithoutId.cornerRadius,
      presentationProps,
      maxRadius: props.maxRadius
    }), /* @__PURE__ */ React22.createElement(SetPiePayloadLegend, _extends13({}, propsWithoutId, {
      id
    })), /* @__PURE__ */ React22.createElement(PieImpl, _extends13({}, propsWithoutId, {
      id
    }))));
  }
  var Pie = PieFn;
  Pie.displayName = "Pie";

  // node_modules/recharts/es6/component/Dots.js
  var React23 = __toESM(require_react());
  var import_react37 = __toESM(require_react());
  var _excluded10 = ["points"];
  function ownKeys26(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread26(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys26(Object(t), true).forEach(function(r3) {
        _defineProperty29(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys26(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty29(e, r2, t) {
    return (r2 = _toPropertyKey29(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey29(t) {
    var i = _toPrimitive29(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive29(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  function _extends14() {
    return _extends14 = Object.assign ? Object.assign.bind() : function(n) {
      for (var e = 1; e < arguments.length; e++) {
        var t = arguments[e];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends14.apply(null, arguments);
  }
  function _objectWithoutProperties10(e, t) {
    if (null == e) return {};
    var o, r2, i = _objectWithoutPropertiesLoose10(e, t);
    if (Object.getOwnPropertySymbols) {
      var n = Object.getOwnPropertySymbols(e);
      for (r2 = 0; r2 < n.length; r2++) o = n[r2], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
    }
    return i;
  }
  function _objectWithoutPropertiesLoose10(r2, e) {
    if (null == r2) return {};
    var t = {};
    for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
      if (-1 !== e.indexOf(n)) continue;
      t[n] = r2[n];
    }
    return t;
  }
  function DotItem(_ref2) {
    var option = _ref2.option, dotProps = _ref2.dotProps, className = _ref2.className;
    if (/* @__PURE__ */ (0, import_react37.isValidElement)(option)) {
      return /* @__PURE__ */ (0, import_react37.cloneElement)(option, dotProps);
    }
    if (typeof option === "function") {
      return option(dotProps);
    }
    var finalClassName = clsx(className, typeof option !== "boolean" ? option.className : "");
    var _ref22 = dotProps !== null && dotProps !== void 0 ? dotProps : {}, points = _ref22.points, props = _objectWithoutProperties10(_ref22, _excluded10);
    return /* @__PURE__ */ React23.createElement(Dot, _extends14({}, props, {
      className: finalClassName
    }));
  }
  function shouldRenderDots(points, dot) {
    if (points == null) {
      return false;
    }
    if (dot) {
      return true;
    }
    return points.length === 1;
  }
  function Dots(_ref3) {
    var points = _ref3.points, dot = _ref3.dot, className = _ref3.className, dotClassName = _ref3.dotClassName, dataKey = _ref3.dataKey, baseProps = _ref3.baseProps, needClip = _ref3.needClip, clipPathId = _ref3.clipPathId, _ref3$zIndex = _ref3.zIndex, zIndex = _ref3$zIndex === void 0 ? DefaultZIndexes.scatter : _ref3$zIndex;
    if (!shouldRenderDots(points, dot)) {
      return null;
    }
    var clipDot = isClipDot(dot);
    var customDotProps = svgPropertiesAndEventsFromUnknown(dot);
    var dots = points.map((entry, i) => {
      var _entry$x, _entry$y;
      var dotProps = _objectSpread26(_objectSpread26(_objectSpread26({
        r: 3
      }, baseProps), customDotProps), {}, {
        index: i,
        cx: (_entry$x = entry.x) !== null && _entry$x !== void 0 ? _entry$x : void 0,
        cy: (_entry$y = entry.y) !== null && _entry$y !== void 0 ? _entry$y : void 0,
        dataKey,
        value: entry.value,
        payload: entry.payload,
        points
      });
      return /* @__PURE__ */ React23.createElement(DotItem, {
        key: "dot-".concat(i),
        option: dot,
        dotProps,
        className: dotClassName
      });
    });
    var layerProps = {};
    if (needClip && clipPathId != null) {
      layerProps.clipPath = "url(#clipPath-".concat(clipDot ? "" : "dots-").concat(clipPathId, ")");
    }
    return /* @__PURE__ */ React23.createElement(ZIndexLayer, {
      zIndex
    }, /* @__PURE__ */ React23.createElement(Layer, _extends14({
      className
    }, layerProps), dots));
  }

  // node_modules/recharts/es6/component/ActivePoints.js
  var React24 = __toESM(require_react());
  var import_react38 = __toESM(require_react());

  // node_modules/recharts/es6/state/cartesianAxisSlice.js
  function ownKeys27(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread27(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys27(Object(t), true).forEach(function(r3) {
        _defineProperty30(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys27(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty30(e, r2, t) {
    return (r2 = _toPropertyKey30(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey30(t) {
    var i = _toPrimitive30(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive30(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  var defaultAxisId = 0;
  var initialState8 = {
    xAxis: {},
    yAxis: {},
    zAxis: {}
  };
  var cartesianAxisSlice = createSlice({
    name: "cartesianAxis",
    initialState: initialState8,
    reducers: {
      addXAxis: {
        reducer(state, action) {
          state.xAxis[action.payload.id] = castDraft(action.payload);
        },
        prepare: prepareAutoBatched()
      },
      replaceXAxis: {
        reducer(state, action) {
          var _action$payload = action.payload, prev = _action$payload.prev, next = _action$payload.next;
          if (state.xAxis[prev.id] !== void 0) {
            if (prev.id !== next.id) {
              delete state.xAxis[prev.id];
            }
            state.xAxis[next.id] = castDraft(next);
          }
        },
        prepare: prepareAutoBatched()
      },
      removeXAxis: {
        reducer(state, action) {
          delete state.xAxis[action.payload.id];
        },
        prepare: prepareAutoBatched()
      },
      addYAxis: {
        reducer(state, action) {
          state.yAxis[action.payload.id] = castDraft(action.payload);
        },
        prepare: prepareAutoBatched()
      },
      replaceYAxis: {
        reducer(state, action) {
          var _action$payload2 = action.payload, prev = _action$payload2.prev, next = _action$payload2.next;
          if (state.yAxis[prev.id] !== void 0) {
            if (prev.id !== next.id) {
              delete state.yAxis[prev.id];
            }
            state.yAxis[next.id] = castDraft(next);
          }
        },
        prepare: prepareAutoBatched()
      },
      removeYAxis: {
        reducer(state, action) {
          delete state.yAxis[action.payload.id];
        },
        prepare: prepareAutoBatched()
      },
      addZAxis: {
        reducer(state, action) {
          state.zAxis[action.payload.id] = castDraft(action.payload);
        },
        prepare: prepareAutoBatched()
      },
      replaceZAxis: {
        reducer(state, action) {
          var _action$payload3 = action.payload, prev = _action$payload3.prev, next = _action$payload3.next;
          if (state.zAxis[prev.id] !== void 0) {
            if (prev.id !== next.id) {
              delete state.zAxis[prev.id];
            }
            state.zAxis[next.id] = castDraft(next);
          }
        },
        prepare: prepareAutoBatched()
      },
      removeZAxis: {
        reducer(state, action) {
          delete state.zAxis[action.payload.id];
        },
        prepare: prepareAutoBatched()
      },
      updateYAxisWidth(state, action) {
        var _action$payload4 = action.payload, id = _action$payload4.id, width = _action$payload4.width;
        var axis = state.yAxis[id];
        if (axis) {
          var _history$;
          var history = axis.widthHistory || [];
          if (history.length === 3 && history[0] === history[2] && width === history[1] && width !== axis.width && Math.abs(width - ((_history$ = history[0]) !== null && _history$ !== void 0 ? _history$ : 0)) <= 1) {
            return;
          }
          var newHistory = [...history, width].slice(-3);
          state.yAxis[id] = _objectSpread27(_objectSpread27({}, axis), {}, {
            width,
            widthHistory: newHistory
          });
        }
      }
    }
  });
  var _cartesianAxisSlice$a = cartesianAxisSlice.actions;
  var addXAxis = _cartesianAxisSlice$a.addXAxis;
  var replaceXAxis = _cartesianAxisSlice$a.replaceXAxis;
  var removeXAxis = _cartesianAxisSlice$a.removeXAxis;
  var addYAxis = _cartesianAxisSlice$a.addYAxis;
  var replaceYAxis = _cartesianAxisSlice$a.replaceYAxis;
  var removeYAxis = _cartesianAxisSlice$a.removeYAxis;
  var addZAxis = _cartesianAxisSlice$a.addZAxis;
  var replaceZAxis = _cartesianAxisSlice$a.replaceZAxis;
  var removeZAxis = _cartesianAxisSlice$a.removeZAxis;
  var updateYAxisWidth = _cartesianAxisSlice$a.updateYAxisWidth;
  var cartesianAxisReducer = cartesianAxisSlice.reducer;

  // node_modules/recharts/es6/state/selectors/selectChartOffset.js
  var selectChartOffset = createSelector([selectChartOffsetInternal], (offsetInternal) => {
    return {
      top: offsetInternal.top,
      bottom: offsetInternal.bottom,
      left: offsetInternal.left,
      right: offsetInternal.right
    };
  });

  // node_modules/recharts/es6/state/selectors/selectPlotArea.js
  var selectPlotArea = createSelector([selectChartOffset, selectChartWidth, selectChartHeight], (offset, chartWidth, chartHeight) => {
    if (!offset || chartWidth == null || chartHeight == null) {
      return void 0;
    }
    return {
      x: offset.left,
      y: offset.top,
      width: Math.max(0, chartWidth - offset.left - offset.right),
      height: Math.max(0, chartHeight - offset.top - offset.bottom)
    };
  });

  // node_modules/recharts/es6/hooks.js
  var usePlotArea = () => {
    return useAppSelector(selectPlotArea);
  };
  var useActiveTooltipDataPoints = () => {
    return useAppSelector(selectActiveTooltipDataPoints);
  };

  // node_modules/recharts/es6/component/ActivePoints.js
  function ownKeys28(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread28(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys28(Object(t), true).forEach(function(r3) {
        _defineProperty31(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys28(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty31(e, r2, t) {
    return (r2 = _toPropertyKey31(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey31(t) {
    var i = _toPrimitive31(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive31(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  var ActivePoint = (_ref2) => {
    var point6 = _ref2.point, childIndex = _ref2.childIndex, mainColor = _ref2.mainColor, activeDot = _ref2.activeDot, dataKey = _ref2.dataKey, clipPath = _ref2.clipPath;
    if (activeDot === false || point6.x == null || point6.y == null) {
      return null;
    }
    var dotPropsTyped = {
      index: childIndex,
      dataKey,
      cx: point6.x,
      cy: point6.y,
      r: 4,
      fill: mainColor !== null && mainColor !== void 0 ? mainColor : "none",
      strokeWidth: 2,
      stroke: "#fff",
      payload: point6.payload,
      value: point6.value
    };
    var dotProps = _objectSpread28(_objectSpread28(_objectSpread28({}, dotPropsTyped), svgPropertiesNoEventsFromUnknown(activeDot)), adaptEventHandlers(activeDot));
    var dot;
    if (/* @__PURE__ */ (0, import_react38.isValidElement)(activeDot)) {
      dot = /* @__PURE__ */ (0, import_react38.cloneElement)(activeDot, dotProps);
    } else if (typeof activeDot === "function") {
      dot = activeDot(dotProps);
    } else {
      dot = /* @__PURE__ */ React24.createElement(Dot, dotProps);
    }
    return /* @__PURE__ */ React24.createElement(Layer, {
      className: "recharts-active-dot",
      clipPath
    }, dot);
  };
  function ActivePoints(_ref2) {
    var points = _ref2.points, mainColor = _ref2.mainColor, activeDot = _ref2.activeDot, itemDataKey = _ref2.itemDataKey, clipPath = _ref2.clipPath, _ref2$zIndex = _ref2.zIndex, zIndex = _ref2$zIndex === void 0 ? DefaultZIndexes.activeDot : _ref2$zIndex;
    var activeTooltipIndex = useAppSelector(selectActiveTooltipIndex);
    var activeDataPoints = useActiveTooltipDataPoints();
    if (points == null || activeDataPoints == null) {
      return null;
    }
    var activePoint = points.find((p) => activeDataPoints.includes(p.payload));
    if (isNullish(activePoint)) {
      return null;
    }
    return /* @__PURE__ */ React24.createElement(ZIndexLayer, {
      zIndex
    }, /* @__PURE__ */ React24.createElement(ActivePoint, {
      point: activePoint,
      childIndex: Number(activeTooltipIndex),
      mainColor,
      dataKey: itemDataKey,
      activeDot,
      clipPath
    }));
  }

  // node_modules/recharts/es6/state/selectors/combiners/combineBarSizeList.js
  function _slicedToArray16(r2, e) {
    return _arrayWithHoles16(r2) || _iterableToArrayLimit16(r2, e) || _unsupportedIterableToArray16(r2, e) || _nonIterableRest16();
  }
  function _nonIterableRest16() {
    throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  function _unsupportedIterableToArray16(r2, a2) {
    if (r2) {
      if ("string" == typeof r2) return _arrayLikeToArray16(r2, a2);
      var t = {}.toString.call(r2).slice(8, -1);
      return "Object" === t && r2.constructor && (t = r2.constructor.name), "Map" === t || "Set" === t ? Array.from(r2) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray16(r2, a2) : void 0;
    }
  }
  function _arrayLikeToArray16(r2, a2) {
    (null == a2 || a2 > r2.length) && (a2 = r2.length);
    for (var e = 0, n = Array(a2); e < a2; e++) n[e] = r2[e];
    return n;
  }
  function _iterableToArrayLimit16(r2, l) {
    var t = null == r2 ? null : "undefined" != typeof Symbol && r2[Symbol.iterator] || r2["@@iterator"];
    if (null != t) {
      var e, n, i, u, a2 = [], f = true, o = false;
      try {
        if (i = (t = t.call(r2)).next, 0 === l) {
          if (Object(t) !== t) return;
          f = false;
        } else for (; !(f = (e = i.call(t)).done) && (a2.push(e.value), a2.length !== l); f = true) ;
      } catch (r3) {
        o = true, n = r3;
      } finally {
        try {
          if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return;
        } finally {
          if (o) throw n;
        }
      }
      return a2;
    }
  }
  function _arrayWithHoles16(r2) {
    if (Array.isArray(r2)) return r2;
  }
  var getBarSize = (globalSize, totalSize, selfSize) => {
    var barSize = selfSize !== null && selfSize !== void 0 ? selfSize : globalSize;
    if (isNullish(barSize)) {
      return void 0;
    }
    return getPercentValue(barSize, totalSize, 0);
  };
  var combineBarSizeList = (allBars, globalSize, totalSize) => {
    var initialValue = {};
    var stackedBars = allBars.filter(isStacked);
    var unstackedBars = allBars.filter((b) => b.stackId == null);
    var groupByStack = stackedBars.reduce((acc, bar) => {
      var s = acc[bar.stackId];
      if (s == null) {
        s = [];
      }
      s.push(bar);
      acc[bar.stackId] = s;
      return acc;
    }, initialValue);
    var stackedSizeList = Object.entries(groupByStack).map((_ref2) => {
      var _bars$;
      var _ref22 = _slicedToArray16(_ref2, 2), stackId = _ref22[0], bars = _ref22[1];
      var dataKeys = bars.map((b) => b.dataKey);
      var barSize = getBarSize(globalSize, totalSize, (_bars$ = bars[0]) === null || _bars$ === void 0 ? void 0 : _bars$.barSize);
      return {
        stackId,
        dataKeys,
        barSize
      };
    });
    var unstackedSizeList = unstackedBars.map((b) => {
      var dataKeys = [b.dataKey].filter((dk) => dk != null);
      var barSize = getBarSize(globalSize, totalSize, b.barSize);
      return {
        stackId: void 0,
        dataKeys,
        barSize
      };
    });
    return [...stackedSizeList, ...unstackedSizeList];
  };

  // node_modules/recharts/es6/state/selectors/combiners/combineAllBarPositions.js
  function ownKeys29(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread29(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys29(Object(t), true).forEach(function(r3) {
        _defineProperty32(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys29(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty32(e, r2, t) {
    return (r2 = _toPropertyKey32(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey32(t) {
    var i = _toPrimitive32(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive32(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  function getBarPositions(barGap, barCategoryGap, bandSize, sizeList, maxBarSize) {
    var _sizeList$;
    var len = sizeList.length;
    if (len < 1) {
      return void 0;
    }
    var realBarGap = getPercentValue(barGap, bandSize, 0, true);
    var result;
    var initialValue = [];
    if (isWellBehavedNumber((_sizeList$ = sizeList[0]) === null || _sizeList$ === void 0 ? void 0 : _sizeList$.barSize)) {
      var useFull = false;
      var fullBarSize = bandSize / len;
      var sum3 = sizeList.reduce((res, entry) => res + (entry.barSize || 0), 0);
      sum3 += (len - 1) * realBarGap;
      if (sum3 >= bandSize) {
        sum3 -= (len - 1) * realBarGap;
        realBarGap = 0;
      }
      if (sum3 >= bandSize && fullBarSize > 0) {
        useFull = true;
        fullBarSize *= 0.9;
        sum3 = len * fullBarSize;
      }
      var offset = Math.round((bandSize - sum3) / 2);
      var prev = {
        offset: offset - realBarGap,
        size: 0
      };
      result = sizeList.reduce((res, entry) => {
        var _entry$barSize;
        var newPosition = {
          stackId: entry.stackId,
          dataKeys: entry.dataKeys,
          position: {
            offset: prev.offset + prev.size + realBarGap,
            size: useFull ? fullBarSize : (_entry$barSize = entry.barSize) !== null && _entry$barSize !== void 0 ? _entry$barSize : 0
          }
        };
        var newRes = [...res, newPosition];
        prev = newPosition.position;
        return newRes;
      }, initialValue);
    } else {
      var _offset = getPercentValue(barCategoryGap, bandSize, 0, true);
      if (bandSize - 2 * _offset - (len - 1) * realBarGap <= 0) {
        realBarGap = 0;
      }
      var originalSize = (bandSize - 2 * _offset - (len - 1) * realBarGap) / len;
      if (originalSize > 1) {
        originalSize = Math.round(originalSize);
      }
      var size = isWellBehavedNumber(maxBarSize) ? Math.min(originalSize, maxBarSize) : originalSize;
      result = sizeList.reduce((res, entry, i) => [...res, {
        stackId: entry.stackId,
        dataKeys: entry.dataKeys,
        position: {
          offset: _offset + (originalSize + realBarGap) * i + (originalSize - size) / 2,
          size
        }
      }], initialValue);
    }
    return result;
  }
  var combineAllBarPositions = (sizeList, globalMaxBarSize, barGap, barCategoryGap, barBandSize, bandSize, childMaxBarSize) => {
    var maxBarSize = isNullish(childMaxBarSize) ? globalMaxBarSize : childMaxBarSize;
    var allBarPositions = getBarPositions(barGap, barCategoryGap, barBandSize !== bandSize ? barBandSize : bandSize, sizeList, maxBarSize);
    if (barBandSize !== bandSize && allBarPositions != null) {
      allBarPositions = allBarPositions.map((pos) => _objectSpread29(_objectSpread29({}, pos), {}, {
        position: _objectSpread29(_objectSpread29({}, pos.position), {}, {
          offset: pos.position.offset - barBandSize / 2
        })
      }));
    }
    return allBarPositions;
  };

  // node_modules/recharts/es6/state/selectors/combiners/combineStackedData.js
  var combineStackedData = (stackGroups, barSettings) => {
    var stackSeriesIdentifier = getStackSeriesIdentifier(barSettings);
    if (!stackGroups || stackSeriesIdentifier == null || barSettings == null) {
      return void 0;
    }
    var stackId = barSettings.stackId;
    if (stackId == null) {
      return void 0;
    }
    var stackGroup = stackGroups[stackId];
    if (!stackGroup) {
      return void 0;
    }
    var stackedData = stackGroup.stackedData;
    if (!stackedData) {
      return void 0;
    }
    return stackedData.find((sd) => sd.key === stackSeriesIdentifier);
  };

  // node_modules/recharts/es6/state/selectors/combiners/combineBarPosition.js
  var combineBarPosition = (allBarPositions, barSettings) => {
    if (allBarPositions == null || barSettings == null) {
      return void 0;
    }
    var position = allBarPositions.find((p) => p.stackId === barSettings.stackId && barSettings.dataKey != null && p.dataKeys.includes(barSettings.dataKey));
    if (position == null) {
      return void 0;
    }
    return position.position;
  };

  // node_modules/recharts/es6/zIndex/getZIndexFromUnknown.js
  function getZIndexFromUnknown(input, defaultZIndex) {
    if (input && typeof input === "object" && "zIndex" in input && typeof input.zIndex === "number" && isWellBehavedNumber(input.zIndex)) {
      return input.zIndex;
    }
    return defaultZIndex;
  }

  // node_modules/recharts/es6/context/chartDataContext.js
  var import_react39 = __toESM(require_react());
  var ChartDataContextProvider = (props) => {
    var chartData = props.chartData;
    var dispatch2 = useAppDispatch();
    var isPanorama = useIsPanorama();
    (0, import_react39.useEffect)(() => {
      if (isPanorama) {
        return () => {
        };
      }
      dispatch2(setChartData(chartData));
      return () => {
        dispatch2(setChartData(void 0));
      };
    }, [chartData, dispatch2, isPanorama]);
    return null;
  };

  // node_modules/recharts/es6/state/brushSlice.js
  var initialState9 = {
    x: 0,
    y: 0,
    width: 0,
    height: 0,
    padding: {
      top: 0,
      right: 0,
      bottom: 0,
      left: 0
    }
  };
  var brushSlice = createSlice({
    name: "brush",
    initialState: initialState9,
    reducers: {
      setBrushSettings(_state, action) {
        if (action.payload == null) {
          return initialState9;
        }
        return action.payload;
      }
    }
  });
  var setBrushSettings = brushSlice.actions.setBrushSettings;
  var brushReducer = brushSlice.reducer;

  // node_modules/recharts/es6/cartesian/ReferenceLine.js
  var React26 = __toESM(require_react());
  var import_react41 = __toESM(require_react());

  // node_modules/recharts/es6/util/CartesianUtils.js
  var rectWithPoints = (_ref2, _ref22) => {
    var x1 = _ref2.x, y1 = _ref2.y;
    var x22 = _ref22.x, y22 = _ref22.y;
    return {
      x: Math.min(x1, x22),
      y: Math.min(y1, y22),
      width: Math.abs(x22 - x1),
      height: Math.abs(y22 - y1)
    };
  };
  var rectWithCoords = (_ref3) => {
    var x1 = _ref3.x1, y1 = _ref3.y1, x22 = _ref3.x2, y22 = _ref3.y2;
    return rectWithPoints({
      x: x1,
      y: y1
    }, {
      x: x22,
      y: y22
    });
  };
  function normalizeAngle(angle) {
    return (angle % 180 + 180) % 180;
  }
  var getAngledRectangleWidth = function getAngledRectangleWidth2(_ref4) {
    var width = _ref4.width, height = _ref4.height;
    var angle = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 0;
    var normalizedAngle = normalizeAngle(angle);
    var angleRadians = normalizedAngle * Math.PI / 180;
    var angleThreshold = Math.atan(height / width);
    var angledWidth = angleRadians > angleThreshold && angleRadians < Math.PI - angleThreshold ? height / Math.sin(angleRadians) : width / Math.cos(angleRadians);
    return Math.abs(angledWidth);
  };

  // node_modules/recharts/es6/state/referenceElementsSlice.js
  var initialState10 = {
    dots: [],
    areas: [],
    lines: []
  };
  var referenceElementsSlice = createSlice({
    name: "referenceElements",
    initialState: initialState10,
    reducers: {
      addDot: (state, action) => {
        state.dots.push(action.payload);
      },
      removeDot: (state, action) => {
        var index2 = current(state).dots.findIndex((dot) => dot === action.payload);
        if (index2 !== -1) {
          state.dots.splice(index2, 1);
        }
      },
      addArea: (state, action) => {
        state.areas.push(action.payload);
      },
      removeArea: (state, action) => {
        var index2 = current(state).areas.findIndex((area) => area === action.payload);
        if (index2 !== -1) {
          state.areas.splice(index2, 1);
        }
      },
      addLine: (state, action) => {
        state.lines.push(castDraft(action.payload));
      },
      removeLine: (state, action) => {
        var index2 = current(state).lines.findIndex((line) => line === action.payload);
        if (index2 !== -1) {
          state.lines.splice(index2, 1);
        }
      }
    }
  });
  var _referenceElementsSli = referenceElementsSlice.actions;
  var addDot = _referenceElementsSli.addDot;
  var removeDot = _referenceElementsSli.removeDot;
  var addArea = _referenceElementsSli.addArea;
  var removeArea = _referenceElementsSli.removeArea;
  var addLine = _referenceElementsSli.addLine;
  var removeLine = _referenceElementsSli.removeLine;
  var referenceElementsReducer = referenceElementsSlice.reducer;

  // node_modules/recharts/es6/container/ClipPathProvider.js
  var React25 = __toESM(require_react());
  var import_react40 = __toESM(require_react());
  function _slicedToArray17(r2, e) {
    return _arrayWithHoles17(r2) || _iterableToArrayLimit17(r2, e) || _unsupportedIterableToArray17(r2, e) || _nonIterableRest17();
  }
  function _nonIterableRest17() {
    throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  function _unsupportedIterableToArray17(r2, a2) {
    if (r2) {
      if ("string" == typeof r2) return _arrayLikeToArray17(r2, a2);
      var t = {}.toString.call(r2).slice(8, -1);
      return "Object" === t && r2.constructor && (t = r2.constructor.name), "Map" === t || "Set" === t ? Array.from(r2) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray17(r2, a2) : void 0;
    }
  }
  function _arrayLikeToArray17(r2, a2) {
    (null == a2 || a2 > r2.length) && (a2 = r2.length);
    for (var e = 0, n = Array(a2); e < a2; e++) n[e] = r2[e];
    return n;
  }
  function _iterableToArrayLimit17(r2, l) {
    var t = null == r2 ? null : "undefined" != typeof Symbol && r2[Symbol.iterator] || r2["@@iterator"];
    if (null != t) {
      var e, n, i, u, a2 = [], f = true, o = false;
      try {
        if (i = (t = t.call(r2)).next, 0 === l) {
          if (Object(t) !== t) return;
          f = false;
        } else for (; !(f = (e = i.call(t)).done) && (a2.push(e.value), a2.length !== l); f = true) ;
      } catch (r3) {
        o = true, n = r3;
      } finally {
        try {
          if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return;
        } finally {
          if (o) throw n;
        }
      }
      return a2;
    }
  }
  function _arrayWithHoles17(r2) {
    if (Array.isArray(r2)) return r2;
  }
  var ClipPathIdContext = /* @__PURE__ */ (0, import_react40.createContext)(void 0);
  var ClipPathProvider = (_ref2) => {
    var children = _ref2.children;
    var _useState = (0, import_react40.useState)("".concat(uniqueId("recharts"), "-clip")), _useState2 = _slicedToArray17(_useState, 1), clipPathId = _useState2[0];
    var plotArea = usePlotArea();
    if (plotArea == null) {
      return null;
    }
    var x5 = plotArea.x, y5 = plotArea.y, width = plotArea.width, height = plotArea.height;
    return /* @__PURE__ */ React25.createElement(ClipPathIdContext.Provider, {
      value: clipPathId
    }, /* @__PURE__ */ React25.createElement("defs", null, /* @__PURE__ */ React25.createElement("clipPath", {
      id: clipPathId
    }, /* @__PURE__ */ React25.createElement("rect", {
      x: x5,
      y: y5,
      height,
      width
    }))), children);
  };
  var useClipPathId = () => {
    return (0, import_react40.useContext)(ClipPathIdContext);
  };

  // node_modules/recharts/es6/util/scale/CartesianScaleHelper.js
  var CartesianScaleHelperImpl = class {
    constructor(_ref2) {
      var x5 = _ref2.x, y5 = _ref2.y;
      this.xAxisScale = x5;
      this.yAxisScale = y5;
    }
    map(value2, _ref2) {
      var _this$xAxisScale$map, _this$yAxisScale$map;
      var position = _ref2.position;
      return {
        x: (_this$xAxisScale$map = this.xAxisScale.map(value2.x, {
          position
        })) !== null && _this$xAxisScale$map !== void 0 ? _this$xAxisScale$map : 0,
        y: (_this$yAxisScale$map = this.yAxisScale.map(value2.y, {
          position
        })) !== null && _this$yAxisScale$map !== void 0 ? _this$yAxisScale$map : 0
      };
    }
    mapWithFallback(value2, _ref3) {
      var _this$xAxisScale$map2, _this$yAxisScale$map2;
      var position = _ref3.position, fallback = _ref3.fallback;
      var fallbackY, fallbackX;
      if (fallback === "rangeMin") {
        fallbackY = this.yAxisScale.rangeMin();
      } else if (fallback === "rangeMax") {
        fallbackY = this.yAxisScale.rangeMax();
      } else {
        fallbackY = 0;
      }
      if (fallback === "rangeMin") {
        fallbackX = this.xAxisScale.rangeMin();
      } else if (fallback === "rangeMax") {
        fallbackX = this.xAxisScale.rangeMax();
      } else {
        fallbackX = 0;
      }
      return {
        x: (_this$xAxisScale$map2 = this.xAxisScale.map(value2.x, {
          position
        })) !== null && _this$xAxisScale$map2 !== void 0 ? _this$xAxisScale$map2 : fallbackX,
        y: (_this$yAxisScale$map2 = this.yAxisScale.map(value2.y, {
          position
        })) !== null && _this$yAxisScale$map2 !== void 0 ? _this$yAxisScale$map2 : fallbackY
      };
    }
    isInRange(_ref4) {
      var x5 = _ref4.x, y5 = _ref4.y;
      var xInRange = x5 == null || this.xAxisScale.isInRange(x5);
      var yInRange = y5 == null || this.yAxisScale.isInRange(y5);
      return xInRange && yInRange;
    }
  };

  // node_modules/recharts/es6/cartesian/ReferenceLine.js
  function ownKeys30(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread30(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys30(Object(t), true).forEach(function(r3) {
        _defineProperty33(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys30(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty33(e, r2, t) {
    return (r2 = _toPropertyKey33(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey33(t) {
    var i = _toPrimitive33(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive33(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  function _extends15() {
    return _extends15 = Object.assign ? Object.assign.bind() : function(n) {
      for (var e = 1; e < arguments.length; e++) {
        var t = arguments[e];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends15.apply(null, arguments);
  }
  var renderLine = (option, props) => {
    var line;
    if (/* @__PURE__ */ React26.isValidElement(option)) {
      line = /* @__PURE__ */ React26.cloneElement(option, props);
    } else if (typeof option === "function") {
      line = option(props);
    } else {
      if (!isWellBehavedNumber(props.x1) || !isWellBehavedNumber(props.y1) || !isWellBehavedNumber(props.x2) || !isWellBehavedNumber(props.y2)) {
        return null;
      }
      line = /* @__PURE__ */ React26.createElement("line", _extends15({}, props, {
        className: "recharts-reference-line-line"
      }));
    }
    return line;
  };
  var getHorizontalLineEndPoints = (yCoord, ifOverflow, position, yAxisOrientation, yAxisScale, viewBox) => {
    var x5 = viewBox.x, width = viewBox.width;
    var coord = yAxisScale.map(yCoord, {
      position
    });
    if (!isWellBehavedNumber(coord)) {
      return null;
    }
    if (ifOverflow === "discard" && !yAxisScale.isInRange(coord)) {
      return null;
    }
    var points = [{
      x: x5 + width,
      y: coord
    }, {
      x: x5,
      y: coord
    }];
    return yAxisOrientation === "left" ? points.reverse() : points;
  };
  var getVerticalLineEndPoints = (xCoord, ifOverflow, position, xAxisOrientation, xAxisScale, viewBox) => {
    var y5 = viewBox.y, height = viewBox.height;
    var coord = xAxisScale.map(xCoord, {
      position
    });
    if (!isWellBehavedNumber(coord)) {
      return null;
    }
    if (ifOverflow === "discard" && !xAxisScale.isInRange(coord)) {
      return null;
    }
    var points = [{
      x: coord,
      y: y5 + height
    }, {
      x: coord,
      y: y5
    }];
    return xAxisOrientation === "top" ? points.reverse() : points;
  };
  var getSegmentLineEndPoints = (segment, ifOverflow, position, scales) => {
    var points = [scales.mapWithFallback(segment[0], {
      position,
      fallback: "rangeMin"
    }), scales.mapWithFallback(segment[1], {
      position,
      fallback: "rangeMax"
    })];
    if (ifOverflow === "discard" && points.some((p) => !scales.isInRange(p))) {
      return null;
    }
    return points;
  };
  var getEndPoints = (xAxisScale, yAxisScale, viewBox, position, xAxisOrientation, yAxisOrientation, props) => {
    var xCoord = props.x, yCoord = props.y, segment = props.segment, ifOverflow = props.ifOverflow;
    var isFixedX = isNumOrStr(xCoord);
    var isFixedY = isNumOrStr(yCoord);
    if (isFixedY) {
      return getHorizontalLineEndPoints(yCoord, ifOverflow, position, yAxisOrientation, yAxisScale, viewBox);
    }
    if (isFixedX) {
      return getVerticalLineEndPoints(xCoord, ifOverflow, position, xAxisOrientation, xAxisScale, viewBox);
    }
    if (segment != null && segment.length === 2) {
      return getSegmentLineEndPoints(segment, ifOverflow, position, new CartesianScaleHelperImpl({
        x: xAxisScale,
        y: yAxisScale
      }));
    }
    return null;
  };
  function ReportReferenceLine(props) {
    var dispatch2 = useAppDispatch();
    (0, import_react41.useEffect)(() => {
      dispatch2(addLine(props));
      return () => {
        dispatch2(removeLine(props));
      };
    });
    return null;
  }
  function ReferenceLineImpl(props) {
    var xAxisId = props.xAxisId, yAxisId = props.yAxisId, shape = props.shape, className = props.className, ifOverflow = props.ifOverflow;
    var isPanorama = useIsPanorama();
    var clipPathId = useClipPathId();
    var xAxis = useAppSelector((state) => selectXAxisSettings(state, xAxisId));
    var yAxis = useAppSelector((state) => selectYAxisSettings(state, yAxisId));
    var xAxisScale = useAppSelector((state) => selectAxisScale(state, "xAxis", xAxisId, isPanorama));
    var yAxisScale = useAppSelector((state) => selectAxisScale(state, "yAxis", yAxisId, isPanorama));
    var viewBox = useViewBox();
    if (!clipPathId || !viewBox || xAxis == null || yAxis == null || xAxisScale == null || yAxisScale == null) {
      return null;
    }
    var endPoints = getEndPoints(xAxisScale, yAxisScale, viewBox, props.position, xAxis.orientation, yAxis.orientation, props);
    if (!endPoints) {
      return null;
    }
    var point1 = endPoints[0];
    var point22 = endPoints[1];
    if (point1 == null || point22 == null) {
      return null;
    }
    var x1 = point1.x, y1 = point1.y;
    var x22 = point22.x, y22 = point22.y;
    var clipPath = ifOverflow === "hidden" ? "url(#".concat(clipPathId, ")") : void 0;
    var lineProps = _objectSpread30(_objectSpread30({
      clipPath
    }, svgPropertiesAndEvents(props)), {}, {
      x1,
      y1,
      x2: x22,
      y2: y22
    });
    var rect = rectWithCoords({
      x1,
      y1,
      x2: x22,
      y2: y22
    });
    return /* @__PURE__ */ React26.createElement(ZIndexLayer, {
      zIndex: props.zIndex
    }, /* @__PURE__ */ React26.createElement(Layer, {
      className: clsx("recharts-reference-line", className)
    }, renderLine(shape, lineProps), /* @__PURE__ */ React26.createElement(CartesianLabelContextProvider, _extends15({}, rect, {
      lowerWidth: rect.width,
      upperWidth: rect.width
    }), /* @__PURE__ */ React26.createElement(CartesianLabelFromLabelProp, {
      label: props.label
    }), props.children)));
  }
  var referenceLineDefaultProps = {
    ifOverflow: "discard",
    xAxisId: 0,
    yAxisId: 0,
    fill: "none",
    label: false,
    stroke: "#ccc",
    fillOpacity: 1,
    strokeWidth: 1,
    position: "middle",
    zIndex: DefaultZIndexes.line
  };
  function ReferenceLine(outsideProps) {
    var props = resolveDefaultProps(outsideProps, referenceLineDefaultProps);
    return /* @__PURE__ */ React26.createElement(React26.Fragment, null, /* @__PURE__ */ React26.createElement(ReportReferenceLine, {
      yAxisId: props.yAxisId,
      xAxisId: props.xAxisId,
      ifOverflow: props.ifOverflow,
      x: props.x,
      y: props.y,
      segment: props.segment
    }), /* @__PURE__ */ React26.createElement(ReferenceLineImpl, props));
  }
  ReferenceLine.displayName = "ReferenceLine";

  // node_modules/recharts/es6/cartesian/CartesianAxis.js
  var React27 = __toESM(require_react());
  var import_react42 = __toESM(require_react());

  // node_modules/recharts/es6/util/getEveryNth.js
  function getEveryNth(array, n) {
    if (n < 1) {
      return [];
    }
    if (n === 1) {
      return array;
    }
    var result = [];
    for (var i = 0; i < array.length; i += n) {
      var item = array[i];
      if (item !== void 0) {
        result.push(item);
      }
    }
    return result;
  }

  // node_modules/recharts/es6/util/TickUtils.js
  function getAngledTickWidth(contentSize, unitSize, angle) {
    var size = {
      width: contentSize.width + unitSize.width,
      height: contentSize.height + unitSize.height
    };
    return getAngledRectangleWidth(size, angle);
  }
  function getTickBoundaries(viewBox, sign2, sizeKey) {
    var isWidth = sizeKey === "width";
    var x5 = viewBox.x, y5 = viewBox.y, width = viewBox.width, height = viewBox.height;
    if (sign2 === 1) {
      return {
        start: isWidth ? x5 : y5,
        end: isWidth ? x5 + width : y5 + height
      };
    }
    return {
      start: isWidth ? x5 + width : y5 + height,
      end: isWidth ? x5 : y5
    };
  }
  function isVisible(sign2, tickPosition, getSize, start, end) {
    if (sign2 * tickPosition < sign2 * start || sign2 * tickPosition > sign2 * end) {
      return false;
    }
    var size = getSize();
    return sign2 * (tickPosition - sign2 * size / 2 - start) >= 0 && sign2 * (tickPosition + sign2 * size / 2 - end) <= 0;
  }
  function getNumberIntervalTicks(ticks, interval2) {
    return getEveryNth(ticks, interval2 + 1);
  }

  // node_modules/recharts/es6/cartesian/getEquidistantTicks.js
  function getEquidistantTicks(sign2, boundaries, getTickSize, ticks, minTickGap) {
    var result = (ticks || []).slice();
    var initialStart = boundaries.start, end = boundaries.end;
    var index2 = 0;
    var stepsize = 1;
    var start = initialStart;
    var _loop = function _loop2() {
      var entry = ticks === null || ticks === void 0 ? void 0 : ticks[index2];
      if (entry === void 0) {
        return {
          v: getEveryNth(ticks, stepsize)
        };
      }
      var i = index2;
      var size;
      var getSize = () => {
        if (size === void 0) {
          size = getTickSize(entry, i);
        }
        return size;
      };
      var tickCoord = entry.coordinate;
      var isShow = index2 === 0 || isVisible(sign2, tickCoord, getSize, start, end);
      if (!isShow) {
        index2 = 0;
        start = initialStart;
        stepsize += 1;
      }
      if (isShow) {
        start = tickCoord + sign2 * (getSize() / 2 + minTickGap);
        index2 += stepsize;
      }
    }, _ret;
    while (stepsize <= result.length) {
      _ret = _loop();
      if (_ret) return _ret.v;
    }
    return [];
  }
  function getEquidistantPreserveEndTicks(sign2, boundaries, getTickSize, ticks, minTickGap) {
    var result = (ticks || []).slice();
    var len = result.length;
    if (len === 0) {
      return [];
    }
    var initialStart = boundaries.start, end = boundaries.end;
    for (var stepsize = 1; stepsize <= len; stepsize++) {
      var offset = (len - 1) % stepsize;
      var start = initialStart;
      var ok = true;
      var _loop2 = function _loop22() {
        var entry = ticks[index2];
        if (entry == null) {
          return 0;
        }
        var i = index2;
        var size;
        var getSize = () => {
          if (size === void 0) {
            size = getTickSize(entry, i);
          }
          return size;
        };
        var tickCoord = entry.coordinate;
        var isShow = index2 === offset || isVisible(sign2, tickCoord, getSize, start, end);
        if (!isShow) {
          ok = false;
          return 1;
        }
        if (isShow) {
          start = tickCoord + sign2 * (getSize() / 2 + minTickGap);
        }
      }, _ret2;
      for (var index2 = offset; index2 < len; index2 += stepsize) {
        _ret2 = _loop2();
        if (_ret2 === 0) continue;
        if (_ret2 === 1) break;
      }
      if (ok) {
        var finalTicks = [];
        for (var _index = offset; _index < len; _index += stepsize) {
          var tick = ticks[_index];
          if (tick != null) {
            finalTicks.push(tick);
          }
        }
        return finalTicks;
      }
    }
    return [];
  }

  // node_modules/recharts/es6/cartesian/getTicks.js
  function ownKeys31(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread31(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys31(Object(t), true).forEach(function(r3) {
        _defineProperty34(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys31(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty34(e, r2, t) {
    return (r2 = _toPropertyKey34(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey34(t) {
    var i = _toPrimitive34(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive34(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  function getTicksEnd(sign2, boundaries, getTickSize, ticks, minTickGap) {
    var result = (ticks || []).slice();
    var len = result.length;
    var start = boundaries.start;
    var end = boundaries.end;
    var _loop = function _loop2(i2) {
      var initialEntry = result[i2];
      if (initialEntry == null) {
        return 1;
      }
      var entry = initialEntry;
      var size;
      var getSize = () => {
        if (size === void 0) {
          size = getTickSize(initialEntry, i2);
        }
        return size;
      };
      if (i2 === len - 1) {
        var gap = sign2 * (entry.coordinate + sign2 * getSize() / 2 - end);
        result[i2] = entry = _objectSpread31(_objectSpread31({}, entry), {}, {
          tickCoord: gap > 0 ? entry.coordinate - gap * sign2 : entry.coordinate
        });
      } else {
        result[i2] = entry = _objectSpread31(_objectSpread31({}, entry), {}, {
          tickCoord: entry.coordinate
        });
      }
      if (entry.tickCoord != null) {
        var isShow = isVisible(sign2, entry.tickCoord, getSize, start, end);
        if (isShow) {
          end = entry.tickCoord - sign2 * (getSize() / 2 + minTickGap);
          result[i2] = _objectSpread31(_objectSpread31({}, entry), {}, {
            isShow: true
          });
        }
      }
    };
    for (var i = len - 1; i >= 0; i--) {
      if (_loop(i)) continue;
    }
    return result;
  }
  function getTicksStart(sign2, boundaries, getTickSize, ticks, minTickGap, preserveEnd) {
    var result = (ticks || []).slice();
    var len = result.length;
    var start = boundaries.start, end = boundaries.end;
    if (preserveEnd) {
      var tail = ticks[len - 1];
      if (tail != null) {
        var tailSize = getTickSize(tail, len - 1);
        var tailGap = sign2 * (tail.coordinate + sign2 * tailSize / 2 - end);
        result[len - 1] = tail = _objectSpread31(_objectSpread31({}, tail), {}, {
          tickCoord: tailGap > 0 ? tail.coordinate - tailGap * sign2 : tail.coordinate
        });
        if (tail.tickCoord != null) {
          var isTailShow = isVisible(sign2, tail.tickCoord, () => tailSize, start, end);
          if (isTailShow) {
            end = tail.tickCoord - sign2 * (tailSize / 2 + minTickGap);
            result[len - 1] = _objectSpread31(_objectSpread31({}, tail), {}, {
              isShow: true
            });
          }
        }
      }
    }
    var count = preserveEnd ? len - 1 : len;
    var _loop2 = function _loop22(i2) {
      var initialEntry = result[i2];
      if (initialEntry == null) {
        return 1;
      }
      var entry = initialEntry;
      var size;
      var getSize = () => {
        if (size === void 0) {
          size = getTickSize(initialEntry, i2);
        }
        return size;
      };
      if (i2 === 0) {
        var gap = sign2 * (entry.coordinate - sign2 * getSize() / 2 - start);
        result[i2] = entry = _objectSpread31(_objectSpread31({}, entry), {}, {
          tickCoord: gap < 0 ? entry.coordinate - gap * sign2 : entry.coordinate
        });
      } else {
        result[i2] = entry = _objectSpread31(_objectSpread31({}, entry), {}, {
          tickCoord: entry.coordinate
        });
      }
      if (entry.tickCoord != null) {
        var isShow = isVisible(sign2, entry.tickCoord, getSize, start, end);
        if (isShow) {
          start = entry.tickCoord + sign2 * (getSize() / 2 + minTickGap);
          result[i2] = _objectSpread31(_objectSpread31({}, entry), {}, {
            isShow: true
          });
        }
      }
    };
    for (var i = 0; i < count; i++) {
      if (_loop2(i)) continue;
    }
    return result;
  }
  function getTicks(props, fontSize, letterSpacing) {
    var tick = props.tick, ticks = props.ticks, viewBox = props.viewBox, minTickGap = props.minTickGap, orientation = props.orientation, interval2 = props.interval, tickFormatter = props.tickFormatter, unit2 = props.unit, angle = props.angle;
    if (!ticks || !ticks.length || !tick) {
      return [];
    }
    if (isNumber(interval2) || Global.isSsr) {
      var _getNumberIntervalTic;
      return (_getNumberIntervalTic = getNumberIntervalTicks(ticks, isNumber(interval2) ? interval2 : 0)) !== null && _getNumberIntervalTic !== void 0 ? _getNumberIntervalTic : [];
    }
    var candidates = [];
    var sizeKey = orientation === "top" || orientation === "bottom" ? "width" : "height";
    var unitSize = unit2 && sizeKey === "width" ? getStringSize(unit2, {
      fontSize,
      letterSpacing
    }) : {
      width: 0,
      height: 0
    };
    var getTickSize = (content, index2) => {
      var value2 = typeof tickFormatter === "function" ? tickFormatter(content.value, index2) : content.value;
      return sizeKey === "width" ? getAngledTickWidth(getStringSize(value2, {
        fontSize,
        letterSpacing
      }), unitSize, angle) : getStringSize(value2, {
        fontSize,
        letterSpacing
      })[sizeKey];
    };
    var tick0 = ticks[0];
    var tick1 = ticks[1];
    var sign2 = ticks.length >= 2 && tick0 != null && tick1 != null ? mathSign(tick1.coordinate - tick0.coordinate) : 1;
    var boundaries = getTickBoundaries(viewBox, sign2, sizeKey);
    if (interval2 === "equidistantPreserveStart") {
      return getEquidistantTicks(sign2, boundaries, getTickSize, ticks, minTickGap);
    }
    if (interval2 === "equidistantPreserveEnd") {
      return getEquidistantPreserveEndTicks(sign2, boundaries, getTickSize, ticks, minTickGap);
    }
    if (interval2 === "preserveStart" || interval2 === "preserveStartEnd") {
      candidates = getTicksStart(sign2, boundaries, getTickSize, ticks, minTickGap, interval2 === "preserveStartEnd");
    } else {
      candidates = getTicksEnd(sign2, boundaries, getTickSize, ticks, minTickGap);
    }
    return candidates.filter((entry) => entry.isShow);
  }

  // node_modules/recharts/es6/util/YAxisUtils.js
  var getCalculatedYAxisWidth = (_ref2) => {
    var ticks = _ref2.ticks, label = _ref2.label, _ref$labelGapWithTick = _ref2.labelGapWithTick, labelGapWithTick = _ref$labelGapWithTick === void 0 ? 5 : _ref$labelGapWithTick, _ref$tickSize = _ref2.tickSize, tickSize = _ref$tickSize === void 0 ? 0 : _ref$tickSize, _ref$tickMargin = _ref2.tickMargin, tickMargin = _ref$tickMargin === void 0 ? 0 : _ref$tickMargin;
    var maxTickWidth = 0;
    if (ticks) {
      Array.from(ticks).forEach((tickNode) => {
        if (tickNode) {
          var bbox = tickNode.getBoundingClientRect();
          if (bbox.width > maxTickWidth) {
            maxTickWidth = bbox.width;
          }
        }
      });
      var labelWidth = label ? label.getBoundingClientRect().width : 0;
      var tickWidth = tickSize + tickMargin;
      var updatedYAxisWidth = maxTickWidth + tickWidth + labelWidth + (label ? labelGapWithTick : 0);
      return Math.round(updatedYAxisWidth);
    }
    return 0;
  };

  // node_modules/recharts/es6/state/renderedTicksSlice.js
  var initialState11 = {
    xAxis: {},
    yAxis: {}
  };
  var renderedTicksSlice = createSlice({
    name: "renderedTicks",
    initialState: initialState11,
    reducers: {
      setRenderedTicks: (state, action) => {
        var _action$payload = action.payload, axisType = _action$payload.axisType, axisId = _action$payload.axisId, ticks = _action$payload.ticks;
        state[axisType][axisId] = castDraft(ticks);
      },
      removeRenderedTicks: (state, action) => {
        var _action$payload2 = action.payload, axisType = _action$payload2.axisType, axisId = _action$payload2.axisId;
        delete state[axisType][axisId];
      }
    }
  });
  var _renderedTicksSlice$a = renderedTicksSlice.actions;
  var setRenderedTicks = _renderedTicksSlice$a.setRenderedTicks;
  var removeRenderedTicks = _renderedTicksSlice$a.removeRenderedTicks;
  var renderedTicksReducer = renderedTicksSlice.reducer;

  // node_modules/recharts/es6/cartesian/CartesianAxis.js
  var _excluded11 = ["axisLine", "width", "height", "className", "hide", "ticks", "axisType", "axisId"];
  function _slicedToArray18(r2, e) {
    return _arrayWithHoles18(r2) || _iterableToArrayLimit18(r2, e) || _unsupportedIterableToArray18(r2, e) || _nonIterableRest18();
  }
  function _nonIterableRest18() {
    throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  function _unsupportedIterableToArray18(r2, a2) {
    if (r2) {
      if ("string" == typeof r2) return _arrayLikeToArray18(r2, a2);
      var t = {}.toString.call(r2).slice(8, -1);
      return "Object" === t && r2.constructor && (t = r2.constructor.name), "Map" === t || "Set" === t ? Array.from(r2) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray18(r2, a2) : void 0;
    }
  }
  function _arrayLikeToArray18(r2, a2) {
    (null == a2 || a2 > r2.length) && (a2 = r2.length);
    for (var e = 0, n = Array(a2); e < a2; e++) n[e] = r2[e];
    return n;
  }
  function _iterableToArrayLimit18(r2, l) {
    var t = null == r2 ? null : "undefined" != typeof Symbol && r2[Symbol.iterator] || r2["@@iterator"];
    if (null != t) {
      var e, n, i, u, a2 = [], f = true, o = false;
      try {
        if (i = (t = t.call(r2)).next, 0 === l) {
          if (Object(t) !== t) return;
          f = false;
        } else for (; !(f = (e = i.call(t)).done) && (a2.push(e.value), a2.length !== l); f = true) ;
      } catch (r3) {
        o = true, n = r3;
      } finally {
        try {
          if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return;
        } finally {
          if (o) throw n;
        }
      }
      return a2;
    }
  }
  function _arrayWithHoles18(r2) {
    if (Array.isArray(r2)) return r2;
  }
  function _objectWithoutProperties11(e, t) {
    if (null == e) return {};
    var o, r2, i = _objectWithoutPropertiesLoose11(e, t);
    if (Object.getOwnPropertySymbols) {
      var n = Object.getOwnPropertySymbols(e);
      for (r2 = 0; r2 < n.length; r2++) o = n[r2], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
    }
    return i;
  }
  function _objectWithoutPropertiesLoose11(r2, e) {
    if (null == r2) return {};
    var t = {};
    for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
      if (-1 !== e.indexOf(n)) continue;
      t[n] = r2[n];
    }
    return t;
  }
  function _extends16() {
    return _extends16 = Object.assign ? Object.assign.bind() : function(n) {
      for (var e = 1; e < arguments.length; e++) {
        var t = arguments[e];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends16.apply(null, arguments);
  }
  function ownKeys32(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread32(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys32(Object(t), true).forEach(function(r3) {
        _defineProperty35(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys32(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty35(e, r2, t) {
    return (r2 = _toPropertyKey35(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey35(t) {
    var i = _toPrimitive35(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive35(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  var defaultCartesianAxisProps = {
    x: 0,
    y: 0,
    width: 0,
    height: 0,
    viewBox: {
      x: 0,
      y: 0,
      width: 0,
      height: 0
    },
    // The orientation of axis
    orientation: "bottom",
    // The ticks
    ticks: [],
    stroke: "#666",
    tickLine: true,
    axisLine: true,
    tick: true,
    mirror: false,
    minTickGap: 5,
    // The width or height of tick
    tickSize: 6,
    tickMargin: 2,
    interval: "preserveEnd",
    zIndex: DefaultZIndexes.axis
  };
  function AxisLine(axisLineProps) {
    var x5 = axisLineProps.x, y5 = axisLineProps.y, width = axisLineProps.width, height = axisLineProps.height, orientation = axisLineProps.orientation, mirror = axisLineProps.mirror, axisLine = axisLineProps.axisLine, otherSvgProps = axisLineProps.otherSvgProps;
    if (!axisLine) {
      return null;
    }
    var props = _objectSpread32(_objectSpread32(_objectSpread32({}, otherSvgProps), svgPropertiesNoEvents(axisLine)), {}, {
      fill: "none"
    });
    if (orientation === "top" || orientation === "bottom") {
      var needHeight = +(orientation === "top" && !mirror || orientation === "bottom" && mirror);
      props = _objectSpread32(_objectSpread32({}, props), {}, {
        x1: x5,
        y1: y5 + needHeight * height,
        x2: x5 + width,
        y2: y5 + needHeight * height
      });
    } else {
      var needWidth = +(orientation === "left" && !mirror || orientation === "right" && mirror);
      props = _objectSpread32(_objectSpread32({}, props), {}, {
        x1: x5 + needWidth * width,
        y1: y5,
        x2: x5 + needWidth * width,
        y2: y5 + height
      });
    }
    return /* @__PURE__ */ React27.createElement("line", _extends16({}, props, {
      className: clsx("recharts-cartesian-axis-line", get(axisLine, "className"))
    }));
  }
  function getTickLineCoord(data, x5, y5, width, height, orientation, tickSize, mirror, tickMargin) {
    var x1, x22, y1, y22, tx, ty;
    var sign2 = mirror ? -1 : 1;
    var finalTickSize = data.tickSize || tickSize;
    var tickCoord = isNumber(data.tickCoord) ? data.tickCoord : data.coordinate;
    switch (orientation) {
      case "top":
        x1 = x22 = data.coordinate;
        y22 = y5 + +!mirror * height;
        y1 = y22 - sign2 * finalTickSize;
        ty = y1 - sign2 * tickMargin;
        tx = tickCoord;
        break;
      case "left":
        y1 = y22 = data.coordinate;
        x22 = x5 + +!mirror * width;
        x1 = x22 - sign2 * finalTickSize;
        tx = x1 - sign2 * tickMargin;
        ty = tickCoord;
        break;
      case "right":
        y1 = y22 = data.coordinate;
        x22 = x5 + +mirror * width;
        x1 = x22 + sign2 * finalTickSize;
        tx = x1 + sign2 * tickMargin;
        ty = tickCoord;
        break;
      default:
        x1 = x22 = data.coordinate;
        y22 = y5 + +mirror * height;
        y1 = y22 + sign2 * finalTickSize;
        ty = y1 + sign2 * tickMargin;
        tx = tickCoord;
        break;
    }
    return {
      line: {
        x1,
        y1,
        x2: x22,
        y2: y22
      },
      tick: {
        x: tx,
        y: ty
      }
    };
  }
  function getTickTextAnchor(orientation, mirror) {
    switch (orientation) {
      case "left":
        return mirror ? "start" : "end";
      case "right":
        return mirror ? "end" : "start";
      default:
        return "middle";
    }
  }
  function getTickVerticalAnchor(orientation, mirror) {
    switch (orientation) {
      case "left":
      case "right":
        return "middle";
      case "top":
        return mirror ? "start" : "end";
      default:
        return mirror ? "end" : "start";
    }
  }
  function TickItem(props) {
    var option = props.option, tickProps = props.tickProps, value2 = props.value;
    var tickItem;
    var combinedClassName = clsx(tickProps.className, "recharts-cartesian-axis-tick-value");
    if (/* @__PURE__ */ React27.isValidElement(option)) {
      tickItem = /* @__PURE__ */ React27.cloneElement(option, _objectSpread32(_objectSpread32({}, tickProps), {}, {
        className: combinedClassName
      }));
    } else if (typeof option === "function") {
      tickItem = option(_objectSpread32(_objectSpread32({}, tickProps), {}, {
        className: combinedClassName
      }));
    } else {
      var className = "recharts-cartesian-axis-tick-value";
      if (typeof option !== "boolean") {
        className = clsx(className, getClassNameFromUnknown(option));
      }
      tickItem = /* @__PURE__ */ React27.createElement(Text, _extends16({}, tickProps, {
        className
      }), value2);
    }
    return tickItem;
  }
  function RenderedTicksReporter(_ref2) {
    var ticks = _ref2.ticks, axisType = _ref2.axisType, axisId = _ref2.axisId;
    var dispatch2 = useAppDispatch();
    (0, import_react42.useEffect)(() => {
      if (axisId == null || axisType == null) {
        return noop;
      }
      var tickItems = ticks.map((tick) => ({
        value: tick.value,
        coordinate: tick.coordinate,
        offset: tick.offset,
        index: tick.index
      }));
      dispatch2(setRenderedTicks({
        ticks: tickItems,
        axisId,
        axisType
      }));
      return () => {
        dispatch2(removeRenderedTicks({
          axisId,
          axisType
        }));
      };
    }, [dispatch2, ticks, axisId, axisType]);
    return null;
  }
  var Ticks = /* @__PURE__ */ (0, import_react42.forwardRef)((props, ref) => {
    var _props$ticks = props.ticks, ticks = _props$ticks === void 0 ? [] : _props$ticks, tick = props.tick, tickLine = props.tickLine, stroke = props.stroke, tickFormatter = props.tickFormatter, unit2 = props.unit, padding = props.padding, tickTextProps = props.tickTextProps, orientation = props.orientation, mirror = props.mirror, x5 = props.x, y5 = props.y, width = props.width, height = props.height, tickSize = props.tickSize, tickMargin = props.tickMargin, fontSize = props.fontSize, letterSpacing = props.letterSpacing, getTicksConfig = props.getTicksConfig, events = props.events, axisType = props.axisType, axisId = props.axisId;
    var finalTicks = getTicks(_objectSpread32(_objectSpread32({}, getTicksConfig), {}, {
      ticks
    }), fontSize, letterSpacing);
    var axisProps = svgPropertiesNoEvents(getTicksConfig);
    var customTickProps = svgPropertiesNoEventsFromUnknown(tick);
    var textAnchor = isValidTextAnchor(axisProps.textAnchor) ? axisProps.textAnchor : getTickTextAnchor(orientation, mirror);
    var verticalAnchor = getTickVerticalAnchor(orientation, mirror);
    var tickLinePropsObject = {};
    if (typeof tickLine === "object") {
      tickLinePropsObject = tickLine;
    }
    var tickLineProps = _objectSpread32(_objectSpread32({}, axisProps), {}, {
      fill: "none"
    }, tickLinePropsObject);
    var tickLineCoords = finalTicks.map((entry) => _objectSpread32({
      entry
    }, getTickLineCoord(entry, x5, y5, width, height, orientation, tickSize, mirror, tickMargin)));
    var tickLines = tickLineCoords.map((_ref2) => {
      var entry = _ref2.entry, lineCoord = _ref2.line;
      return /* @__PURE__ */ React27.createElement(Layer, {
        className: "recharts-cartesian-axis-tick",
        key: "tick-".concat(entry.value, "-").concat(entry.coordinate, "-").concat(entry.tickCoord)
      }, tickLine && /* @__PURE__ */ React27.createElement("line", _extends16({}, tickLineProps, lineCoord, {
        className: clsx("recharts-cartesian-axis-tick-line", get(tickLine, "className"))
      })));
    });
    var tickLabels = tickLineCoords.map((_ref3, i) => {
      var _ref4, _tickTextProps$angle;
      var entry = _ref3.entry, tickCoord = _ref3.tick;
      var tickProps = _objectSpread32(_objectSpread32(_objectSpread32(_objectSpread32({
        verticalAnchor
      }, axisProps), {}, {
        textAnchor,
        stroke: "none",
        fill: stroke
      }, tickCoord), {}, {
        index: i,
        payload: entry,
        visibleTicksCount: finalTicks.length,
        tickFormatter,
        padding
      }, tickTextProps), {}, {
        angle: (_ref4 = (_tickTextProps$angle = tickTextProps === null || tickTextProps === void 0 ? void 0 : tickTextProps.angle) !== null && _tickTextProps$angle !== void 0 ? _tickTextProps$angle : axisProps.angle) !== null && _ref4 !== void 0 ? _ref4 : 0
      });
      var finalTickProps = _objectSpread32(_objectSpread32({}, tickProps), customTickProps);
      return /* @__PURE__ */ React27.createElement(Layer, _extends16({
        className: "recharts-cartesian-axis-tick-label",
        key: "tick-label-".concat(entry.value, "-").concat(entry.coordinate, "-").concat(entry.tickCoord)
      }, adaptEventsOfChild(events, entry, i)), tick && /* @__PURE__ */ React27.createElement(TickItem, {
        option: tick,
        tickProps: finalTickProps,
        value: "".concat(typeof tickFormatter === "function" ? tickFormatter(entry.value, i) : entry.value).concat(unit2 || "")
      }));
    });
    return /* @__PURE__ */ React27.createElement("g", {
      className: "recharts-cartesian-axis-ticks recharts-".concat(axisType, "-ticks")
    }, /* @__PURE__ */ React27.createElement(RenderedTicksReporter, {
      ticks: finalTicks,
      axisId,
      axisType
    }), tickLabels.length > 0 && /* @__PURE__ */ React27.createElement(ZIndexLayer, {
      zIndex: DefaultZIndexes.label
    }, /* @__PURE__ */ React27.createElement("g", {
      className: "recharts-cartesian-axis-tick-labels recharts-".concat(axisType, "-tick-labels"),
      ref
    }, tickLabels)), tickLines.length > 0 && /* @__PURE__ */ React27.createElement("g", {
      className: "recharts-cartesian-axis-tick-lines recharts-".concat(axisType, "-tick-lines")
    }, tickLines));
  });
  var CartesianAxisComponent = /* @__PURE__ */ (0, import_react42.forwardRef)((props, ref) => {
    var axisLine = props.axisLine, width = props.width, height = props.height, className = props.className, hide = props.hide, ticks = props.ticks, axisType = props.axisType, axisId = props.axisId, rest = _objectWithoutProperties11(props, _excluded11);
    var _useState = (0, import_react42.useState)(""), _useState2 = _slicedToArray18(_useState, 2), fontSize = _useState2[0], setFontSize = _useState2[1];
    var _useState3 = (0, import_react42.useState)(""), _useState4 = _slicedToArray18(_useState3, 2), letterSpacing = _useState4[0], setLetterSpacing = _useState4[1];
    var tickRefs = (0, import_react42.useRef)(null);
    (0, import_react42.useImperativeHandle)(ref, () => ({
      getCalculatedWidth: () => {
        var _props$labelRef;
        return getCalculatedYAxisWidth({
          ticks: tickRefs.current,
          label: (_props$labelRef = props.labelRef) === null || _props$labelRef === void 0 ? void 0 : _props$labelRef.current,
          labelGapWithTick: 5,
          tickSize: props.tickSize,
          tickMargin: props.tickMargin
        });
      }
    }));
    var layerRef = (0, import_react42.useCallback)((el) => {
      if (el) {
        var tickNodes = el.getElementsByClassName("recharts-cartesian-axis-tick-value");
        tickRefs.current = tickNodes;
        var tick = tickNodes[0];
        if (tick) {
          var computedStyle = window.getComputedStyle(tick);
          var calculatedFontSize = computedStyle.fontSize;
          var calculatedLetterSpacing = computedStyle.letterSpacing;
          if (calculatedFontSize !== fontSize || calculatedLetterSpacing !== letterSpacing) {
            setFontSize(calculatedFontSize);
            setLetterSpacing(calculatedLetterSpacing);
          }
        }
      }
    }, [fontSize, letterSpacing]);
    if (hide) {
      return null;
    }
    if (width != null && width <= 0 || height != null && height <= 0) {
      return null;
    }
    return /* @__PURE__ */ React27.createElement(ZIndexLayer, {
      zIndex: props.zIndex
    }, /* @__PURE__ */ React27.createElement(Layer, {
      className: clsx("recharts-cartesian-axis", className)
    }, /* @__PURE__ */ React27.createElement(AxisLine, {
      x: props.x,
      y: props.y,
      width,
      height,
      orientation: props.orientation,
      mirror: props.mirror,
      axisLine,
      otherSvgProps: svgPropertiesNoEvents(props)
    }), /* @__PURE__ */ React27.createElement(Ticks, {
      ref: layerRef,
      axisType,
      events: rest,
      fontSize,
      getTicksConfig: props,
      height: props.height,
      letterSpacing,
      mirror: props.mirror,
      orientation: props.orientation,
      padding: props.padding,
      stroke: props.stroke,
      tick: props.tick,
      tickFormatter: props.tickFormatter,
      tickLine: props.tickLine,
      tickMargin: props.tickMargin,
      tickSize: props.tickSize,
      tickTextProps: props.tickTextProps,
      ticks,
      unit: props.unit,
      width: props.width,
      x: props.x,
      y: props.y,
      axisId
    }), /* @__PURE__ */ React27.createElement(CartesianLabelContextProvider, {
      x: props.x,
      y: props.y,
      width: props.width,
      height: props.height,
      lowerWidth: props.width,
      upperWidth: props.width
    }, /* @__PURE__ */ React27.createElement(CartesianLabelFromLabelProp, {
      label: props.label,
      labelRef: props.labelRef
    }), props.children)));
  });
  var CartesianAxis = /* @__PURE__ */ React27.forwardRef((outsideProps, ref) => {
    var props = resolveDefaultProps(outsideProps, defaultCartesianAxisProps);
    return /* @__PURE__ */ React27.createElement(CartesianAxisComponent, _extends16({}, props, {
      ref
    }));
  });
  CartesianAxis.displayName = "CartesianAxis";

  // node_modules/recharts/es6/cartesian/CartesianGrid.js
  var React28 = __toESM(require_react());
  var _excluded12 = ["x1", "y1", "x2", "y2", "key"];
  var _excluded27 = ["offset"];
  var _excluded33 = ["xAxisId", "yAxisId"];
  var _excluded43 = ["xAxisId", "yAxisId"];
  function ownKeys33(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread33(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys33(Object(t), true).forEach(function(r3) {
        _defineProperty36(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys33(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty36(e, r2, t) {
    return (r2 = _toPropertyKey36(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey36(t) {
    var i = _toPrimitive36(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive36(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  function _extends17() {
    return _extends17 = Object.assign ? Object.assign.bind() : function(n) {
      for (var e = 1; e < arguments.length; e++) {
        var t = arguments[e];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends17.apply(null, arguments);
  }
  function _objectWithoutProperties12(e, t) {
    if (null == e) return {};
    var o, r2, i = _objectWithoutPropertiesLoose12(e, t);
    if (Object.getOwnPropertySymbols) {
      var n = Object.getOwnPropertySymbols(e);
      for (r2 = 0; r2 < n.length; r2++) o = n[r2], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
    }
    return i;
  }
  function _objectWithoutPropertiesLoose12(r2, e) {
    if (null == r2) return {};
    var t = {};
    for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
      if (-1 !== e.indexOf(n)) continue;
      t[n] = r2[n];
    }
    return t;
  }
  var Background = (props) => {
    var fill = props.fill;
    if (!fill || fill === "none") {
      return null;
    }
    var fillOpacity = props.fillOpacity, x5 = props.x, y5 = props.y, width = props.width, height = props.height, ry = props.ry;
    return /* @__PURE__ */ React28.createElement("rect", {
      x: x5,
      y: y5,
      ry,
      width,
      height,
      stroke: "none",
      fill,
      fillOpacity,
      className: "recharts-cartesian-grid-bg"
    });
  };
  function LineItem(_ref2) {
    var option = _ref2.option, lineItemProps = _ref2.lineItemProps;
    var lineItem;
    if (/* @__PURE__ */ React28.isValidElement(option)) {
      lineItem = /* @__PURE__ */ React28.cloneElement(option, lineItemProps);
    } else if (typeof option === "function") {
      lineItem = option(lineItemProps);
    } else {
      var _svgPropertiesNoEvent;
      var x1 = lineItemProps.x1, y1 = lineItemProps.y1, x22 = lineItemProps.x2, y22 = lineItemProps.y2, key = lineItemProps.key, others = _objectWithoutProperties12(lineItemProps, _excluded12);
      var _ref22 = (_svgPropertiesNoEvent = svgPropertiesNoEvents(others)) !== null && _svgPropertiesNoEvent !== void 0 ? _svgPropertiesNoEvent : {}, __ = _ref22.offset, restOfFilteredProps = _objectWithoutProperties12(_ref22, _excluded27);
      lineItem = /* @__PURE__ */ React28.createElement("line", _extends17({}, restOfFilteredProps, {
        x1,
        y1,
        x2: x22,
        y2: y22,
        fill: "none",
        key
      }));
    }
    return lineItem;
  }
  function HorizontalGridLines(props) {
    var x5 = props.x, width = props.width, _props$horizontal = props.horizontal, horizontal = _props$horizontal === void 0 ? true : _props$horizontal, horizontalPoints = props.horizontalPoints;
    if (!horizontal || !horizontalPoints || !horizontalPoints.length) {
      return null;
    }
    var xAxisId = props.xAxisId, yAxisId = props.yAxisId, otherLineItemProps = _objectWithoutProperties12(props, _excluded33);
    var items = horizontalPoints.map((entry, i) => {
      var lineItemProps = _objectSpread33(_objectSpread33({}, otherLineItemProps), {}, {
        x1: x5,
        y1: entry,
        x2: x5 + width,
        y2: entry,
        key: "line-".concat(i),
        index: i
      });
      return /* @__PURE__ */ React28.createElement(LineItem, {
        key: "line-".concat(i),
        option: horizontal,
        lineItemProps
      });
    });
    return /* @__PURE__ */ React28.createElement("g", {
      className: "recharts-cartesian-grid-horizontal"
    }, items);
  }
  function VerticalGridLines(props) {
    var y5 = props.y, height = props.height, _props$vertical = props.vertical, vertical = _props$vertical === void 0 ? true : _props$vertical, verticalPoints = props.verticalPoints;
    if (!vertical || !verticalPoints || !verticalPoints.length) {
      return null;
    }
    var xAxisId = props.xAxisId, yAxisId = props.yAxisId, otherLineItemProps = _objectWithoutProperties12(props, _excluded43);
    var items = verticalPoints.map((entry, i) => {
      var lineItemProps = _objectSpread33(_objectSpread33({}, otherLineItemProps), {}, {
        x1: entry,
        y1: y5,
        x2: entry,
        y2: y5 + height,
        key: "line-".concat(i),
        index: i
      });
      return /* @__PURE__ */ React28.createElement(LineItem, {
        option: vertical,
        lineItemProps,
        key: "line-".concat(i)
      });
    });
    return /* @__PURE__ */ React28.createElement("g", {
      className: "recharts-cartesian-grid-vertical"
    }, items);
  }
  function HorizontalStripes(props) {
    var horizontalFill = props.horizontalFill, fillOpacity = props.fillOpacity, x5 = props.x, y5 = props.y, width = props.width, height = props.height, horizontalPoints = props.horizontalPoints, _props$horizontal2 = props.horizontal, horizontal = _props$horizontal2 === void 0 ? true : _props$horizontal2;
    if (!horizontal || !horizontalFill || !horizontalFill.length || horizontalPoints == null) {
      return null;
    }
    var roundedSortedHorizontalPoints = horizontalPoints.map((e) => Math.round(e + y5 - y5)).sort((a2, b) => a2 - b);
    if (y5 !== roundedSortedHorizontalPoints[0]) {
      roundedSortedHorizontalPoints.unshift(0);
    }
    var items = roundedSortedHorizontalPoints.map((entry, i) => {
      var nextPoint = roundedSortedHorizontalPoints[i + 1];
      var lastStripe = nextPoint == null;
      var lineHeight = lastStripe ? y5 + height - entry : nextPoint - entry;
      if (lineHeight <= 0) {
        return null;
      }
      var colorIndex = i % horizontalFill.length;
      return /* @__PURE__ */ React28.createElement("rect", {
        key: "react-".concat(i),
        y: entry,
        x: x5,
        height: lineHeight,
        width,
        stroke: "none",
        fill: horizontalFill[colorIndex],
        fillOpacity,
        className: "recharts-cartesian-grid-bg"
      });
    });
    return /* @__PURE__ */ React28.createElement("g", {
      className: "recharts-cartesian-gridstripes-horizontal"
    }, items);
  }
  function VerticalStripes(props) {
    var _props$vertical2 = props.vertical, vertical = _props$vertical2 === void 0 ? true : _props$vertical2, verticalFill = props.verticalFill, fillOpacity = props.fillOpacity, x5 = props.x, y5 = props.y, width = props.width, height = props.height, verticalPoints = props.verticalPoints;
    if (!vertical || !verticalFill || !verticalFill.length) {
      return null;
    }
    var roundedSortedVerticalPoints = verticalPoints.map((e) => Math.round(e + x5 - x5)).sort((a2, b) => a2 - b);
    if (x5 !== roundedSortedVerticalPoints[0]) {
      roundedSortedVerticalPoints.unshift(0);
    }
    var items = roundedSortedVerticalPoints.map((entry, i) => {
      var nextPoint = roundedSortedVerticalPoints[i + 1];
      var lastStripe = nextPoint == null;
      var lineWidth = lastStripe ? x5 + width - entry : nextPoint - entry;
      if (lineWidth <= 0) {
        return null;
      }
      var colorIndex = i % verticalFill.length;
      return /* @__PURE__ */ React28.createElement("rect", {
        key: "react-".concat(i),
        x: entry,
        y: y5,
        width: lineWidth,
        height,
        stroke: "none",
        fill: verticalFill[colorIndex],
        fillOpacity,
        className: "recharts-cartesian-grid-bg"
      });
    });
    return /* @__PURE__ */ React28.createElement("g", {
      className: "recharts-cartesian-gridstripes-vertical"
    }, items);
  }
  var defaultVerticalCoordinatesGenerator = (_ref3, syncWithTicks) => {
    var xAxis = _ref3.xAxis, width = _ref3.width, height = _ref3.height, offset = _ref3.offset;
    return getCoordinatesOfGrid(getTicks(_objectSpread33(_objectSpread33(_objectSpread33({}, defaultCartesianAxisProps), xAxis), {}, {
      ticks: getTicksOfAxis(xAxis, true),
      viewBox: {
        x: 0,
        y: 0,
        width,
        height
      }
    })), offset.left, offset.left + offset.width, syncWithTicks);
  };
  var defaultHorizontalCoordinatesGenerator = (_ref4, syncWithTicks) => {
    var yAxis = _ref4.yAxis, width = _ref4.width, height = _ref4.height, offset = _ref4.offset;
    return getCoordinatesOfGrid(getTicks(_objectSpread33(_objectSpread33(_objectSpread33({}, defaultCartesianAxisProps), yAxis), {}, {
      ticks: getTicksOfAxis(yAxis, true),
      viewBox: {
        x: 0,
        y: 0,
        width,
        height
      }
    })), offset.top, offset.top + offset.height, syncWithTicks);
  };
  var defaultCartesianGridProps = {
    horizontal: true,
    vertical: true,
    // The ordinates of horizontal grid lines
    horizontalPoints: [],
    // The abscissas of vertical grid lines
    verticalPoints: [],
    stroke: "#ccc",
    fill: "none",
    // The fill of colors of grid lines
    verticalFill: [],
    horizontalFill: [],
    xAxisId: 0,
    yAxisId: 0,
    syncWithTicks: false,
    zIndex: DefaultZIndexes.grid
  };
  function CartesianGrid(props) {
    var chartWidth = useChartWidth();
    var chartHeight = useChartHeight();
    var offset = useOffsetInternal();
    var propsIncludingDefaults = _objectSpread33(_objectSpread33({}, resolveDefaultProps(props, defaultCartesianGridProps)), {}, {
      x: isNumber(props.x) ? props.x : offset.left,
      y: isNumber(props.y) ? props.y : offset.top,
      width: isNumber(props.width) ? props.width : offset.width,
      height: isNumber(props.height) ? props.height : offset.height
    });
    var xAxisId = propsIncludingDefaults.xAxisId, yAxisId = propsIncludingDefaults.yAxisId, x5 = propsIncludingDefaults.x, y5 = propsIncludingDefaults.y, width = propsIncludingDefaults.width, height = propsIncludingDefaults.height, syncWithTicks = propsIncludingDefaults.syncWithTicks, horizontalValues = propsIncludingDefaults.horizontalValues, verticalValues = propsIncludingDefaults.verticalValues;
    var isPanorama = useIsPanorama();
    var xAxis = useAppSelector((state) => selectAxisPropsNeededForCartesianGridTicksGenerator(state, "xAxis", xAxisId, isPanorama));
    var yAxis = useAppSelector((state) => selectAxisPropsNeededForCartesianGridTicksGenerator(state, "yAxis", yAxisId, isPanorama));
    if (!isPositiveNumber(width) || !isPositiveNumber(height) || !isNumber(x5) || !isNumber(y5)) {
      return null;
    }
    var verticalCoordinatesGenerator = propsIncludingDefaults.verticalCoordinatesGenerator || defaultVerticalCoordinatesGenerator;
    var horizontalCoordinatesGenerator = propsIncludingDefaults.horizontalCoordinatesGenerator || defaultHorizontalCoordinatesGenerator;
    var horizontalPoints = propsIncludingDefaults.horizontalPoints, verticalPoints = propsIncludingDefaults.verticalPoints;
    if ((!horizontalPoints || !horizontalPoints.length) && typeof horizontalCoordinatesGenerator === "function") {
      var isHorizontalValues = horizontalValues && horizontalValues.length;
      var generatorResult = horizontalCoordinatesGenerator({
        yAxis: yAxis ? _objectSpread33(_objectSpread33({}, yAxis), {}, {
          ticks: isHorizontalValues ? horizontalValues : yAxis.ticks
        }) : void 0,
        width: chartWidth !== null && chartWidth !== void 0 ? chartWidth : width,
        height: chartHeight !== null && chartHeight !== void 0 ? chartHeight : height,
        offset
      }, isHorizontalValues ? true : syncWithTicks);
      warn2(Array.isArray(generatorResult), "horizontalCoordinatesGenerator should return Array but instead it returned [".concat(typeof generatorResult, "]"));
      if (Array.isArray(generatorResult)) {
        horizontalPoints = generatorResult;
      }
    }
    if ((!verticalPoints || !verticalPoints.length) && typeof verticalCoordinatesGenerator === "function") {
      var isVerticalValues = verticalValues && verticalValues.length;
      var _generatorResult = verticalCoordinatesGenerator({
        xAxis: xAxis ? _objectSpread33(_objectSpread33({}, xAxis), {}, {
          ticks: isVerticalValues ? verticalValues : xAxis.ticks
        }) : void 0,
        width: chartWidth !== null && chartWidth !== void 0 ? chartWidth : width,
        height: chartHeight !== null && chartHeight !== void 0 ? chartHeight : height,
        offset
      }, isVerticalValues ? true : syncWithTicks);
      warn2(Array.isArray(_generatorResult), "verticalCoordinatesGenerator should return Array but instead it returned [".concat(typeof _generatorResult, "]"));
      if (Array.isArray(_generatorResult)) {
        verticalPoints = _generatorResult;
      }
    }
    return /* @__PURE__ */ React28.createElement(ZIndexLayer, {
      zIndex: propsIncludingDefaults.zIndex
    }, /* @__PURE__ */ React28.createElement("g", {
      className: "recharts-cartesian-grid"
    }, /* @__PURE__ */ React28.createElement(Background, {
      fill: propsIncludingDefaults.fill,
      fillOpacity: propsIncludingDefaults.fillOpacity,
      x: propsIncludingDefaults.x,
      y: propsIncludingDefaults.y,
      width: propsIncludingDefaults.width,
      height: propsIncludingDefaults.height,
      ry: propsIncludingDefaults.ry
    }), /* @__PURE__ */ React28.createElement(HorizontalStripes, _extends17({}, propsIncludingDefaults, {
      horizontalPoints
    })), /* @__PURE__ */ React28.createElement(VerticalStripes, _extends17({}, propsIncludingDefaults, {
      verticalPoints
    })), /* @__PURE__ */ React28.createElement(HorizontalGridLines, _extends17({}, propsIncludingDefaults, {
      offset,
      horizontalPoints,
      xAxis,
      yAxis
    })), /* @__PURE__ */ React28.createElement(VerticalGridLines, _extends17({}, propsIncludingDefaults, {
      offset,
      verticalPoints,
      xAxis,
      yAxis
    }))));
  }
  CartesianGrid.displayName = "CartesianGrid";

  // node_modules/recharts/es6/cartesian/Line.js
  var React32 = __toESM(require_react());
  var import_react45 = __toESM(require_react());

  // node_modules/recharts/es6/cartesian/LineDrawShape.js
  var React29 = __toESM(require_react());
  var _excluded13 = ["animationElapsedTime", "isAnimating", "isEntrance", "visibleLength", "strokeDasharray", "connectNulls"];
  function _extends18() {
    return _extends18 = Object.assign ? Object.assign.bind() : function(n) {
      for (var e = 1; e < arguments.length; e++) {
        var t = arguments[e];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends18.apply(null, arguments);
  }
  function _objectWithoutProperties13(e, t) {
    if (null == e) return {};
    var o, r2, i = _objectWithoutPropertiesLoose13(e, t);
    if (Object.getOwnPropertySymbols) {
      var n = Object.getOwnPropertySymbols(e);
      for (r2 = 0; r2 < n.length; r2++) o = n[r2], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
    }
    return i;
  }
  function _objectWithoutPropertiesLoose13(r2, e) {
    if (null == r2) return {};
    var t = {};
    for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
      if (-1 !== e.indexOf(n)) continue;
      t[n] = r2[n];
    }
    return t;
  }
  function getTotalLength(path3) {
    try {
      return path3 && path3.getTotalLength && path3.getTotalLength() || 0;
    } catch (_unused) {
      return 0;
    }
  }
  function generateSimpleStrokeDasharray(totalLength, length) {
    return "".concat(length, "px ").concat(totalLength, "px");
  }
  function normalizeDashPattern(lines) {
    return lines.length % 2 !== 0 ? [...lines, ...lines] : lines;
  }
  function repeat(lines, count) {
    var result = [];
    for (var i = 0; i < count; ++i) {
      result.push(...lines);
    }
    return result;
  }
  function getStrokeDasharray(length, totalLength, lines) {
    var normalizedLines = normalizeDashPattern(lines);
    var lineLength = normalizedLines.reduce((pre, next) => pre + next, 0);
    if (!lineLength) {
      return generateSimpleStrokeDasharray(totalLength, length);
    }
    var count = Math.floor(length / lineLength);
    var remainLength = length % lineLength;
    var remainLines = [];
    for (var i = 0, sum3 = 0; i < normalizedLines.length; sum3 += (_normalizedLines$i = normalizedLines[i]) !== null && _normalizedLines$i !== void 0 ? _normalizedLines$i : 0, ++i) {
      var _normalizedLines$i;
      var lineValue = normalizedLines[i];
      if (lineValue != null && sum3 + lineValue > remainLength) {
        remainLines = [...normalizedLines.slice(0, i), remainLength - sum3];
        break;
      }
    }
    var emptyLines = remainLines.length % 2 === 0 ? [0, totalLength] : [totalLength];
    return [...repeat(normalizedLines, count), ...remainLines, ...emptyLines].map((line) => "".concat(line, "px")).join(", ");
  }
  function computeAnimatedStrokeDasharray(userStrokeDasharray, totalLength, visibleLength) {
    if (userStrokeDasharray) {
      var lines = "".concat(userStrokeDasharray).split(/[,\s]+/gim).map((num) => parseFloat(num));
      return getStrokeDasharray(visibleLength, totalLength, lines);
    }
    return generateSimpleStrokeDasharray(totalLength, visibleLength);
  }
  function LineDrawShape(props) {
    var _animationElapsedTime = props.animationElapsedTime, isAnimating = props.isAnimating, isEntrance = props.isEntrance, visibleLength = props.visibleLength, userStrokeDasharray = props.strokeDasharray, connectNulls = props.connectNulls, curveProps = _objectWithoutProperties13(props, _excluded13);
    var finalConnectNulls = connectNulls !== null && connectNulls !== void 0 ? connectNulls : false;
    var strokeDasharray;
    if (visibleLength != null) {
      var _pathRef$current;
      var pathRef = curveProps.pathRef;
      var totalLength = getTotalLength((_pathRef$current = pathRef === null || pathRef === void 0 ? void 0 : pathRef.current) !== null && _pathRef$current !== void 0 ? _pathRef$current : null);
      strokeDasharray = computeAnimatedStrokeDasharray(userStrokeDasharray, totalLength, visibleLength);
    } else if (userStrokeDasharray != null) {
      strokeDasharray = String(userStrokeDasharray);
    }
    return /* @__PURE__ */ React29.createElement(Curve, _extends18({}, curveProps, {
      connectNulls: finalConnectNulls,
      strokeDasharray
    }));
  }

  // node_modules/recharts/es6/cartesian/useAnimatedLineLength.js
  var import_react43 = __toESM(require_react());
  function useAnimatedLineLength(points) {
    var startingLengthRef = (0, import_react43.useRef)(0);
    var maxAnimatedLengthRef = (0, import_react43.useRef)(0);
    var reachedFullRef = (0, import_react43.useRef)(false);
    var prevPointsRef = (0, import_react43.useRef)(points);
    if (prevPointsRef.current !== points) {
      startingLengthRef.current = maxAnimatedLengthRef.current;
      prevPointsRef.current = points;
    }
    return (0, import_react43.useCallback)((animationElapsedTime, totalLength) => {
      if (reachedFullRef.current) {
        return null;
      }
      var visibleLength = Math.min(round(startingLengthRef.current + animationElapsedTime * totalLength), totalLength);
      if (animationElapsedTime > 0 && totalLength > 0) {
        maxAnimatedLengthRef.current = Math.max(maxAnimatedLengthRef.current, visibleLength);
        if (visibleLength >= totalLength) {
          reachedFullRef.current = true;
          return null;
        }
      }
      return visibleLength;
    }, []);
  }

  // node_modules/recharts/es6/context/ErrorBarContext.js
  var React30 = __toESM(require_react());
  var import_react44 = __toESM(require_react());

  // node_modules/recharts/es6/state/errorBarSlice.js
  var initialState12 = {};
  var errorBarSlice = createSlice({
    name: "errorBars",
    initialState: initialState12,
    reducers: {
      addErrorBar: (state, action) => {
        var _action$payload = action.payload, itemId = _action$payload.itemId, errorBar = _action$payload.errorBar;
        if (!state[itemId]) {
          state[itemId] = [];
        }
        state[itemId].push(errorBar);
      },
      replaceErrorBar: (state, action) => {
        var _action$payload2 = action.payload, itemId = _action$payload2.itemId, prev = _action$payload2.prev, next = _action$payload2.next;
        if (state[itemId]) {
          state[itemId] = state[itemId].map((e) => e.dataKey === prev.dataKey && e.direction === prev.direction ? next : e);
        }
      },
      removeErrorBar: (state, action) => {
        var _action$payload3 = action.payload, itemId = _action$payload3.itemId, errorBar = _action$payload3.errorBar;
        if (state[itemId]) {
          state[itemId] = state[itemId].filter((e) => e.dataKey !== errorBar.dataKey || e.direction !== errorBar.direction);
        }
      }
    }
  });
  var _errorBarSlice$action = errorBarSlice.actions;
  var addErrorBar = _errorBarSlice$action.addErrorBar;
  var replaceErrorBar = _errorBarSlice$action.replaceErrorBar;
  var removeErrorBar = _errorBarSlice$action.removeErrorBar;
  var errorBarReducer = errorBarSlice.reducer;

  // node_modules/recharts/es6/context/ErrorBarContext.js
  var _excluded14 = ["children"];
  function _objectWithoutProperties14(e, t) {
    if (null == e) return {};
    var o, r2, i = _objectWithoutPropertiesLoose14(e, t);
    if (Object.getOwnPropertySymbols) {
      var n = Object.getOwnPropertySymbols(e);
      for (r2 = 0; r2 < n.length; r2++) o = n[r2], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
    }
    return i;
  }
  function _objectWithoutPropertiesLoose14(r2, e) {
    if (null == r2) return {};
    var t = {};
    for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
      if (-1 !== e.indexOf(n)) continue;
      t[n] = r2[n];
    }
    return t;
  }
  var initialContextState = {
    data: [],
    xAxisId: "xAxis-0",
    yAxisId: "yAxis-0",
    dataPointFormatter: () => ({
      x: 0,
      y: 0,
      value: 0
    }),
    errorBarOffset: 0
  };
  var ErrorBarContext = /* @__PURE__ */ (0, import_react44.createContext)(initialContextState);
  function SetErrorBarContext(props) {
    var children = props.children, rest = _objectWithoutProperties14(props, _excluded14);
    return /* @__PURE__ */ React30.createElement(ErrorBarContext.Provider, {
      value: rest
    }, children);
  }

  // node_modules/recharts/es6/cartesian/GraphicalItemClipPath.js
  var React31 = __toESM(require_react());
  function useNeedsClip(xAxisId, yAxisId) {
    var _xAxis$allowDataOverf, _yAxis$allowDataOverf;
    var xAxis = useAppSelector((state) => selectXAxisSettings(state, xAxisId));
    var yAxis = useAppSelector((state) => selectYAxisSettings(state, yAxisId));
    var needClipX = (_xAxis$allowDataOverf = xAxis === null || xAxis === void 0 ? void 0 : xAxis.allowDataOverflow) !== null && _xAxis$allowDataOverf !== void 0 ? _xAxis$allowDataOverf : implicitXAxis.allowDataOverflow;
    var needClipY = (_yAxis$allowDataOverf = yAxis === null || yAxis === void 0 ? void 0 : yAxis.allowDataOverflow) !== null && _yAxis$allowDataOverf !== void 0 ? _yAxis$allowDataOverf : implicitYAxis.allowDataOverflow;
    var needClip = needClipX || needClipY;
    return {
      needClip,
      needClipX,
      needClipY
    };
  }
  function GraphicalItemClipPath(_ref2) {
    var xAxisId = _ref2.xAxisId, yAxisId = _ref2.yAxisId, clipPathId = _ref2.clipPathId;
    var plotArea = usePlotArea();
    var _useNeedsClip = useNeedsClip(xAxisId, yAxisId), needClipX = _useNeedsClip.needClipX, needClipY = _useNeedsClip.needClipY, needClip = _useNeedsClip.needClip;
    var xAxisRange = useAppSelector((state) => selectXAxisRange(state, xAxisId, false));
    var yAxisRange = useAppSelector((state) => selectYAxisRange(state, yAxisId, false));
    if (!needClip || !plotArea) {
      return null;
    }
    var x5 = plotArea.x, y5 = plotArea.y, width = plotArea.width, height = plotArea.height;
    var clipX = needClipX && xAxisRange ? Math.min(xAxisRange[0], xAxisRange[1]) : x5 - width / 2;
    var clipY = needClipY && yAxisRange ? Math.min(yAxisRange[0], yAxisRange[1]) : y5 - height / 2;
    var clipWidth = needClipX && xAxisRange ? Math.abs(xAxisRange[1] - xAxisRange[0]) : width * 2;
    var clipHeight = needClipY && yAxisRange ? Math.abs(yAxisRange[1] - yAxisRange[0]) : height * 2;
    return /* @__PURE__ */ React31.createElement("clipPath", {
      id: "clipPath-".concat(clipPathId)
    }, /* @__PURE__ */ React31.createElement("rect", {
      x: clipX,
      y: clipY,
      width: clipWidth,
      height: clipHeight
    }));
  }

  // node_modules/recharts/es6/state/selectors/lineSelectors.js
  var selectXAxisWithScale = (state, xAxisId, _yAxisId, isPanorama) => selectAxisWithScale(state, "xAxis", xAxisId, isPanorama);
  var selectXAxisTicks = (state, xAxisId, _yAxisId, isPanorama) => selectTicksOfGraphicalItem(state, "xAxis", xAxisId, isPanorama);
  var selectYAxisWithScale = (state, _xAxisId, yAxisId, isPanorama) => selectAxisWithScale(state, "yAxis", yAxisId, isPanorama);
  var selectYAxisTicks = (state, _xAxisId, yAxisId, isPanorama) => selectTicksOfGraphicalItem(state, "yAxis", yAxisId, isPanorama);
  var selectBandSize = createSelector([selectChartLayout, selectXAxisWithScale, selectYAxisWithScale, selectXAxisTicks, selectYAxisTicks], (layout, xAxis, yAxis, xAxisTicks, yAxisTicks) => {
    if (isCategoricalAxis(layout, "xAxis")) {
      return getBandSizeOfAxis(xAxis, xAxisTicks, false);
    }
    return getBandSizeOfAxis(yAxis, yAxisTicks, false);
  });
  var pickLineId = (_state, _xAxisId, _yAxisId, _isPanorama, id) => id;
  function isLineSettings(item) {
    return item.type === "line";
  }
  var selectSynchronisedLineSettings = createSelector([selectUnfilteredCartesianItems, pickLineId], (graphicalItems, id) => graphicalItems.filter(isLineSettings).find((x5) => x5.id === id));
  var selectLinePoints = createSelector([selectChartLayout, selectXAxisWithScale, selectYAxisWithScale, selectXAxisTicks, selectYAxisTicks, selectSynchronisedLineSettings, selectBandSize, selectChartDataWithIndexesIfNotInPanoramaPosition4], (layout, xAxis, yAxis, xAxisTicks, yAxisTicks, lineSettings, bandSize, _ref2) => {
    var chartData = _ref2.chartData, dataStartIndex = _ref2.dataStartIndex, dataEndIndex = _ref2.dataEndIndex;
    if (lineSettings == null || xAxis == null || yAxis == null || xAxisTicks == null || yAxisTicks == null || xAxisTicks.length === 0 || yAxisTicks.length === 0 || bandSize == null || layout !== "horizontal" && layout !== "vertical") {
      return void 0;
    }
    var dataKey = lineSettings.dataKey, data = lineSettings.data;
    var displayedData;
    if (data != null && data.length > 0) {
      displayedData = data;
    } else {
      displayedData = chartData === null || chartData === void 0 ? void 0 : chartData.slice(dataStartIndex, dataEndIndex + 1);
    }
    if (displayedData == null) {
      return void 0;
    }
    return computeLinePoints({
      layout,
      xAxis,
      yAxis,
      xAxisTicks,
      yAxisTicks,
      dataKey,
      bandSize,
      displayedData
    });
  });

  // node_modules/recharts/es6/util/getRadiusAndStrokeWidthFromDot.js
  function getRadiusAndStrokeWidthFromDot(dot) {
    var props = svgPropertiesNoEventsFromUnknown(dot);
    var defaultR = 3;
    var defaultStrokeWidth = 2;
    if (props != null) {
      var r2 = props.r, strokeWidth = props.strokeWidth;
      var realR = Number(r2);
      var realStrokeWidth = Number(strokeWidth);
      if (Number.isNaN(realR) || realR < 0) {
        realR = defaultR;
      }
      if (Number.isNaN(realStrokeWidth) || realStrokeWidth < 0) {
        realStrokeWidth = defaultStrokeWidth;
      }
      return {
        r: realR,
        strokeWidth: realStrokeWidth
      };
    }
    return {
      r: defaultR,
      strokeWidth: defaultStrokeWidth
    };
  }

  // node_modules/recharts/es6/cartesian/Line.js
  var _excluded15 = ["id"];
  var _excluded28 = ["type", "layout", "connectNulls", "needClip", "shape", "strokeDasharray"];
  var _excluded34 = ["activeDot", "animateNewValues", "animationBegin", "animationDuration", "animationEasing", "connectNulls", "dot", "hide", "isAnimationActive", "label", "legendType", "xAxisId", "yAxisId", "id"];
  function _extends19() {
    return _extends19 = Object.assign ? Object.assign.bind() : function(n) {
      for (var e = 1; e < arguments.length; e++) {
        var t = arguments[e];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends19.apply(null, arguments);
  }
  function _objectWithoutProperties15(e, t) {
    if (null == e) return {};
    var o, r2, i = _objectWithoutPropertiesLoose15(e, t);
    if (Object.getOwnPropertySymbols) {
      var n = Object.getOwnPropertySymbols(e);
      for (r2 = 0; r2 < n.length; r2++) o = n[r2], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
    }
    return i;
  }
  function _objectWithoutPropertiesLoose15(r2, e) {
    if (null == r2) return {};
    var t = {};
    for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
      if (-1 !== e.indexOf(n)) continue;
      t[n] = r2[n];
    }
    return t;
  }
  function ownKeys34(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread34(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys34(Object(t), true).forEach(function(r3) {
        _defineProperty37(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys34(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty37(e, r2, t) {
    return (r2 = _toPropertyKey37(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey37(t) {
    var i = _toPrimitive37(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive37(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  function getTotalLength2(mainCurve) {
    try {
      return mainCurve && mainCurve.getTotalLength && mainCurve.getTotalLength() || 0;
    } catch (_unused) {
      return 0;
    }
  }
  function averageShift(items) {
    var total = 0;
    var count = 0;
    for (var item of items) {
      if (item.status === "matched" && item.prev.x != null && item.next.x != null) {
        total += item.next.x - item.prev.x;
        count++;
      }
    }
    return count > 0 ? total / count : 0;
  }
  var defaultLineAnimateItems = (items, animationElapsedTime) => {
    if (items == null) {
      return [];
    }
    if (animationElapsedTime === 1) return items.flatMap((item2) => item2.status === "removed" ? [] : [item2.next]);
    var shift = averageShift(items);
    var result = [];
    for (var item of items) {
      if (item.status === "matched") {
        result.push(_objectSpread34(_objectSpread34({}, item.next), {}, {
          x: interpolate(item.prev.x, item.next.x, animationElapsedTime),
          y: interpolate(item.prev.y, item.next.y, animationElapsedTime)
        }));
      } else if (item.status === "added") {
        if (item.next.x != null) {
          var entryX = item.next.x - shift;
          result.push(_objectSpread34(_objectSpread34({}, item.next), {}, {
            x: interpolate(entryX, item.next.x, animationElapsedTime),
            y: item.next.y
          }));
        } else {
          result.push(item.next);
        }
      } else if (item.status === "removed") {
        if (item.prev.x != null) {
          var exitX = item.prev.x + shift;
          result.push(_objectSpread34(_objectSpread34({}, item.prev), {}, {
            x: interpolate(item.prev.x, exitX, animationElapsedTime),
            y: item.prev.y
          }));
        }
      }
    }
    return result;
  };
  var defaultLineProps = {
    activeDot: true,
    animateNewValues: true,
    animationBegin: 0,
    animationDuration: 1500,
    animationEasing: "ease",
    animationInterpolateFn: defaultLineAnimateItems,
    animationMatchBy: matchByIndex,
    connectNulls: false,
    dot: true,
    fill: "#fff",
    hide: false,
    isAnimationActive: "auto",
    label: false,
    legendType: "line",
    shape: LineDrawShape,
    stroke: "#3182bd",
    strokeWidth: 1,
    xAxisId: 0,
    yAxisId: 0,
    zIndex: DefaultZIndexes.line,
    type: "linear"
  };
  var computeLegendPayloadFromAreaData = (props) => {
    var dataKey = props.dataKey, name = props.name, stroke = props.stroke, legendType = props.legendType, hide = props.hide;
    return [{
      inactive: hide,
      dataKey,
      type: legendType,
      color: stroke,
      value: getTooltipNameProp(name, dataKey),
      payload: props
    }];
  };
  var SetLineTooltipEntrySettings = /* @__PURE__ */ React32.memo((_ref2) => {
    var dataKey = _ref2.dataKey, data = _ref2.data, stroke = _ref2.stroke, strokeWidth = _ref2.strokeWidth, fill = _ref2.fill, name = _ref2.name, hide = _ref2.hide, unit2 = _ref2.unit, formatter = _ref2.formatter, tooltipType = _ref2.tooltipType, id = _ref2.id;
    var tooltipEntrySettings = {
      dataDefinedOnItem: data,
      getPosition: noop,
      settings: {
        stroke,
        strokeWidth,
        fill,
        dataKey,
        nameKey: void 0,
        name: getTooltipNameProp(name, dataKey),
        hide,
        type: tooltipType,
        color: stroke,
        unit: unit2,
        formatter,
        graphicalItemId: id
      }
    };
    return /* @__PURE__ */ React32.createElement(SetTooltipEntrySettings, {
      tooltipEntrySettings
    });
  });
  function LineDotsWrapper(_ref2) {
    var clipPathId = _ref2.clipPathId, points = _ref2.points, props = _ref2.props;
    var dot = props.dot, dataKey = props.dataKey, needClip = props.needClip;
    var id = props.id, propsWithoutId = _objectWithoutProperties15(props, _excluded15);
    var lineProps = svgPropertiesNoEvents(propsWithoutId);
    return /* @__PURE__ */ React32.createElement(Dots, {
      points,
      dot,
      className: "recharts-line-dots",
      dotClassName: "recharts-line-dot",
      dataKey,
      baseProps: lineProps,
      needClip,
      clipPathId
    });
  }
  function LineLabelListProvider(_ref3) {
    var showLabels = _ref3.showLabels, children = _ref3.children, points = _ref3.points;
    var labelListEntries = (0, import_react45.useMemo)(() => {
      return points === null || points === void 0 ? void 0 : points.map((point6) => {
        var _point$x, _point$y;
        var viewBox = {
          x: (_point$x = point6.x) !== null && _point$x !== void 0 ? _point$x : 0,
          y: (_point$y = point6.y) !== null && _point$y !== void 0 ? _point$y : 0,
          width: 0,
          lowerWidth: 0,
          upperWidth: 0,
          height: 0
        };
        return _objectSpread34(_objectSpread34({}, viewBox), {}, {
          value: point6.value,
          payload: point6.payload,
          viewBox,
          /*
           * Line is not passing parentViewBox to the LabelList so the labels can escape - looks like a bug, should we pass parentViewBox?
           * Or should this just be the root chart viewBox?
           */
          parentViewBox: void 0,
          fill: void 0
        });
      });
    }, [points]);
    return /* @__PURE__ */ React32.createElement(CartesianLabelListContextProvider, {
      value: showLabels ? labelListEntries : void 0
    }, children);
  }
  function StaticCurve(_ref4) {
    var clipPathId = _ref4.clipPathId, pathRef = _ref4.pathRef, points = _ref4.points, props = _ref4.props, animationElapsedTime = _ref4.animationElapsedTime, isAnimating = _ref4.isAnimating, isEntrance = _ref4.isEntrance, visibleLength = _ref4.visibleLength;
    var type = props.type, layout = props.layout, connectNulls = props.connectNulls, needClip = props.needClip, shape = props.shape, strokeDasharray = props.strokeDasharray, others = _objectWithoutProperties15(props, _excluded28);
    var curveProps = _objectSpread34(_objectSpread34({}, svgPropertiesAndEvents(others)), {}, {
      fill: "none",
      className: "recharts-line-curve",
      clipPath: needClip ? "url(#clipPath-".concat(clipPathId, ")") : void 0,
      points,
      type,
      layout,
      connectNulls,
      strokeDasharray: strokeDasharray !== null && strokeDasharray !== void 0 ? strokeDasharray : props.strokeDasharray,
      pathRef,
      animationElapsedTime,
      isAnimating,
      isEntrance: props.animateNewValues ? isEntrance : false,
      visibleLength
    });
    return /* @__PURE__ */ React32.createElement(React32.Fragment, null, (points === null || points === void 0 ? void 0 : points.length) > 1 && /* @__PURE__ */ React32.createElement(Shape, {
      option: shape,
      DefaultShape: defaultLineProps.shape,
      shapeProps: curveProps
    }), /* @__PURE__ */ React32.createElement(LineDotsWrapper, {
      points,
      clipPathId,
      props
    }));
  }
  function CurveWithAnimation(_ref5) {
    var clipPathId = _ref5.clipPathId, props = _ref5.props, pathRef = _ref5.pathRef, previousPointsRef = _ref5.previousPointsRef;
    var points = props.points, isAnimationActive = props.isAnimationActive, animationBegin = props.animationBegin, animationDuration = props.animationDuration, animationEasing = props.animationEasing, animationMatchBy = props.animationMatchBy, animationInterpolateFn = props.animationInterpolateFn, layout = props.layout;
    var totalLength = getTotalLength2(pathRef.current);
    var _useAnimationCallback = useAnimationCallbacks(props.onAnimationStart, props.onAnimationEnd), isAnimating = _useAnimationCallback.isAnimating, handleAnimationStart = _useAnimationCallback.handleAnimationStart, handleAnimationEnd = _useAnimationCallback.handleAnimationEnd;
    var showLabels = !isAnimating;
    var getVisibleLength = useAnimatedLineLength(points);
    var shouldUpdatePreviousRef = (0, import_react45.useCallback)((animationElapsedTime) => animationElapsedTime > 0 && totalLength > 0, [totalLength]);
    return /* @__PURE__ */ React32.createElement(LineLabelListProvider, {
      points,
      showLabels
    }, props.children, /* @__PURE__ */ React32.createElement(AnimatedItems, {
      animationInput: points,
      animationIdPrefix: "recharts-line-",
      items: points,
      previousItemsRef: previousPointsRef,
      isAnimationActive,
      animationBegin,
      animationDuration,
      animationEasing,
      onAnimationStart: handleAnimationStart,
      onAnimationEnd: handleAnimationEnd,
      animationInterpolateFn,
      animationMatchBy,
      shouldUpdatePreviousRef,
      layout
    }, (stepData, animationElapsedTime, isEntrance) => {
      var animationActive = isAnimating || animationElapsedTime < 1;
      var visibleLength = animationActive ? getVisibleLength(animationElapsedTime, totalLength) : null;
      return /* @__PURE__ */ React32.createElement(StaticCurve, {
        props,
        points: stepData,
        clipPathId,
        pathRef,
        animationElapsedTime,
        isAnimating: animationActive,
        isEntrance,
        visibleLength
      });
    }), /* @__PURE__ */ React32.createElement(LabelListFromLabelProp, {
      label: props.label
    }));
  }
  function RenderCurve(_ref6) {
    var clipPathId = _ref6.clipPathId, props = _ref6.props;
    var previousPointsRef = (0, import_react45.useRef)(null);
    var pathRef = (0, import_react45.useRef)(null);
    return /* @__PURE__ */ React32.createElement(CurveWithAnimation, {
      props,
      clipPathId,
      previousPointsRef,
      pathRef
    });
  }
  var errorBarDataPointFormatter = (dataPoint, dataKey) => {
    var _dataPoint$x, _dataPoint$y;
    return {
      x: (_dataPoint$x = dataPoint.x) !== null && _dataPoint$x !== void 0 ? _dataPoint$x : void 0,
      y: (_dataPoint$y = dataPoint.y) !== null && _dataPoint$y !== void 0 ? _dataPoint$y : void 0,
      value: dataPoint.value,
      // getValueByDataKey does not validate the output type
      errorVal: getValueByDataKey(dataPoint.payload, dataKey)
    };
  };
  var LineWithState = class extends import_react45.Component {
    render() {
      var _this$props = this.props, hide = _this$props.hide, dot = _this$props.dot, points = _this$props.points, className = _this$props.className, xAxisId = _this$props.xAxisId, yAxisId = _this$props.yAxisId, top = _this$props.top, left2 = _this$props.left, width = _this$props.width, height = _this$props.height, id = _this$props.id, needClip = _this$props.needClip, zIndex = _this$props.zIndex;
      if (hide) {
        return null;
      }
      var layerClass = clsx("recharts-line", className);
      var clipPathId = id;
      var _getRadiusAndStrokeWi = getRadiusAndStrokeWidthFromDot(dot), r2 = _getRadiusAndStrokeWi.r, strokeWidth = _getRadiusAndStrokeWi.strokeWidth;
      var clipDot = isClipDot(dot);
      var dotSize = r2 * 2 + strokeWidth;
      var activePointsClipPath = needClip ? "url(#clipPath-".concat(clipDot ? "" : "dots-").concat(clipPathId, ")") : void 0;
      return /* @__PURE__ */ React32.createElement(ZIndexLayer, {
        zIndex
      }, /* @__PURE__ */ React32.createElement(Layer, {
        className: layerClass
      }, needClip && /* @__PURE__ */ React32.createElement("defs", null, /* @__PURE__ */ React32.createElement(GraphicalItemClipPath, {
        clipPathId,
        xAxisId,
        yAxisId
      }), !clipDot && /* @__PURE__ */ React32.createElement("clipPath", {
        id: "clipPath-dots-".concat(clipPathId)
      }, /* @__PURE__ */ React32.createElement("rect", {
        x: left2 - dotSize / 2,
        y: top - dotSize / 2,
        width: width + dotSize,
        height: height + dotSize
      }))), /* @__PURE__ */ React32.createElement(SetErrorBarContext, {
        xAxisId,
        yAxisId,
        data: points,
        dataPointFormatter: errorBarDataPointFormatter,
        errorBarOffset: 0
      }, /* @__PURE__ */ React32.createElement(RenderCurve, {
        props: this.props,
        clipPathId
      }))), /* @__PURE__ */ React32.createElement(ActivePoints, {
        activeDot: this.props.activeDot,
        points,
        mainColor: this.props.stroke,
        itemDataKey: this.props.dataKey,
        clipPath: activePointsClipPath
      }));
    }
  };
  function LineImpl(props) {
    var _resolveDefaultProps = resolveDefaultProps(props, defaultLineProps), activeDot = _resolveDefaultProps.activeDot, animateNewValues = _resolveDefaultProps.animateNewValues, animationBegin = _resolveDefaultProps.animationBegin, animationDuration = _resolveDefaultProps.animationDuration, animationEasing = _resolveDefaultProps.animationEasing, connectNulls = _resolveDefaultProps.connectNulls, dot = _resolveDefaultProps.dot, hide = _resolveDefaultProps.hide, isAnimationActive = _resolveDefaultProps.isAnimationActive, label = _resolveDefaultProps.label, legendType = _resolveDefaultProps.legendType, xAxisId = _resolveDefaultProps.xAxisId, yAxisId = _resolveDefaultProps.yAxisId, id = _resolveDefaultProps.id, everythingElse = _objectWithoutProperties15(_resolveDefaultProps, _excluded34);
    var _useNeedsClip = useNeedsClip(xAxisId, yAxisId), needClip = _useNeedsClip.needClip;
    var plotArea = usePlotArea();
    var layout = useChartLayout();
    var isPanorama = useIsPanorama();
    var points = useAppSelector((state) => selectLinePoints(state, xAxisId, yAxisId, isPanorama, id));
    if (layout !== "horizontal" && layout !== "vertical" || points == null || plotArea == null) {
      return null;
    }
    var height = plotArea.height, width = plotArea.width, left2 = plotArea.x, top = plotArea.y;
    return /* @__PURE__ */ React32.createElement(LineWithState, _extends19({}, everythingElse, {
      id,
      connectNulls,
      dot,
      activeDot,
      animateNewValues,
      animationBegin,
      animationDuration,
      animationEasing,
      isAnimationActive,
      hide,
      label,
      legendType,
      xAxisId,
      yAxisId,
      points,
      layout,
      height,
      width,
      left: left2,
      top,
      needClip
    }));
  }
  function computeLinePoints(_ref7) {
    var layout = _ref7.layout, xAxis = _ref7.xAxis, yAxis = _ref7.yAxis, xAxisTicks = _ref7.xAxisTicks, yAxisTicks = _ref7.yAxisTicks, dataKey = _ref7.dataKey, bandSize = _ref7.bandSize, displayedData = _ref7.displayedData;
    return displayedData.map((entry, index2) => {
      var value2 = getValueByDataKey(entry, dataKey);
      if (layout === "horizontal") {
        var _x = getCateCoordinateOfLine({
          axis: xAxis,
          ticks: xAxisTicks,
          bandSize,
          entry,
          index: index2
        });
        var _y = isNullish(value2) ? null : yAxis.scale.map(value2);
        return {
          x: _x,
          y: _y !== null && _y !== void 0 ? _y : null,
          value: value2,
          payload: entry
        };
      }
      var x5 = isNullish(value2) ? null : xAxis.scale.map(value2);
      var y5 = getCateCoordinateOfLine({
        axis: yAxis,
        ticks: yAxisTicks,
        bandSize,
        entry,
        index: index2
      });
      if (x5 == null || y5 == null) {
        return null;
      }
      return {
        x: x5,
        y: y5,
        value: value2,
        payload: entry
      };
    }).filter(Boolean);
  }
  function LineFn(outsideProps) {
    var props = resolveDefaultProps(outsideProps, defaultLineProps);
    var isPanorama = useIsPanorama();
    return /* @__PURE__ */ React32.createElement(RegisterGraphicalItemId, {
      id: props.id,
      type: "line"
    }, (id) => /* @__PURE__ */ React32.createElement(React32.Fragment, null, /* @__PURE__ */ React32.createElement(SetLegendPayload, {
      legendPayload: computeLegendPayloadFromAreaData(props)
    }), /* @__PURE__ */ React32.createElement(SetLineTooltipEntrySettings, {
      dataKey: props.dataKey,
      data: props.data,
      stroke: props.stroke,
      strokeWidth: props.strokeWidth,
      fill: props.fill,
      name: props.name,
      hide: props.hide,
      unit: props.unit,
      formatter: props.formatter,
      tooltipType: props.tooltipType,
      id
    }), /* @__PURE__ */ React32.createElement(SetCartesianGraphicalItem, {
      type: "line",
      id,
      data: props.data,
      xAxisId: props.xAxisId,
      yAxisId: props.yAxisId,
      zAxisId: 0,
      dataKey: props.dataKey,
      hide: props.hide,
      isPanorama
    }), /* @__PURE__ */ React32.createElement(LineImpl, _extends19({}, props, {
      id
    }))));
  }
  var Line = /* @__PURE__ */ React32.memo(LineFn, propsAreEqual);
  Line.displayName = "Line";

  // node_modules/recharts/es6/cartesian/Area.js
  var React34 = __toESM(require_react());
  var import_react46 = __toESM(require_react());

  // node_modules/recharts/es6/state/selectors/graphicalItemSelectors.js
  function selectXAxisIdFromGraphicalItemId(state, id) {
    var _state$graphicalItems, _state$graphicalItems2;
    return (_state$graphicalItems = (_state$graphicalItems2 = state.graphicalItems.cartesianItems.find((item) => item.id === id)) === null || _state$graphicalItems2 === void 0 ? void 0 : _state$graphicalItems2.xAxisId) !== null && _state$graphicalItems !== void 0 ? _state$graphicalItems : defaultAxisId;
  }
  function selectYAxisIdFromGraphicalItemId(state, id) {
    var _state$graphicalItems3, _state$graphicalItems4;
    return (_state$graphicalItems3 = (_state$graphicalItems4 = state.graphicalItems.cartesianItems.find((item) => item.id === id)) === null || _state$graphicalItems4 === void 0 ? void 0 : _state$graphicalItems4.yAxisId) !== null && _state$graphicalItems3 !== void 0 ? _state$graphicalItems3 : defaultAxisId;
  }

  // node_modules/recharts/es6/state/selectors/areaSelectors.js
  var selectXAxisWithScale2 = (state, graphicalItemId, isPanorama) => selectAxisWithScale(state, "xAxis", selectXAxisIdFromGraphicalItemId(state, graphicalItemId), isPanorama);
  var selectXAxisTicks2 = (state, graphicalItemId, isPanorama) => selectTicksOfGraphicalItem(state, "xAxis", selectXAxisIdFromGraphicalItemId(state, graphicalItemId), isPanorama);
  var selectYAxisWithScale2 = (state, graphicalItemId, isPanorama) => selectAxisWithScale(state, "yAxis", selectYAxisIdFromGraphicalItemId(state, graphicalItemId), isPanorama);
  var selectYAxisTicks2 = (state, graphicalItemId, isPanorama) => selectTicksOfGraphicalItem(state, "yAxis", selectYAxisIdFromGraphicalItemId(state, graphicalItemId), isPanorama);
  var selectBandSize2 = createSelector([selectChartLayout, selectXAxisWithScale2, selectYAxisWithScale2, selectXAxisTicks2, selectYAxisTicks2], (layout, xAxis, yAxis, xAxisTicks, yAxisTicks) => {
    if (isCategoricalAxis(layout, "xAxis")) {
      return getBandSizeOfAxis(xAxis, xAxisTicks, false);
    }
    return getBandSizeOfAxis(yAxis, yAxisTicks, false);
  });
  var pickAreaId = (_state, id) => id;
  var selectSynchronisedAreaSettings = createSelector([selectUnfilteredCartesianItems, pickAreaId], (graphicalItems, id) => graphicalItems.filter((item) => item.type === "area").find((item) => item.id === id));
  var selectNumericalAxisType = (state) => {
    var layout = selectChartLayout(state);
    var isXAxisCategorical = isCategoricalAxis(layout, "xAxis");
    return isXAxisCategorical ? "yAxis" : "xAxis";
  };
  var selectNumericalAxisIdFromGraphicalItemId = (state, graphicalItemId) => {
    var axisType = selectNumericalAxisType(state);
    if (axisType === "yAxis") {
      return selectYAxisIdFromGraphicalItemId(state, graphicalItemId);
    }
    return selectXAxisIdFromGraphicalItemId(state, graphicalItemId);
  };
  var selectNumericalAxisStackGroups = (state, graphicalItemId, isPanorama) => selectStackGroups(state, selectNumericalAxisType(state), selectNumericalAxisIdFromGraphicalItemId(state, graphicalItemId), isPanorama);
  var selectGraphicalItemStackedData = createSelector([selectSynchronisedAreaSettings, selectNumericalAxisStackGroups], (areaSettings, stackGroups) => {
    var _stackGroups$stackId;
    if (areaSettings == null || stackGroups == null) {
      return void 0;
    }
    var stackId = areaSettings.stackId;
    var stackSeriesIdentifier = getStackSeriesIdentifier(areaSettings);
    if (stackId == null || stackSeriesIdentifier == null) {
      return void 0;
    }
    var groups = (_stackGroups$stackId = stackGroups[stackId]) === null || _stackGroups$stackId === void 0 ? void 0 : _stackGroups$stackId.stackedData;
    var found = groups === null || groups === void 0 ? void 0 : groups.find((v) => v.key === stackSeriesIdentifier);
    if (found == null) {
      return void 0;
    }
    return found.map((item) => [item[0], item[1]]);
  });
  var selectArea = createSelector([selectChartLayout, selectXAxisWithScale2, selectYAxisWithScale2, selectXAxisTicks2, selectYAxisTicks2, selectGraphicalItemStackedData, selectChartDataWithIndexesIfNotInPanoramaPosition3, selectBandSize2, selectSynchronisedAreaSettings, selectChartBaseValue], (layout, xAxis, yAxis, xAxisTicks, yAxisTicks, stackedData, _ref2, bandSize, areaSettings, chartBaseValue) => {
    var chartData = _ref2.chartData, dataStartIndex = _ref2.dataStartIndex, dataEndIndex = _ref2.dataEndIndex;
    if (areaSettings == null || layout !== "horizontal" && layout !== "vertical" || xAxis == null || yAxis == null || xAxisTicks == null || yAxisTicks == null || xAxisTicks.length === 0 || yAxisTicks.length === 0 || bandSize == null) {
      return void 0;
    }
    var data = areaSettings.data;
    var displayedData;
    if (data && data.length > 0) {
      displayedData = data;
    } else {
      displayedData = chartData === null || chartData === void 0 ? void 0 : chartData.slice(dataStartIndex, dataEndIndex + 1);
    }
    if (displayedData == null) {
      return void 0;
    }
    return computeArea({
      layout,
      xAxis,
      yAxis,
      xAxisTicks,
      yAxisTicks,
      dataStartIndex,
      areaSettings,
      stackedData,
      displayedData,
      chartBaseValue,
      bandSize
    });
  });

  // node_modules/recharts/es6/cartesian/AreaRevealShape.js
  var React33 = __toESM(require_react());
  var _excluded16 = ["animationElapsedTime", "isAnimating", "isEntrance", "layout", "isRange", "stroke", "connectNulls"];
  var _excluded29 = ["id", "baseLine"];
  function _extends20() {
    return _extends20 = Object.assign ? Object.assign.bind() : function(n) {
      for (var e = 1; e < arguments.length; e++) {
        var t = arguments[e];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends20.apply(null, arguments);
  }
  function _objectWithoutProperties16(e, t) {
    if (null == e) return {};
    var o, r2, i = _objectWithoutPropertiesLoose16(e, t);
    if (Object.getOwnPropertySymbols) {
      var n = Object.getOwnPropertySymbols(e);
      for (r2 = 0; r2 < n.length; r2++) o = n[r2], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
    }
    return i;
  }
  function _objectWithoutPropertiesLoose16(r2, e) {
    if (null == r2) return {};
    var t = {};
    for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
      if (-1 !== e.indexOf(n)) continue;
      t[n] = r2[n];
    }
    return t;
  }
  function HorizontalClipRect(_ref2) {
    var _points$, _points;
    var alpha = _ref2.alpha, baseLine = _ref2.baseLine, points = _ref2.points, strokeWidth = _ref2.strokeWidth;
    var startX = (_points$ = points[0]) === null || _points$ === void 0 ? void 0 : _points$.x;
    var endX = (_points = points[points.length - 1]) === null || _points === void 0 ? void 0 : _points.x;
    if (!isWellBehavedNumber(startX) || !isWellBehavedNumber(endX)) {
      return null;
    }
    var width = alpha * Math.abs(startX - endX);
    var maxY = Math.max(...points.map((entry) => entry.y || 0));
    if (isNumber(baseLine)) {
      maxY = Math.max(baseLine, maxY);
    } else if (baseLine && Array.isArray(baseLine) && baseLine.length) {
      maxY = Math.max(...baseLine.map((entry) => entry.y || 0), maxY);
    }
    if (isNumber(maxY)) {
      return /* @__PURE__ */ React33.createElement("rect", {
        x: startX < endX ? startX : startX - width,
        y: 0,
        width,
        height: Math.floor(maxY + (strokeWidth ? parseInt("".concat(strokeWidth), 10) : 1))
      });
    }
    return null;
  }
  function VerticalClipRect(_ref2) {
    var _points$2, _points2;
    var alpha = _ref2.alpha, baseLine = _ref2.baseLine, points = _ref2.points, strokeWidth = _ref2.strokeWidth;
    var startY = (_points$2 = points[0]) === null || _points$2 === void 0 ? void 0 : _points$2.y;
    var endY = (_points2 = points[points.length - 1]) === null || _points2 === void 0 ? void 0 : _points2.y;
    if (!isWellBehavedNumber(startY) || !isWellBehavedNumber(endY)) {
      return null;
    }
    var height = alpha * Math.abs(startY - endY);
    var maxX = Math.max(...points.map((entry) => entry.x || 0));
    if (isNumber(baseLine)) {
      maxX = Math.max(baseLine, maxX);
    } else if (baseLine && Array.isArray(baseLine) && baseLine.length) {
      maxX = Math.max(...baseLine.map((entry) => entry.x || 0), maxX);
    }
    if (isNumber(maxX)) {
      return /* @__PURE__ */ React33.createElement("rect", {
        x: 0,
        y: startY < endY ? startY : startY - height,
        width: maxX + (strokeWidth ? parseInt("".concat(strokeWidth), 10) : 1),
        height: Math.floor(height)
      });
    }
    return null;
  }
  function RevealClipRect(_ref3) {
    var alpha = _ref3.alpha, layout = _ref3.layout, points = _ref3.points, baseLine = _ref3.baseLine, strokeWidth = _ref3.strokeWidth;
    if (layout === "vertical") {
      return /* @__PURE__ */ React33.createElement(VerticalClipRect, {
        alpha,
        points,
        baseLine,
        strokeWidth
      });
    }
    return /* @__PURE__ */ React33.createElement(HorizontalClipRect, {
      alpha,
      points,
      baseLine,
      strokeWidth
    });
  }
  function AreaRevealShape(props) {
    var _props$animationElaps = props.animationElapsedTime, animationElapsedTime = _props$animationElaps === void 0 ? 1 : _props$animationElaps, _props$isAnimating = props.isAnimating, isAnimating = _props$isAnimating === void 0 ? false : _props$isAnimating, _props$isEntrance = props.isEntrance, isEntrance = _props$isEntrance === void 0 ? false : _props$isEntrance, layoutProp = props.layout, isRange = props.isRange, stroke = props.stroke, connectNulls = props.connectNulls, restProps = _objectWithoutProperties16(props, _excluded16);
    var layout = layoutProp === "vertical" ? "vertical" : "horizontal";
    var finalConnectNulls = connectNulls !== null && connectNulls !== void 0 ? connectNulls : false;
    var clipId = useId();
    var id = restProps.id, baseLine = restProps.baseLine, propsWithoutIdBaseline = _objectWithoutProperties16(restProps, _excluded29);
    var strokeSvgProps = svgPropertiesNoEvents(propsWithoutIdBaseline);
    var fillCurve = /* @__PURE__ */ React33.createElement(Curve, _extends20({}, restProps, {
      id,
      baseLine,
      connectNulls: finalConnectNulls,
      stroke: "none",
      className: "recharts-area-area",
      layout
    }));
    var strokeCurve = stroke !== "none" && /* @__PURE__ */ React33.createElement(Curve, _extends20({}, strokeSvgProps, {
      className: "recharts-area-curve",
      layout,
      type: restProps.type,
      connectNulls: finalConnectNulls,
      fill: "none",
      stroke,
      points: restProps.points
    }));
    var baselineCurve = stroke !== "none" && isRange && Array.isArray(baseLine) && /* @__PURE__ */ React33.createElement(Curve, _extends20({}, strokeSvgProps, {
      className: "recharts-area-curve",
      layout,
      type: restProps.type,
      connectNulls: finalConnectNulls,
      fill: "none",
      stroke,
      points: baseLine
    }));
    if (isEntrance && (isAnimating || animationElapsedTime < 1)) {
      var _restProps$points;
      return /* @__PURE__ */ React33.createElement(Layer, null, /* @__PURE__ */ React33.createElement("defs", null, /* @__PURE__ */ React33.createElement("clipPath", {
        id: clipId
      }, /* @__PURE__ */ React33.createElement(RevealClipRect, {
        alpha: animationElapsedTime,
        points: (_restProps$points = restProps.points) !== null && _restProps$points !== void 0 ? _restProps$points : [],
        baseLine,
        layout,
        strokeWidth: restProps.strokeWidth
      }))), /* @__PURE__ */ React33.createElement(Layer, {
        clipPath: "url(#".concat(clipId, ")")
      }, fillCurve, strokeCurve, baselineCurve));
    }
    return /* @__PURE__ */ React33.createElement(React33.Fragment, null, fillCurve, strokeCurve, baselineCurve);
  }

  // node_modules/recharts/es6/cartesian/Area.js
  var _excluded17 = ["id"];
  var _excluded210 = ["activeDot", "animationBegin", "animationDuration", "animationEasing", "connectNulls", "dot", "fill", "fillOpacity", "hide", "isAnimationActive", "legendType", "stroke", "xAxisId", "yAxisId"];
  function _extends21() {
    return _extends21 = Object.assign ? Object.assign.bind() : function(n) {
      for (var e = 1; e < arguments.length; e++) {
        var t = arguments[e];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends21.apply(null, arguments);
  }
  function _objectWithoutProperties17(e, t) {
    if (null == e) return {};
    var o, r2, i = _objectWithoutPropertiesLoose17(e, t);
    if (Object.getOwnPropertySymbols) {
      var n = Object.getOwnPropertySymbols(e);
      for (r2 = 0; r2 < n.length; r2++) o = n[r2], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
    }
    return i;
  }
  function _objectWithoutPropertiesLoose17(r2, e) {
    if (null == r2) return {};
    var t = {};
    for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
      if (-1 !== e.indexOf(n)) continue;
      t[n] = r2[n];
    }
    return t;
  }
  function ownKeys35(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread35(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys35(Object(t), true).forEach(function(r3) {
        _defineProperty38(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys35(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty38(e, r2, t) {
    return (r2 = _toPropertyKey38(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey38(t) {
    var i = _toPrimitive38(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive38(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  var defaultAreaAnimateItems = (items, animationElapsedTime) => {
    if (items == null) {
      return [];
    }
    if (animationElapsedTime === 1) {
      return items.flatMap((item) => item.status === "removed" ? [] : [item.next]);
    }
    return items.flatMap((item) => {
      if (item.status === "matched") {
        return [_objectSpread35(_objectSpread35({}, item.next), {}, {
          x: interpolate(item.prev.x, item.next.x, animationElapsedTime),
          y: interpolate(item.prev.y, item.next.y, animationElapsedTime)
        })];
      }
      if (item.status === "added") {
        return [item.next];
      }
      return [];
    });
  };
  var defaultAreaProps = {
    activeDot: true,
    animationBegin: 0,
    animationDuration: 1500,
    animationEasing: "ease",
    animationMatchBy: matchByIndex,
    animationInterpolateFn: defaultAreaAnimateItems,
    connectNulls: false,
    dot: false,
    fill: "#3182bd",
    fillOpacity: 0.6,
    hide: false,
    isAnimationActive: "auto",
    legendType: "line",
    stroke: "#3182bd",
    strokeWidth: 1,
    type: "linear",
    label: false,
    shape: AreaRevealShape,
    xAxisId: 0,
    yAxisId: 0,
    zIndex: DefaultZIndexes.area
  };
  function getLegendItemColor(stroke, fill) {
    return stroke && stroke !== "none" ? stroke : fill;
  }
  var computeLegendPayloadFromAreaData2 = (props) => {
    var dataKey = props.dataKey, name = props.name, stroke = props.stroke, fill = props.fill, legendType = props.legendType, hide = props.hide;
    return [{
      inactive: hide,
      dataKey,
      type: legendType,
      color: getLegendItemColor(stroke, fill),
      value: getTooltipNameProp(name, dataKey),
      payload: props
    }];
  };
  var SetAreaTooltipEntrySettings = /* @__PURE__ */ React34.memo((_ref2) => {
    var dataKey = _ref2.dataKey, data = _ref2.data, stroke = _ref2.stroke, strokeWidth = _ref2.strokeWidth, fill = _ref2.fill, name = _ref2.name, hide = _ref2.hide, unit2 = _ref2.unit, formatter = _ref2.formatter, tooltipType = _ref2.tooltipType, id = _ref2.id;
    var tooltipEntrySettings = {
      dataDefinedOnItem: data,
      getPosition: noop,
      settings: {
        stroke,
        strokeWidth,
        fill,
        dataKey,
        nameKey: void 0,
        name: getTooltipNameProp(name, dataKey),
        hide,
        type: tooltipType,
        color: getLegendItemColor(stroke, fill),
        unit: unit2,
        formatter,
        graphicalItemId: id
      }
    };
    return /* @__PURE__ */ React34.createElement(SetTooltipEntrySettings, {
      tooltipEntrySettings
    });
  });
  function AreaDotsWrapper(_ref2) {
    var clipPathId = _ref2.clipPathId, points = _ref2.points, props = _ref2.props;
    var needClip = props.needClip, dot = props.dot, dataKey = props.dataKey;
    var areaProps = svgPropertiesNoEvents(props);
    return /* @__PURE__ */ React34.createElement(Dots, {
      points,
      dot,
      className: "recharts-area-dots",
      dotClassName: "recharts-area-dot",
      dataKey,
      baseProps: areaProps,
      needClip,
      clipPathId
    });
  }
  function AreaLabelListProvider(_ref3) {
    var showLabels = _ref3.showLabels, children = _ref3.children, points = _ref3.points;
    var labelListEntries = points.map((point6) => {
      var _point$x, _point$y;
      var viewBox = {
        x: (_point$x = point6.x) !== null && _point$x !== void 0 ? _point$x : 0,
        y: (_point$y = point6.y) !== null && _point$y !== void 0 ? _point$y : 0,
        width: 0,
        lowerWidth: 0,
        upperWidth: 0,
        height: 0
      };
      return _objectSpread35(_objectSpread35({}, viewBox), {}, {
        value: point6.value,
        payload: point6.payload,
        parentViewBox: void 0,
        viewBox,
        fill: void 0
      });
    });
    return /* @__PURE__ */ React34.createElement(CartesianLabelListContextProvider, {
      value: showLabels ? labelListEntries : void 0
    }, children);
  }
  function StaticArea(_ref4) {
    var points = _ref4.points, baseLine = _ref4.baseLine, needClip = _ref4.needClip, clipPathId = _ref4.clipPathId, props = _ref4.props, animationElapsedTime = _ref4.animationElapsedTime, isAnimating = _ref4.isAnimating, isEntrance = _ref4.isEntrance;
    var layout = props.layout, type = props.type, stroke = props.stroke, connectNulls = props.connectNulls, isRange = props.isRange, shape = props.shape;
    var id = props.id, propsWithoutId = _objectWithoutProperties17(props, _excluded17);
    var propsWithEvents = svgPropertiesAndEvents(propsWithoutId);
    var curveProps = _objectSpread35(_objectSpread35({}, propsWithEvents), {}, {
      id,
      points,
      connectNulls,
      type,
      baseLine,
      layout,
      stroke,
      isRange,
      animationElapsedTime,
      isAnimating,
      isEntrance
    });
    return /* @__PURE__ */ React34.createElement(React34.Fragment, null, (points === null || points === void 0 ? void 0 : points.length) > 1 && /* @__PURE__ */ React34.createElement(Layer, {
      clipPath: needClip ? "url(#clipPath-".concat(clipPathId, ")") : void 0
    }, /* @__PURE__ */ React34.createElement(Shape, {
      option: shape,
      DefaultShape: defaultAreaProps.shape,
      shapeProps: curveProps
    })), /* @__PURE__ */ React34.createElement(AreaDotsWrapper, {
      points,
      props: propsWithoutId,
      clipPathId
    }));
  }
  function interpolateScalarBaseLine(baseLine, prevBaseLine, animationElapsedTime) {
    if (isNumber(baseLine)) {
      var previousNumberBaseLine = isNumber(prevBaseLine) ? prevBaseLine : void 0;
      return interpolate(previousNumberBaseLine, baseLine, animationElapsedTime);
    }
    if (isNullish(baseLine) || isNan(baseLine)) {
      var _previousNumberBaseLine = isNumber(prevBaseLine) ? prevBaseLine : void 0;
      return interpolate(_previousNumberBaseLine, 0, animationElapsedTime);
    }
    return baseLine;
  }
  function AreaWithAnimation(_ref5) {
    var needClip = _ref5.needClip, clipPathId = _ref5.clipPathId, props = _ref5.props, previousPointsRef = _ref5.previousPointsRef, previousBaselineRef = _ref5.previousBaselineRef;
    var points = props.points, baseLine = props.baseLine, isAnimationActive = props.isAnimationActive, animationBegin = props.animationBegin, animationDuration = props.animationDuration, animationEasing = props.animationEasing, animationMatchBy = props.animationMatchBy, animationInterpolateFn = props.animationInterpolateFn;
    var animationInput = (0, import_react46.useMemo)(() => ({
      points,
      baseLine
    }), [points, baseLine]);
    var baseLineAnimationState = useAnimationStartSnapshot(animationInput, previousBaselineRef);
    var layout = useCartesianChartLayout();
    var _useAnimationCallback = useAnimationCallbacks(props.onAnimationStart, props.onAnimationEnd), isAnimating = _useAnimationCallback.isAnimating, handleAnimationStart = _useAnimationCallback.handleAnimationStart, handleAnimationEnd = _useAnimationCallback.handleAnimationEnd;
    var prevBaseLine = baseLineAnimationState.startValue;
    if (layout == null) {
      return null;
    }
    var baseLineAnimationItems;
    if (Array.isArray(baseLine) && Array.isArray(prevBaseLine)) {
      baseLineAnimationItems = matchAnimationItems(prevBaseLine, baseLine, animationMatchBy);
    } else if (Array.isArray(baseLine)) {
      baseLineAnimationItems = matchAnimationItems(null, baseLine, animationMatchBy);
    } else {
      baseLineAnimationItems = null;
    }
    return /* @__PURE__ */ React34.createElement(AnimatedItems, {
      animationInput,
      animationIdPrefix: "recharts-area-",
      items: points,
      previousItemsRef: previousPointsRef,
      isAnimationActive,
      animationBegin,
      animationDuration,
      animationEasing,
      onAnimationStart: handleAnimationStart,
      onAnimationEnd: handleAnimationEnd,
      animationInterpolateFn,
      animationMatchBy,
      layout
    }, (stepPoints, animationElapsedTime, isEntrance) => {
      var stepBaseLine;
      if (animationElapsedTime === 1) {
        stepBaseLine = baseLine;
      } else if (Array.isArray(baseLine)) {
        stepBaseLine = animationInterpolateFn(baseLineAnimationItems, animationElapsedTime, layout);
      } else {
        stepBaseLine = isEntrance ? baseLine : interpolateScalarBaseLine(baseLine, prevBaseLine, animationElapsedTime);
      }
      baseLineAnimationState.syncStepValue(stepBaseLine, animationElapsedTime);
      return /* @__PURE__ */ React34.createElement(AreaLabelListProvider, {
        showLabels: !isAnimating,
        points
      }, props.children, /* @__PURE__ */ React34.createElement(StaticArea, {
        points: stepPoints,
        baseLine: stepBaseLine,
        needClip,
        clipPathId,
        props,
        animationElapsedTime,
        isAnimating: isAnimating || animationElapsedTime < 1,
        isEntrance
      }), /* @__PURE__ */ React34.createElement(LabelListFromLabelProp, {
        label: props.label
      }));
    });
  }
  function RenderArea(_ref6) {
    var needClip = _ref6.needClip, clipPathId = _ref6.clipPathId, props = _ref6.props;
    var previousPointsRef = (0, import_react46.useRef)(null);
    var previousBaselineRef = (0, import_react46.useRef)();
    return /* @__PURE__ */ React34.createElement(AreaWithAnimation, {
      needClip,
      clipPathId,
      props,
      previousPointsRef,
      previousBaselineRef
    });
  }
  var AreaWithState = class extends import_react46.PureComponent {
    render() {
      var _this$props = this.props, hide = _this$props.hide, dot = _this$props.dot, points = _this$props.points, className = _this$props.className, top = _this$props.top, left2 = _this$props.left, needClip = _this$props.needClip, xAxisId = _this$props.xAxisId, yAxisId = _this$props.yAxisId, width = _this$props.width, height = _this$props.height, id = _this$props.id, baseLine = _this$props.baseLine, zIndex = _this$props.zIndex;
      if (hide) {
        return null;
      }
      var layerClass = clsx("recharts-area", className);
      var clipPathId = id;
      var _getRadiusAndStrokeWi = getRadiusAndStrokeWidthFromDot(dot), r2 = _getRadiusAndStrokeWi.r, strokeWidth = _getRadiusAndStrokeWi.strokeWidth;
      var clipDot = isClipDot(dot);
      var dotSize = r2 * 2 + strokeWidth;
      var activePointsClipPath = needClip ? "url(#clipPath-".concat(clipDot ? "" : "dots-").concat(clipPathId, ")") : void 0;
      return /* @__PURE__ */ React34.createElement(ZIndexLayer, {
        zIndex
      }, /* @__PURE__ */ React34.createElement(Layer, {
        className: layerClass
      }, needClip && /* @__PURE__ */ React34.createElement("defs", null, /* @__PURE__ */ React34.createElement(GraphicalItemClipPath, {
        clipPathId,
        xAxisId,
        yAxisId
      }), !clipDot && /* @__PURE__ */ React34.createElement("clipPath", {
        id: "clipPath-dots-".concat(clipPathId)
      }, /* @__PURE__ */ React34.createElement("rect", {
        x: left2 - dotSize / 2,
        y: top - dotSize / 2,
        width: width + dotSize,
        height: height + dotSize
      }))), /* @__PURE__ */ React34.createElement(RenderArea, {
        needClip,
        clipPathId,
        props: this.props
      })), /* @__PURE__ */ React34.createElement(ActivePoints, {
        points,
        mainColor: getLegendItemColor(this.props.stroke, this.props.fill),
        itemDataKey: this.props.dataKey,
        activeDot: this.props.activeDot,
        clipPath: activePointsClipPath
      }), this.props.isRange && Array.isArray(baseLine) && /* @__PURE__ */ React34.createElement(ActivePoints, {
        points: baseLine,
        mainColor: getLegendItemColor(this.props.stroke, this.props.fill),
        itemDataKey: this.props.dataKey,
        activeDot: this.props.activeDot,
        clipPath: activePointsClipPath
      }));
    }
  };
  function AreaImpl(props) {
    var _useAppSelector;
    var activeDot = props.activeDot, animationBegin = props.animationBegin, animationDuration = props.animationDuration, animationEasing = props.animationEasing, connectNulls = props.connectNulls, dot = props.dot, fill = props.fill, fillOpacity = props.fillOpacity, hide = props.hide, isAnimationActive = props.isAnimationActive, legendType = props.legendType, stroke = props.stroke, xAxisId = props.xAxisId, yAxisId = props.yAxisId, everythingElse = _objectWithoutProperties17(props, _excluded210);
    var layout = useChartLayout();
    var chartName = useChartName();
    var _useNeedsClip = useNeedsClip(xAxisId, yAxisId), needClip = _useNeedsClip.needClip;
    var isPanorama = useIsPanorama();
    var _ref7 = (_useAppSelector = useAppSelector((state) => selectArea(state, props.id, isPanorama))) !== null && _useAppSelector !== void 0 ? _useAppSelector : {}, points = _ref7.points, isRange = _ref7.isRange, baseLine = _ref7.baseLine;
    var plotArea = usePlotArea();
    if (layout !== "horizontal" && layout !== "vertical" || plotArea == null) {
      return null;
    }
    if (chartName !== "AreaChart" && chartName !== "ComposedChart") {
      return null;
    }
    var height = plotArea.height, width = plotArea.width, left2 = plotArea.x, top = plotArea.y;
    if (!points || !points.length) {
      return null;
    }
    return /* @__PURE__ */ React34.createElement(AreaWithState, _extends21({}, everythingElse, {
      activeDot,
      animationBegin,
      animationDuration,
      animationEasing,
      baseLine,
      connectNulls,
      dot,
      fill,
      fillOpacity,
      height,
      hide,
      layout,
      isAnimationActive,
      isRange,
      legendType,
      needClip,
      points,
      stroke,
      width,
      left: left2,
      top,
      xAxisId,
      yAxisId
    }));
  }
  var getBaseValue = (layout, chartBaseValue, itemBaseValue, xAxis, yAxis) => {
    var baseValue = itemBaseValue !== null && itemBaseValue !== void 0 ? itemBaseValue : chartBaseValue;
    if (isNumber(baseValue)) {
      return baseValue;
    }
    var numericAxis = layout === "horizontal" ? yAxis : xAxis;
    var domain = numericAxis.scale.domain();
    if (numericAxis.type === "number") {
      var domainMax = Math.max(domain[0], domain[1]);
      var domainMin = Math.min(domain[0], domain[1]);
      if (baseValue === "dataMin") {
        return domainMin;
      }
      if (baseValue === "dataMax") {
        return domainMax;
      }
      return domainMax < 0 ? domainMax : Math.max(Math.min(domain[0], domain[1]), 0);
    }
    if (baseValue === "dataMin") {
      return domain[0];
    }
    if (baseValue === "dataMax") {
      return domain[1];
    }
    return domain[0];
  };
  function computeArea(_ref8) {
    var _ref8$areaSettings = _ref8.areaSettings, connectNulls = _ref8$areaSettings.connectNulls, itemBaseValue = _ref8$areaSettings.baseValue, dataKey = _ref8$areaSettings.dataKey, stackedData = _ref8.stackedData, layout = _ref8.layout, chartBaseValue = _ref8.chartBaseValue, xAxis = _ref8.xAxis, yAxis = _ref8.yAxis, displayedData = _ref8.displayedData, dataStartIndex = _ref8.dataStartIndex, xAxisTicks = _ref8.xAxisTicks, yAxisTicks = _ref8.yAxisTicks, bandSize = _ref8.bandSize;
    var hasStack = stackedData && stackedData.length;
    var baseValue = getBaseValue(layout, chartBaseValue, itemBaseValue, xAxis, yAxis);
    var isHorizontalLayout = layout === "horizontal";
    var isRange = false;
    var points = displayedData.map((entry, index2) => {
      var _valueAsArray$, _valueAsArray, _xAxis$scale$map;
      var valueAsArray;
      if (hasStack) {
        valueAsArray = stackedData[dataStartIndex + index2];
      } else {
        var rawValue = getValueByDataKey(entry, dataKey);
        if (!Array.isArray(rawValue)) {
          valueAsArray = [baseValue, rawValue];
        } else {
          valueAsArray = rawValue;
          isRange = true;
        }
      }
      var value1 = (_valueAsArray$ = (_valueAsArray = valueAsArray) === null || _valueAsArray === void 0 ? void 0 : _valueAsArray[1]) !== null && _valueAsArray$ !== void 0 ? _valueAsArray$ : null;
      var isBreakPoint = value1 == null || hasStack && !connectNulls && getValueByDataKey(entry, dataKey) == null;
      if (isHorizontalLayout) {
        var _yAxis$scale$map;
        return {
          x: getCateCoordinateOfLine({
            axis: xAxis,
            ticks: xAxisTicks,
            bandSize,
            entry,
            index: index2
          }),
          y: isBreakPoint ? null : (_yAxis$scale$map = yAxis.scale.map(value1)) !== null && _yAxis$scale$map !== void 0 ? _yAxis$scale$map : null,
          value: valueAsArray,
          payload: entry
        };
      }
      return {
        x: isBreakPoint ? null : (_xAxis$scale$map = xAxis.scale.map(value1)) !== null && _xAxis$scale$map !== void 0 ? _xAxis$scale$map : null,
        y: getCateCoordinateOfLine({
          axis: yAxis,
          ticks: yAxisTicks,
          bandSize,
          entry,
          index: index2
        }),
        value: valueAsArray,
        payload: entry
      };
    });
    var baseLine;
    if (hasStack || isRange) {
      baseLine = points.map((entry) => {
        var _xAxis$scale$map2;
        var x5 = Array.isArray(entry.value) ? entry.value[0] : null;
        if (isHorizontalLayout) {
          var _yAxis$scale$map2;
          return {
            x: entry.x,
            y: x5 != null && entry.y != null ? (_yAxis$scale$map2 = yAxis.scale.map(x5)) !== null && _yAxis$scale$map2 !== void 0 ? _yAxis$scale$map2 : null : null,
            payload: entry.payload
          };
        }
        return {
          x: x5 != null ? (_xAxis$scale$map2 = xAxis.scale.map(x5)) !== null && _xAxis$scale$map2 !== void 0 ? _xAxis$scale$map2 : null : null,
          y: entry.y,
          payload: entry.payload
        };
      });
    } else {
      baseLine = isHorizontalLayout ? yAxis.scale.map(baseValue) : xAxis.scale.map(baseValue);
    }
    return {
      points,
      baseLine: baseLine !== null && baseLine !== void 0 ? baseLine : 0,
      isRange
    };
  }
  function AreaFn(outsideProps) {
    var props = resolveDefaultProps(outsideProps, defaultAreaProps);
    var isPanorama = useIsPanorama();
    return /* @__PURE__ */ React34.createElement(RegisterGraphicalItemId, {
      id: props.id,
      type: "area"
    }, (id) => /* @__PURE__ */ React34.createElement(React34.Fragment, null, /* @__PURE__ */ React34.createElement(SetLegendPayload, {
      legendPayload: computeLegendPayloadFromAreaData2(props)
    }), /* @__PURE__ */ React34.createElement(SetAreaTooltipEntrySettings, {
      dataKey: props.dataKey,
      data: props.data,
      stroke: props.stroke,
      strokeWidth: props.strokeWidth,
      fill: props.fill,
      name: props.name,
      hide: props.hide,
      unit: props.unit,
      formatter: props.formatter,
      tooltipType: props.tooltipType,
      id
    }), /* @__PURE__ */ React34.createElement(SetCartesianGraphicalItem, {
      type: "area",
      id,
      data: props.data,
      dataKey: props.dataKey,
      xAxisId: props.xAxisId,
      yAxisId: props.yAxisId,
      zAxisId: 0,
      stackId: getNormalizedStackId(props.stackId),
      hide: props.hide,
      barSize: void 0,
      baseValue: props.baseValue,
      isPanorama,
      connectNulls: props.connectNulls
    }), /* @__PURE__ */ React34.createElement(AreaImpl, _extends21({}, props, {
      id
    }))));
  }
  var Area = /* @__PURE__ */ React34.memo(AreaFn, propsAreEqual);
  Area.displayName = "Area";

  // node_modules/recharts/es6/cartesian/Bar.js
  var React37 = __toESM(require_react());
  var import_react48 = __toESM(require_react());

  // node_modules/recharts/es6/util/BarUtils.js
  var React35 = __toESM(require_react());

  // node_modules/tiny-invariant/dist/esm/tiny-invariant.js
  var isProduction = false;
  var prefix = "Invariant failed";
  function invariant(condition, message) {
    if (condition) {
      return;
    }
    if (isProduction) {
      throw new Error(prefix);
    }
    var provided = typeof message === "function" ? message() : message;
    var value2 = provided ? "".concat(prefix, ": ").concat(provided) : prefix;
    throw new Error(value2);
  }

  // node_modules/recharts/es6/util/BarUtils.js
  var _excluded18 = ["option"];
  function _objectWithoutProperties18(e, t) {
    if (null == e) return {};
    var o, r2, i = _objectWithoutPropertiesLoose18(e, t);
    if (Object.getOwnPropertySymbols) {
      var n = Object.getOwnPropertySymbols(e);
      for (r2 = 0; r2 < n.length; r2++) o = n[r2], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
    }
    return i;
  }
  function _objectWithoutPropertiesLoose18(r2, e) {
    if (null == r2) return {};
    var t = {};
    for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
      if (-1 !== e.indexOf(n)) continue;
      t[n] = r2[n];
    }
    return t;
  }
  var defaultBarShape = Rectangle;
  function BarRectangle(_ref2) {
    var option = _ref2.option, shapeProps = _objectWithoutProperties18(_ref2, _excluded18);
    return /* @__PURE__ */ React35.createElement(Shape, {
      option,
      DefaultShape: defaultBarShape,
      shapeProps,
      activeClassName: "recharts-active-bar",
      inActiveClassName: "recharts-inactive-bar"
    });
  }
  var minPointSizeCallback = function minPointSizeCallback2(minPointSize) {
    var defaultValue = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 0;
    return (value2, index2) => {
      if (isNumber(minPointSize)) return minPointSize;
      var isValueNumberOrNil = isNumber(value2) || isNullish(value2);
      if (isValueNumberOrNil) {
        return minPointSize(value2, index2);
      }
      !isValueNumberOrNil ? true ? invariant(false, "minPointSize callback function received a value with type of ".concat(typeof value2, ". Currently only numbers or null/undefined are supported.")) : invariant(false) : void 0;
      return defaultValue;
    };
  };

  // node_modules/recharts/es6/state/selectors/barSelectors.js
  var pickIsPanorama = (_state, _id, isPanorama) => isPanorama;
  var pickBarId = (_state, id) => id;
  var selectSynchronisedBarSettings = createSelector([selectUnfilteredCartesianItems, pickBarId], (graphicalItems, id) => graphicalItems.filter((item) => item.type === "bar").find((item) => item.id === id));
  var selectMaxBarSize = createSelector([selectSynchronisedBarSettings], (barSettings) => barSettings === null || barSettings === void 0 ? void 0 : barSettings.maxBarSize);
  var pickCells2 = (_state, _id, _isPanorama, cells) => cells;
  var selectAllVisibleBars = createSelector([selectChartLayout, selectUnfilteredCartesianItems, selectXAxisIdFromGraphicalItemId, selectYAxisIdFromGraphicalItemId, pickIsPanorama], (layout, allItems, xAxisId, yAxisId, isPanorama) => allItems.filter((i) => {
    if (layout === "horizontal") {
      return i.xAxisId === xAxisId;
    }
    return i.yAxisId === yAxisId;
  }).filter((i) => i.isPanorama === isPanorama).filter((i) => i.hide === false).filter((i) => i.type === "bar"));
  var selectBarStackGroups = (state, id, isPanorama) => {
    var layout = selectChartLayout(state);
    var xAxisId = selectXAxisIdFromGraphicalItemId(state, id);
    var yAxisId = selectYAxisIdFromGraphicalItemId(state, id);
    if (xAxisId == null || yAxisId == null) {
      return void 0;
    }
    if (layout === "horizontal") {
      return selectStackGroups(state, "yAxis", yAxisId, isPanorama);
    }
    return selectStackGroups(state, "xAxis", xAxisId, isPanorama);
  };
  var selectBarCartesianAxisSize = (state, id) => {
    var layout = selectChartLayout(state);
    var xAxisId = selectXAxisIdFromGraphicalItemId(state, id);
    var yAxisId = selectYAxisIdFromGraphicalItemId(state, id);
    if (xAxisId == null || yAxisId == null) {
      return void 0;
    }
    if (layout === "horizontal") {
      return selectCartesianAxisSize(state, "xAxis", xAxisId);
    }
    return selectCartesianAxisSize(state, "yAxis", yAxisId);
  };
  var selectBarSizeList = createSelector([selectAllVisibleBars, selectRootBarSize, selectBarCartesianAxisSize], combineBarSizeList);
  var selectBarBandSize = (state, id, isPanorama) => {
    var _ref2, _getBandSizeOfAxis;
    var barSettings = selectSynchronisedBarSettings(state, id);
    if (barSettings == null) {
      return 0;
    }
    var xAxisId = selectXAxisIdFromGraphicalItemId(state, id);
    var yAxisId = selectYAxisIdFromGraphicalItemId(state, id);
    if (xAxisId == null || yAxisId == null) {
      return 0;
    }
    var layout = selectChartLayout(state);
    var globalMaxBarSize = selectRootMaxBarSize(state);
    var childMaxBarSize = barSettings.maxBarSize;
    var maxBarSize = isNullish(childMaxBarSize) ? globalMaxBarSize : childMaxBarSize;
    var axis, ticks;
    if (layout === "horizontal") {
      axis = selectAxisWithScale(state, "xAxis", xAxisId, isPanorama);
      ticks = selectTicksOfGraphicalItem(state, "xAxis", xAxisId, isPanorama);
    } else {
      axis = selectAxisWithScale(state, "yAxis", yAxisId, isPanorama);
      ticks = selectTicksOfGraphicalItem(state, "yAxis", yAxisId, isPanorama);
    }
    return (_ref2 = (_getBandSizeOfAxis = getBandSizeOfAxis(axis, ticks, true)) !== null && _getBandSizeOfAxis !== void 0 ? _getBandSizeOfAxis : maxBarSize) !== null && _ref2 !== void 0 ? _ref2 : 0;
  };
  var selectAxisBandSize = (state, id, isPanorama) => {
    var layout = selectChartLayout(state);
    var xAxisId = selectXAxisIdFromGraphicalItemId(state, id);
    var yAxisId = selectYAxisIdFromGraphicalItemId(state, id);
    if (xAxisId == null || yAxisId == null) {
      return void 0;
    }
    var axis, ticks;
    if (layout === "horizontal") {
      axis = selectAxisWithScale(state, "xAxis", xAxisId, isPanorama);
      ticks = selectTicksOfGraphicalItem(state, "xAxis", xAxisId, isPanorama);
    } else {
      axis = selectAxisWithScale(state, "yAxis", yAxisId, isPanorama);
      ticks = selectTicksOfGraphicalItem(state, "yAxis", yAxisId, isPanorama);
    }
    return getBandSizeOfAxis(axis, ticks);
  };
  var selectAllBarPositions = createSelector([selectBarSizeList, selectRootMaxBarSize, selectBarGap, selectBarCategoryGap, selectBarBandSize, selectAxisBandSize, selectMaxBarSize], combineAllBarPositions);
  var selectXAxisWithScale3 = (state, id, isPanorama) => {
    var xAxisId = selectXAxisIdFromGraphicalItemId(state, id);
    if (xAxisId == null) {
      return void 0;
    }
    return selectAxisWithScale(state, "xAxis", xAxisId, isPanorama);
  };
  var selectYAxisWithScale3 = (state, id, isPanorama) => {
    var yAxisId = selectYAxisIdFromGraphicalItemId(state, id);
    if (yAxisId == null) {
      return void 0;
    }
    return selectAxisWithScale(state, "yAxis", yAxisId, isPanorama);
  };
  var selectXAxisTicks3 = (state, id, isPanorama) => {
    var xAxisId = selectXAxisIdFromGraphicalItemId(state, id);
    if (xAxisId == null) {
      return void 0;
    }
    return selectTicksOfGraphicalItem(state, "xAxis", xAxisId, isPanorama);
  };
  var selectYAxisTicks3 = (state, id, isPanorama) => {
    var yAxisId = selectYAxisIdFromGraphicalItemId(state, id);
    if (yAxisId == null) {
      return void 0;
    }
    return selectTicksOfGraphicalItem(state, "yAxis", yAxisId, isPanorama);
  };
  var selectBarPosition = createSelector([selectAllBarPositions, selectSynchronisedBarSettings], combineBarPosition);
  var selectStackedDataOfItem = createSelector([selectBarStackGroups, selectSynchronisedBarSettings], combineStackedData);
  var selectBarRectangles = createSelector([selectChartOffsetInternal, selectAxisViewBox, selectXAxisWithScale3, selectYAxisWithScale3, selectXAxisTicks3, selectYAxisTicks3, selectBarPosition, selectChartLayout, selectChartDataWithIndexesIfNotInPanoramaPosition3, selectAxisBandSize, selectStackedDataOfItem, selectSynchronisedBarSettings, pickCells2], (offset, axisViewBox, xAxis, yAxis, xAxisTicks, yAxisTicks, pos, layout, _ref2, bandSize, stackedData, barSettings, cells) => {
    var chartData = _ref2.chartData, dataStartIndex = _ref2.dataStartIndex, dataEndIndex = _ref2.dataEndIndex;
    if (barSettings == null || pos == null || axisViewBox == null || layout !== "horizontal" && layout !== "vertical" || xAxis == null || yAxis == null || xAxisTicks == null || yAxisTicks == null || bandSize == null) {
      return void 0;
    }
    var data = barSettings.data;
    var displayedData;
    if (data != null && data.length > 0) {
      displayedData = data;
    } else {
      displayedData = chartData === null || chartData === void 0 ? void 0 : chartData.slice(dataStartIndex, dataEndIndex + 1);
    }
    if (displayedData == null) {
      return void 0;
    }
    return computeBarRectangles({
      layout,
      barSettings,
      pos,
      parentViewBox: axisViewBox,
      bandSize,
      xAxis,
      yAxis,
      xAxisTicks,
      yAxisTicks,
      stackedData,
      displayedData,
      offset,
      cells,
      dataStartIndex
    });
  });

  // node_modules/recharts/es6/cartesian/BarStack.js
  var React36 = __toESM(require_react());
  var import_react47 = __toESM(require_react());
  var _excluded19 = ["index"];
  function _extends22() {
    return _extends22 = Object.assign ? Object.assign.bind() : function(n) {
      for (var e = 1; e < arguments.length; e++) {
        var t = arguments[e];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends22.apply(null, arguments);
  }
  function _objectWithoutProperties19(e, t) {
    if (null == e) return {};
    var o, r2, i = _objectWithoutPropertiesLoose19(e, t);
    if (Object.getOwnPropertySymbols) {
      var n = Object.getOwnPropertySymbols(e);
      for (r2 = 0; r2 < n.length; r2++) o = n[r2], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
    }
    return i;
  }
  function _objectWithoutPropertiesLoose19(r2, e) {
    if (null == r2) return {};
    var t = {};
    for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
      if (-1 !== e.indexOf(n)) continue;
      t[n] = r2[n];
    }
    return t;
  }
  var BarStackContext = /* @__PURE__ */ (0, import_react47.createContext)(void 0);
  var useStackId = (childStackId) => {
    var stackSettings = (0, import_react47.useContext)(BarStackContext);
    if (stackSettings != null) {
      return stackSettings.stackId;
    }
    if (childStackId == null) {
      return void 0;
    }
    return getNormalizedStackId(childStackId);
  };
  var getClipPathId = (stackId, index2) => {
    return "recharts-bar-stack-clip-path-".concat(stackId, "-").concat(index2);
  };
  var useBarStackClipPathUrl = (index2) => {
    var barStackContext = (0, import_react47.useContext)(BarStackContext);
    if (barStackContext == null) {
      return void 0;
    }
    var stackId = barStackContext.stackId;
    return "url(#".concat(getClipPathId(stackId, index2), ")");
  };
  var BarStackClipLayer = (_ref2) => {
    var index2 = _ref2.index, rest = _objectWithoutProperties19(_ref2, _excluded19);
    var clipPathUrl = useBarStackClipPathUrl(index2);
    return /* @__PURE__ */ React36.createElement(Layer, _extends22({
      className: "recharts-bar-stack-layer",
      clipPath: clipPathUrl
    }, rest));
  };

  // node_modules/recharts/es6/cartesian/Bar.js
  var _excluded20 = ["onMouseEnter", "onMouseLeave", "onClick"];
  var _excluded211 = ["value", "background", "tooltipPosition"];
  var _excluded35 = ["id"];
  var _excluded44 = ["onMouseEnter", "onClick", "onMouseLeave"];
  function _slicedToArray19(r2, e) {
    return _arrayWithHoles19(r2) || _iterableToArrayLimit19(r2, e) || _unsupportedIterableToArray19(r2, e) || _nonIterableRest19();
  }
  function _nonIterableRest19() {
    throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  function _unsupportedIterableToArray19(r2, a2) {
    if (r2) {
      if ("string" == typeof r2) return _arrayLikeToArray19(r2, a2);
      var t = {}.toString.call(r2).slice(8, -1);
      return "Object" === t && r2.constructor && (t = r2.constructor.name), "Map" === t || "Set" === t ? Array.from(r2) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray19(r2, a2) : void 0;
    }
  }
  function _arrayLikeToArray19(r2, a2) {
    (null == a2 || a2 > r2.length) && (a2 = r2.length);
    for (var e = 0, n = Array(a2); e < a2; e++) n[e] = r2[e];
    return n;
  }
  function _iterableToArrayLimit19(r2, l) {
    var t = null == r2 ? null : "undefined" != typeof Symbol && r2[Symbol.iterator] || r2["@@iterator"];
    if (null != t) {
      var e, n, i, u, a2 = [], f = true, o = false;
      try {
        if (i = (t = t.call(r2)).next, 0 === l) {
          if (Object(t) !== t) return;
          f = false;
        } else for (; !(f = (e = i.call(t)).done) && (a2.push(e.value), a2.length !== l); f = true) ;
      } catch (r3) {
        o = true, n = r3;
      } finally {
        try {
          if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return;
        } finally {
          if (o) throw n;
        }
      }
      return a2;
    }
  }
  function _arrayWithHoles19(r2) {
    if (Array.isArray(r2)) return r2;
  }
  function _extends23() {
    return _extends23 = Object.assign ? Object.assign.bind() : function(n) {
      for (var e = 1; e < arguments.length; e++) {
        var t = arguments[e];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends23.apply(null, arguments);
  }
  function ownKeys36(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread36(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys36(Object(t), true).forEach(function(r3) {
        _defineProperty39(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys36(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty39(e, r2, t) {
    return (r2 = _toPropertyKey39(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey39(t) {
    var i = _toPrimitive39(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive39(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  function _objectWithoutProperties20(e, t) {
    if (null == e) return {};
    var o, r2, i = _objectWithoutPropertiesLoose20(e, t);
    if (Object.getOwnPropertySymbols) {
      var n = Object.getOwnPropertySymbols(e);
      for (r2 = 0; r2 < n.length; r2++) o = n[r2], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
    }
    return i;
  }
  function _objectWithoutPropertiesLoose20(r2, e) {
    if (null == r2) return {};
    var t = {};
    for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
      if (-1 !== e.indexOf(n)) continue;
      t[n] = r2[n];
    }
    return t;
  }
  var computeLegendPayloadFromBarData = (props) => {
    var dataKey = props.dataKey, name = props.name, fill = props.fill, legendType = props.legendType, hide = props.hide;
    return [{
      inactive: hide,
      dataKey,
      type: legendType,
      color: fill,
      value: getTooltipNameProp(name, dataKey),
      payload: props
    }];
  };
  var SetBarTooltipEntrySettings = /* @__PURE__ */ React37.memo((_ref2) => {
    var dataKey = _ref2.dataKey, stroke = _ref2.stroke, strokeWidth = _ref2.strokeWidth, fill = _ref2.fill, name = _ref2.name, hide = _ref2.hide, unit2 = _ref2.unit, formatter = _ref2.formatter, tooltipType = _ref2.tooltipType, id = _ref2.id;
    var tooltipEntrySettings = {
      dataDefinedOnItem: void 0,
      getPosition: noop,
      settings: {
        stroke,
        strokeWidth,
        fill,
        dataKey,
        nameKey: void 0,
        name: getTooltipNameProp(name, dataKey),
        hide,
        type: tooltipType,
        color: fill,
        unit: unit2,
        formatter,
        graphicalItemId: id
      }
    };
    return /* @__PURE__ */ React37.createElement(SetTooltipEntrySettings, {
      tooltipEntrySettings
    });
  });
  function BarBackground(props) {
    var activeIndex = useAppSelector(selectActiveTooltipIndex);
    var data = props.data, dataKey = props.dataKey, backgroundFromProps = props.background, allOtherBarProps = props.allOtherBarProps;
    var onMouseEnterFromProps = allOtherBarProps.onMouseEnter, onMouseLeaveFromProps = allOtherBarProps.onMouseLeave, onItemClickFromProps = allOtherBarProps.onClick, restOfAllOtherProps = _objectWithoutProperties20(allOtherBarProps, _excluded20);
    var onMouseEnterFromContext = useMouseEnterItemDispatch(onMouseEnterFromProps, dataKey, allOtherBarProps.id);
    var onMouseLeaveFromContext = useMouseLeaveItemDispatch(onMouseLeaveFromProps);
    var onClickFromContext = useMouseClickItemDispatch(onItemClickFromProps, dataKey, allOtherBarProps.id);
    if (!backgroundFromProps || data == null) {
      return null;
    }
    var backgroundProps = svgPropertiesNoEventsFromUnknown(backgroundFromProps);
    return /* @__PURE__ */ React37.createElement(ZIndexLayer, {
      zIndex: getZIndexFromUnknown(backgroundFromProps, DefaultZIndexes.barBackground)
    }, data.map((entry, i) => {
      var value2 = entry.value, backgroundFromDataEntry = entry.background, tooltipPosition = entry.tooltipPosition, rest = _objectWithoutProperties20(entry, _excluded211);
      if (!backgroundFromDataEntry) {
        return null;
      }
      var onMouseEnter = onMouseEnterFromContext(entry, entry.originalDataIndex);
      var onMouseLeave = onMouseLeaveFromContext(entry, entry.originalDataIndex);
      var onClick = onClickFromContext(entry, entry.originalDataIndex);
      var barRectangleProps = _objectSpread36(_objectSpread36(_objectSpread36(_objectSpread36(_objectSpread36({
        option: backgroundFromProps,
        isActive: String(entry.originalDataIndex) === activeIndex
      }, rest), {}, {
        // @ts-expect-error backgroundProps is contributing unknown props
        fill: "#eee"
      }, backgroundFromDataEntry), backgroundProps), adaptEventsOfChild(restOfAllOtherProps, entry, i)), {}, {
        onMouseEnter,
        onMouseLeave,
        onClick,
        dataKey,
        index: i,
        className: "recharts-bar-background-rectangle"
      });
      return /* @__PURE__ */ React37.createElement(BarRectangle, _extends23({
        key: "background-bar-".concat(i)
      }, barRectangleProps));
    }));
  }
  function BarLabelListProvider(_ref2) {
    var showLabels = _ref2.showLabels, children = _ref2.children, rects = _ref2.rects;
    var labelListEntries = rects === null || rects === void 0 ? void 0 : rects.map((entry) => {
      var viewBox = {
        x: entry.x,
        y: entry.y,
        width: entry.width,
        lowerWidth: entry.width,
        upperWidth: entry.width,
        height: entry.height
      };
      return _objectSpread36(_objectSpread36({}, viewBox), {}, {
        value: entry.value,
        payload: entry.payload,
        parentViewBox: entry.parentViewBox,
        viewBox,
        fill: entry.fill
      });
    });
    return /* @__PURE__ */ React37.createElement(CartesianLabelListContextProvider, {
      value: showLabels ? labelListEntries : void 0
    }, children);
  }
  function BarRectangleWithActiveState(props) {
    var shape = props.shape, activeBar = props.activeBar, baseProps = props.baseProps, entry = props.entry, index2 = props.index, dataKey = props.dataKey;
    var activeIndex = useAppSelector(selectActiveTooltipIndex);
    var activeDataKey = useAppSelector(selectActiveTooltipDataKey);
    var isActive = activeBar && String(entry.originalDataIndex) === activeIndex && (activeDataKey == null || dataKey === activeDataKey);
    var _useState = (0, import_react48.useState)(false), _useState2 = _slicedToArray19(_useState, 2), stayInLayer = _useState2[0], setStayInLayer = _useState2[1];
    var _useState3 = (0, import_react48.useState)(false), _useState4 = _slicedToArray19(_useState3, 2), hasMountedActive = _useState4[0], setHasMountedActive = _useState4[1];
    (0, import_react48.useEffect)(() => {
      var rafId4;
      if (isActive) {
        setStayInLayer(true);
        rafId4 = requestAnimationFrame(() => {
          setHasMountedActive(true);
        });
      } else {
        setHasMountedActive(false);
      }
      return () => {
        cancelAnimationFrame(rafId4);
      };
    }, [isActive]);
    var handleTransitionEnd = (0, import_react48.useCallback)(() => {
      if (!isActive) {
        setStayInLayer(false);
      }
    }, [isActive]);
    var isVisuallyActive = isActive && hasMountedActive;
    var shouldRenderInLayer = isActive || stayInLayer;
    var option;
    if (isActive) {
      if (activeBar === true) {
        option = shape;
      } else {
        option = activeBar;
      }
    } else {
      option = shape;
    }
    var content = /* @__PURE__ */ React37.createElement(BarRectangle, _extends23({}, baseProps, {
      name: String(baseProps.name)
    }, entry, {
      isActive: isVisuallyActive,
      option,
      index: index2,
      dataKey,
      animationElapsedTime: props.animationElapsedTime,
      isAnimating: props.isAnimating,
      isEntrance: props.isEntrance,
      onTransitionEnd: handleTransitionEnd
    }));
    if (shouldRenderInLayer) {
      return /* @__PURE__ */ React37.createElement(ZIndexLayer, {
        zIndex: DefaultZIndexes.activeBar
      }, /* @__PURE__ */ React37.createElement(BarStackClipLayer, {
        index: entry.originalDataIndex
      }, content));
    }
    return content;
  }
  function BarRectangleNeverActive(props) {
    var shape = props.shape, baseProps = props.baseProps, entry = props.entry, index2 = props.index, dataKey = props.dataKey;
    return /* @__PURE__ */ React37.createElement(BarRectangle, _extends23({}, baseProps, {
      name: String(baseProps.name)
    }, entry, {
      isActive: false,
      option: shape,
      index: index2,
      dataKey,
      animationElapsedTime: props.animationElapsedTime,
      isAnimating: props.isAnimating,
      isEntrance: props.isEntrance
    }));
  }
  function BarRectangles(_ref3) {
    var _svgPropertiesNoEvent;
    var data = _ref3.data, props = _ref3.props, animationElapsedTime = _ref3.animationElapsedTime, isAnimating = _ref3.isAnimating, isEntrance = _ref3.isEntrance;
    var _ref4 = (_svgPropertiesNoEvent = svgPropertiesNoEvents(props)) !== null && _svgPropertiesNoEvent !== void 0 ? _svgPropertiesNoEvent : {}, id = _ref4.id, baseProps = _objectWithoutProperties20(_ref4, _excluded35);
    var shape = props.shape, dataKey = props.dataKey, activeBar = props.activeBar;
    var onMouseEnterFromProps = props.onMouseEnter, onItemClickFromProps = props.onClick, onMouseLeaveFromProps = props.onMouseLeave, restOfAllOtherProps = _objectWithoutProperties20(props, _excluded44);
    var onMouseEnterFromContext = useMouseEnterItemDispatch(onMouseEnterFromProps, dataKey, id);
    var onMouseLeaveFromContext = useMouseLeaveItemDispatch(onMouseLeaveFromProps);
    var onClickFromContext = useMouseClickItemDispatch(onItemClickFromProps, dataKey, id);
    if (!data) {
      return null;
    }
    return /* @__PURE__ */ React37.createElement(React37.Fragment, null, data.map((entry, i) => {
      return /* @__PURE__ */ React37.createElement(BarStackClipLayer, _extends23({
        index: entry.originalDataIndex,
        key: "rectangle-".concat(entry === null || entry === void 0 ? void 0 : entry.x, "-").concat(entry === null || entry === void 0 ? void 0 : entry.y, "-").concat(entry === null || entry === void 0 ? void 0 : entry.value, "-").concat(i),
        className: "recharts-bar-rectangle"
      }, adaptEventsOfChild(restOfAllOtherProps, entry, i), {
        onMouseEnter: onMouseEnterFromContext(entry, entry.originalDataIndex),
        onMouseLeave: onMouseLeaveFromContext(entry, entry.originalDataIndex),
        onClick: onClickFromContext(entry, entry.originalDataIndex)
      }), activeBar ? /* @__PURE__ */ React37.createElement(BarRectangleWithActiveState, {
        shape,
        activeBar,
        baseProps,
        entry,
        index: i,
        dataKey,
        animationElapsedTime,
        isAnimating,
        isEntrance
      }) : (
        /*
         * If the `activeBar` prop is falsy, then let's call the variant without hooks.
         * Using the `selectActiveTooltipIndex` selector is usually fast
         * but in charts with large-ish amount of data even the few nanoseconds add up to a noticeable jank.
         * If the activeBar is false then we don't need to know which index is active - because we won't use it anyway.
         * So let's just skip the hooks altogether. That way, React can skip rendering the component,
         * and can skip the tree reconciliation for its children too.
         * Because we can't call hooks conditionally, we need to have a separate component for that.
         */
        /* @__PURE__ */ React37.createElement(BarRectangleNeverActive, {
          shape,
          baseProps,
          entry,
          index: i,
          dataKey,
          animationElapsedTime,
          isAnimating,
          isEntrance
        })
      ));
    }));
  }
  var defaultBarAnimateItems = (items, animationElapsedTime, layout) => {
    if (items == null) return [];
    if (animationElapsedTime === 1) {
      return items.flatMap((item) => item.status === "removed" ? [] : [item.next]);
    }
    return items.flatMap((item) => {
      if (item.status === "removed") {
        if (layout === "horizontal") {
          return [_objectSpread36(_objectSpread36({}, item.prev), {}, {
            height: interpolate(item.prev.height, 0, animationElapsedTime),
            y: interpolate(item.prev.y, item.prev.y + item.prev.height, animationElapsedTime)
          })];
        }
        return [_objectSpread36(_objectSpread36({}, item.prev), {}, {
          width: interpolate(item.prev.width, 0, animationElapsedTime)
        })];
      }
      if (item.status === "matched") {
        return [_objectSpread36(_objectSpread36({}, item.next), {}, {
          x: interpolate(item.prev.x, item.next.x, animationElapsedTime),
          y: interpolate(item.prev.y, item.next.y, animationElapsedTime),
          width: interpolate(item.prev.width, item.next.width, animationElapsedTime),
          height: interpolate(item.prev.height, item.next.height, animationElapsedTime)
        })];
      }
      var next = item.next;
      if (layout === "horizontal") {
        return [_objectSpread36(_objectSpread36({}, next), {}, {
          height: interpolate(0, next.height, animationElapsedTime),
          y: interpolate(next.stackedBarStart, next.y, animationElapsedTime)
        })];
      }
      return [_objectSpread36(_objectSpread36({}, next), {}, {
        width: interpolate(0, next.width, animationElapsedTime),
        x: interpolate(next.stackedBarStart, next.x, animationElapsedTime)
      })];
    });
  };
  function RectanglesWithAnimation(_ref5) {
    var props = _ref5.props, previousRectanglesRef = _ref5.previousRectanglesRef;
    var data = props.data, isAnimationActive = props.isAnimationActive, animationBegin = props.animationBegin, animationDuration = props.animationDuration, animationEasing = props.animationEasing, animationInterpolateFn = props.animationInterpolateFn, layout = props.layout;
    var _useAnimationCallback = useAnimationCallbacks(props.onAnimationStart, props.onAnimationEnd), isAnimating = _useAnimationCallback.isAnimating, handleAnimationStart = _useAnimationCallback.handleAnimationStart, handleAnimationEnd = _useAnimationCallback.handleAnimationEnd;
    return /* @__PURE__ */ React37.createElement(BarLabelListProvider, {
      showLabels: !isAnimating,
      rects: data
    }, /* @__PURE__ */ React37.createElement(AnimatedItems, {
      animationInput: data,
      animationIdPrefix: "recharts-bar-",
      items: data,
      previousItemsRef: previousRectanglesRef,
      isAnimationActive,
      animationBegin,
      animationDuration,
      animationEasing,
      onAnimationStart: handleAnimationStart,
      onAnimationEnd: handleAnimationEnd,
      animationInterpolateFn,
      animationMatchBy: props.animationMatchBy,
      layout
    }, (stepData, animationElapsedTime, isEntrance) => /* @__PURE__ */ React37.createElement(Layer, null, /* @__PURE__ */ React37.createElement(BarRectangles, {
      props,
      data: stepData,
      animationElapsedTime,
      isAnimating: isAnimating || animationElapsedTime < 1,
      isEntrance
    }))), /* @__PURE__ */ React37.createElement(LabelListFromLabelProp, {
      label: props.label
    }), props.children);
  }
  function RenderRectangles(props) {
    var previousRectanglesRef = (0, import_react48.useRef)(null);
    return /* @__PURE__ */ React37.createElement(RectanglesWithAnimation, {
      previousRectanglesRef,
      props
    });
  }
  var defaultMinPointSize = 0;
  var errorBarDataPointFormatter2 = (dataPoint, dataKey) => {
    var value2 = Array.isArray(dataPoint.value) ? dataPoint.value[1] : dataPoint.value;
    return {
      x: dataPoint.x,
      y: dataPoint.y,
      value: value2,
      // getValueByDataKey does not validate the output type
      errorVal: getValueByDataKey(dataPoint, dataKey)
    };
  };
  var BarWithState = class extends import_react48.PureComponent {
    render() {
      var _this$props = this.props, hide = _this$props.hide, data = _this$props.data, dataKey = _this$props.dataKey, className = _this$props.className, xAxisId = _this$props.xAxisId, yAxisId = _this$props.yAxisId, needClip = _this$props.needClip, background = _this$props.background, id = _this$props.id;
      if (hide || data == null) {
        return null;
      }
      var layerClass = clsx("recharts-bar", className);
      var clipPathId = id;
      return /* @__PURE__ */ React37.createElement(Layer, {
        className: layerClass,
        id
      }, needClip && /* @__PURE__ */ React37.createElement("defs", null, /* @__PURE__ */ React37.createElement(GraphicalItemClipPath, {
        clipPathId,
        xAxisId,
        yAxisId
      })), /* @__PURE__ */ React37.createElement(Layer, {
        className: "recharts-bar-rectangles",
        clipPath: needClip ? "url(#clipPath-".concat(clipPathId, ")") : void 0
      }, /* @__PURE__ */ React37.createElement(BarBackground, {
        data,
        dataKey,
        background,
        allOtherBarProps: this.props
      }), /* @__PURE__ */ React37.createElement(RenderRectangles, this.props)));
    }
  };
  var defaultBarProps = {
    activeBar: false,
    animationBegin: 0,
    animationDuration: 400,
    animationEasing: "ease",
    animationInterpolateFn: defaultBarAnimateItems,
    animationMatchBy: matchAppend,
    background: false,
    hide: false,
    isAnimationActive: "auto",
    label: false,
    legendType: "rect",
    minPointSize: defaultMinPointSize,
    shape: defaultBarShape,
    xAxisId: 0,
    yAxisId: 0,
    zIndex: DefaultZIndexes.bar
  };
  function BarImpl(props) {
    var xAxisId = props.xAxisId, yAxisId = props.yAxisId, hide = props.hide, legendType = props.legendType, minPointSize = props.minPointSize, activeBar = props.activeBar, animationBegin = props.animationBegin, animationDuration = props.animationDuration, animationEasing = props.animationEasing, isAnimationActive = props.isAnimationActive;
    var _useNeedsClip = useNeedsClip(xAxisId, yAxisId), needClip = _useNeedsClip.needClip;
    var layout = useChartLayout();
    var isPanorama = useIsPanorama();
    var cells = findAllByType(props.children, Cell);
    var rects = useAppSelector((state) => selectBarRectangles(state, props.id, isPanorama, cells));
    if (layout !== "vertical" && layout !== "horizontal") {
      return null;
    }
    var errorBarOffset;
    var firstDataPoint = rects === null || rects === void 0 ? void 0 : rects[0];
    if (firstDataPoint == null || firstDataPoint.height == null || firstDataPoint.width == null) {
      errorBarOffset = 0;
    } else {
      errorBarOffset = layout === "vertical" ? firstDataPoint.height / 2 : firstDataPoint.width / 2;
    }
    return /* @__PURE__ */ React37.createElement(SetErrorBarContext, {
      xAxisId,
      yAxisId,
      data: rects,
      dataPointFormatter: errorBarDataPointFormatter2,
      errorBarOffset
    }, /* @__PURE__ */ React37.createElement(BarWithState, _extends23({}, props, {
      layout,
      needClip,
      data: rects,
      xAxisId,
      yAxisId,
      hide,
      legendType,
      minPointSize,
      activeBar,
      animationBegin,
      animationDuration,
      animationEasing,
      isAnimationActive
    })));
  }
  function computeBarRectangles(_ref6) {
    var layout = _ref6.layout, _ref6$barSettings = _ref6.barSettings, dataKey = _ref6$barSettings.dataKey, minPointSizeProp = _ref6$barSettings.minPointSize, hasCustomShape = _ref6$barSettings.hasCustomShape, pos = _ref6.pos, bandSize = _ref6.bandSize, xAxis = _ref6.xAxis, yAxis = _ref6.yAxis, xAxisTicks = _ref6.xAxisTicks, yAxisTicks = _ref6.yAxisTicks, stackedData = _ref6.stackedData, displayedData = _ref6.displayedData, offset = _ref6.offset, cells = _ref6.cells, parentViewBox = _ref6.parentViewBox, dataStartIndex = _ref6.dataStartIndex;
    var numericAxis = layout === "horizontal" ? yAxis : xAxis;
    var stackedDomain = stackedData ? numericAxis.scale.domain() : null;
    var baseValue = getBaseValueOfBar({
      numericAxis
    });
    var stackedBarStart = numericAxis.scale.map(baseValue);
    return displayedData.map((entry, index2) => {
      var value2, x5, y5, width, height, background;
      if (stackedData) {
        var untruncatedValue = stackedData[index2 + dataStartIndex];
        if (untruncatedValue == null) {
          return null;
        }
        value2 = truncateByDomain(untruncatedValue, stackedDomain);
      } else {
        value2 = getValueByDataKey(entry, dataKey);
        if (!Array.isArray(value2)) {
          value2 = [baseValue, value2];
        }
      }
      var minPointSize = minPointSizeCallback(minPointSizeProp, defaultMinPointSize)(value2[1], index2);
      if (layout === "horizontal") {
        var _ref7;
        var baseValueScale = yAxis.scale.map(value2[0]);
        var currentValueScale = yAxis.scale.map(value2[1]);
        if (baseValueScale == null || currentValueScale == null) {
          return null;
        }
        x5 = getCateCoordinateOfBar({
          axis: xAxis,
          ticks: xAxisTicks,
          bandSize,
          offset: pos.offset,
          entry,
          index: index2
        });
        y5 = (_ref7 = currentValueScale !== null && currentValueScale !== void 0 ? currentValueScale : baseValueScale) !== null && _ref7 !== void 0 ? _ref7 : void 0;
        width = pos.size;
        var computedHeight = baseValueScale - currentValueScale;
        height = isNan(computedHeight) ? 0 : computedHeight;
        background = {
          x: x5,
          y: offset.top,
          width,
          height: offset.height
        };
        if (Math.abs(minPointSize) > 0 && Math.abs(height) < Math.abs(minPointSize)) {
          var delta = mathSign(height || minPointSize) * (Math.abs(minPointSize) - Math.abs(height));
          y5 -= delta;
          height += delta;
        }
      } else {
        var _baseValueScale = xAxis.scale.map(value2[0]);
        var _currentValueScale = xAxis.scale.map(value2[1]);
        if (_baseValueScale == null || _currentValueScale == null) {
          return null;
        }
        x5 = _baseValueScale;
        y5 = getCateCoordinateOfBar({
          axis: yAxis,
          ticks: yAxisTicks,
          bandSize,
          offset: pos.offset,
          entry,
          index: index2
        });
        width = _currentValueScale - _baseValueScale;
        height = pos.size;
        background = {
          x: offset.left,
          y: y5,
          width: offset.width,
          height
        };
        if (Math.abs(minPointSize) > 0 && Math.abs(width) < Math.abs(minPointSize)) {
          var _delta = mathSign(width || minPointSize) * (Math.abs(minPointSize) - Math.abs(width));
          width += _delta;
        }
      }
      if (x5 == null || y5 == null || width == null || height == null || !hasCustomShape && (width === 0 || height === 0)) {
        return null;
      }
      var barRectangleItem = _objectSpread36(_objectSpread36({}, entry), {}, {
        stackedBarStart,
        x: x5,
        y: y5,
        width,
        height,
        value: stackedData ? value2 : value2[1],
        payload: entry,
        background,
        tooltipPosition: {
          x: x5 + width / 2,
          y: y5 + height / 2
        },
        parentViewBox,
        originalDataIndex: index2
      }, cells && cells[index2] && cells[index2].props);
      return barRectangleItem;
    }).filter(Boolean);
  }
  function BarFn(outsideProps) {
    var props = resolveDefaultProps(outsideProps, defaultBarProps);
    var stackId = useStackId(props.stackId);
    var isPanorama = useIsPanorama();
    return /* @__PURE__ */ React37.createElement(RegisterGraphicalItemId, {
      id: props.id,
      type: "bar"
    }, (id) => /* @__PURE__ */ React37.createElement(React37.Fragment, null, /* @__PURE__ */ React37.createElement(SetLegendPayload, {
      legendPayload: computeLegendPayloadFromBarData(props)
    }), /* @__PURE__ */ React37.createElement(SetBarTooltipEntrySettings, {
      dataKey: props.dataKey,
      stroke: props.stroke,
      strokeWidth: props.strokeWidth,
      fill: props.fill,
      name: props.name,
      hide: props.hide,
      unit: props.unit,
      formatter: props.formatter,
      tooltipType: props.tooltipType,
      id
    }), /* @__PURE__ */ React37.createElement(SetCartesianGraphicalItem, {
      type: "bar",
      id,
      data: void 0,
      xAxisId: props.xAxisId,
      yAxisId: props.yAxisId,
      zAxisId: 0,
      dataKey: props.dataKey,
      stackId,
      hide: props.hide,
      barSize: props.barSize,
      minPointSize: props.minPointSize,
      maxBarSize: props.maxBarSize,
      isPanorama,
      hasCustomShape: props.shape != null && props.shape !== defaultBarShape
    }), /* @__PURE__ */ React37.createElement(ZIndexLayer, {
      zIndex: props.zIndex
    }, /* @__PURE__ */ React37.createElement(BarImpl, _extends23({}, props, {
      id
    })))));
  }
  var Bar = /* @__PURE__ */ React37.memo(BarFn, propsAreEqual);
  Bar.displayName = "Bar";

  // node_modules/recharts/es6/cartesian/XAxis.js
  var React38 = __toESM(require_react());
  var import_react49 = __toESM(require_react());

  // node_modules/recharts/es6/util/axisPropsAreEqual.js
  var _excluded21 = ["domain", "range"];
  var _excluded212 = ["domain", "range"];
  function _objectWithoutProperties21(e, t) {
    if (null == e) return {};
    var o, r2, i = _objectWithoutPropertiesLoose21(e, t);
    if (Object.getOwnPropertySymbols) {
      var n = Object.getOwnPropertySymbols(e);
      for (r2 = 0; r2 < n.length; r2++) o = n[r2], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
    }
    return i;
  }
  function _objectWithoutPropertiesLoose21(r2, e) {
    if (null == r2) return {};
    var t = {};
    for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
      if (-1 !== e.indexOf(n)) continue;
      t[n] = r2[n];
    }
    return t;
  }
  function shortArraysAreEqual(arr1, arr2) {
    if (arr1 === arr2) {
      return true;
    }
    if (Array.isArray(arr1) && arr1.length === 2 && Array.isArray(arr2) && arr2.length === 2) {
      return arr1[0] === arr2[0] && arr1[1] === arr2[1];
    }
    return false;
  }
  function axisPropsAreEqual(prevProps, nextProps) {
    if (prevProps === nextProps) {
      return true;
    }
    var prevDomain = prevProps.domain, prevRange = prevProps.range, prevRest = _objectWithoutProperties21(prevProps, _excluded21);
    var nextDomain = nextProps.domain, nextRange = nextProps.range, nextRest = _objectWithoutProperties21(nextProps, _excluded212);
    if (!shortArraysAreEqual(prevDomain, nextDomain)) {
      return false;
    }
    if (!shortArraysAreEqual(prevRange, nextRange)) {
      return false;
    }
    return propsAreEqual(prevRest, nextRest);
  }

  // node_modules/recharts/es6/cartesian/XAxis.js
  var _excluded30 = ["type"];
  var _excluded213 = ["dangerouslySetInnerHTML", "ticks", "scale"];
  var _excluded36 = ["id", "scale"];
  function _extends24() {
    return _extends24 = Object.assign ? Object.assign.bind() : function(n) {
      for (var e = 1; e < arguments.length; e++) {
        var t = arguments[e];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends24.apply(null, arguments);
  }
  function ownKeys37(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread37(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys37(Object(t), true).forEach(function(r3) {
        _defineProperty40(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys37(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty40(e, r2, t) {
    return (r2 = _toPropertyKey40(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey40(t) {
    var i = _toPrimitive40(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive40(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  function _objectWithoutProperties22(e, t) {
    if (null == e) return {};
    var o, r2, i = _objectWithoutPropertiesLoose22(e, t);
    if (Object.getOwnPropertySymbols) {
      var n = Object.getOwnPropertySymbols(e);
      for (r2 = 0; r2 < n.length; r2++) o = n[r2], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
    }
    return i;
  }
  function _objectWithoutPropertiesLoose22(r2, e) {
    if (null == r2) return {};
    var t = {};
    for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
      if (-1 !== e.indexOf(n)) continue;
      t[n] = r2[n];
    }
    return t;
  }
  function SetXAxisSettings(props) {
    var dispatch2 = useAppDispatch();
    var prevSettingsRef = (0, import_react49.useRef)(null);
    var layout = useCartesianChartLayout();
    var typeFromProps = props.type, restProps = _objectWithoutProperties22(props, _excluded30);
    var evaluatedType = getAxisTypeBasedOnLayout(layout, "xAxis", typeFromProps);
    var settings = (0, import_react49.useMemo)(() => {
      if (evaluatedType == null) {
        return void 0;
      }
      return _objectSpread37(_objectSpread37({}, restProps), {}, {
        type: evaluatedType
      });
    }, [restProps, evaluatedType]);
    (0, import_react49.useLayoutEffect)(() => {
      if (settings == null) {
        return;
      }
      if (prevSettingsRef.current === null) {
        dispatch2(addXAxis(settings));
      } else if (prevSettingsRef.current !== settings) {
        dispatch2(replaceXAxis({
          prev: prevSettingsRef.current,
          next: settings
        }));
      }
      prevSettingsRef.current = settings;
    }, [settings, dispatch2]);
    (0, import_react49.useLayoutEffect)(() => {
      return () => {
        if (prevSettingsRef.current) {
          dispatch2(removeXAxis(prevSettingsRef.current));
          prevSettingsRef.current = null;
        }
      };
    }, [dispatch2]);
    return null;
  }
  var XAxisImpl = (props) => {
    var xAxisId = props.xAxisId, className = props.className;
    var viewBox = useAppSelector(selectAxisViewBox);
    var isPanorama = useIsPanorama();
    var axisType = "xAxis";
    var cartesianTickItems = useAppSelector((state) => selectTicksOfAxis(state, axisType, xAxisId, isPanorama));
    var axisSize = useAppSelector((state) => selectXAxisSize(state, xAxisId));
    var position = useAppSelector((state) => selectXAxisPosition(state, xAxisId));
    var synchronizedSettings = useAppSelector((state) => selectXAxisSettingsNoDefaults(state, xAxisId));
    if (axisSize == null || position == null || synchronizedSettings == null) {
      return null;
    }
    var dangerouslySetInnerHTML = props.dangerouslySetInnerHTML, ticks = props.ticks, del = props.scale, allOtherProps = _objectWithoutProperties22(props, _excluded213);
    var id = synchronizedSettings.id, del2 = synchronizedSettings.scale, restSynchronizedSettings = _objectWithoutProperties22(synchronizedSettings, _excluded36);
    return /* @__PURE__ */ React38.createElement(CartesianAxis, _extends24({}, allOtherProps, restSynchronizedSettings, {
      x: position.x,
      y: position.y,
      width: axisSize.width,
      height: axisSize.height,
      className: clsx("recharts-".concat(axisType, " ").concat(axisType), className),
      viewBox,
      ticks: cartesianTickItems,
      axisType,
      axisId: xAxisId
    }));
  };
  var xAxisDefaultProps = {
    allowDataOverflow: implicitXAxis.allowDataOverflow,
    allowDecimals: implicitXAxis.allowDecimals,
    allowDuplicatedCategory: implicitXAxis.allowDuplicatedCategory,
    angle: implicitXAxis.angle,
    axisLine: defaultCartesianAxisProps.axisLine,
    height: implicitXAxis.height,
    hide: false,
    includeHidden: implicitXAxis.includeHidden,
    interval: implicitXAxis.interval,
    label: false,
    minTickGap: implicitXAxis.minTickGap,
    mirror: implicitXAxis.mirror,
    orientation: implicitXAxis.orientation,
    padding: implicitXAxis.padding,
    reversed: implicitXAxis.reversed,
    scale: implicitXAxis.scale,
    tick: implicitXAxis.tick,
    tickCount: implicitXAxis.tickCount,
    tickLine: defaultCartesianAxisProps.tickLine,
    tickSize: defaultCartesianAxisProps.tickSize,
    type: implicitXAxis.type,
    niceTicks: implicitXAxis.niceTicks,
    xAxisId: 0
  };
  var XAxisSettingsDispatcher = (outsideProps) => {
    var props = resolveDefaultProps(outsideProps, xAxisDefaultProps);
    return /* @__PURE__ */ React38.createElement(React38.Fragment, null, /* @__PURE__ */ React38.createElement(SetXAxisSettings, {
      allowDataOverflow: props.allowDataOverflow,
      allowDecimals: props.allowDecimals,
      allowDuplicatedCategory: props.allowDuplicatedCategory,
      angle: props.angle,
      dataKey: props.dataKey,
      domain: props.domain,
      height: props.height,
      hide: props.hide,
      id: props.xAxisId,
      includeHidden: props.includeHidden,
      interval: props.interval,
      minTickGap: props.minTickGap,
      mirror: props.mirror,
      name: props.name,
      orientation: props.orientation,
      padding: props.padding,
      reversed: props.reversed,
      scale: props.scale,
      tick: props.tick,
      tickCount: props.tickCount,
      tickFormatter: props.tickFormatter,
      ticks: props.ticks,
      type: props.type,
      unit: props.unit,
      niceTicks: props.niceTicks
    }), /* @__PURE__ */ React38.createElement(XAxisImpl, props));
  };
  var XAxis = /* @__PURE__ */ React38.memo(XAxisSettingsDispatcher, axisPropsAreEqual);
  XAxis.displayName = "XAxis";

  // node_modules/recharts/es6/cartesian/YAxis.js
  var React39 = __toESM(require_react());
  var import_react50 = __toESM(require_react());
  var _excluded31 = ["type"];
  var _excluded214 = ["dangerouslySetInnerHTML", "ticks", "scale"];
  var _excluded37 = ["id", "scale"];
  function _extends25() {
    return _extends25 = Object.assign ? Object.assign.bind() : function(n) {
      for (var e = 1; e < arguments.length; e++) {
        var t = arguments[e];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends25.apply(null, arguments);
  }
  function ownKeys38(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread38(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys38(Object(t), true).forEach(function(r3) {
        _defineProperty41(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys38(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty41(e, r2, t) {
    return (r2 = _toPropertyKey41(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey41(t) {
    var i = _toPrimitive41(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive41(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  function _objectWithoutProperties23(e, t) {
    if (null == e) return {};
    var o, r2, i = _objectWithoutPropertiesLoose23(e, t);
    if (Object.getOwnPropertySymbols) {
      var n = Object.getOwnPropertySymbols(e);
      for (r2 = 0; r2 < n.length; r2++) o = n[r2], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
    }
    return i;
  }
  function _objectWithoutPropertiesLoose23(r2, e) {
    if (null == r2) return {};
    var t = {};
    for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
      if (-1 !== e.indexOf(n)) continue;
      t[n] = r2[n];
    }
    return t;
  }
  function SetYAxisSettings(props) {
    var dispatch2 = useAppDispatch();
    var prevSettingsRef = (0, import_react50.useRef)(null);
    var layout = useCartesianChartLayout();
    var typeFromProps = props.type, restProps = _objectWithoutProperties23(props, _excluded31);
    var evaluatedType = getAxisTypeBasedOnLayout(layout, "yAxis", typeFromProps);
    var settings = (0, import_react50.useMemo)(() => {
      if (evaluatedType == null) {
        return void 0;
      }
      return _objectSpread38(_objectSpread38({}, restProps), {}, {
        type: evaluatedType
      });
    }, [evaluatedType, restProps]);
    (0, import_react50.useLayoutEffect)(() => {
      if (settings == null) {
        return;
      }
      if (prevSettingsRef.current === null) {
        dispatch2(addYAxis(settings));
      } else if (prevSettingsRef.current !== settings) {
        dispatch2(replaceYAxis({
          prev: prevSettingsRef.current,
          next: settings
        }));
      }
      prevSettingsRef.current = settings;
    }, [settings, dispatch2]);
    (0, import_react50.useLayoutEffect)(() => {
      return () => {
        if (prevSettingsRef.current) {
          dispatch2(removeYAxis(prevSettingsRef.current));
          prevSettingsRef.current = null;
        }
      };
    }, [dispatch2]);
    return null;
  }
  function YAxisImpl(props) {
    var yAxisId = props.yAxisId, className = props.className, width = props.width, label = props.label;
    var cartesianAxisRef = (0, import_react50.useRef)(null);
    var labelRef = (0, import_react50.useRef)(null);
    var viewBox = useAppSelector(selectAxisViewBox);
    var isPanorama = useIsPanorama();
    var dispatch2 = useAppDispatch();
    var axisType = "yAxis";
    var axisSize = useAppSelector((state) => selectYAxisSize(state, yAxisId));
    var position = useAppSelector((state) => selectYAxisPosition(state, yAxisId));
    var cartesianTickItems = useAppSelector((state) => selectTicksOfAxis(state, axisType, yAxisId, isPanorama));
    var synchronizedSettings = useAppSelector((state) => selectYAxisSettingsNoDefaults(state, yAxisId));
    (0, import_react50.useLayoutEffect)(() => {
      if (width !== "auto" || !axisSize || isLabelContentAFunction(label) || /* @__PURE__ */ (0, import_react50.isValidElement)(label) || synchronizedSettings == null) {
        return;
      }
      var axisComponent = cartesianAxisRef.current;
      if (!axisComponent) {
        return;
      }
      var updatedYAxisWidth = axisComponent.getCalculatedWidth();
      if (Math.round(axisSize.width) !== Math.round(updatedYAxisWidth)) {
        dispatch2(updateYAxisWidth({
          id: yAxisId,
          width: updatedYAxisWidth
        }));
      }
    }, [
      // The dependency on cartesianAxisRef.current is not needed because useLayoutEffect will run after every render.
      // The ref will be populated by then.
      // To re-run this effect when ticks change, we can depend on the ticks array from the store.
      cartesianTickItems,
      axisSize,
      dispatch2,
      label,
      yAxisId,
      width,
      synchronizedSettings
    ]);
    if (axisSize == null || position == null || synchronizedSettings == null) {
      return null;
    }
    var dangerouslySetInnerHTML = props.dangerouslySetInnerHTML, ticks = props.ticks, del = props.scale, allOtherProps = _objectWithoutProperties23(props, _excluded214);
    var id = synchronizedSettings.id, del2 = synchronizedSettings.scale, restSynchronizedSettings = _objectWithoutProperties23(synchronizedSettings, _excluded37);
    return /* @__PURE__ */ React39.createElement(CartesianAxis, _extends25({}, allOtherProps, restSynchronizedSettings, {
      ref: cartesianAxisRef,
      labelRef,
      x: position.x,
      y: position.y,
      tickTextProps: width === "auto" ? {
        width: void 0
      } : {
        width
      },
      width: axisSize.width,
      height: axisSize.height,
      className: clsx("recharts-".concat(axisType, " ").concat(axisType), className),
      viewBox,
      ticks: cartesianTickItems,
      axisType,
      axisId: yAxisId
    }));
  }
  var yAxisDefaultProps = {
    allowDataOverflow: implicitYAxis.allowDataOverflow,
    allowDecimals: implicitYAxis.allowDecimals,
    allowDuplicatedCategory: implicitYAxis.allowDuplicatedCategory,
    angle: implicitYAxis.angle,
    axisLine: defaultCartesianAxisProps.axisLine,
    hide: false,
    includeHidden: implicitYAxis.includeHidden,
    interval: implicitYAxis.interval,
    label: false,
    minTickGap: implicitYAxis.minTickGap,
    mirror: implicitYAxis.mirror,
    orientation: implicitYAxis.orientation,
    padding: implicitYAxis.padding,
    reversed: implicitYAxis.reversed,
    scale: implicitYAxis.scale,
    tick: implicitYAxis.tick,
    tickCount: implicitYAxis.tickCount,
    tickLine: defaultCartesianAxisProps.tickLine,
    tickSize: defaultCartesianAxisProps.tickSize,
    type: implicitYAxis.type,
    niceTicks: implicitYAxis.niceTicks,
    width: implicitYAxis.width,
    yAxisId: 0
  };
  var YAxisSettingsDispatcher = (outsideProps) => {
    var props = resolveDefaultProps(outsideProps, yAxisDefaultProps);
    return /* @__PURE__ */ React39.createElement(React39.Fragment, null, /* @__PURE__ */ React39.createElement(SetYAxisSettings, {
      interval: props.interval,
      id: props.yAxisId,
      scale: props.scale,
      type: props.type,
      domain: props.domain,
      allowDataOverflow: props.allowDataOverflow,
      dataKey: props.dataKey,
      allowDuplicatedCategory: props.allowDuplicatedCategory,
      allowDecimals: props.allowDecimals,
      tickCount: props.tickCount,
      padding: props.padding,
      includeHidden: props.includeHidden,
      reversed: props.reversed,
      ticks: props.ticks,
      width: props.width,
      orientation: props.orientation,
      mirror: props.mirror,
      hide: props.hide,
      unit: props.unit,
      name: props.name,
      angle: props.angle,
      minTickGap: props.minTickGap,
      tick: props.tick,
      tickFormatter: props.tickFormatter,
      niceTicks: props.niceTicks
    }), /* @__PURE__ */ React39.createElement(YAxisImpl, props));
  };
  var YAxis = /* @__PURE__ */ React39.memo(YAxisSettingsDispatcher, axisPropsAreEqual);
  YAxis.displayName = "YAxis";

  // node_modules/recharts/es6/chart/LineChart.js
  var React46 = __toESM(require_react());
  var import_react61 = __toESM(require_react());

  // node_modules/recharts/es6/chart/CartesianChart.js
  var React45 = __toESM(require_react());
  var import_react60 = __toESM(require_react());

  // node_modules/recharts/es6/state/RechartsStoreProvider.js
  var React40 = __toESM(require_react());
  var import_react51 = __toESM(require_react());

  // node_modules/recharts/es6/state/selectors/selectActivePropsFromChartPointer.js
  var pickChartPointer = (_state, chartPointer) => chartPointer;
  var selectActivePropsFromChartPointer = createSelector([pickChartPointer, selectChartLayout, selectPolarViewBox, selectTooltipAxisType, selectTooltipAxisRangeWithReverse, selectTooltipAxisTicks, selectOrderedTooltipTicks, selectChartOffsetInternal], combineActiveProps);

  // node_modules/recharts/es6/util/getRelativeCoordinate.js
  function isSvgPointer(pointer) {
    return "getBBox" in pointer.currentTarget && typeof pointer.currentTarget.getBBox === "function";
  }
  function getRelativeCoordinate(event) {
    var rect = event.currentTarget.getBoundingClientRect();
    var scaleX, scaleY;
    if (isSvgPointer(event)) {
      var bbox = event.currentTarget.getBBox();
      scaleX = bbox.width > 0 ? rect.width / bbox.width : 1;
      scaleY = bbox.height > 0 ? rect.height / bbox.height : 1;
    } else {
      var element = event.currentTarget;
      scaleX = element.offsetWidth > 0 ? rect.width / element.offsetWidth : 1;
      scaleY = element.offsetHeight > 0 ? rect.height / element.offsetHeight : 1;
    }
    var getCoordinates = (clientX, clientY) => ({
      /*
       * Here it's important to use:
       * - event.clientX and event.clientY to get the mouse position relative to the viewport, including scroll.
       * - pageX and pageY are not used because they are relative to the whole document, and ignore scroll.
       * - rect.left and rect.top are used to get the position of the chart relative to the viewport.
       * - offsetX and offsetY are not used because they are relative to the offset parent
       *  which may or may not be the same as the clientX and clientY, depending on the position of the chart in the DOM
       *  and surrounding element styles. CSS position: relative, absolute, fixed, will change the offset parent.
       * - scaleX and scaleY are necessary for when the chart element is scaled using CSS `transform: scale(N)`.
       */
      relativeX: Math.round((clientX - rect.left) / scaleX),
      relativeY: Math.round((clientY - rect.top) / scaleY)
    });
    if ("touches" in event) {
      return Array.from(event.touches).map((touch) => getCoordinates(touch.clientX, touch.clientY));
    }
    return getCoordinates(event.clientX, event.clientY);
  }

  // node_modules/recharts/es6/state/mouseEventsMiddleware.js
  var mouseClickAction = createAction("mouseClick");
  var mouseClickMiddleware = createListenerMiddleware();
  mouseClickMiddleware.startListening({
    actionCreator: mouseClickAction,
    effect: (action, listenerApi) => {
      var mousePointer = action.payload;
      var activeProps = selectActivePropsFromChartPointer(listenerApi.getState(), getRelativeCoordinate(mousePointer));
      if ((activeProps === null || activeProps === void 0 ? void 0 : activeProps.activeIndex) != null) {
        listenerApi.dispatch(setMouseClickAxisIndex({
          activeIndex: activeProps.activeIndex,
          activeDataKey: void 0,
          activeCoordinate: activeProps.activeCoordinate
        }));
      }
    }
  });
  var mouseMoveAction = createAction("mouseMove");
  var mouseMoveMiddleware = createListenerMiddleware();
  var rafId = null;
  var timeoutId = null;
  var latestChartPointer = null;
  mouseMoveMiddleware.startListening({
    actionCreator: mouseMoveAction,
    effect: (action, listenerApi) => {
      var mousePointer = action.payload;
      var state = listenerApi.getState();
      var _state$eventSettings = state.eventSettings, throttleDelay = _state$eventSettings.throttleDelay, throttledEvents = _state$eventSettings.throttledEvents;
      var isThrottled = throttledEvents === "all" || (throttledEvents === null || throttledEvents === void 0 ? void 0 : throttledEvents.includes("mousemove"));
      if (rafId !== null) {
        cancelAnimationFrame(rafId);
        rafId = null;
      }
      if (timeoutId !== null && (typeof throttleDelay !== "number" || !isThrottled)) {
        clearTimeout(timeoutId);
        timeoutId = null;
      }
      latestChartPointer = getRelativeCoordinate(mousePointer);
      var callback = () => {
        var currentState = listenerApi.getState();
        var tooltipEventType = selectTooltipEventType(currentState, currentState.tooltip.settings.shared);
        if (!latestChartPointer) {
          rafId = null;
          timeoutId = null;
          return;
        }
        if (tooltipEventType === "axis") {
          var activeProps = selectActivePropsFromChartPointer(currentState, latestChartPointer);
          if ((activeProps === null || activeProps === void 0 ? void 0 : activeProps.activeIndex) != null) {
            listenerApi.dispatch(setMouseOverAxisIndex({
              activeIndex: activeProps.activeIndex,
              activeDataKey: void 0,
              activeCoordinate: activeProps.activeCoordinate
            }));
          } else {
            listenerApi.dispatch(mouseLeaveChart());
          }
        }
        rafId = null;
        timeoutId = null;
      };
      if (!isThrottled) {
        callback();
        return;
      }
      if (throttleDelay === "raf") {
        rafId = requestAnimationFrame(callback);
      } else if (typeof throttleDelay === "number") {
        if (timeoutId === null) {
          timeoutId = setTimeout(callback, throttleDelay);
        }
      }
    }
  });

  // node_modules/recharts/es6/state/reduxDevtoolsJsonStringifyReplacer.js
  function reduxDevtoolsJsonStringifyReplacer(key, value2) {
    if (value2 instanceof HTMLElement) {
      return "HTMLElement <".concat(value2.tagName, ' class="').concat(value2.className, '">');
    }
    if (value2 === window) {
      return "global.window";
    }
    if (key === "children" && typeof value2 === "object" && value2 !== null) {
      return "<<CHILDREN>>";
    }
    return value2;
  }

  // node_modules/recharts/es6/state/rootPropsSlice.js
  var initialState13 = {
    accessibilityLayer: true,
    barCategoryGap: "10%",
    barGap: 4,
    barSize: void 0,
    className: void 0,
    maxBarSize: void 0,
    stackOffset: "none",
    syncId: void 0,
    syncMethod: "index",
    baseValue: void 0,
    reverseStackOrder: false
  };
  var rootPropsSlice = createSlice({
    name: "rootProps",
    initialState: initialState13,
    reducers: {
      updateOptions: (state, action) => {
        var _action$payload$barGa;
        state.accessibilityLayer = action.payload.accessibilityLayer;
        state.barCategoryGap = action.payload.barCategoryGap;
        state.barGap = (_action$payload$barGa = action.payload.barGap) !== null && _action$payload$barGa !== void 0 ? _action$payload$barGa : initialState13.barGap;
        state.barSize = action.payload.barSize;
        state.maxBarSize = action.payload.maxBarSize;
        state.stackOffset = action.payload.stackOffset;
        state.syncId = action.payload.syncId;
        state.syncMethod = action.payload.syncMethod;
        state.className = action.payload.className;
        state.baseValue = action.payload.baseValue;
        state.reverseStackOrder = action.payload.reverseStackOrder;
      }
    }
  });
  var rootPropsReducer = rootPropsSlice.reducer;
  var updateOptions = rootPropsSlice.actions.updateOptions;

  // node_modules/recharts/es6/state/polarOptionsSlice.js
  var initialState14 = null;
  var reducers = {
    updatePolarOptions: (state, action) => {
      if (state === null) {
        return action.payload;
      }
      state.startAngle = action.payload.startAngle;
      state.endAngle = action.payload.endAngle;
      state.cx = action.payload.cx;
      state.cy = action.payload.cy;
      state.innerRadius = action.payload.innerRadius;
      state.outerRadius = action.payload.outerRadius;
      return state;
    }
  };
  var polarOptionsSlice = createSlice({
    name: "polarOptions",
    initialState: initialState14,
    reducers
  });
  var updatePolarOptions = polarOptionsSlice.actions.updatePolarOptions;
  var polarOptionsReducer = polarOptionsSlice.reducer;

  // node_modules/recharts/es6/state/keyboardEventsMiddleware.js
  var keyDownAction = createAction("keyDown");
  var focusAction = createAction("focus");
  var blurAction = createAction("blur");
  var keyboardEventsMiddleware = createListenerMiddleware();
  var rafId2 = null;
  var timeoutId2 = null;
  var latestKeyboardActionPayload = null;
  keyboardEventsMiddleware.startListening({
    actionCreator: keyDownAction,
    effect: (action, listenerApi) => {
      latestKeyboardActionPayload = action.payload;
      if (rafId2 !== null) {
        cancelAnimationFrame(rafId2);
        rafId2 = null;
      }
      var state = listenerApi.getState();
      var _state$eventSettings = state.eventSettings, throttleDelay = _state$eventSettings.throttleDelay, throttledEvents = _state$eventSettings.throttledEvents;
      var isThrottled = throttledEvents === "all" || throttledEvents.includes("keydown");
      if (timeoutId2 !== null && (typeof throttleDelay !== "number" || !isThrottled)) {
        clearTimeout(timeoutId2);
        timeoutId2 = null;
      }
      var callback = () => {
        try {
          var currentState = listenerApi.getState();
          var accessibilityLayerIsActive = currentState.rootProps.accessibilityLayer !== false;
          if (!accessibilityLayerIsActive) {
            return;
          }
          var keyboardInteraction = currentState.tooltip.keyboardInteraction;
          var key = latestKeyboardActionPayload;
          if (key !== "ArrowRight" && key !== "ArrowLeft" && key !== "Enter") {
            return;
          }
          var resolvedIndex = combineActiveTooltipIndex(keyboardInteraction, selectTooltipDisplayedData(currentState), selectTooltipAxisDataKey(currentState), selectTooltipAxisDomain(currentState));
          var currentIndex = resolvedIndex == null ? -1 : Number(resolvedIndex);
          var isOutsideDomain = !Number.isFinite(currentIndex) || currentIndex < 0;
          var tooltipTicks = selectTooltipAxisTicks(currentState);
          var displayedData = selectTooltipDisplayedData(currentState);
          var tooltipEventType = selectTooltipEventType(currentState, currentState.tooltip.settings.shared);
          if (key === "Enter") {
            if (isOutsideDomain) {
              return;
            }
            var _coordinate = selectCoordinateForDefaultIndex(currentState, tooltipEventType, "hover", String(keyboardInteraction.index));
            listenerApi.dispatch(setKeyboardInteraction({
              active: !keyboardInteraction.active,
              activeIndex: keyboardInteraction.index,
              activeCoordinate: _coordinate
            }));
            return;
          }
          var direction = selectChartDirection(currentState);
          var directionMultiplier = direction === "left-to-right" ? 1 : -1;
          var movement = key === "ArrowRight" ? 1 : -1;
          var nextIndex;
          if (isOutsideDomain) {
            var axisDataKey = selectTooltipAxisDataKey(currentState);
            var domain = selectTooltipAxisDomain(currentState);
            var effectiveMovement = movement * directionMultiplier;
            var mkInteraction = (i2) => ({
              active: false,
              index: String(i2),
              dataKey: void 0,
              graphicalItemId: void 0,
              coordinate: void 0
            });
            nextIndex = -1;
            if (effectiveMovement > 0) {
              for (var i = 0; i < displayedData.length; i++) {
                if (combineActiveTooltipIndex(mkInteraction(i), displayedData, axisDataKey, domain) != null) {
                  nextIndex = i;
                  break;
                }
              }
            } else {
              for (var _i = displayedData.length - 1; _i >= 0; _i--) {
                if (combineActiveTooltipIndex(mkInteraction(_i), displayedData, axisDataKey, domain) != null) {
                  nextIndex = _i;
                  break;
                }
              }
            }
            if (nextIndex < 0) {
              return;
            }
          } else {
            nextIndex = currentIndex + movement * directionMultiplier;
            var dataLength = (tooltipTicks === null || tooltipTicks === void 0 ? void 0 : tooltipTicks.length) || displayedData.length;
            if (dataLength === 0 || nextIndex >= dataLength || nextIndex < 0) {
              return;
            }
          }
          var coordinate = selectCoordinateForDefaultIndex(currentState, tooltipEventType, "hover", String(nextIndex));
          listenerApi.dispatch(setKeyboardInteraction({
            active: true,
            activeIndex: nextIndex.toString(),
            activeCoordinate: coordinate
          }));
        } finally {
          rafId2 = null;
          timeoutId2 = null;
        }
      };
      if (!isThrottled) {
        callback();
        return;
      }
      if (throttleDelay === "raf") {
        rafId2 = requestAnimationFrame(callback);
      } else if (typeof throttleDelay === "number") {
        if (timeoutId2 === null) {
          callback();
          latestKeyboardActionPayload = null;
          timeoutId2 = setTimeout(() => {
            if (latestKeyboardActionPayload) {
              callback();
            } else {
              timeoutId2 = null;
              rafId2 = null;
            }
          }, throttleDelay);
        }
      }
    }
  });
  keyboardEventsMiddleware.startListening({
    actionCreator: focusAction,
    effect: (_action, listenerApi) => {
      var state = listenerApi.getState();
      var accessibilityLayerIsActive = state.rootProps.accessibilityLayer !== false;
      if (!accessibilityLayerIsActive) {
        return;
      }
      var keyboardInteraction = state.tooltip.keyboardInteraction;
      if (keyboardInteraction.active) {
        return;
      }
      if (keyboardInteraction.index == null) {
        var nextIndex = "0";
        var tooltipEventType = selectTooltipEventType(state, state.tooltip.settings.shared);
        var coordinate = selectCoordinateForDefaultIndex(state, tooltipEventType, "hover", String(nextIndex));
        listenerApi.dispatch(setKeyboardInteraction({
          active: true,
          activeIndex: nextIndex,
          activeCoordinate: coordinate
        }));
      }
    }
  });
  keyboardEventsMiddleware.startListening({
    actionCreator: blurAction,
    effect: (_action, listenerApi) => {
      var state = listenerApi.getState();
      var accessibilityLayerIsActive = state.rootProps.accessibilityLayer !== false;
      if (!accessibilityLayerIsActive) {
        return;
      }
      var keyboardInteraction = state.tooltip.keyboardInteraction;
      if (keyboardInteraction.active) {
        listenerApi.dispatch(setKeyboardInteraction({
          active: false,
          activeIndex: keyboardInteraction.index,
          activeCoordinate: keyboardInteraction.coordinate
        }));
      }
    }
  });

  // node_modules/recharts/es6/util/createEventProxy.js
  function createEventProxy(reactEvent) {
    reactEvent.persist();
    var currentTarget = reactEvent.currentTarget;
    return new Proxy(reactEvent, {
      get: (target, prop) => {
        if (prop === "currentTarget") {
          return currentTarget;
        }
        var value2 = Reflect.get(target, prop);
        if (typeof value2 === "function") {
          return value2.bind(target);
        }
        return value2;
      }
    });
  }

  // node_modules/recharts/es6/state/externalEventsMiddleware.js
  var externalEventAction = createAction("externalEvent");
  var externalEventsMiddleware = createListenerMiddleware();
  var rafIdMap = /* @__PURE__ */ new Map();
  var timeoutIdMap = /* @__PURE__ */ new Map();
  var latestEventMap = /* @__PURE__ */ new Map();
  externalEventsMiddleware.startListening({
    actionCreator: externalEventAction,
    effect: (action, listenerApi) => {
      var _action$payload = action.payload, handler = _action$payload.handler, reactEvent = _action$payload.reactEvent;
      if (handler == null) {
        return;
      }
      var eventType = reactEvent.type;
      var eventProxy = createEventProxy(reactEvent);
      latestEventMap.set(eventType, {
        handler,
        reactEvent: eventProxy
      });
      var existingRafId = rafIdMap.get(eventType);
      if (existingRafId !== void 0) {
        cancelAnimationFrame(existingRafId);
        rafIdMap.delete(eventType);
      }
      var state = listenerApi.getState();
      var _state$eventSettings = state.eventSettings, throttleDelay = _state$eventSettings.throttleDelay, throttledEvents = _state$eventSettings.throttledEvents;
      var eventListAsString = throttledEvents;
      var isThrottled = eventListAsString === "all" || (eventListAsString === null || eventListAsString === void 0 ? void 0 : eventListAsString.includes(eventType));
      var existingTimeoutId = timeoutIdMap.get(eventType);
      if (existingTimeoutId !== void 0 && (typeof throttleDelay !== "number" || !isThrottled)) {
        clearTimeout(existingTimeoutId);
        timeoutIdMap.delete(eventType);
      }
      var callback = () => {
        var latestAction = latestEventMap.get(eventType);
        try {
          if (!latestAction) {
            return;
          }
          var latestHandler = latestAction.handler, latestEvent = latestAction.reactEvent;
          var currentState = listenerApi.getState();
          var nextState = {
            activeCoordinate: selectActiveTooltipCoordinate(currentState),
            activeDataKey: selectActiveTooltipDataKey(currentState),
            activeIndex: selectActiveTooltipIndex(currentState),
            activeLabel: selectActiveLabel(currentState),
            activeTooltipIndex: selectActiveTooltipIndex(currentState),
            isTooltipActive: selectIsTooltipActive(currentState)
          };
          if (latestHandler) {
            latestHandler(nextState, latestEvent);
          }
        } finally {
          rafIdMap.delete(eventType);
          timeoutIdMap.delete(eventType);
          latestEventMap.delete(eventType);
        }
      };
      if (!isThrottled) {
        callback();
        return;
      }
      if (throttleDelay === "raf") {
        var rafId4 = requestAnimationFrame(callback);
        rafIdMap.set(eventType, rafId4);
      } else if (typeof throttleDelay === "number") {
        if (!timeoutIdMap.has(eventType)) {
          callback();
          var timeoutId4 = setTimeout(callback, throttleDelay);
          timeoutIdMap.set(eventType, timeoutId4);
        }
      } else {
        callback();
      }
    }
  });

  // node_modules/recharts/es6/state/selectors/touchSelectors.js
  var selectAllTooltipPayloadConfiguration = createSelector([selectTooltipState], (tooltipState) => tooltipState.tooltipItemPayloads);
  var selectTooltipCoordinate = createSelector([selectAllTooltipPayloadConfiguration, (_state, tooltipIndex) => tooltipIndex, (_state, _tooltipIndex, graphicalItemId) => graphicalItemId], (allTooltipConfigurations, tooltipIndex, graphicalItemId) => {
    if (tooltipIndex == null) {
      return void 0;
    }
    var mostRelevantTooltipConfiguration = allTooltipConfigurations.find((tooltipConfiguration) => {
      return tooltipConfiguration.settings.graphicalItemId === graphicalItemId;
    });
    if (mostRelevantTooltipConfiguration == null) {
      return void 0;
    }
    var getPosition = mostRelevantTooltipConfiguration.getPosition;
    if (getPosition == null) {
      return void 0;
    }
    return getPosition(tooltipIndex);
  });

  // node_modules/recharts/es6/state/touchEventsMiddleware.js
  var touchEventAction = createAction("touchMove");
  var touchEventMiddleware = createListenerMiddleware();
  var rafId3 = null;
  var timeoutId3 = null;
  var latestChartPointers = null;
  var latestTouchEvent = null;
  touchEventMiddleware.startListening({
    actionCreator: touchEventAction,
    effect: (action, listenerApi) => {
      var touchEvent = action.payload;
      if (touchEvent.touches == null || touchEvent.touches.length === 0) {
        return;
      }
      latestTouchEvent = createEventProxy(touchEvent);
      var state = listenerApi.getState();
      var _state$eventSettings = state.eventSettings, throttleDelay = _state$eventSettings.throttleDelay, throttledEvents = _state$eventSettings.throttledEvents;
      var isThrottled = throttledEvents === "all" || throttledEvents.includes("touchmove");
      if (rafId3 !== null) {
        cancelAnimationFrame(rafId3);
        rafId3 = null;
      }
      if (timeoutId3 !== null && (typeof throttleDelay !== "number" || !isThrottled)) {
        clearTimeout(timeoutId3);
        timeoutId3 = null;
      }
      latestChartPointers = Array.from(touchEvent.touches).map((touch) => getRelativeCoordinate({
        clientX: touch.clientX,
        clientY: touch.clientY,
        currentTarget: touchEvent.currentTarget
      }));
      var callback = () => {
        if (latestTouchEvent == null) {
          return;
        }
        var currentState = listenerApi.getState();
        var tooltipEventType = selectTooltipEventType(currentState, currentState.tooltip.settings.shared);
        if (tooltipEventType === "axis") {
          var _latestChartPointers;
          var latestTouchPointer = (_latestChartPointers = latestChartPointers) === null || _latestChartPointers === void 0 ? void 0 : _latestChartPointers[0];
          if (latestTouchPointer == null) {
            rafId3 = null;
            timeoutId3 = null;
            return;
          }
          var activeProps = selectActivePropsFromChartPointer(currentState, latestTouchPointer);
          if ((activeProps === null || activeProps === void 0 ? void 0 : activeProps.activeIndex) != null) {
            listenerApi.dispatch(setMouseOverAxisIndex({
              activeIndex: activeProps.activeIndex,
              activeDataKey: void 0,
              activeCoordinate: activeProps.activeCoordinate
            }));
          }
        } else if (tooltipEventType === "item") {
          var _target$getAttribute;
          var touch = latestTouchEvent.touches[0];
          if (document.elementFromPoint == null || touch == null) {
            return;
          }
          var target = document.elementFromPoint(touch.clientX, touch.clientY);
          if (!target || !target.getAttribute) {
            return;
          }
          var itemIndex = target.getAttribute(DATA_ITEM_INDEX_ATTRIBUTE_NAME);
          var graphicalItemId = (_target$getAttribute = target.getAttribute(DATA_ITEM_GRAPHICAL_ITEM_ID_ATTRIBUTE_NAME)) !== null && _target$getAttribute !== void 0 ? _target$getAttribute : void 0;
          var settings = selectAllGraphicalItemsSettings(currentState).find((item) => item.id === graphicalItemId);
          if (itemIndex == null || settings == null || graphicalItemId == null) {
            return;
          }
          var dataKey = settings.dataKey;
          var coordinate = selectTooltipCoordinate(currentState, itemIndex, graphicalItemId);
          listenerApi.dispatch(setActiveMouseOverItemIndex({
            activeDataKey: dataKey,
            activeIndex: itemIndex,
            activeCoordinate: coordinate,
            activeGraphicalItemId: graphicalItemId
          }));
        }
        rafId3 = null;
        timeoutId3 = null;
      };
      if (!isThrottled) {
        callback();
        return;
      }
      if (throttleDelay === "raf") {
        rafId3 = requestAnimationFrame(callback);
      } else if (typeof throttleDelay === "number") {
        if (timeoutId3 === null) {
          callback();
          latestTouchEvent = null;
          timeoutId3 = setTimeout(() => {
            if (latestTouchEvent) {
              callback();
            } else {
              timeoutId3 = null;
              rafId3 = null;
            }
          }, throttleDelay);
        }
      }
    }
  });

  // node_modules/recharts/es6/state/eventSettingsSlice.js
  var initialEventSettingsState = {
    throttleDelay: "raf",
    throttledEvents: ["mousemove", "touchmove", "pointermove", "scroll", "wheel"]
  };
  var eventSettingsSlice = createSlice({
    name: "eventSettings",
    initialState: initialEventSettingsState,
    reducers: {
      setEventSettings: (state, action) => {
        if (action.payload.throttleDelay != null) {
          state.throttleDelay = action.payload.throttleDelay;
        }
        if (action.payload.throttledEvents != null) {
          state.throttledEvents = castDraft(action.payload.throttledEvents);
        }
      }
    }
  });
  var setEventSettings = eventSettingsSlice.actions.setEventSettings;
  var eventSettingsReducer = eventSettingsSlice.reducer;

  // node_modules/recharts/es6/state/store.js
  var rootReducer = combineReducers({
    brush: brushReducer,
    cartesianAxis: cartesianAxisReducer,
    chartData: chartDataReducer,
    errorBars: errorBarReducer,
    eventSettings: eventSettingsReducer,
    graphicalItems: graphicalItemsReducer,
    layout: chartLayoutReducer,
    legend: legendReducer,
    options: optionsReducer,
    polarAxis: polarAxisReducer,
    polarOptions: polarOptionsReducer,
    referenceElements: referenceElementsReducer,
    renderedTicks: renderedTicksReducer,
    rootProps: rootPropsReducer,
    tooltip: tooltipReducer,
    zIndex: zIndexReducer
  });
  var createRechartsStore = function createRechartsStore2(preloadedState) {
    var chartName = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : "Chart";
    return configureStore({
      reducer: rootReducer,
      // redux-toolkit v1 types are unhappy with the preloadedState type. Remove the `as any` when bumping to v2
      preloadedState,
      // @ts-expect-error redux-toolkit v1 types are unhappy with the middleware array. Remove this comment when bumping to v2
      middleware: (getDefaultMiddleware) => {
        var _process$env$NODE_ENV;
        return getDefaultMiddleware({
          serializableCheck: false,
          immutableCheck: !["commonjs", "es6", "production"].includes((_process$env$NODE_ENV = "es6") !== null && _process$env$NODE_ENV !== void 0 ? _process$env$NODE_ENV : "")
        }).concat([mouseClickMiddleware.middleware, mouseMoveMiddleware.middleware, keyboardEventsMiddleware.middleware, externalEventsMiddleware.middleware, touchEventMiddleware.middleware]);
      },
      /*
       * I can't find out how to satisfy typescript here.
       * We return `EnhancerArray<[StoreEnhancer<{}, {}>, StoreEnhancer]>` from this function,
       * but the types say we should return `EnhancerArray<StoreEnhancer<{}, {}>`.
       * Looks like it's badly inferred generics, but it won't allow me to provide the correct type manually either.
       * So let's just ignore the error for now.
       */
      // @ts-expect-error mismatched generics
      enhancers: (getDefaultEnhancers) => {
        var enhancers = getDefaultEnhancers;
        if (typeof getDefaultEnhancers === "function") {
          enhancers = getDefaultEnhancers();
        }
        return enhancers.concat(autoBatchEnhancer({
          type: "raf"
        }));
      },
      devTools: Global.devToolsEnabled && {
        serialize: {
          replacer: reduxDevtoolsJsonStringifyReplacer
        },
        name: "recharts-".concat(chartName)
      }
    });
  };

  // node_modules/recharts/es6/state/RechartsStoreProvider.js
  function RechartsStoreProvider(_ref2) {
    var preloadedState = _ref2.preloadedState, children = _ref2.children, reduxStoreName = _ref2.reduxStoreName;
    var isPanorama = useIsPanorama();
    var storeRef = (0, import_react51.useRef)(null);
    if (isPanorama) {
      return children;
    }
    if (storeRef.current == null) {
      storeRef.current = createRechartsStore(preloadedState, reduxStoreName);
    }
    var nonNullContext = RechartsReduxContext;
    return /* @__PURE__ */ React40.createElement(Provider_default, {
      context: nonNullContext,
      store: storeRef.current
    }, children);
  }

  // node_modules/recharts/es6/state/ReportMainChartProps.js
  var import_react52 = __toESM(require_react());
  function ReportMainChartPropsImpl(_ref2) {
    var layout = _ref2.layout, margin = _ref2.margin;
    var dispatch2 = useAppDispatch();
    var isPanorama = useIsPanorama();
    (0, import_react52.useEffect)(() => {
      if (!isPanorama) {
        dispatch2(setLayout(layout));
        dispatch2(setMargin(margin));
      }
    }, [dispatch2, isPanorama, layout, margin]);
    return null;
  }
  var ReportMainChartProps = /* @__PURE__ */ (0, import_react52.memo)(ReportMainChartPropsImpl, propsAreEqual);

  // node_modules/recharts/es6/state/ReportChartProps.js
  var import_react53 = __toESM(require_react());
  function ReportChartProps(props) {
    var dispatch2 = useAppDispatch();
    (0, import_react53.useEffect)(() => {
      dispatch2(updateOptions(props));
    }, [dispatch2, props]);
    return null;
  }

  // node_modules/recharts/es6/state/ReportEventSettings.js
  var import_react54 = __toESM(require_react());
  var ReportEventSettingsImpl = (props) => {
    var dispatch2 = useAppDispatch();
    (0, import_react54.useEffect)(() => {
      dispatch2(setEventSettings(props));
    }, [dispatch2, props]);
    return null;
  };
  var ReportEventSettings = /* @__PURE__ */ (0, import_react54.memo)(ReportEventSettingsImpl, propsAreEqual);

  // node_modules/recharts/es6/chart/CategoricalChart.js
  var React44 = __toESM(require_react());
  var import_react59 = __toESM(require_react());

  // node_modules/recharts/es6/container/RootSurface.js
  var React42 = __toESM(require_react());
  var import_react56 = __toESM(require_react());

  // node_modules/recharts/es6/zIndex/ZIndexPortal.js
  var React41 = __toESM(require_react());
  var import_react55 = __toESM(require_react());
  function ZIndexSvgPortal(_ref2) {
    var zIndex = _ref2.zIndex, isPanorama = _ref2.isPanorama;
    var ref = (0, import_react55.useRef)(null);
    var dispatch2 = useAppDispatch();
    (0, import_react55.useLayoutEffect)(() => {
      if (ref.current) {
        dispatch2(registerZIndexPortalElement({
          zIndex,
          element: ref.current,
          isPanorama
        }));
      }
      return () => {
        dispatch2(unregisterZIndexPortalElement({
          zIndex,
          isPanorama
        }));
      };
    }, [dispatch2, zIndex, isPanorama]);
    return /* @__PURE__ */ React41.createElement("g", {
      tabIndex: -1,
      ref,
      className: "recharts-zIndex-layer_".concat(zIndex)
    });
  }
  function AllZIndexPortals(_ref2) {
    var children = _ref2.children, isPanorama = _ref2.isPanorama;
    var allRegisteredZIndexes = useAppSelector(selectAllRegisteredZIndexes);
    if (!allRegisteredZIndexes || allRegisteredZIndexes.length === 0) {
      return children;
    }
    var allNegativeZIndexes = allRegisteredZIndexes.filter((zIndex) => zIndex < 0);
    var allPositiveZIndexes = allRegisteredZIndexes.filter((zIndex) => zIndex > 0);
    return /* @__PURE__ */ React41.createElement(React41.Fragment, null, allNegativeZIndexes.map((zIndex) => /* @__PURE__ */ React41.createElement(ZIndexSvgPortal, {
      key: zIndex,
      zIndex,
      isPanorama
    })), children, allPositiveZIndexes.map((zIndex) => /* @__PURE__ */ React41.createElement(ZIndexSvgPortal, {
      key: zIndex,
      zIndex,
      isPanorama
    })));
  }

  // node_modules/recharts/es6/container/RootSurface.js
  var _excluded38 = ["children"];
  function _objectWithoutProperties24(e, t) {
    if (null == e) return {};
    var o, r2, i = _objectWithoutPropertiesLoose24(e, t);
    if (Object.getOwnPropertySymbols) {
      var n = Object.getOwnPropertySymbols(e);
      for (r2 = 0; r2 < n.length; r2++) o = n[r2], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
    }
    return i;
  }
  function _objectWithoutPropertiesLoose24(r2, e) {
    if (null == r2) return {};
    var t = {};
    for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
      if (-1 !== e.indexOf(n)) continue;
      t[n] = r2[n];
    }
    return t;
  }
  function _extends26() {
    return _extends26 = Object.assign ? Object.assign.bind() : function(n) {
      for (var e = 1; e < arguments.length; e++) {
        var t = arguments[e];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends26.apply(null, arguments);
  }
  var FULL_WIDTH_AND_HEIGHT = {
    width: "100%",
    height: "100%",
    /*
     * display: block is necessary here because the default for an SVG is display: inline,
     * which in some browsers (Chrome) adds a little bit of extra space above and below the SVG
     * to make space for the descender of letters like "g" and "y". This throws off the height calculation
     * and causes the container to grow indefinitely on each render with responsive=true.
     * Display: block removes that extra space.
     *
     * Interestingly, Firefox does not have this problem, but it doesn't hurt to add the style anyway.
     */
    display: "block"
  };
  var MainChartSurface = /* @__PURE__ */ (0, import_react56.forwardRef)((props, ref) => {
    var width = useChartWidth();
    var height = useChartHeight();
    var hasAccessibilityLayer = useAccessibilityLayer();
    if (!isPositiveNumber(width) || !isPositiveNumber(height)) {
      return null;
    }
    var children = props.children, otherAttributes = props.otherAttributes, title = props.title, desc = props.desc;
    var tabIndex, role;
    if (otherAttributes != null) {
      if (typeof otherAttributes.tabIndex === "number") {
        tabIndex = otherAttributes.tabIndex;
      } else {
        tabIndex = hasAccessibilityLayer ? 0 : void 0;
      }
      if (typeof otherAttributes.role === "string") {
        role = otherAttributes.role;
      } else {
        role = hasAccessibilityLayer ? "application" : void 0;
      }
    }
    return /* @__PURE__ */ React42.createElement(Surface, _extends26({}, otherAttributes, {
      title,
      desc,
      role,
      tabIndex,
      width,
      height,
      style: FULL_WIDTH_AND_HEIGHT,
      ref
    }), children);
  });
  var BrushPanoramaSurface = (_ref2) => {
    var children = _ref2.children;
    var brushDimensions = useAppSelector(selectBrushDimensions);
    if (!brushDimensions) {
      return null;
    }
    var width = brushDimensions.width, height = brushDimensions.height, y5 = brushDimensions.y, x5 = brushDimensions.x;
    return /* @__PURE__ */ React42.createElement(Surface, {
      width,
      height,
      x: x5,
      y: y5
    }, children);
  };
  var RootSurface = /* @__PURE__ */ (0, import_react56.forwardRef)((_ref2, ref) => {
    var children = _ref2.children, rest = _objectWithoutProperties24(_ref2, _excluded38);
    var isPanorama = useIsPanorama();
    if (isPanorama) {
      return /* @__PURE__ */ React42.createElement(BrushPanoramaSurface, null, /* @__PURE__ */ React42.createElement(AllZIndexPortals, {
        isPanorama: true
      }, children));
    }
    return /* @__PURE__ */ React42.createElement(MainChartSurface, _extends26({
      ref
    }, rest), /* @__PURE__ */ React42.createElement(AllZIndexPortals, {
      isPanorama: false
    }, children));
  });

  // node_modules/recharts/es6/chart/RechartsWrapper.js
  var React43 = __toESM(require_react());
  var import_react58 = __toESM(require_react());

  // node_modules/recharts/es6/util/useReportScale.js
  var import_react57 = __toESM(require_react());
  function _slicedToArray20(r2, e) {
    return _arrayWithHoles20(r2) || _iterableToArrayLimit20(r2, e) || _unsupportedIterableToArray20(r2, e) || _nonIterableRest20();
  }
  function _nonIterableRest20() {
    throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  function _unsupportedIterableToArray20(r2, a2) {
    if (r2) {
      if ("string" == typeof r2) return _arrayLikeToArray20(r2, a2);
      var t = {}.toString.call(r2).slice(8, -1);
      return "Object" === t && r2.constructor && (t = r2.constructor.name), "Map" === t || "Set" === t ? Array.from(r2) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray20(r2, a2) : void 0;
    }
  }
  function _arrayLikeToArray20(r2, a2) {
    (null == a2 || a2 > r2.length) && (a2 = r2.length);
    for (var e = 0, n = Array(a2); e < a2; e++) n[e] = r2[e];
    return n;
  }
  function _iterableToArrayLimit20(r2, l) {
    var t = null == r2 ? null : "undefined" != typeof Symbol && r2[Symbol.iterator] || r2["@@iterator"];
    if (null != t) {
      var e, n, i, u, a2 = [], f = true, o = false;
      try {
        if (i = (t = t.call(r2)).next, 0 === l) {
          if (Object(t) !== t) return;
          f = false;
        } else for (; !(f = (e = i.call(t)).done) && (a2.push(e.value), a2.length !== l); f = true) ;
      } catch (r3) {
        o = true, n = r3;
      } finally {
        try {
          if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return;
        } finally {
          if (o) throw n;
        }
      }
      return a2;
    }
  }
  function _arrayWithHoles20(r2) {
    if (Array.isArray(r2)) return r2;
  }
  function useReportScale() {
    var dispatch2 = useAppDispatch();
    var _useState = (0, import_react57.useState)(null), _useState2 = _slicedToArray20(_useState, 2), ref = _useState2[0], setRef = _useState2[1];
    var scale = useAppSelector(selectContainerScale);
    (0, import_react57.useEffect)(() => {
      if (ref == null) {
        return;
      }
      var rect = ref.getBoundingClientRect();
      var newScale = rect.width / ref.offsetWidth;
      if (isWellBehavedNumber(newScale) && newScale !== scale) {
        dispatch2(setScale(newScale));
      }
    }, [ref, dispatch2, scale]);
    return setRef;
  }

  // node_modules/recharts/es6/chart/RechartsWrapper.js
  function ownKeys39(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread39(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys39(Object(t), true).forEach(function(r3) {
        _defineProperty42(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys39(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty42(e, r2, t) {
    return (r2 = _toPropertyKey42(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey42(t) {
    var i = _toPrimitive42(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive42(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  function _extends27() {
    return _extends27 = Object.assign ? Object.assign.bind() : function(n) {
      for (var e = 1; e < arguments.length; e++) {
        var t = arguments[e];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends27.apply(null, arguments);
  }
  function _slicedToArray21(r2, e) {
    return _arrayWithHoles21(r2) || _iterableToArrayLimit21(r2, e) || _unsupportedIterableToArray21(r2, e) || _nonIterableRest21();
  }
  function _nonIterableRest21() {
    throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  function _unsupportedIterableToArray21(r2, a2) {
    if (r2) {
      if ("string" == typeof r2) return _arrayLikeToArray21(r2, a2);
      var t = {}.toString.call(r2).slice(8, -1);
      return "Object" === t && r2.constructor && (t = r2.constructor.name), "Map" === t || "Set" === t ? Array.from(r2) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray21(r2, a2) : void 0;
    }
  }
  function _arrayLikeToArray21(r2, a2) {
    (null == a2 || a2 > r2.length) && (a2 = r2.length);
    for (var e = 0, n = Array(a2); e < a2; e++) n[e] = r2[e];
    return n;
  }
  function _iterableToArrayLimit21(r2, l) {
    var t = null == r2 ? null : "undefined" != typeof Symbol && r2[Symbol.iterator] || r2["@@iterator"];
    if (null != t) {
      var e, n, i, u, a2 = [], f = true, o = false;
      try {
        if (i = (t = t.call(r2)).next, 0 === l) {
          if (Object(t) !== t) return;
          f = false;
        } else for (; !(f = (e = i.call(t)).done) && (a2.push(e.value), a2.length !== l); f = true) ;
      } catch (r3) {
        o = true, n = r3;
      } finally {
        try {
          if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return;
        } finally {
          if (o) throw n;
        }
      }
      return a2;
    }
  }
  function _arrayWithHoles21(r2) {
    if (Array.isArray(r2)) return r2;
  }
  var EventSynchronizer = () => {
    useSynchronisedEventsFromOtherCharts();
    return null;
  };
  function getNumberOrZero(value2) {
    if (typeof value2 === "number") {
      return value2;
    }
    if (typeof value2 === "string") {
      var parsed = parseFloat(value2);
      if (!Number.isNaN(parsed)) {
        return parsed;
      }
    }
    return 0;
  }
  var ResponsiveDiv = /* @__PURE__ */ (0, import_react58.forwardRef)((props, ref) => {
    var _props$style, _props$style2;
    var observerRef = (0, import_react58.useRef)(null);
    var _useState = (0, import_react58.useState)({
      containerWidth: getNumberOrZero((_props$style = props.style) === null || _props$style === void 0 ? void 0 : _props$style.width),
      containerHeight: getNumberOrZero((_props$style2 = props.style) === null || _props$style2 === void 0 ? void 0 : _props$style2.height)
    }), _useState2 = _slicedToArray21(_useState, 2), sizes = _useState2[0], setSizes = _useState2[1];
    var setContainerSize = (0, import_react58.useCallback)((newWidth, newHeight) => {
      setSizes((prevState) => {
        var roundedWidth = Math.round(newWidth);
        var roundedHeight = Math.round(newHeight);
        if (prevState.containerWidth === roundedWidth && prevState.containerHeight === roundedHeight) {
          return prevState;
        }
        return {
          containerWidth: roundedWidth,
          containerHeight: roundedHeight
        };
      });
    }, []);
    var innerRef = (0, import_react58.useCallback)((node) => {
      if (typeof ref === "function") {
        ref(node);
      }
      if (observerRef.current != null) {
        observerRef.current.disconnect();
        observerRef.current = null;
      }
      if (node != null && typeof ResizeObserver !== "undefined") {
        var _node$getBoundingClie = node.getBoundingClientRect(), containerWidth = _node$getBoundingClie.width, containerHeight = _node$getBoundingClie.height;
        setContainerSize(containerWidth, containerHeight);
        var callback = (entries) => {
          var entry = entries[0];
          if (entry == null) {
            return;
          }
          var _entry$contentRect = entry.contentRect, width = _entry$contentRect.width, height = _entry$contentRect.height;
          setContainerSize(width, height);
        };
        var observer = new ResizeObserver(callback);
        observer.observe(node);
        observerRef.current = observer;
      }
    }, [ref, setContainerSize]);
    (0, import_react58.useEffect)(() => {
      return () => {
        var observer = observerRef.current;
        if (observer != null) {
          observer.disconnect();
        }
      };
    }, [setContainerSize]);
    return /* @__PURE__ */ React43.createElement(React43.Fragment, null, /* @__PURE__ */ React43.createElement(ReportChartSize, {
      width: sizes.containerWidth,
      height: sizes.containerHeight
    }), /* @__PURE__ */ React43.createElement("div", _extends27({
      ref: innerRef
    }, props)));
  });
  var ReadSizeOnceDiv = /* @__PURE__ */ (0, import_react58.forwardRef)((props, ref) => {
    var width = props.width, height = props.height;
    var _useState3 = (0, import_react58.useState)({
      containerWidth: getNumberOrZero(width),
      containerHeight: getNumberOrZero(height)
    }), _useState4 = _slicedToArray21(_useState3, 2), sizes = _useState4[0], setSizes = _useState4[1];
    var setContainerSize = (0, import_react58.useCallback)((newWidth, newHeight) => {
      setSizes((prevState) => {
        var roundedWidth = Math.round(newWidth);
        var roundedHeight = Math.round(newHeight);
        if (prevState.containerWidth === roundedWidth && prevState.containerHeight === roundedHeight) {
          return prevState;
        }
        return {
          containerWidth: roundedWidth,
          containerHeight: roundedHeight
        };
      });
    }, []);
    var innerRef = (0, import_react58.useCallback)((node) => {
      if (typeof ref === "function") {
        ref(node);
      }
      if (node != null) {
        var _node$getBoundingClie2 = node.getBoundingClientRect(), containerWidth = _node$getBoundingClie2.width, containerHeight = _node$getBoundingClie2.height;
        setContainerSize(containerWidth, containerHeight);
      }
    }, [ref, setContainerSize]);
    return /* @__PURE__ */ React43.createElement(React43.Fragment, null, /* @__PURE__ */ React43.createElement(ReportChartSize, {
      width: sizes.containerWidth,
      height: sizes.containerHeight
    }), /* @__PURE__ */ React43.createElement("div", _extends27({
      ref: innerRef
    }, props)));
  });
  var StaticDiv = /* @__PURE__ */ (0, import_react58.forwardRef)((props, ref) => {
    var width = props.width, height = props.height;
    return /* @__PURE__ */ React43.createElement(React43.Fragment, null, /* @__PURE__ */ React43.createElement(ReportChartSize, {
      width,
      height
    }), /* @__PURE__ */ React43.createElement("div", _extends27({
      ref
    }, props)));
  });
  var NonResponsiveDiv = /* @__PURE__ */ (0, import_react58.forwardRef)((props, ref) => {
    var width = props.width, height = props.height;
    if (typeof width === "string" || typeof height === "string") {
      return /* @__PURE__ */ React43.createElement(ReadSizeOnceDiv, _extends27({}, props, {
        ref
      }));
    }
    if (typeof width === "number" && typeof height === "number") {
      return /* @__PURE__ */ React43.createElement(StaticDiv, _extends27({}, props, {
        width,
        height,
        ref
      }));
    }
    return /* @__PURE__ */ React43.createElement(React43.Fragment, null, /* @__PURE__ */ React43.createElement(ReportChartSize, {
      width,
      height
    }), /* @__PURE__ */ React43.createElement("div", _extends27({
      ref
    }, props)));
  });
  function getWrapperDivComponent(responsive) {
    return responsive ? ResponsiveDiv : NonResponsiveDiv;
  }
  var RechartsWrapper = /* @__PURE__ */ (0, import_react58.forwardRef)((props, ref) => {
    var children = props.children, className = props.className, heightFromProps = props.height, onClick = props.onClick, onContextMenu = props.onContextMenu, onDoubleClick = props.onDoubleClick, onMouseDown = props.onMouseDown, onMouseEnter = props.onMouseEnter, onMouseLeave = props.onMouseLeave, onMouseMove = props.onMouseMove, onMouseUp = props.onMouseUp, onTouchEnd = props.onTouchEnd, onTouchMove = props.onTouchMove, onTouchStart = props.onTouchStart, style = props.style, widthFromProps = props.width, responsive = props.responsive, _props$dispatchTouchE = props.dispatchTouchEvents, dispatchTouchEvents = _props$dispatchTouchE === void 0 ? true : _props$dispatchTouchE;
    var containerRef = (0, import_react58.useRef)(null);
    var dispatch2 = useAppDispatch();
    var _useState5 = (0, import_react58.useState)(null), _useState6 = _slicedToArray21(_useState5, 2), tooltipPortal = _useState6[0], setTooltipPortal = _useState6[1];
    var _useState7 = (0, import_react58.useState)(null), _useState8 = _slicedToArray21(_useState7, 2), legendPortal = _useState8[0], setLegendPortal = _useState8[1];
    var setScaleRef = useReportScale();
    var responsiveContainerCalculations = useResponsiveContainerContext();
    var width = (responsiveContainerCalculations === null || responsiveContainerCalculations === void 0 ? void 0 : responsiveContainerCalculations.width) > 0 ? responsiveContainerCalculations.width : widthFromProps;
    var height = (responsiveContainerCalculations === null || responsiveContainerCalculations === void 0 ? void 0 : responsiveContainerCalculations.height) > 0 ? responsiveContainerCalculations.height : heightFromProps;
    var innerRef = (0, import_react58.useCallback)((node) => {
      setScaleRef(node);
      if (typeof ref === "function") {
        ref(node);
      }
      setTooltipPortal(node);
      setLegendPortal(node);
      if (node != null) {
        containerRef.current = node;
      }
    }, [setScaleRef, ref, setTooltipPortal, setLegendPortal]);
    var myOnClick = (0, import_react58.useCallback)((e) => {
      dispatch2(mouseClickAction(e));
      dispatch2(externalEventAction({
        handler: onClick,
        reactEvent: e
      }));
    }, [dispatch2, onClick]);
    var myOnMouseEnter = (0, import_react58.useCallback)((e) => {
      dispatch2(mouseMoveAction(e));
      dispatch2(externalEventAction({
        handler: onMouseEnter,
        reactEvent: e
      }));
    }, [dispatch2, onMouseEnter]);
    var myOnMouseLeave = (0, import_react58.useCallback)((e) => {
      dispatch2(mouseLeaveChart());
      dispatch2(externalEventAction({
        handler: onMouseLeave,
        reactEvent: e
      }));
    }, [dispatch2, onMouseLeave]);
    var myOnMouseMove = (0, import_react58.useCallback)((e) => {
      dispatch2(mouseMoveAction(e));
      dispatch2(externalEventAction({
        handler: onMouseMove,
        reactEvent: e
      }));
    }, [dispatch2, onMouseMove]);
    var onFocus = (0, import_react58.useCallback)(() => {
      dispatch2(focusAction());
    }, [dispatch2]);
    var onBlur = (0, import_react58.useCallback)(() => {
      dispatch2(blurAction());
    }, [dispatch2]);
    var onKeyDown = (0, import_react58.useCallback)((e) => {
      dispatch2(keyDownAction(e.key));
    }, [dispatch2]);
    var myOnContextMenu = (0, import_react58.useCallback)((e) => {
      dispatch2(externalEventAction({
        handler: onContextMenu,
        reactEvent: e
      }));
    }, [dispatch2, onContextMenu]);
    var myOnDoubleClick = (0, import_react58.useCallback)((e) => {
      dispatch2(externalEventAction({
        handler: onDoubleClick,
        reactEvent: e
      }));
    }, [dispatch2, onDoubleClick]);
    var myOnMouseDown = (0, import_react58.useCallback)((e) => {
      dispatch2(externalEventAction({
        handler: onMouseDown,
        reactEvent: e
      }));
    }, [dispatch2, onMouseDown]);
    var myOnMouseUp = (0, import_react58.useCallback)((e) => {
      dispatch2(externalEventAction({
        handler: onMouseUp,
        reactEvent: e
      }));
    }, [dispatch2, onMouseUp]);
    var myOnTouchStart = (0, import_react58.useCallback)((e) => {
      dispatch2(externalEventAction({
        handler: onTouchStart,
        reactEvent: e
      }));
    }, [dispatch2, onTouchStart]);
    var myOnTouchMove = (0, import_react58.useCallback)((e) => {
      if (dispatchTouchEvents) {
        dispatch2(touchEventAction(e));
      }
      dispatch2(externalEventAction({
        handler: onTouchMove,
        reactEvent: e
      }));
    }, [dispatch2, dispatchTouchEvents, onTouchMove]);
    var myOnTouchEnd = (0, import_react58.useCallback)((e) => {
      dispatch2(externalEventAction({
        handler: onTouchEnd,
        reactEvent: e
      }));
    }, [dispatch2, onTouchEnd]);
    var WrapperDiv = getWrapperDivComponent(responsive);
    return /* @__PURE__ */ React43.createElement(TooltipPortalContext.Provider, {
      value: tooltipPortal
    }, /* @__PURE__ */ React43.createElement(LegendPortalContext.Provider, {
      value: legendPortal
    }, /* @__PURE__ */ React43.createElement(WrapperDiv, {
      width: width !== null && width !== void 0 ? width : style === null || style === void 0 ? void 0 : style.width,
      height: height !== null && height !== void 0 ? height : style === null || style === void 0 ? void 0 : style.height,
      className: clsx("recharts-wrapper", className),
      style: _objectSpread39({
        position: "relative",
        cursor: "default",
        width,
        height
      }, style),
      onClick: myOnClick,
      onContextMenu: myOnContextMenu,
      onDoubleClick: myOnDoubleClick,
      onFocus,
      onBlur,
      onKeyDown,
      onMouseDown: myOnMouseDown,
      onMouseEnter: myOnMouseEnter,
      onMouseLeave: myOnMouseLeave,
      onMouseMove: myOnMouseMove,
      onMouseUp: myOnMouseUp,
      onTouchEnd: myOnTouchEnd,
      onTouchMove: myOnTouchMove,
      onTouchStart: myOnTouchStart,
      ref: innerRef
    }, /* @__PURE__ */ React43.createElement(EventSynchronizer, null), children)));
  });

  // node_modules/recharts/es6/chart/CategoricalChart.js
  var _excluded39 = ["width", "height", "responsive", "children", "className", "style", "compact", "title", "desc"];
  function _objectWithoutProperties25(e, t) {
    if (null == e) return {};
    var o, r2, i = _objectWithoutPropertiesLoose25(e, t);
    if (Object.getOwnPropertySymbols) {
      var n = Object.getOwnPropertySymbols(e);
      for (r2 = 0; r2 < n.length; r2++) o = n[r2], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
    }
    return i;
  }
  function _objectWithoutPropertiesLoose25(r2, e) {
    if (null == r2) return {};
    var t = {};
    for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
      if (-1 !== e.indexOf(n)) continue;
      t[n] = r2[n];
    }
    return t;
  }
  var CategoricalChart = /* @__PURE__ */ (0, import_react59.forwardRef)((props, ref) => {
    var width = props.width, height = props.height, responsive = props.responsive, children = props.children, className = props.className, style = props.style, compact = props.compact, title = props.title, desc = props.desc, others = _objectWithoutProperties25(props, _excluded39);
    var attrs = svgPropertiesNoEvents(others);
    if (compact) {
      return /* @__PURE__ */ React44.createElement(React44.Fragment, null, /* @__PURE__ */ React44.createElement(ReportChartSize, {
        width,
        height
      }), /* @__PURE__ */ React44.createElement(RootSurface, {
        otherAttributes: attrs,
        title,
        desc
      }, children));
    }
    return /* @__PURE__ */ React44.createElement(RechartsWrapper, {
      className,
      style,
      width,
      height,
      responsive: responsive !== null && responsive !== void 0 ? responsive : false,
      onClick: props.onClick,
      onMouseLeave: props.onMouseLeave,
      onMouseEnter: props.onMouseEnter,
      onMouseMove: props.onMouseMove,
      onMouseDown: props.onMouseDown,
      onMouseUp: props.onMouseUp,
      onContextMenu: props.onContextMenu,
      onDoubleClick: props.onDoubleClick,
      onTouchStart: props.onTouchStart,
      onTouchMove: props.onTouchMove,
      onTouchEnd: props.onTouchEnd
    }, /* @__PURE__ */ React44.createElement(RootSurface, {
      otherAttributes: attrs,
      title,
      desc,
      ref
    }, /* @__PURE__ */ React44.createElement(ClipPathProvider, null, children)));
  });

  // node_modules/recharts/es6/chart/CartesianChart.js
  function _extends28() {
    return _extends28 = Object.assign ? Object.assign.bind() : function(n) {
      for (var e = 1; e < arguments.length; e++) {
        var t = arguments[e];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends28.apply(null, arguments);
  }
  function ownKeys40(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread40(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys40(Object(t), true).forEach(function(r3) {
        _defineProperty43(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys40(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty43(e, r2, t) {
    return (r2 = _toPropertyKey43(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey43(t) {
    var i = _toPrimitive43(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive43(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  var defaultMargin = {
    top: 5,
    right: 5,
    bottom: 5,
    left: 5
  };
  var defaultCartesianChartProps = _objectSpread40({
    accessibilityLayer: true,
    barCategoryGap: "10%",
    barGap: 4,
    layout: "horizontal",
    margin: defaultMargin,
    responsive: false,
    reverseStackOrder: false,
    stackOffset: "none",
    syncMethod: "index"
  }, initialEventSettingsState);
  var CartesianChart = /* @__PURE__ */ (0, import_react60.forwardRef)(function CartesianChart2(props, ref) {
    var _categoricalChartProp;
    var rootChartProps = resolveDefaultProps(props.categoricalChartProps, defaultCartesianChartProps);
    var chartName = props.chartName, defaultTooltipEventType = props.defaultTooltipEventType, validateTooltipEventTypes = props.validateTooltipEventTypes, tooltipPayloadSearcher = props.tooltipPayloadSearcher, categoricalChartProps = props.categoricalChartProps;
    var options = {
      chartName,
      defaultTooltipEventType,
      validateTooltipEventTypes,
      tooltipPayloadSearcher,
      eventEmitter: void 0
    };
    return /* @__PURE__ */ React45.createElement(RechartsStoreProvider, {
      preloadedState: {
        options
      },
      reduxStoreName: (_categoricalChartProp = categoricalChartProps.id) !== null && _categoricalChartProp !== void 0 ? _categoricalChartProp : chartName
    }, /* @__PURE__ */ React45.createElement(ChartDataContextProvider, {
      chartData: categoricalChartProps.data
    }), /* @__PURE__ */ React45.createElement(ReportMainChartProps, {
      layout: rootChartProps.layout,
      margin: rootChartProps.margin
    }), /* @__PURE__ */ React45.createElement(ReportEventSettings, {
      throttleDelay: rootChartProps.throttleDelay,
      throttledEvents: rootChartProps.throttledEvents
    }), /* @__PURE__ */ React45.createElement(ReportChartProps, {
      baseValue: rootChartProps.baseValue,
      accessibilityLayer: rootChartProps.accessibilityLayer,
      barCategoryGap: rootChartProps.barCategoryGap,
      maxBarSize: rootChartProps.maxBarSize,
      stackOffset: rootChartProps.stackOffset,
      barGap: rootChartProps.barGap,
      barSize: rootChartProps.barSize,
      syncId: rootChartProps.syncId,
      syncMethod: rootChartProps.syncMethod,
      className: rootChartProps.className,
      reverseStackOrder: rootChartProps.reverseStackOrder
    }), /* @__PURE__ */ React45.createElement(CategoricalChart, _extends28({}, rootChartProps, {
      ref
    })));
  });

  // node_modules/recharts/es6/chart/LineChart.js
  var allowedTooltipTypes = ["axis"];
  var LineChart = /* @__PURE__ */ (0, import_react61.forwardRef)((props, ref) => {
    return /* @__PURE__ */ React46.createElement(CartesianChart, {
      chartName: "LineChart",
      defaultTooltipEventType: "axis",
      validateTooltipEventTypes: allowedTooltipTypes,
      tooltipPayloadSearcher: arrayTooltipSearcher,
      categoricalChartProps: props,
      ref
    });
  });

  // node_modules/recharts/es6/chart/BarChart.js
  var React47 = __toESM(require_react());
  var import_react62 = __toESM(require_react());
  var allowedTooltipTypes2 = ["axis", "item"];
  var BarChart = /* @__PURE__ */ (0, import_react62.forwardRef)((props, ref) => {
    return /* @__PURE__ */ React47.createElement(CartesianChart, {
      chartName: "BarChart",
      defaultTooltipEventType: "axis",
      validateTooltipEventTypes: allowedTooltipTypes2,
      tooltipPayloadSearcher: arrayTooltipSearcher,
      categoricalChartProps: props,
      ref
    });
  });

  // node_modules/recharts/es6/chart/PieChart.js
  var React49 = __toESM(require_react());
  var import_react65 = __toESM(require_react());

  // node_modules/recharts/es6/chart/PolarChart.js
  var import_react64 = __toESM(require_react());
  var React48 = __toESM(require_react());

  // node_modules/recharts/es6/state/ReportPolarOptions.js
  var import_react63 = __toESM(require_react());
  function ReportPolarOptions(props) {
    var dispatch2 = useAppDispatch();
    (0, import_react63.useEffect)(() => {
      dispatch2(updatePolarOptions(props));
    }, [dispatch2, props]);
    return null;
  }

  // node_modules/recharts/es6/chart/PolarChart.js
  var _excluded40 = ["layout"];
  function _extends29() {
    return _extends29 = Object.assign ? Object.assign.bind() : function(n) {
      for (var e = 1; e < arguments.length; e++) {
        var t = arguments[e];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends29.apply(null, arguments);
  }
  function _objectWithoutProperties26(e, t) {
    if (null == e) return {};
    var o, r2, i = _objectWithoutPropertiesLoose26(e, t);
    if (Object.getOwnPropertySymbols) {
      var n = Object.getOwnPropertySymbols(e);
      for (r2 = 0; r2 < n.length; r2++) o = n[r2], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
    }
    return i;
  }
  function _objectWithoutPropertiesLoose26(r2, e) {
    if (null == r2) return {};
    var t = {};
    for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
      if (-1 !== e.indexOf(n)) continue;
      t[n] = r2[n];
    }
    return t;
  }
  function ownKeys41(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread41(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys41(Object(t), true).forEach(function(r3) {
        _defineProperty44(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys41(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty44(e, r2, t) {
    return (r2 = _toPropertyKey44(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey44(t) {
    var i = _toPrimitive44(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive44(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  var defaultMargin2 = {
    top: 5,
    right: 5,
    bottom: 5,
    left: 5
  };
  var defaultPolarChartProps = _objectSpread41({
    accessibilityLayer: true,
    stackOffset: "none",
    barCategoryGap: "10%",
    barGap: 4,
    margin: defaultMargin2,
    reverseStackOrder: false,
    syncMethod: "index",
    layout: "radial",
    responsive: false,
    cx: "50%",
    cy: "50%",
    innerRadius: 0,
    outerRadius: "80%"
  }, initialEventSettingsState);
  var PolarChart = /* @__PURE__ */ (0, import_react64.forwardRef)(function PolarChart2(props, ref) {
    var _polarChartProps$id;
    var polarChartProps = resolveDefaultProps(props.categoricalChartProps, defaultPolarChartProps);
    var layout = polarChartProps.layout, otherCategoricalProps = _objectWithoutProperties26(polarChartProps, _excluded40);
    var chartName = props.chartName, defaultTooltipEventType = props.defaultTooltipEventType, validateTooltipEventTypes = props.validateTooltipEventTypes, tooltipPayloadSearcher = props.tooltipPayloadSearcher;
    var options = {
      chartName,
      defaultTooltipEventType,
      validateTooltipEventTypes,
      tooltipPayloadSearcher,
      eventEmitter: void 0
    };
    return /* @__PURE__ */ React48.createElement(RechartsStoreProvider, {
      preloadedState: {
        options
      },
      reduxStoreName: (_polarChartProps$id = polarChartProps.id) !== null && _polarChartProps$id !== void 0 ? _polarChartProps$id : chartName
    }, /* @__PURE__ */ React48.createElement(ChartDataContextProvider, {
      chartData: polarChartProps.data
    }), /* @__PURE__ */ React48.createElement(ReportMainChartProps, {
      layout,
      margin: polarChartProps.margin
    }), /* @__PURE__ */ React48.createElement(ReportEventSettings, {
      throttleDelay: polarChartProps.throttleDelay,
      throttledEvents: polarChartProps.throttledEvents
    }), /* @__PURE__ */ React48.createElement(ReportChartProps, {
      baseValue: void 0,
      accessibilityLayer: polarChartProps.accessibilityLayer,
      barCategoryGap: polarChartProps.barCategoryGap,
      maxBarSize: polarChartProps.maxBarSize,
      stackOffset: polarChartProps.stackOffset,
      barGap: polarChartProps.barGap,
      barSize: polarChartProps.barSize,
      syncId: polarChartProps.syncId,
      syncMethod: polarChartProps.syncMethod,
      className: polarChartProps.className,
      reverseStackOrder: polarChartProps.reverseStackOrder
    }), /* @__PURE__ */ React48.createElement(ReportPolarOptions, {
      cx: polarChartProps.cx,
      cy: polarChartProps.cy,
      startAngle: polarChartProps.startAngle,
      endAngle: polarChartProps.endAngle,
      innerRadius: polarChartProps.innerRadius,
      outerRadius: polarChartProps.outerRadius
    }), /* @__PURE__ */ React48.createElement(CategoricalChart, _extends29({}, otherCategoricalProps, {
      ref
    })));
  });

  // node_modules/recharts/es6/chart/PieChart.js
  function ownKeys42(e, r2) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread42(e) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys42(Object(t), true).forEach(function(r3) {
        _defineProperty45(e, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys42(Object(t)).forEach(function(r3) {
        Object.defineProperty(e, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e;
  }
  function _defineProperty45(e, r2, t) {
    return (r2 = _toPropertyKey45(r2)) in e ? Object.defineProperty(e, r2, { value: t, enumerable: true, configurable: true, writable: true }) : e[r2] = t, e;
  }
  function _toPropertyKey45(t) {
    var i = _toPrimitive45(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _toPrimitive45(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e = t[Symbol.toPrimitive];
    if (void 0 !== e) {
      var i = e.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  var allowedTooltipTypes3 = ["item"];
  var defaultPieChartProps = _objectSpread42(_objectSpread42({}, defaultPolarChartProps), {}, {
    layout: "centric",
    startAngle: 0,
    endAngle: 360
  });
  var PieChart = /* @__PURE__ */ (0, import_react65.forwardRef)((props, ref) => {
    var propsWithDefaults = resolveDefaultProps(props, defaultPieChartProps);
    return /* @__PURE__ */ React49.createElement(PolarChart, {
      chartName: "PieChart",
      defaultTooltipEventType: "item",
      validateTooltipEventTypes: allowedTooltipTypes3,
      tooltipPayloadSearcher: arrayTooltipSearcher,
      categoricalChartProps: propsWithDefaults,
      ref
    });
  });

  // node_modules/recharts/es6/chart/AreaChart.js
  var React50 = __toESM(require_react());
  var import_react66 = __toESM(require_react());
  var allowedTooltipTypes4 = ["axis"];
  var AreaChart = /* @__PURE__ */ (0, import_react66.forwardRef)((props, ref) => {
    return /* @__PURE__ */ React50.createElement(CartesianChart, {
      chartName: "AreaChart",
      defaultTooltipEventType: "axis",
      validateTooltipEventTypes: allowedTooltipTypes4,
      tooltipPayloadSearcher: arrayTooltipSearcher,
      categoricalChartProps: props,
      ref
    });
  });

  // node_modules/recharts/es6/chart/ComposedChart.js
  var React51 = __toESM(require_react());
  var import_react67 = __toESM(require_react());
  var allowedTooltipTypes5 = ["axis"];
  var ComposedChart = /* @__PURE__ */ (0, import_react67.forwardRef)((props, ref) => {
    return /* @__PURE__ */ React51.createElement(CartesianChart, {
      chartName: "ComposedChart",
      defaultTooltipEventType: "axis",
      validateTooltipEventTypes: allowedTooltipTypes5,
      tooltipPayloadSearcher: arrayTooltipSearcher,
      categoricalChartProps: props,
      ref
    });
  });

  // src/ui/charts/ContentTypeDonut.tsx
  var import_jsx_runtime4 = __toESM(require_jsx_runtime(), 1);
  function ContentTypeDonut({
    data,
    theme,
    width
  }) {
    return /* @__PURE__ */ (0, import_jsx_runtime4.jsxs)("div", { style: { position: "relative" }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime4.jsxs)(PieChart, { width, height: 260, children: [
        /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(
          Pie,
          {
            data: data.slices,
            dataKey: "count",
            nameKey: "label",
            cx: "50%",
            cy: "50%",
            innerRadius: 72,
            outerRadius: 104,
            stroke: theme.ink.card,
            strokeWidth: 2,
            isAnimationActive: false,
            children: data.slices.map((slice4) => /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(Cell, { fill: colorForSlot(theme, slice4.colorSlot) }, slice4.key))
          }
        ),
        /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(
          Tooltip,
          {
            content: ({ active, payload }) => {
              if (!active || !payload?.length) return null;
              const slice4 = payload[0].payload;
              return /* @__PURE__ */ (0, import_jsx_runtime4.jsxs)("div", { className: "retraced-tooltip", children: [
                /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("div", { className: "retraced-tooltip-title", children: slice4.label }),
                /* @__PURE__ */ (0, import_jsx_runtime4.jsxs)("div", { children: [
                  formatCount(slice4.count),
                  " messages · ",
                  Math.round(100 * slice4.count / Math.max(1, data.total)),
                  "%"
                ] })
              ] });
            }
          }
        )
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime4.jsxs)("div", { className: "retraced-donut-center", children: [
        /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("span", { className: "retraced-donut-total", children: formatCompact(data.total) }),
        /* @__PURE__ */ (0, import_jsx_runtime4.jsx)("span", { className: "retraced-note", children: "type tags" })
      ] })
    ] });
  }

  // src/ui/data/histogram.ts
  function medianBucket(buckets) {
    const total = buckets.reduce((a2, b) => a2 + b, 0);
    if (total === 0) return null;
    const target = (total + 1) / 2;
    let cumulative = 0;
    for (let i = 0; i < buckets.length; i++) {
      cumulative += buckets[i];
      if (cumulative >= target) return i;
    }
    return buckets.length - 1;
  }
  function sumInto(target, source) {
    if (!Array.isArray(source)) return;
    const n = Math.min(target.length, source.length);
    for (let i = 0; i < n; i++) target[i] += source[i];
  }

  // src/ui/data/content.ts
  var MAX_WEEKS = 800;
  var TYPE_ORDER = [
    { key: "text", label: "Text" },
    { key: "image", label: "Images" },
    { key: "link", label: "Links" },
    { key: "gif", label: "GIFs" },
    { key: "sticker", label: "Stickers" },
    { key: "attachment", label: "Attachments" }
  ];
  function contentTypeSplit(rows) {
    const sums = /* @__PURE__ */ new Map();
    for (const row of rows) {
      for (const { key } of TYPE_ORDER) sums.set(key, (sums.get(key) ?? 0) + row.contentTypes[key]);
    }
    const slices = [];
    let total = 0;
    TYPE_ORDER.forEach(({ key, label }, slot) => {
      const count = sums.get(key) ?? 0;
      total += count;
      if (count > 0) slices.push({ key, label, count, colorSlot: slot });
    });
    return { slices, total };
  }
  function topDomains(rows, max4 = 10) {
    return [...rows].sort((a2, b) => b.count - a2.count).slice(0, max4).map((r2) => ({ domain: r2.domain, count: r2.count }));
  }
  function emojiByServer(rows, opts) {
    const groups = /* @__PURE__ */ new Map();
    for (const row of rows) {
      if (row.count <= 0) continue;
      let group = groups.get(row.guildScope);
      if (!group) {
        groups.set(
          row.guildScope,
          group = { scope: row.guildScope, label: row.guildScope === "dm" ? "DMs" : opts.guildLabel(row.guildScope), emoji: [], total: 0 }
        );
      }
      group.emoji.push({ emojiId: row.emojiId, name: row.name, count: row.count });
      group.total += row.count;
    }
    return [...groups.values()].sort((a2, b) => b.total - a2.total).map(({ total: _total, ...group }) => ({
      ...group,
      emoji: group.emoji.sort((a2, b) => b.count - a2.count).slice(0, opts.perServer ?? 10)
    }));
  }
  var MIN_WEEK_WORDS = 50;
  function vocabRichnessWeekly(rows) {
    const byWeek = /* @__PURE__ */ new Map();
    for (const row of rows) {
      if (row.words === 0 && (row.theirWords ?? 0) === 0) continue;
      const week = mondayOf(row.date);
      const entry = byWeek.get(week) ?? { words: 0, unique: 0, theirWords: 0, theirUnique: 0 };
      entry.words += row.words;
      entry.unique += row.uniqueWords;
      entry.theirWords += row.theirWords ?? 0;
      entry.theirUnique += row.theirUniqueWords ?? 0;
      byWeek.set(week, entry);
    }
    if (byWeek.size === 0) return [];
    const weeks = [...byWeek.keys()].sort();
    const last = weeks[weeks.length - 1];
    const out = [];
    let cursor = weeks[0];
    for (let i = 0; i < MAX_WEEKS; i++) {
      const entry = byWeek.get(cursor);
      out.push({
        week: cursor,
        minePct: entry && entry.words >= MIN_WEEK_WORDS ? Math.round(100 * entry.unique / entry.words) : null,
        theirsPct: entry && entry.theirWords >= MIN_WEEK_WORDS ? Math.round(100 * entry.theirUnique / entry.theirWords) : null
      });
      if (cursor === last) break;
      cursor = shiftDate(cursor, 7);
    }
    return out;
  }
  var LENGTH_BUCKET_LABELS = ["<10", "10–50", "50–200", "200–1k", "1k+"];
  function lengthShareWeekly(rows, side) {
    const byWeek = /* @__PURE__ */ new Map();
    for (const row of rows) {
      const buckets = side === "mine" ? row.lengthBuckets : row.theirLengthBuckets;
      if (!buckets || buckets.every((n) => n === 0)) continue;
      const week = mondayOf(row.date);
      let entry = byWeek.get(week);
      if (!entry) byWeek.set(week, entry = new Array(LENGTH_BUCKET_COUNT).fill(0));
      sumInto(entry, buckets);
    }
    if (byWeek.size === 0) return [];
    const weeks = [...byWeek.keys()].sort();
    const last = weeks[weeks.length - 1];
    const out = [];
    let cursor = weeks[0];
    for (let i = 0; i < MAX_WEEKS; i++) {
      const buckets = byWeek.get(cursor) ?? new Array(LENGTH_BUCKET_COUNT).fill(0);
      const total = buckets.reduce((a2, b) => a2 + b, 0);
      out.push({
        week: cursor,
        total,
        shares: buckets.map((n) => total === 0 ? 0 : Math.round(100 * n / total * 10) / 10)
      });
      if (cursor === last) break;
      cursor = shiftDate(cursor, 7);
    }
    return out;
  }

  // src/ui/charts/axis.ts
  var MONTHS = ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"];
  function monthTicks(dates) {
    const ticks = [];
    let lastMonth = "";
    for (const date2 of dates) {
      const month = date2.slice(0, 7);
      if (month !== lastMonth) {
        ticks.push(date2);
        lastMonth = month;
      }
    }
    return ticks.length > 13 ? ticks.filter((_, i) => i % 2 === 0) : ticks;
  }
  function tickLabel(date2, withYear) {
    const month = MONTHS[Number(date2.slice(5, 7)) - 1] ?? date2;
    return withYear ? `${month} ’${date2.slice(2, 4)}` : month;
  }

  // src/ui/charts/LengthBandsChart.tsx
  var import_jsx_runtime5 = __toESM(require_jsx_runtime(), 1);
  function LengthBandsChart({ data, theme, width }) {
    const rows = data.map((w) => {
      const row = { week: w.week, total: w.total };
      w.shares.forEach((share, i) => row[`b${i}`] = share);
      return row;
    });
    const weeks = data.map((w) => w.week);
    const ticks = monthTicks(weeks);
    const spansYears = weeks.length > 0 && weeks[0].slice(0, 4) !== weeks[weeks.length - 1].slice(0, 4);
    return /* @__PURE__ */ (0, import_jsx_runtime5.jsxs)(AreaChart, { width, height: 240, data: rows, margin: { top: 8, right: 8, bottom: 0, left: 0 }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(
        XAxis,
        {
          dataKey: "week",
          ticks,
          tickFormatter: (d) => tickLabel(d, spansYears),
          tick: { fill: theme.ink.muted, fontSize: 11 },
          axisLine: { stroke: theme.ink.axis },
          tickLine: false,
          interval: "preserveStartEnd"
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(
        YAxis,
        {
          width: 40,
          ticks: [0, 25, 50, 75, 100],
          tickFormatter: (v) => `${v}%`,
          tick: { fill: theme.ink.muted, fontSize: 11 },
          axisLine: false,
          tickLine: false,
          domain: [0, 100]
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(
        Tooltip,
        {
          cursor: { stroke: theme.ink.axis },
          content: ({ active, payload, label }) => {
            if (!active || !payload?.length) return null;
            const row = payload[0].payload;
            if (!row.total) return null;
            return /* @__PURE__ */ (0, import_jsx_runtime5.jsxs)("div", { className: "retraced-tooltip", children: [
              /* @__PURE__ */ (0, import_jsx_runtime5.jsxs)("div", { className: "retraced-tooltip-title", children: [
                "Week of ",
                label
              ] }),
              LENGTH_BUCKET_LABELS.map((bandLabel, i) => /* @__PURE__ */ (0, import_jsx_runtime5.jsxs)("div", { className: "retraced-tooltip-row", children: [
                /* @__PURE__ */ (0, import_jsx_runtime5.jsx)("span", { className: "retraced-legend-dot", style: { background: theme.lengthRamp[i] } }),
                /* @__PURE__ */ (0, import_jsx_runtime5.jsxs)("span", { children: [
                  bandLabel,
                  " chars"
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime5.jsxs)("span", { className: "retraced-tooltip-value", children: [
                  row[`b${i}`],
                  "%"
                ] })
              ] }, bandLabel))
            ] });
          }
        }
      ),
      LENGTH_BUCKET_LABELS.map((_, i) => /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(
        Area,
        {
          dataKey: `b${i}`,
          stackId: "length",
          type: "monotone",
          fill: theme.lengthRamp[i],
          fillOpacity: 1,
          stroke: theme.ink.card,
          strokeWidth: 1.5,
          isAnimationActive: false
        },
        i
      ))
    ] });
  }

  // src/ui/charts/VocabRichnessChart.tsx
  var import_jsx_runtime6 = __toESM(require_jsx_runtime(), 1);
  function VocabRichnessChart({
    data,
    youColor,
    themColor,
    theme,
    width
  }) {
    const weeks = data.map((w) => w.week);
    const ticks = monthTicks(weeks);
    const spansYears = weeks.length > 0 && weeks[0].slice(0, 4) !== weeks[weeks.length - 1].slice(0, 4);
    return /* @__PURE__ */ (0, import_jsx_runtime6.jsxs)(LineChart, { width, height: 220, data, margin: { top: 8, right: 8, bottom: 0, left: 0 }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(CartesianGrid, { vertical: false, stroke: theme.ink.grid }),
      /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(
        XAxis,
        {
          dataKey: "week",
          ticks,
          tickFormatter: (d) => tickLabel(d, spansYears),
          tick: { fill: theme.ink.muted, fontSize: 11 },
          axisLine: { stroke: theme.ink.axis },
          tickLine: false,
          interval: "preserveStartEnd"
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(
        YAxis,
        {
          width: 40,
          tickCount: 4,
          allowDecimals: false,
          tickFormatter: (v) => `${v}%`,
          tick: { fill: theme.ink.muted, fontSize: 11 },
          axisLine: false,
          tickLine: false
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(
        Tooltip,
        {
          cursor: { stroke: theme.ink.axis },
          content: ({ active, payload, label }) => {
            if (!active || !payload?.length) return null;
            const week = payload[0].payload;
            return /* @__PURE__ */ (0, import_jsx_runtime6.jsxs)("div", { className: "retraced-tooltip", children: [
              /* @__PURE__ */ (0, import_jsx_runtime6.jsxs)("div", { className: "retraced-tooltip-title", children: [
                "Week of ",
                label
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime6.jsxs)("div", { className: "retraced-tooltip-row", children: [
                /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("span", { className: "retraced-legend-dot", style: { background: youColor } }),
                /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("span", { children: "you" }),
                /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("span", { className: "retraced-tooltip-value", children: week.minePct === null ? "—" : `${week.minePct}%` })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime6.jsxs)("div", { className: "retraced-tooltip-row", children: [
                /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("span", { className: "retraced-legend-dot", style: { background: themColor } }),
                /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("span", { children: "them (DMs)" }),
                /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("span", { className: "retraced-tooltip-value", children: week.theirsPct === null ? "—" : `${week.theirsPct}%` })
              ] })
            ] });
          }
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(Line, { type: "monotone", dataKey: "minePct", stroke: youColor, strokeWidth: 2, dot: false, connectNulls: false, isAnimationActive: false, activeDot: { r: 3 } }),
      /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(Line, { type: "monotone", dataKey: "theirsPct", stroke: themColor, strokeWidth: 2, dot: false, connectNulls: false, isAnimationActive: false, activeDot: { r: 3 } })
    ] });
  }

  // src/ui/page/SearchCard.tsx
  var import_react68 = __toESM(require_react(), 1);
  var import_jsx_runtime7 = __toESM(require_jsx_runtime(), 1);
  var escapeRegExp = (s) => s.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
  function Highlighted({ content, terms }) {
    const usable = terms.filter((t) => t.length >= 2);
    if (usable.length === 0) return content;
    const re2 = new RegExp(`(${usable.map(escapeRegExp).join("|")})`, "gi");
    const parts = content.split(re2);
    return parts.map((part, i) => i % 2 === 1 ? /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("mark", { children: part }, i) : part);
  }
  function SearchCard({
    engine,
    fromTs,
    channels,
    channelLabel,
    authorLabel
  }) {
    const [query, setQuery] = (0, import_react68.useState)("");
    const [scope, setScope] = (0, import_react68.useState)("");
    const [results, setResults] = (0, import_react68.useState)(null);
    const [searching, setSearching] = (0, import_react68.useState)(false);
    (0, import_react68.useEffect)(() => {
      const db = engine?.getDb();
      if (!db || tokenize(query).length === 0) {
        setResults(null);
        setSearching(false);
        return;
      }
      setSearching(true);
      let alive = true;
      const timer2 = setTimeout(() => {
        searchMessages(db, {
          query,
          channelId: scope === "" ? void 0 : scope,
          fromTs: fromTs ?? void 0
        }).then((rows) => {
          if (alive) {
            setResults(rows);
            setSearching(false);
          }
        }).catch(() => {
          if (alive) setSearching(false);
        });
      }, 250);
      return () => {
        alive = false;
        clearTimeout(timer2);
      };
    }, [engine, query, scope, fromTs]);
    const terms = (0, import_react68.useMemo)(() => tokenize(query), [query]);
    return /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("section", { className: "retraced-chart-card", children: [
      /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("header", { className: "retraced-chart-head", children: /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("div", { children: [
        /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("h3", { className: "retraced-chart-title", children: "Search your history" }),
        /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("p", { className: "retraced-note", children: "covers your messages everywhere and both sides of DMs — other people's server messages are never stored, so they can't be searched" })
      ] }) }),
      /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("div", { className: "retraced-search-controls", children: [
        /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(
          "input",
          {
            type: "search",
            className: "retraced-input",
            placeholder: "type to search…",
            value: query,
            onChange: (e) => setQuery(e.target.value),
            "aria-label": "Search stored messages"
          }
        ),
        /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("select", { className: "retraced-select", value: scope, onChange: (e) => setScope(e.target.value), "aria-label": "Limit to a channel", children: [
          /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("option", { value: "", children: "everywhere" }),
          channels.map((c2) => /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("option", { value: c2.id, children: c2.label }, c2.id))
        ] })
      ] }),
      results === null ? /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("p", { className: "retraced-note retraced-search-hint", children: searching ? "searching…" : "Results appear as you type — the current time range applies." }) : results.length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("div", { className: "retraced-empty", children: [
        /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("span", { className: "retraced-empty-title", children: "No matches" }),
        /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("span", { className: "retraced-note", children: "Nothing stored matches that. Remember: other people's server messages are never kept, so they never match." })
      ] }) : /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("ul", { className: "retraced-search-results", children: [
        results.map((row) => /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("li", { className: "retraced-search-hit", children: [
          /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("div", { className: "retraced-search-meta", children: [
            /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("span", { children: new Date(row.ts).toLocaleString() }),
            /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("span", { children: "·" }),
            /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("span", { children: channelLabel(row.channelId) }),
            /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("span", { children: "·" }),
            /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("span", { children: authorLabel(row) }),
            row.deletedAt !== null ? /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("span", { className: "retraced-chip", children: "deleted" }) : null,
            row.editCount > 0 ? /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("span", { className: "retraced-chip", children: "edited" }) : null
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("div", { className: "retraced-search-content", children: /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(Highlighted, { content: row.content ?? "", terms }) })
        ] }, row.messageId)),
        results.length >= 50 ? /* @__PURE__ */ (0, import_jsx_runtime7.jsxs)("li", { className: "retraced-note", children: [
          "first ",
          formatCount(50),
          " matches shown — narrow the search for more"
        ] }) : null
      ] })
    ] });
  }

  // src/ui/page/useRollups.ts
  var import_react69 = __toESM(require_react(), 1);
  var EMPTY_SNAPSHOT = {
    status: "idle",
    statusDetail: null,
    session: { byKind: {}, total: 0, buffered: 0, flushedEvents: 0, flushes: 0, dropped: 0, openTypingSessions: 0, lastFlushTs: null }
  };
  function useRollups(engine) {
    const subscribe = (0, import_react69.useMemo)(() => engine?.subscribe ?? (() => () => void 0), [engine]);
    const getSnapshot = (0, import_react69.useMemo)(() => engine?.getSnapshot ?? (() => EMPTY_SNAPSHOT), [engine]);
    const snapshot = (0, import_react69.useSyncExternalStore)(subscribe, getSnapshot);
    const [data, setData] = (0, import_react69.useState)({
      daily: [],
      hourly: [],
      peers: [],
      sessions: [],
      voice: [],
      emoji: [],
      domains: [],
      loaded: false,
      dbMissing: false
    });
    (0, import_react69.useEffect)(() => {
      let alive = true;
      const db = engine?.getDb();
      if (!db) {
        setData((d) => ({ ...d, loaded: true, dbMissing: true }));
        return;
      }
      void (async () => {
        const today = dateKey(Date.now());
        const [daily, hourly, peers, sessions, voice, emoji, domains] = await Promise.all([
          getDailyRange(db, null, today),
          getHourlyRange(db, null, today),
          getAllPeers(db),
          getSessionRange(db, null, today),
          getAllVoice(db),
          getAllEmoji(db),
          getAllDomains(db)
        ]);
        if (alive) setData({ daily, hourly, peers, sessions, voice, emoji, domains, loaded: true, dbMissing: false });
      })().catch(() => {
        if (alive) setData((d) => ({ ...d, loaded: true }));
      });
      return () => {
        alive = false;
      };
    }, [engine, snapshot.status, snapshot.session.flushes]);
    return data;
  }

  // src/ui/page/ContentTab.tsx
  var import_jsx_runtime8 = __toESM(require_jsx_runtime(), 1);
  function ContentTab({ engine, rangeDays }) {
    const theme = (0, import_react70.useMemo)(readChartTheme, []);
    const labels = (0, import_react70.useMemo)(createLabelResolvers, []);
    const data = useRollups(engine);
    const [width, measureRef] = useElementWidth();
    const chartWidth = Math.max(320, width - 32);
    const today = dateKey(Date.now());
    const from2 = rangeDays === null ? null : shiftDate(today, -(rangeDays - 1));
    const rangeDaily = (0, import_react70.useMemo)(() => from2 ? data.daily.filter((r2) => r2.date >= from2) : data.daily, [data.daily, from2]);
    const split = (0, import_react70.useMemo)(() => contentTypeSplit(rangeDaily), [rangeDaily]);
    const emojiGroups = (0, import_react70.useMemo)(() => emojiByServer(data.emoji, { guildLabel: labels.guildLabel }), [data.emoji, labels]);
    const [emojiScope, setEmojiScope] = (0, import_react70.useState)(null);
    const emojiGroup = emojiGroups.find((g) => g.scope === emojiScope) ?? emojiGroups[0] ?? null;
    const domains = (0, import_react70.useMemo)(() => topDomains(data.domains), [data.domains]);
    const vocab = (0, import_react70.useMemo)(() => vocabRichnessWeekly(rangeDaily), [rangeDaily]);
    const vocabSignal = vocab.filter((w) => w.minePct !== null || w.theirsPct !== null).length;
    const [termKind, setTermKind] = (0, import_react70.useState)("word");
    const [terms, setTerms] = (0, import_react70.useState)(null);
    (0, import_react70.useEffect)(() => {
      const db = engine?.getDb();
      if (!db || !data.loaded) return;
      let alive = true;
      const stopWords = new Set(engine?.getSettings().stopWords ?? []);
      const skip = (term) => {
        const parts = term.split(" ");
        return parts.every((p) => stopWords.has(p));
      };
      void Promise.all([
        getTopTerms(db, "mine", { kind: termKind, limit: 15, skip }),
        getTopTerms(db, "theirs", { kind: termKind, limit: 15, skip })
      ]).then(([mine, theirs]) => {
        if (alive) setTerms({ mine, theirs });
      });
      return () => {
        alive = false;
      };
    }, [engine, data.loaded, termKind]);
    const [lengthSide, setLengthSide] = (0, import_react70.useState)("mine");
    const lengthWeeks = (0, import_react70.useMemo)(() => lengthShareWeekly(rangeDaily, lengthSide), [rangeDaily, lengthSide]);
    const channelOptions = (0, import_react70.useMemo)(() => {
      const totals = /* @__PURE__ */ new Map();
      for (const row of data.daily) {
        totals.set(row.channelId, (totals.get(row.channelId) ?? 0) + row.sent + (row.theirSent ?? 0));
      }
      return [...totals.entries()].filter(([, n]) => n > 0).sort((a2, b) => b[1] - a2[1]).slice(0, 30).map(([id]) => ({ id, label: labels.channelLabel(id) }));
    }, [data.daily, labels]);
    const peerNames = (0, import_react70.useMemo)(() => new Map(data.peers.map((p) => [p.userId, p.label ?? p.userId])), [data.peers]);
    const authorLabel = (row) => row.isOwn ? "you" : peerNames.get(row.authorId) ?? row.authorId;
    const fromTs = from2 === null ? null : parseDateKey(from2).getTime();
    if (!data.loaded) return /* @__PURE__ */ (0, import_jsx_runtime8.jsx)("p", { className: "retraced-note", children: "loading…" });
    const youColor = theme.series[0];
    const themColor = theme.series[4];
    const emojiUrl = (id) => `https://cdn.discordapp.com/emojis/${id}.png?size=32`;
    return /* @__PURE__ */ (0, import_jsx_runtime8.jsxs)("div", { ref: measureRef, className: "retraced-overview", children: [
      data.dbMissing ? /* @__PURE__ */ (0, import_jsx_runtime8.jsx)("p", { className: "retraced-banner", children: "Capture is unavailable, so charts have nothing to read — see the Overview tab for details." }) : null,
      /* @__PURE__ */ (0, import_jsx_runtime8.jsx)(
        ChartCard,
        {
          title: "What your messages are made of",
          subtitle: "content types across your sent messages",
          legend: split.slices.map((s) => ({ label: s.label, color: colorForSlot(theme, s.colorSlot) })),
          empty: split.total === 0 ? "Send a few messages and their content mix shows up here." : null,
          table: {
            columns: ["Type", "Messages", "Share"],
            rows: split.slices.map((s) => [s.label, s.count, `${Math.round(100 * s.count / Math.max(1, split.total))}%`])
          },
          children: /* @__PURE__ */ (0, import_jsx_runtime8.jsx)(ContentTypeDonut, { data: split, theme, width: chartWidth })
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime8.jsx)(
        ChartCard,
        {
          title: "Top custom emoji",
          subtitle: "your most-used, per server · all time",
          actions: emojiGroups.length > 1 ? /* @__PURE__ */ (0, import_jsx_runtime8.jsx)(
            "select",
            {
              className: "retraced-select",
              value: emojiGroup?.scope ?? "",
              onChange: (e) => setEmojiScope(e.target.value),
              "aria-label": "Choose a server",
              children: emojiGroups.map((g) => /* @__PURE__ */ (0, import_jsx_runtime8.jsx)("option", { value: g.scope, children: g.label }, g.scope))
            }
          ) : null,
          empty: !emojiGroup ? "Use a custom emoji (in a message or reaction) and it lands here." : null,
          table: emojiGroup ? {
            columns: ["Emoji", "Uses"],
            rows: emojiGroup.emoji.map((e) => [`:${e.name}:`, e.count])
          } : null,
          children: emojiGroup ? /* @__PURE__ */ (0, import_jsx_runtime8.jsx)(
            BarRowList,
            {
              rows: emojiGroup.emoji.map((e) => ({
                key: e.emojiId,
                label: `:${e.name}:`,
                value: e.count,
                icon: /* @__PURE__ */ (0, import_jsx_runtime8.jsx)(
                  "img",
                  {
                    className: "retraced-emoji",
                    src: emojiUrl(e.emojiId),
                    alt: "",
                    loading: "lazy",
                    onError: (ev) => {
                      ev.currentTarget.style.display = "none";
                    }
                  }
                )
              })),
              color: theme.series[3],
              theme
            }
          ) : null
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime8.jsx)(
        ChartCard,
        {
          title: "Top link domains",
          subtitle: "links seen in captured channels · all time",
          empty: domains.length === 0 ? "Share a link (or catch one in a DM) and its domain is counted here." : null,
          table: { columns: ["Domain", "Links"], rows: domains.map((d) => [d.domain, d.count]) },
          children: /* @__PURE__ */ (0, import_jsx_runtime8.jsx)(BarRowList, { rows: domains.map((d) => ({ key: d.domain, label: d.domain, value: d.count })), color: colorForSlot(theme, 2), theme })
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime8.jsx)(
        ChartCard,
        {
          title: "Vocabulary richness",
          subtitle: "share of each week's words that were new that day · your line spans everywhere, theirs is DMs only",
          legend: [
            { label: "You — everywhere", color: youColor },
            { label: "Them — DMs only", color: themColor }
          ],
          empty: vocabSignal < 2 ? "Needs a couple of weeks with enough words to mean anything — keep chatting." : null,
          table: {
            columns: ["Week", "You %", "Them (DMs) %"],
            rows: vocab.map((w) => [w.week, w.minePct === null ? "—" : `${w.minePct}%`, w.theirsPct === null ? "—" : `${w.theirsPct}%`])
          },
          children: /* @__PURE__ */ (0, import_jsx_runtime8.jsx)(VocabRichnessChart, { data: vocab, youColor, themColor, theme, width: chartWidth })
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime8.jsx)(
        ChartCard,
        {
          title: "Most-used words",
          subtitle: "you everywhere · them in DMs only · stopwords editable in plugin settings",
          actions: /* @__PURE__ */ (0, import_jsx_runtime8.jsxs)("div", { className: "retraced-ranges", role: "group", "aria-label": "Words or phrases", children: [
            /* @__PURE__ */ (0, import_jsx_runtime8.jsx)("button", { type: "button", className: "retraced-range-pill", "aria-pressed": termKind === "word", onClick: () => setTermKind("word"), children: "words" }),
            /* @__PURE__ */ (0, import_jsx_runtime8.jsx)("button", { type: "button", className: "retraced-range-pill", "aria-pressed": termKind === "phrase", onClick: () => setTermKind("phrase"), children: "phrases" })
          ] }),
          empty: terms !== null && terms.mine.length === 0 && terms.theirs.length === 0 ? "Once messages accumulate, the words you lean on show up here." : null,
          table: terms ? {
            columns: ["Yours", "Count", "Theirs (DMs)", "Count"],
            rows: Array.from({ length: Math.max(terms.mine.length, terms.theirs.length) }, (_, i) => [
              terms.mine[i]?.term ?? "",
              terms.mine[i]?.count ?? "",
              terms.theirs[i]?.term ?? "",
              terms.theirs[i]?.count ?? ""
            ])
          } : null,
          children: terms ? /* @__PURE__ */ (0, import_jsx_runtime8.jsxs)("div", { className: "retraced-words-columns", children: [
            /* @__PURE__ */ (0, import_jsx_runtime8.jsxs)("div", { children: [
              /* @__PURE__ */ (0, import_jsx_runtime8.jsx)("p", { className: "retraced-note retraced-words-head", children: "You — everywhere" }),
              /* @__PURE__ */ (0, import_jsx_runtime8.jsx)(BarRowList, { rows: terms.mine.map((t) => ({ key: t.term, label: t.term, value: t.count })), color: youColor, theme })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime8.jsxs)("div", { children: [
              /* @__PURE__ */ (0, import_jsx_runtime8.jsx)("p", { className: "retraced-note retraced-words-head", children: "Them — DMs only" }),
              /* @__PURE__ */ (0, import_jsx_runtime8.jsx)(BarRowList, { rows: terms.theirs.map((t) => ({ key: t.term, label: t.term, value: t.count })), color: themColor, theme })
            ] })
          ] }) : /* @__PURE__ */ (0, import_jsx_runtime8.jsx)("p", { className: "retraced-note", children: "loading…" })
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime8.jsx)(
        ChartCard,
        {
          title: "Message length over time",
          subtitle: "weekly share by length band (characters)",
          legend: LENGTH_BUCKET_LABELS.map((label, i) => ({ label, color: theme.lengthRamp[i] })),
          actions: /* @__PURE__ */ (0, import_jsx_runtime8.jsxs)("div", { className: "retraced-ranges", role: "group", "aria-label": "Whose messages", children: [
            /* @__PURE__ */ (0, import_jsx_runtime8.jsx)("button", { type: "button", className: "retraced-range-pill", "aria-pressed": lengthSide === "mine", onClick: () => setLengthSide("mine"), children: "yours" }),
            /* @__PURE__ */ (0, import_jsx_runtime8.jsx)("button", { type: "button", className: "retraced-range-pill", "aria-pressed": lengthSide === "theirs", onClick: () => setLengthSide("theirs"), children: "theirs (DMs)" })
          ] }),
          empty: lengthWeeks.length < 2 ? "Length bands need a couple of weeks of messages — captured from Phase 6 onward." : null,
          table: {
            columns: ["Week", ...LENGTH_BUCKET_LABELS.map((l) => `${l} chars`), "Messages"],
            rows: lengthWeeks.map((w) => [w.week, ...w.shares.map((s) => `${s}%`), formatCount(w.total)])
          },
          children: /* @__PURE__ */ (0, import_jsx_runtime8.jsx)(LengthBandsChart, { data: lengthWeeks, theme, width: chartWidth })
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime8.jsx)(SearchCard, { engine, fromTs, channels: channelOptions, channelLabel: labels.channelLabel, authorLabel })
    ] });
  }

  // src/ui/page/DataTab.tsx
  var import_react71 = __toESM(require_react(), 1);

  // src/db/export.ts
  async function exportAll(db) {
    const stores = {};
    for (const name of ALL_STORES) {
      if (name === "meta") {
        const tx = db.transaction("meta");
        const [keys, values] = await Promise.all([tx.store.getAllKeys(), tx.store.getAll()]);
        stores.meta = keys.map((key, i) => [key, values[i]]);
      } else {
        stores[name] = await db.getAll(name);
      }
    }
    return { format: "retraced-export", version: DB_VERSION, exportedAt: Date.now(), stores };
  }
  function validate(envelope) {
    if (!envelope || typeof envelope !== "object") throw new Error("not a Retraced export");
    if (envelope.format !== "retraced-export") throw new Error("not a Retraced export");
    if (typeof envelope.version !== "number" || envelope.version < 1 || envelope.version > DB_VERSION) {
      throw new Error(`unsupported export version ${String(envelope.version)} (this build reads up to v${DB_VERSION})`);
    }
    if (!envelope.stores || typeof envelope.stores !== "object") throw new Error("export has no stores");
    for (const rows of Object.values(envelope.stores)) {
      if (!Array.isArray(rows)) throw new Error("malformed store payload");
    }
  }
  async function importAll(db, envelope) {
    validate(envelope);
    await wipeAll(db);
    let rows = 0;
    const tx = db.transaction([...ALL_STORES], "readwrite");
    for (const name of ALL_STORES) {
      const payload = envelope.stores[name];
      if (!payload) continue;
      const store = tx.objectStore(name);
      if (name === "meta") {
        for (const entry of payload) {
          void store.put(entry[1], entry[0]);
          rows++;
        }
      } else {
        for (const row of payload) {
          void store.put(row);
          rows++;
        }
      }
    }
    await tx.done;
    return { rows };
  }

  // src/db/rebuild.ts
  var BATCH = 2e3;
  async function rebuildFromEvents(db, opts) {
    const eventRows = await db.getAll("events");
    if (eventRows.length === 0) return { events: 0 };
    eventRows.sort((a2, b) => a2.ts - b.ts);
    const fromDate = dateKey(eventRows[0].ts);
    const toDate = dateKey(eventRows[eventRows.length - 1].ts);
    {
      const tx = db.transaction(["daily", "hourly", "voice", "sessions"], "readwrite");
      let dailyCursor = await tx.objectStore("daily").index("date").openCursor(IDBKeyRange.bound(fromDate, toDate));
      while (dailyCursor) {
        await dailyCursor.delete();
        dailyCursor = await dailyCursor.continue();
      }
      void tx.objectStore("hourly").delete(IDBKeyRange.bound([fromDate, 0], [toDate, 23]));
      void tx.objectStore("voice").delete(IDBKeyRange.bound([fromDate, ""], [toDate, "￿"]));
      void tx.objectStore("sessions").delete(IDBKeyRange.bound(fromDate, toDate));
      await tx.done;
    }
    const ctx = createReducerContext({
      ownUserId: "",
      // only read on peer-attribution paths, whose outputs are stripped below
      settings: () => ({ conversationGapMinutes: opts.conversationGapMinutes }),
      resolvePeerLabel: () => null,
      dmRecipients: () => null
    });
    for (let i = 0; i < eventRows.length; i += BATCH) {
      const pending = new PendingBuffer();
      for (const row of eventRows.slice(i, i + BATCH)) reduceEvent(row.event, pending, ctx);
      pending.events = [];
      pending.messageCreates.clear();
      pending.messageEdits = [];
      pending.messageDeletes = [];
      pending.peers.clear();
      pending.emoji.clear();
      pending.domains.clear();
      pending.words.clear();
      pending.searchIndex = [];
      await flushPending(db, pending, { now: Date.now(), heads: ctx.heads });
    }
    {
      const tx = db.transaction(["messages", "daily"], "readwrite");
      const daily = tx.objectStore("daily");
      let cursor = await tx.objectStore("messages").openCursor();
      while (cursor) {
        const row = cursor.value;
        if (row.isOwn && row.deletedAt !== null) {
          const date2 = dateKey(row.deletedAt);
          if (date2 >= fromDate && date2 <= toDate) {
            const dailyRow = await daily.get([date2, row.channelId]) ?? emptyDaily(date2, row.channelId, row.guildId);
            dailyRow.deleted += 1;
            dailyRow.deleteAfterBuckets = addToBuckets(
              dailyRow.deleteAfterBuckets,
              DELETE_BUCKET_COUNT,
              row.deleteAfterMs ?? 0,
              DELETE_BUCKET_BOUNDS_MS
            );
            await daily.put(dailyRow);
          }
        }
        cursor = await cursor.continue();
      }
      await tx.done;
    }
    return { events: eventRows.length };
  }
  async function rebuildContentIndex(db) {
    const tx = db.transaction(["messages", "words", "searchIndex"], "readwrite");
    void tx.objectStore("words").clear();
    void tx.objectStore("searchIndex").clear();
    const searchIndex = tx.objectStore("searchIndex");
    const wordCounts = /* @__PURE__ */ new Map();
    let processed = 0;
    let cursor = await tx.objectStore("messages").openCursor();
    while (cursor) {
      const row = cursor.value;
      if (row.content !== null) {
        processed++;
        const scope = row.isOwn ? "mine" : "theirs";
        const tokens = tokenize(row.content);
        for (const token of tokens) {
          const key = `${scope} ${token}`;
          wordCounts.set(key, (wordCounts.get(key) ?? 0) + 1);
        }
        for (const phrase of bigrams(tokens)) {
          const key = `${scope} ${phrase}`;
          wordCounts.set(key, (wordCounts.get(key) ?? 0) + 1);
        }
        for (const term of new Set(tokens)) {
          void searchIndex.put({ term, messageId: row.messageId, ts: row.ts, channelId: row.channelId, guildId: row.guildId, isOwn: row.isOwn });
        }
      }
      cursor = await cursor.continue();
    }
    const words = tx.objectStore("words");
    for (const [key, count] of wordCounts) {
      const sep = key.indexOf(" ");
      void words.put({ scope: key.slice(0, sep), term: key.slice(sep + 1), count });
    }
    await tx.done;
    return { messages: processed };
  }

  // src/ui/page/DataTab.tsx
  var import_jsx_runtime9 = __toESM(require_jsx_runtime(), 1);
  var DAY_MS = 864e5;
  function formatBytes(n) {
    if (n >= 1048576) return `${(n / 1048576).toFixed(1)} MB`;
    if (n >= 1024) return `${(n / 1024).toFixed(0)} kB`;
    return `${n} B`;
  }
  function DataTab({ engine }) {
    const data = useRollups(engine);
    const db = engine?.getDb() ?? null;
    const [stats, setStats] = (0, import_react71.useState)(null);
    const [usage, setUsage] = (0, import_react71.useState)(null);
    const [lastExportTs, setLastExportTs] = (0, import_react71.useState)(null);
    const [reload, setReload] = (0, import_react71.useState)(0);
    const [busy, setBusy] = (0, import_react71.useState)(null);
    const [status, setStatus] = (0, import_react71.useState)(null);
    const [pending, setPending] = (0, import_react71.useState)(null);
    const [peerId, setPeerId] = (0, import_react71.useState)("");
    const [fromDate, setFromDate] = (0, import_react71.useState)("");
    const [toDate, setToDate] = (0, import_react71.useState)("");
    const fileRef = (0, import_react71.useRef)(null);
    (0, import_react71.useEffect)(() => {
      if (!db) return;
      let alive = true;
      void getStoreStats(db).then((s) => alive && setStats(s));
      void engine?.getStorageEstimate().then((e) => alive && setUsage(e));
      void db.get("meta", "lastExportTs").then((ts) => alive && setLastExportTs(typeof ts === "number" ? ts : null));
      return () => {
        alive = false;
      };
    }, [db, engine, reload]);
    const runAction = (0, import_react71.useCallback)(
      (label, action) => {
        setBusy(label);
        setStatus(null);
        setPending(null);
        void action().then((message) => setStatus(message)).catch((e) => setStatus(`${label} failed: ${String(e?.message ?? e)}`)).finally(() => {
          setBusy(null);
          setReload((n) => n + 1);
        });
      },
      []
    );
    const doExport = () => {
      if (!db) return;
      runAction("export", async () => {
        const envelope = await exportAll(db);
        const stamp = (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
        const blob = new Blob([JSON.stringify(envelope)], { type: "application/json" });
        const url = URL.createObjectURL(blob);
        const a2 = document.createElement("a");
        a2.href = url;
        a2.download = `retraced-backup-${stamp}.json`;
        a2.click();
        URL.revokeObjectURL(url);
        await db.put("meta", Date.now(), "lastExportTs");
        const rows = Object.values(envelope.stores).reduce((n, r2) => n + r2.length, 0);
        return `Exported ${formatCount(rows)} rows.`;
      });
    };
    const onImportFile = (file) => {
      if (!db || !file) return;
      void file.text().then((text) => {
        const envelope = JSON.parse(text);
        if (envelope?.format !== "retraced-export") throw new Error("that file is not a Retraced backup");
        const rows = Object.values(envelope.stores ?? {}).reduce((n, r2) => n + (Array.isArray(r2) ? r2.length : 0), 0);
        setPending({
          description: `Replace everything with the backup from ${new Date(envelope.exportedAt).toLocaleDateString()} (${formatCount(rows)} rows)? Current data is erased first.`,
          run: async () => {
            const result = await importAll(db, envelope);
            return `Imported ${formatCount(result.rows)} rows.`;
          }
        });
      }).catch((e) => setStatus(`Import failed: ${String(e?.message ?? e)}`));
    };
    const peers = (0, import_react71.useMemo)(
      () => [...data.peers].sort((a2, b) => (a2.label ?? a2.userId).localeCompare(b.label ?? b.userId)),
      [data.peers]
    );
    const totalBytes = stats?.reduce((n, s) => n + s.approxBytes, 0) ?? 0;
    const exportAge = lastExportTs === null ? null : Math.floor((Date.now() - lastExportTs) / DAY_MS);
    const exportStale = exportAge === null || exportAge > 30;
    const disabled = !db || busy !== null;
    return /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("div", { className: "retraced-overview", children: [
      !db ? /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("p", { className: "retraced-banner", children: "The database is unavailable, so there is nothing to manage here." }) : null,
      status ? /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("p", { className: "retraced-banner retraced-banner--info", children: status }) : null,
      pending ? /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("div", { className: "retraced-banner retraced-confirm", children: [
        /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("span", { children: pending.description }),
        /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("span", { className: "retraced-confirm-buttons", children: [
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("button", { type: "button", className: "retraced-button retraced-button--danger", onClick: () => runAction("confirm", pending.run), children: "Yes, do it" }),
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("button", { type: "button", className: "retraced-ghost-button", onClick: () => setPending(null), children: "Cancel" })
        ] })
      ] }) : null,
      /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("section", { className: "retraced-chart-card", children: [
        /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("header", { className: "retraced-chart-head", children: /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("h3", { className: "retraced-chart-title", children: "Keep a copy" }),
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("p", { className: "retraced-note", children: "Everything lives in Discord's browser storage on this device — a reinstall or cache clear can erase it without warning." })
        ] }) }),
        /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("div", { className: "retraced-devtools-row", children: [
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("button", { type: "button", className: "retraced-button", disabled, onClick: doExport, children: "Export backup" }),
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("button", { type: "button", className: "retraced-ghost-button", disabled, onClick: () => fileRef.current?.click(), children: "Import backup…" }),
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(
            "input",
            {
              ref: fileRef,
              type: "file",
              accept: "application/json,.json",
              style: { display: "none" },
              onChange: (e) => {
                onImportFile(e.currentTarget.files?.[0]);
                e.currentTarget.value = "";
              }
            }
          ),
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("span", { className: exportStale ? "retraced-note retraced-note--nudge" : "retraced-note", children: exportAge === null ? "never exported yet" : exportAge === 0 ? "last export: today" : `last export: ${exportAge} days ago` })
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("section", { className: "retraced-chart-card", children: [
        /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("header", { className: "retraced-chart-head", children: /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("h3", { className: "retraced-chart-title", children: "Storage" }),
          /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("p", { className: "retraced-note", children: [
            "rows and estimated size per store",
            usage ? ` · browser reports ${formatBytes(usage.usage)} used of ${formatBytes(usage.quota)}` : ""
          ] })
        ] }) }),
        stats ? /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("div", { className: "retraced-table-wrap", children: /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("table", { className: "retraced-table", children: [
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("thead", { children: /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("tr", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("th", { children: "Store" }),
            /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("th", { children: "Rows" }),
            /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("th", { children: "~Size" })
          ] }) }),
          /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("tbody", { children: [
            stats.map((s) => /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("tr", { children: [
              /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("td", { children: s.name }),
              /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("td", { children: formatCount(s.rows) }),
              /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("td", { children: formatBytes(s.approxBytes) })
            ] }, s.name)),
            /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("tr", { children: [
              /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("td", { children: "total (logical)" }),
              /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("td", {}),
              /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("td", { children: formatBytes(totalBytes) })
            ] })
          ] })
        ] }) }) : /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("p", { className: "retraced-note", children: "loading…" })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("section", { className: "retraced-chart-card", children: [
        /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("header", { className: "retraced-chart-head", children: /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("h3", { className: "retraced-chart-title", children: "Remove data" }),
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("p", { className: "retraced-note", children: "every removal asks once, then acts — there is no undo beyond your backups" })
        ] }) }),
        /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("div", { className: "retraced-devtools-row", children: [
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(
            "button",
            {
              type: "button",
              className: "retraced-ghost-button",
              disabled,
              onClick: () => setPending({
                description: "Erase message text everywhere (messages, search, word lists)? Counts and every chart except the content ones survive.",
                run: async () => {
                  await wipeContentOnly(db);
                  return "Stored text erased. Rollups kept.";
                }
              }),
              children: "Content only"
            }
          ),
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(
            "button",
            {
              type: "button",
              className: "retraced-ghost-button retraced-ghost-button--danger",
              disabled,
              onClick: () => setPending({
                description: "Erase EVERYTHING Retraced has stored — statistics included? This is the full reset.",
                run: async () => {
                  await wipeAll(db);
                  return "Everything erased.";
                }
              }),
              children: "Wipe everything"
            }
          )
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("div", { className: "retraced-devtools-row", children: [
          /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("select", { className: "retraced-select", value: peerId, onChange: (e) => setPeerId(e.target.value), "aria-label": "Person to remove", disabled, children: [
            /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("option", { value: "", children: "remove a person…" }),
            peers.map((p) => /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("option", { value: p.userId, children: p.label ?? p.userId }, p.userId))
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(
            "button",
            {
              type: "button",
              className: "retraced-ghost-button",
              disabled: disabled || peerId === "",
              onClick: () => {
                const peer = peers.find((p) => p.userId === peerId);
                setPending({
                  description: `Remove ${peer?.label ?? peerId} — their profile row, their messages, and their search entries?`,
                  run: async () => {
                    await wipePeer(db, peerId);
                    setPeerId("");
                    return "Person removed.";
                  }
                });
              },
              children: "Remove person"
            }
          )
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("div", { className: "retraced-devtools-row", children: [
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("input", { type: "date", className: "retraced-input retraced-input--date", value: fromDate, onChange: (e) => setFromDate(e.target.value), "aria-label": "From date", disabled }),
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("span", { className: "retraced-note", children: "to" }),
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("input", { type: "date", className: "retraced-input retraced-input--date", value: toDate, onChange: (e) => setToDate(e.target.value), "aria-label": "To date", disabled }),
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(
            "button",
            {
              type: "button",
              className: "retraced-ghost-button",
              disabled: disabled || fromDate === "" || toDate === "" || fromDate > toDate,
              onClick: () => setPending({
                description: `Erase everything dated ${fromDate} through ${toDate} — messages, statistics, voice, sessions? All-time totals (people, words, emoji) stay.`,
                run: async () => {
                  await wipeDateRange(db, parseDateKey(fromDate).getTime(), parseDateKey(toDate).getTime() + DAY_MS - 1);
                  return `Erased ${fromDate} – ${toDate}.`;
                }
              }),
              children: "Remove range"
            }
          )
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("section", { className: "retraced-chart-card", children: [
        /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("header", { className: "retraced-chart-head", children: /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("h3", { className: "retraced-chart-title", children: "Repair" }),
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("p", { className: "retraced-note", children: "for when something looks wrong — both are safe to run any time" })
        ] }) }),
        /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("div", { className: "retraced-devtools-row", children: [
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(
            "button",
            {
              type: "button",
              className: "retraced-ghost-button",
              disabled,
              onClick: () => runAction("rebuild statistics", async () => {
                const result = await rebuildFromEvents(db, {
                  conversationGapMinutes: engine?.getSettings().conversationGapMinutes ?? 30
                });
                return `Recomputed daily statistics from ${formatCount(result.events)} logged events.`;
              }),
              children: "Rebuild recent statistics"
            }
          ),
          /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("span", { className: "retraced-note", children: [
            "replays the raw event log (last ",
            engine?.getSettings().eventRetentionDays ?? 30,
            " days) through the pipeline"
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("div", { className: "retraced-devtools-row", children: [
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(
            "button",
            {
              type: "button",
              className: "retraced-ghost-button",
              disabled,
              onClick: () => runAction("rebuild search", async () => {
                const result = await rebuildContentIndex(db);
                return `Search index rebuilt from ${formatCount(result.messages)} stored messages.`;
              }),
              children: "Rebuild search index"
            }
          ),
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("span", { className: "retraced-note", children: "re-reads every stored message — also backfills search for history captured before the index existed" })
        ] }),
        busy ? /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("p", { className: "retraced-note", children: [
          "working: ",
          busy,
          "…"
        ] }) : null
      ] })
    ] });
  }

  // src/ui/page/HesitationTab.tsx
  var import_react72 = __toESM(require_react(), 1);

  // src/ui/charts/ChannelHesitationChart.tsx
  var import_jsx_runtime10 = __toESM(require_jsx_runtime(), 1);
  function niceCeil(n) {
    const power = 10 ** Math.floor(Math.log10(Math.max(n, 1)));
    const mantissa = n / power;
    const step = [1, 1.5, 2, 2.5, 3, 4, 5, 6, 8, 10].find((s) => mantissa <= s) ?? 10;
    return step * power;
  }
  function ChannelHesitationChart({ data, theme, width }) {
    const rows = data.map((c2) => ({ ...c2, abortedNeg: -c2.aborted }));
    const height = 40 + rows.length * 30;
    const aborted = theme.series[5];
    const committed = theme.series[0];
    const left2 = niceCeil(Math.max(...data.map((c2) => c2.aborted), 1));
    const right2 = niceCeil(Math.max(...data.map((c2) => c2.committed), 1));
    return /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)(BarChart, { layout: "vertical", width, height, data: rows, stackOffset: "sign", margin: { top: 4, right: 16, bottom: 0, left: 8 }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(
        XAxis,
        {
          type: "number",
          domain: [-left2, right2],
          ticks: [-left2, 0, Math.round(right2 / 2), right2],
          tickFormatter: (v) => formatCount(Math.abs(v)),
          tick: { fill: theme.ink.muted, fontSize: 11 },
          axisLine: { stroke: theme.ink.axis },
          tickLine: false
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(YAxis, { type: "category", dataKey: "label", width: 150, tick: { fill: theme.ink.muted, fontSize: 11 }, axisLine: false, tickLine: false }),
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(ReferenceLine, { x: 0, stroke: theme.ink.axis }),
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(
        Tooltip,
        {
          cursor: { fill: theme.ink.grid },
          content: ({ active, payload }) => {
            if (!active || !payload?.length) return null;
            const row = payload[0].payload;
            return /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("div", { className: "retraced-tooltip", children: [
              /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("div", { className: "retraced-tooltip-title", children: row.label }),
              /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("div", { className: "retraced-tooltip-row", children: [
                /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: "retraced-legend-dot", style: { background: aborted } }),
                /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { children: "aborted" }),
                /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: "retraced-tooltip-value", children: formatCount(row.aborted) })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("div", { className: "retraced-tooltip-row", children: [
                /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: "retraced-legend-dot", style: { background: committed } }),
                /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { children: "committed" }),
                /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("span", { className: "retraced-tooltip-value", children: formatCount(row.committed) })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime10.jsxs)("div", { className: "retraced-note", children: [
                "hesitation ",
                row.pct,
                "%"
              ] })
            ] });
          }
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(Bar, { dataKey: "abortedNeg", stackId: "typing", fill: aborted, radius: [4, 0, 0, 4], maxBarSize: 16, isAnimationActive: false }),
      /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(Bar, { dataKey: "committed", stackId: "typing", fill: committed, radius: [0, 4, 4, 0], maxBarSize: 16, isAnimationActive: false })
    ] });
  }

  // src/ui/data/hesitation.ts
  var MAX_WEEKS2 = 800;
  var MIN_WEEK_RESOLUTIONS = 3;
  var MIN_CHANNEL_RESOLUTIONS = 5;
  var round1 = (n) => Math.round(n * 10) / 10;
  function disambiguate(rows, guildLabel) {
    if (!guildLabel) return rows;
    const counts = /* @__PURE__ */ new Map();
    for (const row of rows) counts.set(row.label, (counts.get(row.label) ?? 0) + 1);
    return rows.map(
      (row) => (counts.get(row.label) ?? 0) > 1 && row.guildId !== null ? { ...row, label: `${row.label} · ${guildLabel(row.guildId)}` } : row
    );
  }
  function hesitationWeekly(rows) {
    const byWeek = /* @__PURE__ */ new Map();
    for (const row of rows) {
      if (row.typingAborted === 0 && row.typingCommitted === 0) continue;
      const week = mondayOf(row.date);
      const entry = byWeek.get(week) ?? { aborted: 0, committed: 0 };
      entry.aborted += row.typingAborted;
      entry.committed += row.typingCommitted;
      byWeek.set(week, entry);
    }
    if (byWeek.size === 0) return [];
    const weeks = [...byWeek.keys()].sort();
    const last = weeks[weeks.length - 1];
    const out = [];
    let cursor = weeks[0];
    for (let i = 0; i < MAX_WEEKS2; i++) {
      const entry = byWeek.get(cursor) ?? { aborted: 0, committed: 0 };
      const total = entry.aborted + entry.committed;
      out.push({
        week: cursor,
        aborted: entry.aborted,
        committed: entry.committed,
        pct: total >= MIN_WEEK_RESOLUTIONS ? round1(100 * entry.aborted / total) : null
      });
      if (cursor === last) break;
      cursor = shiftDate(cursor, 7);
    }
    return out;
  }
  function channelHesitation(rows, opts) {
    const byChannel = /* @__PURE__ */ new Map();
    for (const row of rows) {
      if (row.typingAborted === 0 && row.typingCommitted === 0) continue;
      const entry = byChannel.get(row.channelId) ?? { guildId: row.guildId, aborted: 0, committed: 0 };
      entry.aborted += row.typingAborted;
      entry.committed += row.typingCommitted;
      if (entry.guildId === null && row.guildId !== null) entry.guildId = row.guildId;
      byChannel.set(row.channelId, entry);
    }
    const out = [];
    for (const [channelId, entry] of byChannel) {
      const total = entry.aborted + entry.committed;
      if (total < MIN_CHANNEL_RESOLUTIONS) continue;
      out.push({
        channelId,
        guildId: entry.guildId,
        label: opts.channelLabel(channelId),
        aborted: entry.aborted,
        committed: entry.committed,
        pct: round1(100 * entry.aborted / total)
      });
    }
    const volume = (c2) => c2.aborted + c2.committed;
    out.sort(opts.sortBy === "rate" ? (a2, b) => b.pct - a2.pct || volume(b) - volume(a2) : (a2, b) => volume(b) - volume(a2));
    return disambiguate(out.slice(0, opts.max ?? 12), opts.guildLabel);
  }
  function peerHesitation(peers, max4 = 10) {
    return peers.filter((p) => p.typingAbortedAtThem > 0).sort((a2, b) => b.typingAbortedAtThem - a2.typingAbortedAtThem).slice(0, max4).map((p) => ({ userId: p.userId, label: p.label ?? p.userId, aborted: p.typingAbortedAtThem }));
  }
  function editDeleteRates(rows, opts) {
    const minSent = opts.minSent ?? 20;
    const byChannel = /* @__PURE__ */ new Map();
    for (const row of rows) {
      if (row.sent === 0 && row.edited === 0 && row.deleted === 0) continue;
      const entry = byChannel.get(row.channelId) ?? { guildId: row.guildId, sent: 0, edited: 0, deleted: 0 };
      entry.sent += row.sent;
      entry.edited += row.edited;
      entry.deleted += row.deleted;
      if (entry.guildId === null && row.guildId !== null) entry.guildId = row.guildId;
      byChannel.set(row.channelId, entry);
    }
    const out = [...byChannel.entries()].filter(([, e]) => e.sent >= minSent).sort((a2, b) => b[1].sent - a2[1].sent).slice(0, opts.max ?? 10).map(([channelId, e]) => ({
      channelId,
      guildId: e.guildId,
      label: opts.channelLabel(channelId),
      sent: e.sent,
      editPct: round1(100 * e.edited / e.sent),
      deletePct: round1(100 * e.deleted / e.sent)
    }));
    return disambiguate(out, opts.guildLabel);
  }
  var DELETE_BUCKET_LABELS = ["<10s", "10–60s", "1–10m", "10m–1h", "1–6h", "6–24h", "24h+"];
  function deleteLifetime(rows) {
    const buckets = new Array(DELETE_BUCKET_COUNT).fill(0);
    for (const row of rows) sumInto(buckets, row.deleteAfterBuckets);
    return {
      buckets,
      total: buckets.reduce((a2, b) => a2 + b, 0),
      medianBucket: medianBucket(buckets)
    };
  }

  // src/ui/charts/DeleteLifetimeChart.tsx
  var import_jsx_runtime11 = __toESM(require_jsx_runtime(), 1);
  function DeleteLifetimeChart({ data, theme, width }) {
    const rows = data.buckets.map((count, i) => ({ label: DELETE_BUCKET_LABELS[i], count }));
    const deleted = theme.series[7];
    return /* @__PURE__ */ (0, import_jsx_runtime11.jsxs)(BarChart, { width, height: 200, data: rows, margin: { top: 8, right: 8, bottom: 0, left: 0 }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime11.jsx)(CartesianGrid, { vertical: false, stroke: theme.ink.grid }),
      /* @__PURE__ */ (0, import_jsx_runtime11.jsx)(XAxis, { dataKey: "label", tick: { fill: theme.ink.muted, fontSize: 11 }, axisLine: { stroke: theme.ink.axis }, tickLine: false, interval: 0 }),
      /* @__PURE__ */ (0, import_jsx_runtime11.jsx)(YAxis, { width: 36, tickCount: 4, allowDecimals: false, tick: { fill: theme.ink.muted, fontSize: 11 }, axisLine: false, tickLine: false }),
      /* @__PURE__ */ (0, import_jsx_runtime11.jsx)(
        Tooltip,
        {
          cursor: { fill: theme.ink.grid },
          content: ({ active, payload }) => {
            if (!active || !payload?.length) return null;
            const row = payload[0].payload;
            return /* @__PURE__ */ (0, import_jsx_runtime11.jsxs)("div", { className: "retraced-tooltip", children: [
              /* @__PURE__ */ (0, import_jsx_runtime11.jsxs)("div", { className: "retraced-tooltip-title", children: [
                "lived ",
                row.label
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime11.jsxs)("div", { children: [
                formatCount(row.count),
                " deletions"
              ] })
            ] });
          }
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime11.jsx)(Bar, { dataKey: "count", fill: deleted, radius: [4, 4, 0, 0], maxBarSize: 44, isAnimationActive: false })
    ] });
  }

  // src/ui/charts/EditDeleteChart.tsx
  var import_jsx_runtime12 = __toESM(require_jsx_runtime(), 1);
  function EditDeleteChart({ data, theme, width }) {
    const height = 40 + data.length * 42;
    const edited = theme.series[6];
    const deleted = theme.series[7];
    return /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)(BarChart, { layout: "vertical", width, height, data, margin: { top: 4, right: 16, bottom: 0, left: 8 }, barGap: 2, children: [
      /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(
        XAxis,
        {
          type: "number",
          tickFormatter: (v) => `${v}%`,
          tick: { fill: theme.ink.muted, fontSize: 11 },
          axisLine: { stroke: theme.ink.axis },
          tickLine: false
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(YAxis, { type: "category", dataKey: "label", width: 150, tick: { fill: theme.ink.muted, fontSize: 11 }, axisLine: false, tickLine: false }),
      /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(
        Tooltip,
        {
          cursor: { fill: theme.ink.grid },
          content: ({ active, payload }) => {
            if (!active || !payload?.length) return null;
            const row = payload[0].payload;
            return /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)("div", { className: "retraced-tooltip", children: [
              /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("div", { className: "retraced-tooltip-title", children: row.label }),
              /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)("div", { className: "retraced-tooltip-row", children: [
                /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("span", { className: "retraced-legend-dot", style: { background: edited } }),
                /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("span", { children: "edited" }),
                /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)("span", { className: "retraced-tooltip-value", children: [
                  row.editPct,
                  "%"
                ] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)("div", { className: "retraced-tooltip-row", children: [
                /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("span", { className: "retraced-legend-dot", style: { background: deleted } }),
                /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("span", { children: "deleted" }),
                /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)("span", { className: "retraced-tooltip-value", children: [
                  row.deletePct,
                  "%"
                ] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)("div", { className: "retraced-note", children: [
                "of ",
                formatCount(row.sent),
                " sent"
              ] })
            ] });
          }
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(Bar, { dataKey: "editPct", fill: edited, radius: [0, 4, 4, 0], maxBarSize: 12, isAnimationActive: false }),
      /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(Bar, { dataKey: "deletePct", fill: deleted, radius: [0, 4, 4, 0], maxBarSize: 12, isAnimationActive: false })
    ] });
  }

  // src/ui/charts/HesitationOverTime.tsx
  var import_jsx_runtime13 = __toESM(require_jsx_runtime(), 1);
  function HesitationOverTime({ data, theme, width }) {
    const weeks = data.map((w) => w.week);
    const ticks = monthTicks(weeks);
    const spansYears = weeks.length > 0 && weeks[0].slice(0, 4) !== weeks[weeks.length - 1].slice(0, 4);
    const aborted = theme.series[5];
    return /* @__PURE__ */ (0, import_jsx_runtime13.jsxs)(LineChart, { width, height: 220, data, margin: { top: 8, right: 8, bottom: 0, left: 0 }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(CartesianGrid, { vertical: false, stroke: theme.ink.grid }),
      /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(
        XAxis,
        {
          dataKey: "week",
          ticks,
          tickFormatter: (d) => tickLabel(d, spansYears),
          tick: { fill: theme.ink.muted, fontSize: 11 },
          axisLine: { stroke: theme.ink.axis },
          tickLine: false,
          interval: "preserveStartEnd"
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(
        YAxis,
        {
          width: 40,
          tickCount: 4,
          allowDecimals: false,
          tickFormatter: (v) => `${v}%`,
          tick: { fill: theme.ink.muted, fontSize: 11 },
          axisLine: false,
          tickLine: false
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(
        Tooltip,
        {
          cursor: { stroke: theme.ink.axis },
          content: ({ active, payload, label }) => {
            if (!active || !payload?.length) return null;
            const week = payload[0].payload;
            if (week.pct === null) return null;
            return /* @__PURE__ */ (0, import_jsx_runtime13.jsxs)("div", { className: "retraced-tooltip", children: [
              /* @__PURE__ */ (0, import_jsx_runtime13.jsxs)("div", { className: "retraced-tooltip-title", children: [
                "Week of ",
                label
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime13.jsxs)("div", { children: [
                week.pct,
                "% abandoned"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime13.jsxs)("div", { className: "retraced-note", children: [
                formatCount(week.aborted),
                " aborted · ",
                formatCount(week.committed),
                " sent"
              ] })
            ] });
          }
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(Line, { type: "monotone", dataKey: "pct", stroke: aborted, strokeWidth: 2, dot: false, connectNulls: false, isAnimationActive: false, activeDot: { r: 3 } })
    ] });
  }

  // src/ui/charts/PeerHesitationChart.tsx
  var import_jsx_runtime14 = __toESM(require_jsx_runtime(), 1);
  function PeerHesitationChart({ data, theme, width }) {
    const height = 40 + data.length * 28;
    const aborted = theme.series[5];
    return /* @__PURE__ */ (0, import_jsx_runtime14.jsxs)(BarChart, { layout: "vertical", width, height, data, margin: { top: 4, right: 16, bottom: 0, left: 8 }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(XAxis, { type: "number", allowDecimals: false, tick: { fill: theme.ink.muted, fontSize: 11 }, axisLine: { stroke: theme.ink.axis }, tickLine: false }),
      /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(YAxis, { type: "category", dataKey: "label", width: 140, tick: { fill: theme.ink.muted, fontSize: 11 }, axisLine: false, tickLine: false }),
      /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(
        Tooltip,
        {
          cursor: { fill: theme.ink.grid },
          content: ({ active, payload }) => {
            if (!active || !payload?.length) return null;
            const row = payload[0].payload;
            return /* @__PURE__ */ (0, import_jsx_runtime14.jsxs)("div", { className: "retraced-tooltip", children: [
              /* @__PURE__ */ (0, import_jsx_runtime14.jsx)("div", { className: "retraced-tooltip-title", children: row.label }),
              /* @__PURE__ */ (0, import_jsx_runtime14.jsxs)("div", { children: [
                formatCount(row.aborted),
                " abandoned typing runs"
              ] })
            ] });
          }
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(Bar, { dataKey: "aborted", fill: aborted, radius: [0, 4, 4, 0], maxBarSize: 16, isAnimationActive: false })
    ] });
  }

  // src/ui/page/HesitationTab.tsx
  var import_jsx_runtime15 = __toESM(require_jsx_runtime(), 1);
  function HesitationTab({ engine, rangeDays }) {
    const theme = (0, import_react72.useMemo)(readChartTheme, []);
    const labels = (0, import_react72.useMemo)(createLabelResolvers, []);
    const data = useRollups(engine);
    const [width, measureRef] = useElementWidth();
    const chartWidth = Math.max(320, width - 32);
    const [sortBy3, setSortBy] = (0, import_react72.useState)("volume");
    const today = dateKey(Date.now());
    const from2 = rangeDays === null ? null : shiftDate(today, -(rangeDays - 1));
    const rangeDaily = (0, import_react72.useMemo)(() => from2 ? data.daily.filter((r2) => r2.date >= from2) : data.daily, [data.daily, from2]);
    const weekly = (0, import_react72.useMemo)(() => hesitationWeekly(rangeDaily), [rangeDaily]);
    const weeksWithSignal = weekly.filter((w) => w.pct !== null).length;
    const channels = (0, import_react72.useMemo)(
      () => channelHesitation(rangeDaily, { channelLabel: labels.channelLabel, guildLabel: labels.guildLabel, sortBy: sortBy3 }),
      [rangeDaily, labels, sortBy3]
    );
    const peers = (0, import_react72.useMemo)(() => peerHesitation(data.peers), [data.peers]);
    const rates = (0, import_react72.useMemo)(
      () => editDeleteRates(rangeDaily, { channelLabel: labels.channelLabel, guildLabel: labels.guildLabel }),
      [rangeDaily, labels]
    );
    const lifetime = (0, import_react72.useMemo)(() => deleteLifetime(rangeDaily), [rangeDaily]);
    const abortedColor = theme.series[5];
    const committedColor = theme.series[0];
    if (!data.loaded) return /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("p", { className: "retraced-note", children: "loading…" });
    return /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)("div", { ref: measureRef, className: "retraced-overview", children: [
      data.dbMissing ? /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("p", { className: "retraced-banner", children: "Capture is unavailable, so charts have nothing to read — see the Overview tab for details." }) : null,
      /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(
        ChartCard,
        {
          title: "Hesitation over time",
          subtitle: "weekly share of typing runs you abandoned before sending",
          empty: weeksWithSignal < 2 ? "This line needs a couple of weeks of typing history — it fills in as you chat." : null,
          table: {
            columns: ["Week", "Abandoned %", "Aborted", "Committed"],
            rows: weekly.map((w) => [w.week, w.pct === null ? "—" : `${w.pct}%`, w.aborted, w.committed])
          },
          children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(HesitationOverTime, { data: weekly, theme, width: chartWidth })
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(
        ChartCard,
        {
          title: "Where you hesitate",
          subtitle: "abandoned vs sent typing runs, per channel",
          legend: [
            { label: "Aborted", color: abortedColor },
            { label: "Committed", color: committedColor }
          ],
          actions: /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)("div", { className: "retraced-ranges", role: "group", "aria-label": "Sort channels", children: [
            /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("button", { type: "button", className: "retraced-range-pill", "aria-pressed": sortBy3 === "volume", onClick: () => setSortBy("volume"), children: "most typing" }),
            /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("button", { type: "button", className: "retraced-range-pill", "aria-pressed": sortBy3 === "rate", onClick: () => setSortBy("rate"), children: "most hesitant" })
          ] }),
          empty: channels.length === 0 ? "No channel has enough typing history yet — this needs a handful of typing runs per channel." : null,
          table: {
            columns: ["Channel", "Aborted", "Committed", "Hesitation %"],
            rows: channels.map((c2) => [c2.label, c2.aborted, c2.committed, `${c2.pct}%`])
          },
          children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(ChannelHesitationChart, { data: channels, theme, width: chartWidth })
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(
        ChartCard,
        {
          title: "Who makes you hesitate",
          subtitle: "typing runs abandoned in DMs, per person · all time",
          empty: peers.length === 0 ? "Nothing here yet — abandoned typing in a DM shows up against that person." : null,
          table: {
            columns: ["Person", "Abandoned runs"],
            rows: peers.map((p) => [p.label, p.aborted])
          },
          children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(PeerHesitationChart, { data: peers, theme, width: chartWidth })
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(
        ChartCard,
        {
          title: "Edits and deletions",
          subtitle: "share of your sent messages you later edited or deleted, per channel",
          legend: [
            { label: "Edited", color: theme.series[6] },
            { label: "Deleted", color: theme.series[7] }
          ],
          empty: rates.length === 0 ? "Rates appear once a channel has at least 20 of your messages." : null,
          table: {
            columns: ["Channel", "Sent", "Edited %", "Deleted %"],
            rows: rates.map((c2) => [c2.label, c2.sent, `${c2.editPct}%`, `${c2.deletePct}%`])
          },
          children: /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(EditDeleteChart, { data: rates, theme, width: chartWidth })
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)(
        ChartCard,
        {
          title: "Time to delete",
          subtitle: "how long your deleted messages lived",
          empty: lifetime.total === 0 ? "No deletions captured yet — a deleted message records how long it survived." : null,
          table: {
            columns: ["Lifetime", "Deletions"],
            rows: lifetime.buckets.map((count, i) => [DELETE_BUCKET_LABELS[i], count])
          },
          children: [
            /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)("div", { className: "retraced-hero-block", children: [
              /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("span", { className: "retraced-hero-figure", children: lifetime.medianBucket !== null ? DELETE_BUCKET_LABELS[lifetime.medianBucket] : "—" }),
              /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)("span", { className: "retraced-note", children: [
                "median lifetime · ",
                formatCount(lifetime.total),
                " deletions"
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(DeleteLifetimeChart, { data: lifetime, theme, width: chartWidth })
          ]
        }
      )
    ] });
  }

  // src/ui/page/OverviewTab.tsx
  var import_react76 = __toESM(require_react(), 1);

  // src/ui/charts/CalendarHeatmap.tsx
  var import_react73 = __toESM(require_react(), 1);
  var import_jsx_runtime16 = __toESM(require_jsx_runtime(), 1);
  var CELL = 11;
  var GAP = 3;
  var STEP = CELL + GAP;
  var MONTHS2 = ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"];
  function CalendarHeatmap({ years, theme }) {
    const [hover, setHover] = (0, import_react73.useState)(null);
    const today = dateKey(Date.now());
    return /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { className: "retraced-calendar", style: { position: "relative" }, children: [
      years.map((year) => /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(YearGrid, { year, theme, today, onHover: setHover }, year.year)),
      /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { className: "retraced-legend retraced-calendar-legend", children: [
        /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("span", { className: "retraced-note", children: "less" }),
        /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("svg", { width: 5 * STEP, height: CELL, role: "presentation", children: [theme.calendarZero, ...theme.calendarBins].map((fill, i) => /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("rect", { x: i * STEP, width: CELL, height: CELL, rx: 2.5, fill }, i)) }),
        /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("span", { className: "retraced-note", children: "more" })
      ] }),
      hover ? /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("div", { className: "retraced-tooltip retraced-tooltip--floating", style: { left: hover.x, top: hover.y }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { className: "retraced-tooltip-title", children: hover.date }),
        /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { children: hover.count === 0 ? "no messages" : `${formatCount(hover.count)} messages` })
      ] }) : null
    ] });
  }
  function YearGrid({
    year,
    theme,
    today,
    onHover
  }) {
    const jan1 = `${year.year}-01-01`;
    const dec31 = `${year.year}-12-31`;
    const gridStart = mondayOf(jan1);
    const counts = new Map(year.days.map((d) => [d.date, d]));
    const cells = [];
    let cursor = gridStart;
    for (let i = 0; i < 371; i++) {
      if (cursor > dec31 || cursor > today) break;
      if (cursor >= jan1) {
        const day = counts.get(cursor);
        cells.push({
          date: cursor,
          column: Math.floor(i / 7),
          row: i % 7,
          count: day?.count ?? 0,
          fill: day && day.level > 0 ? theme.calendarBins[day.level - 1] : theme.calendarZero
        });
      }
      cursor = shiftDate(cursor, 1);
    }
    const columns = (cells[cells.length - 1]?.column ?? 0) + 1;
    const monthLabels = [];
    for (let month = 0; month < 12; month++) {
      const first = `${year.year}-${String(month + 1).padStart(2, "0")}-01`;
      if (first > today) break;
      const dayIndex = Math.round((parseDateKey(first).getTime() - parseDateKey(gridStart).getTime()) / 864e5);
      const column = Math.floor(dayIndex / 7);
      if (column >= 0 && column < columns) monthLabels.push({ column, label: MONTHS2[month] });
    }
    const left2 = 34;
    const top = 18;
    const width = left2 + columns * STEP;
    const height = top + 7 * STEP;
    return /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("div", { className: "retraced-calendar-year", children: /* @__PURE__ */ (0, import_jsx_runtime16.jsxs)("svg", { width, height, role: "img", "aria-label": `Activity calendar ${year.year}`, children: [
      /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("text", { x: 0, y: top + 8, className: "retraced-svg-label", fill: theme.ink.muted, fontWeight: 600, children: year.year }),
      monthLabels.map((m2) => /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("text", { x: left2 + m2.column * STEP, y: 11, className: "retraced-svg-label", fill: theme.ink.muted, children: m2.label }, m2.label)),
      cells.map((cell) => /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(
        "rect",
        {
          x: left2 + cell.column * STEP,
          y: top + cell.row * STEP,
          width: CELL,
          height: CELL,
          rx: 2.5,
          fill: cell.fill,
          onMouseEnter: (e) => {
            const host = e.currentTarget.ownerSVGElement?.parentElement?.parentElement ?? null;
            const hostRect = host?.getBoundingClientRect();
            const rect = e.currentTarget.getBoundingClientRect();
            onHover({
              x: rect.left - (hostRect?.left ?? 0) + CELL / 2,
              y: rect.top - (hostRect?.top ?? 0) - 8,
              date: cell.date,
              count: cell.count
            });
          },
          onMouseLeave: () => onHover(null),
          children: /* @__PURE__ */ (0, import_jsx_runtime16.jsx)("title", { children: `${cell.date} — ${cell.count} messages` })
        },
        cell.date
      ))
    ] }) });
  }

  // src/ui/charts/MessagesPerDay.tsx
  var import_jsx_runtime17 = __toESM(require_jsx_runtime(), 1);
  function MessagesPerDay({ data, theme, width }) {
    const ticks = monthTicks(data.map((d) => d.date));
    const spansYears = data.length > 0 && data[0].date.slice(0, 4) !== data[data.length - 1].date.slice(0, 4);
    return /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)(ComposedChart, { width, height: 220, data, margin: { top: 8, right: 8, bottom: 0, left: 0 }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(CartesianGrid, { vertical: false, stroke: theme.ink.grid }),
      /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(
        XAxis,
        {
          dataKey: "date",
          ticks,
          tickFormatter: (d) => tickLabel(d, spansYears),
          tick: { fill: theme.ink.muted, fontSize: 11 },
          axisLine: { stroke: theme.ink.axis },
          tickLine: false,
          interval: "preserveStartEnd"
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(
        YAxis,
        {
          width: 36,
          tickCount: 4,
          allowDecimals: false,
          tick: { fill: theme.ink.muted, fontSize: 11 },
          axisLine: false,
          tickLine: false
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(
        Tooltip,
        {
          cursor: { stroke: theme.ink.axis },
          content: ({ active, payload, label }) => {
            if (!active || !payload?.length) return null;
            const point6 = payload[0].payload;
            return /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: "retraced-tooltip", children: [
              /* @__PURE__ */ (0, import_jsx_runtime17.jsx)("div", { className: "retraced-tooltip-title", children: label }),
              /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { children: [
                formatCount(point6.sent),
                " messages"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)("div", { className: "retraced-note", children: [
                "7-day average ",
                point6.avg7.toFixed(1)
              ] })
            ] });
          }
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(Area, { type: "monotone", dataKey: "sent", stroke: theme.series[0], strokeWidth: 1, strokeOpacity: 0.55, fill: theme.series[0], fillOpacity: 0.16, isAnimationActive: false, activeDot: { r: 3 } }),
      /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(Line, { type: "monotone", dataKey: "avg7", stroke: theme.ink.primary, strokeWidth: 2, dot: false, isAnimationActive: false })
    ] });
  }

  // node_modules/d3-sankey/src/align.js
  function justify(node, n) {
    return node.sourceLinks.length ? node.depth : n - 1;
  }

  // node_modules/d3-sankey/src/constant.js
  function constant(x5) {
    return function() {
      return x5;
    };
  }

  // node_modules/d3-sankey/src/sankey.js
  function ascendingSourceBreadth(a2, b) {
    return ascendingBreadth(a2.source, b.source) || a2.index - b.index;
  }
  function ascendingTargetBreadth(a2, b) {
    return ascendingBreadth(a2.target, b.target) || a2.index - b.index;
  }
  function ascendingBreadth(a2, b) {
    return a2.y0 - b.y0;
  }
  function value(d) {
    return d.value;
  }
  function defaultId(d) {
    return d.index;
  }
  function defaultNodes(graph) {
    return graph.nodes;
  }
  function defaultLinks(graph) {
    return graph.links;
  }
  function find(nodeById, id) {
    const node = nodeById.get(id);
    if (!node) throw new Error("missing: " + id);
    return node;
  }
  function computeLinkBreadths({ nodes }) {
    for (const node of nodes) {
      let y0 = node.y0;
      let y1 = y0;
      for (const link2 of node.sourceLinks) {
        link2.y0 = y0 + link2.width / 2;
        y0 += link2.width;
      }
      for (const link2 of node.targetLinks) {
        link2.y1 = y1 + link2.width / 2;
        y1 += link2.width;
      }
    }
  }
  function Sankey() {
    let x0 = 0, y0 = 0, x1 = 1, y1 = 1;
    let dx = 24;
    let dy = 8, py;
    let id = defaultId;
    let align = justify;
    let sort;
    let linkSort;
    let nodes = defaultNodes;
    let links = defaultLinks;
    let iterations = 6;
    function sankey() {
      const graph = { nodes: nodes.apply(null, arguments), links: links.apply(null, arguments) };
      computeNodeLinks(graph);
      computeNodeValues(graph);
      computeNodeDepths(graph);
      computeNodeHeights(graph);
      computeNodeBreadths(graph);
      computeLinkBreadths(graph);
      return graph;
    }
    sankey.update = function(graph) {
      computeLinkBreadths(graph);
      return graph;
    };
    sankey.nodeId = function(_) {
      return arguments.length ? (id = typeof _ === "function" ? _ : constant(_), sankey) : id;
    };
    sankey.nodeAlign = function(_) {
      return arguments.length ? (align = typeof _ === "function" ? _ : constant(_), sankey) : align;
    };
    sankey.nodeSort = function(_) {
      return arguments.length ? (sort = _, sankey) : sort;
    };
    sankey.nodeWidth = function(_) {
      return arguments.length ? (dx = +_, sankey) : dx;
    };
    sankey.nodePadding = function(_) {
      return arguments.length ? (dy = py = +_, sankey) : dy;
    };
    sankey.nodes = function(_) {
      return arguments.length ? (nodes = typeof _ === "function" ? _ : constant(_), sankey) : nodes;
    };
    sankey.links = function(_) {
      return arguments.length ? (links = typeof _ === "function" ? _ : constant(_), sankey) : links;
    };
    sankey.linkSort = function(_) {
      return arguments.length ? (linkSort = _, sankey) : linkSort;
    };
    sankey.size = function(_) {
      return arguments.length ? (x0 = y0 = 0, x1 = +_[0], y1 = +_[1], sankey) : [x1 - x0, y1 - y0];
    };
    sankey.extent = function(_) {
      return arguments.length ? (x0 = +_[0][0], x1 = +_[1][0], y0 = +_[0][1], y1 = +_[1][1], sankey) : [[x0, y0], [x1, y1]];
    };
    sankey.iterations = function(_) {
      return arguments.length ? (iterations = +_, sankey) : iterations;
    };
    function computeNodeLinks({ nodes: nodes2, links: links2 }) {
      for (const [i, node] of nodes2.entries()) {
        node.index = i;
        node.sourceLinks = [];
        node.targetLinks = [];
      }
      const nodeById = new Map(nodes2.map((d, i) => [id(d, i, nodes2), d]));
      for (const [i, link2] of links2.entries()) {
        link2.index = i;
        let { source, target } = link2;
        if (typeof source !== "object") source = link2.source = find(nodeById, source);
        if (typeof target !== "object") target = link2.target = find(nodeById, target);
        source.sourceLinks.push(link2);
        target.targetLinks.push(link2);
      }
      if (linkSort != null) {
        for (const { sourceLinks, targetLinks } of nodes2) {
          sourceLinks.sort(linkSort);
          targetLinks.sort(linkSort);
        }
      }
    }
    function computeNodeValues({ nodes: nodes2 }) {
      for (const node of nodes2) {
        node.value = node.fixedValue === void 0 ? Math.max(sum(node.sourceLinks, value), sum(node.targetLinks, value)) : node.fixedValue;
      }
    }
    function computeNodeDepths({ nodes: nodes2 }) {
      const n = nodes2.length;
      let current2 = new Set(nodes2);
      let next = /* @__PURE__ */ new Set();
      let x5 = 0;
      while (current2.size) {
        for (const node of current2) {
          node.depth = x5;
          for (const { target } of node.sourceLinks) {
            next.add(target);
          }
        }
        if (++x5 > n) throw new Error("circular link");
        current2 = next;
        next = /* @__PURE__ */ new Set();
      }
    }
    function computeNodeHeights({ nodes: nodes2 }) {
      const n = nodes2.length;
      let current2 = new Set(nodes2);
      let next = /* @__PURE__ */ new Set();
      let x5 = 0;
      while (current2.size) {
        for (const node of current2) {
          node.height = x5;
          for (const { source } of node.targetLinks) {
            next.add(source);
          }
        }
        if (++x5 > n) throw new Error("circular link");
        current2 = next;
        next = /* @__PURE__ */ new Set();
      }
    }
    function computeNodeLayers({ nodes: nodes2 }) {
      const x5 = max2(nodes2, (d) => d.depth) + 1;
      const kx = (x1 - x0 - dx) / (x5 - 1);
      const columns = new Array(x5);
      for (const node of nodes2) {
        const i = Math.max(0, Math.min(x5 - 1, Math.floor(align.call(null, node, x5))));
        node.layer = i;
        node.x0 = x0 + i * kx;
        node.x1 = node.x0 + dx;
        if (columns[i]) columns[i].push(node);
        else columns[i] = [node];
      }
      if (sort) for (const column of columns) {
        column.sort(sort);
      }
      return columns;
    }
    function initializeNodeBreadths(columns) {
      const ky = min2(columns, (c2) => (y1 - y0 - (c2.length - 1) * py) / sum(c2, value));
      for (const nodes2 of columns) {
        let y5 = y0;
        for (const node of nodes2) {
          node.y0 = y5;
          node.y1 = y5 + node.value * ky;
          y5 = node.y1 + py;
          for (const link2 of node.sourceLinks) {
            link2.width = link2.value * ky;
          }
        }
        y5 = (y1 - y5 + py) / (nodes2.length + 1);
        for (let i = 0; i < nodes2.length; ++i) {
          const node = nodes2[i];
          node.y0 += y5 * (i + 1);
          node.y1 += y5 * (i + 1);
        }
        reorderLinks(nodes2);
      }
    }
    function computeNodeBreadths(graph) {
      const columns = computeNodeLayers(graph);
      py = Math.min(dy, (y1 - y0) / (max2(columns, (c2) => c2.length) - 1));
      initializeNodeBreadths(columns);
      for (let i = 0; i < iterations; ++i) {
        const alpha = Math.pow(0.99, i);
        const beta = Math.max(1 - alpha, (i + 1) / iterations);
        relaxRightToLeft(columns, alpha, beta);
        relaxLeftToRight(columns, alpha, beta);
      }
    }
    function relaxLeftToRight(columns, alpha, beta) {
      for (let i = 1, n = columns.length; i < n; ++i) {
        const column = columns[i];
        for (const target of column) {
          let y5 = 0;
          let w = 0;
          for (const { source, value: value2 } of target.targetLinks) {
            let v = value2 * (target.layer - source.layer);
            y5 += targetTop(source, target) * v;
            w += v;
          }
          if (!(w > 0)) continue;
          let dy2 = (y5 / w - target.y0) * alpha;
          target.y0 += dy2;
          target.y1 += dy2;
          reorderNodeLinks(target);
        }
        if (sort === void 0) column.sort(ascendingBreadth);
        resolveCollisions(column, beta);
      }
    }
    function relaxRightToLeft(columns, alpha, beta) {
      for (let n = columns.length, i = n - 2; i >= 0; --i) {
        const column = columns[i];
        for (const source of column) {
          let y5 = 0;
          let w = 0;
          for (const { target, value: value2 } of source.sourceLinks) {
            let v = value2 * (target.layer - source.layer);
            y5 += sourceTop(source, target) * v;
            w += v;
          }
          if (!(w > 0)) continue;
          let dy2 = (y5 / w - source.y0) * alpha;
          source.y0 += dy2;
          source.y1 += dy2;
          reorderNodeLinks(source);
        }
        if (sort === void 0) column.sort(ascendingBreadth);
        resolveCollisions(column, beta);
      }
    }
    function resolveCollisions(nodes2, alpha) {
      const i = nodes2.length >> 1;
      const subject = nodes2[i];
      resolveCollisionsBottomToTop(nodes2, subject.y0 - py, i - 1, alpha);
      resolveCollisionsTopToBottom(nodes2, subject.y1 + py, i + 1, alpha);
      resolveCollisionsBottomToTop(nodes2, y1, nodes2.length - 1, alpha);
      resolveCollisionsTopToBottom(nodes2, y0, 0, alpha);
    }
    function resolveCollisionsTopToBottom(nodes2, y5, i, alpha) {
      for (; i < nodes2.length; ++i) {
        const node = nodes2[i];
        const dy2 = (y5 - node.y0) * alpha;
        if (dy2 > 1e-6) node.y0 += dy2, node.y1 += dy2;
        y5 = node.y1 + py;
      }
    }
    function resolveCollisionsBottomToTop(nodes2, y5, i, alpha) {
      for (; i >= 0; --i) {
        const node = nodes2[i];
        const dy2 = (node.y1 - y5) * alpha;
        if (dy2 > 1e-6) node.y0 -= dy2, node.y1 -= dy2;
        y5 = node.y0 - py;
      }
    }
    function reorderNodeLinks({ sourceLinks, targetLinks }) {
      if (linkSort === void 0) {
        for (const { source: { sourceLinks: sourceLinks2 } } of targetLinks) {
          sourceLinks2.sort(ascendingTargetBreadth);
        }
        for (const { target: { targetLinks: targetLinks2 } } of sourceLinks) {
          targetLinks2.sort(ascendingSourceBreadth);
        }
      }
    }
    function reorderLinks(nodes2) {
      if (linkSort === void 0) {
        for (const { sourceLinks, targetLinks } of nodes2) {
          sourceLinks.sort(ascendingTargetBreadth);
          targetLinks.sort(ascendingSourceBreadth);
        }
      }
    }
    function targetTop(source, target) {
      let y5 = source.y0 - (source.sourceLinks.length - 1) * py / 2;
      for (const { target: node, width } of source.sourceLinks) {
        if (node === target) break;
        y5 += width + py;
      }
      for (const { source: node, width } of target.targetLinks) {
        if (node === source) break;
        y5 -= width;
      }
      return y5;
    }
    function sourceTop(source, target) {
      let y5 = target.y0 - (target.targetLinks.length - 1) * py / 2;
      for (const { source: node, width } of target.targetLinks) {
        if (node === source) break;
        y5 += width + py;
      }
      for (const { target: node, width } of source.sourceLinks) {
        if (node === target) break;
        y5 -= width;
      }
      return y5;
    }
    return sankey;
  }

  // node_modules/d3-sankey/node_modules/d3-path/src/path.js
  var pi3 = Math.PI;
  var tau3 = 2 * pi3;
  var epsilon3 = 1e-6;
  var tauEpsilon2 = tau3 - epsilon3;
  function Path2() {
    this._x0 = this._y0 = // start of current subpath
    this._x1 = this._y1 = null;
    this._ = "";
  }
  function path2() {
    return new Path2();
  }
  Path2.prototype = path2.prototype = {
    constructor: Path2,
    moveTo: function(x5, y5) {
      this._ += "M" + (this._x0 = this._x1 = +x5) + "," + (this._y0 = this._y1 = +y5);
    },
    closePath: function() {
      if (this._x1 !== null) {
        this._x1 = this._x0, this._y1 = this._y0;
        this._ += "Z";
      }
    },
    lineTo: function(x5, y5) {
      this._ += "L" + (this._x1 = +x5) + "," + (this._y1 = +y5);
    },
    quadraticCurveTo: function(x1, y1, x5, y5) {
      this._ += "Q" + +x1 + "," + +y1 + "," + (this._x1 = +x5) + "," + (this._y1 = +y5);
    },
    bezierCurveTo: function(x1, y1, x22, y22, x5, y5) {
      this._ += "C" + +x1 + "," + +y1 + "," + +x22 + "," + +y22 + "," + (this._x1 = +x5) + "," + (this._y1 = +y5);
    },
    arcTo: function(x1, y1, x22, y22, r2) {
      x1 = +x1, y1 = +y1, x22 = +x22, y22 = +y22, r2 = +r2;
      var x0 = this._x1, y0 = this._y1, x21 = x22 - x1, y21 = y22 - y1, x01 = x0 - x1, y01 = y0 - y1, l01_2 = x01 * x01 + y01 * y01;
      if (r2 < 0) throw new Error("negative radius: " + r2);
      if (this._x1 === null) {
        this._ += "M" + (this._x1 = x1) + "," + (this._y1 = y1);
      } else if (!(l01_2 > epsilon3)) ;
      else if (!(Math.abs(y01 * x21 - y21 * x01) > epsilon3) || !r2) {
        this._ += "L" + (this._x1 = x1) + "," + (this._y1 = y1);
      } else {
        var x20 = x22 - x0, y20 = y22 - y0, l21_2 = x21 * x21 + y21 * y21, l20_2 = x20 * x20 + y20 * y20, l21 = Math.sqrt(l21_2), l01 = Math.sqrt(l01_2), l = r2 * Math.tan((pi3 - Math.acos((l21_2 + l01_2 - l20_2) / (2 * l21 * l01))) / 2), t01 = l / l01, t21 = l / l21;
        if (Math.abs(t01 - 1) > epsilon3) {
          this._ += "L" + (x1 + t01 * x01) + "," + (y1 + t01 * y01);
        }
        this._ += "A" + r2 + "," + r2 + ",0,0," + +(y01 * x20 > x01 * y20) + "," + (this._x1 = x1 + t21 * x21) + "," + (this._y1 = y1 + t21 * y21);
      }
    },
    arc: function(x5, y5, r2, a0, a1, ccw) {
      x5 = +x5, y5 = +y5, r2 = +r2, ccw = !!ccw;
      var dx = r2 * Math.cos(a0), dy = r2 * Math.sin(a0), x0 = x5 + dx, y0 = y5 + dy, cw = 1 ^ ccw, da = ccw ? a0 - a1 : a1 - a0;
      if (r2 < 0) throw new Error("negative radius: " + r2);
      if (this._x1 === null) {
        this._ += "M" + x0 + "," + y0;
      } else if (Math.abs(this._x1 - x0) > epsilon3 || Math.abs(this._y1 - y0) > epsilon3) {
        this._ += "L" + x0 + "," + y0;
      }
      if (!r2) return;
      if (da < 0) da = da % tau3 + tau3;
      if (da > tauEpsilon2) {
        this._ += "A" + r2 + "," + r2 + ",0,1," + cw + "," + (x5 - dx) + "," + (y5 - dy) + "A" + r2 + "," + r2 + ",0,1," + cw + "," + (this._x1 = x0) + "," + (this._y1 = y0);
      } else if (da > epsilon3) {
        this._ += "A" + r2 + "," + r2 + ",0," + +(da >= pi3) + "," + cw + "," + (this._x1 = x5 + r2 * Math.cos(a1)) + "," + (this._y1 = y5 + r2 * Math.sin(a1));
      }
    },
    rect: function(x5, y5, w, h) {
      this._ += "M" + (this._x0 = this._x1 = +x5) + "," + (this._y0 = this._y1 = +y5) + "h" + +w + "v" + +h + "h" + -w + "Z";
    },
    toString: function() {
      return this._;
    }
  };
  var path_default = path2;

  // node_modules/d3-sankey/node_modules/d3-shape/src/constant.js
  function constant_default3(x5) {
    return function constant2() {
      return x5;
    };
  }

  // node_modules/d3-sankey/node_modules/d3-shape/src/point.js
  function x2(p) {
    return p[0];
  }
  function y2(p) {
    return p[1];
  }

  // node_modules/d3-sankey/node_modules/d3-shape/src/array.js
  var slice2 = Array.prototype.slice;

  // node_modules/d3-sankey/node_modules/d3-shape/src/link/index.js
  function linkSource(d) {
    return d.source;
  }
  function linkTarget(d) {
    return d.target;
  }
  function link(curve) {
    var source = linkSource, target = linkTarget, x5 = x2, y5 = y2, context = null;
    function link2() {
      var buffer, argv = slice2.call(arguments), s = source.apply(this, argv), t = target.apply(this, argv);
      if (!context) context = buffer = path_default();
      curve(context, +x5.apply(this, (argv[0] = s, argv)), +y5.apply(this, argv), +x5.apply(this, (argv[0] = t, argv)), +y5.apply(this, argv));
      if (buffer) return context = null, buffer + "" || null;
    }
    link2.source = function(_) {
      return arguments.length ? (source = _, link2) : source;
    };
    link2.target = function(_) {
      return arguments.length ? (target = _, link2) : target;
    };
    link2.x = function(_) {
      return arguments.length ? (x5 = typeof _ === "function" ? _ : constant_default3(+_), link2) : x5;
    };
    link2.y = function(_) {
      return arguments.length ? (y5 = typeof _ === "function" ? _ : constant_default3(+_), link2) : y5;
    };
    link2.context = function(_) {
      return arguments.length ? (context = _ == null ? null : _, link2) : context;
    };
    return link2;
  }
  function curveHorizontal(context, x0, y0, x1, y1) {
    context.moveTo(x0, y0);
    context.bezierCurveTo(x0 = (x0 + x1) / 2, y0, x0, y1, x1, y1);
  }
  function linkHorizontal() {
    return link(curveHorizontal);
  }

  // node_modules/d3-sankey/src/sankeyLinkHorizontal.js
  function horizontalSource(d) {
    return [d.source.x1, d.y0];
  }
  function horizontalTarget(d) {
    return [d.target.x0, d.y1];
  }
  function sankeyLinkHorizontal_default() {
    return linkHorizontal().source(horizontalSource).target(horizontalTarget);
  }

  // src/ui/charts/SankeyFlow.tsx
  var import_react74 = __toESM(require_react(), 1);
  var import_jsx_runtime18 = __toESM(require_jsx_runtime(), 1);
  function SankeyFlow({ data, theme, width }) {
    const height = 340;
    const [activeLink, setActiveLink] = (0, import_react74.useState)(null);
    const layout = (0, import_react74.useMemo)(() => {
      const generator = Sankey().nodeId((d) => d.id).nodeWidth(10).nodePadding(14).nodeAlign(justify).extent([
        [4, 10],
        [width - 4, height - 10]
      ]);
      return generator({
        nodes: data.nodes.map((n) => ({ ...n })),
        links: data.links.map((l) => ({ ...l }))
      });
    }, [data, width]);
    const nodeColor = (node) => colorForSlot(theme, node.colorSlot);
    return /* @__PURE__ */ (0, import_jsx_runtime18.jsxs)("svg", { width, height, role: "img", "aria-label": "Message flow from you to servers and channels", children: [
      /* @__PURE__ */ (0, import_jsx_runtime18.jsx)("defs", { children: layout.links.map((link2, i) => {
        const source = link2.source;
        const target = link2.target;
        return /* @__PURE__ */ (0, import_jsx_runtime18.jsxs)("linearGradient", { id: `retraced-sankey-${i}`, gradientUnits: "userSpaceOnUse", x1: source.x1 ?? 0, x2: target.x0 ?? 0, children: [
          /* @__PURE__ */ (0, import_jsx_runtime18.jsx)("stop", { offset: "0%", stopColor: nodeColor(source) }),
          /* @__PURE__ */ (0, import_jsx_runtime18.jsx)("stop", { offset: "100%", stopColor: nodeColor(target) })
        ] }, i);
      }) }),
      /* @__PURE__ */ (0, import_jsx_runtime18.jsx)("g", { fill: "none", children: layout.links.map((link2, i) => /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(
        "path",
        {
          d: sankeyLinkHorizontal_default()(link2) ?? void 0,
          stroke: `url(#retraced-sankey-${i})`,
          strokeWidth: Math.max(1, link2.width ?? 1),
          strokeOpacity: activeLink === null ? 0.3 : activeLink === i ? 0.65 : 0.12,
          onMouseEnter: () => setActiveLink(i),
          onMouseLeave: () => setActiveLink(null),
          children: /* @__PURE__ */ (0, import_jsx_runtime18.jsx)("title", { children: `${link2.source.label} → ${link2.target.label}: ${link2.value} messages` })
        },
        i
      )) }),
      /* @__PURE__ */ (0, import_jsx_runtime18.jsx)("g", { children: layout.nodes.map((node) => {
        const x0 = node.x0 ?? 0;
        const y0 = node.y0 ?? 0;
        const nodeHeight = Math.max(2, (node.y1 ?? 0) - y0);
        const isLast = !node.sourceLinks || node.sourceLinks.length === 0;
        const labelX = isLast ? x0 - 7 : (node.x1 ?? 0) + 7;
        return /* @__PURE__ */ (0, import_jsx_runtime18.jsxs)("g", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime18.jsx)("rect", { x: x0, y: y0, width: (node.x1 ?? 0) - x0, height: nodeHeight, rx: 3, fill: nodeColor(node), children: /* @__PURE__ */ (0, import_jsx_runtime18.jsx)("title", { children: `${node.label}: ${node.value ?? 0} messages` }) }),
          /* @__PURE__ */ (0, import_jsx_runtime18.jsxs)(
            "text",
            {
              x: labelX,
              y: y0 + nodeHeight / 2,
              dominantBaseline: "middle",
              textAnchor: isLast ? "end" : "start",
              className: "retraced-svg-label",
              fill: theme.ink.primary,
              children: [
                node.label,
                /* @__PURE__ */ (0, import_jsx_runtime18.jsx)("tspan", { fill: theme.ink.muted, children: `  ${formatCompact(node.value ?? 0)}` })
              ]
            }
          )
        ] }, node.id);
      }) })
    ] });
  }

  // src/ui/charts/ServerStreamgraph.tsx
  var import_jsx_runtime19 = __toESM(require_jsx_runtime(), 1);
  function ServerStreamgraph({ data, theme, width }) {
    const weeks = data.weeks.map((w) => w.week);
    const ticks = monthTicks(weeks);
    const spansYears = weeks.length > 0 && weeks[0].slice(0, 4) !== weeks[weeks.length - 1].slice(0, 4);
    return /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)(AreaChart, { width, height: 280, data: data.weeks, stackOffset: "wiggle", margin: { top: 16, right: 8, bottom: 4, left: 8 }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(
        XAxis,
        {
          dataKey: "week",
          ticks,
          tickFormatter: (d) => tickLabel(d, spansYears),
          tick: { fill: theme.ink.muted, fontSize: 11 },
          axisLine: { stroke: theme.ink.axis },
          tickLine: false,
          interval: "preserveStartEnd"
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(
        Tooltip,
        {
          cursor: { stroke: theme.ink.axis },
          content: ({ active, payload, label }) => {
            if (!active || !payload?.length) return null;
            const items = payload.filter((p) => typeof p.value === "number" && p.value > 0).sort((a2, b) => b.value - a2.value);
            if (items.length === 0) return null;
            return /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { className: "retraced-tooltip", children: [
              /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { className: "retraced-tooltip-title", children: [
                "Week of ",
                label
              ] }),
              items.map((item) => {
                const key = String(item.dataKey);
                return /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)("div", { className: "retraced-tooltip-row", children: [
                  /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { className: "retraced-legend-dot", style: { background: item.color } }),
                  /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { children: data.series.find((s) => s.key === key)?.label ?? key }),
                  /* @__PURE__ */ (0, import_jsx_runtime19.jsx)("span", { className: "retraced-tooltip-value", children: formatCount(item.value) })
                ] }, key);
              })
            ] });
          }
        }
      ),
      data.series.map((series) => /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(
        Area,
        {
          type: "basis",
          dataKey: series.key,
          stackId: "share",
          fill: colorForSlot(theme, series.colorSlot),
          fillOpacity: 0.9,
          stroke: theme.ink.card,
          strokeWidth: 1.5,
          isAnimationActive: false
        },
        series.key
      ))
    ] });
  }

  // src/ui/charts/SummaryCards.tsx
  var import_jsx_runtime20 = __toESM(require_jsx_runtime(), 1);
  function SummaryCards({ stats }) {
    const cards = [
      {
        key: "night-owl",
        label: "Night owl",
        value: stats.nightOwlPct === null ? "—" : `${stats.nightOwlPct}%`,
        detail: "of messages sent 22:00–06:00"
      },
      {
        key: "streak",
        label: "Active streak",
        value: `${stats.currentStreakDays}`,
        detail: stats.currentStreakDays === 1 ? "day in a row" : "days in a row"
      },
      {
        key: "burst",
        label: "Longest burst",
        value: stats.longestBurst > 0 ? formatCount(stats.longestBurst) : "—",
        detail: "messages in one conversation"
      },
      {
        key: "most-messaged",
        label: "Most messaged",
        value: stats.mostMessaged?.label ?? "—",
        detail: stats.mostMessaged ? `${formatCount(stats.mostMessaged.total)} messages, all time` : "no DM traffic yet"
      },
      {
        key: "ghost",
        label: "Ghost rate",
        value: stats.ghostRatePct === null ? "—" : `${stats.ghostRatePct}%`,
        detail: "DM days with their opener, no reply"
      },
      {
        key: "hesitation",
        label: "Hesitation index",
        value: stats.hesitation === null ? "—" : `${stats.hesitation.pct}%`,
        detail: stats.hesitation ? `${formatCount(stats.hesitation.aborted)} abandoned of ${formatCount(stats.hesitation.aborted + stats.hesitation.committed)} typed` : "starts counting as you type"
      }
    ];
    return /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("div", { className: "retraced-cards", children: cards.map((card) => /* @__PURE__ */ (0, import_jsx_runtime20.jsxs)("div", { className: "retraced-card", children: [
      /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("span", { className: "retraced-card-label", children: card.label }),
      /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("span", { className: "retraced-card-value", title: card.value.length > 14 ? card.value : void 0, children: card.value }),
      /* @__PURE__ */ (0, import_jsx_runtime20.jsx)("span", { className: "retraced-note", children: card.detail })
    ] }, card.key)) });
  }

  // src/ui/data/selectors.ts
  var MAX_RANGE_DAYS = 5e3;
  function messagesPerDaySeries(rows, from2, to2) {
    if (from2 > to2) return [];
    const byDate = /* @__PURE__ */ new Map();
    for (const row of rows) byDate.set(row.date, (byDate.get(row.date) ?? 0) + row.sent);
    const out = [];
    const window2 = [];
    let windowSum = 0;
    let cursor = from2;
    for (let i = 0; i < MAX_RANGE_DAYS; i++) {
      const sent = byDate.get(cursor) ?? 0;
      window2.push(sent);
      windowSum += sent;
      if (window2.length > 7) windowSum -= window2.shift();
      out.push({ date: cursor, sent, avg7: windowSum / window2.length });
      if (cursor === to2) break;
      cursor = shiftDate(cursor, 1);
    }
    return out;
  }
  function guildSlotOrder(allRows) {
    const totals = /* @__PURE__ */ new Map();
    for (const row of allRows) {
      if (row.guildId !== null && row.sent > 0) totals.set(row.guildId, (totals.get(row.guildId) ?? 0) + row.sent);
    }
    const order = /* @__PURE__ */ new Map();
    [...totals.entries()].sort((a2, b) => b[1] - a2[1]).forEach(([guildId], i) => order.set(guildId, i + 1));
    return order;
  }
  function serverShareWeekly(rows, opts) {
    const maxGuilds = opts.maxGuilds ?? 5;
    const totals = /* @__PURE__ */ new Map();
    const cells = /* @__PURE__ */ new Map();
    for (const row of rows) {
      if (row.sent <= 0) continue;
      const key = row.guildId ?? "dms";
      const week = mondayOf(row.date);
      totals.set(key, (totals.get(key) ?? 0) + row.sent);
      let weekCells = cells.get(week);
      if (!weekCells) cells.set(week, weekCells = /* @__PURE__ */ new Map());
      weekCells.set(key, (weekCells.get(key) ?? 0) + row.sent);
    }
    if (cells.size === 0) return { weeks: [], series: [] };
    const guildTotals = [...totals.entries()].filter(([key]) => key !== "dms").sort((a2, b) => b[1] - a2[1]);
    const topGuilds = guildTotals.slice(0, maxGuilds).map(([key]) => key);
    const hasOther = guildTotals.length > topGuilds.length;
    const series = [];
    if ((totals.get("dms") ?? 0) > 0) series.push({ key: "dms", label: "DMs", colorSlot: 0 });
    topGuilds.forEach(
      (guildId, i) => series.push({ key: guildId, label: opts.guildLabel(guildId), colorSlot: opts.slotFor?.(guildId) ?? i + 1 })
    );
    if (hasOther) series.push({ key: "other", label: "Other servers", colorSlot: -1 });
    const weekKeys = [...cells.keys()].sort();
    const first = weekKeys[0];
    const last = weekKeys[weekKeys.length - 1];
    const topSet = new Set(topGuilds);
    const weeks = [];
    let cursor = first;
    for (let i = 0; i < MAX_RANGE_DAYS / 7; i++) {
      const weekCells = cells.get(cursor);
      const entry = { week: cursor };
      for (const s of series) entry[s.key] = 0;
      if (weekCells) {
        for (const [key, sent] of weekCells) {
          const target = key === "dms" ? "dms" : topSet.has(key) ? key : "other";
          if (target in entry) entry[target] = entry[target] + sent;
        }
      }
      weeks.push(entry);
      if (cursor === last) break;
      cursor = shiftDate(cursor, 7);
    }
    return { weeks, series };
  }
  function calendarByYear(rows) {
    const byDate = /* @__PURE__ */ new Map();
    for (const row of rows) {
      if (row.sent <= 0) continue;
      byDate.set(row.date, (byDate.get(row.date) ?? 0) + row.sent);
    }
    if (byDate.size === 0) return [];
    const sorted = [...byDate.values()].sort((a2, b) => a2 - b);
    const q = (f) => sorted[Math.floor(f * (sorted.length - 1))];
    const [q1, q2, q3] = [q(0.25), q(0.5), q(0.75)];
    const level = (count) => count <= 0 ? 0 : count <= q1 ? 1 : count <= q2 ? 2 : count <= q3 ? 3 : 4;
    const years = /* @__PURE__ */ new Map();
    for (const [date2, count] of byDate) {
      const year = Number(date2.slice(0, 4));
      let entry = years.get(year);
      if (!entry) years.set(year, entry = { year, days: [], max: 0 });
      entry.days.push({ date: date2, count, level: level(count) });
      entry.max = Math.max(entry.max, count);
    }
    return [...years.values()].sort((a2, b) => b.year - a2.year);
  }
  function sankeyFlows(rows, opts) {
    const maxGuilds = opts.maxGuilds ?? 5;
    const channelsPerGuild = opts.channelsPerGuild ?? 3;
    const groupTotals = /* @__PURE__ */ new Map();
    const channelTotals = /* @__PURE__ */ new Map();
    let totalSent = 0;
    for (const row of rows) {
      if (row.sent <= 0) continue;
      totalSent += row.sent;
      const group = row.guildId ?? "dms";
      groupTotals.set(group, (groupTotals.get(group) ?? 0) + row.sent);
      let channels = channelTotals.get(group);
      if (!channels) channelTotals.set(group, channels = /* @__PURE__ */ new Map());
      channels.set(row.channelId, (channels.get(row.channelId) ?? 0) + row.sent);
    }
    if (totalSent === 0) return null;
    const nodes = [{ id: "you", label: "You", kind: "root", colorSlot: -2 }];
    const links = [];
    const guildEntries = [...groupTotals.entries()].filter(([g]) => g !== "dms").sort((a2, b) => b[1] - a2[1]);
    const named2 = guildEntries.slice(0, maxGuilds);
    const foldedGuilds = guildEntries.slice(maxGuilds);
    const addChannelLayer = (groupId, nodeId, colorSlot) => {
      const channels = [...(channelTotals.get(groupId) ?? /* @__PURE__ */ new Map()).entries()].sort((a2, b) => b[1] - a2[1]);
      const top = channels.slice(0, channelsPerGuild);
      const rest = channels.slice(channelsPerGuild).reduce((n, [, v]) => n + v, 0);
      for (const [channelId, value2] of top) {
        nodes.push({ id: `channel:${channelId}`, label: opts.channelLabel(channelId), kind: "channel", colorSlot });
        links.push({ source: nodeId, target: `channel:${channelId}`, value: value2 });
      }
      if (rest > 0) {
        nodes.push({ id: `${nodeId}:other`, label: groupId === "dms" ? "other DMs" : "everything else", kind: "other", colorSlot });
        links.push({ source: nodeId, target: `${nodeId}:other`, value: rest });
      }
    };
    const dmsTotal = groupTotals.get("dms") ?? 0;
    if (dmsTotal > 0) {
      nodes.push({ id: "dms", label: "DMs", kind: "dms", colorSlot: 0 });
      links.push({ source: "you", target: "dms", value: dmsTotal });
      addChannelLayer("dms", "dms", 0);
    }
    named2.forEach(([guildId, value2], i) => {
      const nodeId = `guild:${guildId}`;
      const slot = opts.slotFor?.(guildId) ?? i + 1;
      nodes.push({ id: nodeId, label: opts.guildLabel(guildId), kind: "guild", colorSlot: slot });
      links.push({ source: "you", target: nodeId, value: value2 });
      addChannelLayer(guildId, nodeId, slot);
    });
    if (foldedGuilds.length > 0) {
      const value2 = foldedGuilds.reduce((n, [, v]) => n + v, 0);
      nodes.push({ id: "guilds:other", label: "Other servers", kind: "other", colorSlot: -1 });
      links.push({ source: "you", target: "guilds:other", value: value2 });
    }
    return { nodes, links };
  }
  var isNightHour = (hour) => hour >= 22 || hour < 6;
  function summaryStats(input) {
    const totalSent = input.daily.reduce((n, r2) => n + r2.sent, 0);
    let nightSent = 0;
    let hourlyTotal = 0;
    for (const row of input.hourly) {
      hourlyTotal += row.sent;
      if (isNightHour(row.hour)) nightSent += row.sent;
    }
    const activeDates = new Set(input.streakDaily.filter((r2) => r2.sent > 0).map((r2) => r2.date));
    let streak = 0;
    let cursor = activeDates.has(input.today) ? input.today : shiftDate(input.today, -1);
    while (activeDates.has(cursor)) {
      streak++;
      cursor = shiftDate(cursor, -1);
    }
    const longestBurst = input.daily.reduce((n, r2) => Math.max(n, r2.longestBurst ?? 0), 0);
    let mostMessaged = null;
    for (const peer of input.peers) {
      const total = peer.msgToThem + peer.msgFromThem;
      if (total > 0 && total > (mostMessaged?.total ?? 0)) {
        mostMessaged = { label: peer.label ?? peer.userId, total };
      }
    }
    let openerDays = 0;
    let ghostedDays = 0;
    for (const row of input.daily) {
      if (row.guildId !== null || row.initiatedByThem <= 0) continue;
      openerDays++;
      if (row.sent === 0) ghostedDays++;
    }
    const aborted = input.daily.reduce((n, r2) => n + r2.typingAborted, 0);
    const committed = input.daily.reduce((n, r2) => n + r2.typingCommitted, 0);
    return {
      totalSent,
      nightOwlPct: hourlyTotal > 0 ? Math.round(100 * nightSent / hourlyTotal) : null,
      currentStreakDays: streak,
      longestBurst,
      mostMessaged,
      ghostRatePct: openerDays > 0 ? Math.round(100 * ghostedDays / openerDays) : null,
      hesitation: aborted + committed > 0 ? { pct: Math.round(100 * aborted / (aborted + committed)), aborted, committed } : null
    };
  }

  // src/ui/page/CaptureReadout.tsx
  var import_react75 = __toESM(require_react(), 1);

  // src/dev/synthetic.ts
  var SYNTHETIC_OWN_ID = "1000";
  var PEER_NAMES = {
    "2001": "alex",
    "2002": "sam",
    "2003": "jo",
    "2004": "max",
    "2005": "kit",
    "2006": "ren",
    "2007": "ash",
    "2008": "lee"
  };
  var PEERS = [
    { id: "2001", chattiness: 1, replyMeanMs: 4e4, hesitancy: 0.1 },
    { id: "2002", chattiness: 0.8, replyMeanMs: 9e4, hesitancy: 0.22 },
    { id: "2003", chattiness: 0.6, replyMeanMs: 3e5, hesitancy: 0.05 },
    { id: "2004", chattiness: 0.5, replyMeanMs: 25e3, hesitancy: 0.15 },
    { id: "2005", chattiness: 0.4, replyMeanMs: 6e5, hesitancy: 0.3 },
    { id: "2006", chattiness: 0.3, replyMeanMs: 12e4, hesitancy: 0.08 },
    { id: "2007", chattiness: 0.25, replyMeanMs: 15e5, hesitancy: 0.12 },
    { id: "2008", chattiness: 0.2, replyMeanMs: 2e5, hesitancy: 0.18 }
  ];
  var GUILD_CHANNELS = [
    { channelId: "g1-general", guildId: "g1", kind: "guild" },
    { channelId: "g1-memes", guildId: "g1", kind: "guild" },
    { channelId: "g1-dev", guildId: "g1", kind: "guild" },
    { channelId: "g2-general", guildId: "g2", kind: "guild" },
    { channelId: "g2-offtopic", guildId: "g2", kind: "guild" },
    { channelId: "g3-lounge", guildId: "g3", kind: "guild" }
  ];
  var VOICE_CHANNEL = { channelId: "g1-vc", guildId: "g1", kind: "guild" };
  var GROUP_DM = { channelId: "group-1", guildId: null, kind: "group-dm" };
  var GROUP_MEMBERS = ["2001", "2002", "2003"];
  var dmChannel = (peerId) => ({ channelId: `dm-${peerId}`, guildId: null, kind: "dm" });
  var WORDS = "the a and but so anyway yeah no maybe lol haha nice cool wild okay sure right thanks sorry wait what when where how why game play match stream video song album track code build bug test ship deploy server channel message tomorrow tonight later soon week weekend morning coffee lunch dinner movie episode season book idea plan thing work school class project deadline meeting call drive walk run gym sleep tired awake busy free home out back".split(" ");
  var EMOJI_BANK = [
    { id: "900001", name: "pog" },
    { id: "900002", name: "kekw" },
    { id: "900003", name: "sadge" },
    { id: "900004", name: "hmm" },
    { id: "900005", name: "fire" },
    { id: "900006", name: "clown" },
    { id: "900007", name: "salute" },
    { id: "900008", name: "peepoLove" },
    { id: "900009", name: "monkaS" },
    { id: "900010", name: "gigachad" },
    { id: "900011", name: "catjam" },
    { id: "900012", name: "pepehands" }
  ];
  var DOMAINS = ["youtube.com", "twitter.com", "github.com", "reddit.com", "twitch.tv", "spotify.com", "imgur.com", "wikipedia.org"];
  var HOUR_WEIGHTS = [2, 1, 1, 1, 1, 1, 2, 3, 4, 5, 5, 5, 6, 6, 6, 7, 8, 9, 10, 12, 14, 13, 10, 5];
  function mulberry32(seed2) {
    let a2 = seed2 >>> 0;
    return () => {
      a2 = a2 + 1831565813 | 0;
      let t = Math.imul(a2 ^ a2 >>> 15, 1 | a2);
      t = t + Math.imul(t ^ t >>> 7, 61 | t) ^ t;
      return ((t ^ t >>> 14) >>> 0) / 4294967296;
    };
  }
  function makeRng(seed2) {
    const base = mulberry32(seed2);
    base.int = (min3, max4) => min3 + Math.floor(base() * (max4 - min3 + 1));
    base.pick = (items) => items[Math.floor(base() * items.length)];
    base.zipf = (items) => items[Math.min(items.length - 1, Math.floor(items.length * Math.pow(base(), 2)))];
    base.chance = (p) => base() < p;
    return base;
  }
  var NO_FLAGS = { text: true, image: false, link: false, gif: false, sticker: false, attachment: false };
  function makeContent(rng) {
    const count = rng.int(2, 22);
    const words = [];
    for (let i = 0; i < count; i++) words.push(rng.pick(WORDS));
    return words.join(" ");
  }
  function makeMessage(rng, seq, ts, channel, authorId, extras = {}) {
    const isOwn = authorId === SYNTHETIC_OWN_ID;
    let content = makeContent(rng);
    const customEmoji = [];
    const domains = [];
    const flags = { ...NO_FLAGS };
    if (isOwn && rng.chance(0.07)) {
      const emoji = rng.zipf(EMOJI_BANK);
      content += ` <:${emoji.name}:${emoji.id}>`;
      customEmoji.push({ ...emoji });
    }
    if (rng.chance(0.05)) {
      const domain = rng.zipf(DOMAINS);
      content += ` https://${domain}/x${seq.n}`;
      domains.push(domain);
      flags.link = true;
    }
    if (rng.chance(0.04)) {
      flags.attachment = true;
      flags.image = true;
    }
    const analysis = analyzeContent(content);
    return {
      kind: "message-created",
      ts,
      channel,
      messageId: `syn-${seq.n++}`,
      authorId,
      isOwn,
      content: isContentStorable({ isOwn, channelKind: channel.kind }) ? content : null,
      chars: analysis.chars,
      words: analysis.words,
      contentTypes: flags,
      replyToId: null,
      replyToAuthorId: null,
      replyToTs: null,
      customEmoji,
      domains,
      ...extras
    };
  }
  function* generateSyntheticEvents(opts = {}) {
    const days = opts.days ?? 730;
    const endTs = opts.endTs ?? Date.now();
    const rng = makeRng(opts.seed ?? 1);
    const seq = { n: 1 };
    for (let d = days - 1; d >= 0; d--) {
      const dayStart = new Date(endTs - d * 864e5);
      dayStart.setHours(0, 0, 0, 0);
      const weekday = dayStart.getDay();
      const dayFactor = (weekday === 0 || weekday === 5 || weekday === 6 ? 1.35 : 1) * (0.55 + rng() * 0.9);
      const conversationBudget = Math.round(9 * dayFactor);
      const dayEvents = [];
      for (let c2 = 0; c2 < conversationBudget; c2++) {
        const hour = pickHour(rng);
        let ts = dayStart.getTime() + hour * 36e5 + rng.int(0, 3599e3);
        const roll = rng();
        const channel = roll < 0.5 ? rng.pick(GUILD_CHANNELS) : roll < 0.85 ? dmChannel(rng.zipf(PEERS).id) : GROUP_DM;
        const isDm = channel.kind === "dm";
        const peer = isDm ? PEERS.find((p) => channel.channelId === `dm-${p.id}`) : rng.zipf(PEERS);
        const length = rng.int(2, isDm ? 14 : 9);
        let lastAuthor = null;
        let lastOwnMessageId = null;
        for (let m2 = 0; m2 < length; m2++) {
          const mine = isDm ? lastAuthor === SYNTHETIC_OWN_ID ? rng.chance(0.35) : rng.chance(0.75) : rng.chance(0.4);
          const authorId = mine ? SYNTHETIC_OWN_ID : isDm ? peer.id : rng.pick(channel.kind === "group-dm" ? GROUP_MEMBERS : PEERS.map((p) => p.id));
          const gap = mine ? rng.int(4e3, 12e4) : Math.round(peer.replyMeanMs * (0.3 + rng() * 1.6));
          ts += gap;
          if (mine) {
            const typingMs = rng.int(2e3, 45e3);
            dayEvents.push({
              kind: "typing-resolved",
              ts,
              channel,
              startedAt: ts - typingMs,
              durationMs: typingMs,
              committed: true,
              peerId: isDm ? peer.id : null
            });
          }
          const extras = {};
          if (!isDm && mine && lastOwnMessageId === null && rng.chance(0.25) && lastAuthor) {
            extras.replyToId = `syn-${seq.n - 1}`;
            extras.replyToAuthorId = lastAuthor;
            extras.replyToTs = ts - gap;
          }
          const message = makeMessage(rng, seq, ts, channel, authorId, extras);
          dayEvents.push(message);
          lastAuthor = authorId;
          if (mine) lastOwnMessageId = message.messageId;
          if (rng.chance(0.1)) {
            const reactorIsMe = !mine && rng.chance(0.5);
            dayEvents.push({
              kind: "reaction",
              ts: ts + rng.int(2e3, 6e4),
              channel,
              direction: 1,
              actorId: reactorIsMe ? SYNTHETIC_OWN_ID : peer.id,
              actorIsOwn: reactorIsMe,
              messageId: message.messageId,
              messageAuthorId: authorId,
              messageAuthorIsOwn: message.isOwn,
              emoji: rng.chance(0.5) ? { id: rng.zipf(EMOJI_BANK).id, name: "syn" } : { id: null, name: "👍" }
            });
          }
          if (mine && rng.chance(0.035)) {
            dayEvents.push({
              kind: "message-edited",
              ts: ts + rng.int(8e3, 3e5),
              channel,
              messageId: message.messageId,
              authorId,
              isOwn: true,
              content: message.content !== null ? `${message.content} (edited)` : null,
              editLatencyMs: rng.int(8e3, 3e5)
            });
          }
          if (mine && rng.chance(0.012)) {
            dayEvents.push({
              kind: "message-deleted",
              ts: ts + rng.int(2e4, 72e5),
              channelId: channel.channelId,
              guildId: channel.guildId,
              messageId: message.messageId
            });
          }
        }
        if (isDm && rng.chance(peer.hesitancy * 2)) {
          const abortMs = rng.int(3e3, 6e4);
          const abortTs = ts + rng.int(6e4, 18e5);
          dayEvents.push({
            kind: "typing-resolved",
            ts: abortTs,
            channel,
            startedAt: abortTs - abortMs,
            durationMs: abortMs,
            committed: false,
            peerId: peer.id
          });
        }
        if (!isDm && rng.chance(0.8)) {
          dayEvents.push({
            kind: "dwell",
            ts: ts + 1e3,
            channel,
            durationMs: rng.int(6e4, 20 * 6e4)
          });
        }
      }
      if (rng.chance(0.3)) {
        const start = dayStart.getTime() + rng.int(18, 22) * 36e5;
        const seconds2 = rng.int(15 * 60, 90 * 60);
        const coPresent = {};
        for (let i = 0, n = rng.int(1, 3); i < n; i++) {
          coPresent[rng.zipf(PEERS).id] = rng.int(Math.round(seconds2 / 2), seconds2);
        }
        dayEvents.push({ kind: "voice-segment", ts: start, channel: VOICE_CHANNEL, seconds: seconds2, coPresent });
      }
      dayEvents.sort((a2, b) => a2.ts - b.ts);
      yield* dayEvents;
    }
  }
  function pickHour(rng) {
    const total = HOUR_WEIGHTS.reduce((a2, b) => a2 + b, 0);
    let roll = rng() * total;
    for (let h = 0; h < 24; h++) {
      roll -= HOUR_WEIGHTS[h];
      if (roll <= 0) return h;
    }
    return 23;
  }
  async function runSynthetic(db, opts = {}) {
    const ctx = createReducerContext({
      ownUserId: SYNTHETIC_OWN_ID,
      settings: () => ({ conversationGapMinutes: 30 }),
      resolvePeerLabel: (id) => ({ label: PEER_NAMES[id] ?? `synth-${id}`, avatarHash: null }),
      dmRecipients: (channelId) => channelId.startsWith("dm-") ? [channelId.slice(3)] : channelId === GROUP_DM.channelId ? [...GROUP_MEMBERS] : null
    });
    let pending = new PendingBuffer();
    let events = 0;
    let lastTs = 0;
    for (const ev of generateSyntheticEvents(opts)) {
      reduceEvent(ev, pending, ctx);
      events++;
      lastTs = ev.ts;
      if (pending.events.length >= 2e3) {
        await flushPending(db, pending, { now: lastTs, heads: ctx.heads });
        pending = new PendingBuffer();
        opts.onProgress?.(events);
      }
    }
    await flushPending(db, pending, { now: lastTs || Date.now(), heads: ctx.heads });
    opts.onProgress?.(events);
    return { events, days: opts.days ?? 730 };
  }

  // src/ui/page/CaptureReadout.tsx
  var import_jsx_runtime21 = __toESM(require_jsx_runtime(), 1);
  function CaptureReadout({ engine }) {
    const snap = (0, import_react75.useSyncExternalStore)(engine.subscribe, engine.getSnapshot);
    const [rows, setRows] = (0, import_react75.useState)(null);
    const [storage, setStorage] = (0, import_react75.useState)(null);
    const [busy, setBusy] = (0, import_react75.useState)(null);
    (0, import_react75.useEffect)(() => {
      let alive = true;
      const refresh = async () => {
        const [rowCounts, estimate] = await Promise.all([engine.getRowCounts(), engine.getStorageEstimate()]);
        if (alive) {
          setRows(rowCounts);
          setStorage(estimate);
        }
      };
      void refresh();
      const timer2 = setInterval(() => void refresh(), 5e3);
      return () => {
        alive = false;
        clearInterval(timer2);
      };
    }, [engine]);
    const s = snap.session;
    const counters = [
      ["messages", s.byKind["message-created"] ?? 0],
      ["edits", s.byKind["message-edited"] ?? 0],
      ["deletes", s.byKind["message-deleted"] ?? 0],
      ["typing resolved", s.byKind["typing-resolved"] ?? 0],
      ["reactions", s.byKind["reaction"] ?? 0],
      ["dwell segments", s.byKind["dwell"] ?? 0],
      ["voice segments", s.byKind["voice-segment"] ?? 0],
      ["dropped", s.dropped]
    ];
    const runDevSynthetic = async (days) => {
      const db = engine.getDb();
      if (!db || busy) return;
      setBusy(`generating ${days}d…`);
      try {
        await runSynthetic(db, { days, seed: Date.now() % 1e5, onProgress: (n) => setBusy(`generating… ${n} events`) });
      } catch (e) {
        warn("synthetic generation failed", e);
      } finally {
        setBusy(null);
      }
    };
    const runWipe = async () => {
      const db = engine.getDb();
      if (!db || busy) return;
      if (typeof confirm === "function" && !confirm("Retraced: wipe ALL captured data? This cannot be undone.")) return;
      setBusy("wiping…");
      try {
        await wipeAll(db);
      } catch (e) {
        warn("wipe failed", e);
      } finally {
        setBusy(null);
      }
    };
    return /* @__PURE__ */ (0, import_jsx_runtime21.jsxs)("section", { className: "retraced-readout", children: [
      /* @__PURE__ */ (0, import_jsx_runtime21.jsxs)("h3", { children: [
        "Capture ",
        /* @__PURE__ */ (0, import_jsx_runtime21.jsx)("span", { className: `retraced-status retraced-status--${snap.status}`, children: snap.status })
      ] }),
      snap.statusDetail ? /* @__PURE__ */ (0, import_jsx_runtime21.jsx)("p", { className: "retraced-note", children: snap.statusDetail }) : null,
      /* @__PURE__ */ (0, import_jsx_runtime21.jsx)("div", { className: "retraced-counter-grid", children: counters.map(([label, value2]) => /* @__PURE__ */ (0, import_jsx_runtime21.jsxs)("div", { className: "retraced-counter", children: [
        /* @__PURE__ */ (0, import_jsx_runtime21.jsx)("span", { className: "retraced-counter-value", children: value2 }),
        /* @__PURE__ */ (0, import_jsx_runtime21.jsx)("span", { className: "retraced-note", children: label })
      ] }, label)) }),
      /* @__PURE__ */ (0, import_jsx_runtime21.jsxs)("p", { className: "retraced-note", children: [
        "session events: ",
        s.total,
        " · buffered: ",
        s.buffered,
        " · flushed: ",
        s.flushedEvents,
        " in ",
        s.flushes,
        " flushes",
        s.lastFlushTs ? ` · last flush ${Math.max(0, Math.round((Date.now() - s.lastFlushTs) / 1e3))}s ago` : "",
        s.openTypingSessions > 0 ? ` · typing now in ${s.openTypingSessions} channel(s)` : ""
      ] }),
      rows ? /* @__PURE__ */ (0, import_jsx_runtime21.jsxs)("p", { className: "retraced-note", children: [
        "stored rows — ",
        Object.entries(rows).map(([store, count]) => `${store}: ${count}`).join(" · ")
      ] }) : null,
      storage ? /* @__PURE__ */ (0, import_jsx_runtime21.jsxs)("p", { className: "retraced-note", children: [
        "storage used: ",
        (storage.usage / 1048576).toFixed(1),
        " MiB of ",
        (storage.quota / 1073741824).toFixed(1),
        " GiB quota"
      ] }) : null,
      /* @__PURE__ */ (0, import_jsx_runtime21.jsxs)("details", { className: "retraced-devtools", children: [
        /* @__PURE__ */ (0, import_jsx_runtime21.jsx)("summary", { className: "retraced-note", children: "dev tools" }),
        /* @__PURE__ */ (0, import_jsx_runtime21.jsxs)("div", { className: "retraced-devtools-row", children: [
          /* @__PURE__ */ (0, import_jsx_runtime21.jsx)("button", { type: "button", className: "retraced-button", disabled: busy !== null, onClick: () => void runDevSynthetic(365), children: "Generate 1y synthetic data" }),
          /* @__PURE__ */ (0, import_jsx_runtime21.jsx)("button", { type: "button", className: "retraced-button", disabled: busy !== null, onClick: () => void runDevSynthetic(90), children: "Generate 90d" }),
          /* @__PURE__ */ (0, import_jsx_runtime21.jsx)("button", { type: "button", className: "retraced-button retraced-button--danger", disabled: busy !== null, onClick: () => void runWipe(), children: "Wipe all data" }),
          busy ? /* @__PURE__ */ (0, import_jsx_runtime21.jsx)("span", { className: "retraced-note", children: busy }) : null
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime21.jsx)("p", { className: "retraced-note", children: "Synthetic data exercises the real capture pipeline so charts can be developed before a week of real usage exists." })
      ] })
    ] });
  }

  // src/ui/page/OverviewTab.tsx
  var import_jsx_runtime22 = __toESM(require_jsx_runtime(), 1);
  function OverviewTab({ engine, rangeDays }) {
    const theme = (0, import_react76.useMemo)(readChartTheme, []);
    const labels = (0, import_react76.useMemo)(createLabelResolvers, []);
    const data = useRollups(engine);
    const [width, measureRef] = useElementWidth();
    const chartWidth = Math.max(320, width - 32);
    const today = dateKey(Date.now());
    const from2 = rangeDays === null ? null : shiftDate(today, -(rangeDays - 1));
    const rangeDaily = (0, import_react76.useMemo)(() => from2 ? data.daily.filter((r2) => r2.date >= from2) : data.daily, [data.daily, from2]);
    const rangeHourly = (0, import_react76.useMemo)(() => from2 ? data.hourly.filter((r2) => r2.date >= from2) : data.hourly, [data.hourly, from2]);
    const stats = (0, import_react76.useMemo)(
      () => summaryStats({ daily: rangeDaily, hourly: rangeHourly, peers: data.peers, streakDaily: data.daily, today }),
      [rangeDaily, rangeHourly, data.peers, data.daily, today]
    );
    const seriesFrom = from2 ?? rangeDaily.reduce((min3, r2) => r2.date < min3 ? r2.date : min3, today);
    const daySeries = (0, import_react76.useMemo)(() => messagesPerDaySeries(rangeDaily, seriesFrom, today), [rangeDaily, seriesFrom, today]);
    const slotOrder = (0, import_react76.useMemo)(() => guildSlotOrder(data.daily), [data.daily]);
    const slotFor = (guildId) => slotOrder.get(guildId) ?? -1;
    const stream = (0, import_react76.useMemo)(
      () => serverShareWeekly(rangeDaily, { guildLabel: labels.guildLabel, slotFor }),
      [rangeDaily, labels, slotOrder]
    );
    const sankeyData = (0, import_react76.useMemo)(
      () => sankeyFlows(rangeDaily, { guildLabel: labels.guildLabel, channelLabel: labels.channelLabel, slotFor }),
      [rangeDaily, labels, slotOrder]
    );
    const calendar2 = (0, import_react76.useMemo)(() => calendarByYear(data.daily), [data.daily]);
    if (!data.loaded) return /* @__PURE__ */ (0, import_jsx_runtime22.jsx)("p", { className: "retraced-note", children: "loading…" });
    return /* @__PURE__ */ (0, import_jsx_runtime22.jsxs)("div", { ref: measureRef, className: "retraced-overview", children: [
      data.dbMissing ? /* @__PURE__ */ (0, import_jsx_runtime22.jsx)("p", { className: "retraced-banner", children: "Capture is unavailable, so charts have nothing to read — details in the capture card below." }) : null,
      /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(
        ChartCard,
        {
          title: "Where your messages go",
          subtitle: `you → servers → channels · ${formatCount(stats.totalSent)} messages in range`,
          empty: sankeyData === null ? "Send a few messages and they will flow here — you → servers → channels." : null,
          table: sankeyData ? {
            columns: ["From", "To", "Messages"],
            rows: sankeyData.links.map((l) => [
              sankeyData.nodes.find((n) => n.id === l.source)?.label ?? l.source,
              sankeyData.nodes.find((n) => n.id === l.target)?.label ?? l.target,
              formatCount(l.value)
            ])
          } : null,
          children: sankeyData ? /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(SankeyFlow, { data: sankeyData, theme, width: chartWidth }) : null
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(SummaryCards, { stats }),
      /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(
        ChartCard,
        {
          title: "Messages per day",
          subtitle: "your sent messages, with a 7-day average",
          legend: [
            { label: "Sent", color: theme.series[0] },
            { label: "7-day average", color: theme.ink.primary }
          ],
          empty: stats.totalSent === 0 ? "No messages in this range yet — try a wider range, or just go chat." : null,
          table: {
            columns: ["Date", "Messages", "7-day avg"],
            rows: daySeries.map((p) => [p.date, p.sent, p.avg7.toFixed(1)])
          },
          children: /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(MessagesPerDay, { data: daySeries, theme, width: chartWidth })
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(
        ChartCard,
        {
          title: "Server share over time",
          subtitle: "weekly message volume, by where it went",
          legend: stream.series.map((s) => ({ label: s.label, color: colorForSlot(theme, s.colorSlot) })),
          empty: stream.weeks.length < 2 ? "Needs at least two weeks of activity to draw the flow — check back soon." : null,
          table: {
            columns: ["Week", ...stream.series.map((s) => s.label)],
            rows: stream.weeks.map((w) => [w.week, ...stream.series.map((s) => w[s.key])])
          },
          children: /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(ServerStreamgraph, { data: stream, theme, width: chartWidth })
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(
        ChartCard,
        {
          title: "Activity calendar",
          subtitle: "every day, all time",
          empty: calendar2.length === 0 ? "A year view builds up day by day — check back after a few days of chatting." : null,
          table: calendar2.length > 0 ? {
            columns: ["Date", "Messages"],
            rows: calendar2.flatMap((y5) => y5.days.map((d) => [d.date, d.count]))
          } : null,
          children: /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(CalendarHeatmap, { years: calendar2, theme })
        }
      ),
      engine ? /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(CaptureReadout, { engine }) : null
    ] });
  }

  // src/ui/page/PeopleTab.tsx
  var import_react78 = __toESM(require_react(), 1);

  // src/ui/charts/DivergingPairChart.tsx
  var import_jsx_runtime23 = __toESM(require_jsx_runtime(), 1);
  function niceCeil2(n) {
    const power = 10 ** Math.floor(Math.log10(Math.max(n, 1)));
    const mantissa = n / power;
    const step = [1, 1.5, 2, 2.5, 3, 4, 5, 6, 8, 10].find((s) => mantissa <= s) ?? 10;
    return step * power;
  }
  function DivergingPairChart({
    data,
    leftName,
    rightName,
    leftColor,
    rightColor,
    theme,
    width
  }) {
    const rows = data.map((r2) => ({ ...r2, leftNeg: -r2.left }));
    const height = 40 + rows.length * 30;
    const left2 = niceCeil2(Math.max(...data.map((r2) => r2.left), 1));
    const right2 = niceCeil2(Math.max(...data.map((r2) => r2.right), 1));
    return /* @__PURE__ */ (0, import_jsx_runtime23.jsxs)(BarChart, { layout: "vertical", width, height, data: rows, stackOffset: "sign", margin: { top: 4, right: 16, bottom: 0, left: 8 }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime23.jsx)(
        XAxis,
        {
          type: "number",
          domain: [-left2, right2],
          ticks: [-left2, 0, Math.round(right2 / 2), right2],
          tickFormatter: (v) => formatCount(Math.abs(v)),
          tick: { fill: theme.ink.muted, fontSize: 11 },
          axisLine: { stroke: theme.ink.axis },
          tickLine: false
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime23.jsx)(YAxis, { type: "category", dataKey: "label", width: 150, tick: { fill: theme.ink.muted, fontSize: 11 }, axisLine: false, tickLine: false }),
      /* @__PURE__ */ (0, import_jsx_runtime23.jsx)(ReferenceLine, { x: 0, stroke: theme.ink.axis }),
      /* @__PURE__ */ (0, import_jsx_runtime23.jsx)(
        Tooltip,
        {
          cursor: { fill: theme.ink.grid },
          content: ({ active, payload }) => {
            if (!active || !payload?.length) return null;
            const row = payload[0].payload;
            return /* @__PURE__ */ (0, import_jsx_runtime23.jsxs)("div", { className: "retraced-tooltip", children: [
              /* @__PURE__ */ (0, import_jsx_runtime23.jsx)("div", { className: "retraced-tooltip-title", children: row.label }),
              /* @__PURE__ */ (0, import_jsx_runtime23.jsxs)("div", { className: "retraced-tooltip-row", children: [
                /* @__PURE__ */ (0, import_jsx_runtime23.jsx)("span", { className: "retraced-legend-dot", style: { background: leftColor } }),
                /* @__PURE__ */ (0, import_jsx_runtime23.jsx)("span", { children: leftName }),
                /* @__PURE__ */ (0, import_jsx_runtime23.jsx)("span", { className: "retraced-tooltip-value", children: formatCount(row.left) })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime23.jsxs)("div", { className: "retraced-tooltip-row", children: [
                /* @__PURE__ */ (0, import_jsx_runtime23.jsx)("span", { className: "retraced-legend-dot", style: { background: rightColor } }),
                /* @__PURE__ */ (0, import_jsx_runtime23.jsx)("span", { children: rightName }),
                /* @__PURE__ */ (0, import_jsx_runtime23.jsx)("span", { className: "retraced-tooltip-value", children: formatCount(row.right) })
              ] }),
              row.note ? /* @__PURE__ */ (0, import_jsx_runtime23.jsx)("div", { className: "retraced-note", children: row.note }) : null
            ] });
          }
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime23.jsx)(Bar, { dataKey: "leftNeg", stackId: "pair", fill: leftColor, radius: [4, 0, 0, 4], maxBarSize: 16, isAnimationActive: false }),
      /* @__PURE__ */ (0, import_jsx_runtime23.jsx)(Bar, { dataKey: "right", stackId: "pair", fill: rightColor, radius: [0, 4, 4, 0], maxBarSize: 16, isAnimationActive: false })
    ] });
  }

  // node_modules/d3-quadtree/src/add.js
  function add_default(d) {
    const x5 = +this._x.call(null, d), y5 = +this._y.call(null, d);
    return add(this.cover(x5, y5), x5, y5, d);
  }
  function add(tree, x5, y5, d) {
    if (isNaN(x5) || isNaN(y5)) return tree;
    var parent, node = tree._root, leaf = { data: d }, x0 = tree._x0, y0 = tree._y0, x1 = tree._x1, y1 = tree._y1, xm, ym, xp, yp, right2, bottom, i, j;
    if (!node) return tree._root = leaf, tree;
    while (node.length) {
      if (right2 = x5 >= (xm = (x0 + x1) / 2)) x0 = xm;
      else x1 = xm;
      if (bottom = y5 >= (ym = (y0 + y1) / 2)) y0 = ym;
      else y1 = ym;
      if (parent = node, !(node = node[i = bottom << 1 | right2])) return parent[i] = leaf, tree;
    }
    xp = +tree._x.call(null, node.data);
    yp = +tree._y.call(null, node.data);
    if (x5 === xp && y5 === yp) return leaf.next = node, parent ? parent[i] = leaf : tree._root = leaf, tree;
    do {
      parent = parent ? parent[i] = new Array(4) : tree._root = new Array(4);
      if (right2 = x5 >= (xm = (x0 + x1) / 2)) x0 = xm;
      else x1 = xm;
      if (bottom = y5 >= (ym = (y0 + y1) / 2)) y0 = ym;
      else y1 = ym;
    } while ((i = bottom << 1 | right2) === (j = (yp >= ym) << 1 | xp >= xm));
    return parent[j] = node, parent[i] = leaf, tree;
  }
  function addAll(data) {
    var d, i, n = data.length, x5, y5, xz = new Array(n), yz = new Array(n), x0 = Infinity, y0 = Infinity, x1 = -Infinity, y1 = -Infinity;
    for (i = 0; i < n; ++i) {
      if (isNaN(x5 = +this._x.call(null, d = data[i])) || isNaN(y5 = +this._y.call(null, d))) continue;
      xz[i] = x5;
      yz[i] = y5;
      if (x5 < x0) x0 = x5;
      if (x5 > x1) x1 = x5;
      if (y5 < y0) y0 = y5;
      if (y5 > y1) y1 = y5;
    }
    if (x0 > x1 || y0 > y1) return this;
    this.cover(x0, y0).cover(x1, y1);
    for (i = 0; i < n; ++i) {
      add(this, xz[i], yz[i], data[i]);
    }
    return this;
  }

  // node_modules/d3-quadtree/src/cover.js
  function cover_default(x5, y5) {
    if (isNaN(x5 = +x5) || isNaN(y5 = +y5)) return this;
    var x0 = this._x0, y0 = this._y0, x1 = this._x1, y1 = this._y1;
    if (isNaN(x0)) {
      x1 = (x0 = Math.floor(x5)) + 1;
      y1 = (y0 = Math.floor(y5)) + 1;
    } else {
      var z = x1 - x0 || 1, node = this._root, parent, i;
      while (x0 > x5 || x5 >= x1 || y0 > y5 || y5 >= y1) {
        i = (y5 < y0) << 1 | x5 < x0;
        parent = new Array(4), parent[i] = node, node = parent, z *= 2;
        switch (i) {
          case 0:
            x1 = x0 + z, y1 = y0 + z;
            break;
          case 1:
            x0 = x1 - z, y1 = y0 + z;
            break;
          case 2:
            x1 = x0 + z, y0 = y1 - z;
            break;
          case 3:
            x0 = x1 - z, y0 = y1 - z;
            break;
        }
      }
      if (this._root && this._root.length) this._root = node;
    }
    this._x0 = x0;
    this._y0 = y0;
    this._x1 = x1;
    this._y1 = y1;
    return this;
  }

  // node_modules/d3-quadtree/src/data.js
  function data_default() {
    var data = [];
    this.visit(function(node) {
      if (!node.length) do
        data.push(node.data);
      while (node = node.next);
    });
    return data;
  }

  // node_modules/d3-quadtree/src/extent.js
  function extent_default(_) {
    return arguments.length ? this.cover(+_[0][0], +_[0][1]).cover(+_[1][0], +_[1][1]) : isNaN(this._x0) ? void 0 : [[this._x0, this._y0], [this._x1, this._y1]];
  }

  // node_modules/d3-quadtree/src/quad.js
  function quad_default(node, x0, y0, x1, y1) {
    this.node = node;
    this.x0 = x0;
    this.y0 = y0;
    this.x1 = x1;
    this.y1 = y1;
  }

  // node_modules/d3-quadtree/src/find.js
  function find_default(x5, y5, radius) {
    var data, x0 = this._x0, y0 = this._y0, x1, y1, x22, y22, x32 = this._x1, y32 = this._y1, quads = [], node = this._root, q, i;
    if (node) quads.push(new quad_default(node, x0, y0, x32, y32));
    if (radius == null) radius = Infinity;
    else {
      x0 = x5 - radius, y0 = y5 - radius;
      x32 = x5 + radius, y32 = y5 + radius;
      radius *= radius;
    }
    while (q = quads.pop()) {
      if (!(node = q.node) || (x1 = q.x0) > x32 || (y1 = q.y0) > y32 || (x22 = q.x1) < x0 || (y22 = q.y1) < y0) continue;
      if (node.length) {
        var xm = (x1 + x22) / 2, ym = (y1 + y22) / 2;
        quads.push(
          new quad_default(node[3], xm, ym, x22, y22),
          new quad_default(node[2], x1, ym, xm, y22),
          new quad_default(node[1], xm, y1, x22, ym),
          new quad_default(node[0], x1, y1, xm, ym)
        );
        if (i = (y5 >= ym) << 1 | x5 >= xm) {
          q = quads[quads.length - 1];
          quads[quads.length - 1] = quads[quads.length - 1 - i];
          quads[quads.length - 1 - i] = q;
        }
      } else {
        var dx = x5 - +this._x.call(null, node.data), dy = y5 - +this._y.call(null, node.data), d2 = dx * dx + dy * dy;
        if (d2 < radius) {
          var d = Math.sqrt(radius = d2);
          x0 = x5 - d, y0 = y5 - d;
          x32 = x5 + d, y32 = y5 + d;
          data = node.data;
        }
      }
    }
    return data;
  }

  // node_modules/d3-quadtree/src/remove.js
  function remove_default(d) {
    if (isNaN(x5 = +this._x.call(null, d)) || isNaN(y5 = +this._y.call(null, d))) return this;
    var parent, node = this._root, retainer, previous, next, x0 = this._x0, y0 = this._y0, x1 = this._x1, y1 = this._y1, x5, y5, xm, ym, right2, bottom, i, j;
    if (!node) return this;
    if (node.length) while (true) {
      if (right2 = x5 >= (xm = (x0 + x1) / 2)) x0 = xm;
      else x1 = xm;
      if (bottom = y5 >= (ym = (y0 + y1) / 2)) y0 = ym;
      else y1 = ym;
      if (!(parent = node, node = node[i = bottom << 1 | right2])) return this;
      if (!node.length) break;
      if (parent[i + 1 & 3] || parent[i + 2 & 3] || parent[i + 3 & 3]) retainer = parent, j = i;
    }
    while (node.data !== d) if (!(previous = node, node = node.next)) return this;
    if (next = node.next) delete node.next;
    if (previous) return next ? previous.next = next : delete previous.next, this;
    if (!parent) return this._root = next, this;
    next ? parent[i] = next : delete parent[i];
    if ((node = parent[0] || parent[1] || parent[2] || parent[3]) && node === (parent[3] || parent[2] || parent[1] || parent[0]) && !node.length) {
      if (retainer) retainer[j] = node;
      else this._root = node;
    }
    return this;
  }
  function removeAll(data) {
    for (var i = 0, n = data.length; i < n; ++i) this.remove(data[i]);
    return this;
  }

  // node_modules/d3-quadtree/src/root.js
  function root_default() {
    return this._root;
  }

  // node_modules/d3-quadtree/src/size.js
  function size_default() {
    var size = 0;
    this.visit(function(node) {
      if (!node.length) do
        ++size;
      while (node = node.next);
    });
    return size;
  }

  // node_modules/d3-quadtree/src/visit.js
  function visit_default(callback) {
    var quads = [], q, node = this._root, child, x0, y0, x1, y1;
    if (node) quads.push(new quad_default(node, this._x0, this._y0, this._x1, this._y1));
    while (q = quads.pop()) {
      if (!callback(node = q.node, x0 = q.x0, y0 = q.y0, x1 = q.x1, y1 = q.y1) && node.length) {
        var xm = (x0 + x1) / 2, ym = (y0 + y1) / 2;
        if (child = node[3]) quads.push(new quad_default(child, xm, ym, x1, y1));
        if (child = node[2]) quads.push(new quad_default(child, x0, ym, xm, y1));
        if (child = node[1]) quads.push(new quad_default(child, xm, y0, x1, ym));
        if (child = node[0]) quads.push(new quad_default(child, x0, y0, xm, ym));
      }
    }
    return this;
  }

  // node_modules/d3-quadtree/src/visitAfter.js
  function visitAfter_default(callback) {
    var quads = [], next = [], q;
    if (this._root) quads.push(new quad_default(this._root, this._x0, this._y0, this._x1, this._y1));
    while (q = quads.pop()) {
      var node = q.node;
      if (node.length) {
        var child, x0 = q.x0, y0 = q.y0, x1 = q.x1, y1 = q.y1, xm = (x0 + x1) / 2, ym = (y0 + y1) / 2;
        if (child = node[0]) quads.push(new quad_default(child, x0, y0, xm, ym));
        if (child = node[1]) quads.push(new quad_default(child, xm, y0, x1, ym));
        if (child = node[2]) quads.push(new quad_default(child, x0, ym, xm, y1));
        if (child = node[3]) quads.push(new quad_default(child, xm, ym, x1, y1));
      }
      next.push(q);
    }
    while (q = next.pop()) {
      callback(q.node, q.x0, q.y0, q.x1, q.y1);
    }
    return this;
  }

  // node_modules/d3-quadtree/src/x.js
  function defaultX(d) {
    return d[0];
  }
  function x_default(_) {
    return arguments.length ? (this._x = _, this) : this._x;
  }

  // node_modules/d3-quadtree/src/y.js
  function defaultY(d) {
    return d[1];
  }
  function y_default(_) {
    return arguments.length ? (this._y = _, this) : this._y;
  }

  // node_modules/d3-quadtree/src/quadtree.js
  function quadtree(nodes, x5, y5) {
    var tree = new Quadtree(x5 == null ? defaultX : x5, y5 == null ? defaultY : y5, NaN, NaN, NaN, NaN);
    return nodes == null ? tree : tree.addAll(nodes);
  }
  function Quadtree(x5, y5, x0, y0, x1, y1) {
    this._x = x5;
    this._y = y5;
    this._x0 = x0;
    this._y0 = y0;
    this._x1 = x1;
    this._y1 = y1;
    this._root = void 0;
  }
  function leaf_copy(leaf) {
    var copy3 = { data: leaf.data }, next = copy3;
    while (leaf = leaf.next) next = next.next = { data: leaf.data };
    return copy3;
  }
  var treeProto = quadtree.prototype = Quadtree.prototype;
  treeProto.copy = function() {
    var copy3 = new Quadtree(this._x, this._y, this._x0, this._y0, this._x1, this._y1), node = this._root, nodes, child;
    if (!node) return copy3;
    if (!node.length) return copy3._root = leaf_copy(node), copy3;
    nodes = [{ source: node, target: copy3._root = new Array(4) }];
    while (node = nodes.pop()) {
      for (var i = 0; i < 4; ++i) {
        if (child = node.source[i]) {
          if (child.length) nodes.push({ source: child, target: node.target[i] = new Array(4) });
          else node.target[i] = leaf_copy(child);
        }
      }
    }
    return copy3;
  };
  treeProto.add = add_default;
  treeProto.addAll = addAll;
  treeProto.cover = cover_default;
  treeProto.data = data_default;
  treeProto.extent = extent_default;
  treeProto.find = find_default;
  treeProto.remove = remove_default;
  treeProto.removeAll = removeAll;
  treeProto.root = root_default;
  treeProto.size = size_default;
  treeProto.visit = visit_default;
  treeProto.visitAfter = visitAfter_default;
  treeProto.x = x_default;
  treeProto.y = y_default;

  // node_modules/d3-force/src/constant.js
  function constant_default4(x5) {
    return function() {
      return x5;
    };
  }

  // node_modules/d3-force/src/jiggle.js
  function jiggle_default(random) {
    return (random() - 0.5) * 1e-6;
  }

  // node_modules/d3-force/src/collide.js
  function x3(d) {
    return d.x + d.vx;
  }
  function y3(d) {
    return d.y + d.vy;
  }
  function collide_default(radius) {
    var nodes, radii, random, strength = 1, iterations = 1;
    if (typeof radius !== "function") radius = constant_default4(radius == null ? 1 : +radius);
    function force() {
      var i, n = nodes.length, tree, node, xi, yi, ri, ri2;
      for (var k = 0; k < iterations; ++k) {
        tree = quadtree(nodes, x3, y3).visitAfter(prepare);
        for (i = 0; i < n; ++i) {
          node = nodes[i];
          ri = radii[node.index], ri2 = ri * ri;
          xi = node.x + node.vx;
          yi = node.y + node.vy;
          tree.visit(apply);
        }
      }
      function apply(quad, x0, y0, x1, y1) {
        var data = quad.data, rj = quad.r, r2 = ri + rj;
        if (data) {
          if (data.index > node.index) {
            var x5 = xi - data.x - data.vx, y5 = yi - data.y - data.vy, l = x5 * x5 + y5 * y5;
            if (l < r2 * r2) {
              if (x5 === 0) x5 = jiggle_default(random), l += x5 * x5;
              if (y5 === 0) y5 = jiggle_default(random), l += y5 * y5;
              l = (r2 - (l = Math.sqrt(l))) / l * strength;
              node.vx += (x5 *= l) * (r2 = (rj *= rj) / (ri2 + rj));
              node.vy += (y5 *= l) * r2;
              data.vx -= x5 * (r2 = 1 - r2);
              data.vy -= y5 * r2;
            }
          }
          return;
        }
        return x0 > xi + r2 || x1 < xi - r2 || y0 > yi + r2 || y1 < yi - r2;
      }
    }
    function prepare(quad) {
      if (quad.data) return quad.r = radii[quad.data.index];
      for (var i = quad.r = 0; i < 4; ++i) {
        if (quad[i] && quad[i].r > quad.r) {
          quad.r = quad[i].r;
        }
      }
    }
    function initialize() {
      if (!nodes) return;
      var i, n = nodes.length, node;
      radii = new Array(n);
      for (i = 0; i < n; ++i) node = nodes[i], radii[node.index] = +radius(node, i, nodes);
    }
    force.initialize = function(_nodes, _random) {
      nodes = _nodes;
      random = _random;
      initialize();
    };
    force.iterations = function(_) {
      return arguments.length ? (iterations = +_, force) : iterations;
    };
    force.strength = function(_) {
      return arguments.length ? (strength = +_, force) : strength;
    };
    force.radius = function(_) {
      return arguments.length ? (radius = typeof _ === "function" ? _ : constant_default4(+_), initialize(), force) : radius;
    };
    return force;
  }

  // node_modules/d3-force/src/link.js
  function index(d) {
    return d.index;
  }
  function find2(nodeById, nodeId) {
    var node = nodeById.get(nodeId);
    if (!node) throw new Error("node not found: " + nodeId);
    return node;
  }
  function link_default(links) {
    var id = index, strength = defaultStrength, strengths, distance = constant_default4(30), distances, nodes, count, bias, random, iterations = 1;
    if (links == null) links = [];
    function defaultStrength(link2) {
      return 1 / Math.min(count[link2.source.index], count[link2.target.index]);
    }
    function force(alpha) {
      for (var k = 0, n = links.length; k < iterations; ++k) {
        for (var i = 0, link2, source, target, x5, y5, l, b; i < n; ++i) {
          link2 = links[i], source = link2.source, target = link2.target;
          x5 = target.x + target.vx - source.x - source.vx || jiggle_default(random);
          y5 = target.y + target.vy - source.y - source.vy || jiggle_default(random);
          l = Math.sqrt(x5 * x5 + y5 * y5);
          l = (l - distances[i]) / l * alpha * strengths[i];
          x5 *= l, y5 *= l;
          target.vx -= x5 * (b = bias[i]);
          target.vy -= y5 * b;
          source.vx += x5 * (b = 1 - b);
          source.vy += y5 * b;
        }
      }
    }
    function initialize() {
      if (!nodes) return;
      var i, n = nodes.length, m2 = links.length, nodeById = new Map(nodes.map((d, i2) => [id(d, i2, nodes), d])), link2;
      for (i = 0, count = new Array(n); i < m2; ++i) {
        link2 = links[i], link2.index = i;
        if (typeof link2.source !== "object") link2.source = find2(nodeById, link2.source);
        if (typeof link2.target !== "object") link2.target = find2(nodeById, link2.target);
        count[link2.source.index] = (count[link2.source.index] || 0) + 1;
        count[link2.target.index] = (count[link2.target.index] || 0) + 1;
      }
      for (i = 0, bias = new Array(m2); i < m2; ++i) {
        link2 = links[i], bias[i] = count[link2.source.index] / (count[link2.source.index] + count[link2.target.index]);
      }
      strengths = new Array(m2), initializeStrength();
      distances = new Array(m2), initializeDistance();
    }
    function initializeStrength() {
      if (!nodes) return;
      for (var i = 0, n = links.length; i < n; ++i) {
        strengths[i] = +strength(links[i], i, links);
      }
    }
    function initializeDistance() {
      if (!nodes) return;
      for (var i = 0, n = links.length; i < n; ++i) {
        distances[i] = +distance(links[i], i, links);
      }
    }
    force.initialize = function(_nodes, _random) {
      nodes = _nodes;
      random = _random;
      initialize();
    };
    force.links = function(_) {
      return arguments.length ? (links = _, initialize(), force) : links;
    };
    force.id = function(_) {
      return arguments.length ? (id = _, force) : id;
    };
    force.iterations = function(_) {
      return arguments.length ? (iterations = +_, force) : iterations;
    };
    force.strength = function(_) {
      return arguments.length ? (strength = typeof _ === "function" ? _ : constant_default4(+_), initializeStrength(), force) : strength;
    };
    force.distance = function(_) {
      return arguments.length ? (distance = typeof _ === "function" ? _ : constant_default4(+_), initializeDistance(), force) : distance;
    };
    return force;
  }

  // node_modules/d3-dispatch/src/dispatch.js
  var noop4 = { value: () => {
  } };
  function dispatch() {
    for (var i = 0, n = arguments.length, _ = {}, t; i < n; ++i) {
      if (!(t = arguments[i] + "") || t in _ || /[\s.]/.test(t)) throw new Error("illegal type: " + t);
      _[t] = [];
    }
    return new Dispatch(_);
  }
  function Dispatch(_) {
    this._ = _;
  }
  function parseTypenames(typenames, types) {
    return typenames.trim().split(/^|\s+/).map(function(t) {
      var name = "", i = t.indexOf(".");
      if (i >= 0) name = t.slice(i + 1), t = t.slice(0, i);
      if (t && !types.hasOwnProperty(t)) throw new Error("unknown type: " + t);
      return { type: t, name };
    });
  }
  Dispatch.prototype = dispatch.prototype = {
    constructor: Dispatch,
    on: function(typename, callback) {
      var _ = this._, T = parseTypenames(typename + "", _), t, i = -1, n = T.length;
      if (arguments.length < 2) {
        while (++i < n) if ((t = (typename = T[i]).type) && (t = get3(_[t], typename.name))) return t;
        return;
      }
      if (callback != null && typeof callback !== "function") throw new Error("invalid callback: " + callback);
      while (++i < n) {
        if (t = (typename = T[i]).type) _[t] = set2(_[t], typename.name, callback);
        else if (callback == null) for (t in _) _[t] = set2(_[t], typename.name, null);
      }
      return this;
    },
    copy: function() {
      var copy3 = {}, _ = this._;
      for (var t in _) copy3[t] = _[t].slice();
      return new Dispatch(copy3);
    },
    call: function(type, that) {
      if ((n = arguments.length - 2) > 0) for (var args = new Array(n), i = 0, n, t; i < n; ++i) args[i] = arguments[i + 2];
      if (!this._.hasOwnProperty(type)) throw new Error("unknown type: " + type);
      for (t = this._[type], i = 0, n = t.length; i < n; ++i) t[i].value.apply(that, args);
    },
    apply: function(type, that, args) {
      if (!this._.hasOwnProperty(type)) throw new Error("unknown type: " + type);
      for (var t = this._[type], i = 0, n = t.length; i < n; ++i) t[i].value.apply(that, args);
    }
  };
  function get3(type, name) {
    for (var i = 0, n = type.length, c2; i < n; ++i) {
      if ((c2 = type[i]).name === name) {
        return c2.value;
      }
    }
  }
  function set2(type, name, callback) {
    for (var i = 0, n = type.length; i < n; ++i) {
      if (type[i].name === name) {
        type[i] = noop4, type = type.slice(0, i).concat(type.slice(i + 1));
        break;
      }
    }
    if (callback != null) type.push({ name, value: callback });
    return type;
  }
  var dispatch_default = dispatch;

  // node_modules/d3-timer/src/timer.js
  var frame = 0;
  var timeout = 0;
  var interval = 0;
  var pokeDelay = 1e3;
  var taskHead;
  var taskTail;
  var clockLast = 0;
  var clockNow = 0;
  var clockSkew = 0;
  var clock = typeof performance === "object" && performance.now ? performance : Date;
  var setFrame = typeof window === "object" && window.requestAnimationFrame ? window.requestAnimationFrame.bind(window) : function(f) {
    setTimeout(f, 17);
  };
  function now() {
    return clockNow || (setFrame(clearNow), clockNow = clock.now() + clockSkew);
  }
  function clearNow() {
    clockNow = 0;
  }
  function Timer() {
    this._call = this._time = this._next = null;
  }
  Timer.prototype = timer.prototype = {
    constructor: Timer,
    restart: function(callback, delay, time2) {
      if (typeof callback !== "function") throw new TypeError("callback is not a function");
      time2 = (time2 == null ? now() : +time2) + (delay == null ? 0 : +delay);
      if (!this._next && taskTail !== this) {
        if (taskTail) taskTail._next = this;
        else taskHead = this;
        taskTail = this;
      }
      this._call = callback;
      this._time = time2;
      sleep();
    },
    stop: function() {
      if (this._call) {
        this._call = null;
        this._time = Infinity;
        sleep();
      }
    }
  };
  function timer(callback, delay, time2) {
    var t = new Timer();
    t.restart(callback, delay, time2);
    return t;
  }
  function timerFlush() {
    now();
    ++frame;
    var t = taskHead, e;
    while (t) {
      if ((e = clockNow - t._time) >= 0) t._call.call(void 0, e);
      t = t._next;
    }
    --frame;
  }
  function wake() {
    clockNow = (clockLast = clock.now()) + clockSkew;
    frame = timeout = 0;
    try {
      timerFlush();
    } finally {
      frame = 0;
      nap();
      clockNow = 0;
    }
  }
  function poke() {
    var now2 = clock.now(), delay = now2 - clockLast;
    if (delay > pokeDelay) clockSkew -= delay, clockLast = now2;
  }
  function nap() {
    var t02, t12 = taskHead, t2, time2 = Infinity;
    while (t12) {
      if (t12._call) {
        if (time2 > t12._time) time2 = t12._time;
        t02 = t12, t12 = t12._next;
      } else {
        t2 = t12._next, t12._next = null;
        t12 = t02 ? t02._next = t2 : taskHead = t2;
      }
    }
    taskTail = t02;
    sleep(time2);
  }
  function sleep(time2) {
    if (frame) return;
    if (timeout) timeout = clearTimeout(timeout);
    var delay = time2 - clockNow;
    if (delay > 24) {
      if (time2 < Infinity) timeout = setTimeout(wake, time2 - clock.now() - clockSkew);
      if (interval) interval = clearInterval(interval);
    } else {
      if (!interval) clockLast = clock.now(), interval = setInterval(poke, pokeDelay);
      frame = 1, setFrame(wake);
    }
  }

  // node_modules/d3-force/src/lcg.js
  var a = 1664525;
  var c = 1013904223;
  var m = 4294967296;
  function lcg_default() {
    let s = 1;
    return () => (s = (a * s + c) % m) / m;
  }

  // node_modules/d3-force/src/simulation.js
  function x4(d) {
    return d.x;
  }
  function y4(d) {
    return d.y;
  }
  var initialRadius = 10;
  var initialAngle = Math.PI * (3 - Math.sqrt(5));
  function simulation_default(nodes) {
    var simulation, alpha = 1, alphaMin = 1e-3, alphaDecay = 1 - Math.pow(alphaMin, 1 / 300), alphaTarget = 0, velocityDecay = 0.6, forces = /* @__PURE__ */ new Map(), stepper = timer(step), event = dispatch_default("tick", "end"), random = lcg_default();
    if (nodes == null) nodes = [];
    function step() {
      tick();
      event.call("tick", simulation);
      if (alpha < alphaMin) {
        stepper.stop();
        event.call("end", simulation);
      }
    }
    function tick(iterations) {
      var i, n = nodes.length, node;
      if (iterations === void 0) iterations = 1;
      for (var k = 0; k < iterations; ++k) {
        alpha += (alphaTarget - alpha) * alphaDecay;
        forces.forEach(function(force) {
          force(alpha);
        });
        for (i = 0; i < n; ++i) {
          node = nodes[i];
          if (node.fx == null) node.x += node.vx *= velocityDecay;
          else node.x = node.fx, node.vx = 0;
          if (node.fy == null) node.y += node.vy *= velocityDecay;
          else node.y = node.fy, node.vy = 0;
        }
      }
      return simulation;
    }
    function initializeNodes() {
      for (var i = 0, n = nodes.length, node; i < n; ++i) {
        node = nodes[i], node.index = i;
        if (node.fx != null) node.x = node.fx;
        if (node.fy != null) node.y = node.fy;
        if (isNaN(node.x) || isNaN(node.y)) {
          var radius = initialRadius * Math.sqrt(0.5 + i), angle = i * initialAngle;
          node.x = radius * Math.cos(angle);
          node.y = radius * Math.sin(angle);
        }
        if (isNaN(node.vx) || isNaN(node.vy)) {
          node.vx = node.vy = 0;
        }
      }
    }
    function initializeForce(force) {
      if (force.initialize) force.initialize(nodes, random);
      return force;
    }
    initializeNodes();
    return simulation = {
      tick,
      restart: function() {
        return stepper.restart(step), simulation;
      },
      stop: function() {
        return stepper.stop(), simulation;
      },
      nodes: function(_) {
        return arguments.length ? (nodes = _, initializeNodes(), forces.forEach(initializeForce), simulation) : nodes;
      },
      alpha: function(_) {
        return arguments.length ? (alpha = +_, simulation) : alpha;
      },
      alphaMin: function(_) {
        return arguments.length ? (alphaMin = +_, simulation) : alphaMin;
      },
      alphaDecay: function(_) {
        return arguments.length ? (alphaDecay = +_, simulation) : +alphaDecay;
      },
      alphaTarget: function(_) {
        return arguments.length ? (alphaTarget = +_, simulation) : alphaTarget;
      },
      velocityDecay: function(_) {
        return arguments.length ? (velocityDecay = 1 - _, simulation) : 1 - velocityDecay;
      },
      randomSource: function(_) {
        return arguments.length ? (random = _, forces.forEach(initializeForce), simulation) : random;
      },
      force: function(name, _) {
        return arguments.length > 1 ? (_ == null ? forces.delete(name) : forces.set(name, initializeForce(_)), simulation) : forces.get(name);
      },
      find: function(x5, y5, radius) {
        var i = 0, n = nodes.length, dx, dy, d2, node, closest;
        if (radius == null) radius = Infinity;
        else radius *= radius;
        for (i = 0; i < n; ++i) {
          node = nodes[i];
          dx = x5 - node.x;
          dy = y5 - node.y;
          d2 = dx * dx + dy * dy;
          if (d2 < radius) closest = node, radius = d2;
        }
        return closest;
      },
      on: function(name, _) {
        return arguments.length > 1 ? (event.on(name, _), simulation) : event.on(name);
      }
    };
  }

  // node_modules/d3-force/src/manyBody.js
  function manyBody_default() {
    var nodes, node, random, alpha, strength = constant_default4(-30), strengths, distanceMin2 = 1, distanceMax2 = Infinity, theta2 = 0.81;
    function force(_) {
      var i, n = nodes.length, tree = quadtree(nodes, x4, y4).visitAfter(accumulate);
      for (alpha = _, i = 0; i < n; ++i) node = nodes[i], tree.visit(apply);
    }
    function initialize() {
      if (!nodes) return;
      var i, n = nodes.length, node2;
      strengths = new Array(n);
      for (i = 0; i < n; ++i) node2 = nodes[i], strengths[node2.index] = +strength(node2, i, nodes);
    }
    function accumulate(quad) {
      var strength2 = 0, q, c2, weight = 0, x5, y5, i;
      if (quad.length) {
        for (x5 = y5 = i = 0; i < 4; ++i) {
          if ((q = quad[i]) && (c2 = Math.abs(q.value))) {
            strength2 += q.value, weight += c2, x5 += c2 * q.x, y5 += c2 * q.y;
          }
        }
        quad.x = x5 / weight;
        quad.y = y5 / weight;
      } else {
        q = quad;
        q.x = q.data.x;
        q.y = q.data.y;
        do
          strength2 += strengths[q.data.index];
        while (q = q.next);
      }
      quad.value = strength2;
    }
    function apply(quad, x1, _, x22) {
      if (!quad.value) return true;
      var x5 = quad.x - node.x, y5 = quad.y - node.y, w = x22 - x1, l = x5 * x5 + y5 * y5;
      if (w * w / theta2 < l) {
        if (l < distanceMax2) {
          if (x5 === 0) x5 = jiggle_default(random), l += x5 * x5;
          if (y5 === 0) y5 = jiggle_default(random), l += y5 * y5;
          if (l < distanceMin2) l = Math.sqrt(distanceMin2 * l);
          node.vx += x5 * quad.value * alpha / l;
          node.vy += y5 * quad.value * alpha / l;
        }
        return true;
      } else if (quad.length || l >= distanceMax2) return;
      if (quad.data !== node || quad.next) {
        if (x5 === 0) x5 = jiggle_default(random), l += x5 * x5;
        if (y5 === 0) y5 = jiggle_default(random), l += y5 * y5;
        if (l < distanceMin2) l = Math.sqrt(distanceMin2 * l);
      }
      do
        if (quad.data !== node) {
          w = strengths[quad.data.index] * alpha / l;
          node.vx += x5 * w;
          node.vy += y5 * w;
        }
      while (quad = quad.next);
    }
    force.initialize = function(_nodes, _random) {
      nodes = _nodes;
      random = _random;
      initialize();
    };
    force.strength = function(_) {
      return arguments.length ? (strength = typeof _ === "function" ? _ : constant_default4(+_), initialize(), force) : strength;
    };
    force.distanceMin = function(_) {
      return arguments.length ? (distanceMin2 = _ * _, force) : Math.sqrt(distanceMin2);
    };
    force.distanceMax = function(_) {
      return arguments.length ? (distanceMax2 = _ * _, force) : Math.sqrt(distanceMax2);
    };
    force.theta = function(_) {
      return arguments.length ? (theta2 = _ * _, force) : Math.sqrt(theta2);
    };
    return force;
  }

  // node_modules/d3-force/src/x.js
  function x_default2(x5) {
    var strength = constant_default4(0.1), nodes, strengths, xz;
    if (typeof x5 !== "function") x5 = constant_default4(x5 == null ? 0 : +x5);
    function force(alpha) {
      for (var i = 0, n = nodes.length, node; i < n; ++i) {
        node = nodes[i], node.vx += (xz[i] - node.x) * strengths[i] * alpha;
      }
    }
    function initialize() {
      if (!nodes) return;
      var i, n = nodes.length;
      strengths = new Array(n);
      xz = new Array(n);
      for (i = 0; i < n; ++i) {
        strengths[i] = isNaN(xz[i] = +x5(nodes[i], i, nodes)) ? 0 : +strength(nodes[i], i, nodes);
      }
    }
    force.initialize = function(_) {
      nodes = _;
      initialize();
    };
    force.strength = function(_) {
      return arguments.length ? (strength = typeof _ === "function" ? _ : constant_default4(+_), initialize(), force) : strength;
    };
    force.x = function(_) {
      return arguments.length ? (x5 = typeof _ === "function" ? _ : constant_default4(+_), initialize(), force) : x5;
    };
    return force;
  }

  // node_modules/d3-force/src/y.js
  function y_default2(y5) {
    var strength = constant_default4(0.1), nodes, strengths, yz;
    if (typeof y5 !== "function") y5 = constant_default4(y5 == null ? 0 : +y5);
    function force(alpha) {
      for (var i = 0, n = nodes.length, node; i < n; ++i) {
        node = nodes[i], node.vy += (yz[i] - node.y) * strengths[i] * alpha;
      }
    }
    function initialize() {
      if (!nodes) return;
      var i, n = nodes.length;
      strengths = new Array(n);
      yz = new Array(n);
      for (i = 0; i < n; ++i) {
        strengths[i] = isNaN(yz[i] = +y5(nodes[i], i, nodes)) ? 0 : +strength(nodes[i], i, nodes);
      }
    }
    force.initialize = function(_) {
      nodes = _;
      initialize();
    };
    force.strength = function(_) {
      return arguments.length ? (strength = typeof _ === "function" ? _ : constant_default4(+_), initialize(), force) : strength;
    };
    force.y = function(_) {
      return arguments.length ? (y5 = typeof _ === "function" ? _ : constant_default4(+_), initialize(), force) : y5;
    };
    return force;
  }

  // src/ui/charts/ForceGraph.tsx
  var import_react77 = __toESM(require_react(), 1);
  var import_jsx_runtime24 = __toESM(require_jsx_runtime(), 1);
  var HEIGHT = 400;
  var LABELED_PEERS = 10;
  function ForceGraph({
    data,
    theme,
    width
  }) {
    const [hover, setHover] = (0, import_react77.useState)(null);
    const layout = (0, import_react77.useMemo)(() => {
      const maxVolume = Math.max(...data.nodes.filter((n) => !n.isYou).map((n) => n.volume), 1);
      const radiusOf = (n) => n.isYou ? 22 : 7 + 15 * Math.sqrt(n.volume / maxVolume);
      const nodes = data.nodes.map((n) => ({ ...n, x: 0, y: 0 }));
      const links = data.links.map((l) => ({ ...l }));
      const you = nodes.find((n) => n.isYou);
      if (you) {
        you.fx = width / 2;
        you.fy = HEIGHT / 2;
      }
      const simulation = simulation_default(nodes).force(
        "link",
        link_default(links).id((d) => d.id).distance((l) => l.kind === "voice" ? 140 : 190 - 80 * (l.weight / (maxVolume * 2))).strength((l) => l.kind === "voice" ? 0.05 : 0.25)
      ).force("charge", manyBody_default().strength(-320)).force("x", x_default2(width / 2).strength(0.04)).force("y", y_default2(HEIGHT / 2).strength(0.06)).force("collide", collide_default((n) => radiusOf(n) + 18)).stop();
      for (let i = 0; i < 250; i++) simulation.tick();
      for (const node of nodes) {
        const r2 = radiusOf(node);
        node.x = Math.max(r2 + 4, Math.min(width - r2 - 4, node.x));
        node.y = Math.max(r2 + 4, Math.min(HEIGHT - r2 - 4, node.y));
      }
      const labeled = new Set(
        [...nodes].filter((n) => !n.isYou).sort((a2, b) => b.volume - a2.volume).slice(0, LABELED_PEERS).map((n) => n.id)
      );
      return { nodes, links, radiusOf, labeled };
    }, [data, width]);
    const adjacent = (0, import_react77.useMemo)(() => {
      if (!hover) return null;
      const ids = /* @__PURE__ */ new Set([hover]);
      for (const l of layout.links) {
        if (l.source.id === hover) ids.add(l.target.id);
        if (l.target.id === hover) ids.add(l.source.id);
      }
      return ids;
    }, [hover, layout]);
    const dimmed = (id) => adjacent !== null && !adjacent.has(id);
    const linkDimmed = (l) => adjacent !== null && !(adjacent.has(l.source.id) && adjacent.has(l.target.id));
    const hovered = hover ? layout.nodes.find((n) => n.id === hover) : null;
    return /* @__PURE__ */ (0, import_jsx_runtime24.jsxs)("div", { style: { position: "relative" }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime24.jsxs)("svg", { width, height: HEIGHT, role: "img", "aria-label": "Your social graph", children: [
        layout.links.map((l, i) => /* @__PURE__ */ (0, import_jsx_runtime24.jsx)(
          "line",
          {
            x1: l.source.x,
            y1: l.source.y,
            x2: l.target.x,
            y2: l.target.y,
            stroke: l.kind === "voice" ? theme.series[4] : theme.otherColor,
            strokeWidth: Math.max(1, Math.min(l.kind === "voice" ? 4 : 5, Math.sqrt(l.weight) / 4)),
            strokeOpacity: linkDimmed(l) ? 0.08 : l.kind === "voice" ? 0.7 : 0.35
          },
          i
        )),
        layout.nodes.map((n) => /* @__PURE__ */ (0, import_jsx_runtime24.jsxs)("g", { opacity: dimmed(n.id) ? 0.3 : 1, children: [
          /* @__PURE__ */ (0, import_jsx_runtime24.jsx)(
            "circle",
            {
              cx: n.x,
              cy: n.y,
              r: layout.radiusOf(n),
              fill: n.isYou ? theme.ink.primary : theme.series[0],
              stroke: theme.ink.card,
              strokeWidth: 2,
              onMouseEnter: () => setHover(n.id),
              onMouseLeave: () => setHover(null),
              children: /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("title", { children: n.isYou ? "you" : `${n.label} — ${formatCount(n.volume)} messages` })
            }
          ),
          n.isYou || layout.labeled.has(n.id) || hover === n.id ? /* @__PURE__ */ (0, import_jsx_runtime24.jsx)(
            "text",
            {
              x: n.x,
              y: n.y + layout.radiusOf(n) + 12,
              textAnchor: "middle",
              className: "retraced-svg-label",
              fill: n.isYou || hover === n.id ? theme.ink.primary : theme.ink.muted,
              fontWeight: n.isYou ? 600 : 400,
              children: n.label
            }
          ) : null
        ] }, n.id))
      ] }),
      hovered && !hovered.isYou ? /* @__PURE__ */ (0, import_jsx_runtime24.jsxs)(
        "div",
        {
          className: "retraced-tooltip retraced-tooltip--floating",
          style: { left: hovered.x, top: hovered.y - layout.radiusOf(hovered) - 6 },
          children: [
            /* @__PURE__ */ (0, import_jsx_runtime24.jsx)("div", { className: "retraced-tooltip-title", children: hovered.label }),
            /* @__PURE__ */ (0, import_jsx_runtime24.jsxs)("div", { children: [
              formatCount(hovered.volume),
              " messages, both directions"
            ] })
          ]
        }
      ) : null
    ] });
  }

  // src/ui/charts/LatencyDistributionChart.tsx
  var import_jsx_runtime25 = __toESM(require_jsx_runtime(), 1);
  function LatencyDistributionChart({
    data,
    youColor,
    themColor,
    theme,
    width
  }) {
    return /* @__PURE__ */ (0, import_jsx_runtime25.jsxs)(BarChart, { width, height: 220, data, margin: { top: 8, right: 8, bottom: 0, left: 0 }, barGap: 2, children: [
      /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(CartesianGrid, { vertical: false, stroke: theme.ink.grid }),
      /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(XAxis, { dataKey: "label", tick: { fill: theme.ink.muted, fontSize: 11 }, axisLine: { stroke: theme.ink.axis }, tickLine: false, interval: 0 }),
      /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(YAxis, { width: 36, tickCount: 4, allowDecimals: false, tick: { fill: theme.ink.muted, fontSize: 11 }, axisLine: false, tickLine: false }),
      /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
        Tooltip,
        {
          cursor: { fill: theme.ink.grid },
          content: ({ active, payload }) => {
            if (!active || !payload?.length) return null;
            const row = payload[0].payload;
            return /* @__PURE__ */ (0, import_jsx_runtime25.jsxs)("div", { className: "retraced-tooltip", children: [
              /* @__PURE__ */ (0, import_jsx_runtime25.jsx)("div", { className: "retraced-tooltip-title", children: row.label }),
              /* @__PURE__ */ (0, import_jsx_runtime25.jsxs)("div", { className: "retraced-tooltip-row", children: [
                /* @__PURE__ */ (0, import_jsx_runtime25.jsx)("span", { className: "retraced-legend-dot", style: { background: youColor } }),
                /* @__PURE__ */ (0, import_jsx_runtime25.jsx)("span", { children: "your replies" }),
                /* @__PURE__ */ (0, import_jsx_runtime25.jsx)("span", { className: "retraced-tooltip-value", children: formatCount(row.mine) })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime25.jsxs)("div", { className: "retraced-tooltip-row", children: [
                /* @__PURE__ */ (0, import_jsx_runtime25.jsx)("span", { className: "retraced-legend-dot", style: { background: themColor } }),
                /* @__PURE__ */ (0, import_jsx_runtime25.jsx)("span", { children: "their replies" }),
                /* @__PURE__ */ (0, import_jsx_runtime25.jsx)("span", { className: "retraced-tooltip-value", children: formatCount(row.theirs) })
              ] })
            ] });
          }
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(Bar, { dataKey: "mine", fill: youColor, radius: [4, 4, 0, 0], maxBarSize: 24, isAnimationActive: false }),
      /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(Bar, { dataKey: "theirs", fill: themColor, radius: [4, 4, 0, 0], maxBarSize: 24, isAnimationActive: false })
    ] });
  }

  // src/ui/data/people.ts
  var REPLY_BUCKET_LABELS = ["<10s", "10–30s", "30s–2m", "2–10m", "10m–1h", "1–6h", "6h+"];
  var labelOf = (p) => p.label ?? p.userId;
  var sum2 = (ns) => ns.reduce((a2, b) => a2 + b, 0);
  var MIN_CONVERSATIONS = 3;
  function initiatorRatio(peers, opts = {}) {
    const min3 = opts.min ?? MIN_CONVERSATIONS;
    const rows = [];
    for (const p of peers) {
      const total2 = p.initiatedByMe + p.initiatedByThem;
      if (total2 < min3) continue;
      rows.push({
        userId: p.userId,
        label: labelOf(p),
        mine: p.initiatedByMe,
        theirs: p.initiatedByThem,
        theirSharePct: Math.round(100 * p.initiatedByThem / total2)
      });
    }
    const total = (r2) => r2.mine + r2.theirs;
    const sortBy3 = opts.sortBy ?? "total";
    rows.sort(
      sortBy3 === "them" ? (a2, b) => b.theirSharePct - a2.theirSharePct || total(b) - total(a2) : sortBy3 === "me" ? (a2, b) => a2.theirSharePct - b.theirSharePct || total(b) - total(a2) : (a2, b) => total(b) - total(a2)
    );
    return rows.slice(0, opts.max ?? 12);
  }
  var MIN_LATENCIES = 5;
  function replyMedians(peers, opts = {}) {
    const min3 = opts.min ?? MIN_LATENCIES;
    const rows = [];
    for (const p of peers) {
      const mineCount = sum2(p.latencyBucketsMine);
      const theirsCount = sum2(p.latencyBucketsTheirs);
      if (mineCount < min3 && theirsCount < min3) continue;
      rows.push({
        userId: p.userId,
        label: labelOf(p),
        mineBucket: mineCount > 0 ? medianBucket(p.latencyBucketsMine) : null,
        theirsBucket: theirsCount > 0 ? medianBucket(p.latencyBucketsTheirs) : null,
        mineCount,
        theirsCount,
        volume: p.msgToThem + p.msgFromThem
      });
    }
    rows.sort((a2, b) => b.volume - a2.volume);
    return rows.slice(0, opts.max ?? 15).map(({ volume: _volume, ...row }) => row);
  }
  function latencyDistribution(peer) {
    return REPLY_BUCKET_LABELS.map((label, i) => ({
      label,
      mine: peer.latencyBucketsMine[i] ?? 0,
      theirs: peer.latencyBucketsTheirs[i] ?? 0
    }));
  }
  function socialGraph(peers, voice, opts = {}) {
    const threshold2 = opts.threshold ?? 10;
    const maxNodes = opts.maxNodes ?? 40;
    const kept = peers.map((p) => ({ p, volume: p.msgToThem + p.msgFromThem })).filter(({ volume }) => volume >= threshold2).sort((a2, b) => b.volume - a2.volume).slice(0, maxNodes);
    const nodes = [{ id: "you", label: "you", volume: kept.reduce((n, k) => n + k.volume, 0), isYou: true }];
    const links = [];
    const included = /* @__PURE__ */ new Set();
    for (const { p, volume } of kept) {
      nodes.push({ id: p.userId, label: labelOf(p), volume, isYou: false });
      links.push({ source: "you", target: p.userId, weight: volume, kind: "dm" });
      included.add(p.userId);
    }
    const pairSeconds = /* @__PURE__ */ new Map();
    for (const row of voice) {
      const present = Object.entries(row.coPresent).filter(([userId]) => included.has(userId));
      for (let i = 0; i < present.length; i++) {
        for (let j = i + 1; j < present.length; j++) {
          const [a2, aSec] = present[i];
          const [b, bSec] = present[j];
          const key = a2 < b ? `${a2} ${b}` : `${b} ${a2}`;
          pairSeconds.set(key, (pairSeconds.get(key) ?? 0) + Math.min(aSec, bSec));
        }
      }
    }
    for (const [key, weight] of pairSeconds) {
      const sep = key.indexOf(" ");
      links.push({ source: key.slice(0, sep), target: key.slice(sep + 1), weight, kind: "voice" });
    }
    return { nodes, links };
  }
  var MIN_REACTIONS = 3;
  function reactionBalance(peers, opts = {}) {
    const min3 = opts.min ?? MIN_REACTIONS;
    return peers.filter((p) => p.reactionsToThem + p.reactionsFromThem >= min3).sort((a2, b) => b.reactionsToThem + b.reactionsFromThem - (a2.reactionsToThem + a2.reactionsFromThem)).slice(0, opts.max ?? 12).map((p) => ({ userId: p.userId, label: labelOf(p), given: p.reactionsToThem, received: p.reactionsFromThem }));
  }

  // src/ui/charts/ReplyMedianChart.tsx
  var import_jsx_runtime26 = __toESM(require_jsx_runtime(), 1);
  var AXIS_TICK_LABELS = ["0", "10s", "30s", "2m", "10m", "1h", "6h", ""];
  function ReplyMedianChart({
    data,
    youColor,
    themColor,
    theme,
    width
  }) {
    const rows = data.map((r2) => ({
      ...r2,
      minePos: r2.mineBucket === null ? null : r2.mineBucket + 0.5,
      theirsPos: r2.theirsBucket === null ? null : r2.theirsBucket + 0.5
    }));
    const height = 40 + rows.length * 42;
    return /* @__PURE__ */ (0, import_jsx_runtime26.jsxs)(BarChart, { layout: "vertical", width, height, data: rows, margin: { top: 4, right: 16, bottom: 0, left: 8 }, barGap: 2, children: [
      /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(
        XAxis,
        {
          type: "number",
          domain: [0, REPLY_BUCKET_LABELS.length],
          ticks: [0, 1, 2, 3, 4, 5, 6, 7],
          tickFormatter: (v) => AXIS_TICK_LABELS[v] ?? "",
          tick: { fill: theme.ink.muted, fontSize: 11 },
          axisLine: { stroke: theme.ink.axis },
          tickLine: false
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(YAxis, { type: "category", dataKey: "label", width: 150, tick: { fill: theme.ink.muted, fontSize: 11 }, axisLine: false, tickLine: false }),
      /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(
        Tooltip,
        {
          cursor: { fill: theme.ink.grid },
          content: ({ active, payload }) => {
            if (!active || !payload?.length) return null;
            const row = payload[0].payload;
            return /* @__PURE__ */ (0, import_jsx_runtime26.jsxs)("div", { className: "retraced-tooltip", children: [
              /* @__PURE__ */ (0, import_jsx_runtime26.jsx)("div", { className: "retraced-tooltip-title", children: row.label }),
              /* @__PURE__ */ (0, import_jsx_runtime26.jsxs)("div", { className: "retraced-tooltip-row", children: [
                /* @__PURE__ */ (0, import_jsx_runtime26.jsx)("span", { className: "retraced-legend-dot", style: { background: youColor } }),
                /* @__PURE__ */ (0, import_jsx_runtime26.jsx)("span", { children: "you reply in" }),
                /* @__PURE__ */ (0, import_jsx_runtime26.jsx)("span", { className: "retraced-tooltip-value", children: row.mineBucket === null ? "—" : REPLY_BUCKET_LABELS[row.mineBucket] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime26.jsxs)("div", { className: "retraced-tooltip-row", children: [
                /* @__PURE__ */ (0, import_jsx_runtime26.jsx)("span", { className: "retraced-legend-dot", style: { background: themColor } }),
                /* @__PURE__ */ (0, import_jsx_runtime26.jsx)("span", { children: "they reply in" }),
                /* @__PURE__ */ (0, import_jsx_runtime26.jsx)("span", { className: "retraced-tooltip-value", children: row.theirsBucket === null ? "—" : REPLY_BUCKET_LABELS[row.theirsBucket] })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime26.jsxs)("div", { className: "retraced-note", children: [
                "medians over ",
                formatCount(row.mineCount),
                " / ",
                formatCount(row.theirsCount),
                " replies"
              ] })
            ] });
          }
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(Bar, { dataKey: "minePos", fill: youColor, radius: [0, 4, 4, 0], maxBarSize: 12, isAnimationActive: false }),
      /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(Bar, { dataKey: "theirsPos", fill: themColor, radius: [0, 4, 4, 0], maxBarSize: 12, isAnimationActive: false })
    ] });
  }

  // src/ui/page/PeopleTab.tsx
  var import_jsx_runtime27 = __toESM(require_jsx_runtime(), 1);
  function PeopleTab({ engine }) {
    const theme = (0, import_react78.useMemo)(readChartTheme, []);
    const data = useRollups(engine);
    const [width, measureRef] = useElementWidth();
    const chartWidth = Math.max(320, width - 32);
    const [initiatorSort, setInitiatorSort] = (0, import_react78.useState)("total");
    const [replySort, setReplySort] = (0, import_react78.useState)("them");
    const [selectedPeer, setSelectedPeer] = (0, import_react78.useState)(null);
    const [threshold2, setThreshold] = (0, import_react78.useState)(10);
    const youColor = theme.series[0];
    const themColor = theme.series[4];
    const initiators = (0, import_react78.useMemo)(() => initiatorRatio(data.peers, { sortBy: initiatorSort }), [data.peers, initiatorSort]);
    const medians = (0, import_react78.useMemo)(() => {
      const rows = replyMedians(data.peers);
      const key = (b) => b === null ? -1 : b;
      rows.sort(
        replySort === "them" ? (a2, b) => key(b.theirsBucket) - key(a2.theirsBucket) : (a2, b) => key(b.mineBucket) - key(a2.mineBucket)
      );
      return rows;
    }, [data.peers, replySort]);
    const latencyPeers = (0, import_react78.useMemo)(
      () => data.peers.filter((p) => p.latencyBucketsMine.some((n) => n > 0) || p.latencyBucketsTheirs.some((n) => n > 0)).sort((a2, b) => b.msgToThem + b.msgFromThem - (a2.msgToThem + a2.msgFromThem)).slice(0, 25),
      [data.peers]
    );
    const selected = latencyPeers.find((p) => p.userId === selectedPeer) ?? latencyPeers[0] ?? null;
    const distribution = (0, import_react78.useMemo)(() => selected ? latencyDistribution(selected) : null, [selected]);
    const maxVolume = (0, import_react78.useMemo)(() => Math.max(...data.peers.map((p) => p.msgToThem + p.msgFromThem), 10), [data.peers]);
    const graph = (0, import_react78.useMemo)(() => socialGraph(data.peers, data.voice, { threshold: threshold2 }), [data.peers, data.voice, threshold2]);
    const reactions = (0, import_react78.useMemo)(() => reactionBalance(data.peers), [data.peers]);
    if (!data.loaded) return /* @__PURE__ */ (0, import_jsx_runtime27.jsx)("p", { className: "retraced-note", children: "loading…" });
    const pairLegend = [
      { label: "You", color: youColor },
      { label: "Them", color: themColor }
    ];
    return /* @__PURE__ */ (0, import_jsx_runtime27.jsxs)("div", { ref: measureRef, className: "retraced-overview", children: [
      data.dbMissing ? /* @__PURE__ */ (0, import_jsx_runtime27.jsx)("p", { className: "retraced-banner", children: "Capture is unavailable, so charts have nothing to read — see the Overview tab for details." }) : null,
      /* @__PURE__ */ (0, import_jsx_runtime27.jsx)("p", { className: "retraced-note", children: "The People tab reads all-time per-person rollups, so the time-range filter doesn't apply here." }),
      /* @__PURE__ */ (0, import_jsx_runtime27.jsx)(
        ChartCard,
        {
          title: "Who reaches out first",
          subtitle: "conversation openers in each DM · all time",
          legend: pairLegend,
          actions: /* @__PURE__ */ (0, import_jsx_runtime27.jsxs)("div", { className: "retraced-ranges", role: "group", "aria-label": "Sort people", children: [
            /* @__PURE__ */ (0, import_jsx_runtime27.jsx)("button", { type: "button", className: "retraced-range-pill", "aria-pressed": initiatorSort === "total", onClick: () => setInitiatorSort("total"), children: "most talked" }),
            /* @__PURE__ */ (0, import_jsx_runtime27.jsx)("button", { type: "button", className: "retraced-range-pill", "aria-pressed": initiatorSort === "them", onClick: () => setInitiatorSort("them"), children: "they start" }),
            /* @__PURE__ */ (0, import_jsx_runtime27.jsx)("button", { type: "button", className: "retraced-range-pill", "aria-pressed": initiatorSort === "me", onClick: () => setInitiatorSort("me"), children: "you start" })
          ] }),
          empty: initiators.length === 0 ? "Once a few DM conversations have opened, who opened them shows up here." : null,
          table: {
            columns: ["Person", "You opened", "They opened", "Their share"],
            rows: initiators.map((r2) => [r2.label, r2.mine, r2.theirs, `${r2.theirSharePct}%`])
          },
          children: /* @__PURE__ */ (0, import_jsx_runtime27.jsx)(
            DivergingPairChart,
            {
              data: initiators.map((r2) => ({ label: r2.label, left: r2.mine, right: r2.theirs, note: `they open ${r2.theirSharePct}% of conversations` })),
              leftName: "you opened",
              rightName: "they opened",
              leftColor: youColor,
              rightColor: themColor,
              theme,
              width: chartWidth
            }
          )
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime27.jsx)(
        ChartCard,
        {
          title: "Reply speed",
          subtitle: "median time to reply, per person · all time",
          legend: pairLegend,
          actions: /* @__PURE__ */ (0, import_jsx_runtime27.jsxs)("div", { className: "retraced-ranges", role: "group", "aria-label": "Sort by pace", children: [
            /* @__PURE__ */ (0, import_jsx_runtime27.jsx)("button", { type: "button", className: "retraced-range-pill", "aria-pressed": replySort === "them", onClick: () => setReplySort("them"), children: "their pace" }),
            /* @__PURE__ */ (0, import_jsx_runtime27.jsx)("button", { type: "button", className: "retraced-range-pill", "aria-pressed": replySort === "me", onClick: () => setReplySort("me"), children: "your pace" })
          ] }),
          empty: medians.length === 0 ? "Reply speeds appear after a handful of back-and-forth exchanges with someone." : null,
          table: {
            columns: ["Person", "Your median", "Their median", "Your replies", "Their replies"],
            rows: medians.map((r2) => [
              r2.label,
              r2.mineBucket === null ? "—" : REPLY_BUCKET_LABELS[r2.mineBucket],
              r2.theirsBucket === null ? "—" : REPLY_BUCKET_LABELS[r2.theirsBucket],
              r2.mineCount,
              r2.theirsCount
            ])
          },
          children: /* @__PURE__ */ (0, import_jsx_runtime27.jsx)(ReplyMedianChart, { data: medians, youColor, themColor, theme, width: chartWidth })
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime27.jsx)(
        ChartCard,
        {
          title: `Reply latency with ${selected ? selected.label ?? selected.userId : "…"}`,
          subtitle: "how reply gaps distribute, both directions · all time",
          legend: pairLegend,
          actions: latencyPeers.length > 1 ? /* @__PURE__ */ (0, import_jsx_runtime27.jsx)(
            "select",
            {
              className: "retraced-select",
              "aria-label": "Choose a person",
              value: selected?.userId ?? "",
              onChange: (e) => setSelectedPeer(e.target.value),
              children: latencyPeers.map((p) => /* @__PURE__ */ (0, import_jsx_runtime27.jsx)("option", { value: p.userId, children: p.label ?? p.userId }, p.userId))
            }
          ) : null,
          empty: !distribution ? "Pick up a conversation and the reply-gap distribution appears here." : null,
          table: distribution ? {
            columns: ["Gap", "Your replies", "Their replies"],
            rows: distribution.map((r2) => [r2.label, r2.mine, r2.theirs])
          } : null,
          children: distribution ? /* @__PURE__ */ (0, import_jsx_runtime27.jsx)(LatencyDistributionChart, { data: distribution, youColor, themColor, theme, width: chartWidth }) : null
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime27.jsx)(
        ChartCard,
        {
          title: "Your circle",
          subtitle: "people sized by messages · teal links share voice time with you · all time",
          actions: /* @__PURE__ */ (0, import_jsx_runtime27.jsxs)("label", { className: "retraced-slider", children: [
            /* @__PURE__ */ (0, import_jsx_runtime27.jsxs)("span", { className: "retraced-note", children: [
              "at least ",
              threshold2,
              " messages"
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime27.jsx)(
              "input",
              {
                type: "range",
                min: 1,
                max: Math.max(20, Math.min(200, maxVolume)),
                value: threshold2,
                onChange: (e) => setThreshold(Number(e.target.value))
              }
            )
          ] }),
          empty: graph.nodes.length <= 1 ? "The graph appears once a few people cross the message threshold — try lowering it." : null,
          table: {
            columns: ["Person", "Messages"],
            rows: graph.nodes.filter((n) => !n.isYou).map((n) => [n.label, formatCount(n.volume)])
          },
          children: /* @__PURE__ */ (0, import_jsx_runtime27.jsx)(ForceGraph, { data: graph, theme, width: chartWidth })
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime27.jsx)(
        ChartCard,
        {
          title: "Reactions, given and received",
          subtitle: "emoji reactions between you and each person · all time",
          legend: [
            { label: "You gave", color: youColor },
            { label: "They gave", color: themColor }
          ],
          empty: reactions.length === 0 ? "React to someone (or earn a reaction) and the exchange shows up here." : null,
          table: {
            columns: ["Person", "You gave", "They gave"],
            rows: reactions.map((r2) => [r2.label, r2.given, r2.received])
          },
          children: /* @__PURE__ */ (0, import_jsx_runtime27.jsx)(
            DivergingPairChart,
            {
              data: reactions.map((r2) => ({ label: r2.label, left: r2.given, right: r2.received })),
              leftName: "you gave",
              rightName: "they gave",
              leftColor: youColor,
              rightColor: themColor,
              theme,
              width: chartWidth
            }
          )
        }
      )
    ] });
  }

  // src/ui/page/RhythmTab.tsx
  var import_react81 = __toESM(require_react(), 1);

  // src/ui/charts/DayHourHeatmap.tsx
  var import_react79 = __toESM(require_react(), 1);
  var import_jsx_runtime28 = __toESM(require_jsx_runtime(), 1);
  var DAYS = ["Mon", "Tue", "Wed", "Thu", "Fri", "Sat", "Sun"];
  var GAP2 = 2;
  function DayHourHeatmap({ data, theme, width }) {
    const [hover, setHover] = (0, import_react79.useState)(null);
    const left2 = 36;
    const top = 4;
    const bottom = 18;
    const step = Math.max(10, Math.min(32, Math.floor((width - left2) / 24)));
    const cell = step - GAP2;
    const svgWidth = left2 + 24 * step;
    const svgHeight = top + 7 * step + bottom;
    const nonzero = data.grid.flat().filter((v) => v > 0).sort((a2, b) => a2 - b);
    const q = (f) => nonzero[Math.floor(f * (nonzero.length - 1))] ?? 0;
    const [q1, q2, q3] = [q(0.25), q(0.5), q(0.75)];
    const fillFor = (v) => {
      if (v <= 0) return theme.calendarZero;
      const level = v <= q1 ? 0 : v <= q2 ? 1 : v <= q3 ? 2 : 3;
      return theme.calendarBins[level];
    };
    return /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("div", { style: { position: "relative", overflowX: "auto" }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("svg", { width: svgWidth, height: svgHeight, role: "img", "aria-label": "Messages by weekday and hour", children: [
        DAYS.map((day, row) => /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("text", { x: 0, y: top + row * step + cell / 2 + 3.5, className: "retraced-svg-label", fill: theme.ink.muted, children: day }, day)),
        [0, 6, 12, 18].map((h) => /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("text", { x: left2 + h * step, y: top + 7 * step + 12, className: "retraced-svg-label", fill: theme.ink.muted, children: String(h).padStart(2, "0") }, h)),
        data.grid.map(
          (rowValues, row) => rowValues.map((count, h) => /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(
            "rect",
            {
              x: left2 + h * step,
              y: top + row * step,
              width: cell,
              height: cell,
              rx: 2.5,
              fill: fillFor(count),
              onMouseEnter: (e) => {
                const host = e.currentTarget.ownerSVGElement?.parentElement ?? null;
                const hostRect = host?.getBoundingClientRect();
                const rect = e.currentTarget.getBoundingClientRect();
                setHover({
                  x: rect.left - (hostRect?.left ?? 0) + cell / 2,
                  y: rect.top - (hostRect?.top ?? 0) - 8,
                  label: `${DAYS[row]} ${String(h).padStart(2, "0")}:00–${String((h + 1) % 24).padStart(2, "0")}:00`,
                  count
                });
              },
              onMouseLeave: () => setHover(null),
              children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("title", { children: `${DAYS[row]} ${String(h).padStart(2, "0")}:00 — ${count} messages` })
            },
            `${row}-${h}`
          ))
        )
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("div", { className: "retraced-legend retraced-calendar-legend", children: [
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("span", { className: "retraced-note", children: "less" }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("svg", { width: 5 * (cell + GAP2), height: cell, role: "presentation", children: [theme.calendarZero, ...theme.calendarBins].map((fill, i) => /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("rect", { x: i * (cell + GAP2), width: cell, height: cell, rx: 2.5, fill }, i)) }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("span", { className: "retraced-note", children: "more" })
      ] }),
      hover ? /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("div", { className: "retraced-tooltip retraced-tooltip--floating", style: { left: hover.x, top: hover.y }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("div", { className: "retraced-tooltip-title", children: hover.label }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("div", { children: hover.count === 0 ? "no messages" : `${formatCount(hover.count)} messages` })
      ] }) : null
    ] });
  }

  // src/ui/charts/NightOwlLine.tsx
  var import_jsx_runtime29 = __toESM(require_jsx_runtime(), 1);
  function NightOwlLine({ data, theme, width }) {
    const months = data.map((p) => p.month);
    const ticks = months.length > 13 ? months.filter((_, i) => i % 2 === 0) : months;
    const spansYears = months.length > 0 && months[0].slice(0, 4) !== months[months.length - 1].slice(0, 4);
    return /* @__PURE__ */ (0, import_jsx_runtime29.jsxs)(LineChart, { width, height: 220, data, margin: { top: 8, right: 8, bottom: 0, left: 0 }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime29.jsx)(CartesianGrid, { vertical: false, stroke: theme.ink.grid }),
      /* @__PURE__ */ (0, import_jsx_runtime29.jsx)(
        XAxis,
        {
          dataKey: "month",
          ticks,
          tickFormatter: (m2) => tickLabel(`${m2}-01`, spansYears),
          tick: { fill: theme.ink.muted, fontSize: 11 },
          axisLine: { stroke: theme.ink.axis },
          tickLine: false,
          interval: "preserveStartEnd"
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime29.jsx)(
        YAxis,
        {
          width: 40,
          tickCount: 4,
          allowDecimals: false,
          tickFormatter: (v) => `${v}%`,
          tick: { fill: theme.ink.muted, fontSize: 11 },
          axisLine: false,
          tickLine: false
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime29.jsx)(
        Tooltip,
        {
          cursor: { stroke: theme.ink.axis },
          content: ({ active, payload, label }) => {
            if (!active || !payload?.length) return null;
            const point6 = payload[0].payload;
            if (point6.pct === null) return null;
            return /* @__PURE__ */ (0, import_jsx_runtime29.jsxs)("div", { className: "retraced-tooltip", children: [
              /* @__PURE__ */ (0, import_jsx_runtime29.jsx)("div", { className: "retraced-tooltip-title", children: label }),
              /* @__PURE__ */ (0, import_jsx_runtime29.jsxs)("div", { children: [
                point6.pct,
                "% at night"
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime29.jsxs)("div", { className: "retraced-note", children: [
                formatCount(point6.total),
                " messages that month"
              ] })
            ] });
          }
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime29.jsx)(Line, { type: "monotone", dataKey: "pct", stroke: theme.series[0], strokeWidth: 2, dot: false, connectNulls: false, isAnimationActive: false, activeDot: { r: 3 } })
    ] });
  }

  // src/ui/charts/RadialClock.tsx
  var import_react80 = __toESM(require_react(), 1);
  var import_jsx_runtime30 = __toESM(require_jsx_runtime(), 1);
  var TAU = Math.PI * 2;
  var hh = (h) => `${String((h + 24) % 24).padStart(2, "0")}:00`;
  function RadialClock({ data, theme, width }) {
    const [hover, setHover] = (0, import_react80.useState)(null);
    const height = 320;
    const cx = width / 2;
    const cy = 160;
    const maxR = 118;
    const max4 = Math.max(...data.hours, 1);
    const r2 = (v) => v / max4 * maxR;
    const angle = (h) => h / 24 * TAU;
    const point6 = (h, radius) => [cx + Math.sin(angle(h)) * radius, cy - Math.cos(angle(h)) * radius];
    const shape = lineRadial_default().angle((_, i) => angle(i)).radius((v) => r2(v)).curve(catmullRomClosed_default)(data.hours) ?? "";
    const arcGen = arc_default();
    const nightPath = arcGen({ innerRadius: 0, outerRadius: maxR, startAngle: angle(22), endAngle: angle(30) }) ?? "";
    const wedge = (h) => arcGen({ innerRadius: 0, outerRadius: maxR + 6, startAngle: angle(h - 0.5), endAngle: angle(h + 0.5) }) ?? "";
    const peak = data.peakHour;
    const peakPoint = peak !== null ? point6(peak, r2(data.hours[peak])) : null;
    const peakOnLeft = peak !== null && peak > 12;
    return /* @__PURE__ */ (0, import_jsx_runtime30.jsxs)("div", { style: { position: "relative" }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime30.jsxs)("svg", { width, height, role: "img", "aria-label": "Messages by hour of day", children: [
        /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("path", { d: nightPath, transform: `translate(${cx},${cy})`, fill: theme.calendarZero }),
        [1 / 3, 2 / 3, 1].map((f) => /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("circle", { cx, cy, r: maxR * f, fill: "none", stroke: theme.ink.grid }, f)),
        [0, 6, 12, 18].map((h) => {
          const [x5, y5] = point6(h, maxR);
          return /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("line", { x1: cx, y1: cy, x2: x5, y2: y5, stroke: theme.ink.grid }, h);
        }),
        /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("text", { x: cx + 5, y: cy - maxR + 11, className: "retraced-svg-label", fill: theme.ink.muted, children: formatCompact(max4) }),
        /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("path", { d: shape, transform: `translate(${cx},${cy})`, fill: theme.series[0], fillOpacity: 0.22, stroke: theme.series[0], strokeWidth: 2, strokeLinejoin: "round" }),
        /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("text", { x: cx, y: cy - maxR - 8, textAnchor: "middle", className: "retraced-svg-label", fill: theme.ink.muted, children: "00" }),
        /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("text", { x: cx + maxR + 14, y: cy + 3, textAnchor: "middle", className: "retraced-svg-label", fill: theme.ink.muted, children: "06" }),
        /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("text", { x: cx, y: cy + maxR + 14, textAnchor: "middle", className: "retraced-svg-label", fill: theme.ink.muted, children: "12" }),
        /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("text", { x: cx - maxR - 14, y: cy + 3, textAnchor: "middle", className: "retraced-svg-label", fill: theme.ink.muted, children: "18" }),
        peak !== null && peakPoint ? /* @__PURE__ */ (0, import_jsx_runtime30.jsxs)("g", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("circle", { cx: peakPoint[0], cy: peakPoint[1], r: 4, fill: theme.series[0], stroke: theme.ink.card, strokeWidth: 2 }),
          /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(
            "text",
            {
              x: peakPoint[0] + (peakOnLeft ? -8 : 8),
              y: peakPoint[1] - 6,
              textAnchor: peakOnLeft ? "end" : "start",
              className: "retraced-svg-label",
              fill: theme.ink.primary,
              fontWeight: 600,
              children: `${hh(peak)} · ${formatCompact(data.hours[peak])}`
            }
          )
        ] }) : null,
        data.hours.map((count, h) => /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(
          "path",
          {
            d: wedge(h),
            transform: `translate(${cx},${cy})`,
            fill: hover === h ? theme.ink.grid : "transparent",
            onMouseEnter: () => setHover(h),
            onMouseLeave: () => setHover(null),
            children: /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("title", { children: `${hh(h)}–${hh(h + 1)} — ${formatCount(count)} messages` })
          },
          h
        ))
      ] }),
      hover !== null ? /* @__PURE__ */ (0, import_jsx_runtime30.jsxs)(
        "div",
        {
          className: "retraced-tooltip retraced-tooltip--floating",
          style: { left: point6(hover, maxR * 0.72)[0], top: point6(hover, maxR * 0.72)[1] - 10 },
          children: [
            /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("div", { className: "retraced-tooltip-title", children: `${hh(hover)}–${hh(hover + 1)}` }),
            /* @__PURE__ */ (0, import_jsx_runtime30.jsxs)("div", { children: [
              formatCount(data.hours[hover]),
              " messages"
            ] })
          ]
        }
      ) : null
    ] });
  }

  // src/ui/data/rhythm.ts
  var MAX_MONTHS = 600;
  function hourOfDayProfile(rows) {
    const hours = new Array(24).fill(0);
    let total = 0;
    for (const row of rows) {
      hours[row.hour] += row.sent;
      total += row.sent;
    }
    let peakHour = null;
    if (total > 0) {
      peakHour = 0;
      for (let h = 1; h < 24; h++) if (hours[h] > hours[peakHour]) peakHour = h;
    }
    return { hours, total, peakHour };
  }
  function dayHourGrid(rows) {
    const grid = Array.from({ length: 7 }, () => new Array(24).fill(0));
    let max4 = 0;
    let total = 0;
    for (const row of rows) {
      const weekday = (parseDateKey(row.date).getDay() + 6) % 7;
      const cell = grid[weekday][row.hour] += row.sent;
      if (cell > max4) max4 = cell;
      total += row.sent;
    }
    return { grid, max: max4, total };
  }
  var MIN_MONTH_MESSAGES = 10;
  function nextMonth(month) {
    let year = Number(month.slice(0, 4));
    let m2 = Number(month.slice(5, 7)) + 1;
    if (m2 > 12) {
      m2 = 1;
      year++;
    }
    return `${year}-${String(m2).padStart(2, "0")}`;
  }
  function nightOwlByMonth(rows) {
    const byMonth = /* @__PURE__ */ new Map();
    for (const row of rows) {
      const month = row.date.slice(0, 7);
      const entry = byMonth.get(month) ?? { night: 0, total: 0 };
      entry.total += row.sent;
      if (isNightHour(row.hour)) entry.night += row.sent;
      byMonth.set(month, entry);
    }
    if (byMonth.size === 0) return [];
    const months = [...byMonth.keys()].sort();
    const last = months[months.length - 1];
    const out = [];
    let cursor = months[0];
    for (let i = 0; i < MAX_MONTHS; i++) {
      const entry = byMonth.get(cursor);
      const total = entry?.total ?? 0;
      out.push({
        month: cursor,
        total,
        pct: entry && total >= MIN_MONTH_MESSAGES ? Math.round(100 * entry.night / total) : null
      });
      if (cursor === last) break;
      cursor = nextMonth(cursor);
    }
    return out;
  }
  var SESSION_BUCKET_LABELS = ["<5m", "5–15m", "15–30m", "30m–1h", "1–2h", "2–4h", "4h+"];
  function sessionHistogram(rows) {
    const buckets = new Array(SESSION_BUCKET_COUNT).fill(0);
    let totalMs = 0;
    for (const row of rows) {
      sumInto(buckets, row.buckets);
      totalMs += row.totalMs;
    }
    return {
      buckets,
      total: buckets.reduce((a2, b) => a2 + b, 0),
      medianBucket: medianBucket(buckets),
      totalMs
    };
  }

  // src/ui/charts/SessionLengths.tsx
  var import_jsx_runtime31 = __toESM(require_jsx_runtime(), 1);
  function SessionLengths({ data, theme, width }) {
    const rows = data.buckets.map((count, i) => ({ label: SESSION_BUCKET_LABELS[i], count }));
    return /* @__PURE__ */ (0, import_jsx_runtime31.jsxs)(BarChart, { width, height: 220, data: rows, margin: { top: 8, right: 8, bottom: 0, left: 0 }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime31.jsx)(CartesianGrid, { vertical: false, stroke: theme.ink.grid }),
      /* @__PURE__ */ (0, import_jsx_runtime31.jsx)(XAxis, { dataKey: "label", tick: { fill: theme.ink.muted, fontSize: 11 }, axisLine: { stroke: theme.ink.axis }, tickLine: false, interval: 0 }),
      /* @__PURE__ */ (0, import_jsx_runtime31.jsx)(YAxis, { width: 36, tickCount: 4, allowDecimals: false, tick: { fill: theme.ink.muted, fontSize: 11 }, axisLine: false, tickLine: false }),
      /* @__PURE__ */ (0, import_jsx_runtime31.jsx)(
        Tooltip,
        {
          cursor: { fill: theme.ink.grid },
          content: ({ active, payload }) => {
            if (!active || !payload?.length) return null;
            const row = payload[0].payload;
            return /* @__PURE__ */ (0, import_jsx_runtime31.jsxs)("div", { className: "retraced-tooltip", children: [
              /* @__PURE__ */ (0, import_jsx_runtime31.jsx)("div", { className: "retraced-tooltip-title", children: row.label }),
              /* @__PURE__ */ (0, import_jsx_runtime31.jsxs)("div", { children: [
                formatCount(row.count),
                " sessions"
              ] })
            ] });
          }
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime31.jsx)(Bar, { dataKey: "count", fill: theme.series[0], radius: [4, 4, 0, 0], maxBarSize: 44, isAnimationActive: false })
    ] });
  }

  // src/ui/page/RhythmTab.tsx
  var import_jsx_runtime32 = __toESM(require_jsx_runtime(), 1);
  var DAYS2 = ["Mon", "Tue", "Wed", "Thu", "Fri", "Sat", "Sun"];
  var hh2 = (h) => `${String(h).padStart(2, "0")}:00`;
  function RhythmTab({ engine, rangeDays }) {
    const theme = (0, import_react81.useMemo)(readChartTheme, []);
    const data = useRollups(engine);
    const [width, measureRef] = useElementWidth();
    const chartWidth = Math.max(320, width - 32);
    const today = dateKey(Date.now());
    const from2 = rangeDays === null ? null : shiftDate(today, -(rangeDays - 1));
    const rangeHourly = (0, import_react81.useMemo)(() => from2 ? data.hourly.filter((r2) => r2.date >= from2) : data.hourly, [data.hourly, from2]);
    const rangeSessions = (0, import_react81.useMemo)(() => from2 ? data.sessions.filter((r2) => r2.date >= from2) : data.sessions, [data.sessions, from2]);
    const profile = (0, import_react81.useMemo)(() => hourOfDayProfile(rangeHourly), [rangeHourly]);
    const grid = (0, import_react81.useMemo)(() => dayHourGrid(rangeHourly), [rangeHourly]);
    const nightPoints = (0, import_react81.useMemo)(() => nightOwlByMonth(rangeHourly), [rangeHourly]);
    const sessions = (0, import_react81.useMemo)(() => sessionHistogram(rangeSessions), [rangeSessions]);
    const monthsWithSignal = nightPoints.filter((p) => p.pct !== null).length;
    if (!data.loaded) return /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("p", { className: "retraced-note", children: "loading…" });
    return /* @__PURE__ */ (0, import_jsx_runtime32.jsxs)("div", { ref: measureRef, className: "retraced-overview", children: [
      data.dbMissing ? /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("p", { className: "retraced-banner", children: "Capture is unavailable, so charts have nothing to read — see the Overview tab for details." }) : null,
      /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(
        ChartCard,
        {
          title: "Around the clock",
          subtitle: "your messages by hour of day · the 22:00–06:00 night sector is shaded",
          empty: profile.total === 0 ? "A day or two of chatting will draw your daily shape here." : null,
          table: {
            columns: ["Hour", "Messages"],
            rows: profile.hours.map((count, h) => [hh2(h), count])
          },
          children: /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(RadialClock, { data: profile, theme, width: chartWidth })
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(
        ChartCard,
        {
          title: "Your week, hour by hour",
          subtitle: "messages by weekday and hour",
          empty: grid.total === 0 ? "Once messages span a few days, your weekly pattern shows up here." : null,
          table: {
            columns: ["Day", ...Array.from({ length: 24 }, (_, h) => String(h).padStart(2, "0"))],
            rows: grid.grid.map((row, i) => [DAYS2[i], ...row])
          },
          children: /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(DayHourHeatmap, { data: grid, theme, width: chartWidth })
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(
        ChartCard,
        {
          title: "Night owl by month",
          subtitle: "share of messages sent between 22:00 and 06:00",
          empty: monthsWithSignal < 2 ? "This line needs at least two months with real activity — check back later." : null,
          table: {
            columns: ["Month", "Night %", "Messages"],
            rows: nightPoints.map((p) => [p.month, p.pct === null ? "—" : `${p.pct}%`, p.total])
          },
          children: /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(NightOwlLine, { data: nightPoints, theme, width: chartWidth })
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(
        ChartCard,
        {
          title: "Session lengths",
          subtitle: `runs of messaging without a 30-minute break${sessions.medianBucket !== null ? ` · median ${SESSION_BUCKET_LABELS[sessions.medianBucket]}` : ""}`,
          empty: sessions.total === 0 ? "Sessions appear once a break separates two runs of messages — keep chatting." : null,
          table: {
            columns: ["Length", "Sessions"],
            rows: sessions.buckets.map((count, i) => [SESSION_BUCKET_LABELS[i], count])
          },
          children: /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(SessionLengths, { data: sessions, theme, width: chartWidth })
        }
      )
    ] });
  }

  // src/ui/page/VoiceTab.tsx
  var import_react83 = __toESM(require_react(), 1);

  // node_modules/d3-chord/src/math.js
  var abs2 = Math.abs;
  var cos2 = Math.cos;
  var sin2 = Math.sin;
  var pi4 = Math.PI;
  var halfPi2 = pi4 / 2;
  var tau4 = pi4 * 2;
  var max3 = Math.max;
  var epsilon4 = 1e-12;

  // node_modules/d3-chord/src/chord.js
  function range2(i, j) {
    return Array.from({ length: j - i }, (_, k) => i + k);
  }
  function compareValue(compare) {
    return function(a2, b) {
      return compare(
        a2.source.value + a2.target.value,
        b.source.value + b.target.value
      );
    };
  }
  function chord_default() {
    return chord(false, false);
  }
  function chord(directed, transpose) {
    var padAngle = 0, sortGroups = null, sortSubgroups = null, sortChords = null;
    function chord2(matrix) {
      var n = matrix.length, groupSums = new Array(n), groupIndex = range2(0, n), chords = new Array(n * n), groups = new Array(n), k = 0, dx;
      matrix = Float64Array.from({ length: n * n }, transpose ? (_, i) => matrix[i % n][i / n | 0] : (_, i) => matrix[i / n | 0][i % n]);
      for (let i = 0; i < n; ++i) {
        let x5 = 0;
        for (let j = 0; j < n; ++j) x5 += matrix[i * n + j] + directed * matrix[j * n + i];
        k += groupSums[i] = x5;
      }
      k = max3(0, tau4 - padAngle * n) / k;
      dx = k ? padAngle : tau4 / n;
      {
        let x5 = 0;
        if (sortGroups) groupIndex.sort((a2, b) => sortGroups(groupSums[a2], groupSums[b]));
        for (const i of groupIndex) {
          const x0 = x5;
          if (directed) {
            const subgroupIndex = range2(~n + 1, n).filter((j) => j < 0 ? matrix[~j * n + i] : matrix[i * n + j]);
            if (sortSubgroups) subgroupIndex.sort((a2, b) => sortSubgroups(a2 < 0 ? -matrix[~a2 * n + i] : matrix[i * n + a2], b < 0 ? -matrix[~b * n + i] : matrix[i * n + b]));
            for (const j of subgroupIndex) {
              if (j < 0) {
                const chord3 = chords[~j * n + i] || (chords[~j * n + i] = { source: null, target: null });
                chord3.target = { index: i, startAngle: x5, endAngle: x5 += matrix[~j * n + i] * k, value: matrix[~j * n + i] };
              } else {
                const chord3 = chords[i * n + j] || (chords[i * n + j] = { source: null, target: null });
                chord3.source = { index: i, startAngle: x5, endAngle: x5 += matrix[i * n + j] * k, value: matrix[i * n + j] };
              }
            }
            groups[i] = { index: i, startAngle: x0, endAngle: x5, value: groupSums[i] };
          } else {
            const subgroupIndex = range2(0, n).filter((j) => matrix[i * n + j] || matrix[j * n + i]);
            if (sortSubgroups) subgroupIndex.sort((a2, b) => sortSubgroups(matrix[i * n + a2], matrix[i * n + b]));
            for (const j of subgroupIndex) {
              let chord3;
              if (i < j) {
                chord3 = chords[i * n + j] || (chords[i * n + j] = { source: null, target: null });
                chord3.source = { index: i, startAngle: x5, endAngle: x5 += matrix[i * n + j] * k, value: matrix[i * n + j] };
              } else {
                chord3 = chords[j * n + i] || (chords[j * n + i] = { source: null, target: null });
                chord3.target = { index: i, startAngle: x5, endAngle: x5 += matrix[i * n + j] * k, value: matrix[i * n + j] };
                if (i === j) chord3.source = chord3.target;
              }
              if (chord3.source && chord3.target && chord3.source.value < chord3.target.value) {
                const source = chord3.source;
                chord3.source = chord3.target;
                chord3.target = source;
              }
            }
            groups[i] = { index: i, startAngle: x0, endAngle: x5, value: groupSums[i] };
          }
          x5 += dx;
        }
      }
      chords = Object.values(chords);
      chords.groups = groups;
      return sortChords ? chords.sort(sortChords) : chords;
    }
    chord2.padAngle = function(_) {
      return arguments.length ? (padAngle = max3(0, _), chord2) : padAngle;
    };
    chord2.sortGroups = function(_) {
      return arguments.length ? (sortGroups = _, chord2) : sortGroups;
    };
    chord2.sortSubgroups = function(_) {
      return arguments.length ? (sortSubgroups = _, chord2) : sortSubgroups;
    };
    chord2.sortChords = function(_) {
      return arguments.length ? (_ == null ? sortChords = null : (sortChords = compareValue(_))._ = _, chord2) : sortChords && sortChords._;
    };
    return chord2;
  }

  // node_modules/d3-chord/src/array.js
  var slice3 = Array.prototype.slice;

  // node_modules/d3-chord/src/constant.js
  function constant_default5(x5) {
    return function() {
      return x5;
    };
  }

  // node_modules/d3-chord/src/ribbon.js
  function defaultSource(d) {
    return d.source;
  }
  function defaultTarget(d) {
    return d.target;
  }
  function defaultRadius(d) {
    return d.radius;
  }
  function defaultStartAngle(d) {
    return d.startAngle;
  }
  function defaultEndAngle(d) {
    return d.endAngle;
  }
  function defaultPadAngle() {
    return 0;
  }
  function ribbon(headRadius) {
    var source = defaultSource, target = defaultTarget, sourceRadius = defaultRadius, targetRadius = defaultRadius, startAngle = defaultStartAngle, endAngle = defaultEndAngle, padAngle = defaultPadAngle, context = null;
    function ribbon2() {
      var buffer, s = source.apply(this, arguments), t = target.apply(this, arguments), ap = padAngle.apply(this, arguments) / 2, argv = slice3.call(arguments), sr = +sourceRadius.apply(this, (argv[0] = s, argv)), sa0 = startAngle.apply(this, argv) - halfPi2, sa1 = endAngle.apply(this, argv) - halfPi2, tr = +targetRadius.apply(this, (argv[0] = t, argv)), ta0 = startAngle.apply(this, argv) - halfPi2, ta1 = endAngle.apply(this, argv) - halfPi2;
      if (!context) context = buffer = path();
      if (ap > epsilon4) {
        if (abs2(sa1 - sa0) > ap * 2 + epsilon4) sa1 > sa0 ? (sa0 += ap, sa1 -= ap) : (sa0 -= ap, sa1 += ap);
        else sa0 = sa1 = (sa0 + sa1) / 2;
        if (abs2(ta1 - ta0) > ap * 2 + epsilon4) ta1 > ta0 ? (ta0 += ap, ta1 -= ap) : (ta0 -= ap, ta1 += ap);
        else ta0 = ta1 = (ta0 + ta1) / 2;
      }
      context.moveTo(sr * cos2(sa0), sr * sin2(sa0));
      context.arc(0, 0, sr, sa0, sa1);
      if (sa0 !== ta0 || sa1 !== ta1) {
        if (headRadius) {
          var hr = +headRadius.apply(this, arguments), tr2 = tr - hr, ta2 = (ta0 + ta1) / 2;
          context.quadraticCurveTo(0, 0, tr2 * cos2(ta0), tr2 * sin2(ta0));
          context.lineTo(tr * cos2(ta2), tr * sin2(ta2));
          context.lineTo(tr2 * cos2(ta1), tr2 * sin2(ta1));
        } else {
          context.quadraticCurveTo(0, 0, tr * cos2(ta0), tr * sin2(ta0));
          context.arc(0, 0, tr, ta0, ta1);
        }
      }
      context.quadraticCurveTo(0, 0, sr * cos2(sa0), sr * sin2(sa0));
      context.closePath();
      if (buffer) return context = null, buffer + "" || null;
    }
    if (headRadius) ribbon2.headRadius = function(_) {
      return arguments.length ? (headRadius = typeof _ === "function" ? _ : constant_default5(+_), ribbon2) : headRadius;
    };
    ribbon2.radius = function(_) {
      return arguments.length ? (sourceRadius = targetRadius = typeof _ === "function" ? _ : constant_default5(+_), ribbon2) : sourceRadius;
    };
    ribbon2.sourceRadius = function(_) {
      return arguments.length ? (sourceRadius = typeof _ === "function" ? _ : constant_default5(+_), ribbon2) : sourceRadius;
    };
    ribbon2.targetRadius = function(_) {
      return arguments.length ? (targetRadius = typeof _ === "function" ? _ : constant_default5(+_), ribbon2) : targetRadius;
    };
    ribbon2.startAngle = function(_) {
      return arguments.length ? (startAngle = typeof _ === "function" ? _ : constant_default5(+_), ribbon2) : startAngle;
    };
    ribbon2.endAngle = function(_) {
      return arguments.length ? (endAngle = typeof _ === "function" ? _ : constant_default5(+_), ribbon2) : endAngle;
    };
    ribbon2.padAngle = function(_) {
      return arguments.length ? (padAngle = typeof _ === "function" ? _ : constant_default5(+_), ribbon2) : padAngle;
    };
    ribbon2.source = function(_) {
      return arguments.length ? (source = _, ribbon2) : source;
    };
    ribbon2.target = function(_) {
      return arguments.length ? (target = _, ribbon2) : target;
    };
    ribbon2.context = function(_) {
      return arguments.length ? (context = _ == null ? null : _, ribbon2) : context;
    };
    return ribbon2;
  }
  function ribbon_default() {
    return ribbon();
  }

  // src/ui/charts/VoiceChordChart.tsx
  var import_react82 = __toESM(require_react(), 1);
  var import_jsx_runtime33 = __toESM(require_jsx_runtime(), 1);
  function VoiceChordChart({ data, theme, width }) {
    const [hover, setHover] = (0, import_react82.useState)(null);
    const height = 380;
    const cx = width / 2;
    const cy = height / 2;
    const outer = Math.min(cx, cy) - 34;
    const inner = outer - 12;
    const layout = (0, import_react82.useMemo)(() => chord_default().padAngle(0.06)(data.matrix), [data]);
    const arcGen = arc_default().innerRadius(inner).outerRadius(outer);
    const ribbonGen = ribbon_default().radius(inner - 3);
    const colorOf = (i) => i === 0 ? theme.ink.primary : colorForSlot(theme, i - 1);
    const dimmed = (a2, b) => hover !== null && a2 !== hover && b !== hover;
    return /* @__PURE__ */ (0, import_jsx_runtime33.jsx)("svg", { width, height, role: "img", "aria-label": "Shared voice time", children: /* @__PURE__ */ (0, import_jsx_runtime33.jsxs)("g", { transform: `translate(${cx},${cy})`, children: [
      layout.map((c2, i) => /* @__PURE__ */ (0, import_jsx_runtime33.jsx)(
        "path",
        {
          d: ribbonGen({ source: c2.source, target: c2.target }),
          fill: colorOf(c2.source.index),
          fillOpacity: dimmed(c2.source.index, c2.target.index) ? 0.06 : 0.55,
          stroke: theme.ink.card,
          strokeWidth: 1,
          children: /* @__PURE__ */ (0, import_jsx_runtime33.jsx)("title", { children: `${data.names[c2.source.index]} ↔ ${data.names[c2.target.index]} — ${formatMinutes(c2.source.value)} shared` })
        },
        i
      )),
      layout.groups.map((g) => {
        const mid = (g.startAngle + g.endAngle) / 2;
        const labelR = outer + 10;
        const x5 = Math.sin(mid) * labelR;
        const y5 = -Math.cos(mid) * labelR;
        return /* @__PURE__ */ (0, import_jsx_runtime33.jsxs)("g", { onMouseEnter: () => setHover(g.index), onMouseLeave: () => setHover(null), children: [
          /* @__PURE__ */ (0, import_jsx_runtime33.jsx)(
            "path",
            {
              d: arcGen({ startAngle: g.startAngle, endAngle: g.endAngle }),
              fill: colorOf(g.index),
              fillOpacity: hover === null || hover === g.index ? 1 : 0.3,
              stroke: theme.ink.card,
              strokeWidth: 1.5
            }
          ),
          /* @__PURE__ */ (0, import_jsx_runtime33.jsx)(
            "text",
            {
              x: x5,
              y: y5 + 3,
              textAnchor: mid > Math.PI ? "end" : "start",
              className: "retraced-svg-label",
              fill: hover === g.index ? theme.ink.primary : theme.ink.muted,
              fontWeight: g.index === 0 ? 600 : 400,
              children: data.names[g.index]
            }
          )
        ] }, g.index);
      })
    ] }) });
  }

  // src/ui/charts/VoiceMinutesChart.tsx
  var import_jsx_runtime34 = __toESM(require_jsx_runtime(), 1);
  function VoiceMinutesChart({ data, theme, width }) {
    const weeks = data.map((w) => w.week);
    const ticks = monthTicks(weeks);
    const spansYears = weeks.length > 0 && weeks[0].slice(0, 4) !== weeks[weeks.length - 1].slice(0, 4);
    return /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)(AreaChart, { width, height: 220, data, margin: { top: 8, right: 8, bottom: 0, left: 0 }, children: [
      /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(CartesianGrid, { vertical: false, stroke: theme.ink.grid }),
      /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(
        XAxis,
        {
          dataKey: "week",
          ticks,
          tickFormatter: (d) => tickLabel(d, spansYears),
          tick: { fill: theme.ink.muted, fontSize: 11 },
          axisLine: { stroke: theme.ink.axis },
          tickLine: false,
          interval: "preserveStartEnd"
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(YAxis, { width: 40, tickCount: 4, allowDecimals: false, tick: { fill: theme.ink.muted, fontSize: 11 }, axisLine: false, tickLine: false }),
      /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(
        Tooltip,
        {
          cursor: { stroke: theme.ink.axis },
          content: ({ active, payload, label }) => {
            if (!active || !payload?.length) return null;
            const week = payload[0].payload;
            return /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)("div", { className: "retraced-tooltip", children: [
              /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)("div", { className: "retraced-tooltip-title", children: [
                "Week of ",
                label
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)("div", { children: [
                formatMinutes(week.minutes),
                " in voice"
              ] })
            ] });
          }
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(Area, { type: "monotone", dataKey: "minutes", stroke: theme.series[0], strokeWidth: 2, fill: theme.series[0], fillOpacity: 0.16, isAnimationActive: false, activeDot: { r: 3 } })
    ] });
  }

  // src/ui/data/voicetab.ts
  var MAX_WEEKS3 = 800;
  function voiceMinutesWeekly(rows) {
    const byWeek = /* @__PURE__ */ new Map();
    for (const row of rows) {
      if (row.seconds <= 0) continue;
      const week = mondayOf(row.date);
      byWeek.set(week, (byWeek.get(week) ?? 0) + row.seconds);
    }
    if (byWeek.size === 0) return [];
    const weeks = [...byWeek.keys()].sort();
    const last = weeks[weeks.length - 1];
    const out = [];
    let cursor = weeks[0];
    for (let i = 0; i < MAX_WEEKS3; i++) {
      out.push({ week: cursor, minutes: Math.round((byWeek.get(cursor) ?? 0) / 60) });
      if (cursor === last) break;
      cursor = shiftDate(cursor, 7);
    }
    return out;
  }
  function voiceChord(rows, peers, opts = {}) {
    const max4 = opts.max ?? 8;
    const withYou = /* @__PURE__ */ new Map();
    for (const row of rows) {
      for (const [userId, seconds2] of Object.entries(row.coPresent)) {
        withYou.set(userId, (withYou.get(userId) ?? 0) + seconds2);
      }
    }
    if (withYou.size === 0) return null;
    const kept = [...withYou.entries()].sort((a2, b) => b[1] - a2[1]).slice(0, max4);
    const indexOf = new Map(kept.map(([userId], i) => [userId, i + 1]));
    const labelOf2 = new Map(peers.map((p) => [p.userId, p.label ?? p.userId]));
    const n = kept.length + 1;
    const matrix = Array.from({ length: n }, () => new Array(n).fill(0));
    for (const [userId, seconds2] of kept) {
      const i = indexOf.get(userId);
      matrix[0][i] = Math.round(seconds2 / 60);
      matrix[i][0] = matrix[0][i];
    }
    for (const row of rows) {
      const present = Object.entries(row.coPresent).filter(([userId]) => indexOf.has(userId));
      for (let a2 = 0; a2 < present.length; a2++) {
        for (let b = a2 + 1; b < present.length; b++) {
          const [ua, sa] = present[a2];
          const [ub, sb] = present[b];
          const [i, j] = [indexOf.get(ua), indexOf.get(ub)];
          const minutes = Math.round(Math.min(sa, sb) / 60);
          matrix[i][j] = (matrix[i][j] ?? 0) + minutes;
          matrix[j][i] = matrix[i][j];
        }
      }
    }
    return {
      names: ["you", ...kept.map(([userId]) => labelOf2.get(userId) ?? userId)],
      matrix
    };
  }
  function topVcChannels(rows, opts) {
    const byChannel = /* @__PURE__ */ new Map();
    for (const row of rows) {
      if (row.seconds <= 0) continue;
      byChannel.set(row.channelId, (byChannel.get(row.channelId) ?? 0) + row.seconds);
    }
    return [...byChannel.entries()].sort((a2, b) => b[1] - a2[1]).slice(0, opts.max ?? 10).map(([channelId, seconds2]) => ({ channelId, label: opts.channelLabel(channelId), minutes: Math.round(seconds2 / 60) }));
  }

  // src/ui/page/VoiceTab.tsx
  var import_jsx_runtime35 = __toESM(require_jsx_runtime(), 1);
  function VoiceTab({ engine, rangeDays }) {
    const theme = (0, import_react83.useMemo)(readChartTheme, []);
    const labels = (0, import_react83.useMemo)(createLabelResolvers, []);
    const data = useRollups(engine);
    const [width, measureRef] = useElementWidth();
    const chartWidth = Math.max(320, width - 32);
    const today = dateKey(Date.now());
    const from2 = rangeDays === null ? null : shiftDate(today, -(rangeDays - 1));
    const rangeVoice = (0, import_react83.useMemo)(() => from2 ? data.voice.filter((r2) => r2.date >= from2) : data.voice, [data.voice, from2]);
    const weekly = (0, import_react83.useMemo)(() => voiceMinutesWeekly(rangeVoice), [rangeVoice]);
    const chord2 = (0, import_react83.useMemo)(() => voiceChord(rangeVoice, data.peers), [rangeVoice, data.peers]);
    const channels = (0, import_react83.useMemo)(() => topVcChannels(rangeVoice, { channelLabel: labels.channelLabel }), [rangeVoice, labels]);
    if (!data.loaded) return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("p", { className: "retraced-note", children: "loading…" });
    return /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)("div", { ref: measureRef, className: "retraced-overview", children: [
      data.dbMissing ? /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("p", { className: "retraced-banner", children: "Capture is unavailable, so charts have nothing to read — see the Overview tab for details." }) : null,
      /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(
        ChartCard,
        {
          title: "Time in voice",
          subtitle: "minutes per week in voice channels",
          empty: weekly.length === 0 ? "Join a voice channel and your minutes start counting here." : null,
          table: { columns: ["Week", "Minutes"], rows: weekly.map((w) => [w.week, w.minutes]) },
          children: /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(VoiceMinutesChart, { data: weekly, theme, width: chartWidth })
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(
        ChartCard,
        {
          title: "Who you share voice with",
          subtitle: "shared minutes between you and your circle — hover a name to isolate them",
          empty: chord2 === null ? "Spend some voice time with people and the shared-minutes ring appears here." : null,
          table: chord2 ? {
            columns: ["Person", "Person", "Shared"],
            rows: chord2.matrix.flatMap(
              (row, i) => row.map((minutes, j) => ({ minutes, j })).filter(({ minutes, j }) => j > i && minutes > 0).map(({ minutes, j }) => [chord2.names[i], chord2.names[j], formatMinutes(minutes)])
            )
          } : null,
          children: chord2 ? /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(VoiceChordChart, { data: chord2, theme, width: chartWidth }) : null
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(
        ChartCard,
        {
          title: "Top voice channels",
          subtitle: "total time per channel",
          empty: channels.length === 0 ? "Voice channels you spend time in rank themselves here." : null,
          table: { columns: ["Channel", "Time"], rows: channels.map((c2) => [c2.label, formatMinutes(c2.minutes)]) },
          children: /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(
            BarRowList,
            {
              rows: channels.map((c2) => ({ key: c2.channelId, label: c2.label, value: c2.minutes, valueLabel: formatMinutes(c2.minutes) })),
              color: theme.series[0],
              theme
            }
          )
        }
      )
    ] });
  }

  // src/ui/page/StatsPage.tsx
  var import_jsx_runtime36 = __toESM(require_jsx_runtime(), 1);
  var TABS = [
    ["overview", "Overview"],
    ["rhythm", "Rhythm"],
    ["hesitation", "Hesitation"],
    ["people", "People"],
    ["content", "Content"],
    ["voice", "Voice"],
    ["data", "Data"]
  ];
  var RANGES = [
    { label: "30d", days: 30 },
    { label: "90d", days: 90 },
    { label: "1y", days: 365 },
    { label: "All", days: null }
  ];
  function StatsPage({
    variant,
    onClose,
    engine
  }) {
    const [tab, setTab] = (0, import_react84.useState)("overview");
    const [rangeDays, setRangeDays] = (0, import_react84.useState)(90);
    return /* @__PURE__ */ (0, import_jsx_runtime36.jsxs)("div", { className: `retraced-page retraced-page--${variant}`, children: [
      /* @__PURE__ */ (0, import_jsx_runtime36.jsxs)("header", { className: "retraced-header", children: [
        /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("h1", { className: "retraced-title", children: "Retraced" }),
        /* @__PURE__ */ (0, import_jsx_runtime36.jsxs)("span", { className: "retraced-version", children: [
          "v",
          plugin_meta_default.version
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("div", { className: "retraced-ranges", role: "group", "aria-label": "Time range", children: RANGES.map((range3) => /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(
          "button",
          {
            type: "button",
            className: "retraced-range-pill",
            "aria-pressed": rangeDays === range3.days,
            onClick: () => setRangeDays(range3.days),
            children: range3.label
          },
          range3.label
        )) }),
        onClose ? /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("button", { type: "button", className: "retraced-close", "aria-label": "Close", onClick: onClose, children: "✕" }) : null
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("nav", { className: "retraced-tabs", role: "tablist", "aria-label": "Statistics sections", children: TABS.map(([id, label]) => /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("button", { type: "button", role: "tab", "aria-selected": tab === id, className: "retraced-tab", onClick: () => setTab(id), children: label }, id)) }),
      /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("main", { className: "retraced-body", children: /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("div", { className: "retraced-content", children: tab === "overview" ? /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(OverviewTab, { engine, rangeDays }) : tab === "rhythm" ? /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(RhythmTab, { engine, rangeDays }) : tab === "hesitation" ? /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(HesitationTab, { engine, rangeDays }) : tab === "people" ? /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(PeopleTab, { engine, rangeDays }) : tab === "content" ? /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(ContentTab, { engine, rangeDays }) : tab === "voice" ? /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(VoiceTab, { engine, rangeDays }) : /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(DataTab, { engine, rangeDays }) }) })
    ] });
  }

  // src/ui/styles.ts
  var STYLE_ID = "retraced-styles";
  var css = `
.retraced-overlay {
  position: fixed;
  inset: 0;
  z-index: 10000;
  display: flex;
  background: var(--background-primary, #313338);
  color: var(--text-normal, #dbdee1);
}

.retraced-page {
  display: flex;
  flex-direction: column;
  flex: 1 1 auto;
  min-height: 0;
  min-width: 0;
  font-family: var(--font-primary, "gg sans", "Noto Sans", "Helvetica Neue", sans-serif);
}

.retraced-page--settings {
  height: 100%;
}

.retraced-header {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 16px 24px;
  border-bottom: 1px solid var(--background-modifier-accent, rgba(255, 255, 255, 0.06));
  flex: 0 0 auto;
}

.retraced-title {
  margin: 0;
  font-size: 20px;
  font-weight: 700;
  color: var(--header-primary, #f2f3f5);
}

.retraced-version {
  font-size: 12px;
  color: var(--text-muted, #949ba4);
}

.retraced-close {
  margin-left: auto;
  border: none;
  background: transparent;
  color: var(--interactive-normal, #b5bac1);
  font-size: 18px;
  line-height: 1;
  padding: 6px 10px;
  border-radius: 4px;
  cursor: pointer;
}

.retraced-close:hover {
  color: var(--interactive-hover, #dbdee1);
  background: var(--background-modifier-hover, rgba(255, 255, 255, 0.04));
}

.retraced-tabs {
  display: flex;
  gap: 4px;
  padding: 8px 24px 0;
  border-bottom: 1px solid var(--background-modifier-accent, rgba(255, 255, 255, 0.06));
  flex: 0 0 auto;
}

.retraced-tab {
  border: none;
  background: none;
  color: var(--interactive-normal, #b5bac1);
  font-size: 14px;
  padding: 8px 12px;
  border-radius: 4px 4px 0 0;
  cursor: pointer;
}

.retraced-tab:hover {
  color: var(--interactive-hover, #dbdee1);
  background: var(--background-modifier-hover, rgba(255, 255, 255, 0.04));
}

.retraced-tab[aria-selected="true"] {
  color: var(--interactive-active, #ffffff);
  box-shadow: inset 0 -2px 0 var(--brand-500, var(--brand-experiment, #5865f2));
}

.retraced-body {
  flex: 1 1 auto;
  overflow-y: auto;
  padding: 24px;
}

.retraced-content {
  max-width: 1040px;
  margin: 0 auto;
  width: 100%;
}

.retraced-overview {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.retraced-banner {
  margin: 0;
  padding: 10px 14px;
  border-radius: 6px;
  font-size: 13px;
  color: var(--text-normal, #dbdee1);
  background: var(--background-modifier-hover, rgba(255, 255, 255, 0.04));
  border-left: 3px solid var(--status-danger, #f23f43);
}

.retraced-ranges {
  display: flex;
  gap: 2px;
  margin-left: 16px;
  padding: 2px;
  border-radius: 6px;
  background: var(--background-secondary, #2b2d31);
}

.retraced-range-pill {
  border: none;
  background: transparent;
  color: var(--interactive-normal, #b5bac1);
  font-size: 12px;
  font-weight: 600;
  padding: 4px 10px;
  border-radius: 4px;
  cursor: pointer;
}

.retraced-range-pill:hover {
  color: var(--interactive-hover, #dbdee1);
}

.retraced-range-pill[aria-pressed="true"] {
  color: var(--interactive-active, #ffffff);
  background: var(--background-modifier-selected, rgba(255, 255, 255, 0.12));
}

.retraced-banner--info {
  border-left-color: var(--brand-500, var(--brand-experiment, #5865f2));
}

.retraced-confirm {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  flex-wrap: wrap;
  border-left-color: var(--status-warning, #f0b232);
}

.retraced-confirm-buttons {
  display: flex;
  gap: 8px;
  flex: 0 0 auto;
}

.retraced-note.retraced-note--nudge {
  color: var(--status-warning, #f0b232);
}

.retraced-input--date {
  flex: 0 0 auto;
  width: 150px;
}

.retraced-ghost-button--danger {
  color: var(--status-danger, #f23f43);
  border-color: color-mix(in srgb, var(--status-danger, #f23f43) 40%, transparent);
}

.retraced-ghost-button--danger:hover {
  color: var(--status-danger, #f23f43);
  background: color-mix(in srgb, var(--status-danger, #f23f43) 12%, transparent);
}

.retraced-chart-card {
  padding: 16px;
  border-radius: 8px;
  background: var(--background-secondary, #2b2d31);
}

.retraced-chart-head {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 12px;
  margin-bottom: 8px;
}

.retraced-chart-title {
  margin: 0 0 2px;
  font-size: 15px;
  font-weight: 700;
  color: var(--header-primary, #f2f3f5);
}

.retraced-ghost-button {
  border: 1px solid var(--background-modifier-accent, rgba(255, 255, 255, 0.08));
  background: transparent;
  color: var(--interactive-normal, #b5bac1);
  font-size: 12px;
  padding: 4px 10px;
  border-radius: 4px;
  cursor: pointer;
  flex: 0 0 auto;
}

.retraced-ghost-button:hover {
  color: var(--interactive-hover, #dbdee1);
  background: var(--background-modifier-hover, rgba(255, 255, 255, 0.04));
}

.retraced-card-actions {
  display: flex;
  align-items: center;
  gap: 8px;
  flex: 0 0 auto;
}

.retraced-chart-card .retraced-ranges {
  margin-left: 0;
  background: var(--background-primary, #313338);
}

.retraced-select {
  border: 1px solid var(--background-modifier-accent, rgba(255, 255, 255, 0.08));
  background: var(--background-primary, #313338);
  color: var(--text-normal, #dbdee1);
  font-size: 12px;
  padding: 4px 8px;
  border-radius: 4px;
  max-width: 180px;
}

.retraced-slider {
  display: flex;
  align-items: center;
  gap: 8px;
}

.retraced-slider input[type="range"] {
  width: 140px;
  accent-color: var(--brand-500, var(--brand-experiment, #5865f2));
}

.retraced-barlist {
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.retraced-barlist-row {
  display: grid;
  grid-template-columns: minmax(120px, 180px) 1fr 64px;
  align-items: center;
  gap: 10px;
}

.retraced-barlist-label {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  font-size: 12px;
  color: var(--text-muted, #949ba4);
  min-width: 0;
}

.retraced-barlist-text {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.retraced-barlist-track {
  height: 14px;
  border-radius: 4px;
  overflow: hidden;
}

.retraced-barlist-bar {
  display: block;
  height: 100%;
  border-radius: 4px;
}

.retraced-barlist-value {
  font-size: 12px;
  font-variant-numeric: tabular-nums;
  text-align: right;
}

.retraced-emoji {
  width: 20px;
  height: 20px;
  flex: 0 0 auto;
}

.retraced-donut-center {
  position: absolute;
  inset: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  pointer-events: none;
  gap: 2px;
}

.retraced-donut-total {
  font-size: 28px;
  font-weight: 700;
  color: var(--header-primary, #f2f3f5);
}

.retraced-words-columns {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20px;
}

.retraced-words-columns > div {
  min-width: 0;
}

.retraced-words-head {
  margin: 0 0 8px;
  font-weight: 600;
}

.retraced-search-controls {
  display: flex;
  gap: 8px;
  margin: 4px 0 12px;
}

.retraced-input {
  flex: 1 1 auto;
  padding: 8px 10px;
  border: none;
  border-radius: 4px;
  background: var(--background-primary, #313338);
  color: var(--text-normal, #dbdee1);
  font-size: 13px;
}

.retraced-search-hint {
  margin: 4px 0;
}

.retraced-search-results {
  list-style: none;
  margin: 0;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 10px;
  max-height: 420px;
  overflow-y: auto;
}

.retraced-search-hit {
  padding: 8px 10px;
  border-radius: 6px;
  background: var(--background-primary, #313338);
}

.retraced-search-meta {
  display: flex;
  align-items: center;
  gap: 6px;
  font-size: 11px;
  color: var(--text-muted, #949ba4);
  margin-bottom: 3px;
  flex-wrap: wrap;
}

.retraced-search-content {
  font-size: 13px;
  color: var(--text-normal, #dbdee1);
  word-break: break-word;
}

.retraced-search-content mark {
  background: color-mix(in srgb, var(--brand-500, #5865f2) 35%, transparent);
  color: inherit;
  border-radius: 2px;
  padding: 0 1px;
}

.retraced-chip {
  font-size: 10px;
  font-weight: 600;
  text-transform: uppercase;
  padding: 1px 6px;
  border-radius: 8px;
  background: var(--background-modifier-hover, rgba(255, 255, 255, 0.06));
}

.retraced-hero-block {
  display: flex;
  flex-direction: column;
  gap: 2px;
  margin: 4px 0 14px;
}

.retraced-hero-figure {
  font-size: 32px;
  font-weight: 700;
  line-height: 1.1;
  color: var(--header-primary, #f2f3f5);
}

.retraced-legend {
  display: flex;
  flex-wrap: wrap;
  gap: 4px 12px;
  margin: 4px 0 10px;
}

.retraced-legend-pill {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  font-size: 12px;
  color: var(--text-muted, #949ba4);
}

.retraced-legend-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  flex: 0 0 auto;
}

.retraced-empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4px;
  padding: 40px 16px;
  text-align: center;
}

.retraced-empty-title {
  color: var(--header-primary, #f2f3f5);
  font-weight: 600;
}

.retraced-cards {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
  gap: 12px;
}

.retraced-card {
  display: flex;
  flex-direction: column;
  gap: 2px;
  padding: 14px 16px;
  border-radius: 8px;
  background: var(--background-secondary, #2b2d31);
  min-width: 0;
}

.retraced-card-label {
  font-size: 11px;
  font-weight: 700;
  letter-spacing: 0.04em;
  text-transform: uppercase;
  color: var(--text-muted, #949ba4);
}

.retraced-card-value {
  font-size: 24px;
  font-weight: 700;
  color: var(--header-primary, #f2f3f5);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.retraced-tooltip {
  background: var(--background-floating, #111214);
  color: var(--text-normal, #dbdee1);
  border-radius: 6px;
  padding: 8px 10px;
  font-size: 12px;
  box-shadow: 0 8px 16px rgba(0, 0, 0, 0.24);
  pointer-events: none;
  max-width: 260px;
}

.retraced-tooltip-title {
  font-weight: 700;
  color: var(--header-primary, #f2f3f5);
  margin-bottom: 2px;
}

.retraced-tooltip-row {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 1px 0;
}

.retraced-tooltip-value {
  margin-left: auto;
  font-variant-numeric: tabular-nums;
}

.retraced-tooltip--floating {
  position: absolute;
  transform: translate(-50%, -100%);
  z-index: 10;
}

.retraced-table-wrap {
  max-height: 320px;
  overflow: auto;
  border: 1px solid var(--background-modifier-accent, rgba(255, 255, 255, 0.06));
  border-radius: 6px;
}

.retraced-table {
  width: 100%;
  border-collapse: collapse;
  font-size: 12px;
}

.retraced-table th,
.retraced-table td {
  text-align: left;
  padding: 6px 10px;
  border-bottom: 1px solid var(--background-modifier-accent, rgba(255, 255, 255, 0.06));
  font-variant-numeric: tabular-nums;
}

.retraced-table th {
  position: sticky;
  top: 0;
  background: var(--background-secondary, #2b2d31);
  color: var(--text-muted, #949ba4);
  font-weight: 600;
}

.retraced-calendar {
  display: flex;
  flex-direction: column;
  gap: 12px;
  overflow-x: auto;
  padding-bottom: 4px;
}

.retraced-calendar-legend {
  align-items: center;
  margin: 0;
}

.retraced-svg-label {
  font-size: 10px;
  font-family: var(--font-primary, "gg sans", "Noto Sans", sans-serif);
}

.retraced-muted {
  color: var(--text-muted, #949ba4);
}

.retraced-settings-panel {
  display: flex;
  flex-direction: column;
  gap: 16px;
  color: var(--text-normal, #dbdee1);
}

.retraced-settings-open {
  display: flex;
  align-items: center;
  gap: 12px;
  flex-wrap: wrap;
}

.retraced-button {
  border: none;
  border-radius: 4px;
  padding: 8px 16px;
  font-size: 14px;
  cursor: pointer;
  color: #ffffff;
  background: var(--brand-500, var(--brand-experiment, #5865f2));
}

.retraced-button:hover {
  filter: brightness(1.1);
}

.retraced-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  padding: 8px 0;
  border-bottom: 1px solid var(--background-modifier-accent, rgba(255, 255, 255, 0.06));
}

.retraced-row-text {
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.retraced-row-label {
  color: var(--header-primary, #f2f3f5);
  font-size: 15px;
}

.retraced-note {
  color: var(--text-muted, #949ba4);
  font-size: 12px;
}

.retraced-row--stacked {
  flex-direction: column;
  align-items: stretch;
}

.retraced-textarea {
  width: 100%;
  padding: 8px;
  border: none;
  border-radius: 4px;
  background: var(--background-secondary, #2b2d31);
  color: var(--text-normal, #dbdee1);
  font-family: inherit;
  font-size: 12px;
  resize: vertical;
}

.retraced-row input[type="number"] {
  width: 80px;
  padding: 6px 8px;
  border: none;
  border-radius: 4px;
  background: var(--background-secondary, #2b2d31);
  color: var(--text-normal, #dbdee1);
}

.retraced-row input[type="checkbox"] {
  width: 20px;
  height: 20px;
  accent-color: var(--brand-500, var(--brand-experiment, #5865f2));
}

.retraced-readout {
  margin-top: 24px;
  padding: 16px;
  border-radius: 8px;
  background: var(--background-secondary, #2b2d31);
  max-width: 720px;
}

.retraced-readout h3 {
  margin: 0 0 8px;
  color: var(--header-primary, #f2f3f5);
  font-size: 16px;
  display: flex;
  align-items: center;
  gap: 8px;
}

.retraced-status {
  font-size: 11px;
  font-weight: 600;
  text-transform: uppercase;
  padding: 2px 8px;
  border-radius: 10px;
  background: var(--background-modifier-hover, rgba(255, 255, 255, 0.06));
  color: var(--text-muted, #949ba4);
}

.retraced-status--running {
  background: color-mix(in srgb, var(--status-positive, #23a559) 25%, transparent);
  color: var(--status-positive, #23a559);
}

.retraced-status--degraded,
.retraced-status--unavailable {
  background: color-mix(in srgb, var(--status-danger, #f23f43) 20%, transparent);
  color: var(--status-danger, #f23f43);
}

.retraced-counter-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(110px, 1fr));
  gap: 8px;
  margin: 12px 0;
}

.retraced-counter {
  display: flex;
  flex-direction: column;
  padding: 8px 10px;
  border-radius: 6px;
  background: var(--background-primary, #313338);
}

.retraced-counter-value {
  font-size: 20px;
  font-weight: 700;
  color: var(--header-primary, #f2f3f5);
  font-variant-numeric: tabular-nums;
}

.retraced-devtools {
  margin-top: 12px;
}

.retraced-devtools-row {
  display: flex;
  align-items: center;
  gap: 8px;
  flex-wrap: wrap;
  margin: 8px 0;
}

.retraced-button--danger {
  background: var(--status-danger, #f23f43);
}

.retraced-button:disabled {
  opacity: 0.5;
  cursor: default;
}
`;

  // src/index.ts
  var Retraced = class {
    disposer = null;
    overlay = null;
    engine = null;
    settings = new SettingsStore();
    constructor(_bdMeta) {
    }
    start() {
      if (this.disposer) return;
      const d = this.disposer = new Disposer();
      try {
        this.settings.load();
      } catch (e) {
        warn("failed to load settings — using defaults", e);
      }
      bd().DOM.addStyle(STYLE_ID, css);
      d.add(() => bd().DOM.removeStyle(STYLE_ID));
      const engine = this.engine = new CaptureEngine({
        hooks: createDiscordHooks(),
        settings: () => this.settings.get()
      });
      void engine.start().catch((e) => warn("capture failed to start", e));
      d.add(() => {
        void engine.stop().catch((e) => warn("capture stop failed", e));
      });
      let contentWasEnabled = this.settings.get().contentStorageEnabled;
      d.add(
        this.settings.subscribe((s) => {
          if (contentWasEnabled && !s.contentStorageEnabled) {
            void engine.purgeContent().catch((e) => warn("content purge failed", e));
          }
          contentWasEnabled = s.contentStorageEnabled;
        })
      );
      const overlay = this.overlay = new OverlayController(
        (close) => (0, import_react85.createElement)(StatsPage, { variant: "overlay", engine, onClose: close })
      );
      d.add(() => overlay.close());
      document.addEventListener("keydown", this.onHotkey, true);
      d.add(() => document.removeEventListener("keydown", this.onHotkey, true));
      try {
        injectSettingsSection(() => (0, import_react85.createElement)(StatsPage, { variant: "settings", engine }));
      } catch (e) {
        warn("settings sidebar injection failed — the page is still available via Ctrl+Shift+H", e);
      }
      d.add(() => {
        try {
          bd().Patcher.unpatchAll(PLUGIN_ID);
        } catch {
        }
      });
      log(`v${plugin_meta_default.version} started — press Ctrl+Shift+H to open the stats page`);
    }
    stop() {
      if (!this.disposer) return;
      this.disposer.dispose();
      this.disposer = null;
      this.overlay = null;
      this.engine = null;
      log("stopped");
    }
    getSettingsPanel() {
      return (0, import_react85.createElement)(SettingsPanel, {
        store: this.settings,
        onOpenStats: () => this.overlay?.open()
      });
    }
    onHotkey = (e) => {
      if (e.ctrlKey && e.shiftKey && !e.altKey && !e.metaKey && e.code === "KeyH") {
        e.preventDefault();
        e.stopPropagation();
        this.overlay?.toggle();
      }
    };
  };
  return __toCommonJS(index_exports);
})();
module.exports = __RetracedExports.default;
